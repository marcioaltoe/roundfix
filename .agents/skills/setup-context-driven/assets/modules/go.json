{
  "schemaVersion": "setup-context-driven/module-v2", "id": "go", "version": 2, "kind": "language-runtime", "title": "Go repository guidance",
  "dependsOn": ["core"], "conflictsWith": [],
  "requiredSkills": [
    "golang-cli",
    "golang-concurrency",
    "golang-context",
    "golang-error-handling",
    "golang-lint",
    "golang-testing"
  ],
  "skillDispatch": [
    {
      "skill": "golang-cli",
      "triggers": [
        {
          "id": "trigger.go.golang-cli",
          "when": "Changing Go command structure, flags, package layout, version output, or command tests."
        }
      ]
    },
    {
      "skill": "golang-concurrency",
      "triggers": [
        {
          "id": "trigger.go.golang-concurrency",
          "when": "Using goroutines, channels, locks, worker pools, or other concurrent Go behavior."
        }
      ]
    },
    {
      "skill": "golang-context",
      "triggers": [
        {
          "id": "trigger.go.golang-context",
          "when": "Designing Go IO boundaries, cancellation, timeouts, deadlines, or request-scoped values."
        }
      ]
    },
    {
      "skill": "golang-error-handling",
      "triggers": [
        {
          "id": "trigger.go.golang-error-handling",
          "when": "Creating, wrapping, matching, logging, or exposing Go errors."
        }
      ]
    },
    {
      "skill": "golang-lint",
      "triggers": [
        {
          "id": "trigger.go.golang-lint",
          "when": "Changing Go lint configuration or adding a lint suppression."
        }
      ]
    },
    {
      "skill": "golang-testing",
      "triggers": [
        {
          "id": "trigger.go.golang-testing",
          "when": "Writing or reviewing Go tests, fixtures, fuzz tests, or integration tests."
        }
      ]
    }
  ],
  "requiredDecisions": [],
  "rootBlocks": [{"id": "root.go", "version": 2, "template": "template.root.go", "rules": [], "references": [
    {"id": "reference.root.go", "token": "reference.go", "ownership": "setup", "managedId": "guide.go"}
  ]}],
  "supportingGuides": [{"id": "guide.go", "version": 2, "path": "docs/agents/go.md", "template": "template.guide.go", "rules": ["rule.go.stdlib-first", "rule.go.context-errors", "rule.go.observable-tests"], "references": []}],
  "rules": [
    {"id": "rule.go.stdlib-first", "version": 2, "coverage": ["coverage.go", "coverage.dependencies"], "guidance": "Keep Go command entry points thin and behavior in cohesive packages. Prefer the standard library; add a dependency only for a named job it cannot perform, and change `go.mod` and `go.sum` through Go tooling rather than hand edits."},
    {"id": "rule.go.context-errors", "version": 2, "coverage": ["coverage.go", "coverage.backend"], "guidance": "Use context-first signatures for blocking and IO work. Give every goroutine an owner and cancellation path. Wrap errors with the failed operation using `%w`, and preserve `errors.Is` or `errors.As` matching where callers need it."},
    {"id": "rule.go.observable-tests", "version": 2, "coverage": ["coverage.go", "coverage.verification-integrity"], "guidance": "Use stdlib `testing`. Test observable package and CLI behavior through public runners, including stdout, stderr, files, exit codes, cancellation, and failure paths; do not add production-only test hooks."}
  ]
}
