# Spec docs layout

Use `docs/specs/<feature-slug>/` for local Specs. Keep `_idea.md`, `_prd.md`,
`_techspec.md`, `_tasks.md`, Task files, and `qa/` evidence under that feature
folder. Archive completed Specs under `docs/specs/_archived/`.
