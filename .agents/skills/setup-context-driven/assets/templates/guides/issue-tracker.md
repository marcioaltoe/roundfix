# Issue tracker

Specs are local files under `docs/specs/<feature-slug>/`. The Task Graph lives
in `_tasks.md`; dependencies live only there. Each Task owns its own status in
frontmatter.

Do not track Task progress in `_tasks.md`. Do not edit another Task file while
executing one assigned Task.
