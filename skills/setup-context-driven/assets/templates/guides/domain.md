# Domain docs

Read `CONTEXT.md` before naming project concepts in code, tests, Specs, or
user-facing text. If `CONTEXT-MAP.md` exists, read the mapped context that owns
the files being changed.

Read relevant ADRs before changing behavior. If a change contradicts an ADR,
flag the conflict before implementing it.
