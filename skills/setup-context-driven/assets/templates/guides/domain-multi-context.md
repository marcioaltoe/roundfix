# Domain docs

Read `CONTEXT-MAP.md` first, then read the mapped `CONTEXT.md` that owns the
files being changed before naming project concepts in code, tests, Specs, or
user-facing text.

Read relevant ADRs before changing behavior. If a change contradicts an ADR,
flag the conflict before implementing it.
