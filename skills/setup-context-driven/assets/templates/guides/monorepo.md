# Monorepo

Identify the package and bounded context before editing. Read the owning
context's `CONTEXT.md`, local ADRs, and package-level instructions when present.

Keep changes inside the Task slice. Cross-package edits need an explicit reason,
an owning contract, and verification that covers the affected boundary.
