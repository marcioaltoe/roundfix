# Spec routing

Route large initiatives through idea, PRD, techspec, tasks, implementation, QA,
and archive. Route ordinary features through PRD or techspec before tasks when
requirements or architecture need decisions. Route small refactors and bug
fixes through a focused techspec when they affect behavior.

Trivial changes can skip a Spec only when the intent, acceptance criteria, and
verification are obvious from the request and local context.
