{
  "schemaVersion": "setup-context-driven/module-v1",
  "id": "monorepo",
  "version": 1,
  "kind": "repository-shape",
  "title": "Monorepo context guidance",
  "dependsOn": ["context-workflow"],
  "conflictsWith": [],
  "requiredSkills": ["domain-modeling"],
  "requiredDecisions": ["domain.layout"],
  "rootBlocks": [
    {
      "id": "root.monorepo",
      "version": 1,
      "template": "template.root.monorepo",
      "rules": ["rule.monorepo.context-boundaries"]
    }
  ],
  "supportingGuides": [
    {
      "id": "guide.monorepo",
      "version": 1,
      "path": "docs/agents/monorepo.md",
      "template": "template.guide.monorepo",
      "rules": ["rule.monorepo.context-boundaries"]
    }
  ],
  "rules": [
    {
      "id": "rule.monorepo.context-boundaries",
      "version": 1,
      "summary": "Respect context boundaries and read the mapped context before editing a package."
    }
  ]
}
