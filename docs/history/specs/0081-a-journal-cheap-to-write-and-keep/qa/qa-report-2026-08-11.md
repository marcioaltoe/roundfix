---
spec: 0081-a-journal-cheap-to-write-and-keep
date: 2026-08-11
build: 7dea0bd3f25594bfbc0673c0558c2e67db8c6179
status: closed
verdict: fail
rows_blocked_environment: 0
rows_blocked_finding: 0
rows_blocked_declared: 0
surfaces: [backend, data, docs, cli]
---

# QA Report — A journal cheap to write and cheap to keep

## Scope and environment

Full authored terminal QA gate for task_09 against Run Worktree build
`7dea0bd3f25594bfbc0673c0558c2e67db8c6179`. The Run Worktree branch is
report-only: no commit, push, Pull Request creation, customer credential, or
operator Run Database mutation is authorized. There is no open Pull Request,
and this Spec declares no Unreachable Acceptance. The real data journeys use
fresh temporary Roundfix Homes and the production Store/CLI paths compiled
from this build.

The authored graph names `task_09` as its terminal `type: qa` node; its two
dependencies, task_06 and task_08, are completed. The strict authoring
precondition passed with no findings. Its
`SC-VOCABULARY-UNDOCUMENTED` detector was skipped because the TechSpec has no
Vocabulary Contract, so R06 retains the public vocabulary/non-regression
obligation instead of crediting that detector.

## Static gate

- `rtk roundfix spec check 0081-a-journal-cheap-to-write-and-keep --strict`:
  passed; no findings; `SC-VOCABULARY-UNDOCUMENTED` skipped as recorded above.
- `rtk env GOCACHE=/private/tmp/roundfix-0081-qa-gocache make verify`:
  failed at `fmt-check` before tests because `internal/store/journal.go`,
  `internal/store/journal_consumer_corpus_test.go`, and
  `internal/store/journal_header_test.go` need formatting (F-01). The failure
  is code-caused but does not prevent the unchanged build's focused runtime
  flows from executing.
- `rtk make verify-docs`: passed, including `docscontract`, the measured
  sanctioned-ownership checks, corpus budget, a current `bin/roundfix` build,
  and Spec consistency. The expected missing Vocabulary Contract skip remains.

## Mechanical findings

None.

## Results

| # | Story / criterion / sweep | Actor and surface | Status | Evidence |
| - | --- | --- | --- | --- |
| R01 | Run-start wall-clock is independent of journal size across empty and full-retention databases. | Session starting a Run; data/backend | pass | Two fresh current-build runs: p50 51/59 us at 0 events, 41/40 us at 1,000, and 37/38 us at 10,000; both runs passed and the former size curve was absent. Evidence: `docs/specs/0081-a-journal-cheap-to-write-and-keep/qa/evidence/2026-08-11-gate/evidence.md#r01-run-start-cost`. |
| R02 | Six parallel Runs at the pre-raise 5,000 ms `busy_timeout` complete with zero `SQLITE_BUSY`; cursors remain contiguous and publisher order survives cancellation. | Operator running six Runs; data/backend | pass | Six Runs wrote 1,536 events at 5,000 ms with 0 `SQLITE_BUSY` and 6 completions; read-back and cancellation subtests passed. Evidence: `docs/specs/0081-a-journal-cheap-to-write-and-keep/qa/evidence/2026-08-11-gate/evidence.md#r02-parallel-runs`. |
| R03 | Event-write cost per 1,000 agent events improves against baseline because 128-event batching amortizes transactions, not because fewer events are produced. | Daemon appending agent output; data/backend | fail | Current build: 1,536 events, 38,136 us/1,000, all batch boundaries pass. The pre-change baseline contains only per-append percentiles and no comparable per-1,000 throughput; its same-harness after explicitly excludes batching, so the required improvement cannot be established (F-02). |
| R04 | Bytes per Run are reported before and after, and retained Runs keep raw payload bytes under the unchanged terminal-only, age-based retention shape. | Maintainer deciding retention; data/docs | pass | Before and fresh after bytes match at 61,440 / 2,162,688 / 21,135,360 for 0 / 1,000 / 10,000 events; exact ADR/lifecycle diff is empty; byte round-trip and GC semantics pass. External source: the maintainer Finding's 2.9 GB / 1,645,457-event observation. |
| R05 | Cockpit refresh cost tracks new events rather than total events while rendering, summary fallback, follow behavior, and terminal-size states remain stable. | Operator watching a long Run; TUI through CLI | pass | The same two new events cost 2 header rows + 1 full row after both 20- and 400-event backlogs; 12 focused tests passed across replay, snapshots, 80x24/120x40/200x50, degenerate sizes, keyboard, follow, fallback, and no-color states. |
| R06 | A recorded pre-change journal remains behaviorally identical through `events`, attach, cockpit rendering, reconcile replay detection, and `gc`; command/schema vocabulary stays unchanged. | Supervisor/operator; CLI/TUI/data/docs | fail | Current focused consumer and help checks pass, but `journal_consumer_corpus_test.go` was added in the same commit as the read change and reaches only full read, `events`, header projection, and batch clocks—not attach, full Cockpit, reconcile, or GC. No pre-change all-consumer replay exists (F-03). |

## Detailed row evidence

### R01

```yaml
inputs:
  - kind: repository_path
    ref: internal/store/journal_baseline_test.go
  - kind: repository_path
    ref: internal/store/journal.go
  - kind: repository_path
    ref: docs/specs/0081-a-journal-cheap-to-write-and-keep/baseline/*.md
  - kind: elapsed_time
    ref: current-build same-machine two-sample Run-start measurement
```

Entry point: the production retention sequence used at operational Run
preflight, exercised on newly migrated temporary Run Databases. Independent
confirmation: a second fresh harness run. Persistence: reopen/read behavior is
covered by the same production database path. Behavior probes: empty journal,
1,000-event journal, 10,000-event full-retention journal, and repeat run.

Status: **pass**. `TestJournalMeasurementHarness` ran twice from the current
build and exited 0. Run A p50 was 51 us (empty), 41 us (1,000 events), and 37
us (10,000); Run B was 59, 40, and 38 us respectively. The 10,000-event
database was 21,135,360 bytes versus 61,440 bytes empty, yet its p50 did not
increase. The second run independently confirmed the first.

### R02

```yaml
inputs:
  - kind: repository_path
    ref: internal/store/journal_parallel_runs_test.go
  - kind: repository_path
    ref: internal/store/journal_writer.go
  - kind: repository_path
    ref: internal/store/writelock_*.go
  - kind: elapsed_time
    ref: current-build six-Run concurrency scenario
```

Entry point: six independent writer Stores against one newly migrated Run
Database with the real Store-scoped Journal Sink. Independent confirmation:
read every Run back through the production journal reader. Persistence:
cursor/order checks after commit. Behavior probes: six-way synchronized
publish, exact 5,000 ms timeout read-back, and cancellation while holding the
writer lock.

Status: **pass**. The fresh scenario completed six of six Runs and 1,536 of
1,536 events with `sqlite_busy=0`. Wall time was 58,577 us (38,136 us per
1,000 events), writer p50/p95 was 25,176/58,539 us, every Run read back
contiguous cursors and publisher order, and cancellation released the lock so
the remaining Runs proceeded.

### R03

```yaml
inputs:
  - kind: repository_path
    ref: internal/store/journal_batch_test.go
  - kind: repository_path
    ref: internal/store/journal_writer.go
  - kind: repository_path
    ref: docs/specs/0081-a-journal-cheap-to-write-and-keep/baseline/2026-08-11-before.md
  - kind: elapsed_time
    ref: current-build batched event-write observation
```

Entry point: the production Store-scoped Journal Sink used by daemon output.
Independent confirmation: read back all events and contiguous cursors after
flush. Persistence: close/reopen read-back. Behavior probes: exact 128-event
count boundary, linger boundary, immediate-durability preflush, and explicit
flush; the metric requires the same event count before and after.

Status: **fail** (F-02). The unchanged build passed every count, linger,
immediate, explicit-flush, ordering, raw-payload, failure-preservation, and
ambiguous-commit rehearsal. The current six-Run observation kept all 1,536
events and reported 38,136 us per 1,000; `journalBatchSize=128` and 256 events
per Run establish two count batches per Run. However, the pre-change baseline
records only direct `AppendRunEvent` p50/p95 samples and no wall cost per 1,000
events. The same-harness repaired report says explicitly that it does not
measure end-to-end JournalWriter batching and makes no batching speedup claim.
Different measurement boundaries cannot prove the required before/after
improvement.

### R04

```yaml
inputs:
  - kind: repository_path
    ref: docs/findings/2026-08-06-three-gigabytes-of-event-journal-inside-the-retention-window.md
  - kind: repository_path
    ref: docs/specs/0081-a-journal-cheap-to-write-and-keep/baseline/*.md
  - kind: repository_path
    ref: docs/adr/0008-run-event-payload-stores-raw-producer-json.md
  - kind: repository_path
    ref: docs/adr/0033-the-run-event-journal-is-pruned-by-retention.md
```

Entry point: the recorded operator database measurement plus same-harness
temporary databases. Independent confirmation: exact current-tree diff of the
binding ADR/lifecycle contract and raw payload byte round-trip. Persistence:
read after batch commit and Store reopen. Behavior probes: byte counts at 0,
1,000, and 10,000 retained events; raw JSON byte preservation; zero-retention
keep-everything; terminal-only age eligibility. The outside evidence is the
maintainer-created Finding outside the Spec, not a Spec-authored rehearsal.

Status: **pass**. The before report and fresh current-build measurement record
the same 61,440, 2,162,688, and 21,135,360 database bytes for 0, 1,000, and
10,000 retained events: no byte reduction is claimed because no payload is
shed. An exact `685d201b..HEAD` diff of ADR-0008, ADR-0033, and the lifecycle
guide is empty. Raw-payload byte round-trip, terminal-only eligibility, run/lock
preservation, dry-run non-mutation, actual GC pruning, and zero-retention
keep-everything all passed. The external acceptance source is
`docs/findings/2026-08-06-three-gigabytes-of-event-journal-inside-the-retention-window.md`,
which recorded 2,915 MB of `run_events`, 1,645,457 events, and a largest Run
of 42,000 before this Spec existed.

### R05

```yaml
inputs:
  - kind: repository_path
    ref: internal/tui/cockpit.go
  - kind: repository_path
    ref: internal/tui/cockpit_forward_cursor_test.go
  - kind: repository_path
    ref: internal/tui/testdata/cockpit_snapshots/*
```

Entry point: the Cockpit model used by the CLI Live Run View, fed from the
production journal reader. Independent confirmation: rendered snapshots after
incremental and replay folds. Persistence: idle repoll and completed-Run
attach replay. Behavior probes: 20 versus 400-event backlog followed by the
same two new events, missing-payload summary fallback, small/large/degenerate
terminal sizes, follow freeze/resume, and clean terminal state.

Status: **pass**. The opening fold paged each backlog once; the next refresh
after the same two new events read exactly two headers and one full payload for
both a 20- and 400-event backlog, then rendered identical Task state. Twelve
focused tests passed: the forward cursor and header projection, missing-payload
summary fallback, completed-Run attach replay, ten unchanged snapshots at
88x24/120x40, responsive 80x24/120x40/200x50 layouts, degenerate dimensions,
keyboard focus, follow freeze/resume, new-event count, and color-independent
markers.

### R06

```yaml
inputs:
  - kind: repository_path
    ref: internal/store/journal_consumer_corpus_test.go
  - kind: repository_path
    ref: internal/cli/*_test.go
  - kind: repository_path
    ref: internal/tui/*_test.go
  - kind: repository_path
    ref: internal/runevent/*
  - kind: repository_path
    ref: CONTEXT.md
```

Entry point: built CLI commands and the production journal replay surfaces.
Independent confirmation: exact schema/output assertions and payload-equality
read-back. Persistence: completed-Run attach/replay and GC/reconcile checks
after journal commit. Behavior probes: daemon and agent payloads, paging,
malformed/missing payload fallback, terminal replay, and no-eligible GC. The
missing TechSpec Vocabulary Contract detector is retained here by checking the
current compiled/help terminology against `CONTEXT.md`.

Status: **fail** (F-03). Four Store corpus tests, four `events` CLI tests,
Spec attach replay/follow, two reconcile payload-coverage tests, the GC checks
credited by R04, and the Cockpit replay/snapshot checks credited by R05 all
pass on current fixtures. Built `events`, `attach`, `gc`, and `reconcile` help
also retain the documented Run Event Journal, Supervisor stream, read-only
Attach, Journal Retention, and payload-dependent replay vocabulary. But Git
ancestry proves `journal_consumer_corpus_test.go` was first added by task_05's
same commit that changed `journal.go` and `cockpit.go`; it is not a recorded
pre-change artifact. Its four tests exercise full-read paging, `events`
projection, the header subset, and batch clocks only. No one recorded corpus
is replayed through attach, full Cockpit rendering, reconcile payload parsing,
and GC, so the required byte-identical before/after consumer audit is absent.

## Findings

### F-01 — Repository Verification stops at unformatted Go files

- Impact: Friction.
- Actor: maintainer preparing the Spec for delivery.
- Journey step: run the repository's authoritative `make verify` gate.
- Expected: the full gate reaches all checks and exits zero.
- Actual: `fmt-check` exits 2 immediately and names three unformatted Go
  files: `internal/store/journal.go`,
  `internal/store/journal_consumer_corpus_test.go`, and
  `internal/store/journal_header_test.go`.
- Reproduction: `rtk env
  GOCACHE=/private/tmp/roundfix-0081-qa-gocache make verify` from the Run
  Worktree root.
- Evidence:
  `docs/specs/0081-a-journal-cheap-to-write-and-keep/qa/evidence/2026-08-11-gate/evidence.md#static-gate`.
- Affected rows: none are behaviorally unreachable; the repository-level
  failure independently forces a failing final verdict.

### F-02 — No comparable pre-change per-1,000 batching measurement

- Impact: Trust-Damage.
- Actor: maintainer deciding whether the write-amplification goal shipped.
- Journey step: compare current per-1,000 agent-event cost with the recorded
  baseline and attribute the delta to batching.
- Expected: before and after numbers at the same measurement boundary, with
  equal event counts and transaction amortization isolated.
- Actual: the current scenario reports 38,136 us per 1,000 across 1,536
  Journal Sink events and all batching rehearsals pass, but the baseline
  reports only direct single-event append percentiles. The repaired report
  explicitly says that same harness does not measure JournalWriter batching.
- Reproduction: compare
  `TestParallelRuns/six_concurrent_Runs_append_events_at_the_pre-raise_timeout`
  with the two baseline Markdown reports, then run
  `go test -count=1 ./internal/store -run 'Batch|AmbiguousCommit' -v`.
- Evidence:
  `docs/specs/0081-a-journal-cheap-to-write-and-keep/qa/evidence/2026-08-11-gate/evidence.md#r03-event-write-cost`.
- Affected rows: R03.

### F-03 — The consumer corpus is neither pre-change nor all-consumer

- Impact: Trust-Damage.
- Actor: supervisor/operator relying on journal replay after the read-path
  change.
- Journey step: replay one recorded pre-change journal through `events`,
  attach, Cockpit, reconcile, and GC and compare behavior byte-for-byte.
- Expected: a corpus committed before the production change, with expected
  output for all five named consumers.
- Actual: Git ancestry shows the corpus test was added in task_05's production
  change commit. Its four tests cover only full read, `events`, header subset,
  and batch-clock calculation. Separate current-fixture tests for the other
  consumers pass, but they cannot establish pre/post identity.
- Reproduction: `git diff-tree --no-commit-id --name-status -r a8af25b7` and
  inspect/run the four tests in `journal_consumer_corpus_test.go`.
- Evidence:
  `docs/specs/0081-a-journal-cheap-to-write-and-keep/qa/evidence/2026-08-11-gate/evidence.md#r06-consumer-non-regression`.
- Affected rows: R06.

## Blocked and skipped

No row is blocked or skipped. R03 and R06 failed directly; neither is softened
into a blocked row. Two optional review-Run Attach checks attempted a GitHub
preflight and hit the declared sandbox denial for `api.github.com`; they do
not change R06's status because local Spec Attach and follower checks ran, and
R06 already fails on missing regression provenance rather than environment.
No row may be skipped in a full gate.

## Coverage

Closed with zero pending or skipped rows: 4 pass, 2 fail, 0 blocked. R01–R06
cover the six PRD Success Metrics in order, all six User Stories, and Core
Features 1–7. R04 carries the outside acceptance source; R05 carries the
relevant TUI sweep; R06 retains the skipped Vocabulary Contract obligation.

Non-Goals were checked from the exact `685d201b..HEAD` changed-path inventory:
the store remains SQLite at schema version 12; `.roundfixrc.yml`, `go.mod`, and
`go.sum` have empty diffs; no migration, second store, stream schema, GC
compaction behavior, second retention window, or early payload shedding
landed. The six-Run and batch observations preserve event count, so the
measured result does not rely on producing fewer events. The full changed set
stays in the Spec artifacts, journal/store paths, CLI journal wiring, and
Cockpit paths declared by the Spec.

Rows blocked by environment: 0. Rows blocked by finding: 0. Rows blocked by
declaration: 0. Findings by impact: 2 Trust-Damage, 1 Friction, 0
Blocks-Completion, 0 Data-Loss, 0 Cosmetic.

## Final verdict

**Fail.** Four of six Success Metric rows pass, but R03 lacks a comparable
pre-change per-1,000 batching measurement, R06 lacks a genuine pre-change
all-consumer replay corpus, and authoritative `make verify` stops at three
unformatted Go files. Before rerunning, format the named files, add the
same-boundary before/after batching measurement without changing event count,
and replay one provenance-backed pre-change journal through `events`, attach,
Cockpit, reconcile, and GC; then run cold `make verify`, R03, R06, and one
adjacent canary each.

## Mechanical skips

| Detector | Missing artifact |
| --- | --- |
| authorization bounded paths | tooling authorization |
| consequent-fix commit order | consequent-fix declaration |
| QA Report structure | QA Report |
| QA evidence paths | QA Report |
