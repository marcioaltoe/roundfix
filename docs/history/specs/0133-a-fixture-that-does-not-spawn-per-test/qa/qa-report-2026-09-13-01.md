---
spec: 0133-a-fixture-that-does-not-spawn-per-test
date: 2026-09-13
build: b448a60ced1cc2b9a3299889ba0934922a9ed576
auditing_binary: "0.12.0 (b448a60c-dirty, built 2026-09-13 16:59:49 -0300)"
auditor_staleness: "current: build commit equals audited HEAD; dirty suffix records the seeded untracked QA report"
status: closed
verdict: pass
rows_blocked_environment: 2
rows_blocked_finding: 0
rows_blocked_declared: 0
surfaces: [cli, backend, docs]
---

# QA Report

## Scope and environment

Full terminal rerun for the assembled Task 01 and Task 02 commits plus the
accepted requirement amendment at `b448a60ced1cc2b9a3299889ba0934922a9ed576`
in the Run Worktree. The actors are a Supervisor using the public Implement and
Baseline commands and a maintainer running the repository's documented Go
package and Verification surfaces. No Open Pull Request exists for
`feat/spec-contained-authorization`; the Pull Request journey uses the
equivalent-evidence contract.

The gate rebuilt `bin/roundfix` from this tree before the matrix, and
`make verify-docs` rebuilt it again from the same head. The final binary reports
`roundfix 0.12.0 (b448a60c-dirty, built 2026-09-13 16:59:49 -0300)`.
The strict precondition command `./bin/roundfix spec check
0133-a-fixture-that-does-not-spawn-per-test --strict` exited 0 with `No
findings`; authored Verification commands were not executed by that check.

## Performed repairs

None.

## Assigned repair failures

None.

## Authorization audit inputs

None.

## Mechanical findings

None.

## Static gate

- Exact `make verify` exited 2 at `fmt-check` under the host's PATH-selected Go
  1.27.1 formatter. It named
  `internal/cli/baseline_skills_restore_test.go` and
  `internal/cli/baseline_assets_sync_test.go`. Both files are byte-identical to
  pre-Task base `47c00f54` and were last changed at `8d333a6c`. The
  repository-compatible Go 1.26.7 `make fmt-check` exited 0, proving the first
  stop is a toolchain environment mismatch.
- `make verify
  GO=/Users/marcio/go/pkg/mod/golang.org/toolchain@v0.0.1-go1.26.7.darwin-arm64/bin/go
  GOFMT=/Users/marcio/go/pkg/mod/golang.org/toolchain@v0.0.1-go1.26.7.darwin-arm64/bin/gofmt`
  advanced through formatting and exited 2 in the all-package test stage when
  seven Daemon concurrency tests exhausted their existing 6–8 second waits. All seven
  passed together in 1.327s when rerun as one focused package command, and the
  full candidate Daemon package passed alone in 5.542s. Under the same active
  host workload, the unchanged pre-repair tree reproduced six of those same
  timeout failures during an all-package run. This proves concurrent host load,
  rather than this Spec's fixture edit, blocked the complete all-package
  observation. No timeout, parallelism, retry, or assertion changed.
- `make verify-docs` exited 0. It rebuilt the binary, passed the documentation,
  sanctioned-ownership and generated-boundary tests, the corpus budget, and
  the repository-wide Spec Consistency Check.
- A fresh 17-case selection across `internal/authorization`, `internal/spec`,
  `internal/cli`, and `internal/daemon` exited 0 through the public CLI runners
  and real Git fixtures. A second four-journey provenance run also exited 0.

The focused concurrency reproduction ran:

```text
env GOCACHE=/private/tmp/roundfix-0133-go126-cache /Users/marcio/go/pkg/mod/golang.org/toolchain@v0.0.1-go1.26.7.darwin-arm64/bin/go test -count=1 ./internal/daemon -run '^(TestTaskCycleRepairReacquiresVerificationCapacityAfterFeedback|TestTaskCycleSchedulesIndependentWaveWithConcurrencyCap|TestTaskCycleStopRequestMidWaveDrainsRunningTasksAndStartsNothingNew|TestTaskCycleStopRequestMidWaveSkipsQAWithEveryTaskCompleted|TestTaskCycleParallelTaskPromptUsesTaskWorktreeContextBase|TestTaskCycleIntegratedVerificationCapacityOneBoundsConcurrentTaskWorktrees|TestTaskCycleVerificationCapacityTwoOverlapsReadyAttemptsWithoutPermitLoss)$'
```

The assembled behavior selection ran:

```text
env GOCACHE=/private/tmp/roundfix-0133-go126-cache /Users/marcio/go/pkg/mod/golang.org/toolchain@v0.0.1-go1.26.7.darwin-arm64/bin/go test -count=1 ./internal/authorization ./internal/spec ./internal/cli ./internal/daemon -run '^(TestAuthorizationReaderClassifiesGrantState|TestAuthorizationReaderTypesPermittedOperations|TestAuthorizationReaderRefusesEmptyPaths|TestOperationAuthorityReportsUnresolvableSpecRoot|TestMissingAuthorizationEvidenceRemainsUnresolved|TestRequireGovernedOperationKeepsRefusalIdentity|TestCurrentRecordPermitsItsDeclaredOperations|TestImplementDispatchesWithoutRecordWhenNoGovernedMutation|TestGovernedChangeStillRefusesWithoutRecord|TestSettleCommitsOrdinaryWorkWithoutRecord|TestFinalPushIsASeparateExplicitOperation|TestGovernedMutationRefusesMissingImplementAuthority|TestGovernedMutationDetectionUsesTheUnfilteredSnapshot|TestTaskCycleFixtureSeedIsCreatedOnce|TestTaskCommitDropsSymlinkCrossingTaskFileAndCommitsRepositoryPaths|TestTaskCycleSettlesCompletedWithoutCommitWhenOnlyExternalTaskFileChanged|TestTaskCycleQAReportExternalProceedsWithoutStaging)$'
```

## Results

| # | Story / criterion / sweep | Actor and surface | Status | Evidence |
| - | --- | --- | --- | --- |
| QA-01 | Goal 1; Story 1; Core Features 1 and 3 — an ordinary Spec without a record dispatches | Supervisor · CLI | pass | Public Implement runner reached one Agent call and a persisted Run; the Settle runner committed ordinary work without a record |
| QA-02 | Story 2; Core Feature 2 — a Governed Path without an operative record still refuses with the established identity | Maintainer · CLI | pass | Public Implement runner reached Agent work, refused before commit, and the Daemon path preserved HEAD and the refusal identity |
| QA-03 | Core Features 1 and 2 — non-operative and empty-path records remain fail-closed without changing the operation vocabulary | Maintainer · backend/CLI | pass | Grant-state, operation-vocabulary, empty-path, and exact-refusal tests passed together |
| QA-04 | Goal 3; Core Feature 1; declared break 1 — the canonical rule and public operation boundary state the same division | Supervisor · docs/CLI | pass | Two rebuilt-binary previews returned `Baseline update: current`, identical digest `63be52d0…`, and `File changes: 0`; module and guide carry the exact division |
| QA-05 | Goal 2; Stories 3 and 4; Core Features 4 and 7; declared break 2 — the package seed is created once and fixtures remain private | Maintainer · backend | pass | Named guard passed in two fresh processes; fixture body uses the shared-seed copy and contains no repository initialization |
| QA-06 | Core Features 5 and 6 — committed provenance, external Spec Roots, symlinked roots, and missing-Git refusal survive | Maintainer · backend | pass | Four real-Git journeys passed in two fresh selections; the reader diff contains only the governed-operation wrapper |
| QA-07 | Story 3; Success Metric 1; outside evidence — candidate Daemon time is measurably below the immediate pre-repair control, with target context recorded | Maintainer · backend | pass | Candidate passed in 5.542s versus pre-repair control 7.324s; external `origin/main` passed in 3.430s |
| QA-08 | Non-Goals and Task 02 scope — no cost repair enters production or changes budgets, deadlines, parallelism, skips, settlement, staging, or commit assertions | Maintainer · repository | pass | Task 02 changes only its Task and fixture test; full candidate package passed and the exact diff adds none of the excluded changes |
| QA-09 | Tooling authority — Task 01's governed changes are bounded, authorized, chronologically valid, and regenerated through the sanctioned command | Maintainer · repository | pass | Grant `91a17947` precedes base, both Task commits, and amendment; exact diffs match bounded and sanctioned scope; regeneration returned `changed:false` |
| QA-10 | Repository Verification and current-tree documentation contract | Maintainer · repository | blocked (environment: PATH Go 1.27.1 mismatch and concurrent host load) | Compatible formatting passed; all seven timed-out cases passed focused; unchanged-base workload reproduced six; `make verify-docs` passed |
| QA-11 | Pull Request controls for the Spec target branch | Reviewer · Pull Request | blocked (environment: no open Pull Request) | Prompt proves none is open; all five equivalent controls are recorded below |

### QA-01

```yaml
inputs:
  - kind: repository_path
    ref: cmd/roundfix/**
  - kind: repository_path
    ref: internal/cli/**
  - kind: repository_path
    ref: internal/daemon/**
  - kind: repository_path
    ref: internal/spec/**
```

Entry point: rebuilt `bin/roundfix implement`. Exercise a scratch Spec with no
authorization record and no Governed Path mutation. The observable is that
dispatch reaches Agent work instead of refusing `implement`; a fresh public
read of the scratch Run and repository state must confirm the journey.

Observed through the public CLI runner over fresh committed Git repositories:
the no-record Spec entered Implement, made exactly one Agent call, exited 0,
and a separate store read found one persisted Run. Independent confirmation
came from the public Settle runner, which advanced Git HEAD and reported the
ordinary committed path without an authorization record. The Daemon's separate
final-push test also confirmed that push remains its own explicit operation.
All cases ran in the fresh 17-case selection and exited 0.

### QA-02

```yaml
inputs:
  - kind: repository_path
    ref: cmd/roundfix/**
  - kind: repository_path
    ref: Makefile
  - kind: repository_path
    ref: internal/authorization/**
  - kind: repository_path
    ref: internal/cli/**
  - kind: repository_path
    ref: internal/daemon/**
```

Entry point: rebuilt `bin/roundfix implement`. Exercise a scratch Spec whose
Agent changes a Governed Path without an operative record. The observable is
the established refusal, no commit, and unchanged target state after a fresh
Git read.

Observed through the public CLI runner: the scratch Spec reached one Agent
call, changed `Makefile`, exited nonzero with `authorization operation
"implement" is not permitted` and the canonical record path, and made zero
committer calls. The independent Daemon path recorded one failed Task, zero
commits, the same refusal identity, and an unchanged HEAD from a fresh
`rev-parse`. Unfiltered-snapshot coverage also proved that a filtered
`.roundfixrc.yml` mutation remains governed while ordinary and pre-existing
changes do not become the Task's mutation.

### QA-03

```yaml
inputs:
  - kind: repository_path
    ref: internal/authorization/**
  - kind: repository_path
    ref: internal/spec/**
  - kind: repository_path
    ref: internal/cli/**
```

Exercise proposed, withdrawn, contradictory, absent, and empty-path records.
Confirm the typed empty-path failure independently and compare emitted
operation tokens and refusal text with the established contract.

Observed in a fresh four-package process: approved, proposed, absent,
contradictory, and empty-path states produced their typed outcomes; the closed
operation vocabulary remained `implement`, `commit`, `push`, `pull_request`,
`merge`, and `release`; and `RequireGovernedOperation` produced the byte-exact
existing `RequireOperation` refusal whenever a Governed Path was present. The
empty-path case remained a `paths` refusal.

### QA-04

```yaml
inputs:
  - kind: repository_path
    ref: cmd/roundfix/**
  - kind: repository_path
    ref: internal/baseline/assets/modules/core.json
  - kind: repository_path
    ref: docs/agents/agent-instructions.md
  - kind: repository_path
    ref: docs/agents/setup-context.json
```

Run the rebuilt public Baseline refresh preview twice. It must report the tree
current both times. Independently read the canonical module and rendered guide
to confirm the declared division.

Observed in two fresh invocations of `./bin/roundfix baseline update --repo .
--no-skills --format text`: each exited 0 with `Baseline update: current`,
catalog digest `sha256:733a35a2…`, Plan Digest `sha256:63be52d0…`, and `File
changes: 0`. Both reproduced the same two pre-existing nested-carrier warnings.
An independent `rg` read found the exact “grants no governed mutation” and
“does not by itself refuse implementation, commit or push” text in the
canonical module and rendered guide.

### QA-05

```yaml
inputs:
  - kind: repository_path
    ref: internal/daemon/task_engine_test.go
  - kind: repository_path
    ref: internal/gittest/gittest.go
```

Run the named seed-reuse guard in two fresh processes, inspect the per-test
fixture body for repository setup, and exercise isolation assertions.

Observed in two sequential fresh processes: the named guard exited 0 in 0.678s
and 0.665s. It constructed two fixtures, required one seed creation and shared
seed identity, then proved distinct Git roots, isolated Task Graphs, isolated
writes, and committed authorization provenance. A fresh extraction of
`newTaskCycleFixture` showed only `taskCycleFixtureSeedForTest` and `copyTo`
before the journey overlay; its body contains no `InitRepo`, `git add`, or
`git commit` call. Both processes ran
`env GOCACHE=/private/tmp/roundfix-0133-go126-cache
/Users/marcio/go/pkg/mod/golang.org/toolchain@v0.0.1-go1.26.7.darwin-arm64/bin/go
test -count=1 ./internal/daemon -run
'^TestTaskCycleFixtureSeedIsCreatedOnce$'`.

### QA-06

```yaml
inputs:
  - kind: repository_path
    ref: internal/daemon/task_engine_test.go
  - kind: repository_path
    ref: internal/spec/authorization.go
  - kind: repository_path
    ref: internal/authorization/authorization.go
```

Run the external and symlinked Spec Root journeys plus the missing-Git
provenance refusal through fresh processes. Confirm each still reaches the real
Git authorization reader.

Observed twice through fresh Go test processes. The combined 17-case selection
and a separate four-journey selection both passed the external Task-file
settlement, external QA Report, symlink-crossing commit, and unresolvable Spec
Root refusal. These fixtures create committed Git provenance. The focused diff
from pre-Task base `47c00f54` to audited HEAD shows the authorization reader
parsing and revision resolution unchanged; the two authorization files add
only the governed-operation wrapper. The independent command was `env
GOCACHE=/private/tmp/roundfix-0133-go126-cache
/Users/marcio/go/pkg/mod/golang.org/toolchain@v0.0.1-go1.26.7.darwin-arm64/bin/go
test -count=1 ./internal/daemon ./internal/spec -run
'^(TestTaskCommitDropsSymlinkCrossingTaskFileAndCommitsRepositoryPaths|TestTaskCycleSettlesCompletedWithoutCommitWhenOnlyExternalTaskFileChanged|TestTaskCycleQAReportExternalProceedsWithoutStaging|TestOperationAuthorityReportsUnresolvableSpecRoot)$'`.

### QA-07

```yaml
inputs:
  - kind: external_repository
    ref: origin/main@91a17947da69be0c72d0400fc6f40b565ed47037
  - kind: repository_path
    ref: internal/daemon/**
  - kind: elapsed_time
    ref: one concurrent go test -count=1 ./internal/daemon per tree on 2026-09-13
```

Measure the candidate and immediate pre-repair control on the same host and
cache policy. Record the external delivery-target measurement and provenance
as context. The candidate must be measurably below the control; target parity
is outside the amended acceptance.

Observed with read-only `git archive` trees and one shared warmed Go 1.26.7
cache. The candidate's full Daemon package passed in 5.542s; the immediate
pre-repair control `47c00f54` passed in 7.324s, a 1.782s or 24.3% reduction.
The external delivery target
`origin/main@91a17947da69be0c72d0400fc6f40b565ed47037`, which this Spec did not
build, passed in 3.430s. The remaining target gap is recorded as the PRD's
explicit Non-Goal and test-performance campaign input; the amended criterion
asks for the measured reduction and both measurements, which this run supplies.
Each tree ran `/usr/bin/time -p env
GOCACHE=/private/tmp/roundfix-0133-go126-cache
/Users/marcio/go/pkg/mod/golang.org/toolchain@v0.0.1-go1.26.7.darwin-arm64/bin/go
test -count=1 ./internal/daemon`; the workdirs were the Run Worktree,
`/private/tmp/roundfix-0133-control-XicPIT`, and
`/private/tmp/roundfix-0133-target-9O4KXZ` respectively.

### QA-08

```yaml
inputs:
  - kind: repository_path
    ref: internal/daemon/task_engine_test.go
  - kind: repository_path
    ref: docs/specs/0133-a-fixture-that-does-not-spawn-per-test/task_02.md
```

Audit Task 02's exact commit, search its additions for budget, deadline,
parallelism, and skip changes, and run the complete Daemon package. Confirm no
production file or existing journey assertion changed.

Observed from `git diff-tree --no-commit-id --name-only -r 9c7a82c0`: Task 02
changes exactly its assigned Task file and
`internal/daemon/task_engine_test.go`. The exact diff moves repository setup
from each `newTaskCycleFixture` call to one package seed, adds immutable
snapshot/copy behavior and the named guard, and leaves every existing journey
assertion intact. It adds no `time.After`, deadline, parallelism configuration,
or skip. The full candidate Daemon package passed in 5.542s.

### QA-09

```yaml
inputs:
  - kind: repository_path
    ref: docs/specs/0133-a-fixture-that-does-not-spawn-per-test/_authorization.md
  - kind: repository_path
    ref: docs/agents/agent-instructions.md
  - kind: repository_path
    ref: docs/agents/setup-context.json
  - kind: repository_path
    ref: internal/baseline/assets/modules/core.json
  - kind: repository_path
    ref: internal/baseline/testdata/**
```

Use `git diff-tree` for the authorization and Task commits, prove grant
ancestry, compare every Governed Path with the bounded grant, and reproduce the
sanctioned digest regeneration without a tracked delta.

Observed with `git diff-tree` for grant `91a17947`, Task 01 `42130c78`, and
Task 02 `9c7a82c0`. The grant carries only this Spec's authorization, PRD, and
TechSpec. Task 01 carries the three exact bounded Governed Paths, its Task,
ordinary implementation/test paths, and deterministic sanctioned Baseline
outputs; Task 02 carries only its Task and fixture test. Four ancestry checks
establish `91a17947 → 47c00f54 → 42130c78 → 9c7a82c0 → b448a60c`.
`make baseline-digests` exited 0 with `changed:false` and left no tracked
delta. No consequent-fix declaration exists; the seeded skip remains below.

### QA-10

```yaml
inputs:
  - kind: repository_path
    ref: "**"
```

Run `make verify` exactly, then run the separate `make verify-docs` Pull
Request-boundary contract. Record every exit and classify any failed subcheck.

Observed environment block with equivalent evidence for every failed control.
The exact host command failed only because PATH selects Go 1.27.1 formatting;
Go 1.26.7 formatting passed. The compatible full gate then passed every
non-Daemon package and reached only seven Daemon wait expirations under the
all-package host load. Those seven passed together in 1.327s; the full Daemon
package passed alone in 5.542s; and the unchanged pre-repair tree reproduced
six of the same waits under the all-package workload. `make verify-docs` exited
0. Unblocking the literal command requires a PATH-compatible Go 1.26 formatter
and a host without competing full repository Runs.

### QA-11

```yaml
inputs:
  - kind: live_service
    ref: Open Pull Request for feat/spec-contained-authorization
  - kind: repository_path
    ref: "**"
```

The prompt proves no Open Pull Request exists. Record equivalent evidence for
approval, checks and status, unresolved review threads, Merge-Ready acceptance,
and review-artifact ancestry.

Observed equivalent controls:

- Approval: the maintainer approved the exact three-path tooling grant on
  2026-09-13; no assembled-change acceptance has been obtained.
- Checks and status: the exact local `make verify` environment block and its
  compatible-toolchain evidence are recorded in QA-10; `make verify-docs`
  passed.
- Unresolved review threads: no Pull Request or Spec-local review artifact
  exists, so no review thread has been created; none is claimed resolved.
- Merge-Ready acceptance: no Review Source evidence exists yet.
- Review-artifact ancestry: no review artifact exists to bind to
  `b448a60ced1cc2b9a3299889ba0934922a9ed576`.

The environment cause is the prompt's explicit `none open; Pull Request
journeys are environment-blocked`. Unblocking requires an Open Pull Request on
`feat/spec-contained-authorization`; this QA Task has no authority to create
one.

## Findings

None.

## Blocked and skipped

- QA-10 is environment-blocked by the host's Go 1.27.1 PATH selection and
  concurrent repository workloads. Equivalent evidence covers both stops: Go
  1.26.7 formatting passes; every non-Daemon package passed in the compatible
  full run; all seven timed-out Daemon cases pass focused; the full Daemon
  package passes alone; and the unchanged base reproduces the contention
  pattern. A compatible Go PATH on an otherwise idle host unblocks the literal
  full command.
- QA-11 is environment-blocked because no Open Pull Request exists. Its detail
  records approval, local checks, thread state, Merge-Ready evidence, and
  review-artifact ancestry. Opening a Pull Request on the target branch
  unblocks the live journey.
- No row is finding-blocked, declared-blocked, or skipped by the QA Agent.

## Coverage

All 11 rows reached terminal status: 9 pass and 2 blocked by environment with
equivalent evidence. The matrix covers all three PRD Goals, all four User
Stories, all seven Core Features, both declared intentional breaks, the
amended performance metric, the outside-evidence obligation, Non-Goals,
tooling authority, full Verification, and Pull Request controls. The Task 03
wording retains the earlier smaller inventory; the gate followed the live
PRD's 3/4/7 inventory.

Blocked counters: `rows_blocked_environment: 2`,
`rows_blocked_finding: 0`, `rows_blocked_declared: 0`. Relevant probes covered
absent, proposed, contradictory, and empty-path records; ordinary and Governed
Path mutations; repeated fresh processes; distinct fixture copies and isolated
writes; external, symlinked, and missing-Git Spec Roots; two convergent
Baseline previews; current, pre-repair, and external-target timings; and
all-package contention against an unchanged control.

## Mechanical skips

| Detector | Missing artifact |
| --- | --- |
| authorization bounded paths | Task commits |
| consequent-fix commit order | consequent-fix declaration |
| QA evidence paths | docs/specs/0133-a-fixture-that-does-not-spawn-per-test/qa/evidence |

## Final verdict

Pass: 9 runnable rows passed and 2 environment-blocked rows carry their causes,
unblocking actions, and equivalent observed evidence; there are no product
findings, declared blocks, finding blocks, or QA skips.
