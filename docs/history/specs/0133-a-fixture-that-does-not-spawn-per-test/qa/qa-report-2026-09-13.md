---
spec: 0133-a-fixture-that-does-not-spawn-per-test
date: 2026-09-13
build: 9c7a82c0a4f5322fb743a6e9d05ebd15b5f5e105
auditing_binary: "0.12.0 (9c7a82c0-dirty, built 2026-09-13 12:09:00 -0300)"
auditor_staleness: "current: build commit equals audited HEAD; dirty suffix records the seeded untracked QA report"
status: closed
verdict: fail
rows_blocked_environment: 1
rows_blocked_finding: 0
rows_blocked_declared: 0
surfaces: [cli, backend, docs]
---

# QA Report

## Scope and environment

Full terminal QA for the assembled Task 01 and Task 02 commits at
`9c7a82c0a4f5322fb743a6e9d05ebd15b5f5e105` in the Run Worktree. The actors
are a Supervisor using the public Implement and Baseline commands and a
maintainer running the repository's documented Go package and Verification
surfaces. No Open Pull Request exists for `feat/spec-contained-authorization`;
the Pull Request journey therefore uses the equivalent-evidence contract.

The gate rebuilt `bin/roundfix` from this tree before the matrix. The resulting
binary reports `roundfix 0.12.0 (9c7a82c0-dirty, built 2026-09-13 12:09:00
-0300)`. The strict precondition command
`./bin/roundfix spec check 0133-a-fixture-that-does-not-spawn-per-test --strict`
exited 0 with `No findings`; its skipped detectors had no matching Finding,
Rollup, Backlog Entry, vocabulary obligation, or references index to inspect.

## Performed repairs

None.

## Assigned repair failures

None.

## Authorization audit inputs

| Task | Outcome | Record | Revision | Detail |
| --- | --- | --- | --- | --- |
| task_01 | granted | docs/specs/0133-a-fixture-that-does-not-spawn-per-test/_authorization.md | 47c00f548f873244d707e957607e524cd3c2331d | operative grant |

## Mechanical findings

None.

## Static gate

- `make verify` exited 2 at `fmt-check`: `internal/cli/baseline_skills_restore_test.go`
  and `internal/cli/baseline_assets_sync_test.go` need formatting. Both paths
  are byte-identical between the pre-Task base `47c00f54` and audited HEAD;
  their last change is `8d333a6c`, so F-001 is a pre-existing code-caused
  repository failure outside this Spec.
- `make verify-docs` exited 0. It rebuilt the binary, passed the docs contract,
  sanctioned-ownership and generated-boundary tests, corpus budget, and the
  repository-wide Spec Consistency Check.
- `go test -count=1 ./internal/authorization ./internal/spec ./internal/cli`
  exited 0 with 1,574 tests passed. This broad unimplicated run continued the
  package sweep after `make verify` stopped; it does not replace that failed
  repository gate.

## Results

| # | Story / criterion / sweep | Actor and surface | Status | Evidence |
| - | --- | --- | --- | --- |
| QA-01 | Goal 1; Story 1; Core Features 1 and 3 — an ordinary Spec without a record dispatches | Supervisor · CLI | pass | Public Implement runner reached one Agent call and a persisted Run; independent Daemon path committed and pushed ordinary work without a record |
| QA-02 | Story 2; Core Feature 2 — a Governed Path without an operative record still refuses with the established identity | Maintainer · CLI | pass | Public Implement runner reached Agent work, then refused the Governed Path with no commit; Daemon path preserved HEAD and the refusal identity |
| QA-03 | Core Features 1 and 2 — non-operative and empty-path records remain fail-closed without changing the operation vocabulary | Maintainer · backend/CLI | pass | Proposed/withdrawn/absent and empty-path reader cases plus exact refusal-identity and closed-operation tests passed across four packages |
| QA-04 | Goal 3; Core Feature 1; declared break 1 — the canonical rule and public operation boundary state the same division | Supervisor · docs/CLI | pass | Two rebuilt-binary previews returned `Baseline update: current`, identical digest `63be52d0…`, and `File changes: 0`; module and guide carry the exact division |
| QA-05 | Goal 2; Stories 3 and 4; Core Features 4 and 7; declared break 2 — the package seed is created once and fixtures remain private | Maintainer · backend | pass | Named guard passed in two fresh processes; fixture body uses shared-seed copy and contains no repository initialization |
| QA-06 | Core Features 5 and 6 — committed provenance, external Spec Roots, symlinked roots, and missing-Git refusal survive | Maintainer · backend | pass | Four real-Git journeys passed together; missing-Git refusal passed again alone; reader diff contains only the governed-operation wrapper |
| QA-07 | Story 3; Success Metric 1; outside evidence — candidate Daemon time returns to the delivery-target range | Maintainer · backend | fail | F-002: candidate package was 7.092s/6.593s versus `origin/main` 4.470s/3.388s; the confirmed pair remains roughly 1.95× |
| QA-08 | Non-Goals and Task 02 scope — no cost repair enters production or changes budgets, deadlines, parallelism, skips, settlement, staging, or commit assertions | Maintainer · repository | pass | Task 02 commit changes only its Task and the fixture test; full candidate package passed twice; exact diff adds no budget, deadline, parallelism, skip, or changed journey assertion |
| QA-09 | Tooling authority — Task 01's governed changes are bounded, authorized, chronologically valid, and regenerated through the sanctioned command | Maintainer · repository | pass | Grant `91a17947` precedes base `47c00f54`, Task 01 `42130c78`, and Task 02 `9c7a82c0`; exact diffs match the bounded and sanctioned scope; regeneration returned `changed:false` |
| QA-10 | Repository Verification and current-tree documentation contract | Maintainer · repository | fail | F-001: `make verify` exited 2 at pre-existing `fmt-check`; `make verify-docs` exited 0 |
| QA-11 | Pull Request controls for the Spec target branch | Reviewer · Pull Request | blocked (environment: no open Pull Request) | Prompt proves none is open; all five equivalent controls recorded below |

### QA-01

```yaml
inputs:
  - kind: repository_path
    ref: cmd/roundfix/**
  - kind: repository_path
    ref: internal/cli/**
  - kind: repository_path
    ref: internal/daemon/**
  - kind: repository_path
    ref: internal/spec/**
```

Entry point: rebuilt `bin/roundfix implement`. Exercise a scratch Spec with no
authorization record and no Governed Path mutation. The observable is that
dispatch reaches Agent work instead of refusing `implement`; a fresh public
read of the scratch Run and repository state must confirm the journey.

Observed through the public CLI runner over a fresh committed Git repository:
the no-record Spec entered the Implement command, made exactly one Agent call,
exited cleanly, and a separate store read found one persisted Run. Independent
confirmation came from the Daemon's `ordinary work without a record commits
and pushes` journey, which exercised implement, commit, and push as separate
operations on ordinary source. Both paths ran in the 27-test four-package
command that exited 0 after one unchanged retry outside the sandbox's denied Go
cache. The rebuilt binary itself supplied the strict precondition and Baseline
journeys; the public Implement runner supplies an injected temporary Roundfix
Home so QA does not write into the Active Run's real machine-wide store.
The subsequent broad authorization, Spec, and CLI run passed 1,574 tests.

### QA-02

```yaml
inputs:
  - kind: repository_path
    ref: cmd/roundfix/**
  - kind: repository_path
    ref: Makefile
  - kind: repository_path
    ref: internal/authorization/**
  - kind: repository_path
    ref: internal/cli/**
  - kind: repository_path
    ref: internal/daemon/**
```

Entry point: rebuilt `bin/roundfix implement`. Exercise a scratch Spec whose
Agent changes a Governed Path without an operative record. The observable is
the established `authorization operation "implement" is not permitted`
refusal, no commit, and unchanged target state after a fresh Git read.

Observed through the public CLI runner: the scratch Spec reached one Agent
call, changed `Makefile`, returned nonzero with `authorization operation
"implement" is not permitted` and the canonical authorization path, and made
zero committer calls. The separate Daemon journey observed one failed Task,
zero commits, unchanged Git HEAD after a fresh `rev-parse`, and the same
refusal text. `TestGovernedMutationDetectionUsesTheUnfilteredSnapshot` also
proved a filtered `.roundfixrc.yml` mutation remains governed while ordinary
source and pre-existing changes do not become this Task's mutation.
The subsequent broad authorization, Spec, and CLI run passed 1,574 tests.

### QA-03

```yaml
inputs:
  - kind: repository_path
    ref: internal/authorization/**
  - kind: repository_path
    ref: internal/spec/**
  - kind: repository_path
    ref: internal/cli/**
```

Exercise the public authorization read path with proposed, withdrawn,
contradictory, absent, and empty-path records. Confirm the typed empty-path
failure independently, and compare the emitted operation tokens and refusal
text with the established contract after a fresh process invocation.

Observed: the four-package command exercised grant-state classification,
permitted-operation typing, absent evidence, empty-path rejection,
`RequireGovernedOperation`'s byte-identical delegation to `RequireOperation`,
public Implement and Settle behavior, and Daemon commit/push separation. It
passed 27 tests including subtests. The initial sandboxed attempt reached 24
passes before the Go build cache returned `operation not permitted`; the one
unchanged full-access retry exited 0. No assertion or environment was weakened.
The subsequent broad authorization, Spec, and CLI run passed 1,574 tests.

### QA-04

```yaml
inputs:
  - kind: repository_path
    ref: cmd/roundfix/**
  - kind: repository_path
    ref: internal/baseline/assets/modules/core.json
  - kind: repository_path
    ref: docs/agents/agent-instructions.md
  - kind: repository_path
    ref: docs/agents/setup-context.json
```

Run the rebuilt public Baseline refresh preview twice. It must report the tree
current both times. Independently read the canonical module and rendered guide
to confirm that a non-operative record grants no governed mutation and does
not itself refuse implementation, commit, or push.

Observed: two fresh invocations of `./bin/roundfix baseline update --repo .
--no-skills --format text` exited 0 with `Baseline update: current`, catalog
digest `sha256:733a35a2…`, Plan Digest `sha256:63be52d0…`, and `File changes:
0`. Both also reproduced the same two pre-existing nested-carrier warnings.
`rg` independently found the exact “grants no governed mutation” and “does not
by itself refuse implementation, commit, or push” clause in the canonical
module and rendered guide. No persistence mutation was expected; the second
fresh process confirmed convergence.

### QA-05

```yaml
inputs:
  - kind: repository_path
    ref: internal/daemon/task_engine_test.go
  - kind: repository_path
    ref: internal/gittest/gittest.go
```

Run the named seed-reuse guard, then rerun it in a fresh process. Inspect the
per-test fixture body for repository setup and exercise its isolation
assertions. The seed must be created once while each fixture retains a distinct
Git root and isolated graph and writes.

Observed: two sequential fresh invocations of `go test -count=1
./internal/daemon -run '^TestTaskCycleFixtureSeedIsCreatedOnce$'` each exited 0
with one test passed. The guard constructs two fixtures, requires one seed
creation and identical seed identity, then proves distinct Git roots, isolated
Task Graphs, isolated writes, and committed authorization provenance for both.
A fresh extraction of `newTaskCycleFixture` shows `taskCycleFixtureSeedForTest`
and `copyTo`; its body contains no `InitRepo`, `git add`, or `git commit` call.
Each test process starts from its own package state, providing the independent
confirmation relevant to this in-process `sync.Once` lifetime.

### QA-06

```yaml
inputs:
  - kind: repository_path
    ref: internal/daemon/task_engine_test.go
  - kind: repository_path
    ref: internal/spec/authorization.go
  - kind: repository_path
    ref: internal/authorization/authorization.go
```

Run the three named external and symlinked Spec Root journeys and the
missing-Git provenance refusal through fresh Go test processes. Confirm that
each still reaches the real Git authorization reader and that the refusal
survives a second invocation.

Observed: one fresh command across `internal/daemon` and `internal/spec`
executed the external Task-file settlement, external QA Report, symlink-crossing
commit, and unresolvable Spec Root refusal; all four passed. A second process
reran `TestOperationAuthorityReportsUnresolvableSpecRoot` alone and passed.
These fixtures initialize and commit real Git repositories and assert the
committed revision used by the authorization result. The focused diff from
`47c00f54` to HEAD shows no edit to authorization record parsing or provenance
resolution: `internal/spec/authorization.go` only adds a forwarding
`RequireGovernedOperation` wrapper, while `internal/authorization` adds that
operation gate beside the unchanged reader.

### QA-07

```yaml
inputs:
  - kind: external_repository
    ref: origin/main@91a17947da69be0c72d0400fc6f40b565ed47037
  - kind: repository_path
    ref: internal/daemon/**
  - kind: elapsed_time
    ref: one concurrent go test -count=1 ./internal/daemon per tree on 2026-09-13
```

Create a read-only archive of `origin/main`, record its exact commit, and run
the same one-package command against target and candidate with the same host
and Go cache policy. The candidate must fall in the target range instead of
roughly doubling it. Repeat or cross-check only to distinguish setup noise from
the package's measured runtime.

Observed outside evidence: `git archive` materialized
`origin/main@91a17947da69be0c72d0400fc6f40b565ed47037` under `/tmp`; that tree was
read-only and was not built by this Spec. Both trees received a compile-only
warm-up with the same `/tmp/roundfix-0133-go-cache`, then the exact command
`go test -count=1 ./internal/daemon` ran one package at a time on the same host.
In comparable full-access runs, `origin/main` passed in 4.470s and 3.388s
package time; the candidate passed in 7.092s and 6.593s. The confirming pair is
1.95×, so the candidate did not return to the target's range. The immediate
pre-Task base `47c00f54` passed in 7.704s, showing some improvement from the
fixture edit but not the promised target-range result. The first sandboxed
candidate run was discarded from comparison after a real commit test failed
because GPG could not access the keybox; both compared trees were rerun under
the same full-access boundary.

### QA-08

```yaml
inputs:
  - kind: repository_path
    ref: internal/daemon/task_engine_test.go
  - kind: repository_path
    ref: docs/specs/0133-a-fixture-that-does-not-spawn-per-test/task_02.md
```

Audit Task 02's exact commit, search its added lines for time budgets,
deadlines, parallelism, and skips, and run the complete Daemon package. Confirm
that the cost repair changes only the fixture test plus its Task record and
that all existing settlement, staging, and commit journeys still pass.

Observed: `git diff-tree` gives exactly the assigned Task file and
`internal/daemon/task_engine_test.go` for Task 02. The exact commit diff creates
the `sync.Once` seed, immutable `fstest.MapFS` snapshot/copy, and named guard;
it moves only the three Git setup calls out of `newTaskCycleFixture`. It adds no
`time.After`, deadline, timeout, concurrency configuration, `t.Skip`, or change
to an existing settlement, staging, or commit assertion. The full candidate
Daemon package passed in both full-access timing runs (7.092s and 6.593s), so
the unchanged journeys executed together rather than only as focused cases.

### QA-09

```yaml
inputs:
  - kind: repository_path
    ref: docs/specs/0133-a-fixture-that-does-not-spawn-per-test/_authorization.md
  - kind: repository_path
    ref: docs/agents/agent-instructions.md
  - kind: repository_path
    ref: docs/agents/setup-context.json
  - kind: repository_path
    ref: internal/baseline/assets/modules/core.json
  - kind: repository_path
    ref: internal/baseline/testdata/**
```

Use `git diff-tree` for the authorization and both Task commits, prove the
grant revision is an ancestor of Task 01, compare every Governed Path with the
bounded grant, and reproduce the sanctioned digest regeneration. A fresh
second regeneration must produce no delta.

Observed: `git diff-tree --no-commit-id --name-only -r` was executed for the
grant and both Task commits. The grant commit carries only this Spec's
authorization, PRD, and TechSpec. Task 01 carries the three exact bounded
Governed Paths, its assigned Task file, ordinary implementation/test paths,
and the deterministic Baseline outputs named by the sanctioned regeneration;
Task 02 carries only `internal/daemon/task_engine_test.go` and its assigned Task
file. Three `git merge-base --is-ancestor` checks establish
`91a17947 → 47c00f54 → 42130c78 → 9c7a82c0`, so authority is separate and
earlier. `make baseline-digests` exited 0 with
`{"type":"baseline-digests","ok":true,"changed":false}` and left no tracked
delta. No consequent-fix declaration exists; the seeded mechanical stage
preserved that detector as skipped.

### QA-10

```yaml
inputs:
  - kind: repository_path
    ref: "**"
```

Run `make verify` exactly, then run the repository's separate `make
verify-docs` Pull Request-boundary contract. Record each exit and every failed
subcheck without substituting focused tests.

### QA-11

```yaml
inputs:
  - kind: live_service
    ref: Open Pull Request for feat/spec-contained-authorization
  - kind: repository_path
    ref: "**"
```

The prompt proves that no Open Pull Request exists and Pull Request journeys
are environment-blocked. Record equivalent evidence for approval, checks and
status, unresolved review threads, Merge-Ready acceptance, and review-artifact
ancestry. This row remains blocked by environment but may support `pass` only
when all five controls have explicit observations.

Observed equivalent controls:

- Approval: the maintainer approved the exact three-path tooling grant on
  2026-09-13; no acceptance of the assembled change has been obtained.
- Checks and status: local `make verify` failed at F-001; local
  `make verify-docs` passed. The failed required check is recorded rather than
  inferred green.
- Unresolved review threads: no Pull Request or Spec-local review artifact
  exists, so no review thread has been created; none can be claimed resolved.
- Merge-Ready acceptance: no Review Source evidence exists yet.
- Review-artifact ancestry: no review artifact exists to bind to
  `9c7a82c0a4f5322fb743a6e9d05ebd15b5f5e105`.

The environment cause is the prompt's explicit `none open; Pull Request
journeys are environment-blocked`. Unblocking requires opening a Pull Request
on `feat/spec-contained-authorization`; this QA Task has no authority to do so.

## Findings

### F-001 — Full Verification stops on two pre-existing unformatted tests

- Impact: Blocks-Completion.
- Actor and step: maintainer, QA-10, run the repository's documented
  `make verify` gate.
- Expected: the complete Verification exits 0.
- Actual: exit 2 at `fmt-check`; the formatter named
  `internal/cli/baseline_skills_restore_test.go` and
  `internal/cli/baseline_assets_sync_test.go`.
- Reproduction: from audited HEAD, run `make verify`; the command stops before
  later checks. `git diff --exit-code 47c00f54..HEAD -- <both paths>` exits 0,
  and `git log -1 --oneline -- <both paths>` names `8d333a6c`, proving the
  failure predates both Spec Task commits.
- Affected rows: QA-10. QA-11 records the failed check as equivalent Pull
  Request status evidence; the functional rows remain runnable.

### F-002 — Candidate Daemon package remains outside the delivery-target range

- Impact: Friction.
- Actor and step: maintainer, QA-07, run the complete Daemon package once on
  the candidate and on the outside `origin/main` oracle.
- Expected: candidate wall clock returns to the target range rather than
  remaining roughly double it.
- Actual: under the same warmed Go cache and full-access Git-signing boundary,
  candidate package times were 7.092s and 6.593s; outside target times were
  4.470s and 3.388s. The confirming pair is 1.95×.
- Reproduction: archive
  `origin/main@91a17947da69be0c72d0400fc6f40b565ed47037`; warm both trees with
  `go test -run '^$' ./internal/daemon`; run `/usr/bin/time -p env
  GOCACHE=/tmp/roundfix-0133-go-cache go test -count=1 ./internal/daemon`
  against each tree. The immediate pre-Task control `47c00f54` measured 7.704s,
  so the edit improves that branch but does not meet the PRD target.
- Affected rows: QA-07 only. QA-05's structural once/reuse lock and QA-08's
  full package correctness still pass.

## Blocked and skipped

- QA-11 is blocked by environment because no Open Pull Request exists. The
  row carries equivalent observations for all required controls; the failed
  check and absent review acceptance remain visible. No row is finding-blocked,
  declared-blocked, or skipped by the QA Agent.

## Coverage

All 11 rows reached terminal status: 8 pass, 2 fail, and 1 blocked by
environment with equivalent evidence. The matrix covers all three PRD Goals,
all four User Stories, all seven Core Features, both declared intentional
breaks, the outside-evidence obligation, Non-Goals, tooling authority, full
Verification, and the Pull Request control sweep. The Task 03 wording says
“both” Goals, “both” User Stories, and “all four” Core Features; this gate
followed the live PRD's larger 3/4/7 inventory so no current promise was
omitted.

Blocked counters: `rows_blocked_environment: 1`,
`rows_blocked_finding: 0`, `rows_blocked_declared: 0`. No QA row was skipped.
Relevant behavior probes covered absent, proposed, withdrawn, contradictory,
and empty-path records; ordinary and Governed Path mutations; repeated fresh
processes; distinct fixture copies and isolated writes; external, symlinked,
and missing-Git Spec Roots; repeat Baseline previews; and two like-for-like
timing pairs.

## Mechanical skips

| Detector | Missing artifact |
| --- | --- |
| consequent-fix commit order | consequent-fix declaration |
| QA Report structure | QA Report |
| QA evidence paths | QA Report |

These skips were materialized before the QA Agent received the seeded report.
They remain the mechanical stage's historical output; the Agent does not
reclassify or remove them.

## Final verdict

Fail: 8 rows passed, 2 failed, and 1 was environment-blocked with equivalent
evidence; impact totals are 1 Blocks-Completion and 1 Friction. Before rerun,
the repository's two pre-existing formatter failures must be corrected so
`make verify` reaches exit 0, and the candidate Daemon package must return to
the measured `origin/main` range rather than the confirmed 1.95× result.
