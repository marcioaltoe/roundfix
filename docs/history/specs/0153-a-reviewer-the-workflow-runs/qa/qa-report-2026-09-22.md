---
spec: 0153-a-reviewer-the-workflow-runs
date: 2026-09-22
build: ba06fb3d9952b3f4c06e05652a6cb61e719588ec
auditing_binary: "0.15.0 (af0d4810, built 2026-09-22 12:24:51 -0300)"
auditor_staleness: "current: commit ancestry: build commit does not predate audited tree"
status: closed
verdict: pass
rows_blocked_environment: 1
rows_blocked_finding: 0
rows_blocked_declared: 0
surfaces: [backend, cli, docs]
---

# QA Report

## Scope and environment

Full terminal QA gate for commit `ba06fb3d9952b3f4c06e05652a6cb61e719588ec` in the Run Worktree. The actors are an operator invoking `roundfix review`, a Spec author checking promise coverage, and a maintainer auditing the candidate for delivery. No Pull Request is open, so that row uses the required environment-blocked equivalent-evidence path. No real reviewer session, paid API call, commit, push, or Pull Request mutation is permitted.

The strict authoring precondition passed with the current auditing binary:

```text
roundfix spec check 0153-a-reviewer-the-workflow-runs --strict
Spec 0153-a-reviewer-the-workflow-runs
No findings. Authored Verification commands were not executed.
```

The checker skipped only detectors whose optional or project-family inputs are absent: a Findings Rollup, active Backlog Entry, TechSpec Vocabulary Contract, and references index. None replaces a declared QA row.

## Static gate

The Daemon ran `make verify` outside the Agent sandbox for this Run Worktree and reported exit 0. Diagnostics were removed on success. Per the QA contract, the Agent recorded that supervised result and did not rerun repository Verification.

## Performed repairs

None.

## Assigned repair failures

None.

## Authorization audit inputs

| Task | Outcome | Record | Revision | Detail |
| --- | --- | --- | --- | --- |
| task_03 | granted | docs/specs/0153-a-reviewer-the-workflow-runs/_authorization.md | 7f13b7aebf7884471ed8cec9ddb1b6c5ffd6f616 | operative grant |
| task_04 | granted | docs/specs/0153-a-reviewer-the-workflow-runs/_authorization.md | 7f13b7aebf7884471ed8cec9ddb1b6c5ffd6f616 | operative grant |

## Mechanical findings

None.

## Results

| # | Story / criterion / sweep | Actor and surface | Status | Evidence |
| - | --- | --- | --- | --- |
| 1 | Daemon repository Verification | Maintainer; repository gate | pass | Daemon-supervised `make verify`: exit 0; diagnostics removed on success |
| 2 | Codex receives the candidate diff, runs read-only, and records repository/base/head | Operator; CLI and backend | pass | Focused public-runner and ACPX-boundary tests passed |
| 3 | Explicit `none` omits without reviewer activity and exits zero | Operator; CLI and backend | pass | Activity-counting public-runner test passed |
| 4 | Every selected-mode failure remains blocked; anomaly beats clean text; timeout has no fallback | Operator; CLI and backend | pass | Focused failure and ACPX classification matrices passed |
| 5 | `claude` and `coderabbit` refuse by provider name | Operator; CLI | pass | Both provider cases passed |
| 6 | Verification spends no paid API use and starts no real reviewer | Maintainer; test and Task contracts | pass | Authored-command audit and stubbed runner boundary passed |
| 7 | Shipped skill, mirror, and user guide describe the command truthfully | Maintainer; docs and CLI contract | pass | Skill check, byte comparison, and content assertions passed |
| 8 | Task 03 governed mutation stayed inside prior bounded authority | Maintainer; Git history | pass | Exact commit paths and grant ancestry passed |
| 9 | Task 04 governed mutations stayed inside prior bounded authority and sanctioned regeneration | Maintainer; Git history | pass | Exact commit paths, grant ancestry, and mirror identity passed |
| 10 | This Spec's artifacts satisfy the promise rule | Spec author; checker CLI | pass | Strict current-tree Spec check reported no findings |
| 11 | Existing pre-PR policy contract remains the command's external foundation | Operator; config and CLI | pass | Pre-Spec policy provenance, unchanged config, and integration tests passed |
| 12 | Pull Request controls through equivalent evidence | Maintainer; Pull Request journey | blocked (environment: no open Pull Request) | Prompt fact plus equivalent evidence for all five Pull Request controls |

## Row evidence

### 1

Provenance: Requirement 1; repository Verification.

```yaml
inputs:
  - kind: repository_path
    ref: "**"
```

- Steps: record the Daemon-supervised repository Verification for the audited head; do not rerun it.
- Expected observable: `make verify` exits 0.
- Independent confirmation: the Daemon supplies the exact command, verdict, and absence of retained success diagnostics.
- Persistence check: bind the result to the audited Run Worktree head.
- Evidence path: this report's Static gate section.

Observed: pass. The supervised command was `make verify`; verdict exit 0; no success diagnostics remained. The result is bound to commit `ba06fb3d9952b3f4c06e05652a6cb61e719588ec` and was not re-derived.

### 2

Provenance: Requirement 2.

```yaml
inputs:
  - kind: repository_path
    ref: internal/cli/review.go
  - kind: repository_path
    ref: internal/cli/review_test.go
  - kind: repository_path
    ref: internal/agent/agent.go
  - kind: repository_path
    ref: internal/agent/acpx_runner.go
  - kind: repository_path
    ref: internal/agent/acpx_runner_test.go
```

- Steps: execute the focused CLI and runtime-boundary tests for candidate-diff prompt content, read-only session permissions, and the persisted clean record.
- Expected observable: the prompt contains the actual diff, the runner request permits reads but not writes, and the record names repository, base, and head.
- Independent confirmation: the fixture repository, captured runner request, ACPX argument boundary, and decoded persisted record are separate observations.
- Persistence check: each test creates fresh repositories and artifacts.
- Evidence path: this report and the named implementation/test inputs.

Observed: pass. Fresh focused tests passed `TestReviewPromptCarriesTheCandidateDiff`, `TestReviewSessionReadsWithoutWriting`, `TestReviewCommandExitsZeroOnExplicitClean`, `TestReviewCommandDefaultsBaseToMain`, the four-outcome record round trip, and the missing-head/invalid-detail refusals. `TestACPXPromptArgsPlaceGlobalsBeforeAgentAndSubcommand/read-only_session` independently passed at the runtime argument boundary. The fixture prompt contained its actual `diff --git` content, the captured request exposed read-only capabilities, and the decoded record asserted repository, base, and head.

### 3

Provenance: Requirement 3.

```yaml
inputs:
  - kind: repository_path
    ref: internal/cli/review.go
  - kind: repository_path
    ref: internal/cli/review_test.go
```

- Steps: execute the `none` policy public-runner test with a stub counting probe, prepare, and prompt activity; decode the persisted record.
- Expected observable: exit 0, outcome `omitted`, and zero reviewer calls or readiness probes.
- Independent confirmation: both the process result and the stub's activity counters must agree.
- Persistence check: the test creates a fresh artifact directory and record.
- Evidence path: this report and `internal/cli/review_test.go`.

Observed: pass. `TestReviewCommandNoneRecordsOmissionWithoutAgentActivity` exited successfully. Its fresh fixture decoded `omitted`, asserted exit 0, and observed zero probe, prepare, and prompt calls on the stub.

### 4

Provenance: Requirement 4.

```yaml
inputs:
  - kind: repository_path
    ref: internal/cli/review.go
  - kind: repository_path
    ref: internal/cli/review_test.go
  - kind: repository_path
    ref: internal/agent/acpx_runner.go
  - kind: repository_path
    ref: internal/agent/acpx_runner_test.go
```

- Steps: execute the focused runtime-failure, timeout, transport-anomaly, empty-output, unclassifiable-output, protocol-stream, and fallback tests.
- Expected observable: every named failure exits 2, records `blocked`, and names its reason; a clean-looking message cannot override an anomaly; timeout causes no fallback.
- Independent confirmation: decoded records, stderr diagnostics, exit codes, and runner activity counts must agree.
- Persistence check: every case creates fresh fixtures and artifacts.
- Evidence path: this report and the named test inputs.

Observed: pass. Fresh tests passed runtime failure, prompt failure without fallback, timeout, transport anomaly, empty output, Agent-message-versus-protocol classification, selection-only fallback, writable-artifact preflight, and unknown-flag refusal. `TestReviewCommandBlocksOnTransportAnomaly` proved a clean-looking message cannot override a non-empty anomaly. `TestReviewCommandKeepsATimeoutBlocked` proved timeout remains blocked without fallback. The ACPX prompt-exit classification matrix also passed all eight terminal-signal cases.

### 5

Provenance: Requirement 5.

```yaml
inputs:
  - kind: repository_path
    ref: internal/cli/review.go
  - kind: repository_path
    ref: internal/cli/review_test.go
```

- Steps: execute the public-runner table cases for `claude` and `coderabbit`.
- Expected observable: each exits 2, names the selected provider, records `blocked`, and never records `omitted`.
- Independent confirmation: process result, persisted record, and zero Agent Runtime activity agree.
- Persistence check: each provider case uses a fresh artifact directory.
- Evidence path: this report and `internal/cli/review_test.go`.

Observed: pass. `TestReviewCommandRefusesUnimplementedProvider` passed for both `claude` and `coderabbit`; each case asserted exit 2, the selected provider in the diagnostic, a persisted `blocked` outcome, and no Agent Runtime activity.

### 6

Provenance: Requirement 6.

```yaml
inputs:
  - kind: repository_path
    ref: docs/specs/0153-a-reviewer-the-workflow-runs/task_*.md
  - kind: repository_path
    ref: internal/cli/review_test.go
  - kind: repository_path
    ref: internal/agent/acpx_runner_test.go
```

- Steps: inspect every authored Verification command for external reviewer invocation, execute only stubbed focused tests, and confirm those tests inject a fake `agent.Runner` rather than constructing the production reviewer.
- Expected observable: no Verification command invokes `roundfix review`, Codex, or another live reviewer; all exercised review paths terminate at the test stub.
- Independent confirmation: command-text search and captured fake-runner activity establish separate boundaries.
- Persistence check: bind the command audit to the current Task artifacts and test tree.
- Evidence path: this report, Task Verification sections, and named test inputs.

Observed: pass. Every authored Verification command was inspected. Tasks 01-03 invoke only Go tests and local help, Task 04 invokes local content checks and `roundfix skills check`, and Task 05 reads the report verdict; none invokes `roundfix review`, Codex, ACPX, or another live reviewer. The fresh review tests inject `recordingReviewRunner` or `reviewCommandRunner` through the `agent.Runner` boundary. All focused review paths in rows 2-5 completed against those stubs, so no real Agent Session or paid API was started.

### 7

Provenance: Requirement 7.

```yaml
inputs:
  - kind: repository_path
    ref: .agents/skills/roundfix/SKILL.md
  - kind: repository_path
    ref: skills/roundfix/SKILL.md
  - kind: repository_path
    ref: docs/user-guide/commands.md
  - kind: repository_path
    ref: docs/user-guide/configuration.md
  - kind: repository_path
    ref: docs/user-guide/usage.md
```

- Steps: run the shipped skill check, compare canonical and distributed skill bytes, and assert the command, option, exits, omission path, blocking cases, and unsupported providers appear in the documented surfaces.
- Expected observable: skill validation passes, mirrors match, and no stale unenforced-policy wording remains.
- Independent confirmation: CLI validation, byte comparison, and content searches are distinct observations.
- Persistence check: bind results to current repository paths.
- Evidence path: this report and the named docs/skill inputs.

Observed: pass. Current-tree `roundfix skills check` reported that the Roundfix skill and all related workflow skills passed. `cmp .agents/skills/roundfix/SKILL.md skills/roundfix/SKILL.md` exited 0. The built public entry point `go run -buildvcs=false ./cmd/roundfix review --help` exited 0 and printed `roundfix review [--base <ref>]` with the default-base contract. Both skill copies document diff-in-prompt, read-only execution, repository/base/head evidence, exit codes 0/1/2, explicit `none`, all five blocking classes, selection-only fallback, and `claude`/`coderabbit` refusal. The user guide carries the same contract, and the stale unenforced-policy search returned no matches as expected.

### 8

Provenance: Requirement 7.

```yaml
inputs:
  - kind: repository_path
    ref: docs/specs/0153-a-reviewer-the-workflow-runs/_authorization.md
  - kind: repository_path
    ref: internal/speccheck/governed.go
  - kind: repository_path
    ref: .git
```

- Steps: enumerate Task 03's committed paths with `git diff-tree`, intersect them with the governed set, and prove the operative authorization commit is a strict ancestor and bounds every governed path.
- Expected observable: only `internal/cli/cli_test.go` is governed, it is authorized, and the grant is not folded into the Task commit.
- Independent confirmation: immutable Git objects and the literal governed-path set decide the result rather than Task prose.
- Persistence check: bind observations to exact commit IDs.
- Evidence path: this report, the authorization record, governed-path source, and Git objects.

Observed: pass. `git diff-tree --no-commit-id --name-status -r 4beb55f1` established Task 03's exact committed paths: its Task file; ordinary source/test paths under `internal/agent` and `internal/cli`; and governed `internal/cli/cli_test.go`. The current literal governed set and `_authorization.md` agree that `internal/cli/cli_test.go` is bounded. `git merge-base --is-ancestor 7f13b7ae 4beb55f1` exited 0, proving the operative grant predates the Task commit and was not folded into it. No other Task 03 path is governed.

### 9

Provenance: Requirement 7.

```yaml
inputs:
  - kind: repository_path
    ref: docs/specs/0153-a-reviewer-the-workflow-runs/_authorization.md
  - kind: repository_path
    ref: internal/speccheck/governed.go
  - kind: repository_path
    ref: .agents/skills/roundfix/SKILL.md
  - kind: repository_path
    ref: skills/roundfix/SKILL.md
  - kind: repository_path
    ref: .git
```

- Steps: enumerate Task 04's committed paths with `git diff-tree`, prove authorization ancestry, compare every governed path to the bounded list, and confirm the regenerated mirror matches the canonical skill.
- Expected observable: both skill paths are governed and authorized before the Task commit; no governed path falls outside the grant; deterministic mirror output matches.
- Independent confirmation: Git history, the governed set, and byte comparison decide the result.
- Persistence check: bind observations to exact commit IDs.
- Evidence path: this report, the authorization record, governed-path source, and Git objects.

Observed: pass. `git diff-tree --no-commit-id --name-status -r ba06fb3d` established Task 04's exact committed paths: its Task file, three ordinary user-guide files, and the two governed skill paths. Both skill paths are exactly bounded in `_authorization.md`; `git merge-base --is-ancestor 7f13b7ae ba06fb3d` exited 0. No derived version pin changed. The sanctioned regeneration's deterministic result is independently confirmed by the byte-identical canonical and distributed skills and the passing skill check.

### 10

Provenance: Requirement 8.

```yaml
inputs:
  - kind: repository_path
    ref: docs/specs/0153-a-reviewer-the-workflow-runs/**
  - kind: repository_path
    ref: internal/speccheck/**
```

- Steps: rerun `roundfix spec check 0153-a-reviewer-the-workflow-runs --strict` after the matrix exists.
- Expected observable: no promise-rule finding; authored Verification commands remain unexecuted by the checker.
- Independent confirmation: the checker parses declared promises and consuming Tasks from committed artifacts.
- Persistence check: rerun against the same audited head.
- Evidence path: this report and the Spec artifacts.

Observed: pass. After the matrix existed, `roundfix spec check 0153-a-reviewer-the-workflow-runs --strict` again reported `No findings. Authored Verification commands were not executed.` Its skipped detectors concern absent project-family or optional artifacts and do not hide the promise-to-Task rule or replace a matrix row.

### 11

Provenance: outside-evidence row.

```yaml
inputs:
  - kind: repository_path
    ref: docs/history/specs/0143-a-repository-says-who-reviews-before-the-pull-request/**
  - kind: repository_path
    ref: internal/config/config.go
  - kind: repository_path
    ref: internal/config/config_test.go
  - kind: repository_path
    ref: internal/cli/review_test.go
  - kind: repository_path
    ref: .git
```

- Steps: identify the pre-existing policy contract and its pre-Spec Git provenance, run its focused precedence/default tests, then run the current command integration that consumes the resolved policy.
- Expected observable: the existing `codex, claude, coderabbit, none` policy and precedence remain unchanged while `review` consumes the resolved value.
- Independent confirmation: Spec 0143 artifacts and commits predate this Spec and were not authored by it.
- Persistence check: bind outside evidence to immutable pre-Spec commits and rerun current tests from fresh fixtures.
- Evidence path: this report, archived Spec 0143, the config tests, and Git objects.

Observed: pass. The external source is archived Spec 0143 and the policy implementation it delivered in commit `e36746876a257f0af1e135a3495b56dd5162fdf8` on 2026-09-18, before this Spec's target commit `7f13b7aebf7884471ed8cec9ddb1b6c5ffd6f616` on 2026-09-22. `git diff --exit-code 7f13b7ae..HEAD -- internal/config/config.go internal/config/config_test.go` exited 0, proving this Spec did not alter that policy. Fresh `TestPrePRReviewPolicyResolution` cases passed the built-in Codex default, user/project precedence, explicit `none`, explicit `codex`, and invalid-value refusals. The current command tests in rows 2-5 then proved `review` consumes those resolved values.

### 12

Provenance: Pull Request row.

```yaml
inputs:
  - kind: live_service
    ref: Pull Request for feat/0153-codex-reviews-the-candidate
  - kind: repository_path
    ref: "**"
```

- Steps: because the prompt confirms no Pull Request is open, record the cause and inspect equivalent evidence for approval, checks/status, unresolved threads, Merge-Ready acceptance, and review-artifact ancestry.
- Expected observable: the row records every control and identifies absent evidence without inferring unavailable Pull Request state.
- Independent confirmation: the prompt supplies the no-PR fact; repository and Run facts supply equivalent evidence.
- Persistence check: environment-blocked live-service rows are not carry-forward eligible.
- Evidence path: this report's Blocked and skipped section.

Observed: blocked by the environment fact in the QA prompt. Equivalent evidence for approval, checks/status, unresolved review threads, Merge-Ready acceptance, and review-artifact ancestry is recorded below; no unavailable Pull Request state is inferred.

## Findings

None.

## Blocked and skipped

Row 12 is `blocked (environment: no open Pull Request)`. The prompt confirms that no Pull Request is open and that Pull Request journeys are environment-blocked. Equivalent evidence:

- Approval: no Pull Request approval was obtained.
- Checks and status: the Daemon ran `make verify` for audited head `ba06fb3d9952b3f4c06e05652a6cb61e719588ec` and reported exit 0.
- Unresolved review threads: no Pull Request exists, so no Pull Request review thread stands.
- Merge-Ready acceptance: no Review Source evidence exists yet; no Merge-Ready claim is made.
- Review-artifact ancestry: no review artifact exists yet; no evidence from another head is credited.

Unblocking action: open a Pull Request from `feat/0153-codex-reviews-the-candidate`, then observe its approval, checks, review threads, Merge-Ready evidence, and review-artifact ancestry read-only. No rows were skipped, finding-blocked, or declared-blocked.

## Coverage

All 11 coverage sources appear in the matrix: Requirements 1-8, repository Verification, the outside-evidence row, and the Pull Request row. Eleven rows passed. One row is environment-blocked with its cause, unblocking action, and equivalent evidence recorded. `rows_blocked_environment: 1`, `rows_blocked_finding: 0`, and `rows_blocked_declared: 0`. Requirement 7 is intentionally split across documentation and the two commit-dependent tooling audits. No frontend sweep or Unreachable Acceptance declaration applies, and no row covers a source outside this set.

## Final verdict

Pass: all 11 runnable rows passed, the one environment-blocked Pull Request row records its cause and equivalent evidence for every control, and no row failed, was skipped, or was finding- or declared-blocked. Findings by impact: Blocks-Completion 0, Data-Loss 0, Trust-Damage 0, Friction 0, Cosmetic 0.

## Mechanical skips

| Detector | Missing artifact |
| --- | --- |
| consequent-fix commit order | consequent-fix declaration |
| QA Report structure | QA Report |
| QA evidence paths | QA Report |
