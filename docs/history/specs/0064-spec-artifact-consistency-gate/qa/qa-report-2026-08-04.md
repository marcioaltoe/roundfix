---
spec: 0064-spec-artifact-consistency-gate
date: 2026-08-04
build: 121fabd5ade78c5c7ea6e899dcf022cfc3810b03
status: closed
verdict: fail
rows_blocked_environment: 0
rows_blocked_finding: 0
surfaces: [backend, cli, docs]
---

# QA report — Spec artifact consistency gate

## Scope and environment

Full rerun of the authored terminal `task_09` gate after corrective tasks 10
and 11. The actor was a Spec author, automation user, contributor, or
repository maintainer using the built `roundfix` CLI and documented Make
targets on macOS. Public entry points were `bin/roundfix spec check`,
top-level and command help, `make spec-budget`, and `make verify`.

The Run Worktree branch was
`roundfix/run-run_20260804T135149Z_5756ba1bc1530250`; the tested source commit
was `121fabd5ade78c5c7ea6e899dcf022cfc3810b03`. `make verify` rebuilt
`bin/roundfix` as `121fabd-dirty` because this in-progress report was the only
worktree delta. Fixture journeys used an uncommitted scratch Git repository
under `/private/tmp/roundfix-0064-qa.hvmBrT` with branch
`ma/qa-fixture`, copied from `internal/speccheck/testdata/repo`. No target
repository artifact was copied back or changed.

The prompt reports no Open Pull Request and states that Pull Request journeys
are environment-blocked. This PRD declares no Pull Request surface or
Pull Request-dependent acceptance criterion, so no matrix row depends on one.

### Project Constraint and graph preflight

- The manifest names `task_09` as the only `type: qa` node. It is terminal and
  depends on completed `task_11`, which transitively covers every non-QA node.
- Both active Spec artifacts carry all four Project Constraint row types and
  cite operative files under `docs/agents/`. The TechSpec's Active ADR row is
  incomplete after the corrective amendment; F-001 records the discrepancy.
- Authorization commit `2e560ce` names Spec 0064 and bounds `Makefile` to the
  local-gate work required by the Spec. It predates task 08 commit `02c35e2`
  and corrective task 11 commit `121fabd`.
- `git diff-tree --no-commit-id --name-only -r <commit>` was run for the
  authoring commit and every implementation Task commit. Task 08 changed only
  `Makefile` and `task_08.md`; corrective authoring commit `c4e7613` landed
  before task 10 and task 11; task 10 commit `cab533f` changed only its Task
  file and the characterization test; task 11 changed only `Makefile` and its
  Task file. No prerequisite or consequent tooling fix was folded into or
  ordered around either tooling Task.
- Final `git -c core.fsmonitor=false status --short` listed only this untracked
  QA report.

## Static gate

`rtk make verify` exited 0 without a pipe. Its executed stages reported:

```text
rtk go test -parallel 16 ./...
Go test: 3198 passed in 25 packages
rtk go test -count=1 -parallel=1 ./internal/speccheck -run '^TestCheckCorpusBudget$'
Go test: 1 passed in 1 packages
Roundfix skill check passed: ... qa-gate, evidence-gate
rtk go build -buildvcs=false ... -o bin/roundfix ./cmd/roundfix
bin/roundfix spec check
Spec 0064-spec-artifact-consistency-gate
No findings.
```

The complete active-corpus command exited 0 and reported no finding for every
active Spec. Expected presence-aware detector skips remained visible.

## Results

| # | Story / criterion / sweep | Actor, entry point, and surface | Status | Evidence |
| --- | --- | --- | --- | --- |
| Q-01 | Authored-gate precondition; Project Constraints; tooling authorization and commit choreography | Maintainer; Spec artifacts and Git ancestry; docs | fail | Graph, status, all Task commit paths, authorization `2e560ce`, and tooling commits `02c35e2`/`121fabd` passed. F-001: `_techspec.md:47` omits governing ADR-0089 while `_techspec.md:275` invokes it and `_prd.md:38` accounts for it. |
| Q-02 | Core Feature 1; clean named-Spec text journey; task 09 clean exit | Spec author; `bin/roundfix spec check 0064-spec-artifact-consistency-gate`; cli/backend | pass | Two initial runs and three recovery runs exited 0, reported `No findings`, recorded only expected skips, and produced no diagnostic. The final target-Spec hashes matched the pre-flow hashes byte-for-byte. |
| Q-03 | Core Feature 1; all-active sweep; Goals 1 and 2 | Maintainer; `bin/roundfix spec check`; cli/backend | pass | The no-slug command ran in `make verify` and again directly. Both exited 0, named every active Spec once, reported no findings, and preserved recorded skips. |
| Q-04 | Core Feature 1; task 04 JSON acceptance | Automation user; `--format json`; cli/backend | pass | Explicit 0064+0075 run exited 0 with two JSONL objects. `jq -e` parsed both as `roundfix-speccheck/v1` with the requested slugs; a second run was byte-identical by `cmp`. Flags before and after a slug both produced valid JSON. |
| Q-05 | Core Features 2 and 7; task 09 error exit | Spec author; built CLI against scratch fixture repository; cli/backend | pass | `spec check tooling-unauthorized` exited 1 twice with `SC-TOOLING-UNAUTHORIZED`, severity `error`, locations at the PRD and authorization record, and a concrete fix. Other dirty fixtures also supplied two located sides. |
| Q-06 | Core Feature 4; gap semantics; task 09 gap exit | Spec author; built CLI against isolated gap-only fixture; cli/backend | pass | Default gap-only run exited 0 with `[gap] SC-ADR-RELATED`; `--strict` exited 1 and rendered it `[error]`; the final default run returned to exit 0 and `[gap]`, proving no persisted promotion. |
| Q-07 | Core Feature 3; Project Constraint and tooling detectors | Spec author; built CLI against scratch fixtures; cli/backend | pass | Missing row, unreasoned row, missing source, unauthorized record, and unbounded tooling each exited 1 with their named `SC-*` error and locations. `clean` and `no-techspec` exited 0; the latter recorded every TechSpec-dependent detector as skipped. |
| Q-08 | Core Feature 5; ADR, coverage, and reference resolution | Spec author; built CLI against scratch fixtures; cli/backend | pass | Gap-only citation, four unmapped features, one untasked feature, an unresolved Task Context path, and valid collective ranges produced the expected gap/error/clean exits and located both sides. The 0056 replay independently exercised direct `SC-ADR-UNLISTED`. |
| Q-09 | Core Feature 6; emitted-vocabulary documentation | Spec author; built CLI against scratch fixtures; cli/backend | pass | Five emitted/four documented reported only `publish:` once although emitted three times. Satisfied and absent-contract fixtures exited 0; invalid RE2 and unreadable emitting/documenting paths returned located findings without panic. Every fix included `emits`, `pattern`, and `documented-in`. |
| Q-10 | Success Metric 1; four motivating finding replays | Maintainer; built CLI against replay fixtures; cli/backend | pass | 0058 QA-001 → `SC-COVERAGE-UNMAPPED`; 0058 QA-004 → `SC-VOCABULARY-UNDOCUMENTED`; 0056 F-001 → `SC-ADR-UNLISTED` plus `SC-ADR-RELATED`; 0056 F-002 → `SC-COVERAGE-UNMAPPED`. All four provenance READMEs name the source report and disclose report-authored, not Git-recovered, fixtures. |
| Q-11 | Success Metrics 2 and 3; controlled corpus budget and observable archived counts | Maintainer; `make spec-budget` and corpus checks; backend/infra | pass | `rtk make spec-budget` exited 0 and ran one test. Two dedicated runs measured 321.223459 ms and 307.6765 ms against 1 s. Active-corpus test exited 0. Golden active counts are all 0; archived counts include 35 related ADR gaps, 35 unlisted ADR errors, 320 missing Constraints, 424 unmapped units, and 183 unresolved references. |
| Q-12 | CLI and docs sweep; discoverability and canonical vocabulary | New Spec author; top-level help, command help, `make help`, `CONTEXT.md`; cli/docs | pass | Public help lists the command, both formats, strict promotion, exit codes 0/1/2, and the read-only contract. `make help` lists `spec-check` and `spec-budget`; glossary terms distinguish check findings from QA verdicts. |
| Q-13 | CLI failure and recovery sweep | Spec author; public CLI; cli | pass | Unknown slug, `--format yaml`, and `--wat` each exited 2, left captured stdout empty, named the bad input on stderr, and suggested command help. A valid 0064 check immediately after each failure exited 0. Interspersed supported flags worked in both positions. |
| Q-14 | Non-Goals and scope-creep check | Maintainer; public CLI plus hashes and help; cli/docs | pass | Pre/post SHA-1 sets for all 14 Spec artifacts were identical. Explicit absence checks found no QA-verdict output and no consistency precondition in `implement --help`. Help promises declaration/citation checking without mutation; no auto-correction or style judgment is exposed. |
| Q-15 | Mandatory repository gate and Makefile integration; regression of prior F-001 | Contributor; `make verify`; cli/backend/infra | pass | Full gate exited 0. `verify` orders ordinary tests, `spec-budget`, skill checks, build, then `spec-check`; both new targets are phony and documented. Ordinary tests passed without the old contended duration failure, and the dedicated proof measured rather than skipped. |

## Findings

### F-001 — TechSpec Active ADR obligations omit the corrective budget decision

- Impact: Trust-Damage.
- Actor and journey: repository maintainer performing Q-01 before crediting
  the Spec's Task evidence.
- Expected: each active, non-legacy PRD and TechSpec accounts for every ADR
  governing changed behavior in its Project Constraints snapshot.
- Actual: `_prd.md:38` lists ADR-0089 as governing the controlled budget
  proof, and `_techspec.md:275` cites ADR-0089 for the same execution
  conditions, but `_techspec.md:47-57` omits it from that artifact's Active ADR
  obligations row. The public `spec check 0064-spec-artifact-consistency-gate`
  nevertheless exits 0 because the PRD row accounts for the citation.
- Reproduction:
  1. Run `rtk bin/roundfix spec check 0064-spec-artifact-consistency-gate`;
     observe exit 0 and `No findings`.
  2. Read `_techspec.md` Project Constraints at line 47; observe the active
     list omits ADR-0089.
  3. Read the same file at line 275; observe the budget proof explicitly
     invokes ADR-0089.
  4. Read `_prd.md` at line 38; observe that the PRD correctly lists ADR-0089
     as governing.
- Evidence: Q-01 plus the three cited locations above.
- Affected rows: Q-01.
- Required repair: add ADR-0089 and its budget-execution obligation to the
  TechSpec's Active ADR obligations row. Then rerun `make verify`, Q-01, and
  the adjacent named-Spec CLI canary.

## Blocked and skipped

No row was blocked or skipped. The prompt's no-Open-Pull-Request fact affects
no declared surface or acceptance criterion in this Spec.

## Coverage

Planned and executed: 15 rows covering all seven Core Features, all three
Success Metrics, all four Non-Goals, task 09 acceptance, and the CLI/backend/
docs surfaces. Behavior probes covered repeatability, strict-state leakage,
invalid input and recovery, interspersed flags, missing artifacts, repeated
vocabulary, unreadable declarations, read-only persistence, JSON stability,
and controlled budget measurement.

Terminal counts: 14 pass, 1 fail, 0 blocked by environment, 0 blocked by
finding, 0 skipped, 0 pending. One Trust-Damage finding is open. The prior
2026-08-03 budget finding did not reproduce and its static-gate and adjacent
canary rows passed.

## Final verdict

Fail: account for ADR-0089 in the TechSpec's Active ADR obligations row, then
rerun the Project Constraint audit, `make verify`, and the named-Spec canary.
