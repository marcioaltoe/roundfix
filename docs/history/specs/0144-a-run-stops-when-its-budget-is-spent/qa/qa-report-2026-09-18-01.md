---
spec: 0144-a-run-stops-when-its-budget-is-spent
date: 2026-09-18
build: b417737ab2affa61bdd4d8d9df21b61fba0a1634
auditing_binary: "0.13.1 (5c40a9d6, built 2026-09-18 09:03:38 -0300)"
auditor_staleness: "stale: commit ancestry: build commit predates audited tree"
status: closed
verdict: pass
rows_blocked_environment: 1
rows_blocked_finding: 0
rows_blocked_declared: 0
surfaces: [backend, cli, docs]
---

# QA Report — A Run stops when its budget is spent

## Scope and environment

Full terminal gate for build `b417737ab2affa61bdd4d8d9df21b61fba0a1634`
on the Run Worktree branch. The actor is a repository maintainer using the
Roundfix CLI. The strict authoring precondition passed with `roundfix spec check
0144-a-run-stops-when-its-budget-is-spent --strict`; it reported no findings
and did not execute authored Verification commands. The auditing binary is
stale by commit ancestry, so its identity is recorded separately from the
audited build.

The QA assignment proves that no Pull Request is open and declares Pull Request
journeys environment-blocked. Repository Verification was already run by the
Daemon outside the Agent sandbox and must not be rerun here.

## Static gate

- `make verify`: passed with exit 0. The Daemon ran this outside the Agent
  sandbox before this gate and removed diagnostics on success. Per the QA
  contract, this Agent recorded the fact and did not rerun the command.

## Performed repairs

None.

## Assigned repair failures

None.

## Authorization audit inputs

| Task | Outcome | Record | Revision | Detail |
| --- | --- | --- | --- | --- |
| task_06 | granted | docs/specs/0144-a-run-stops-when-its-budget-is-spent/_authorization.md | 5c40a9d6a3c3fa035f1e2b3d5f21691015759000 | operative grant |

## Mechanical findings

None.

## Results

| # | Story / criterion / sweep | Actor and surface | Status | Provenance |
| - | --- | --- | --- | --- |
| R1 | Repository Verification | Daemon, repository gate | pass | Requirement 1; repository Verification |
| R2 | Enabled budget bounds the whole Run, cancels owned work, and settles with the existing outcome and reason | Maintainer, backend and CLI | pass | Requirement 2 |
| R3 | Disabled budget preserves current settlement | Maintainer, backend and CLI | pass | Requirement 3 |
| R4 | Carry-forward accepts proved budget-expired work and refuses unproved or unsupported work | Maintainer, CLI | pass | Requirement 4 |
| R5 | Canonical and mirrored Roundfix skills state the bounded Run contract and match | Maintainer, docs | pass | Requirement 5 |
| R6 | Existing watch-loop rule and Run Database outcome are independently exercised | Maintainer, backend | pass | Requirement 6; outside-evidence row |
| R7 | Spec promises and Task commits satisfy the authored contract and authority boundary | Maintainer, repository governance | pass | Requirement 7 |
| R8 | Pull Request controls have equivalent evidence while no Pull Request exists | Maintainer, Pull Request journey | blocked (environment: no open Pull Request) | Pull Request row |

## Row evidence

### R1

```yaml
inputs:
  - kind: repository_path
    ref: Makefile
  - kind: repository_path
    ref: internal/**
  - kind: repository_path
    ref: docs/**
```

Expected: the repository Verification exits 0. Independent confirmation is
the Daemon's recorded exit status. Persistence is not applicable to a static
gate.

Evidence: the Daemon recorded `make verify` exit 0 and removed its diagnostic
artifact on success. This is the supervised static-gate result supplied in the
QA assignment; the command was not rerun.

### R2

```yaml
inputs:
  - kind: repository_path
    ref: internal/daemon/**
  - kind: repository_path
    ref: internal/cli/**
```

Steps: exercise expiring enabled Runs through Worktree setup, Task Worktree
bootstrap, the task cycle, integration and push. Expected: bounded work is
cancelled with termination proof, no later Task starts, completed Task state
remains, the Run settles `BudgetExceeded`, the reason names the configured
maximum and elapsed time, and the Run Worktree and Run Branch remain.
Independent confirmation: CLI-level stored state beside daemon lifecycle
evidence. Persistence: reopen stored Run and Task state through public fixture
store reads.

Evidence: `GOCACHE=/private/tmp/roundfix-qa-0144-go-cache go test -count=1 -v
-run '^(TestImplementRunBudgetBoundsSetupAndIntegration|TestRunImplementBudgetExceededPreservesRunWorktreeAndBranch|TestTaskCycleEndsRunAtBudgetDeadline|TestBudgetExceededRunRecordsItsReason|TestTaskCycleSettlesBudgetOutcomeWithoutError)$'
./internal/cli ./internal/daemon` exited 0 with 11 tests passing. The public
Implement harness covered bootstrap, integration and push cancellation plus the
inside-budget control; daemon lifecycle tests covered both termination-proof
branches, no-later-Task behavior, success-path expiry, stored
`BudgetExceeded`, its maximum-and-elapsed reason, completed Task state, and the
retained Run Worktree and Run Branch.

### R3

```yaml
inputs:
  - kind: repository_path
    ref: internal/daemon/**
  - kind: repository_path
    ref: internal/cli/**
```

Steps: exercise an already-old Run with the budget disabled and a Run that
finishes before an enabled deadline. Expected: no deadline changes settlement
or completion. Independent confirmation: both disabled and finishes-first
paths. Persistence: fixture store state after the lifecycle returns.

Evidence: `GOCACHE=/private/tmp/roundfix-qa-0144-go-cache go test -count=1 -v
-run '^(TestTaskCycleDisabledBudgetDerivesNoDeadline|TestTaskCycleFinishesBeforeBudgetDeadline)$'
./internal/daemon` exited 0 with both tests passing. The disabled path remained
unbounded despite an already-old Run start, and the enabled Run that completed
inside its budget retained its existing successful settlement.

### R4

```yaml
inputs:
  - kind: repository_path
    ref: internal/cli/**
  - kind: repository_path
    ref: internal/worktree/**
```

Steps: invoke the Reconcile Command harness for a proved `BudgetExceeded` Run,
an input-moved Run, a mixed provability set, and an unsupported `Clean` Run.
Expected: only the fully proved accepted set carries forward. Independent
confirmation: checkout and Task files read after each command. Persistence: the
accepted checkout remains fast-forwarded and refused checkouts remain unchanged.

Evidence: `GOCACHE=/private/tmp/roundfix-qa-0144-go-cache go test -count=1 -v
-run '^(TestCarryForwardAcceptsBudgetExceededRun|TestCarryForwardRefusesATaskWhoseInputsMoved|TestCarryForwardRefusesRatherThanCarryingASubset|TestCarryForwardRefusesAnUnacceptedRunOutcomeByName|TestApplyTerminalRunOutcomeRemainsUnchanged)$'
./internal/cli ./internal/worktree` exited 0 with 16 tests passing. The public
Reconcile Command harness fast-forwarded the proved `BudgetExceeded` fixture,
persisted its Task proof, refused moved-input and mixed-provability fixtures
without partial application, refused `Clean` by name, and preserved all other
terminal reconciliation classifications.

### R5

```yaml
inputs:
  - kind: repository_path
    ref: .agents/skills/roundfix/SKILL.md
  - kind: repository_path
    ref: skills/roundfix/SKILL.md
  - kind: repository_path
    ref: internal/cli/**
  - kind: repository_path
    ref: internal/daemon/**
  - kind: repository_path
    ref: internal/worktree/**
```

Steps: follow the operator documentation through the executable budget and
carry-forward flows, check that every accepted-outcome passage names all three
outcomes, then compare both copies byte for byte. Expected: executable behavior
matches every documented fact and both copies match. Independent confirmation:
focused behavior tests plus exact byte comparison. Persistence is not applicable
to immutable documentation in this build.

Evidence: the executable public-flow suites recorded in R2 and R4 passed the
budget settlement, recovery-surface and three-outcome carry-forward behavior
described by the skill. A focused documentation check found the enabled Run
Budget contract and all three outcome passages, found no remaining
``Stopped` or `Unresolved`` two-outcome phrase in either copy, and `diff -q
.agents/skills/roundfix/SKILL.md skills/roundfix/SKILL.md` exited 0. The
canonical text names `BudgetExceeded`, the maximum-and-elapsed reason, the
preserved Run Worktree and Run Branch, and the three accepted carry-forward
outcomes; executable behavior, not reading alone, confirms those claims.

### R6

```yaml
inputs:
  - kind: repository_path
    ref: internal/watch/**
  - kind: repository_path
    ref: internal/store/**
```

Steps: locate the pre-existing watch-loop deadline behavior and Run Database
outcome vocabulary, then execute their existing focused tests. Expected: the
watch loop derives and enforces the same budget rule and the database round
trips `BudgetExceeded`. Independent confirmation: executable tests plus named
source origins. Persistence: Run Database round-trip evidence.

Evidence origin outside the Spec artifacts: `internal/watch/watch.go:404-406`
derives the pre-existing watch-loop deadline from the Run start and configured
maximum, while `internal/watch/watch.go:411` first returns the existing
`BudgetExceeded` outcome; `internal/store/store.go:46,70` defines and registers
that terminal outcome. `go test -count=1 -v -run
'^(TestRunTransientReviewEvidenceExhaustsRunBudgetBeforeTimeout|TestRunWithoutStopRequestKeepsRunBudgetBehavior)$'
./internal/watch` exited 0 with two tests passing. `go test -count=1 -v -run
'^(TestListRunsStateFilterAndLimit|TestTerminalOutcomeEveryStoredTerminalStateIsImmutable)$'
./internal/store` exited 0 with 21 tests passing, including the
`BudgetExceeded` round-trip and terminal-immutability cases. These executable
checks independently confirm the prior rule and persisted vocabulary.

### R7

```yaml
inputs:
  - kind: repository_path
    ref: docs/specs/0144-a-run-stops-when-its-budget-is-spent/**
  - kind: repository_path
    ref: .agents/skills/roundfix/SKILL.md
  - kind: repository_path
    ref: skills/roundfix/SKILL.md
  - kind: repository_path
    ref: internal/**
```

Steps: run the strict Spec check and audit every Task commit with
`git diff-tree`, comparing governed paths and chronological ancestry against
the approved authorization. Expected: promises map cleanly and governed changes
stay within the authorized paths plus each assigned Task file. Independent
confirmation: exact committed paths and ancestry. Persistence: committed
history at the audited head.

Evidence: `roundfix spec check
0144-a-run-stops-when-its-budget-is-spent --strict` exited 0 with no findings
and without running authored Verification. `git diff-tree --no-commit-id
--name-only -r` resolved exact paths for Task commits `51a734d4`, `3321bc35`,
`bbebd95e`, `0d30b9b8`, and `b417737a`. Tasks 01-03 changed only ordinary
source, tests, and their assigned Task files. Task 04 changed only its assigned
Task file and the two authorized skill paths. Corrective Task 06 changed only
its assigned Task file, ordinary source/tests, and the same two authorized
skill paths. `git merge-base --is-ancestor` exited 0 from authorization commit
`9ef244b6` to every Task commit and from corrective-Task base `5c40a9d6` to
`b417737a`; `git diff --check 5c40a9d6 b417737a` exited 0. No prerequisite fix,
consequent fix, or derived pin was declared or folded into either governed
change. Git emitted a non-fatal fsmonitor IPC diagnostic while returning the
committed paths; all audited commands exited 0 and the objects were readable.

### R8

```yaml
inputs:
  - kind: live_service
    ref: GitHub Pull Request state for feat/0144-run-budget
  - kind: repository_path
    ref: docs/specs/0144-a-run-stops-when-its-budget-is-spent/_authorization.md
```

Steps: use the assignment's no-open-Pull-Request fact and record equivalent
evidence for approval, checks and status, unresolved review threads,
Merge-Ready acceptance, and review-artifact ancestry. Expected: the row remains
environment-blocked while every unavailable control is answered explicitly.
Independent confirmation: Daemon-supervised Verification and local commit
ancestry. Persistence: not applicable to absent live Pull Request state.

Evidence and equivalent controls:

- Cause: the QA assignment states that no Pull Request is open and that Pull
  Request journeys are environment-blocked. Unblocking requires opening a Pull
  Request from `feat/0144-run-budget`; this gate has no authority to do so.
- Approval: maintainer authorization for the bounded skill mutation exists in
  the audited ancestry at `9ef244b6`; no Pull Request acceptance has been
  obtained.
- Checks and status: the Daemon-supervised repository Verification passed with
  exit 0, and all seven runnable QA rows passed on the audited head.
- Unresolved review threads: none can stand because no Pull Request exists.
- Merge-Ready acceptance: no Review Source acceptance exists yet because no
  Pull Request exists.
- Review-artifact ancestry: no review artifact exists. The audited
  implementation head is `b417737ab2affa61bdd4d8d9df21b61fba0a1634`;
  `feat/0144-run-budget` is still at
  `5c40a9d6a3c3fa035f1e2b3d5f21691015759000`, which is an ancestor of the
  audited head, until the Daemon settles this Run.

## Findings

None.

## Blocked and skipped

R8 is `blocked (environment: no open Pull Request)`. The QA assignment proves
the cause, and the row records equivalent observed or supervised evidence for
all five Pull Request controls. Opening a Pull Request from the Spec target
branch after Run settlement makes the live journey reachable. No row was
skipped.

## Coverage

Eight of eight rows reached a terminal status: seven passed and one was blocked
by the declared environment fact with complete equivalent evidence. All seven
declared Requirements appear in provenance, alongside the mandatory repository
Verification, outside-evidence, and Pull Request sources. No row names another
source. `rows_blocked_environment: 1`, `rows_blocked_finding: 0`, and
`rows_blocked_declared: 0`. The PRD declares no `Unreachable Acceptance`
section and no frontend surface. No row failed or was skipped.

## Mechanical skips

| Detector | Missing artifact |
| --- | --- |
| consequent-fix commit order | consequent-fix declaration |
| QA evidence paths | docs/specs/0144-a-run-stops-when-its-budget-is-spent/qa/evidence |

## Final verdict

Pass. Every runnable criterion passed on build
`b417737ab2affa61bdd4d8d9df21b61fba0a1634`; the sole environment-blocked Pull
Request row records its cause and complete equivalent evidence, and there are
no findings, declared blocks, or skipped rows.
