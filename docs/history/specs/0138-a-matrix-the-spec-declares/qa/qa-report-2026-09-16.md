---
spec: 0138-a-matrix-the-spec-declares
date: 2026-09-16
build: cb0d18650643796eea00176434fcdd13306f7fb7
auditing_binary: "0.12.0 (cb0d1865, built 2026-09-16 12:14:15 -0300)"
auditor_staleness: "current: commit ancestry: build commit does not predate audited tree"
status: closed
verdict: pass
rows_blocked_environment: 1
rows_blocked_finding: 0
rows_blocked_declared: 0
surfaces: [backend, docs]
---

# QA report — A matrix the Spec declares

## Scope and environment

Full gate for the assembled tree at `cb0d18650643796eea00176434fcdd13306f7fb7`.
The actor is a Roundfix Supervisor or maintainer reading or executing the QA
contract through the repository's skill, prompt builder, tests, and CLI. The
gate rebuilt `/private/tmp/roundfix-qa-0138/roundfix` from that revision; the
binary reports `roundfix 0.12.0 (cb0d1865, built 2026-09-16 12:14:15 -0300)`.

The required precondition command
`/private/tmp/roundfix-qa-0138/roundfix spec check 0138-a-matrix-the-spec-declares --strict`
exited 0 with no findings. It reported four skipped detectors because their
optional subjects were absent: a findings Rollup, a Backlog Entry, a parsed
Vocabulary Contract, and `references/_index.md`. Requirement 6 retains the
glossary check the vocabulary skip did not replace.

The prompt states that no Pull Request is open for the Spec target branch, so
the Pull Request journey uses the required equivalent-evidence path. No
credential, network service, browser, frontend, database, or external write is
part of this docs and backend gate.

## Static gate

The rebuilt binary's strict Spec check exited 0 with no findings. The Daemon's
repository Verification, `make verify`, exited 0 at the audited build and
removed its success diagnostics. Q-07 records that supplied result without
rerunning it. Focused skill, prompt, history, analyzer, and glossary checks are
recorded below.

## Performed repairs

None.

## Assigned repair failures

None.

## Authorization audit inputs

None.

## Mechanical findings

None.

## Results

| # | Status | Provenance |
| - | --- | --- |
| Q-01 | pass | Task 01 criterion 1; Task 01 criterion 2; Requirement 2 |
| Q-02 | pass | Task 01 criterion 3 |
| Q-03 | pass | Task 01 criterion 4 |
| Q-04 | pass | Task 01 criterion 5 |
| Q-05 | pass | Task 02 criterion 1; Task 02 criterion 2; Task 02 criterion 3; Requirement 2 |
| Q-06 | pass | Requirement 3; outside-evidence row |
| Q-07 | pass | Requirement 4; repository Verification |
| Q-08 | pass | Requirement 5 |
| Q-09 | pass | Requirement 6 |
| Q-10 | blocked (environment: no open Pull Request) | Pull Request row |

## Detailed evidence

### Q-01

```yaml
inputs:
  - kind: repository_path
    ref: .agents/skills/qa-gate/SKILL.md
  - kind: repository_path
    ref: skills/qa-gate/SKILL.md
```

- Actor, entry point, surface: gate Agent reading the installed and embedded
  qa-gate skill; docs.
- Steps: scan both copies for every required anchor and both removed rules.
- Expected observable: all anchors are present, both removed rules are absent,
  and the declaration, coverage, and scoped-blocking rules agree.
- Independent confirmation and persistence: rerun the scan against fresh file
  reads from both repository paths.
- Evidence path: this report's Q-01 execution record.
- Execution: the normalized fresh-file scan exited 0. Both skill copies carry
  all eight TechSpec anchors, including the declaration, closed-coverage, fixed
  input, analyzer, and scoped-blocking clauses. Neither copy carries either
  removed broad-scope or cascade-blocking sentence. The skill side of
  Requirement 2 therefore agrees with the declared rule.

### Q-02

```yaml
inputs:
  - kind: repository_path
    ref: .agents/skills/qa-gate/SKILL.md
  - kind: repository_path
    ref: skills/qa-gate/SKILL.md
```

- Actor, entry point, surface: maintainer consuming the canonical and embedded
  skill; docs.
- Steps: compare both files byte-for-byte and inspect their version fields.
- Expected observable: byte identity and `version: 0.0.2` in both copies.
- Independent confirmation and persistence: a fresh `cmp` plus exact version
  count after the initial inspection.
- Evidence path: this report's Q-02 execution record.
- Execution: `cmp -s .agents/skills/qa-gate/SKILL.md
  skills/qa-gate/SKILL.md` exited 0. A fresh exact-line count found one
  `version: 0.0.2` declaration in each copy.

### Q-03

```yaml
inputs:
  - kind: repository_path
    ref: .agents/skills/qa-gate/SKILL.md
  - kind: repository_path
    ref: skills/qa-gate/SKILL.md
  - kind: repository_path
    ref: skills/baseline_skill_contract_test.go
```

- Actor, entry point, surface: maintainer running the skill's public repository
  contract tests; backend test surface.
- Steps: run the four named workflow contract tests from Task 01.
- Expected observable: every test passes with the pinned authoring and tooling
  clauses intact.
- Independent confirmation and persistence: a new test process reads the skill
  files from disk.
- Evidence path: this report's Q-03 execution record.
- Execution: `GOCACHE=/private/tmp/roundfix-qa-0138-gocache-q03 go test
  -count=1 -v ./skills -run
  '^(TestAuthorialSkillSync|TestProjectConstraintQAGate|TestToolingAuthorizationJourney|TestLegacySpecConstraintExemption)$'`
  exited 0. All four named tests and their subtests passed, including the skill
  mirror, QA constraint, and tooling-authorization contracts.

### Q-04

```yaml
inputs:
  - kind: repository_path
    ref: docs/specs/0138-a-matrix-the-spec-declares/_authorization.md
  - kind: repository_path
    ref: docs/specs/0138-a-matrix-the-spec-declares/task_01.md
  - kind: repository_path
    ref: .agents/skills/qa-gate/SKILL.md
  - kind: repository_path
    ref: skills/qa-gate/SKILL.md
```

- Actor, entry point, surface: maintainer auditing the governed Task commit
  through Git; docs and repository history.
- Steps: resolve Task 01's settlement provenance, inspect every changed path
  with `git diff-tree --no-commit-id --name-only -r`, and prove the approved
  grant predates the change.
- Expected observable: only the two bounded skill paths and Task 01's own file
  changed; authorization is approved, traceable, and earlier in ancestry.
- Independent confirmation and persistence: compare the committed tree paths
  with the current assembled paths and grant.
- Evidence path: this report's Q-04 execution record.
- Execution: the approved grant originates in
  `7a15e970feb4e98a5a1c896f35c210e3a5650c13`, and
  `git merge-base --is-ancestor` exited 0 for both the source settlement commit
  `55062f4434744bf073c27fcbb78d5dad9c9f07f9` and the assembled Task 01 commit
  `994dca39735c27e4292f88d89c3d5d62c48c66cf`. Exact
  `git diff-tree --no-commit-id --name-only -r` output for each commit contains
  only `.agents/skills/qa-gate/SKILL.md`, `skills/qa-gate/SKILL.md`, and
  Task 01's own file. The two committed skill copies are identical between the
  source and assembled Task commits; the only Task-file delta is the Daemon's
  appended carry-forward provenance. The grant is approved and dated, consumes
  this Spec, names `implement` and `commit`, and bounds both governed paths.
  No authorization, prerequisite fix, or consequent fix is folded into the
  Task commit, and no path falls outside the exact bounded list plus Task 01.

### Q-05

```yaml
inputs:
  - kind: repository_path
    ref: internal/agent/spec_prompt.go
  - kind: repository_path
    ref: internal/agent/spec_prompt_test.go
  - kind: repository_path
    ref: .agents/skills/qa-gate/SKILL.md
```

- Actor, entry point, surface: gate Agent receiving `BuildQAPrompt` output;
  backend prompt boundary.
- Steps: run the named prompt test and the complete `TestBuildQAPrompt` group,
  then scan the rendered-contract source for required and removed phrases.
- Expected observable: the test named by Task 02 passes, all existing prompt
  tests pass, every required rule appears, and the old broad rule is absent.
- Independent confirmation and persistence: a second process scans the source
  after the test process exits and compares it with the skill rule.
- Evidence path: this report's Q-05 execution record.
- Execution: `GOCACHE=/private/tmp/roundfix-qa-0138-gocache-q05 go test
  -count=1 -v ./internal/agent -run '^TestBuildQAPrompt'` exited 0. All 11
  prompt tests passed, including `TestBuildQAPromptStatesTheDeclaredMatrix` and
  the unchanged checkout, previous-report, and context-bundle tests. A separate
  fresh-file scan found all five contract phrases and no occurrence of
  `validate every user story and acceptance criterion`. Together with Q-01,
  this proves the rendered prompt contract and the skill state the same
  declaration, coverage, and scoped-blocking rules.

### Q-06

```yaml
inputs:
  - kind: repository_path
    ref: docs/history/specs/0119-spec-contained-authorization/**
  - kind: repository_path
    ref: docs/history/specs/0134-a-governed-deletion-the-gate-can-see/**
  - kind: repository_path
    ref: docs/history/specs/0135-a-rename-the-gate-sees-from-both-sides/**
  - kind: repository_path
    ref: docs/history/specs/0136-a-rename-the-committer-can-stage/**
  - kind: repository_path
    ref: docs/history/specs/0137-a-fixture-that-carries-its-committer/**
```

- Actor, entry point, surface: Supervisor replaying historical QA evidence the
  current Spec did not author; docs.
- Steps: read every named report and each earlier qa Task, map every required
  real finding to a surviving source or mechanical finding, map every named
  noise row to the new rule that removes it, and list each declared source no
  earlier report row named.
- Expected observable: all named source paths resolve and all five replay
  claims in the PRD's Acceptance evidence hold.
- Independent confirmation and persistence: source-path inventory plus direct
  re-read of the cited rows and qa Task Requirements.
- Evidence path: this report's Q-06 execution record and cited historical files.
- Execution: every replay source resolved. The source inventory was:
  - Spec 0119's `docs/history/specs/0119-spec-contained-authorization/task_10.md`
    and `docs/history/specs/0119-spec-contained-authorization/qa/qa-report-2026-09-09.md`,
    `docs/history/specs/0119-spec-contained-authorization/qa/qa-report-2026-09-10.md`,
    `docs/history/specs/0119-spec-contained-authorization/qa/qa-report-2026-09-10-01.md`,
    `docs/history/specs/0119-spec-contained-authorization/qa/qa-report-2026-09-10-02.md`,
    `docs/history/specs/0119-spec-contained-authorization/qa/qa-report-2026-09-10-03.md`,
    and `docs/history/specs/0119-spec-contained-authorization/qa/qa-report-2026-09-10-04.md`;
  - Spec 0134's `docs/history/specs/0134-a-governed-deletion-the-gate-can-see/task_04.md`,
    `docs/history/specs/0134-a-governed-deletion-the-gate-can-see/qa/qa-report-2026-09-14.md`,
    `docs/history/specs/0134-a-governed-deletion-the-gate-can-see/qa/qa-report-2026-09-14-01.md`,
    and analyzer records
    `docs/history/specs/0134-a-governed-deletion-the-gate-can-see/qa/evidence/2026-09-14-run-run_20260914T095106Z/04-lint-finding.md`
    and `docs/history/specs/0134-a-governed-deletion-the-gate-can-see/qa/evidence/2026-09-14-run-run_20260914T103059Z/01-scoped-vet.md`;
  - Spec 0135's `docs/history/specs/0135-a-rename-the-gate-sees-from-both-sides/task_03.md`
    and `docs/history/specs/0135-a-rename-the-gate-sees-from-both-sides/qa/qa-report-2026-09-14.md`;
  - Spec 0136's `docs/history/specs/0136-a-rename-the-committer-can-stage/task_04.md`
    and `docs/history/specs/0136-a-rename-the-committer-can-stage/qa/qa-report-2026-09-14.md`
    plus `docs/history/specs/0136-a-rename-the-committer-can-stage/qa/qa-report-2026-09-14-01.md`;
  - Spec 0137's `docs/history/specs/0137-a-fixture-that-carries-its-committer/task_02.md`
    and `docs/history/specs/0137-a-fixture-that-carries-its-committer/qa/qa-report-2026-09-14.md`.
- Spec 0119 replay: Task 10 Requirement 1 still reaches the missing operations
  list; Requirement 2 still reaches the dropped template clauses and rewritten
  setup context as undeclared behavior changes; the non-waivable repository
  Verification reaches the timeout; and `QA-AUTH-PATHS` reaches the
  out-of-grant changed paths mechanically. The six rows blocked only because
  another row found a defect disappear under scoped blocking. The aggregate
  QA-11 row disappears because no row result may be computed from other rows.
  The `SC-REF-UNRESOLVED` report remains a pre-matrix mechanical refusal rather
  than a matrix row. Thus all five real defect classes remain reachable while
  both named noise forms are removed.
- Spec 0136 replay: Task 04 Requirement 3 directly covers the governed-rename
  final-push bypass reported by F-001/R-09, and the passing rerun observes both
  governed paths refuse. R-14's report-closure recount names no declared source
  and repeats report-shape and evidence-path checks now owned mechanically, so
  closed coverage excludes it.
- Spec 0134 analyzer replay: the first report's repository-wide `go vet ./...`
  found 31 `copylocks` diagnostics in unchanged `internal/agent`; the rerun
  scoped vet to the changed packages and passed while attributing those 31
  diagnostics to Spec 0123. That is the current changed-package and
  delivery-target rule.
- Declared-source gap replay: neither Spec 0134 report omits a source declared
  by its Task 04, and the Spec 0135 and Spec 0137 reports omit none declared by
  their Task 03 and Task 02 respectively. Their additional aggregate,
  report-closure, Non-Goal, or review-summary rows are outside today's closed
  source set and are not reproduced. The outside-evidence replay therefore
  passes.

### Q-07

```yaml
inputs:
  - kind: repository_path
    ref: "**"
```

- Actor, entry point, surface: Daemon running the repository's public
  Verification gate; backend and docs.
- Steps: consume the Daemon-owned `make verify` result supplied for this build
  and resolve the current Go toolchain version. Do not rerun the gate.
- Expected observable: exit 0 with no retained diagnostics.
- Independent confirmation and persistence: bind the supplied result to the
  audited full commit and record the independently queried toolchain version.
- Evidence path: this report's Q-07 execution record.
- Execution: the Daemon supplied `make verify` for
  `cb0d18650643796eea00176434fcdd13306f7fb7` with exit 0 and removed its
  diagnostics on success. The gate did not rerun it. A fresh `go version`
  query returned `go version go1.26.5 darwin/arm64`, and Git resolved the
  assembled checkout to the same full commit. The repository Verification is
  clean.

### Q-08

```yaml
inputs:
  - kind: repository_path
    ref: internal/agent/**
  - kind: repository_path
    ref: go.mod
  - kind: repository_path
    ref: go.sum
  - kind: repository_path
    ref: docs/specs/0123-runtime-readiness-and-model-capabilities/references/2026-09-08-acpx-runner-value-receivers-copy-its-mutex.md
```

- Actor, entry point, surface: maintainer running the named analyzer on the
  package this Spec changed; backend.
- Steps: run `go vet ./internal/agent` at the assembled revision and at the
  pre-Task delivery target, then compare diagnostics exactly.
- Expected observable: any identical diagnostic is observed and attributed to
  Spec 0123; no new diagnostic is introduced by this Spec.
- Independent confirmation and persistence: isolated source trees and Go
  caches for the two revisions.
- Evidence path: this report's Q-08 execution record.
- Execution: with `GOTOOLCHAIN=go1.26.5` and separate caches,
  `go vet ./internal/agent` exited 1 at the assembled revision and at delivery
  target `00f30e3d38666c1e91fd05fbada3f4f4a98d428c`. Each run emitted exactly 31
  `passes lock by value` diagnostics, and the complete outputs were
  byte-for-byte identical. They all concern `ACPXRunner` carrying a
  `sync.Mutex`; Spec 0123 owns that runner mutex copying. This Spec introduced
  no analyzer diagnostic, so the observed pre-existing output does not fail
  the row.

### Q-09

```yaml
inputs:
  - kind: repository_path
    ref: CONTEXT.md
  - kind: repository_path
    ref: .agents/skills/qa-gate/SKILL.md
  - kind: repository_path
    ref: skills/qa-gate/SKILL.md
  - kind: repository_path
    ref: internal/agent/spec_prompt.go
  - kind: repository_path
    ref: docs/specs/0138-a-matrix-the-spec-declares/_techspec.md
```

- Actor, entry point, surface: maintainer checking the project glossary after
  the assembled docs and prompt change; docs.
- Steps: compare the shipped vocabulary with `CONTEXT.md` and the TechSpec's
  Vocabulary Contract.
- Expected observable: every canonical term remains represented and no new
  domain term requires a glossary entry.
- Independent confirmation and persistence: fresh searches in `CONTEXT.md` and
  the changed paths after the diff review.
- Evidence path: this report's Q-09 execution record.
- Execution: the diff from pre-Task delivery target `00f30e3d` changes the two
  qa-gate skill copies, the prompt builder and its test, plus the two Task
  records. It does not change `CONTEXT.md`. The shipped wording uses existing
  terms such as QA Report and Mechanical Refusal Code, retains their documented
  spellings, and adds no report key, refusal code, or stable identifier. The
  TechSpec Vocabulary Contract says `No token is coined.` Fresh searches and
  the changed-wording review agree, so no glossary entry is required.

### Q-10

```yaml
inputs:
  - kind: live_service
    ref: GitHub Pull Request state for feat/0138-a-matrix-the-spec-declares at cb0d18650643796eea00176434fcdd13306f7fb7
  - kind: repository_path
    ref: docs/specs/0138-a-matrix-the-spec-declares/_authorization.md
  - kind: repository_path
    ref: docs/specs/0138-a-matrix-the-spec-declares/_prd.md
```

- Actor, entry point, surface: maintainer evaluating the Pull Request controls;
  delivery boundary.
- Steps: use the prompt's no-open-Pull-Request fact and record equivalent
  evidence for approval, checks and status, unresolved review threads,
  Merge-Ready acceptance, and review-artifact ancestry.
- Expected observable: the row records the environmental cause and an observed
  answer for all five controls.
- Independent confirmation and persistence: bind each equivalent observation
  to the audited head and name any evidence that does not yet exist.
- Evidence path: this report's Q-10 execution record.
- Execution: the row is environment-blocked because the run prompt states that
  no Pull Request is open for `feat/0138-a-matrix-the-spec-declares`; there is
  therefore no live PR surface on which to inspect controls. Equivalent
  evidence at `cb0d18650643796eea00176434fcdd13306f7fb7` answers all five:
  - approval: the maintainer approved the exact bounded grant on 2026-09-14
    with `Aprovar como proposto`, recorded in `_authorization.md`;
  - checks and status: the Daemon's repository Verification passed, the rebuilt
    binary's strict Spec check passed, the focused skill and prompt suites
    passed, and Q-08 found no new analyzer diagnostic;
  - unresolved threads: no PR exists, so no PR review thread exists or is
    claimed resolved;
  - Merge-Ready acceptance: no Merge-Ready decision exists yet; and
  - review-artifact ancestry: no current-head PR review artifact exists, so no
    ancestry claim is made. The PRD's three pre-PR review rounds concern Spec
    authoring and do not substitute for current-head Merge-Ready evidence.
  Opening the target-branch PR after integration and observing these controls
  on that PR unblocks the live journey. The equivalent-evidence path is
  complete, so this environmental block does not prevent a pass verdict.

## Mechanical skips

| Detector | Missing artifact |
| --- | --- |
| authorization bounded paths | Task commits |
| consequent-fix commit order | consequent-fix declaration |
| QA Report structure | QA Report |
| QA evidence paths | QA Report |

## Findings

None.

## Blocked and skipped

- Q-10 is blocked by the declared environment fact that no Pull Request is
  open. Its equivalent-evidence path answers all five PR controls and names the
  live action that would unblock the row.
- No row is finding-blocked, declared-blocked, or skipped.

## Coverage

- 16 of 16 coverage sources are named in provenance: five Task 01 criteria,
  three Task 02 criteria, Requirements 2 through 6, the outside-evidence row,
  the Pull Request row, and the repository Verification.
- Nine rows pass and one row is environment-blocked with complete equivalent
  evidence. No result is inferred from another row.
- Zero sources are missing and zero provenance entries are outside the declared
  set. The PRD has no `## Unreachable Acceptance` declaration and does not
  declare the frontend surface, so neither contributes a source.
- Exact blocked counters are: environment 1, finding 0, declared 0.

## Final verdict

**Pass.** Every runnable criterion passed, Q-10 records its environmental cause
and complete equivalent evidence, and no row is finding-blocked,
declared-blocked, or skipped.
