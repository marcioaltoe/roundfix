---
spec: 0061-repository-derived-skill-requirements
date: 2026-07-29
build: d198cf5d3001d873b536cd01af0e30d8f2c1801a
status: closed
verdict: partial
surfaces: [backend, cli, docs]
---

# QA report — Repository-derived skill requirements

## Scope and environment

Full Spec QA ran against commit
`d198cf5d3001d873b536cd01af0e30d8f2c1801a` on Darwin arm64 with Go 1.26.5.
The public application artifact was built from this commit:

```text
GOCACHE=<run-worktree>/.gocache go build -buildvcs=false \
  -o /private/tmp/roundfix-qa-0061 ./cmd/roundfix
```

The default Go cache was sandbox-denied; the unchanged build passed with the
writable Run-local cache. Public Doctor journeys ran in:

- the actual Oraculum, Vortex, and Tax PoC TypeScript repositories named in
  the source finding;
- a clean local clone with TypeScript, exact Go/CLI/TUI, absent, malformed,
  and unknown-module Setup Manifest variants; and
- this Run Worktree with its real Setup Manifest.

Every public Doctor invocation was non-interactive and offline. Git status
before and after each real consumer and scratch repeat was identical. Profile
and Codex hygiene checks failed independently because the installed Codex
binary has an invalid or missing code signature; Doctor still evaluated and
printed Repository Skill Set readiness, as ADR-0049 and ADR-0055 require.

### Project Constraint audit

| Constraint | Applicability and source | Evidence | Result |
| --- | --- | --- | --- |
| Identifier strategy | Not applicable; `docs/agents/domain.md` | The PRD and TechSpec preserve skill names, module IDs, and manifest paths; `main...HEAD` adds no project-owned Internal Identifier. | pass |
| Authentication and HTTP | Not applicable; `docs/agents/cli.md` | `roundfix doctor --help` states that Doctor is offline and read-only. The resolver reads `docs/agents/setup-context.json` and the embedded catalog only. | pass |
| Active ADR obligations | Applicable; `docs/agents/domain.md` | Public Doctor output continued from a failed `profiles:` line to a separate `skills:` line. The resolver remains in `internal/cli`, preserving ADR-0066 and the package-leaf boundary. | pass |
| Tooling Authority | Applicable; `docs/agents/agent-instructions.md` | PRD and TechSpec expressly authorize the two Roundfix Skill paths before implementation. `git diff-tree` for `fb7f695` contains only those paths, `task_04.md`, and the five sanctioned digest outputs. `d198cf5` touches no protected tooling. The baseline suite validates the generated pins. | pass |

## Static gate

`rtk make verify` ran without a pipeline and exited `2`. Its test stage
reported 2,863 passing, five failing, and two skipped tests. All five failures
were owner-process identity cases. The focused unchanged reproduction

```text
rtk proxy env GOCACHE=<run-worktree>/.gocache go test -count=1 \
  -run '^TestOwnerProcessIdentityIsStableForOneProcess$' -v ./internal/store

read current process identity: read start time for process 6930:
fork/exec /bin/ps: operation not permitted
```

failed at the managed sandbox boundary before product behavior could execute.
This is an environment-caused block, not a code-caused test regression. A
full-access macOS session that permits `/bin/ps` must rerun `rtk make verify`.

## Planned matrix

| # | Story / criterion / sweep | Actor and entry point | Steps and behavior probe | Expected observable | Independent confirmation / persistence | Evidence path | Status |
| - | --- | --- | --- | --- | --- | --- | --- |
| 1 | Project Constraints: identifier, authentication/HTTP, active ADR, and Tooling Authority applicability | QA auditor; PRD, TechSpec, accepted ADRs, Task commits | Validate both Spec artifacts against operative `docs/agents/` sources; resolve every Task commit with `git diff-tree`; inspect the authorized Skill/digest choreography | Every constraint has an applicability reason and operative source; protected-tooling paths stay inside the exact authorization plus sanctioned digest fallout | Compare commit paths to the current clean worktree and ADR-0049, ADR-0055, ADR-0066, and ADR-0081 | This report → Project Constraint audit | pass |
| 2 | Repository static verification | QA operator; repository root | Run `rtk make verify` once, without a pipe or substituted narrower gate | Formatter, tests, Skill checks, and build all pass | Record exit code and final gate output | This report → Static gate | blocked |
| 3 | Task 01: explicit external set, empty set, compatibility entry point, and unchanged owned validation | Go package consumer; `skills` public API through the declared focused tests | Run the Task's focused package verification; include the empty-set boundary | Explicit requirements are used verbatim; empty means zero; compatibility behavior and owned classifications stay unchanged | Re-run with `-count=1` and confirm through the assembled Doctor journeys | This report → Results row 3 | pass |
| 4 | Story 1 / Task 02: TypeScript modules exclude Go and TUI skills | TypeScript repository maintainer; built `roundfix doctor` | Run Doctor in a scratch repository whose Setup Manifest selects the TypeScript modules; inspect the `skills:` line | No `golang-*`, `bubbletea`, or `tui-design` external requirement appears | Re-run Doctor from a fresh process and compare deterministic output | This report → Results row 4 | pass |
| 5 | Story 4 / Task 02: this repository retains its Go, CLI, and TUI requirements | Roundfix maintainer; built `roundfix doctor` in this checkout | Run Doctor against this repository's real Setup Manifest | The derived count includes the Go and terminal-UI skills selected here | Confirm against the manifest/catalog resolution test and a second command/read of the manifest | This report → Results row 5 | pass |
| 6 | Story 3 / Task 02: absent or unreadable Setup Manifest | Non-adopter; built `roundfix doctor` in a scratch repository | Run with no manifest, then malformed/non-regular manifest probes | `0 external`; failed readiness states the missing/unreadable manifest and names `roundfix baseline` as the next action | Re-run after restoring the same scratch state; confirm no fallback recommendation list appears | This report → Results row 6 | pass |
| 7 | Task 02 risk: unknown module fails legibly | Repository maintainer; built `roundfix doctor` | Add an unknown selected module and run Doctor | Failure names the unknown module before repository readiness is accepted | Fresh process rerun plus focused resolver test | This report → Results row 7 | pass |
| 8 | Story 2 / Task 03: every missing external skill is reported with skill-scoped remediation | Repository maintainer; built `roundfix doctor` with three required external skills absent | Run the failure journey and inspect the complete `skills:` failure and `next:` actions | One failure names all three; each has `bunx skills add marcioaltoe/skills@<skill>`; no package-wide install appears | Repeat from unchanged scratch state and compare output ordering | This report → Results row 8 | pass |
| 9 | Task 03: owned-skill classification and remediation stay unchanged | Roundfix maintainer; declared Doctor readiness tests | Exercise owned-only and mixed owned/external gaps | Owned gap remains Roundfix-owned, failed, and remediated by `roundfix skills install --target project` with unchanged exit behavior | Confirm with `-count=1` focused tests and affected real-consumer output | This report → Results row 9 | pass |
| 10 | Core Feature 5 / Doctor diagnosis-only contract | Repository maintainer; built `roundfix doctor` and real repository no-mutation test | Snapshot scratch and real repository state, run Doctor twice, compare state | Doctor writes no file, config, or repository state | `git status --short` and filesystem snapshot after a fresh second process; current no-mutation test | This report → Results row 10 | pass |
| 11 | CLI public contract sweep | Automation operator; `roundfix doctor` stdout/stderr and exit code | Exercise success/failure lines, deterministic ordering, invalid repository state, and repeat execution without interaction | Stable `skills:`/`next:` vocabulary, diagnostic stream and non-zero failure behavior remain machine-operable and non-interactive | Compare two fresh invocations and the public command tests | This report → Results row 11 | pass |
| 12 | Task 04 / docs surface: shipped Skill pair, absent-manifest guidance, per-skill command, and derived pins | Agent following the Roundfix Skill; both shipped copies | Read the readiness instructions; run Skill sync and embedded catalog/digest validation | Both copies are byte-identical, describe manifest-derived requirements, zero-external adoption behavior, and per-skill remediation; derived pins validate | `make skills-sync-check`, focused baseline/skills tests, and exact byte comparison | This report → Results row 12 | pass |
| 13 | ADR seam: Repository Skill Set readiness remains independent from Agent Selection Profile proof | Repository maintainer; Doctor public flow and profile/readiness tests | Exercise a profile-readiness failure alongside repository skill evaluation | Repository Skill Set is still evaluated independently; no profile selection or Baseline mutation is introduced | Focused profile/readiness tests plus current source-path inspection | This report → Results row 13 | pass |
| 14 | Non-Goals and scope-creep sweep | QA auditor; Spec diff from `main` and public Doctor behavior | Inspect changed paths and behavior for lock reconciliation, embedded-owned bundle/version/install semantics, network use, and module assignment of `autoresearch`/`exa-web-search` | None of the excluded features shipped; `recommended.txt` is advisory rather than Doctor authority | Compare `main...HEAD` paths, source references, and focused behavioral evidence | This report → Non-Goals | pass |

Chosen high-risk probes are absent/malformed/non-regular manifests, an unknown
module, three simultaneous missing skills, repeat execution, and no-mutation
state comparison. These target the feature's data authority, error recovery,
complete remediation, deterministic output, and diagnosis-only promise.

## Results

| # | Story / criterion / sweep | Actor and surface | Status | Evidence |
| - | --- | --- | --- | --- |
| 1 | Project Constraints and Tooling Authority | QA auditor; docs/Git | pass | Both active Spec artifacts account for all four constraint categories and cite operative agent guides. Task commits `74a8a9a`, `21814a0`, `dca5b4c`, and `fb7f695` were resolved with `git diff-tree`; `fb7f695` stayed inside its two authorized Skill files, Task file, and five ADR-0081 digest outputs. |
| 2 | Repository static verification | QA operator; build/test/docs | blocked | `rtk make verify` exited 2 after 2,863 passes because five owner-process tests could not execute `/bin/ps`; focused reproduction returned `fork/exec /bin/ps: operation not permitted`. |
| 3 | Task 01 public readiness API | Go package consumer; backend | pass | `go test -count=1 ./skills/` passed. A verbose focused rerun passed `TestCheckRepositoryWithExternalUsesExplicitRequirement`, `...AcceptsEmptyRequirement`, `...MatchesExternalCompatibilityEntryPoint`, and `...KeepsOwnedValidation`. |
| 4 | TypeScript manifest excludes Go/TUI requirements | TypeScript maintainer; CLI | pass | The built Doctor ran in Oraculum, Vortex, and Tax PoC: each emitted only the genuine outdated Roundfix-owned Skill and never named a Go/TUI skill. An all-TypeScript-module scratch run listed the derived external gaps without `golang-*`, `bubbletea`, or `tui-design`; a second fresh process produced identical ordered output. |
| 5 | Roundfix manifest retains Go/TUI requirements | Roundfix maintainer; CLI | pass | A clone using the exact `go`, `cli-surface`, and `tui-surface` module set emitted `skills: ok (23 required: 14 Roundfix-owned, 9 external)`. The current repository's full manifest also surfaced its separately real `knowledge-workspace` gap, proving it derives from all selected modules rather than a fixed list. |
| 6 | No readable manifest yields zero external plus adoption action | Non-adopter; CLI | pass | Absent and malformed manifest runs both emitted `skills: failed (14 required: 14 Roundfix-owned, 0 external; Setup Manifest is absent or unreadable; next: roundfix baseline)`. Two absent-manifest processes matched exactly. |
| 7 | Unknown module fails with its name | Repository maintainer; CLI | pass | A public Doctor run with `modules: ["unknown-module"]` emitted `skills: failed (resolve external skill requirement: Setup Manifest names unknown module "unknown-module")`. |
| 8 | Multi-gap skill-scoped remediation | Repository maintainer; CLI | pass | An exact three-gap public run named `agentic-cli-design, bubbletea, tui-design` once and printed one sorted `bunx skills add marcioaltoe/skills@<skill>` action for each. No package-wide action appeared. A second fresh process matched exactly; the focused several-gap test also passed. |
| 9 | Owned remediation remains unchanged | Roundfix maintainer; backend/CLI | pass | The verbose readiness suite passed its `owned_failure` and mixed cases. All three actual TypeScript consumers emitted `outdated: roundfix; next: roundfix skills install --target project`, preserving the owned remediation and failed exit behavior. |
| 10 | Doctor is diagnosis-only | Repository maintainer; CLI | pass | Scratch Git status before and after two public runs remained only the two deliberate fixture edits. Oraculum, Vortex, and Tax PoC each had byte-for-byte identical status listings before and after. The focused suite including `TestRunDoctorRealRepositoryCheckDoesNotMutateState` passed. |
| 11 | CLI public-contract sweep | Automation operator; CLI | pass | `doctor --help` exited 0 and documents offline/read-only behavior. Failure journeys exited 1 with stable `skills:` and `next:` lines; `doctor unexpected` exited 2 with a concise usage diagnostic. All paths were non-interactive, repeatable, and deterministically sorted. |
| 12 | Skill pair and derived pins | Agent/maintainer; docs | pass | `rtk make skills-sync-check` passed; `cmp -s` confirmed byte identity; the full `./internal/baseline/` suite passed in 102.217s; the full `./skills/` suite passed. The shipped Skill contains Setup Manifest, `roundfix baseline`, and per-skill guidance, with no `experimental_install` match. |
| 13 | Agent Selection Profile independence | Repository maintainer; backend/CLI | pass | Every public run printed `skills:` after an independently failed `profiles:` check. The focused Doctor/profile suite passed, and `skills/*.go` has no import of `roundfix/internal/baseline` (expected `rg` exit 1). |
| 14 | Non-Goals and scope-creep sweep | QA auditor; backend/CLI/docs | pass | `main...HEAD` changes only Spec/finding docs, the authorized Skill/digests, repository readiness code, and tests. No lock-reconciliation removal path, owned-name/version/install change, network call, or module declaration for `autoresearch`/`exa-web-search` was added. `CheckRepository` remains the compatibility path while Doctor uses the manifest-derived entry point. |

## Findings

No Spec behavior finding. The current Roundfix repository has a pre-existing
`knowledge-workspace` lock gap, and the three affected TypeScript repositories
have an outdated Roundfix-owned Skill while local Skill refresh work is
already present. The new behavior diagnoses those real states without the
former Go/TUI false requirement and without writing to any checkout.

## Blocked and skipped

- Row 2 is blocked by the managed sandbox's denial of `/bin/ps`. Unblock it by
  running the unchanged `rtk make verify` command in a full-access macOS
  session. Per the QA contract, this block caps the final verdict at
  `partial` even if every remaining row passes.

No row was skipped. Profile/Codex signature failures observed during public
Doctor runs are separate readiness results, not blocks on the independently
printed Repository Skill Set evidence.

## Coverage

- User stories: 4/4 passed.
- Core Features: 5/5 passed.
- Task acceptance criteria: 18/18 passed through current focused tests, public
  application flows, and docs/catalog validation.
- Project Constraints: 4/4 passed.
- Non-Goal groups: 4/4 checked with no scope creep.
- Behavior probes: TypeScript consumers, absent manifest, malformed manifest,
  unknown module, exact three-gap remediation, repeated fresh processes, and
  before/after no-mutation checks all passed.
- Matrix: 13 pass, 0 fail, 1 blocked, 0 skipped, 0 pending.
- Findings by impact: 0 Blocks-Completion, 0 Data-Loss, 0 Trust-Damage,
  0 Friction, 0 Cosmetic.

## Final verdict

Partial: all 13 executable rows pass and no Spec defect was found, but the
managed sandbox blocks the authoritative `rtk make verify` gate at `/bin/ps`;
rerun that unchanged command in a full-access macOS session before this Spec
can receive `verdict: pass`.
