---
spec: 0097-a-wave-that-cannot-collide
date: 2026-09-02
build: e5814eadb616c837ac53506defffe1425b70b652
auditing_binary: "0.11.0 (e5814ead, built 2026-09-02T12:08:00-0300)"
auditor_staleness: "current: commit ancestry: auditing binary build commit e5814ead equals the audited tree head"
status: closed
verdict: pass
rows_blocked_environment: 1
rows_blocked_finding: 0
rows_blocked_declared: 0
surfaces: [backend, infra, cli]
---

# QA Report — A wave that cannot collide

## Scope and environment

- Scope: full authored QA Task `task_06`, covering all three user stories, four
  core features, success metrics, explicit Non-Goals, outside evidence, the
  glossary decision, and the Pull Request journey required by the gate.
- Actor: Supervisor operating the Roundfix CLI and daemon-backed Implement flow.
- Audited tree: `e5814eadb616c837ac53506defffe1425b70b652` on the per-Run
  branch `roundfix/run-run_20260902T134505Z_7af811aeb13c533a`.
- Pull Request: none open; Pull Request journeys are environment-blocked and
  require equivalent local or supervised evidence for every control.
- Declared surfaces: backend and infra. The public entry point used for
  Supervisor journeys is the CLI. No frontend or TUI surface is declared.
- `Unreachable Acceptance`: absent; no row may use a declared-blocked cause.

## Static gate

- `roundfix spec check 0097-a-wave-that-cannot-collide --strict`: pass — no
  findings; authored Verification commands were not executed. The checker
  skipped the absent Vocabulary Contract and absent `references/_index.md`.
- `make verify`: environment-blocked after two runs. Both runs reached the test
  suite; the second failed only two `internal/cli` force-stop tests because the
  sandbox denied process-table access with `operation not permitted`. The same
  two tests fail at unchanged base `0c17ca8d`; every package changed by this
  Spec passed. The first run also had one ACP test failure that passed focused
  and passed in the full rerun, so it was not reproducible.
- `make verify-docs`: pass — docs contract, repository contract, corpus budget,
  and corpus-wide Spec check all exited 0.
- Final mechanical report read: pass — zero findings, blocked rows, or repair
  failures; `Blocking=false`; report structure and every evidence path resolved.
- Static evidence:
  `docs/specs/0097-a-wave-that-cannot-collide/qa/evidence/2026-09-02-run/03-static-gates.txt`.

## Performed repairs

None.

## Assigned repair failures

None.

## Mechanical findings

None.

## Results

| # | Story / criterion / sweep | Actor and surface | Status | Provenance | Evidence |
| - | --- | --- | --- | --- | --- |
| QA-01 | A same-Wave collision is reported at authoring with both Tasks, every shared path, its source, and the `needs`-edge remedy. | Supervisor; CLI/backend | pass | observed | Built CLI exited 1 with `SC-WAVE-COLLISION`, both Task IDs, `internal/spec/collision.go (verification command)`, and the exact `needs` remedy; after adding the edge it exited 0 with no findings. Evidence: `docs/specs/0097-a-wave-that-cannot-collide/qa/evidence/2026-09-02-run/01-authoring-cli.txt`. |
| QA-02 | The Run refuses the same collision before Agent Selection, Agent Session, Task Worktree creation, or plan reordering. | Supervisor; backend | pass | observed | `TestTaskCycleRefusesAWaveCollision` passed against the Run Database, selection-attempt store, prepared-session runner, Task Worktree requests, on-disk Task status, and unchanged Task result. The error names all shared paths and only recommends a `needs` edge. Evidence: `docs/specs/0097-a-wave-that-cannot-collide/qa/evidence/2026-09-02-run/02-feature-checks.txt`. |
| QA-03 | Directly and transitively ordered Tasks are neither reported nor refused. | Supervisor; CLI/backend | pass | observed | The built CLI accepted a direct plus transitive shared-file chain; `TestCollisionsExcludesTransitivelyOrderedTasks` and `TestTaskCycleDispatchesSerializedCollisionGraph` passed, the latter observing dispatch order task_01 then task_02. Evidence: `docs/specs/0097-a-wave-that-cannot-collide/qa/evidence/2026-09-02-run/01-authoring-cli.txt`, `docs/specs/0097-a-wave-that-cannot-collide/qa/evidence/2026-09-02-run/02-feature-checks.txt`. |
| QA-04 | Verification, Context, and prior settlement commits each contribute collision evidence; missing Context remains covered; package selectors, flags, and test names never become paths. | Supervisor; backend | pass | observed | Seven focused collision tests passed, including the no-Context measured shape, declared Context, packed prior settlement commits with command lookup disabled, all pairs/paths, invalid tokens, transitive ordering, and invalid inputs. Evidence: `docs/specs/0097-a-wave-that-cannot-collide/qa/evidence/2026-09-02-run/02-feature-checks.txt`. |
| QA-05 | Bootstrap at Task Capacity above one serializes only bootstrap and survives repeated concurrent attempts without lock collision. | Supervisor; infra/backend | pass | observed | Race-enabled test admitted four siblings across six rounds through a real exclusive lock: 24 commands, zero collisions. The independent-Wave capacity test and assembled CLI concurrent-bootstrap journey also passed, confirming Task execution remains concurrent. Evidence: `docs/specs/0097-a-wave-that-cannot-collide/qa/evidence/2026-09-02-run/02-feature-checks.txt`. |
| QA-06 | Bootstrap failure before start and after work began are distinct, with post-start work reported as possibly applied. | Supervisor; infra/backend | pass | observed | A missing root produced `before-start` / `bootstrap work did not start`; a command wrote `complete` then exited 7 and produced `after-start` / `bootstrap work may have been applied`, with the written state independently read back. Evidence: `docs/specs/0097-a-wave-that-cannot-collide/qa/evidence/2026-09-02-run/02-feature-checks.txt`. |
| QA-07 | Task Worktree creation failure names Run, Task, and configured concurrency, gives the next action, and retains the underlying filesystem error. | Supervisor; CLI/infra | pass | observed | The real filesystem-failure test observed Run `creation-context`, Task `task_02`, concurrency `4`, and `next action:`; `errors.As` and `errors.Is` both reached the underlying `os.PathError`. The assembled CLI failure journey also passed. Evidence: `docs/specs/0097-a-wave-that-cannot-collide/qa/evidence/2026-09-02-run/02-feature-checks.txt`. |
| QA-08 | Non-Goals hold: no default concurrency or recommendation change, no authoring-skill wave planner, no integration-conflict resolver, and no settlement/Verification behavior change. | Maintainer; repository surface | pass | observed | Base-to-head diff changes no `.agents/skills`, `internal/config`, Makefile, dependency file, or existing integration-conflict path. The Run adds a refusal before dispatch and never rewrites the graph; full changed-file audit found no Governed Path. Evidence: `docs/specs/0097-a-wave-that-cannot-collide/qa/evidence/2026-09-02-run/04-outside-and-scope.txt`. |
| QA-09 | Outside evidence corroborates the measured no-Context collision from archived Spec 0113 task files, independently of this Spec's artifacts. | Supervisor; repository evidence | pass | outside | Archived Spec `0113-a-refused-gate-writes-its-refusal-once` states task_05 and task_07 ran as siblings on 2026-08-26, conflicted on `mechanical_test.go`, and discarded a passing task_05. Neither Task declares Context; both Verifications name `internal/speccheck/mechanical.go`; the repaired graph now orders task_05 after task_07. Evidence: `docs/specs/0097-a-wave-that-cannot-collide/qa/evidence/2026-09-02-run/04-outside-and-scope.txt`. |
| QA-10 | Glossary decision: determine whether the known Task-path set is a durable product term or an internal implementation detail. | Maintainer; docs/backend | pass | observed | No glossary term is owed. Public diagnostics use existing `Task` and `Wave` plus ordinary “repository files”; `TaskTouchSet` is an internal Go type and is not emitted or persisted. `CONTEXT.md` already defines every public term this Spec reports. |
| QA-11 | Pull Request controls: approval, checks/status, unresolved threads, Merge-Ready acceptance, and review-artifact ancestry. | Maintainer; Pull Request | blocked (environment: no open Pull Request) | environment | Equivalent evidence records: no acceptance; local checks and their environment-limited result; no thread surface; no Review Source/Merge-Ready evidence; no review artifact; and feature evidence at head `e5814ead`. Unblocking action is to integrate, open the target-branch Pull Request, and observe all five controls there. Evidence: `docs/specs/0097-a-wave-that-cannot-collide/qa/evidence/2026-09-02-run/05-pull-request-equivalent.txt`. |

## Row evidence declarations

### QA-01

```yaml
inputs:
  - kind: repository_path
    ref: internal/speccheck/**
  - kind: repository_path
    ref: internal/spec/**
  - kind: repository_path
    ref: docs/specs/0097-a-wave-that-cannot-collide/**
```

Probe: remove ordering while keeping two Verification-named shared paths; then
restore ordering and rerun to confirm recovery through the documented remedy.

Result: pass. The first built-CLI read failed with the expected finding and the
fresh read after the documented repair passed.

### QA-02

```yaml
inputs:
  - kind: repository_path
    ref: internal/daemon/**
  - kind: repository_path
    ref: internal/spec/**
  - kind: repository_path
    ref: docs/specs/0097-a-wave-that-cannot-collide/**
```

Probes: inspect durable Run state rather than stdout alone, and check that the
refusal recommends ordering without mutating the authored Task Graph.

Result: pass. The durable Run Database and worktree/session fakes independently
confirmed nothing dispatched and no Task status changed.

### QA-03

```yaml
inputs:
  - kind: repository_path
    ref: internal/spec/**
  - kind: repository_path
    ref: internal/daemon/**
```

Probes: direct ordering and a three-Task transitive chain.

Result: pass. Both the built CLI and the shared rule accepted the chain; the Run
caller dispatched the direct case in dependency order.

### QA-04

```yaml
inputs:
  - kind: repository_path
    ref: internal/spec/**
```

Probes: missing Context, each source in isolation, and invalid tokens that look
path-like but do not resolve to repository files.

Result: pass. All named source and filtering checks ran and passed.

### QA-05

```yaml
inputs:
  - kind: repository_path
    ref: internal/worktree/**
```

Probes: four concurrent sibling commands over six rounds and race detection;
both target simultaneous admission rather than timing luck.

Result: pass. Twenty-four real commands completed without a lock collision and
the race detector remained clean.

### QA-06

```yaml
inputs:
  - kind: repository_path
    ref: internal/worktree/**
```

Probes: invalid command before process start and a command that writes durable
state before exiting nonzero.

Result: pass. Both typed stages and their maintainer-facing messages matched,
and the post-start state survived for an independent read.

### QA-07

```yaml
inputs:
  - kind: repository_path
    ref: internal/worktree/**
  - kind: repository_path
    ref: internal/daemon/**
```

Probes: real filesystem failure, `errors.Is`/`errors.As` chain confirmation,
and sequential convenience-path concurrency `1` versus configured concurrency.

Result: pass. The contextual message and wrapped cause were both observed.

### QA-08

```yaml
inputs:
  - kind: repository_path
    ref: internal/**
  - kind: repository_path
    ref: .agents/skills/**
  - kind: repository_path
    ref: docs/specs/0097-a-wave-that-cannot-collide/**
```

Result: pass. Every Task commit's actual paths were read with `git diff-tree`;
no Governed Path or Non-Goal surface changed.

### QA-09

```yaml
inputs:
  - kind: repository_path
    ref: docs/history/specs/0113-*/task_05.md
  - kind: repository_path
    ref: docs/history/specs/0113-*/task_07.md
```

Result: pass. The outside source is the archived 0113 graph and its two Task
files, not any artifact authored by this Spec.

### QA-10

```yaml
inputs:
  - kind: repository_path
    ref: CONTEXT.md
  - kind: repository_path
    ref: internal/spec/collision.go
```

Result: pass. No glossary edit is required.

### QA-11

```yaml
inputs:
  - kind: live_service
    ref: Pull Request and Review Source state for ma/0097-a-wave-that-cannot-collide
  - kind: repository_path
    ref: docs/specs/0097-a-wave-that-cannot-collide/**
```

Result: blocked (environment: no open Pull Request), with equivalent evidence
for every enumerated control. The gate's typed environment-block rule therefore
does not cap the verdict.

## Mechanical skips

| Detector | Missing artifact |
| --- | --- |
| authorization bounded paths | tooling authorization |
| consequent-fix commit order | consequent-fix declaration |
| QA Report structure | QA Report |
| QA evidence paths | QA Report |

## Findings

None.

## Blocked and skipped

- QA-11 is blocked by the confirmed absence of an open Pull Request. Equivalent
  evidence for approval, checks/status, unresolved threads, Merge-Ready, and
  ancestry is recorded in
  `docs/specs/0097-a-wave-that-cannot-collide/qa/evidence/2026-09-02-run/05-pull-request-equivalent.txt`.
  Unblock by integrating the Run, opening the Pull Request on
  `ma/0097-a-wave-that-cannot-collide`, and observing those controls against its
  actual head.
- The repository Verification is environment-limited by sandbox-denied process
  table access. The unchanged-base reproduction and every runnable substitute
  are recorded in
  `docs/specs/0097-a-wave-that-cannot-collide/qa/evidence/2026-09-02-run/03-static-gates.txt`.
- No row was skipped. No row is finding-blocked or declared-blocked.

## Coverage

All three user stories, four core features, four Goals, four Non-Goals, success
metrics, the authored QA Task criteria, outside evidence, glossary decision, and
Pull Request journey are covered.

- Rows: 11 total; 10 pass; 1 blocked (environment); 0 fail; 0 skipped; 0 pending.
- `rows_blocked_environment`: 1.
- `rows_blocked_finding`: 0.
- `rows_blocked_declared`: 0.
- Probes: same-Wave collision and remedy, direct and transitive ordering,
  missing Context, all three touch sources, invalid tokens, repeated concurrent
  bootstrap under race detection, before/after-start failure, real filesystem
  failure, unchanged-base static-gate reproduction, and commit-scope audit.
- Frontend/TUI sweep: not applicable; neither surface is declared.
- Non-Goals: all four checked with no scope creep.

## Final verdict

Pass — all 10 runnable rows passed, the sole environment-blocked Pull Request
row records its cause and equivalent evidence for every control, and there are
no findings, declared blocks, finding blocks, skips, or pending rows.
