---
spec: 0117-a-window-the-preflight-owns
date: 2026-08-26
build: 79f3a2eb
status: closed
verdict: pass
rows_blocked_environment: 1
rows_blocked_finding: 0
rows_blocked_declared: 0
surfaces: [cli, backend, data, docs]
---

# QA Report — A window the Preflight owns

## Scope and environment

- Scope: full authored QA gate for all PRD user stories, acceptance criteria,
  core features, and Non-Goals.
- Build: `79f3a2eb` on the Run Worktree branch, rebuilt with `rtk make build`.
- Real entry point: `./bin/roundfix` from the current source tree.
- Data: isolated temporary homes and disposable Git repositories under `/tmp`;
  no user Run Database is read or changed.
- Pull Request: none open. Pull Request observation is environment-blocked and
  must rely on equivalent Run-terminal evidence.
- Authoring precondition: `rtk ./bin/roundfix spec check
  0117-a-window-the-preflight-owns --strict` exited `0` with no findings.
  `SC-REF-UNRESOLVED` skipped because the Spec has no `references/_index.md`,
  so row QA-10 retains the missing path-resolution check.

## Static gate

- `rtk make verify` ran against the current tree. Every reported package
  passed except `internal/cli`, where two force-stop integration tests failed
  because the sandbox denied process-table enumeration with `operation not
  permitted`.
- The same two tests failed for the same denial on the clean Spec target
  checkout at `730b26ab`, proving an unchanged-base environment constraint.
  The failure does not implicate a Run Window entry point, observable, or
  evidence path. A full-access session that permits process-table enumeration
  is the exact unblocking requirement.
- Fresh focused checks passed for `internal/store`, every `Window` and
  `ImplementRunWindow` CLI test, `internal/watch`, rendered configuration, and
  the vocabulary/docs contract. Exact commands and outputs are in
  [the command transcript](evidence/2026-08-26-run-window/command-transcript.md).
- `rtk make verify-docs` passed after the report and evidence were written,
  including docs contracts, repository contracts, corpus budget, and the
  repository-wide Spec check.

## Performed repairs

None.

## Assigned repair failures

None.

## Mechanical findings

None.

## Results

| # | Story / criterion / sweep | Actor and surface | Status | Evidence |
| - | --- | --- | --- | --- |
| QA-01 | User Story 1 / Core Feature 1: set, show, clear, repository scope, restart persistence | Supervisor · CLI/data | pass | Live set from 23:45 +03 stored next-day 07:00; nested-directory restart reread it; another Git root had an independent window; clear and absent reread passed. |
| QA-02 | User Story 2 / Acceptance 1: a passed cutoff refuses `implement`, names both times and recovery, exit `2`, no Run or preflight side effect | Supervisor · CLI/backend/data | pass | Public `implement` exited `2`; `runs list` returned `No Runs found`, SQL run count was `0`, and the worktree scan was empty. |
| QA-03 | Acceptance 2 / outside evidence: a 07:00 wall-clock cutoff requested after 23:00 resolves to the next day and remains open | Supervisor · CLI | pass | At 23:45 +03 the public command stored 2026-08-27 07:00 +03 and `show` reported 7h14m remaining. Outside source: pre-Spec workflow record at origin/main commit `2aa4511e`. |
| QA-04 | Acceptance 3: repeated set is byte-stable without `--force`; forced set replaces; invalid/past input does not store | Supervisor · CLI/data | pass | Repeat set retained `1787803200|1787777135`; force changed it to `1787806800|1787777168`; a past forced set exited `2` and retained the latter bytes. |
| QA-05 | Core Feature 4 / Acceptance 5: a Run inside the window is created and a predicted crossing reports cutoff, remaining time, and maximum duration | Supervisor · CLI/backend | pass | Public `implement` created Run `run_20260826T205318Z_8513a3442369b65c` and printed cutoff 23:55, remaining 102s, and `max_run_duration` 2h. |
| QA-06 | User Story 3 / Acceptance 4: a Run created before cutoff reaches a terminal outcome after cutoff without window interruption | Supervisor · CLI/backend/data | pass | Run started 23:53:18 +03 and reached its own terminal `Failed` outcome at 23:55:11 +03 from the injected ACP runtime cause; the Run Window did not stop it. |
| QA-07 | Acceptance 6: schema 12 migrates to 13; without a window existing behavior is unchanged | Operator · data/CLI | pass | Copied database moved 12→13, preserved its one Run, added the table empty, returned the absent-window public state, and repeated idempotently. |
| QA-08 | Core Feature 5 / Acceptance 7: command, configuration, guide, glossary, and vocabulary detector explain one start bound and one duration bound | Supervisor/maintainer · CLI/docs | pass | Help and live `show` agree with both guides, rendered config, and glossary; docs contract and config rendering checks exited `0`. |
| QA-09 | Non-Goals: no timer/scheduler, no window-driven stop, no changed Run-budget semantics, no enforcement on `fetch`, `resolve`, or `watch`, no supervisor-loop skill | Operator · CLI/backend | pass | Commit/path audit found enforcement only in `window` and `implement`; `internal/watch` and `skills` diffs were empty; the crossing Run passed the cutoff without a Stop Request. |
| QA-10 | Skipped `SC-REF-UNRESOLVED` equivalent: every repository path and named delivered surface used by this Spec resolves | Maintainer · repository/docs | pass | All cited guides, accepted ADRs, source files, guides, glossary, and outside workflow record resolved; strict Spec check had no finding. |
| QA-11 | User Story 3 Pull Request continuation: completed work can proceed to Pull Request and merge without a window-owned interruption | Supervisor · Pull Request | blocked (environment: no open Pull Request) | Equivalent evidence: Run Event Stream proves the crossing Run reached a terminal outcome after cutoff, and isolated-checkout HEAD stayed `79f3a2eb`; rerun PR observation after the target branch has an open Pull Request. |

## Detailed evidence

### QA-01

```yaml
inputs:
  - kind: repository_path
    ref: internal/cli/window.go
  - kind: repository_path
    ref: internal/store/store.go
```

- Entry and steps: from the Run Worktree, set `07:00` under a scratch home at
  23:45 +03; reopen from `internal/`; set a separate 09:00 window from the user
  checkout with the same database; clear and reopen the first state.
- Expected/observed: next-day cutoff, restart persistence, Git-root isolation,
  and explicit absent state all matched.
- Independent confirmation: SQL row values plus new-process public `show`.
- Evidence: [command transcript](evidence/2026-08-26-run-window/command-transcript.md#night-start-restart-repository-scope-and-set-semantics).

### QA-02

```yaml
inputs:
  - kind: repository_path
    ref: internal/cli/implement.go
  - kind: repository_path
    ref: internal/store/store.go
```

- Entry and steps: after a publicly set absolute cutoff expired, invoke the
  real `implement` command directly on the committed Spec.
- Expected/observed: exit `2`, literal cutoff/current time, force-set and clear
  recovery, and the standard no-side-effects block.
- Independent confirmation/persistence: `runs list`, SQL, and the worktree
  filesystem all remained empty after the refusal.
- Evidence: [command transcript](evidence/2026-08-26-run-window/command-transcript.md#closed-window-refusal).

### QA-03

```yaml
inputs:
  - kind: repository_path
    ref: internal/cli/window.go
  - kind: repository_path
    ref: docs/workflow/2026-08-26-triage-the-queue-earns-its-tokens.md
  - kind: elapsed_time
    ref: local wall-clock next-occurrence calculation
```

- Entry and steps: run `window set 07:00` with local wall clock 23:45 +03,
  then reopen with `show`.
- Expected/observed: the stored instant was 07:00 the next day and the window
  remained open with more than seven hours left.
- Outside evidence: origin/main commit `2aa4511e` carries the pre-Spec workflow
  measurement that a caller-side check can be skipped and cites the prior
  `window.sh` naive-comparison failure. The live flow tests that premise through
  the shipped command, not a Spec-authored fixture.
- Evidence: [command transcript](evidence/2026-08-26-run-window/command-transcript.md#outside-evidence-and-scope).

### QA-04

```yaml
inputs:
  - kind: repository_path
    ref: internal/cli/window.go
  - kind: repository_path
    ref: internal/store/store.go
```

- Entry and steps: set 07:00, repeat with 08:00, repeat with `--force`, then
  attempt a past absolute instant with `--force`.
- Expected/observed: repeat set exited `0` without byte change; force replaced;
  past input exited `2` without changing the replacement.
- Independent confirmation: SQL reads after each write attempt.
- Evidence: [command transcript](evidence/2026-08-26-run-window/command-transcript.md#night-start-restart-repository-scope-and-set-semantics).

### QA-05

```yaml
inputs:
  - kind: repository_path
    ref: internal/cli/implement.go
  - kind: elapsed_time
    ref: Run Window remaining duration at Run creation
```

- Entry and steps: set a 23:55 cutoff, invoke the real `implement` command at
  23:53:18 with the configured two-hour maximum, and hold the isolated ACP turn.
- Expected/observed: the Run was created and the startup report named cutoff,
  102 seconds remaining, and a two-hour maximum without refusing.
- Independent confirmation: public Run listing resolved the same Run id.
- Evidence: [command transcript](evidence/2026-08-26-run-window/command-transcript.md#crossing-and-start-only-control).

### QA-06

```yaml
inputs:
  - kind: repository_path
    ref: internal/cli/implement.go
  - kind: elapsed_time
    ref: cutoff crossing during one Run
```

- Entry and steps: keep Run `run_20260826T205318Z_8513a3442369b65c` inside an
  isolated scripted ACP turn until after its 23:55 cutoff, then release it into
  a named runtime failure.
- Expected/observed: Run start preceded the cutoff and its own terminal outcome
  followed it; no window-owned stop or shortening appeared.
- Independent confirmation/persistence: `runs list` and `events --filter
  outcome` both report terminal `Failed` at 23:55:11 +03; the retained Run
  Worktree remains inspectable. The scripted ACP adapter is the disclosed
  parity deviation and supplied only timing plus the terminal cause.
- Evidence: [command transcript](evidence/2026-08-26-run-window/command-transcript.md#crossing-and-start-only-control).

### QA-07

```yaml
inputs:
  - kind: repository_path
    ref: internal/store/testdata/2026-08-11-prechange-roundfix.db
  - kind: repository_path
    ref: internal/store/store.go
```

- Entry and steps: copy the historical database, record version/table/Run
  counts, open it through public `window show`, record again, and reopen once.
- Expected/observed: version 12 became 13, the one Run persisted, the new table
  remained empty, and absent-window behavior stayed successful and idempotent.
- Independent confirmation: direct SQLite shape and row counts before/after.
- Evidence: [command transcript](evidence/2026-08-26-run-window/command-transcript.md#schema-migration).

### QA-08

```yaml
inputs:
  - kind: repository_path
    ref: CONTEXT.md
  - kind: repository_path
    ref: docs/user-guide/*.md
  - kind: repository_path
    ref: internal/config/config.go
  - kind: repository_path
    ref: internal/docscontract/**
```

- Entry and steps: follow `window --help`, `window show`, both guide sections,
  rendered configuration, and glossary as their intended readers; then execute
  the docs/vocabulary and rendered-config checks.
- Expected/observed: every surface states start bound versus duration bound;
  `Run Window`, `RunWindow`, and `run_window` are documented; both checks pass.
- Independent confirmation: focused CLI tests cover help/show output.
- Evidence: [command transcript](evidence/2026-08-26-run-window/command-transcript.md#focused-checks-and-docs).

### QA-09

```yaml
inputs:
  - kind: repository_path
    ref: internal/**
  - kind: repository_path
    ref: skills/**
```

- Entry and steps: audit every Task commit with `git diff-tree`, inspect the
  base-to-head path set, search all emitted window tokens, and compare
  `internal/watch` plus `skills` to the Spec base.
- Expected/observed: no governed tooling path changed; enforcement exists only
  in `window` and `implement`; watch and skills are byte-unchanged; no timer,
  scheduler, supervisor-loop skill, or window-driven Stop Request shipped.
- Independent confirmation: the live crossing Run continued past cutoff.
- Evidence: [command transcript](evidence/2026-08-26-run-window/command-transcript.md#outside-evidence-and-scope).

### QA-10

```yaml
inputs:
  - kind: repository_path
    ref: docs/specs/0117-a-window-the-preflight-owns/**
  - kind: repository_path
    ref: docs/agents/**
```

- Entry and steps: resolve each path and accepted ADR named by the PRD,
  TechSpec, Task Graph, and Task files after strict Spec check disclosed its
  skipped references-index detector.
- Expected/observed: every cited repository path and delivered surface exists;
  the task node is terminal and depends on completed task_05.
- Independent confirmation: strict Spec check returned no findings.
- Evidence: the resolved path inventory and precondition output are recorded
  in the [command transcript](evidence/2026-08-26-run-window/command-transcript.md#authoring-and-static-gates).

### QA-11

```yaml
inputs:
  - kind: live_service
    ref: Pull Request on ma/a-window-the-preflight-owns
  - kind: repository_path
    ref: internal/cli/implement.go
```

- Cause/proof: the Roundfix QA assignment states that no Pull Request is open
  and Pull Request journeys are environment-blocked. The Run Worktree branch is
  never pushed and has no Pull Request of its own.
- Equivalent evidence: the public Run Event Stream proves a Run admitted before
  cutoff reached its terminal outcome after cutoff; the isolated checkout kept
  HEAD `79f3a2eb`, so the Run Window introduced neither a stop nor a history
  mutation at the boundary.
- Unblocking action: after a Pull Request opens on
  `ma/a-window-the-preflight-owns`, observe the head-bound checks, review
  evidence, and merge journey read-only.
- Evidence: [command transcript](evidence/2026-08-26-run-window/command-transcript.md#crossing-and-start-only-control).

## Mechanical skips

| Detector | Missing artifact |
| --- | --- |
| authorization bounded paths | tooling authorization |
| consequent-fix commit order | consequent-fix declaration |
| QA Report structure | QA Report |
| QA evidence paths | QA Report |

## Findings

None.

## Blocked and skipped

- QA-11 — `blocked (environment: no open Pull Request)`. The assignment proves
  the cause. Equivalent public Run and event-stream evidence demonstrates the
  window admits work before cutoff and does not interrupt it afterwards. Open a
  Pull Request on `ma/a-window-the-preflight-owns` to exercise the read-only
  review and merge journey.
- Static gate environment constraint — `make verify` cannot pass in this
  sandbox because two unrelated force-stop tests require process-table access.
  The unchanged target branch reproduces the same denial. Run the gate in a
  full-access session that permits process enumeration to remove this parity
  gap; no matrix row depends on those force-stop tests.
- Skipped matrix rows: none. Declared-blocked rows: none. Finding-blocked rows:
  none.

## Coverage

Ten of eleven rows passed live. One Pull Request row is environment-blocked
with equivalent observed evidence. All 3 user stories, 7 acceptance criteria,
5 core features, 5 Non-Goals, the skipped reference detector, and all declared
surfaces are represented.

- pass: 10
- fail: 0
- blocked (environment): 1
- blocked (finding): 0
- blocked (declared): 0
- skipped: 0
- impact findings: 0 Blocks-Completion, 0 Data-Loss, 0 Trust-Damage, 0
  Friction, 0 Cosmetic

Behavior probes selected for the high-risk CLI journeys: repeat set without
force (double submit), force replacement, invalid/past input, nested-directory
reopen, process restart/public reread, exact-cutoff boundary, and elapsed-time
cutoff crossing. Browser-only probes do not apply to this CLI-only Spec.

## Final verdict

Pass. Every runnable criterion passed; the one environment-blocked Pull Request
row records its proved cause, exact unblocking action, and equivalent public
Run/Event evidence. There are no failed, skipped, declared-blocked, or
finding-blocked rows, and every evidence path resolves.
