---
spec: 0141-a-commit-that-carries-the-work-and-nothing-else
date: 2026-09-17
build: 0a0cf9829a326e7813a6407eca7c0b8fdcb375cb
auditing_binary: "0.13.1 (1682ae6f, built 2026-09-17 16:41:25 -0300)"
auditor_staleness: "stale: commit ancestry: build commit predates audited tree"
status: closed
verdict: pass
rows_blocked_environment: 1
rows_blocked_finding: 0
rows_blocked_declared: 0
surfaces: [backend, cli, docs]
---

# QA Report — A commit that carries the work and nothing else

## Scope and environment

- Scope: full authored terminal gate from `task_04`; its dependency `task_05`
  and every earlier non-QA dependency are `completed`.
- Candidate: `0a0cf9829a326e7813a6407eca7c0b8fdcb375cb` on the Run Worktree branch.
- Actor: a Roundfix Supervisor exercising task settlement and QA commit
  boundaries through the public `Engine.TaskCycle` and
  `FilterStageablePaths` package seams.
- Pull Request: none open; the prompt declares Pull Request journeys
  environment-blocked.
- Precondition: `roundfix spec check 0141-a-commit-that-carries-the-work-and-nothing-else --strict`
  exited 0 with `No findings`; authored Verification commands were not run.
  It skipped absent rollup, backlog and reference-index artifacts plus the
  Vocabulary Contract detector. The audited TechSpec contains a
  `## Vocabulary Contract`; that skip remains disclosed as a stale-auditor
  limitation.
- Parity: focused flows use real disposable Git repositories and production
  commit filters. No live Pull Request was available.

## Performed repairs

None.

## Assigned repair failures

None.

## Authorization audit inputs

None.

## Mechanical findings

None.

## Results

| # | Story / criterion / sweep | Actor and surface | Status | Provenance | Evidence |
| - | --- | --- | --- | --- | --- |
| R1 | Repository Verification | Supervisor; CLI/repository gate | pass | Requirement 1; repository Verification | Daemon-supplied `make verify`: exit 0, diagnostics removed. |
| R2 | Tracked and untracked executable handling | Supervisor; backend commit filter | pass | Requirement 2 | Focused daemon tests passed tracked executable, tracked regular file, failed-index and owner/group/other untracked executable cases. |
| R3 | Lost output refuses completion and names its cause | Supervisor; backend task settlement | pass | Requirement 3 | Lost-output `Engine.TaskCycle` flow passed and asserted failed status, zero commits, outcome, event and console line. |
| R4 | Refused-by-design output keeps external Spec Root working | Supervisor; backend task settlement | pass | Requirement 4 | External-root flow passed; the clean-task canary passed in the same fresh run. |
| R5 | QA commit excludes Verification writes and retains QA material | Supervisor; backend QA settlement | pass | Requirement 5 | Both verifier-write and no-write flows passed and asserted exact commit paths, seed, evidence and Agent write. |
| R6 | Three pre-existing executable source files reach a settlement commit | Supervisor; backend/Git fixture | pass | Requirement 6; outside-evidence row | Disposable replay kept and committed all three paths; see `qa/evidence/2026-09-17-qa/tracked-executable-replay.md`. |
| R7 | Success Metrics and API Contracts obey the promise rule | Spec maintainer; docs/checker | pass | Requirement 7 | Strict Spec check returned `No findings`; coverage equivalence passed; six promises map to Tasks 01-04. |
| R8 | Pull Request controls | Reviewer; Pull Request | blocked (environment: no open Pull Request) | Pull Request row | Equivalent controls recorded below; candidate head `0a0cf9829a326e7813a6407eca7c0b8fdcb375cb`. |

## Mechanical skips

| Detector | Missing artifact |
| --- | --- |
| authorization bounded paths | tooling authorization |
| consequent-fix commit order | consequent-fix declaration |
| QA Report structure | QA Report |
| QA evidence paths | QA Report |

## Static gate

- `make verify`: pass, exit 0. The Daemon ran it outside the Agent sandbox and
  removed diagnostics on success. It was recorded as supplied and not rerun.
- `roundfix spec check 0141-a-commit-that-carries-the-work-and-nothing-else --strict`:
  pass, exit 0, `No findings`; authored Verification commands were not run.
- `GOCACHE=/private/tmp/roundfix-0141-qa-go-cache go test -count=1 -v -run '^(TestFilterStageablePaths(KeepsTrackedExecutable|KeepsTrackedRegularFile|RefusesUntrackedExecutableWithMode|DropsRegularFileWithAnyExecutePermission|RefusesExecutableWhenIndexQueryFails|PreservesOtherRefusals)|TestTaskCycleLostOutputRefusesCompletion|TestQAReportCommitExcludesVerificationWrites)$' ./internal/daemon`:
  pass, exit 0; every selected test and subtest passed.
- `GOCACHE=/private/tmp/roundfix-0141-qa-spec-cache go test -count=1 -v -run '^TestCoverageEquivalence$' ./internal/spec`:
  pass, exit 0.

## Row evidence

### R1

- Entry point and steps: the Daemon ran the repository's exact `make verify`
  command before the Agent session.
- Expected observable: exit 0 with no retained success diagnostics.
- Independent confirmation and persistence: the Daemon supplied both the exit
  result and diagnostics lifecycle in the QA prompt, bound to this Run.
- Evidence path: this report's Static gate section.

```yaml
inputs:
  - kind: repository_path
    ref: Makefile
  - kind: repository_path
    ref: internal/**
```

### R2

- Entry point and steps: ran the `FilterStageablePaths` flows against fresh Git
  repositories, including three execute modes and an unreadable index answer.
- Expected observable: tracked paths are kept; untracked executables are
  dropped with `executable file` and the exact mode.
- Independent confirmation and persistence: tests check kept and dropped
  collections; the separately named permission-variant regression repeats the
  refusal.
- Evidence path: Static gate and `internal/daemon/task_engine_test.go`.

```yaml
inputs:
  - kind: repository_path
    ref: internal/daemon/task_engine.go
  - kind: repository_path
    ref: internal/daemon/task_engine_test.go
```

### R3

- Entry point and steps: ran a lost-output Task through `Engine.TaskCycle`.
- Expected observable: failed Task, no settlement commit, path plus cause in
  the outcome.
- Independent confirmation and persistence: the flow rereads Task status from
  disk and separately checks the Run result, event and console output.
- Evidence path: Static gate and `internal/daemon/task_engine_test.go`.

```yaml
inputs:
  - kind: repository_path
    ref: internal/daemon/task_engine.go
  - kind: repository_path
    ref: internal/daemon/task_engine_test.go
```

### R4

- Entry point and steps: ran an external-Spec-Root Task and a clean Task through
  `Engine.TaskCycle`.
- Expected observable: external-only refusal settles completed; clean flow
  retains prior commit behavior.
- Independent confirmation and persistence: the flow rereads external Task
  status from disk and checks the refusal event and operator line; the clean
  canary checks one commit and no refusal event.
- Evidence path: Static gate and `internal/daemon/task_engine_test.go`.

```yaml
inputs:
  - kind: repository_path
    ref: internal/daemon/task_engine.go
  - kind: repository_path
    ref: internal/daemon/task_engine_test.go
```

### R5

- Entry point and steps: ran QA settlement with a tracked file written inside
  the Verification window, then with no Verification write.
- Expected observable: the verifier-written path is absent while the seeded
  report, evidence, Agent write and QA Task file remain present.
- Independent confirmation and persistence: both cases assert the exact sorted
  commit paths and final verdict; the no-write case is the adjacent canary.
- Evidence path: Static gate and `internal/daemon/task_engine_test.go`.

```yaml
inputs:
  - kind: repository_path
    ref: internal/daemon/task_engine.go
  - kind: repository_path
    ref: internal/daemon/task_engine_test.go
```

### R6

- Entry point and steps: copied the three executable files already tracked by
  this repository into a disposable Git repository, edited all three, passed
  them to `FilterStageablePaths`, staged the returned paths and committed them.
- Expected observable: all three are kept, none is dropped, and all three occur
  in the resulting commit.
- Independent confirmation and persistence: a fresh `git diff-tree` read names
  all three paths; `git diff 9103a133^ 9103a133` confirms the new index check
  precedes the existing executable refusal.
- Outside source: `.agents/skills/systematic-debugging/find-polluter.sh`,
  `.githooks/commit-msg`, and `.githooks/pre-commit`; all predate this Spec.
- Evidence path: `qa/evidence/2026-09-17-qa/tracked-executable-replay.md`
  and `qa/evidence/2026-09-17-qa/tracked_executable_replay.go`.

```yaml
inputs:
  - kind: repository_path
    ref: .agents/skills/systematic-debugging/find-polluter.sh
  - kind: repository_path
    ref: .githooks/commit-msg
  - kind: repository_path
    ref: .githooks/pre-commit
  - kind: repository_path
    ref: internal/daemon/task_engine.go
  - kind: repository_path
    ref: docs/specs/0141-a-commit-that-carries-the-work-and-nothing-else/qa/evidence/2026-09-17-qa/tracked_executable_replay.go
```

### R7

- Entry point and steps: ran the strict checker and coverage-equivalence test,
  then traced all six numbered promises through the TechSpec map and Tasks.
- Expected observable: each metric and contract is declared, each metric is
  mapped, and every numbered promise reaches a Task Reference.
- Independent confirmation and persistence: the checker reported no findings
  and the coverage-equivalence test exited 0.
- Evidence path: `_prd.md`, `_techspec.md`, Tasks 01-04 and Static gate.

```yaml
inputs:
  - kind: repository_path
    ref: docs/specs/0141-a-commit-that-carries-the-work-and-nothing-else/_prd.md
  - kind: repository_path
    ref: docs/specs/0141-a-commit-that-carries-the-work-and-nothing-else/_techspec.md
  - kind: repository_path
    ref: docs/specs/0141-a-commit-that-carries-the-work-and-nothing-else/task_*.md
```

### R8

- Entry point and steps: used the prompt's supervised Pull Request fact for the
  target branch; no lookup was attempted from the never-pushed Run branch.
- Expected observable: approval, checks/status, unresolved threads,
  Merge-Ready acceptance and review-artifact ancestry are accounted for.
- Independent confirmation and persistence: no approval was obtained;
  `make verify` passed; no PR exists on which a review thread could stand; no
  Merge-Ready acceptance exists; no review artifact exists whose ancestry could
  be claimed. Candidate head is recorded above.
- Evidence path: this report and the Daemon QA prompt.

```yaml
inputs:
  - kind: live_service
    ref: GitHub Pull Request for feat/0141-commit-carries-the-work
  - kind: repository_path
    ref: docs/specs/0141-a-commit-that-carries-the-work-and-nothing-else/qa/qa-report-2026-09-17.md
```

## Findings

None.

## Blocked and skipped

- R8 is blocked by `no open Pull Request`. Unblocking action: open the Spec Pull
  Request on `feat/0141-commit-carries-the-work`. Equivalent supervised
  evidence records every unavailable control, while repository Verification
  supplies checks/status, so this environment block does not prevent `pass`.
- No row is finding-blocked, declared-blocked or skipped.

## Coverage

- Coverage sources: 10/10 — Requirements 1-7, outside-evidence row, Pull
  Request row and repository Verification.
- Results: 7 passed, 1 environment-blocked, 0 failed, 0 finding-blocked,
  0 declared-blocked, 0 skipped, 0 pending.
- The PRD declares no Unreachable Acceptance entries and no frontend surface.
- The outside-evidence row performs edit, filter, stage, commit and fresh
  commit-tree read; no row rests only on reading a file.

## Final verdict

Pass: every runnable criterion passed; the sole environment-blocked Pull
Request row records its cause and equivalent supervised evidence for all five
controls, and there are no findings, declared blocks, skips or pending rows.
