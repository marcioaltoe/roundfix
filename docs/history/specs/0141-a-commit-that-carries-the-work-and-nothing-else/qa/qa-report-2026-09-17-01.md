---
spec: 0141-a-commit-that-carries-the-work-and-nothing-else
date: 2026-09-17
build: 5966b69e64a638ac97cc352d6f6b7f618e3017e4
auditing_binary: "0.13.1 (2e6041f5, built 2026-09-17 17:34:36 -0300)"
auditor_staleness: "stale: commit ancestry: build commit predates audited tree"
status: closed
verdict: pass
rows_blocked_environment: 1
rows_blocked_finding: 0
rows_blocked_declared: 0
surfaces: [backend, cli, docs]
---

# QA Report

## Scope and environment

Full terminal gate for build `5966b69e64a638ac97cc352d6f6b7f618e3017e4` on Run Worktree branch
`roundfix/run-run_20260917T203446Z_96cd97081393140e`. The Spec target branch is
`feat/0141-commit-carries-the-work`; the Run Worktree branch is local-only and
has no Pull Request. The actor is a Roundfix Supervisor exercising the daemon's
public settlement outcomes through its package-level fixture runners and the
`roundfix spec check` CLI. No frontend is declared. The PRD has no Unreachable
Acceptance declarations.

Precondition: `roundfix spec check 0141-a-commit-that-carries-the-work-and-nothing-else --strict`
exited 0 with `No findings`; its four reported skips name absent optional
artifacts or a detector that did not recognize the present Vocabulary Contract.

## Performed repairs

None.

## Assigned repair failures

None.

## Authorization audit inputs

None.

## Mechanical findings

None.

## Static gate

The Daemon ran `make verify` outside the Agent sandbox for this build. It exited
0 and removed its success diagnostics. Per the QA contract, this gate records
that supervised fact and does not rerun the command.

## Results

| # | Story / criterion / sweep | Actor and surface | Status | Evidence |
| - | --- | --- | --- | --- |
| QA-01 | Repository Verification and Requirement 1 | Supervisor · repository gate | pass | Daemon-supervised `make verify` exited 0; success diagnostics removed |
| QA-02 | Requirements 2 and 6; outside-evidence row | Supervisor · daemon settlement | pass | Three pre-existing mode-`100755` source files; 4 focused tests passed uncached |
| QA-03 | Requirement 3 | Supervisor · daemon settlement | pass | Lost-output settlement subtest passed and asserted path/cause outcome |
| QA-04 | Requirement 4 | Supervisor · daemon settlement | pass | External-only refusal and clean-control subtests passed |
| QA-05 | Requirement 5 | Supervisor · QA report commit | pass | Both Verification-window tests and tracked/no-write controls passed |
| QA-06 | Requirement 7 | Spec author · `roundfix spec check` CLI | pass | Strict checker exited 0 with `No findings` |
| QA-07 | Pull Request row | Maintainer · Pull Request controls | blocked (environment: no open Pull Request) | Prompt fact plus complete negative/current-head equivalent-evidence audit |

### QA-01

Entry point: the repository's declared `make verify` gate. Expected observable:
exit 0 with no retained success diagnostics. Independent confirmation: the
Daemon reported the exact command and exit status before the Agent session.
Persistence: bound to audited build `5966b69e`. Evidence path: this report's
Static gate section. This row deliberately declares no carry-forward inputs.

Execution record: the Daemon supplied `command: make verify`, `verdict: pass
(exit 0)`, and `diagnostics: removed on success`. The Agent did not rerun it.

### QA-02

Entry point: the daemon package's real Git-backed fixture runner. Steps: confirm
the repository independently tracks `.githooks/commit-msg`,
`.githooks/pre-commit`, and
`.agents/skills/systematic-debugging/find-polluter.sh` as mode `100755`; run the
focused filter fixture for tracked executable, untracked executable permission
variants, and executable replacement of a tracked directory. Expected:
tracked executable edits remain stageable; every untracked executable is
refused as `executable file` with its exact mode. Independent confirmation:
the tracked-file list comes from the current Git index, not this Spec.
Persistence: rerun with `-count=1`. Evidence path: this row's execution record.

Execution record: `git ls-files -s` reported mode `100755` for all three named
source files. Git history traces the hooks to initial scaffold commit `914da344`
on 2026-06-09 and the debugging script to `317645c7` on 2026-08-07. The files
therefore predate this Spec and originate in the repository,
which makes them outside acceptance evidence rather than a Spec-authored
fixture. The following fresh command exited 0:

`GOCACHE=/private/tmp/roundfix-qa-0141-filter go test -count=1 -v -run '^(TestFilterStageablePathsKeepsTrackedExecutable|TestFilterStageablePathsDropsRegularFileWithAnyExecutePermission|TestFilterStageablePathsRefusesUntrackedExecutableWithMode|TestFilterStageablePathsRefusesExecutableReplacingATrackedDirectory)$' ./internal/daemon`

It passed the tracked executable case, the tracked-directory replacement case,
and both untracked-executable tables for owner, group, and other execute bits.

```yaml
inputs:
  - kind: repository_path
    ref: .githooks/commit-msg
  - kind: repository_path
    ref: .githooks/pre-commit
  - kind: repository_path
    ref: .agents/skills/systematic-debugging/find-polluter.sh
  - kind: repository_path
    ref: internal/daemon/task_engine.go
  - kind: repository_path
    ref: internal/daemon/task_engine_test.go
```

### QA-03

Entry point: the daemon task-cycle fixture. Steps: execute the lost-output case
through Task settlement and read the durable Task and Run outcome assertions.
Expected: the Task does not settle `completed`, and every lost path and cause
appears in the outcome while the existing console line and Run Event remain.
Independent confirmation: rerun the focused test uncached. Persistence: fixture
reopens the stored outcome. Evidence path: this row's execution record.

Execution record: `GOCACHE=/private/tmp/roundfix-qa-0141-settlement go test
-count=1 -v -run '^TestTaskCycleLostOutputRefusesCompletion$'
./internal/daemon` exited 0. Its
`lost_output_fails_with_path_and_reason` subtest passed; the fixture observes
the post-commit Task settlement and stored outcome rather than only reading a
file.

```yaml
inputs:
  - kind: repository_path
    ref: internal/daemon/task_engine.go
  - kind: repository_path
    ref: internal/daemon/task_engine_test.go
```

### QA-04

Entry point: the daemon task-cycle fixture. Steps: execute the external-only
refusal case and the clean control. Expected: the external-only Task settles
completed and the clean Task retains its prior behavior. Independent
confirmation: assert the public settlement result after the commit attempt.
Persistence: rerun the focused test uncached. Evidence path: this row's
execution record.

Execution record: the same fresh task-cycle command exited 0 and passed
`external_refusal_remains_completed` and `clean_Task_settles_as_before`, which
independently confirm the refusal-by-design and no-refusal outcomes.

```yaml
inputs:
  - kind: repository_path
    ref: internal/daemon/task_engine.go
  - kind: repository_path
    ref: internal/daemon/task_engine_test.go
```

### QA-05

Entry point: the daemon QA-step fixture. Steps: run a Verification that writes
a tracked file, a Verification that creates a directory beside a later Agent
write, and a no-write control. Expected: the commit excludes only Verification
writes and carries the seeded report, evidence, Agent writes, and QA Task file.
Independent confirmation: inspect the fixture's resulting Git commit path set
and uncommitted Verification path. Persistence: rerun the focused tests
uncached. Evidence path: this row's execution record.

Execution record: `GOCACHE=/private/tmp/roundfix-qa-0141-commit go test
-count=1 -v -run '^(TestQAReportCommitExcludesVerificationWrites|TestQAReportCommitKeepsAgentEvidenceInANewDirectory)$'
./internal/daemon` exited 0. It passed the tracked-write and no-write controls
and the shared-new-directory case, whose resulting Git path set retains the
Agent evidence and excludes only the Verification write.

```yaml
inputs:
  - kind: repository_path
    ref: internal/daemon/daemon.go
  - kind: repository_path
    ref: internal/daemon/task_engine.go
  - kind: repository_path
    ref: internal/daemon/task_engine_test.go
```

### QA-06

Entry point: `roundfix spec check <slug> --strict`. Steps: run the CLI against
the current Spec, then confirm the numbered Success Metrics and API Contracts
are named by Task References and every Success Metric appears in the TechSpec
Coverage Map. Expected: no promise-rule finding. Independent confirmation: the
CLI parses the artifacts rather than trusting their Result prose. Persistence:
rerun against the audited head. Evidence path: this row's execution record.

Execution record: `roundfix spec check
0141-a-commit-that-carries-the-work-and-nothing-else --strict` exited 0 with
`No findings`. The checker reported four explicit skips for absent optional
artifacts/detectors; none is a promise-rule detector. The PRD declares Success
Metrics 1-3, the TechSpec Coverage Map names all three, the TechSpec declares
API Contracts 1-3, and Task References name each numbered promise.

```yaml
inputs:
  - kind: repository_path
    ref: docs/specs/0141-a-commit-that-carries-the-work-and-nothing-else/_prd.md
  - kind: repository_path
    ref: docs/specs/0141-a-commit-that-carries-the-work-and-nothing-else/_techspec.md
  - kind: repository_path
    ref: docs/specs/0141-a-commit-that-carries-the-work-and-nothing-else/_tasks.md
  - kind: repository_path
    ref: docs/specs/0141-a-commit-that-carries-the-work-and-nothing-else/task_*.md
```

### QA-07

Entry point: the Pull Request controls for target branch
`feat/0141-commit-carries-the-work`. Expected: approval, checks/status,
unresolved-thread state, Merge-Ready acceptance, and review-artifact ancestry
for the claimed head. The prompt establishes that no Pull Request is open, so
the live journey is environment-blocked. Independent confirmation: inspect the
current target and Run heads plus repository-local review evidence without
querying the Run Worktree branch as a PR branch. Persistence: live-service
state is never carried forward. Evidence path: this row's execution record.

Execution record and equivalent evidence:

- Environment cause: the QA prompt states that no Pull Request is open and
  Pull Request journeys are environment-blocked. The Run Worktree branch is
  intentionally never pushed and was not queried as a PR branch.
- Approval: no acceptance has been obtained for current head `5966b69e`.
- Checks and status: the Daemon-supervised repository Verification, `make
  verify`, exited 0 for this gate.
- Unresolved review threads: no Pull Request exists on which a thread could
  stand, and the Spec has no tracked `reviews/` artifact.
- Merge-Ready acceptance: none exists yet for current head `5966b69e`.
- Review-artifact ancestry: there is no artifact to bind. Git shows target head
  `2e6041f5`, audited head `5966b69e`, and `5966b69e` has `2e6041f5` as its
  direct parent. The target-to-audited delta is exactly `task_06.md`,
  `internal/daemon/daemon.go`, `internal/daemon/task_engine.go`, and
  `internal/daemon/task_engine_test.go`.

Unblocking action: integrate the audited commit onto the target branch and open
its Pull Request, then observe approval, checks, threads, Merge-Ready evidence,
and artifact ancestry on that actual PR. ADR-0080 permits the present row to
remain environment-blocked without capping the verdict because every control's
equivalent observed or supervised state is recorded above.

```yaml
inputs:
  - kind: live_service
    ref: GitHub Pull Request for feat/0141-commit-carries-the-work
  - kind: repository_path
    ref: docs/specs/0141-a-commit-that-carries-the-work-and-nothing-else/reviews/**
```

## Mechanical skips

| Detector | Missing artifact |
| --- | --- |
| authorization bounded paths | tooling authorization |
| consequent-fix commit order | consequent-fix declaration |

## Findings

None.

## Blocked and skipped

QA-07 is blocked by the declared environment fact that no Pull Request is open.
Its cause, equivalent evidence for every PR control, and exact unblocking action
are recorded in the row. No row is finding-blocked, declared-blocked, or
skipped. The mechanical skips are retained above and do not omit a coverage
source.

## Coverage

Seven of seven planned rows reached terminal status: six passed and one is
environment-blocked with equivalent evidence. Coverage sources are Requirements
1-7, the outside-evidence row, the Pull Request row, and repository
Verification. QA-02 covers both Requirement 6 and the outside-evidence source;
QA-01 covers both Requirement 1 and repository Verification. There is no
declared frontend sweep or Unreachable Acceptance source.

Counts: `pass: 6`; `blocked (environment): 1`; `fail: 0`; `blocked (finding):
0`; `blocked (declared): 0`; `skipped: 0`; findings by impact tier: none.

## Final verdict

Pass. Every runnable criterion passed on build `5966b69e`, repository
Verification passed under Daemon supervision, and the one environment-blocked
Pull Request row records its cause and equivalent evidence for every required
control. No finding-blocked, declared-blocked, failed, pending, or skipped row
remains.
