---
spec: 0163-baseline-decisions-and-regeneration
date: 2026-09-25
build: deb778610a0d3af05c030629d716f3bde1d389c2
auditing_binary: "0.16.0"
auditor_staleness: "current: declared tree version: auditing binary 0.16.0 does not predate 0.16.0"
status: closed
verdict: pass
rows_blocked_environment: 1
rows_blocked_finding: 0
rows_blocked_declared: 0
surfaces: [backend, cli, docs]
---

# QA Report

## Performed repairs

None.

## Assigned repair failures

None.

## Authorization audit inputs

None.

## Mechanical findings

None.

## Results

| # | Status | Provenance |
| - | --- | --- |
| R1 | pass | Requirement 1; repository Verification |
| R2 | pass | Requirement 2; outside-evidence row |
| R3 | pass | Requirement 3 |
| R4 | pass | Requirement 4 |
| R5 | pass | Requirement 5 |
| R6 | pass | Requirement 6 |
| R7 | pass | Requirement 7 |
| R8 | pass | Requirement 4; Requirement 6 |
| R9 | blocked (environment: no open Pull Request) | Pull Request row |

## Mechanical skips

| Detector | Missing artifact |
| --- | --- |
| authorization bounded paths | Task commits |
| consequent-fix commit order | consequent-fix declaration |
| QA Report structure | QA Report |
| QA evidence paths | QA Report |

## Scope and environment

Full terminal QA for build `deb778610a0d3af05c030629d716f3bde1d389c2`
on the Run Worktree branch. Actor roles are the repository maintainer and the
Roundfix operator. The public surfaces are the built CLI, the documented
command path, and the repository's authored Spec checks. No Pull Request is
open for the target branch, so R9 uses the required equivalent-evidence path.

Precondition: `roundfix spec check 0163-baseline-decisions-and-regeneration --strict`
exited 0 with no findings. It did not execute authored Verification commands.

## Static gate

The Daemon ran `make verify-changed` outside the Agent sandbox. It reported
exit 0 and removed diagnostics on success. R1 records that supplied gate fact;
this Agent will not rerun repository Verification.

## Planned evidence

### R1

```yaml
inputs:
  - kind: repository_path
    ref: .
```

- Actor and surface: Daemon; repository Verification.
- Steps: record the supplied `make verify-changed` command, exit code, and
  diagnostic disposition without rerunning it.
- Expected observable: exit 0 with no retained diagnostics.
- Independent confirmation: the prompt identifies the command and verdict as
  Daemon-owned evidence for this exact QA turn.
- Persistence check: not applicable to a static gate fact.
- Evidence path: this report's Static gate section.
- Observed: the Daemon-supplied gate fact states `make verify-changed` exited 0
  and removed diagnostics on success. The Agent did not rerun the command.

### R2

```yaml
inputs:
  - kind: repository_path
    ref: docs/history/findings/2026-08-07-changing-the-http-contract-discards-its-exceptions.md
  - kind: repository_path
    ref: internal/cli/baseline_human.go
  - kind: repository_path
    ref: internal/cli/baseline_human_test.go
```

- Actor and surface: maintainer; Baseline HTTP decision prompt and explicit
  decision input.
- Steps: replay the archived Finding's multi-exception decision through the
  mode-only change and the explicit replacement path.
- Expected observable: the mode-only change retains all four exceptions and
  `source`; explicit input remains an exact whole-decision replacement.
- Independent confirmation: compare the retained fields and review text, then
  run the explicit-value replacement case.
- Persistence check: rerun from a fresh test process.
- Evidence path: focused command output summarized under R2 after execution.
- Observed: the outside source is
  `docs/history/findings/2026-08-07-changing-the-http-contract-discards-its-exceptions.md`,
  which records `/api/auth/*`, `/health`, `/openapi.json`, `/reference`, and a
  source path/digest. Three fresh `internal/cli` cases passed: field-by-field
  retention without mutating the stored value, review text naming all four
  scopes, and exact explicit-value replacement.

### R3

```yaml
inputs:
  - kind: repository_path
    ref: internal/baseline/preservation.go
  - kind: repository_path
    ref: internal/baseline/plan_test.go
  - kind: repository_path
    ref: internal/cli/baseline_human_test.go
```

- Actor and surface: maintainer; human and JSON Baseline planning.
- Steps: plan Greenfield with and without retained stale managed source.
- Expected observable: stale source refuses before classification and names
  `preservation.mode=preservation` in both surfaces; clean Greenfield plans.
- Independent confirmation: observe both JSON planning and the human prompt
  boundary, including absence of classification calls.
- Persistence check: rerun each fixture from a fresh process.
- Evidence path: focused command output summarized under R3 after execution.
- Observed: four fresh cases passed across `internal/baseline` and
  `internal/cli`. They proved the blocked state, exact finding and Preservation
  action in JSON planning, zero classification prompt/analyzer call in the
  human path, retained source, absent decision skeleton, and the unchanged
  clean-Greenfield path.

### R4

```yaml
inputs:
  - kind: repository_path
    ref: skills/_ownership.yml
  - kind: repository_path
    ref: internal/baseline/derived_ownership.go
  - kind: repository_path
    ref: internal/baseline/derived_ownership_test.go
  - kind: repository_path
    ref: internal/speccheck/mechanical_test.go
```

- Actor and surface: maintainer; derived-output resolver and Spec audit.
- Steps: resolve `make skills-sync`, compare its complete output set and
  exclusions, confirm `make baseline-digests` remains unchanged, and exercise
  grants with and without the regeneration command.
- Expected observable: exactly owned mirror files are returned; the unchanged
  command remains stable; the audit accepts only the authorized case.
- Independent confirmation: compare both resolver output and mechanical-audit
  behavior.
- Persistence check: rerun fixtures from fresh processes.
- Evidence path: focused command output summarized under R4 after execution.
- Observed: five fresh cases passed across `internal/baseline` and
  `internal/speccheck`. The complete owned-mirror set and exclusions matched,
  `make baseline-digests` retained its prior output set, and the mechanical
  audit accepted only the grant naming `make skills-sync`.

### R5

```yaml
inputs:
  - kind: repository_path
    ref: internal/baseline/skills_reconcile.go
  - kind: repository_path
    ref: internal/cli/baseline_skills_reconcile.go
  - kind: repository_path
    ref: internal/baseline/skills_reconcile_test.go
  - kind: repository_path
    ref: internal/cli/baseline_skills_reconcile_test.go
  - kind: repository_path
    ref: internal/cli/doctor_test.go
```

- Actor and surface: Roundfix operator; `roundfix baseline skills reconcile`
  and Doctor.
- Steps: use local Git sources to preview and confirm an obsolete-entry
  removal, then exercise required-removed, unreachable-source, mutable-ref,
  stale-preimage, and Doctor read-only paths.
- Expected observable: only proven-obsolete entries are removed after the
  digest is confirmed; every named negative case writes nothing.
- Independent confirmation: reopen the lock after apply and compare bytes on
  each refusal path.
- Persistence check: rerun reconciliation after apply and observe no changes.
- Evidence path: CLI and focused command output summarized under R5.
- Observed: `go test -count=1 -v` selected seven reconciliation-engine cases
  and four public CLI/Doctor cases in `./internal/baseline ./internal/cli`; all
  eleven passed. The public preview/confirm case reopened the lock, proved the
  obsolete entry absent, and reran to a no-change result. The negative cases
  proved byte-preserving refusal for a required removal, unreachable source,
  mutable revision and stale preimage; Doctor also left the lock unchanged.
- External-process confirmation: the built
  `/private/tmp/roundfix-qa0163-bin` command ran against disposable repository
  `/private/tmp/roundfix-qa0163-manual/repo` and local source commit
  `deb778610a0d3af05c030629d716f3bde1d389c2`. Preview exited 3, classified
  `obsolete-qa0163` as `obsolete`, reported the installed tree `retained`, and
  returned Plan Digest
  `ab87d79aad4ca3078918f98848eb3e2403f3603449fb49fb13c51a413f423a10`.
  Confirmation with that exact digest exited 0 and removed only the lock entry.
  A fresh rerun exited 0 with `applied: false`, an empty skill list, and no
  planned changes; reopening `skills-lock.json` showed an empty skills object.

### R6

```yaml
inputs:
  - kind: repository_path
    ref: .agents/skills/roundfix/SKILL.md
  - kind: repository_path
    ref: skills/roundfix/SKILL.md
  - kind: repository_path
    ref: .agents/skills/setup-context-driven/SKILL.md
  - kind: repository_path
    ref: skills/setup-context-driven/SKILL.md
```

- Actor and surface: skill reader; shipped Roundfix guidance.
- Steps: locate each required behavior in canonical and shipped copies, compare
  mirrors byte-for-byte, and run the skill checker.
- Expected observable: both skills document the changes, mirrors match, and
  the skill check exits 0.
- Independent confirmation: byte comparison plus the public skill checker.
- Persistence check: repeat comparison from disk after the checker.
- Evidence path: command output summarized under R6.
- Observed: targeted search found the reconcile command and positive-absence
  boundary in both Roundfix copies, plus the exact HTTP-retention sentence and
  Greenfield Preservation route in both setup copies. Both recursive
  comparisons exited 0 with no diff. The built-tree
  `go run -buildvcs=false ./cmd/roundfix skills check` exited 0 and reported the
  Roundfix Repository Skill Set passed.

### R7

```yaml
inputs:
  - kind: repository_path
    ref: docs/specs/0163-baseline-decisions-and-regeneration/_prd.md
  - kind: repository_path
    ref: docs/specs/0163-baseline-decisions-and-regeneration/_techspec.md
  - kind: repository_path
    ref: docs/specs/0163-baseline-decisions-and-regeneration/_tasks.md
  - kind: repository_path
    ref: docs/specs/0163-baseline-decisions-and-regeneration/task_*.md
```

- Actor and surface: Spec author; Spec Consistency Check.
- Steps: run the strict check and inspect its named coverage diagnostics.
- Expected observable: no promise is left without a consuming Task and no
  cross-reference is contradicted.
- Independent confirmation: compare the PRD promises with the terminal QA
  requirement coverage recorded in `task_07.md`.
- Persistence check: rerun the read-only strict check against the same build.
- Evidence path: strict-check output summarized under R7.
- Observed: the strict Spec Consistency Check command exited 0 with `No
  findings` and stated that authored Verification commands were not executed.
  Its skipped detector list named only missing optional artifacts: a findings
  rollup, backlog, Vocabulary Contract, and references index. No promise-rule
  or coverage detector was skipped.

### R8

```yaml
inputs:
  - kind: repository_path
    ref: docs/specs/0163-baseline-decisions-and-regeneration/_authorization.md
  - kind: repository_path
    ref: .git
```

- Actor and surface: maintainer; committed tooling authorization and history.
- Steps: enumerate every Task commit with `git diff-tree`, compare governed
  paths with the exact grant, and verify authorization and sanctioned
  regeneration chronology.
- Expected observable: each governed path is bounded before its Task commit;
  authorial changes and generated mirrors are separate and traceable; no
  consequent fix is declared or folded into a tooling commit.
- Independent confirmation: inspect both commit ancestry and actual path sets.
- Persistence check: repeat the path audit from the current head.
- Evidence path: Git evidence summarized under R8.
- Observed: `git diff-tree --no-commit-id --name-only -r` enumerated every
  authoring and Task commit. The initial grant commit `856a8d10` and bounded
  amendment `bbeccf3f` precede Tasks 01-06; `bbeccf3f` specifically precedes
  the Task 03 use of `internal/speccheck/mechanical_test.go`. Corrective grant
  commit `27c3a207` precedes Task 08 commit `deb77861`, which changes only its
  Task file and the bounded
  `internal/cli/baseline_documentation_contract_test.go`. Task 06 changes only
  its Task file plus the four bounded canonical/mirror skill paths. All three
  ancestry checks exited 0. Every governed path is in the exact grant, the
  authorization commits are separate from consuming changes, and no
  consequent-fix declaration exists. Git emitted a non-fatal fsmonitor IPC
  warning while returning complete path sets and exit 0.

### R9

```yaml
inputs:
  - kind: live_service
    ref: Pull Request for feat/0163-baseline-decisions-and-regeneration
  - kind: repository_path
    ref: .git
```

- Actor and surface: reviewer; Pull Request controls.
- Steps: record the supplied no-open-Pull-Request fact and equivalent evidence
  for approval, checks and status, unresolved threads, Merge-Ready acceptance,
  and review-artifact ancestry.
- Expected observable: the environment cause and every control are answered.
- Independent confirmation: current target head and local review-artifact
  inventory.
- Persistence check: not applicable while the Pull Request surface is absent.
- Evidence path: Blocked and skipped section under R9.

## Findings

None.

## Blocked and skipped

### R9 — no open Pull Request

The Roundfix QA assignment states that no Pull Request is open for
`feat/0163-baseline-decisions-and-regeneration`; Pull Request journeys are
therefore environment-blocked. Equivalent observed or supervised evidence
answers every control:

- Approval: no Pull Request acceptance was obtained.
- Checks and status: the Daemon ran `make verify-changed` for this tree and
  reported exit 0; the strict Spec check also exited 0 with no findings.
- Unresolved review threads: no Pull Request exists, so there is no review
  thread surface and no standing thread.
- Merge-Ready acceptance: no Review Source acceptance exists yet.
- Review-artifact ancestry: the Spec directory contains no Review Artifact.
  Both `HEAD` and the target branch resolve to
  `deb778610a0d3af05c030629d716f3bde1d389c2`; no evidence is claimed for a
  different head.

Unblocking action: open the Pull Request from the target branch after this QA
Task settles, then exercise the live controls during the configured pre-PR and
required-check workflow.

## Coverage

Executed: 9 rows — 8 pass and 1 environment-blocked with equivalent evidence.
Declared verification Requirements 1-7 and all non-waivable sources are
mapped. No frontend surface or Unreachable Acceptance declaration exists for
this Spec. No row is finding-blocked, declared-blocked, failed, skipped, or
pending.

## Final verdict

Pass. All eight runnable rows passed; the one environment-blocked Pull Request
row records its cause and equivalent evidence for every control. There are no
failed, finding-blocked, declared-blocked, skipped, or pending rows, and no
findings in any user-impact tier.
