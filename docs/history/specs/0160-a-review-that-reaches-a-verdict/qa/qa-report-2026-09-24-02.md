---
spec: 0160-a-review-that-reaches-a-verdict
date: 2026-09-24
build: 5bb38d7a92618d97be5cb6c14c6966015afa78c4
auditing_binary: "0.16.0 (5bb38d7a, built 2026-09-24 19:30:00 -0300)"
auditor_staleness: "current: commit ancestry: build commit is the audited tree"
status: closed
verdict: pass
rows_blocked_environment: 1
rows_blocked_finding: 0
rows_blocked_declared: 0
surfaces: [backend, cli, docs]
---

# QA Report

## Scope and environment

Full terminal QA gate for commit
`5bb38d7a92618d97be5cb6c14c6966015afa78c4` in the Run Worktree. Actors are an
operator invoking `roundfix review`, a Spec author checking promise coverage,
and a maintainer auditing the candidate. No Pull Request is open, so the Pull
Request row uses the required environment-blocked equivalent-evidence path. No
real reviewer session, paid API call, commit, push, or Pull Request mutation is
permitted.

The installed `roundfix 0.15.0` binary is stale against the tree's declared
`0.16.0` version. The gate therefore built `/private/tmp/roundfix-0160-qa` from
the audited tree with commit stamp `5bb38d7a`; its version output is
`roundfix 0.16.0 (5bb38d7a, built 2026-09-24 19:30:00 -0300)`.

The strict authoring precondition passed before the matrix was built:

```text
roundfix spec check 0160-a-review-that-reaches-a-verdict --strict
Spec 0160-a-review-that-reaches-a-verdict
No findings. Authored Verification commands were not executed.
```

The checker skipped detectors whose project-family inputs are absent: a
Findings Rollup, active Backlog Entry, TechSpec Vocabulary Contract, and
`references/_index.md`. None replaces a declared QA row.

## Static gate

The Daemon ran `make verify` outside the Agent sandbox for this Run Worktree and
reported exit 0. Diagnostics were removed on success. Per the QA contract, the
Agent records that supervised result and does not rerun repository Verification.

## Performed repairs

None.

## Assigned repair failures

None.

## Authorization audit inputs

| Task | Outcome | Record | Revision | Detail |
| --- | --- | --- | --- | --- |
| task_10 | granted | docs/specs/0160-a-review-that-reaches-a-verdict/_authorization.md | 8b53fd3f0767090ec8a0d5c89811a6c710b5bd55 | operative grant |

## Mechanical findings

None.

## Results

| # | Status | Provenance |
| - | --- | --- |
| Q-01 | pass | Requirement 1; repository Verification |
| Q-02 | pass | Requirement 2 |
| Q-03 | pass | Requirement 3 |
| Q-04 | pass | Requirement 4 |
| Q-05 | pass | Requirement 5 |
| Q-06 | pass | Requirement 6 |
| Q-07 | pass | outside-evidence row |
| Q-08 | blocked (environment: no open Pull Request) | Pull Request row |

## Detailed evidence

### Q-01

```yaml
inputs:
  - kind: repository_path
    ref: "**"
```

- Actor, entry point, surface: Daemon running the repository gate; backend,
  CLI, and docs.
- Steps: consume the Daemon-supervised `make verify` result supplied for this
  audited head; do not rerun it.
- Expected observable: command exits 0 with no retained success diagnostics.
- Independent confirmation: bind the supplied command, verdict, and diagnostic
  disposition to the audited commit.
- Persistence check: record the full audited head in report frontmatter.
- Evidence path: this report's Static gate section.
- Result: pass. The supplied Daemon fact is `make verify`, exit 0, with
  diagnostics removed on success. It is bound here to audited head
  `5bb38d7a92618d97be5cb6c14c6966015afa78c4` and was not re-derived.

### Q-02

```yaml
inputs:
  - kind: repository_path
    ref: internal/cli/review.go
  - kind: repository_path
    ref: internal/cli/review_test.go
```

- Actor, entry point, surface: operator using `roundfix review`; CLI and
  backend command runner.
- Steps: exercise presentation variants, emphasized findings headers, ambiguous
  and whole-answer cases, findings-none forms, and answer-file lifecycle.
- Expected observable: only one accounted-for verdict classifies; ambiguous or
  extra-content answers block; every reached outcome retains exact evidence;
  an unreached outcome invents no answer and removes stale evidence.
- Independent confirmation: exit code, decoded record, exact bytes, absent-file
  checks, and runner activity agree in fresh fixture repositories.
- Persistence check: every case creates a fresh repository and artifact root.
- Evidence path: focused command output and the named repository inputs.
- Result: pass. After the matrix existed, the following current-build command
  exited 0 and every named case passed:

  ```text
  GOCACHE=/private/tmp/roundfix-0160-qa-tests-cache-final go test -count=1 -v -run '^(TestReviewClassifiesVerdictVariants|TestReviewRecognisesEmphasizedFindingsHeader|TestReviewBlocksEmphasizedFindingsBesideNoFindings|TestReviewBlocksAmbiguousVerdict|TestReviewBlocksContentBesideANoFindingsVerdict|TestReviewReadsFindingsNoneAsNoFindings|TestReviewIgnoresAQuotedVerdictInsideFindings|TestReviewKeepsTheRawAnswer|TestReviewKeepsNoAnswerWhenTheReviewerWasNotReached|TestReviewRemovesAStaleAnswerFile|TestReviewRecordsEmptySkippedSpecsAsAList|TestReviewRunsTheClaudeProvider|TestReviewRefusesCodeRabbitNamingTheMissingSurface|TestReviewCommandRefusesProviderProfileMismatch|TestReviewPromptCarriesSpecDecisions|TestReviewPromptWithoutSpecIsUnchanged|TestReviewSkipsASpecWithoutDecisions|TestReviewReadsAnArchivedSpecFromTheCandidate|TestReviewBoundsSpecContext|TestReviewRecordsDroppedSpecs|TestReviewReportsSpecReadingFailureDistinctly)$' ./internal/cli
  ```

  The observations include all five presentation variants, emphasized findings
  headers, both and neither verdicts, three extra-content false-pass
  regressions, all terminal findings-none forms, quoted verdict text in a
  findings body, exact raw-answer bytes for reviewed/findings/blocked outcomes,
  no invented pre-prompt answer, stale-answer removal, and `skippedSpecs: []`.

### Q-03

```yaml
inputs:
  - kind: repository_path
    ref: internal/cli/review.go
  - kind: repository_path
    ref: internal/cli/review_test.go
```

- Actor, entry point, surface: operator selecting a pre-PR review provider;
  CLI and backend command runner.
- Steps: exercise the `claude`, mismatched-profile, and `coderabbit` paths.
- Expected observable: `claude` records provider `claude` through read-only
  access, mismatched profiles block, and `coderabbit` names the missing local
  review surface without Agent activity.
- Independent confirmation: exit code, record, answer artifact, access mode,
  and activity counters agree.
- Persistence check: every case creates a fresh repository and artifact root.
- Evidence path: focused command output and the named repository inputs.
- Result: pass. The same post-matrix command passed
  `TestReviewRunsTheClaudeProvider`,
  `TestReviewRefusesCodeRabbitNamingTheMissingSurface`, and
  `TestReviewCommandRefusesProviderProfileMismatch`. Claude persisted provider
  `claude` through read-only access; CodeRabbit stopped with the missing local
  review surface named and no Agent activity; preferred, fallback, and Claude
  runtime mismatches remained blocked.

### Q-04

```yaml
inputs:
  - kind: repository_path
    ref: internal/cli/review.go
  - kind: repository_path
    ref: internal/cli/review_test.go
```

- Actor, entry point, surface: operator reviewing a candidate; CLI and backend
  prompt boundary.
- Steps: exercise active, absent, unusable, archived, oversized, dropped, and
  unreadable Spec context paths.
- Expected observable: usable candidate Decisions and TechSpec context is
  labelled and recorded; a no-Spec prompt is unchanged; unusable context is
  skipped; archived context comes from `HEAD`; bounds and read failures remain
  visible.
- Independent confirmation: captured prompts and decoded records agree with
  independent candidate Git state and runner activity.
- Persistence check: every case creates a fresh candidate repository.
- Evidence path: focused command output and the named repository inputs.
- Result: pass. The same post-matrix command passed the active-Spec and no-Spec
  prompt cases plus unusable-Spec skipping, archived-Spec `HEAD` reads, per-Spec
  and total context bounds, dropped-Spec naming, and distinct local read
  failures. Captured prompts, decoded records, candidate commits, and runner
  activity agreed in fresh fixture repositories.

### Q-05

```yaml
inputs:
  - kind: repository_path
    ref: .agents/skills/roundfix/SKILL.md
  - kind: repository_path
    ref: skills/roundfix/SKILL.md
  - kind: repository_path
    ref: docs/user-guide/commands.md
  - kind: repository_path
    ref: docs/specs/0160-a-review-that-reaches-a-verdict/_authorization.md
  - kind: repository_path
    ref: docs/specs/0160-a-review-that-reaches-a-verdict/task_10.md
  - kind: repository_path
    ref: .git
```

- Actor, entry point, surface: operator and maintainer using public help,
  shipped skill, command guide, and Git authorization history; CLI and docs.
- Steps: validate and compare the skill copies, inspect public help, assert the
  complete contract across all three documentation surfaces, and audit Task 10
  changed paths plus authorization ancestry from Git evidence.
- Expected observable: validation passes, mirrors match, docs describe the
  delivered behavior, and governed edits stay within a prior bounded grant.
- Independent confirmation: executable validation, public help, byte
  comparison, content assertions, `git diff-tree`, and ancestry checks agree.
- Persistence check: rerun against the audited repository and exact commits.
- Evidence path: focused command and Git output plus the named inputs.
- Result: pass. After the matrix existed, the commit-stamped current-tree
  binary's `skills check` passed; `cmp -s
  .agents/skills/roundfix/SKILL.md skills/roundfix/SKILL.md` exited 0; and
  `roundfix review --help` exposed `review [--base <ref>]`. Focused `rg -q`
  checks found the Claude path, whole-answer rule, `Findings: n/a`,
  `answerPath`, archived discovery, `skippedSpecs`, and
  `specContextTruncated` on the required surfaces. A negative search for
  `refuses to execute them for now` exited 1 because the stale wording is
  absent.

  `git diff-tree --no-commit-id --name-status -r 5bb38d7a...` showed only the
  canonical and generated skills, the ordinary command guide, and the owning
  Task file. Authorization commit `a971f81b...` is a separate ancestor of the
  Task commit, approves this Spec, grants `implement` and `commit`, bounds both
  skill paths, and sanctions `make skills-sync`. The skill copies are
  byte-identical, so the derived mirror is reproducible and no out-of-scope
  governed path was observed.

### Q-06

```yaml
inputs:
  - kind: repository_path
    ref: docs/specs/0160-a-review-that-reaches-a-verdict/**
  - kind: repository_path
    ref: internal/speccheck/**
```

- Actor, entry point, surface: Spec author running the strict checker; CLI and
  docs.
- Steps: rerun the strict Spec check with the current-tree binary after this
  matrix exists.
- Expected observable: no promise-rule finding and no authored Verification
  command execution.
- Independent confirmation: the checker parses promises and consuming Tasks
  from the current Spec artifacts.
- Persistence check: rerun against the audited head plus this report.
- Evidence path: focused command output and current Spec artifacts.
- Result: pass. After the matrix existed, the current-tree binary ran
  `roundfix spec check 0160-a-review-that-reaches-a-verdict --strict`, exited 0,
  and reported `No findings. Authored Verification commands were not
  executed.` The four skipped detectors concern absent project-family inputs
  and do not replace or hide the promise-rule source declared by Requirement 6.

### Q-07

```yaml
inputs:
  - kind: external_repository
    ref: /private/tmp/claude-501/-Users-marcio-dev-roundfix/0f9d9d2d-9b0a-4336-8d50-a94fb7322388/scratchpad/wt-0160
  - kind: repository_path
    ref: internal/cli/review.go
  - kind: repository_path
    ref: internal/cli/review_test.go
```

- Actor, entry point, surface: operator relying on independently retained
  review history; CLI and repository history.
- Steps: inspect the supplied user checkout's archived Spec 0153 and QA result,
  prove its delivery predates Spec 0160, and execute the matching current public
  clean, findings, blocked, and configured-omission paths.
- Expected observable: outside evidence establishes the earlier public review
  contract, and current behavior preserves each outcome.
- Independent confirmation: external repository evidence and fresh current
  command-runner observations agree.
- Persistence check: external repository inputs make this row non-carriable.
- Evidence path: exact external paths, Git evidence, and focused command output.
- Result: pass. The outside source is the supplied user checkout's archived
  `docs/history/specs/0153-a-reviewer-the-workflow-runs`. Its newest QA report
  is closed with verdict `pass` and binds build
  `eb02c4f0ac3a675548d13560218b93acd48e9695`. Git ancestry proved delivery
  commit `7befab2ce1bf301359b3b6f10132c9c13cf1964f`, dated 2026-09-22,
  predates Spec 0160 authoring commit
  `a971f81b9e0cacc4154c2565a7e2c1a663fbf894`, dated 2026-09-24.

  After the matrix existed, a fresh run of
  `TestReviewCommandExitsZeroOnExplicitClean`,
  `TestReviewCommandExitsOneAndRecordsFindings`,
  `TestReviewCommandClassifiesAgentMessageNotProtocolStream`, and
  `TestReviewCommandNoneRecordsOmissionWithoutAgentActivity` exited 0. Clean,
  findings, blocked/no-verdict, and explicit configured omission retained their
  earlier public outcomes on the current build.

### Q-08

```yaml
inputs:
  - kind: live_service
    ref: Pull Request for feat/0160-review-reaches-a-verdict
  - kind: repository_path
    ref: "**"
```

- Actor, entry point, surface: maintainer; Pull Request controls.
- Steps: use the supplied no-open-Pull-Request fact and inspect equivalent
  evidence for approval, checks/status, unresolved threads, Merge-Ready
  acceptance, and review-artifact ancestry.
- Expected observable: every control has explicit evidence or explicit absence,
  without inferring unavailable Pull Request state.
- Independent confirmation: the QA prompt supplies the no-PR fact; repository
  and Daemon facts supply equivalent evidence.
- Persistence check: live-service rows are not carry-forward eligible.
- Evidence path: this report's Blocked and skipped section.
- Result: blocked (environment: no open Pull Request). The supplied QA fact
  establishes the cause. Equivalent evidence for all five controls is recorded
  below; no Pull Request state is inferred.

## Findings

None.

## Blocked and skipped

Q-08 is `blocked (environment: no open Pull Request)`. The prompt confirms that
no Pull Request is open and Pull Request journeys are environment-blocked.
Equivalent evidence:

- Approval: no Pull Request acceptance was obtained.
- Checks and status: the Daemon ran `make verify` for audited head
  `5bb38d7a92618d97be5cb6c14c6966015afa78c4` and reported exit 0.
- Unresolved review threads: no Pull Request exists, so no Pull Request review
  thread set exists to leave unresolved.
- Merge-Ready acceptance: no Review Source evidence exists yet; no Merge-Ready
  claim is made.
- Review-artifact ancestry: no review artifact exists yet; no evidence from
  another head is credited.

Unblocking action: after this gate settles, open a Pull Request from
`feat/0160-review-reaches-a-verdict`, then observe all five controls read-only
against its actual head. No row is finding-blocked, declared-blocked, or
skipped.

## Coverage

Coverage is closed over Requirements 1-6, repository Verification, the
outside-evidence row, and the Pull Request row. Seven rows passed and one row is
environment-blocked with its cause, unblocking action, and equivalent evidence
for every Pull Request control. The PRD declares no Unreachable Acceptance and
no frontend surface. No row names another source. Counts:
`rows_blocked_environment: 1`, `rows_blocked_finding: 0`,
`rows_blocked_declared: 0`; failed: 0; skipped: 0.

## Mechanical skips

| Detector | Missing artifact |
| --- | --- |
| consequent-fix commit order | consequent-fix declaration |
| QA evidence paths | docs/specs/0160-a-review-that-reaches-a-verdict/qa/evidence |

## Final verdict

Pass. All seven runnable rows passed; the sole environment-blocked Pull Request
row records its cause, unblocking action, and equivalent evidence for all five
controls. There are no findings, declared blocks, finding blocks, skipped rows,
or unresolved evidence paths. Findings by impact: Blocks-Completion 0,
Data-Loss 0, Trust-Damage 0, Friction 0, Cosmetic 0.
