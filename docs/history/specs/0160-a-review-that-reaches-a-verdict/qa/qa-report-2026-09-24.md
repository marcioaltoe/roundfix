---
spec: 0160-a-review-that-reaches-a-verdict
date: 2026-09-24
build: 340e511f174128a3043ab1165b333fff9799694d
auditing_binary: "0.16.0 (e38c8561, built 2026-09-24 08:07:17 -0300)"
auditor_staleness: "current: commit ancestry: build commit does not predate audited tree"
status: closed
verdict: pass
rows_blocked_environment: 1
rows_blocked_finding: 0
rows_blocked_declared: 0
surfaces: [backend, cli, docs]
---

# QA Report

## Scope and environment

Full terminal QA gate for commit
`340e511f174128a3043ab1165b333fff9799694d` in the Run Worktree. The actors are
an operator invoking `roundfix review`, a Spec author checking promise coverage,
and a maintainer auditing the candidate. No Pull Request is open, so the Pull
Request row uses the required environment-blocked equivalent-evidence path. No
real reviewer session, paid API call, commit, push, or Pull Request mutation is
permitted.

The strict authoring precondition passed:

```text
roundfix spec check 0160-a-review-that-reaches-a-verdict --strict
Spec 0160-a-review-that-reaches-a-verdict
No findings. Authored Verification commands were not executed.
```

The checker skipped detectors whose project-family inputs are absent: a
Findings Rollup, active Backlog Entry, TechSpec Vocabulary Contract, and
`references/_index.md`. None replaces a declared QA row.

## Static gate

The Daemon ran `make verify` outside the Agent sandbox for this Run Worktree and
reported exit 0. Diagnostics were removed on success. Per the QA contract, the
Agent records that supervised result and does not rerun repository Verification.

## Performed repairs

None.

## Assigned repair failures

None.

## Authorization audit inputs

| Task | Outcome | Record | Revision | Detail |
| --- | --- | --- | --- | --- |
| task_04 | granted | docs/specs/0160-a-review-that-reaches-a-verdict/_authorization.md | 216a426b79069fa19c97066ad2768a22323fc3b6 | operative grant |

## Mechanical findings

None.

## Results

| # | Status | Provenance |
| - | --- | --- |
| Q-01 | pass | Requirement 1; repository Verification |
| Q-02 | pass | Requirement 2 |
| Q-03 | pass | Requirement 3 |
| Q-04 | pass | Requirement 4 |
| Q-05 | pass | Requirement 5 |
| Q-06 | pass | Requirement 6 |
| Q-07 | pass | outside-evidence row |
| Q-08 | blocked (environment: no open Pull Request) | Pull Request row |

## Detailed evidence

### Q-01

```yaml
inputs:
  - kind: repository_path
    ref: "**"
```

- Actor, entry point, surface: Daemon running the repository gate; backend,
  CLI, and docs.
- Steps: consume the Daemon-supervised `make verify` result supplied for this
  audited head; do not rerun it.
- Expected observable: command exits 0 with no retained success diagnostics.
- Independent confirmation: bind the supplied command, verdict, and diagnostic
  disposition to the audited commit.
- Persistence check: record the full audited head in report frontmatter.
- Evidence path: this report's Static gate section.
- Result: pass. The supplied Daemon fact is `make verify`, exit 0, with
  diagnostics removed on success. The result is recorded for audited head
  `340e511f174128a3043ab1165b333fff9799694d` and was not re-derived.

### Q-02

```yaml
inputs:
  - kind: repository_path
    ref: internal/cli/review.go
  - kind: repository_path
    ref: internal/cli/review_test.go
```

- Actor, entry point, surface: operator using `roundfix review`; CLI and
  backend command runner.
- Steps: execute the focused command-runner cases for punctuated, emphasized,
  lowercase, and preamble verdicts; both-verdict and no-verdict answers; and
  raw-answer persistence for every reached outcome.
- Expected observable: exactly one substantive verdict classifies, ambiguous
  answers block with the ambiguity named, findings text starts after the
  marker, and each reached outcome records an exact raw answer at `answerPath`.
- Independent confirmation: exit code, decoded record, persisted answer bytes,
  and runner activity agree in fresh fixture repositories.
- Persistence check: every test creates a fresh repository and artifact
  directory.
- Evidence path: focused command output and the named repository inputs.
- Result: pass. `GOCACHE=/private/tmp/roundfix-0160-qa-q02-gocache go
  test -count=1 -v -run
  '^(TestReviewClassifiesVerdictVariants|TestReviewBlocksAmbiguousVerdict|TestReviewKeepsTheRawAnswer)$'
  ./internal/cli` exited 0. All four classification variants, both ambiguity
  cases, and all three raw-answer outcomes passed. The fixtures independently
  checked exit codes, decoded records, ambiguity reasons, exact answer bytes,
  and `answerPath` values.

### Q-03

```yaml
inputs:
  - kind: repository_path
    ref: internal/cli/review.go
  - kind: repository_path
    ref: internal/cli/review_test.go
```

- Actor, entry point, surface: operator selecting a pre-PR review provider;
  CLI and backend command runner.
- Steps: execute the focused `claude` and `coderabbit` command-runner cases.
- Expected observable: `claude` produces a reviewed record naming provider
  `claude` through read-only access; `coderabbit` exits blocked and names the
  missing local review surface without Agent activity.
- Independent confirmation: exit code, persisted record, raw answer, access
  mode, and activity counters agree.
- Persistence check: each case uses a fresh repository and artifact directory.
- Evidence path: focused command output and the named repository inputs.
- Result: pass. `GOCACHE=/private/tmp/roundfix-0160-qa-q03-gocache go
  test -count=1 -v -run
  '^(TestReviewRunsTheClaudeProvider|TestReviewRefusesCodeRabbitNamingTheMissingSurface)$'
  ./internal/cli` exited 0. The Claude case persisted provider `claude`, the
  exact raw answer, and read-only access with one prepare, prompt, and session
  close. The CodeRabbit case blocked with the missing local review surface
  named and zero Agent Runtime activity.

### Q-04

```yaml
inputs:
  - kind: repository_path
    ref: internal/cli/review.go
  - kind: repository_path
    ref: internal/cli/review_test.go
```

- Actor, entry point, surface: operator reviewing a candidate; CLI and backend
  prompt boundary.
- Steps: execute the focused changed-Spec and no-Spec command-runner cases.
- Expected observable: a changed Spec contributes only its PRD Decisions and
  complete TechSpec in a labelled prompt block, its slug appears in `specs`,
  and a candidate without a changed Spec keeps the prior prompt byte-identical
  with an empty `specs` array.
- Independent confirmation: captured runner prompt and decoded record agree
  with independent candidate Git state.
- Persistence check: each case creates a fresh candidate repository.
- Evidence path: focused command output and the named repository inputs.
- Result: pass. `GOCACHE=/private/tmp/roundfix-0160-qa-q04-gocache go
  test -count=1 -v -run
  '^(TestReviewPromptCarriesSpecDecisions|TestReviewPromptWithoutSpecIsUnchanged)$'
  ./internal/cli` exited 0. The changed-Spec case captured a labelled block
  containing only the PRD Decisions section and complete TechSpec, plus the
  consulted slug. The no-Spec case compared the executed prompt byte-for-byte
  with the prior prompt builder and observed an empty `specs` array.

### Q-05

```yaml
inputs:
  - kind: repository_path
    ref: .agents/skills/roundfix/SKILL.md
  - kind: repository_path
    ref: skills/roundfix/SKILL.md
  - kind: repository_path
    ref: docs/user-guide/commands.md
  - kind: repository_path
    ref: cmd/roundfix/**
```

- Actor, entry point, surface: operator and maintainer using public help,
  shipped skill, and command guide; CLI and docs.
- Steps: run the current-tree skill validator and public review help, compare
  both skill copies byte-for-byte, and check the three documented surfaces for
  `claude`, substantive verdicts, `answerPath`, Spec-aware review, and the
  CodeRabbit refusal.
- Expected observable: validation passes, mirrors match, help exposes the
  public review command, and all documented behavior is present without the
  stale Claude-refusal wording.
- Independent confirmation: executable validation, public help, byte
  comparison, and focused content assertions are separate observations.
- Persistence check: rerun against current repository paths.
- Evidence path: focused command output and the named repository inputs.
- Result: pass. The current-tree `go run -buildvcs=false ./cmd/roundfix skills
  check` reported the Roundfix skill and related workflow skills passed. Public
  `roundfix review --help` exposed `review [--base <ref>]`; `cmp` found the
  canonical and distributed skills byte-identical; focused searches found
  Claude execution, substantive classification, `answerPath`, Spec-aware
  review, and the CodeRabbit refusal in both skill copies and the guide. The
  final negative search exited 1 because the stale phrase `refuses to execute
  them for now` was absent; an isolated reproduction confirmed that expected
  no-match was the combined command's only nonzero component.

### Q-06

```yaml
inputs:
  - kind: repository_path
    ref: docs/specs/0160-a-review-that-reaches-a-verdict/**
  - kind: repository_path
    ref: internal/speccheck/**
```

- Actor, entry point, surface: Spec author running the strict checker; CLI and
  docs.
- Steps: rerun `roundfix spec check
  0160-a-review-that-reaches-a-verdict --strict` after this matrix exists.
- Expected observable: no promise-rule finding and no authored Verification
  command execution.
- Independent confirmation: the checker parses promises and consuming Tasks
  from the current Spec artifacts.
- Persistence check: rerun against the same audited head plus this report.
- Evidence path: focused command output and current Spec artifacts.
- Result: pass. After the matrix existed, `roundfix spec check
  0160-a-review-that-reaches-a-verdict --strict` exited 0 and again reported
  `No findings. Authored Verification commands were not executed.` The four
  skipped detectors concern absent project-family inputs and did not hide the
  promise-to-consuming-Task rule declared by Requirement 6.

### Q-07

```yaml
inputs:
  - kind: repository_path
    ref: docs/history/specs/0153-a-reviewer-the-workflow-runs/**
  - kind: repository_path
    ref: internal/cli/review.go
  - kind: repository_path
    ref: internal/cli/review_test.go
  - kind: repository_path
    ref: .git
```

- Actor, entry point, surface: operator relying on the review command contract
  delivered before this Spec; CLI and repository history.
- Steps: prove archived Spec 0153 and its delivery commit predate Spec 0160,
  then execute the current command's pre-existing clean, findings, blocked, and
  configured-omission regression cases.
- Expected observable: the earlier public exit/outcome contract remains intact
  while this Spec extends classification, providers, and evidence.
- Independent confirmation: immutable Git ancestry and fresh command-runner
  outcomes agree; archived Spec 0153 is evidence this Spec did not author.
- Persistence check: bind history to exact commits and use fresh fixture
  repositories for the regression cases.
- Evidence path: archived Spec 0153, Git output, focused command output, and the
  named current inputs.
- Result: pass. The external source is archived Spec 0153 and its delivery
  commit `7befab2ce1bf301359b3b6f10132c9c13cf1964f`, dated 2026-09-22. Git
  ancestry proved that commit predates Spec 0160's authoring commit
  `a971f81b9e0cacc4154c2565a7e2c1a663fbf894`, dated 2026-09-24. A fresh
  focused run of the four pre-existing public command-runner cases exited 0:
  explicit clean remained exit 0 and `reviewed`; findings remained exit 1 and
  persisted; an Agent message without a verdict remained blocked even when the
  protocol stream contained clean text; explicit `none` remained exit 0 with
  zero Agent activity. This is executable evidence from the current build
  grounded in a contract Spec 0160 did not author.

### Q-08

```yaml
inputs:
  - kind: live_service
    ref: Pull Request for feat/0160-review-reaches-a-verdict
  - kind: repository_path
    ref: "**"
```

- Actor, entry point, surface: maintainer; Pull Request controls.
- Steps: because the supplied environment fact says no Pull Request is open,
  record the cause and inspect equivalent evidence for approval, checks/status,
  unresolved threads, Merge-Ready acceptance, and review-artifact ancestry.
- Expected observable: every control has explicit evidence or an explicit
  absence, without inferring unavailable Pull Request state.
- Independent confirmation: the QA prompt supplies the no-PR fact; repository
  and Daemon facts supply equivalent evidence.
- Persistence check: live-service rows are not carry-forward eligible.
- Evidence path: this report's Blocked and skipped section.
- Result: blocked (environment: no open Pull Request). The supplied QA fact
  establishes the cause. Equivalent evidence for all five controls is recorded
  below; no Pull Request state is inferred.

## Findings

None.

## Blocked and skipped

Q-08 is `blocked (environment: no open Pull Request)`. The prompt confirms that
no Pull Request is open and Pull Request journeys are environment-blocked.
Equivalent evidence:

- Approval: no Pull Request acceptance was obtained.
- Checks and status: the Daemon ran `make verify` for audited head
  `340e511f174128a3043ab1165b333fff9799694d` and reported exit 0.
- Unresolved review threads: no Pull Request exists, so no Pull Request review
  thread set exists to leave unresolved.
- Merge-Ready acceptance: no Review Source evidence exists yet; no Merge-Ready
  claim is made.
- Review-artifact ancestry: no review artifact exists yet; no evidence from
  another head is credited.

Unblocking action: after this gate settles, open a Pull Request from
`feat/0160-review-reaches-a-verdict`, then observe all five controls read-only
against its actual head. No row is finding-blocked, declared-blocked, or
skipped.

## Coverage

Coverage is closed over Requirements 1-6, repository Verification, the
outside-evidence row, and the Pull Request row. Seven rows passed and one row is
environment-blocked with its cause, unblocking action, and equivalent evidence
for every Pull Request control. The PRD declares no Unreachable Acceptance and
no frontend surface. No row names another source. Counts:
`rows_blocked_environment: 1`, `rows_blocked_finding: 0`,
`rows_blocked_declared: 0`; failed: 0; skipped: 0.

## Mechanical skips

| Detector | Missing artifact |
| --- | --- |
| consequent-fix commit order | consequent-fix declaration |
| QA Report structure | QA Report |
| QA evidence paths | QA Report |

## Final verdict

Pass. All seven runnable rows passed; the sole environment-blocked Pull Request
row records its cause, unblocking action, and equivalent evidence for all five
controls. There are no findings, declared blocks, finding blocks, skipped rows,
or unresolved evidence paths. Findings by impact: Blocks-Completion 0,
Data-Loss 0, Trust-Damage 0, Friction 0, Cosmetic 0.
