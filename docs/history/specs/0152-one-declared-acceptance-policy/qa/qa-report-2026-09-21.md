---
spec: 0152-one-declared-acceptance-policy
date: 2026-09-21
build: 71b408eb63f62a5414e161998588d8ae23bbf4af
auditing_binary: "0.15.0 (71b408eb, built from the audited tree with -buildvcs=false)"
auditor_staleness: "current: binary built from the audited worktree head"
status: closed
verdict: pass
rows_blocked_environment: 1
rows_blocked_finding: 0
rows_blocked_declared: 0
surfaces: [backend, cli, docs]
---

# QA Report

## Scope and environment

Full terminal QA gate for commit `71b408eb63f62a5414e161998588d8ae23bbf4af` in the Run Worktree. The current Roundfix binary was built from that tree at `/tmp/roundfix-qa-0152-current/roundfix`. The intended actors are a Spec operator invoking the CLI and a maintainer preparing the Spec for delivery. No Pull Request is open, so that row uses the required environment-blocked equivalent-evidence path.

The strict authoring precondition passed with the current-tree binary:

```text
roundfix spec check 0152-one-declared-acceptance-policy --strict
Spec 0152-one-declared-acceptance-policy
No findings. Authored Verification commands were not executed.
```

The installed `roundfix 0.14.1` predates this Spec's changed derived command and was not used as the auditor. Its initial `SC-QA-VERIFICATION-AUTHORED` result was stale; rebuilding from the audited tree removed that mismatch.

## Static gate

The Daemon ran `make verify` outside the Agent sandbox for this Run Worktree and reported exit 0. Diagnostics were removed on success. Per the QA contract, the Agent recorded that supervised result and did not rerun repository Verification.

## Performed repairs

None.

## Assigned repair failures

None.

## Authorization audit inputs

None materialized by the mechanical stage. Row 6 performs the commit-dependent audit from Git history.

## Mechanical findings

None.

## Results

| # | Story / criterion / sweep | Actor and surface | Status | Evidence |
| - | --- | --- | --- | --- |
| 1 | Daemon repository Verification | Maintainer; repository gate | pass | Daemon-supervised `make verify`: exit 0; diagnostics removed on success |
| 2 | One exported decision; archive and derived Verification reach it | Spec operator; backend and CLI | pass | Focused runners passed; production call sites converge on `QAReportEligibility` |
| 3 | A newest qualifying `partial` settles the terminal `qa` Task | Spec operator; generated CLI Verification | pass | `TestDerivedQAVerificationSettlesAQualifyingPartial` passed |
| 4 | Refusal matrix remains closed; environment-blocked `pass` remains accepted | Spec operator; generated CLI Verification | pass | Generated-command and direct-CLI matrices passed |
| 5 | Archive behavior remains unchanged, checked against evidence outside this Spec | Maintainer; archive backend | pass | Pre-existing Spec 0058 replay corpus and archive compatibility table passed |
| 6 | Shipped-skill policy and Task-commit authority | Maintainer; docs and Git history | pass | Skill check/mirror passed; governed paths were separately authorized before Task commits |
| 7 | This Spec's artifacts satisfy the promise rule | Spec author; checker CLI | pass | Current-tree strict Spec check reported no findings |
| 8 | Pull Request controls through equivalent evidence | Maintainer; Pull Request journey | blocked (environment: no open Pull Request) | All five controls recorded through prompt, repository, and supervised Run evidence |

## Row evidence

### 1

Provenance: Requirement 1; repository Verification.

```yaml
inputs:
  - kind: repository_path
    ref: "**"
```

- Steps: record the Daemon-supervised repository Verification for the audited head; do not rerun it.
- Expected observable: `make verify` exits 0.
- Independent confirmation: Daemon supplies the command, verdict, and absence of retained diagnostics.
- Persistence check: the result is bound to the audited Run Worktree head.
- Evidence path: this report's Static gate section.

Observed: pass. The supervised command was `make verify`; verdict exit 0; no success diagnostics remained. The result is recorded as a fact and was not re-derived.

### 2

Provenance: Requirement 2.

```yaml
inputs:
  - kind: repository_path
    ref: internal/spec/qa.go
  - kind: repository_path
    ref: internal/spec/archive.go
  - kind: repository_path
    ref: internal/spec/task.go
  - kind: repository_path
    ref: internal/spec/qa_test.go
  - kind: repository_path
    ref: internal/spec/archive_test.go
  - kind: repository_path
    ref: internal/spec/task_test.go
```

- Steps: execute the focused public Go runners for the exported decision, archive delegation, and derived command; confirm the production call paths name `QAReportEligibility` and the derived command delegates to `qa-report accept`.
- Expected observable: every runner passes and the production paths converge on one decision.
- Independent confirmation: focused test output plus production call-site search.
- Persistence check: rerun against the current audited tree.
- Evidence path: this report and the named implementation/test inputs.

Observed: pass. The focused `internal/spec` runner passed `TestQAReportEligibility`, `TestArchiveAppliesTheOneEligibilityDecision`, and the derived-command suites. A production call-site search found the exported decision at `internal/spec/qa.go:297`, archive delegation at `internal/spec/archive.go:143`, CLI delegation at `internal/cli/qa_report.go:62`, and the rendered `qa-report accept` invocation at `internal/spec/task.go:114`.

### 3

Provenance: Requirement 3.

```yaml
inputs:
  - kind: repository_path
    ref: internal/spec/task.go
  - kind: repository_path
    ref: internal/spec/task_test.go
  - kind: repository_path
    ref: internal/cli/qa_report.go
```

- Steps: run the generated Verification end to end against a newest qualifying `partial` fixture.
- Expected observable: the generated command exits 0 through `roundfix qa-report accept`.
- Independent confirmation: the test asserts the report is newest and the exact generated command accepts it.
- Persistence check: rerun from a fresh test directory.
- Evidence path: this report and `internal/spec/task_test.go`.

Observed: pass. `TestDerivedQAVerificationSettlesAQualifyingPartial` ran the exact generated command against a fresh fixture and passed in 0.69 seconds.

### 4

Provenance: Requirement 4.

```yaml
inputs:
  - kind: repository_path
    ref: internal/spec/task.go
  - kind: repository_path
    ref: internal/spec/task_test.go
  - kind: repository_path
    ref: internal/cli/qa_report_test.go
```

- Steps: execute the generated-command and CLI matrices for `fail`, missing, unparseable, finding-blocked partial, environment-blocked partial, zero-declared partial, over-declared partial, and environment-blocked `pass`.
- Expected observable: every refusal exits nonzero and the allowed `pass` exits 0.
- Independent confirmation: generated-command and direct CLI runners exercise the same shapes through separate entry points.
- Persistence check: each runner creates fresh fixture directories.
- Evidence path: this report and the named test inputs.

Observed: pass. `TestDerivedQAVerificationDelegatesEligibility` passed all 16 cases, including missing and unparseable reports, finding- and environment-blocked partials, zero and over-declared partials, fail/newest-report controls, and the environment-blocked `pass`. `TestDerivedQAVerificationFailsClosedWhenDelegateCannotRun` passed. `TestRunQAReportAcceptCommandAcceptsAQualifyingPartial` and all three direct CLI fail-closed cases passed.

### 5

Provenance: Requirement 5; outside-evidence row.

```yaml
inputs:
  - kind: repository_path
    ref: internal/spec/testdata/archive-replay-0058/**
  - kind: repository_path
    ref: internal/spec/archive.go
  - kind: repository_path
    ref: internal/spec/archive_test.go
```

- Steps: execute archive compatibility tests against the pre-existing Spec 0058 replay corpus and the current accepted/refused shape table.
- Expected observable: accepted reports still archive; unmatched or wrongly declared reports still refuse with the same reasons.
- Independent confirmation: the outside evidence is `internal/spec/testdata/archive-replay-0058/`, introduced before Spec 0152 and not authored by this Spec.
- Persistence check: tests copy the corpus into fresh temporary repositories before each archive attempt.
- Evidence path: this report and `internal/spec/testdata/archive-replay-0058/`.

Observed: pass. `TestSpec0058ReplayArchivesDeclaredUnreachableRelease`, `TestSpec0058ReplayReportsWronglyDeclaredReachableRow`, and `TestSpec0058ReplayRefusesUnmatchedBlockedRow` passed from fresh temporary repositories. `TestArchiveAppliesTheOneEligibilityDecision` passed its nine accepted/refused shapes with exact refusal reasons. The outside corpus originated at commit `366e6642fcf239e609be28890f8563dd61971684` on 2026-08-12, before Spec 0152.

### 6

Provenance: Requirement 6.

```yaml
inputs:
  - kind: repository_path
    ref: .agents/skills/roundfix/SKILL.md
  - kind: repository_path
    ref: skills/roundfix/SKILL.md
  - kind: repository_path
    ref: docs/specs/0152-one-declared-acceptance-policy/_authorization.md
  - kind: repository_path
    ref: internal/speccheck/governed.go
  - kind: repository_path
    ref: .git
```

- Steps: run the skill contract check and mirror comparison; resolve every Task commit's exact paths with `git diff-tree`; compare governed paths and authorization ancestry chronologically.
- Expected observable: both skills state the one policy and match; every governed Task path was authorized before its Task commit and no Task exceeded its bounded paths.
- Independent confirmation: command output comes from the shipped CLI and Git objects, not Task Result prose.
- Persistence check: bind every finding to immutable commit IDs at the audited head.
- Evidence path: this report, the authorization record, and Git commit objects.

Observed: pass. `roundfix skills check` passed, the canonical and distributed skills were byte-identical, and both contained the declared-acceptance wording. Git established this chronology: initial authority at `048aaa48`; policy correction `d8886ae9`; bounded widening for `internal/spec/archive_test.go` at `f8cb3288`; then Task commits `19d02778`, `93c0c6a2`, `68f4a205`, and `e4aec38c`. `git diff-tree` showed Task 02 changed only its Task file plus the two governed archive paths; Task 04 changed only its Task file plus the two authorized skill paths. Tasks 01 and 03 changed their Task files and ordinary source/test paths named outside the governed set. Authorization and prerequisite corrections were separate ancestors, and no consequent-fix commit was required.

### 7

Provenance: Requirement 7.

```yaml
inputs:
  - kind: repository_path
    ref: docs/specs/0152-one-declared-acceptance-policy/**
  - kind: repository_path
    ref: internal/speccheck/**
```

- Steps: run `roundfix spec check 0152-one-declared-acceptance-policy --strict` with the current-tree binary.
- Expected observable: no findings; authored Verification commands remain unexecuted by the checker.
- Independent confirmation: the checker parses citations and promise-to-Task mappings from the committed artifacts.
- Persistence check: rerun after the matrix exists against the same audited head.
- Evidence path: this report and the Spec artifacts.

Observed: pass. The current-tree `roundfix 0.15.0` reported `No findings. Authored Verification commands were not executed.` The skipped detectors concerned absent project families or an optional Vocabulary Contract and did not replace or weaken any named QA row.

### 8

Provenance: Pull Request row.

```yaml
inputs:
  - kind: live_service
    ref: Pull Request for feat/0152-one-acceptance-policy
  - kind: repository_path
    ref: "**"
```

- Steps: because the prompt confirms no Pull Request is open, record the cause and inspect equivalent evidence for approval, checks/status, unresolved threads, Merge-Ready acceptance, and review-artifact ancestry.
- Expected observable: the row records every control and identifies any missing evidence without inferring it.
- Independent confirmation: the prompt supplies the no-PR fact; repository and Run facts supply the equivalent evidence.
- Persistence check: environment-blocked live-service rows are not carry-forward eligible.
- Evidence path: this report's Blocked and skipped section.

Observed: blocked by the declared environment fact, with equivalent evidence recorded below for every Pull Request control.

## Findings

None.

## Blocked and skipped

Row 8 is `blocked (environment: no open Pull Request)`. The prompt confirms that no Pull Request is open and that Pull Request journeys are environment-blocked. Equivalent evidence:

- Approval: no Pull Request approval was obtained.
- Checks and status: the Daemon ran `make verify` for the audited Run Worktree and reported exit 0.
- Unresolved review threads: no Pull Request exists, so no Pull Request review thread stands.
- Merge-Ready acceptance: no Review Source evidence exists yet; no Merge-Ready claim is made.
- Review-artifact ancestry: no review artifact exists yet; the audited repository head is `71b408eb63f62a5414e161998588d8ae23bbf4af`, and no evidence from another head is credited.

Unblocking action: open a Pull Request from `feat/0152-one-acceptance-policy`, then observe its approval, checks, review threads, Merge-Ready evidence, and review-artifact ancestry read-only. No rows were skipped, finding-blocked, or declared-blocked.

## Coverage

All 10 coverage sources appear in the matrix: Requirements 1-7, repository Verification, the outside-evidence row, and the Pull Request row. Seven rows passed. One row is environment-blocked with its cause, unblocking action, and equivalent evidence recorded. `rows_blocked_environment: 1`, `rows_blocked_finding: 0`, and `rows_blocked_declared: 0`. No frontend sweep or Unreachable Acceptance declaration applies, and no row covers a source outside this set.

## Final verdict

Pass: every runnable row passed; the sole environment-blocked row records its cause and equivalent evidence for all Pull Request controls; no row failed, remained pending, was skipped, or was finding- or declared-blocked. Findings by impact: Blocks-Completion 0, Data-Loss 0, Trust-Damage 0, Friction 0, Cosmetic 0.

## Mechanical skips

| Detector | Missing artifact |
| --- | --- |
| authorization bounded paths | Task commits |
| consequent-fix commit order | consequent-fix declaration |
| QA Report structure | QA Report |
| QA evidence paths | QA Report |
