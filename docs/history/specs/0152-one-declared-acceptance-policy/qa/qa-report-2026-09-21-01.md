---
spec: 0152-one-declared-acceptance-policy
date: 2026-09-21
build: e90329d3d08fcdd70f2b6b37d574067f85f56032
auditing_binary: "0.15.0 (go run -buildvcs=false from e90329d3)"
auditor_staleness: "current: auditor ran from the audited worktree head"
status: closed
verdict: pass
rows_blocked_environment: 1
rows_blocked_finding: 0
rows_blocked_declared: 0
surfaces: [backend, cli, docs]
---

# QA Report

## Scope and environment

Full terminal QA gate for commit `e90329d3d08fcdd70f2b6b37d574067f85f56032` in the Run Worktree. The intended actors are a Spec operator invoking Roundfix and a maintainer preparing the Spec for delivery. The auditor is the current-tree `roundfix 0.15.0`, invoked through `go run -buildvcs=false` because the sandbox did not execute the `/private/tmp` output directory as a binary. No Pull Request is open, so that row uses the required environment-blocked equivalent-evidence path.

The strict authoring precondition passed with the current-tree auditor:

```text
roundfix spec check 0152-one-declared-acceptance-policy --strict
Spec 0152-one-declared-acceptance-policy
No findings. Authored Verification commands were not executed.
```

## Static gate

The Daemon ran `make verify` outside the Agent sandbox for this Run Worktree and reported exit 0. Diagnostics were removed on success. Per the QA contract, the Agent recorded that supervised result and did not rerun repository Verification.

## Performed repairs

None.

## Assigned repair failures

None.

## Authorization audit inputs

None materialized by the mechanical stage. Row 6 performs the commit-dependent audit from Git history.

## Mechanical findings

None.

## Results

| # | Story / criterion / sweep | Actor and surface | Status | Evidence |
| - | --- | --- | --- | --- |
| 1 | Daemon repository Verification | Maintainer; repository gate | pass | Daemon-supervised `make verify`: exit 0; diagnostics removed on success |
| 2 | One exported decision; archive and derived Verification reach it | Spec operator; backend and CLI | pass | Focused runners passed; production paths converge on `QAReportEligibility` |
| 3 | A newest qualifying `partial` settles the terminal `qa` Task in process | Spec operator; Implement CLI and Daemon | pass | TaskCycle and Implement CLI verdict regressions passed |
| 4 | Refusal matrix remains closed; environment-blocked `pass` remains accepted | Spec operator; generated CLI Verification | pass | Generated-command, direct-CLI, and settlement matrices passed |
| 5 | Archive behavior remains unchanged, checked against evidence outside this Spec | Maintainer; archive backend | pass | Spec 0058 replay, compatibility matrix, and 102-report archive corpus passed |
| 6 | Shipped-skill policy and Task-commit authority | Maintainer; docs and Git history | pass | Skill/mirror passed; all governed paths had prior bounded authority |
| 7 | This Spec's artifacts satisfy the promise rule | Spec author; checker CLI | pass | Current-tree strict Spec check reported no findings |
| 8 | Pull Request controls through equivalent evidence | Maintainer; Pull Request journey | blocked (environment: no open Pull Request) | Prompt fact plus equivalent evidence for all five Pull Request controls |

## Row evidence

### 1

Provenance: Requirement 1; repository Verification.

```yaml
inputs:
  - kind: repository_path
    ref: "**"
```

- Steps: record the Daemon-supervised repository Verification for the audited head; do not rerun it.
- Expected observable: `make verify` exits 0.
- Independent confirmation: the Daemon supplies the command, verdict, and absence of retained diagnostics.
- Persistence check: bind the result to the audited Run Worktree head.
- Evidence path: this report's Static gate section.

Observed: pass. The supervised command was `make verify`; verdict exit 0; no success diagnostics remained. The result is bound to commit `e90329d3d08fcdd70f2b6b37d574067f85f56032` and was not re-derived.

### 2

Provenance: Requirement 2.

```yaml
inputs:
  - kind: repository_path
    ref: internal/spec/qa.go
  - kind: repository_path
    ref: internal/spec/archive.go
  - kind: repository_path
    ref: internal/spec/task.go
  - kind: repository_path
    ref: internal/cli/qa_report.go
  - kind: repository_path
    ref: internal/spec/qa_test.go
  - kind: repository_path
    ref: internal/spec/archive_test.go
  - kind: repository_path
    ref: internal/spec/task_test.go
```

- Steps: execute the focused runners for the exported decision, archive delegation, and derived command; confirm the production paths converge on `QAReportEligibility` and the generated command delegates to `qa-report accept`.
- Expected observable: every runner passes and the production paths reach one decision.
- Independent confirmation: focused test output plus production call-site search.
- Persistence check: rerun against the current audited tree.
- Evidence path: this report and the named implementation/test inputs.

Observed: pass. The fresh `internal/spec` runner passed `TestQAReportEligibility`, `TestArchiveAppliesTheOneEligibilityDecision`, and the generated-command suites. Production-path search found the exported decision at `internal/spec/qa.go:297`, archive delegation at `internal/spec/archive.go:143`, CLI delegation at `internal/cli/qa_report.go:62`, and the rendered `qa-report accept` invocation at `internal/spec/task.go:114`.

### 3

Provenance: Requirement 3.

```yaml
inputs:
  - kind: repository_path
    ref: internal/daemon/task_engine.go
  - kind: repository_path
    ref: internal/daemon/task_engine_test.go
  - kind: repository_path
    ref: internal/cli/implement.go
  - kind: repository_path
    ref: internal/cli/implement_test.go
  - kind: repository_path
    ref: internal/spec/qa.go
```

- Steps: run the TaskCycle regression for a newest qualifying `partial` with an isolated `PATH`, then run the Implement CLI verdict matrix.
- Expected observable: the `qa` Task settles `completed`, the Run does not end `Unresolved`, and settlement does not need a `roundfix` executable on `PATH`.
- Independent confirmation: the Daemon TaskCycle and CLI Run disposition exercise separate public boundaries.
- Persistence check: both runners create fresh fixture repositories and Run state.
- Evidence path: this report and the named daemon/CLI tests.

Observed: pass. `TestTaskCycleSettlesAQualifyingPartial` passed against a fresh Run fixture with `PATH` isolated from any `roundfix` executable, proving in-process settlement. `TestRunImplementQAVerdictMatrix/qualifying_partial` independently passed the Implement CLI disposition path, while its non-qualifying, fail, missing, and unreadable controls also passed. The qualifying `partial` settled the `qa` Task `completed` and the Run did not end `Unresolved`.

### 4

Provenance: Requirement 4.

```yaml
inputs:
  - kind: repository_path
    ref: internal/spec/task.go
  - kind: repository_path
    ref: internal/spec/task_test.go
  - kind: repository_path
    ref: internal/cli/qa_report.go
  - kind: repository_path
    ref: internal/cli/qa_report_test.go
  - kind: repository_path
    ref: internal/daemon/task_engine_test.go
```

- Steps: execute generated-command, direct CLI, and settlement matrices for `fail`, missing, unparseable/unreadable, finding-blocked partial, environment-blocked partial, zero-declared partial, over-declared partial, and environment-blocked `pass`.
- Expected observable: every refusal exits nonzero or settles failed with its reason; the allowed `pass` exits 0 and settles completed.
- Independent confirmation: generated command, direct CLI, and Daemon settlement exercise separate entry points.
- Persistence check: each runner creates fresh fixtures.
- Evidence path: this report and the named test inputs.

Observed: pass. `TestDerivedQAVerificationDelegatesEligibility` passed all 16 cases, including fail/newest-report ordering, missing and unparseable reports, finding- and environment-blocked partials, zero and over-declared partials, and the environment-blocked `pass`. `TestDerivedQAVerificationFailsClosedWhenDelegateCannotRun` passed. The direct CLI suite passed its qualifying-partial case and failed-report, missing-report, and unparseable-report controls. A current-tree public invocation accepted the previous environment-blocked `pass` report with exit 0 and no output; the same command refused this in-progress report's unsupported `pending` verdict with exit 1 and a diagnostic.

### 5

Provenance: Requirement 5; outside-evidence row.

```yaml
inputs:
  - kind: repository_path
    ref: internal/spec/testdata/archive-replay-0058/**
  - kind: repository_path
    ref: internal/spec/archive.go
  - kind: repository_path
    ref: internal/spec/archive_test.go
```

- Steps: execute archive compatibility tests against the pre-existing Spec 0058 replay corpus and the current accepted/refused shape table.
- Expected observable: accepted reports still archive; unmatched or wrongly declared reports still refuse with the same reasons.
- Independent confirmation: `internal/spec/testdata/archive-replay-0058/` predates Spec 0152 and was not authored by it.
- Persistence check: tests copy the corpus into fresh temporary repositories before each archive attempt.
- Evidence path: this report and `internal/spec/testdata/archive-replay-0058/`.

Observed: pass. `TestSpec0058ReplayArchivesDeclaredUnreachableRelease`, `TestSpec0058ReplayReportsWronglyDeclaredReachableRow`, and `TestSpec0058ReplayRefusesUnmatchedBlockedRow` passed from fresh temporary repositories. `TestArchiveAppliesTheOneEligibilityDecision` passed nine accepted/refused shapes with exact refusal reasons, and `TestArchivedPassCorpusRemainsArchiveEligible` confirmed 102 archived pass Specs. The outside corpus is `internal/spec/testdata/archive-replay-0058/`; Git traces it to commits `8dda1969b417924a2900cd0d46d0ff8d235a08fc` (2026-08-04) and `366e6642fcf239e609be28890f8563dd61971684` (2026-08-12), both predating Spec 0152.

### 6

Provenance: Requirement 6.

```yaml
inputs:
  - kind: repository_path
    ref: .agents/skills/roundfix/SKILL.md
  - kind: repository_path
    ref: skills/roundfix/SKILL.md
  - kind: repository_path
    ref: docs/specs/0152-one-declared-acceptance-policy/_authorization.md
  - kind: repository_path
    ref: internal/speccheck/governed.go
  - kind: repository_path
    ref: .git
```

- Steps: run the skill contract check and mirror comparison; resolve every Task commit's exact paths with `git diff-tree`; compare governed paths and authorization ancestry chronologically.
- Expected observable: both skills state the one policy and match; every governed Task path was authorized before its Task commit and no Task exceeded its bounded paths.
- Independent confirmation: command output comes from the shipped CLI and Git objects, not Task Result prose.
- Persistence check: bind every finding to immutable commit IDs at the audited head.
- Evidence path: this report, the authorization record, and Git commit objects.

Observed: pass. The current-tree `roundfix skills check` passed, the canonical and distributed skills were byte-identical, and both contained the declared-acceptance wording. Git established separate authorization ancestry at `048aaa48`, policy correction at `d8886ae9`, and the bounded `internal/spec/archive_test.go` widening at `f8cb3288`, followed by Task commits `19d02778`, `93c0c6a2`, `68f4a205`, `e4aec38c`, and `e90329d3`. `git diff-tree --no-commit-id --name-status -r` showed Task 02 changed only its Task file plus the two governed archive paths, while Task 04 changed only its Task file plus the two authorized skill paths. Tasks 01, 03, and 06 changed their Task files and ordinary source/test paths outside the governed set. Every governed mutation was within `_authorization.md` and descended from its grant; authorization and correction commits were separate, and no consequent-fix commit was present or needed.

### 7

Provenance: Requirement 7.

```yaml
inputs:
  - kind: repository_path
    ref: docs/specs/0152-one-declared-acceptance-policy/**
  - kind: repository_path
    ref: internal/speccheck/**
```

- Steps: rerun `roundfix spec check 0152-one-declared-acceptance-policy --strict` with the current-tree auditor after the matrix exists.
- Expected observable: no findings; authored Verification commands remain unexecuted by the checker.
- Independent confirmation: the checker parses citations and promise-to-Task mappings from committed artifacts.
- Persistence check: rerun against the same audited head.
- Evidence path: this report and the Spec artifacts.

Observed: pass. The current-tree `roundfix 0.15.0` checker reported `No findings. Authored Verification commands were not executed.` The skipped detectors concern absent optional project families, an optional Vocabulary Contract, and an absent references index; none replaces or weakens a named QA row.

### 8

Provenance: Pull Request row.

```yaml
inputs:
  - kind: live_service
    ref: Pull Request for feat/0152-one-acceptance-policy
  - kind: repository_path
    ref: "**"
```

- Steps: because the prompt confirms no Pull Request is open, record the cause and inspect equivalent evidence for approval, checks/status, unresolved threads, Merge-Ready acceptance, and review-artifact ancestry.
- Expected observable: the row records every control and identifies missing evidence without inferring it.
- Independent confirmation: the prompt supplies the no-PR fact; repository and Run facts supply equivalent evidence.
- Persistence check: environment-blocked live-service rows are not carry-forward eligible.
- Evidence path: this report's Blocked and skipped section.

Observed: blocked by the declared environment fact. Equivalent evidence for approval, checks/status, unresolved threads, Merge-Ready acceptance, and review-artifact ancestry is recorded below; no unavailable Pull Request state is inferred.

## Findings

None.

## Blocked and skipped

Row 8 is `blocked (environment: no open Pull Request)`. The prompt confirms that no Pull Request is open and that Pull Request journeys are environment-blocked. Equivalent evidence:

- Approval: no Pull Request approval was obtained.
- Checks and status: the Daemon ran `make verify` for audited head `e90329d3d08fcdd70f2b6b37d574067f85f56032` and reported exit 0.
- Unresolved review threads: no Pull Request exists, so no Pull Request review thread stands.
- Merge-Ready acceptance: no Review Source evidence exists yet; no Merge-Ready claim is made.
- Review-artifact ancestry: no review artifact exists yet; no evidence from another head is credited.

Unblocking action: open a Pull Request from `feat/0152-one-acceptance-policy`, then observe its approval, checks, review threads, Merge-Ready evidence, and review-artifact ancestry read-only. No rows were skipped, finding-blocked, or declared-blocked.

## Coverage

All 10 coverage sources appear in the matrix: Requirements 1-7, repository Verification, the outside-evidence row, and the Pull Request row. Seven rows passed. One row is environment-blocked with its cause, unblocking action, and equivalent evidence recorded. `rows_blocked_environment: 1`, `rows_blocked_finding: 0`, and `rows_blocked_declared: 0`. No frontend sweep or Unreachable Acceptance declaration applies, and no row covers a source outside this set.

## Final verdict

Pass: every runnable row passed; the sole environment-blocked row records its cause and equivalent evidence for all Pull Request controls; no row failed, remained pending, was skipped, or was finding- or declared-blocked. Findings by impact: Blocks-Completion 0, Data-Loss 0, Trust-Damage 0, Friction 0, Cosmetic 0.

## Mechanical skips

| Detector | Missing artifact |
| --- | --- |
| authorization bounded paths | Task commits |
| consequent-fix commit order | consequent-fix declaration |
| QA evidence paths | docs/specs/0152-one-declared-acceptance-policy/qa/evidence |
