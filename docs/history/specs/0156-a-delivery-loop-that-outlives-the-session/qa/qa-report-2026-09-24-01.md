---
spec: 0156-a-delivery-loop-that-outlives-the-session
date: 2026-09-24
build: acbdcf02677f6404387508b1aefd0cb6c1a57329
auditing_binary: "roundfix 0.16.0 (/private/tmp/roundfix-0156-qa-01, built from acbdcf02677f6404387508b1aefd0cb6c1a57329 with -buildvcs=false)"
auditor_staleness: "current: binary built from the audited worktree before QA execution"
status: closed
verdict: pass
rows_blocked_environment: 1
rows_blocked_finding: 0
rows_blocked_declared: 0
surfaces: [backend, cli, docs]
---

# QA report — A delivery loop that outlives the session

## Scope and environment

- Scope: full authored terminal gate for build `acbdcf02677f6404387508b1aefd0cb6c1a57329` on Run Worktree branch `roundfix/run-run_20260924T181406Z_f180ad4047807d42`.
- Target branch: `feat/0156-durable-delivery-loop` at `08ccae0a6718b1e9a0a7d5e074978d934f116d48`; it is an ancestor of the audited build.
- Actor: a Roundfix operator using the built CLI and the fake GitHub boundary supplied for hermetic delivery journeys.
- Pull Request: none open; Pull Request journeys are environment-blocked by the Run contract.
- Repository Verification: Daemon-supervised `make verify` passed with exit 0; QA records that result and will not rerun it.
- Precondition: `roundfix spec check 0156-a-delivery-loop-that-outlives-the-session --strict` passed with no findings using installed `roundfix 0.15.0`. The checker skipped only artifacts absent from this Spec: `docs/findings` rollup, `docs/backlog`, a Vocabulary Contract, and `references/_index.md`.

## Static gate

- `make verify`: passed with exit 0 in the Daemon-supervised repository Verification; not rerun by QA.
- Current-tree build and strict Spec consistency check: passed.

## Performed repairs

None.

## Assigned repair failures

None.

## Authorization audit inputs

None.

The seeded mechanical stage materialized no inputs. R8 will record the final gate's independent commit-path and authorization-ancestry audit.

## Mechanical findings

None.

## Results

| # | Story / criterion / sweep | Actor and surface | Status | Provenance |
| - | --- | --- | --- | --- |
| R1 | Repository Verification is green for the assembled tree. | Daemon / repository gate | pass | Requirement 1; repository Verification |
| R2 | A two-Spec fixture queue reaches `merged` for every item without operator action between items. | Operator / delivery engine | pass | Requirement 2 |
| R3 | Resume after lost push, pull-request, and merge receipts creates no duplicate pull request and performs no duplicate merge. | Operator / delivery engine | pass | Requirement 3 |
| R4 | A non-archive commit after review parks the item as `review-stale` before publication. | Operator / delivery engine | pass | Requirement 4 |
| R5 | Missing `merge` authorization parks the item before any external action. | Maintainer / authorization boundary | pass | Requirement 5 |
| R6 | A parked item does not stop later items. | Operator / delivery engine | pass | Requirement 6 |
| R7 | Delivery tests do not reach GitHub, and both shipped skill copies describe the command family and remain byte-identical. | Maintainer / test and docs surfaces | pass | Requirement 7 |
| R8 | This Spec satisfies its promise rule and every Task commit stays inside the approved authority and chronology. | Maintainer / Spec and Git history | pass | Requirement 8 |
| R9 | Independent historical evidence supports the need for durable orchestration outside a terminal session. | Maintainer / prior Run evidence | pass | outside-evidence row |
| R10 | Pull Request approval, checks, review threads, Merge-Ready acceptance, and review-artifact ancestry are accounted for. | Reviewer / Pull Request | blocked (environment: no open Pull Request) | Pull Request row |

## Mechanical skips

| Detector | Missing artifact |
| --- | --- |
| authorization bounded paths | Task commits |
| consequent-fix commit order | consequent-fix declaration |
| QA evidence paths | docs/specs/0156-a-delivery-loop-that-outlives-the-session/qa/evidence |

## Row evidence

### R1

```yaml
inputs:
  - kind: repository_path
    ref: "**/*"
```

The Daemon ran `make verify` outside the Agent sandbox against the assembled tree and reported exit 0 with diagnostics removed on success. This is supervised static-gate evidence, not a QA rerun.

### R2

```yaml
inputs:
  - kind: repository_path
    ref: internal/delivery/**
```

`GOCACHE=/private/tmp/roundfix-0156-qa-01-gocache GOPROXY=off GOSUMDB=off go test -count=1 -v -run '^TestDeliveryEngineMergesAQueueWithoutAnOperator$' ./internal/delivery` passed. The executable fixture processed two ordered Specs, exercised enabled review for one and a recorded `none` omission for the other, observed exactly two pull-request and two merge effects, reread both persisted items as `merged` with merge commits, found no unmatched intents, and asserted the full stage-event order for each item.

### R3

```yaml
inputs:
  - kind: repository_path
    ref: internal/delivery/**
```

The same focused command ran `TestDeliveryEngineResumesWithoutDoubleEffects` and passed all three subcases: unmatched push intent, unmatched pull-request intent, and unmatched merge intent. Each subcase reread the durable queue as `merged`, asserted the cumulative external-effect counts did not increase for the already-observed action, and confirmed no unmatched intent remained.

### R4

```yaml
inputs:
  - kind: repository_path
    ref: internal/delivery/**
```

The same focused command ran `TestDeliveryEngineParksAStaleReview` and passed. The fixture supplied an archive result whose parent was a foreign commit, then independently reread the first item as `parked` with blocker `review-stale`, observed zero external calls for it, and confirmed the following item still reached `merged`.

### R5

```yaml
inputs:
  - kind: repository_path
    ref: internal/delivery/**
  - kind: repository_path
    ref: docs/specs/0156-a-delivery-loop-that-outlives-the-session/_authorization.md
```

The same focused command ran `TestDeliveryEngineParksUnauthorizedBeforeExternalAction` and passed. The fixture omitted `merge` authority, reread the persisted item as `parked` with blocker `unauthorized`, and asserted zero calls crossed the pull-request boundary.

### R6

```yaml
inputs:
  - kind: repository_path
    ref: internal/delivery/**
```

The same focused command ran `TestDeliveryEngineParksDeclaredBlockersAndContinues` and passed. The fixture persisted blockers for an Unresolved Run, review findings, blocked review, a failing gate, failed checks, and cancelled checks, then reread the later item as `merged`.

### R7

```yaml
inputs:
  - kind: repository_path
    ref: internal/delivery/**
  - kind: repository_path
    ref: .agents/skills/roundfix/SKILL.md
  - kind: repository_path
    ref: skills/roundfix/SKILL.md
  - kind: repository_path
    ref: docs/user-guide/commands.md
```

`GOCACHE=/private/tmp/roundfix-0156-qa-01-gocache GOPROXY=off GOSUMDB=off go test -count=1 -v ./internal/delivery` passed all Delivery Engine and GitHub boundary cases, including the Task 08 publication-safety regressions. The environment disabled module downloads. A source sweep for `exec.Command`, `exec.CommandContext`, HTTP client calls, and `github.com` in `internal/delivery/*_test.go` returned no matches; the executed boundary cases use injected command runners and engine journeys use the fake pull-request boundary. `/private/tmp/roundfix-0156-qa-01 deliver --help` exited 0 and exposed `start`, `status`, `resume`, and `stop` through the built public CLI. `/private/tmp/roundfix-0156-qa-01 skills check` passed. `cmp -s .agents/skills/roundfix/SKILL.md skills/roundfix/SKILL.md` exited 0, and both copies plus `docs/user-guide/commands.md` describe the same four commands.

### R8

```yaml
inputs:
  - kind: repository_path
    ref: docs/specs/0156-a-delivery-loop-that-outlives-the-session/**
  - kind: repository_path
    ref: .agents/skills/roundfix/SKILL.md
  - kind: repository_path
    ref: skills/roundfix/SKILL.md
  - kind: repository_path
    ref: internal/cli/cli_test.go
```

The installed binary's strict Spec check passed before the matrix with no findings, including the Spec promise mapping; the current-tree binary repeated the same check after it was built. Fresh `git diff-tree --no-commit-id --name-status -r <commit>` audits covered the authorization, Spec, corrective-task, prior-QA, reopen, and every Task commit from `8f0d5301` through `acbdcf02`. The authorization landed alone at `8f0d5301` before every implementation commit. Task 04's only governed change was the expressly bounded `internal/cli/cli_test.go`; Task 05's governed changes were exactly the bounded `.agents/skills/roundfix/SKILL.md` and `skills/roundfix/SKILL.md`; Tasks 01-03, 07, and 08 changed only ordinary source, docs, and their assigned Task files. `git merge-base --is-ancestor 8f0d5301 <task-commit>` exited 0 for Tasks 01-05, 07, and 08. The authorization commit contains only `_authorization.md`, no prerequisite fix was folded into a Task commit, and the authorization declares no consequent-fix commit.

### R9

```yaml
inputs:
  - kind: external_repository
    ref: /Users/marcio/dev/roundfix
  - kind: elapsed_time
    ref: Spec 0154 Run chronology measured on 2026-09-24
```

Outside source: the machine-wide Run Database as read from the separate user checkout `/private/tmp/claude-501/-Users-marcio-dev-roundfix/0f9d9d2d-9b0a-4336-8d50-a94fb7322388/scratchpad/wt-0156`, not this Spec's artifacts. Installed `roundfix 0.15.0`, which can read that database, ran `roundfix runs list --all --state all --limit 0` and showed three Spec 0154 Runs starting at `09:38:16Z`, `11:26:56Z`, and `19:03:36Z`, with durations `1h6m`, `32m`, and `37m`. Independent `roundfix events <run-id> --filter outcome` reads confirmed each reached `Clean` at `10:44:37Z`, `11:59:47Z`, and `19:40:44Z`. The observed gaps between a Clean outcome and the next Run start were about 42 minutes and 7 hours 4 minutes, while the processes themselves completed; this independently supports the Spec's claim that orchestration gaps, rather than Run execution, dominate elapsed delivery time.

### R10

```yaml
inputs:
  - kind: live_service
    ref: Pull Request for feat/0156-durable-delivery-loop
  - kind: repository_path
    ref: docs/specs/0156-a-delivery-loop-that-outlives-the-session/_authorization.md
```

The prompt proves there is no open Pull Request for `feat/0156-durable-delivery-loop`; the Run Worktree branch is never pushed and has no Pull Request of its own. Equivalent evidence for every control: approval — the Spec authorization is `approved`, while no Pull Request approval has been obtained; checks and status — Daemon-supervised `make verify` passed with exit 0 and the current-tree strict Spec check passed; unresolved review threads — none can stand without a Pull Request; Merge-Ready acceptance — none exists yet because the authored QA gate precedes pre-PR review; review-artifact ancestry — no Spec `reviews/` tree exists, the target is `08ccae0a6718b1e9a0a7d5e074978d934f116d48`, the audited Run head is `acbdcf02677f6404387508b1aefd0cb6c1a57329`, and `git merge-base --is-ancestor feat/0156-durable-delivery-loop HEAD` exited 0. Unblocking action: integrate the Run, perform the configured pre-PR review and archive stages, then open the Pull Request from the target branch.

## Findings

None.

## Blocked and skipped

- R10 is blocked by the environment fact that no Pull Request is open. Every Pull Request control has equivalent observed or supervised evidence, so this environment block does not prevent a passing verdict.

## Coverage

All 10 planned rows reached terminal outcomes: 9 passed and 1 was environment-blocked with equivalent evidence. Coverage is 8 of 8 declared Requirements, the repository Verification, the outside-evidence row, and the Pull Request row. `rows_blocked_environment: 1`, `rows_blocked_finding: 0`, and `rows_blocked_declared: 0`. The PRD declares no Unreachable Acceptance rows and no frontend surface. No matrix row was skipped.

## Final verdict

Pass. Every runnable row passed; R10 is environment-blocked because no Pull Request exists and records equivalent evidence for approval, checks and status, unresolved review threads, Merge-Ready acceptance, and review-artifact ancestry. There are no findings, declared blocks, finding blocks, skipped rows, or unresolved evidence paths.
