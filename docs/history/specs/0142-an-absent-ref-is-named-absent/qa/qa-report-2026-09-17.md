---
spec: 0142-an-absent-ref-is-named-absent
date: 2026-09-17
build: 06f85cc41e2417697bbbe350cadb811216ddd007
status: closed
verdict: pass
auditing_binary: "0.13.1 (16d1f04b, built 2026-09-17 19:18:15 -0300)"
auditor_staleness: "stale: commit ancestry: build commit predates audited tree"
rows_blocked_environment: 1
rows_blocked_finding: 0
rows_blocked_declared: 0
surfaces: [backend, cli]
---

# QA Report

## Scope and environment

This full gate audited commit `06f85cc41e2417697bbbe350cadb811216ddd007`
from the Run Worktree branch. The actor was a maintainer reconciling terminal
Spec Runs. Backend behavior was exercised through the TechSpec's real Git
fixture seam, and the CLI entry point was confirmed through
`go run ./cmd/roundfix reconcile --help` with an isolated Go build cache. No
open Pull Request exists for the target branch, as the Daemon's supervised QA
context states.

`roundfix spec check 0142-an-absent-ref-is-named-absent --strict` exited 0 with
`No findings`; it did not execute authored Verification commands. The local
PATH binary reported `roundfix 0.12.0`. The seeded mechanical audit came from
the Auditing Binary recorded in frontmatter and marked it stale because its
build commit predates the audited tree.

## Static gate

- `make verify`: pass, exit 0. The Daemon ran this outside the Agent sandbox
  and removed diagnostics after success. This gate recorded that supplied fact
  and did not rerun the command.
- `roundfix spec check 0142-an-absent-ref-is-named-absent --strict`: pass, exit
  0, `No findings`. The checker skipped `SC-ROLLUP-MEMBER` because no findings
  rollup exists, `SC-BACKLOG-UNMOVED` because no backlog exists, and its
  vocabulary detector because it did not resolve the TechSpec Vocabulary
  Contract. Those skips do not cover a QA source.

## Performed repairs

None.

## Assigned repair failures

None.

## Authorization audit inputs

None.

## Mechanical findings

None.

## Results

| # | Status | Provenance |
| - | --- | --- |
| QA-01 | pass | Requirement 1; repository Verification |
| QA-02 | pass | Requirement 2 |
| QA-03 | pass | Requirement 3 |
| QA-04 | pass | Requirement 4 |
| QA-05 | pass | Requirement 5; outside evidence |
| QA-06 | pass | Requirement 6 |
| QA-07 | blocked (environment: no open Pull Request) | Pull Request row |

### QA-01

Actor and surface: maintainer, repository gate. Entry point: `make verify`.
The expected observable was an exit code of 0 from the repository Verification.
The Daemon independently observed exit 0 outside the Agent sandbox and removed
the success diagnostics. The result applies to audited commit `06f85cc4`; no
persistence check applies to a static gate.

### QA-02

Actor and surface: maintainer, backend resolver. Entry point:
`TestResolveLocalBranchTellsAbsentFromAmbiguous` at the TechSpec's Git fixture
seam. A fresh run created a repository and exercised absent, ambiguous, and
resolvable names. The absent case matched `errBranchAbsent`, rejected
`errBranchAmbiguous`, and named `missing-branch`; the branch-plus-tag case
matched `errBranchAmbiguous` and retained `short ref is ambiguous`; `main`
resolved to its independently read commit.

Evidence: `GOCACHE=/private/tmp/roundfix-qa-0142-go-cache go test -count=1 -v
-run '^TestResolveLocalBranchTellsAbsentFromAmbiguous$' ./internal/worktree`
exited 0. All three subtests passed. Each subtest re-read Git state through a
new command, which supplied independent confirmation; persistence beyond the
fixture lifetime is not part of this resolver contract.

### QA-03

Actor and surface: maintainer, Run Branch set classifier. Entry point:
`TestClassifyRunBranchSetPreservesAbsentTarget` at the TechSpec's Git fixture
seam. The run detached HEAD, deleted the recorded target branch, classified two
existing candidates, and observed both only in `Preserved`. Each reason was
`target branch "main" is absent`. The result contained no current branch,
releasable branch, or release proof.

Evidence: `GOCACHE=/private/tmp/roundfix-qa-0142-go-cache go test -count=1 -v
-run '^TestClassifyRunBranchSetPreservesAbsentTarget$' ./internal/worktree`
exited 0. The test independently re-read the candidate list and every reason
after classification. The fixture's deleted branch persisted for the complete
classification.

### QA-04

Actor and surface: maintainer, Run Branch set classifier. The same fresh
classifier replay first exercised a present target and observed its existing
current branch, QA Report, releasable predecessor, and release proof. It then
created a same-name tag and observed the ambiguous-target refusal. The absent
case observed no current or releasable branch, proving that the new path
released nothing.

Adjacent regression evidence:
`GOCACHE=/private/tmp/roundfix-qa-0142-go-cache go test -count=1 -v -run
'^TestClassifyRunBranchSet(FourFailedCycles|PreservesUnintegratedImplementationWork)$'
./internal/worktree` exited 0. Both existing outcome tests passed from fresh
fixtures, independently confirming the unchanged present-target behavior.

### QA-05

Actor and surface: maintainer, reconciliation evidence. The outside evidence
origin is the Run Database and `roundfix reconcile` observation made on this
machine on 2026-09-17, adopted at
`references/2026-09-17-reconcile-reports-an-absent-ref-as-ambiguous.md`. It
records fourteen refusals that called
`ma/0118-a-task-proved-once-does-not-run-twice` ambiguous while Git listed no
matching ref.

A fresh independent lookup,
`git for-each-ref --format='%(refname)'
refs/heads/ma/0118-a-task-proved-once-does-not-run-twice
refs/tags/ma/0118-a-task-proved-once-does-not-run-twice`, exited 0 with empty
output. QA-02 replayed that zero-ref shape and observed the absent sentinel;
QA-03 replayed it at set classification and observed the absent-branch reason.
The source observation therefore has the same zero-ref shape as the new reason
replaces. The lookup is read-only, so no persistence action applies.

### QA-06

Actor and surface: Spec maintainer, authoring contract. Entry point:
`roundfix spec check 0142-an-absent-ref-is-named-absent --strict`. The command
exited 0 with `No findings`, establishing that the numbered Success Metrics and
API Contracts are declared, the metrics map through the TechSpec Coverage Map,
and Tasks name the promises in References. The independent confirmation is the
checker loading the PRD, TechSpec, Task Graph, and Task files as one Spec. The
artifacts were unchanged between this check and the functional replays.

### QA-07

Actor and surface: reviewer, Pull Request controls. Entry point: the Spec target
branch's Pull Request. The Daemon supplied the environment fact that no Pull
Request is open and that Pull Request journeys are environment-blocked.

Equivalent supervised evidence records every control:

- approval: no Pull Request acceptance was obtained;
- checks and status: the Daemon ran `make verify` and observed exit 0;
- unresolved review threads: none stands because no Pull Request exists;
- Merge-Ready acceptance: no Review Source evidence exists yet;
- review-artifact ancestry: no review artifact exists; the audited head is
  `06f85cc41e2417697bbbe350cadb811216ddd007`.

Unblocking action: open the Pull Request from `feat/0142-absent-ref` after this
gate and observe its controls there. The Run Worktree branch is never pushed
and has no Pull Request of its own.

## Mechanical skips

| Detector | Missing artifact |
| --- | --- |
| authorization bounded paths | tooling authorization |
| consequent-fix commit order | consequent-fix declaration |
| QA Report structure | QA Report |
| QA evidence paths | QA Report |

## Findings

None.

## Blocked and skipped

QA-07 was blocked by the declared environment fact that no Pull Request is
open. Its equivalent supervised evidence is recorded under the row. No planned
QA row was skipped, finding-blocked, or declared-blocked. The mechanical skips
above are preserved from the seeded audit and identify absent inputs rather
than planned QA coverage.

## Coverage

Six runnable rows passed out of six. One Pull Request row was environment
blocked with equivalent supervised evidence. The matrix covers Requirements
1-6, the repository Verification, outside evidence, and the Pull Request row;
no PRD Unreachable Acceptance declaration or frontend sweep applies. Counts:
`rows_blocked_environment=1`, `rows_blocked_finding=0`, and
`rows_blocked_declared=0`.

## Final verdict

Pass. Every runnable criterion passed, the environment-blocked Pull Request row
records its cause and equivalent supervised evidence, and no row failed, was
finding-blocked, was declared-blocked, or was skipped.
