---
spec: 0171-a-deterministic-suite-under-load
date: 2026-09-25
build: 5fdaa3bf342f9578711d53767c00b9107aa436d2
auditing_binary: "0.17.0 (256ad156, built 2026-09-25 15:19:40 -0300)"
auditor_staleness: "stale: commit ancestry: build commit predates audited tree"
status: closed
verdict: fail
rows_blocked_environment: 2
rows_blocked_finding: 0
rows_blocked_declared: 0
surfaces: [backend]
---

# QA report — A deterministic suite under load

## Scope and environment

This full terminal gate exercises the assembled test-suite behavior at commit
`5fdaa3bf342f9578711d53767c00b9107aa436d2`. The actor is a Roundfix maintainer
running Go package tests and repository checks from the Run Worktree on macOS.
The Spec declares only the `backend` surface and no Unreachable Acceptance
items. No frontend sweep applies.

The authored terminal node is `task_06`; it depends on completed Tasks 01-05.
`roundfix spec check 0171-a-deterministic-suite-under-load --strict` passed with
exit 0 before the matrix opened. It reported no findings and did not execute
authored Verification commands. It skipped `SC-ROLLUP-MEMBER` because there is
no findings rollup and skipped the vocabulary detector because the TechSpec has
no Vocabulary Contract. The latter obligation remains in row 9.

The mechanical audit passed without repairs or findings. Its `0.17.0` binary is
stale because its build commit predates the audited tree; that staleness is
preserved above instead of being presented as current evidence.

## Static gate

The Daemon already ran `make verify-changed` outside the Agent sandbox. It
reported exit 0 and removed diagnostics on success. Per the QA assignment, row
1 records that supervised fact and this Agent does not rerun repository
Verification.

## Performed repairs

None.

## Assigned repair failures

None.

## Authorization audit inputs

None.

## Mechanical findings

None.

## Results

| # | Story / criterion / sweep | Actor and surface | Status | Provenance |
| - | --- | --- | --- | --- |
| 1 | Repository Verification | Maintainer, backend static gate | pass | Requirement 1; repository Verification |
| 2 | Deadline-bound waits report ended work and prefer ready values | Maintainer, `go test` | pass | Requirement 2 |
| 3 | Wait and bootstrap sources contain no forbidden fixed durations | Maintainer, source-contract commands | pass | Requirement 3 |
| 4 | Four historical failures pass under stress beside the suite | Maintainer, concurrent `go test` | fail | Requirement 3 |
| 5 | Operational commands use the offline release lookup | Maintainer, `go test` | pass | Requirement 4 |
| 6 | Compiled adapter fixture runs through a relative binary path | Maintainer, compiled Go test binary | pass | Requirement 5 |
| 7 | Go deadline semantics and the four historical failures resolve from outside sources | Maintainer, published Go docs and prior Run reports | blocked (environment: fourth originating report unavailable) | Requirement 6; outside-evidence row |
| 8 | Git scope contains no production or protected-file change | Maintainer, Git history | pass | Requirement 7 |
| 9 | Spec promises name consuming Tasks and no new vocabulary is emitted | Maintainer, strict Spec checker and focused commands | pass | Requirement 8 |
| 10 | Pull Request controls have equivalent evidence | Reviewer, Pull Request surface | blocked (environment: no open Pull Request) | Pull Request row |

## Row evidence

### 1

```yaml
inputs:
  - kind: repository_path
    ref: "**"
```

- Entry point: Daemon-owned repository Verification.
- Steps: record the supplied `make verify-changed` result without rerunning it.
- Expected observable: exit 0 with no retained diagnostics.
- Independent confirmation: the Daemon supplied both the verdict and the
  success-path diagnostic disposition.
- Persistence check: not applicable to a static gate fact.
- Evidence: supervised Daemon result: `make verify-changed` exited 0 and its
  diagnostics were removed on success. The command was not rerun in this Agent
  sandbox.

### 2

```yaml
inputs:
  - kind: repository_path
    ref: internal/testwait/testwait.go
  - kind: repository_path
    ref: internal/testwait/testwait_test.go
```

- Entry point: focused `go test` selection in `./internal/testwait`.
- Steps: run the deadline, ended-work, and simultaneous-ready cases.
- Expected observable: the deadline case waits until its declared bound, the
  ended-work case fails promptly and names the result, and ready wins when both
  channels are ready.
- Independent confirmation: require each named test to appear as passing.
- Persistence check: rerun through the package test process selected by the
  command rather than inspecting helper source.
- Evidence: `GOCACHE=/private/tmp/roundfix-0171-qa-gocache go test -count=1
  -v -run
  '^(TestBoundFollowsTheTestDeadline|TestUntilPrefersTheReadyValueOverAnEndedWork|TestUntilFailsAtOnceWhenTheWorkEnds|TestUntilFailsAtTheDeadlineAndNotBefore)$'
  ./internal/testwait` exited 0. All four named tests passed; the deadline case
  took 0.30 s, while the ended-work and simultaneous-ready cases completed
  immediately.

### 3

```yaml
inputs:
  - kind: repository_path
    ref: internal/cli/implement_test.go
  - kind: repository_path
    ref: internal/daemon/task_engine_test.go
  - kind: repository_path
    ref: internal/worktree/worktree_test.go
```

- Entry point: executable source-contract searches.
- Steps: reject fixed wait APIs, removed named budgets, literal 90-second waits,
  and unexamined one- or five-second bootstrap timeouts; confirm `testwait` use.
- Expected observable: every forbidden search has no match and every required
  helper search has a match.
- Independent confirmation: the behavioral stress in row 4 exercises the same
  migrated paths.
- Persistence check: all three current files are searched in one run.
- Evidence: separate `rg` commands found no forbidden pattern in any of the
  three files (each exited 1 with no output). A positive search exited 0 and
  found `testwait.Bound`, `testwait.Until`, or `testwait.Poll` throughout all
  three files, including the three deadline-derived Implement bootstrap
  timeouts and every non-subject worktree bootstrap timeout.

### 4

```yaml
inputs:
  - kind: repository_path
    ref: internal/cli/implement_test.go
  - kind: repository_path
    ref: internal/daemon/task_engine_test.go
  - kind: repository_path
    ref: internal/worktree/worktree_test.go
  - kind: repository_path
    ref: internal/testwait/testwait.go
  - kind: elapsed_time
    ref: count-10 stress under concurrent package load
```

- Entry point: the four named regression tests through `go test -count=10 -cpu
  1,4`, while other package tests run beside them.
- Steps: start the suite packages and the focused stress concurrently; wait for
  both commands and retain each exit status.
- Expected observable: every iteration passes in the focused command and the
  adjacent package load also completes successfully.
- Independent confirmation: require all four names in verbose pass output and
  the source constraints from row 3.
- Persistence check: 20 scheduler configurations per named test (`count=10`
  across CPU settings 1 and 4).
- Probe: concurrent adjacent packages reproduce the load shape that made the
  previous fixed-duration tests unreliable.
- Evidence: while `GOCACHE=/private/tmp/roundfix-0171-qa-adjacent-gocache go
  test -count=1 ./...` ran beside it, the declared `-count=10 -cpu 1,4` command
  exited 1. The queued-cancellation, Verification-capacity-cancellation, and
  sibling-bootstrap tests passed all 20 scheduler runs. One of 20 runs of
  `TestRunImplementBootstrapsEachConcurrentTaskWorktreeBeforeAgentWork` failed
  after 2.81 s with `expected Clean exit, got 1`; stderr reported Git could not
  create `task_01`'s worktree because reading `task_02/commondir` returned
  `Result too large`. See finding F-001.

  The adjacent load ran for 136 s and exercised the repository packages. It
  exited 1 only because the sandbox denied process-table access to two Force
  Stop integration tests (`operation not permitted`); all other reported
  packages passed. That sandbox-only load-generator result does not replace or
  soften the focused stress failure.

### 5

```yaml
inputs:
  - kind: repository_path
    ref: internal/cli/version_freshness_isolation_test.go
  - kind: repository_path
    ref: internal/cli/implement_test.go
  - kind: repository_path
    ref: internal/daemon/run_disposition_characterization_test.go
```

- Entry point: focused `go test` commands in `./internal/cli` and
  `./internal/daemon`.
- Steps: exercise `fetch`, `resolve`, `watch`, and `implement` with fresh test
  HOMEs, the helper process, built binaries, and a per-test override.
- Expected observable: the offline tag is recorded, seeded caches remain
  byte-identical, and the override still prints the upgrade warning.
- Independent confirmation: the tests read the resulting cache through the
  public file contract and compare built-binary cache bytes before and after.
- Persistence check: every command gets a fresh HOME; the built-binary paths
  reopen the cache after process exit.
- Evidence: `GOCACHE=/private/tmp/roundfix-0171-qa-gocache go test -count=1
  -v -run
  '^(TestSuiteDefaultReleaseLookupStaysOffline|TestOperationalCommandsRecordTheOfflineLookup|TestHelperProcessRecordsTheOfflineLookup|TestBuiltBinaryLeavesTheSeededVersionCacheUntouched|TestPerTestReleaseLookupOverridesTheSuiteDefault|TestRunDispositionCharacterizationPreflightRefusesOnAnUnintegratedBranch)$'
  ./internal/cli ./internal/daemon` exited 0. The four operational-command
  subtests, helper process, built-binary cache comparisons, per-test override,
  and Daemon characterization all passed.

### 6

```yaml
inputs:
  - kind: repository_path
    ref: internal/agent/acpx_runner_test.go
```

- Entry point: `go test -c ./internal/agent`, followed by the compiled binary
  invoked through a relative path.
- Steps: compile outside the package runner, change to the binary directory,
  and select the relative and absolute adapter tests.
- Expected observable: both tests execute the symlinked fixture and observe its
  configured output.
- Independent confirmation: require both named tests in verbose pass output.
- Persistence check: the relative child creates and executes the adapter from a
  separate temporary directory after the parent binary has started.
- Evidence: `go test -c -o /private/tmp/roundfix-0171-agent.test
  ./internal/agent` exited 0. From `internal/agent`, invoking that compiled
  binary through the relative path
  `../../../../../../../../private/tmp/roundfix-0171-agent.test` selected both
  adapter tests; each passed and the suite guard confirmed no repository
  mutation. An earlier launch from `/private/tmp` never reached the tests
  because that working directory breaks the package TestMain's `../..`
  repository-root contract; rerunning from the package directory corrected the
  QA setup without changing the build.

### 7

```yaml
inputs:
  - kind: live_service
    ref: https://pkg.go.dev/testing#T.Deadline
  - kind: external_repository
    ref: prior Roundfix Run QA reports named by the adopted Backlog Entry
```

- Entry point: current Go API documentation and the original QA reports for
  Runs `run_20260925T153433Z_8603eb3e79157622`,
  `run_20260925T155346Z_b801d0ee9b2a247a`, and
  `run_20260924T223414Z_54e6f85ae98b6550`.
- Steps: resolve Go's stated `testing.T.Deadline` semantics and locate the four
  failure records in the named Run reports.
- Expected observable: Go defines the deadline as the time the test binary will
  exceed its timeout, and the reports contain the four failures adopted by the
  Spec.
- Independent confirmation: name the exact source location for each observed
  claim, not the Spec-authored reference that summarizes it.
- Persistence check: re-open each external source during this run.
- Evidence: Context7 resolved the official Go documentation as
  `/websites/go_dev_doc`, and `go doc testing.T.Deadline` independently reported
  that the method returns the time at which the test binary will have exceeded
  the `-timeout` flag; `ok` is false only for `-timeout=0`. Sources:
  <https://pkg.go.dev/testing#T.Deadline> and <https://go.dev/doc/go1.15>. The
  retained Run artifacts independently prove three failures:

  - `/Users/marcio/.roundfix/artifacts/339f8dac2b687a04/runs/run_20260925T153433Z_8603eb3e79157622/verification/batch-002-attempt-1.log:38-39`
    records the queued-cancellation test failing after 91.57 s with one of two
    Agent starts.
  - `/Users/marcio/.roundfix/artifacts/339f8dac2b687a04/runs/run_20260925T155346Z_b801d0ee9b2a247a/verification/batch-001-attempt-1.log:36-37`
    records the concurrent-bootstrap test's Clean-exit failure.
  - `/Users/marcio/.roundfix/artifacts/95e45ae2977a5c2d/runs/run_20260924T223414Z_54e6f85ae98b6550/verification/batch-006-attempt-1.log:12-13`
    records the queued Verification-capacity test starting two commands.

  No retained Run verification log, console log, QA report, current project
  document outside this Spec, or artifact search result contains the claimed
  documentation-only failure of `TestBootstrapSerializesAcrossSiblings`.
  Because the fourth originating source cannot be obtained and Spec-authored
  prose cannot substitute for outside evidence, the row is environment-blocked.

### 8

```yaml
inputs:
  - kind: repository_path
    ref: internal/**
  - kind: repository_path
    ref: Makefile
  - kind: repository_path
    ref: docs/references/coverage-record.json
```

- Entry point: Git diff from the Spec target branch to the audited head.
- Steps: enumerate actual changed paths and reject production files,
  `internal/cli/cli_test.go`, `docs/references/coverage-record.json`, and the
  Makefile.
- Expected observable: only the Task-declared test/helper and Spec paths
  changed.
- Independent confirmation: compare both the three-dot changed-path set and the
  commit-level path lists for the integrated Task commits.
- Persistence check: resolve the target branch and audited head again at
  execution time.
- Evidence: `git diff --name-status
  roundfix/deliver-0171-a-deterministic-suite-under-load-eb5d1ed509eacdb3...HEAD`
  listed only Tasks 01-05, the eight declared test/helper files, and no
  production surface. `git log --name-only` tied those paths to the five Task
  commits. A focused `git diff --quiet` for `Makefile`,
  `internal/cli/cli_test.go`, and `docs/references/coverage-record.json` exited
  0. The sole non-`_test.go` Go addition is `internal/testwait/testwait.go`; a
  repository import search found it only in `_test.go` files.

### 9

```yaml
inputs:
  - kind: repository_path
    ref: docs/specs/0171-a-deterministic-suite-under-load/_prd.md
  - kind: repository_path
    ref: docs/specs/0171-a-deterministic-suite-under-load/_techspec.md
  - kind: repository_path
    ref: docs/specs/0171-a-deterministic-suite-under-load/_tasks.md
  - kind: repository_path
    ref: docs/specs/0171-a-deterministic-suite-under-load/task_*.md
```

- Entry point: the strict Spec checker and focused citation/vocabulary
  commands.
- Steps: require the checker to pass, confirm every numbered Success Metric and
  API Contract is named by a consuming Task, and confirm the change emits no
  new user-facing vocabulary requiring a Vocabulary Contract.
- Expected observable: no promise is unconsumed and no changed production
  surface emits vocabulary.
- Independent confirmation: compare the checker result with the Task References
  and Git changed paths through commands, not a file-read-only judgment.
- Persistence check: rerun the focused commands against the audited head.
- Evidence: the strict Spec check exited 0 with no findings. A focused `rg` over
  Task References showed Success Metrics 1-2 consumed by Tasks 02 and 03,
  Metric 3 and API Contracts 1-2 by Task 01, Metric 4 and API Contract 3 by Task
  04, and Metric 5 by Task 05; Task 06 also names the complete sets. Git evidence
  from row 8 proves no production surface changed, and the only new ordinary Go
  package has no non-test caller, so this Spec emits no user-facing vocabulary
  for the skipped vocabulary detector to document.

### 10

```yaml
inputs:
  - kind: live_service
    ref: Pull Request for roundfix/deliver-0171-a-deterministic-suite-under-load-eb5d1ed509eacdb3
  - kind: repository_path
    ref: "**"
```

- Entry point: Pull Request controls on the Spec target branch.
- Steps: record the environment cause and equivalent evidence for approval,
  checks and status, unresolved review threads, Merge-Ready acceptance, and
  review-artifact ancestry.
- Expected observable: every control has an observed or supervised answer.
- Independent confirmation: tie all repository-local evidence to the audited
  head.
- Persistence check: compare evidence ancestry to the current head.
- Evidence: the assignment states that no Pull Request is open for the Spec
  target branch, so the live journey is unreachable. Equivalent controls are
  fully answered: the maintainer authorized the implementation scope on
  2026-09-25, but no PR acceptance has been obtained; the Daemon's current-head
  `make verify-changed` passed; no PR exists to hold unresolved threads; no
  Merge-Ready acceptance exists yet; and a repository search found no review
  artifact tied to `5fdaa3bf342f9578711d53767c00b9107aa436d2`, so there is
  no review-artifact ancestry to claim. The expected pre-PR review and Pull
  Request controls remain delivery-stage work.

## Findings

### F-001 — Concurrent bootstrap stress still fails under load

- Impact: Blocks-Completion.
- Actor and journey step: maintainer, row 4's count-10 stress while the rest of
  the package suite runs beside it.
- Expected: all four historical regressions pass every iteration at CPU settings
  1 and 4.
- Actual: one of 20 scheduler runs of
  `TestRunImplementBootstrapsEachConcurrentTaskWorktreeBeforeAgentWork` returned
  exit 1. The Implement command could not create `task_01`'s worktree because
  Git reported `fatal: failed to read
  .git/worktrees/<task_02>/commondir: Result too large`.
- Reproduction: run the row 4 focused command concurrently with the adjacent
  `go test -count=1 ./...` command recorded above.
- Evidence: focused command exit 1; failing test duration 2.81 s; affected row
  4 only. The precise lower-level cause of Git's `Result too large` under this
  concurrency remains unknown.

## Blocked and skipped

- Row 7 — `blocked (environment: fourth originating report unavailable)`. The
  three named Run logs and Go documentation are reachable, but the retained
  artifact store does not contain the documentation-only
  `TestBootstrapSerializesAcrossSiblings` failure. Unblock by supplying its
  original verification/QA report or correcting Requirement 6's source
  provenance to an existing outside artifact; Spec-authored summaries are not
  eligible substitutes.
- Row 10 — `blocked (environment: no open Pull Request)`. Equivalent evidence
  above answers every PR control. Unblock at delivery by completing the
  configured pre-PR review, opening the PR from the Spec target branch, and
  observing approval, checks, threads, Merge-Ready acceptance, and ancestry on
  that PR.

## Coverage

Ten rows cover all eight declared verification Requirements plus the three
non-waivable source classes present here: outside evidence, Pull Request
controls, and repository Verification. Requirements 1 and 6 coincide with two
of those non-waivable sources. Seven rows passed, one failed, two were
environment-blocked, and none were finding-blocked, declared-blocked, or
skipped. No row covers PRD stories, Goals, non-QA Task criteria, or a frontend
sweep because ADR-0155 bounds this declared matrix and the PRD declares no
frontend.

## Mechanical skips

| Detector | Missing artifact |
| --- | --- |
| authorization bounded paths | tooling authorization |
| consequent-fix commit order | consequent-fix declaration |
| QA Report structure | QA Report |
| QA evidence paths | QA Report |

## Final verdict

Fail: the concurrent count-10 stress reproduced a Clean-exit failure in the
Implement bootstrap test. Before rerunning the gate, repair F-001 and supply or
correct the unavailable fourth outside-evidence source; the PR row already has
equivalent evidence for this pre-PR stage. Final counts: 7 pass, 1 fail, 2
environment-blocked, 0 finding-blocked, 0 declared-blocked, 0 skipped; findings:
1 Blocks-Completion, 0 Data-Loss, 0 Trust-Damage, 0 Friction, 0 Cosmetic.
