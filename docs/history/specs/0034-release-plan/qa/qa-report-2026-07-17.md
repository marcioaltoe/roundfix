---
spec: 0034-release-plan
date: 2026-07-17
build: "25173eb-dirty"
status: closed
verdict: pass
surfaces: [backend, cli, docs]
---

# QA report — Release Plan

## Scope and environment

Full QA gate for Spec `0034-release-plan` against the local Roundfix CLI in this repository. The gate validates the PRD user stories, core features, success metrics, release guidance, Non-Goals, and all completed task acceptance criteria through the real `roundfix release plan` command, real local Git repositories, documented guidance, and repository verification.

- Repository: `/Users/marcio/.roundfix/worktrees/roundfix-339f8dac/run_20260717T034621Z_5f84d6a9aa6c41a6`
- Build under test: `bin/roundfix`, produced by `rtk make verify` with `BuildCommit=25173eb-dirty`
- QA evidence directory: `docs/specs/0034-release-plan/qa/evidence/2026-07-17-release-plan/`
- Task precondition: `task_01` through `task_05` are `completed`, and each task file carries a `## Result` section.

## Static gate

| Command | Status | Evidence |
| --- | --- | --- |
| `rtk make verify` | pass | `docs/specs/0034-release-plan/qa/evidence/2026-07-17-release-plan/make-verify.txt` shows setup-context tests passing, `rtk go test ./...` with 1443 passing tests in 20 packages, `roundfix skills check` passing, and `go build` producing `bin/roundfix`. |

## Results

| # | Story / criterion / sweep | Actor and surface | Status | Evidence |
| - | --- | --- | --- | --- |
| 1 | Static gate passes on the assembled feature. | Maintainer / backend, cli, docs | pass | `make-verify.txt` records the full gate passing. |
| 2 | User Story 1 and Core Feature 1: committed release range reports base, target, classified changes, highest impact, and proposed version. | Maintainer / CLI | pass | `cli-flow-results.json` includes `base`, `target`, `classification`, `proposedVersion`, and per-commit `changes` for release ranges. |
| 3 | Success metric: fix-only delta from `v0.4.0` proposes `v0.4.1` with state `ready` and no approval requirement. | Maintainer / CLI | pass | Flow `patch-ready` exits `0`, state `ready`, proposed `v0.4.1`, approval not required. |
| 4 | Success metric: compatible feature from `v0.4.1` proposes `v0.5.0`, state `approval_required`, and exact minor approval question. | Maintainer / CLI | pass | Flow `minor-approval-text` exits `3`; `cli-outputs/minor-approval-text.stdout.txt` starts with `Decision: approval_required` and prints `Approve the minor increment to v0.5.0?`. |
| 5 | Success metric: breaking delta from `v1.4.2` proposes `v2.0.0`, state `approval_required`, and exact major approval question. | Maintainer / CLI | pass | Flow `major-breaking` exits `3`, state `approval_required`, proposed `v2.0.0`, question `Approve the major increment to v2.0.0?`. |
| 6 | Success metric: version-zero breaking delta proposes the next minor version, labels breaking, and requires approval. | Maintainer / CLI | pass | Flow `version-zero-breaking` exits `3`, marks breaking, proposes `v0.8.0`, and asks `Approve the minor increment to v0.8.0?`. |
| 7 | Core Features 2-3: breaking outranks feature, feature outranks fix, fix outranks no-release, independent of commit order. | Maintainer / CLI | pass | Flows `mixed-fix-feature` and `mixed-feature-fix` both select impact `minor` and propose `v0.5.0`; `major-breaking` outranks feature/fix; `no-release` remains below patch. |
| 8 | User Story 5 and Core Feature 4: documentation-, test-, fixture-, planning-, and CI-only committed changes produce `no_release`. | Maintainer / CLI | pass | Flow `no-release` exits `0`, state `no_release`, impact `none`, and has no `proposedVersion`. |
| 9 | User Story 3 and Core Feature 4: ambiguous shipped-surface changes return `manual_classification_required`, identify blocking commits, and show the manual rerun shape. | Maintainer / CLI | pass | Flow `manual-required` exits `3`; `cli-outputs/manual-required.stdout.txt` lists blocking commits and `--impact <none|patch|minor|major> --reason <text>`. |
| 10 | Manual classification: `--impact` plus non-empty `--reason` records the reason, participates in max-impact selection, and does not approve a resulting minor or breaking version. | Maintainer / CLI | pass | Flow `manual-minor` records `manualReason`, selects proposed `v0.5.0`, exits `3`, and still requires approval. Flow `manual-downgrade` rejects impact below the automatic minimum with exit `2` and no stdout plan. |
| 11 | User Story 4 and Core Feature 8: JSON output is one `roundfix.release-plan/v1` object with evidence for every commit, stdout/stderr isolation, and exit `0`/`2`/`3` mapping. | Automation caller / CLI | pass | `cli-flow-results.json` validates schema `roundfix.release-plan/v1`, per-commit evidence, empty stderr for JSON plans, and exit codes across ready, no-release, approval, manual-required, dirty, invalid, and downgrade flows. |
| 12 | Invalid input and dirty repository states emit no partial plan, return exit `2`, name actionable next steps, and block classification. | Automation caller / CLI | pass | Flows `dirty-tree`, `pre-release-base`, and `manual-downgrade` exit `2`, write no stdout plan, and write actionable stderr diagnostics naming paths or invalid classification. |
| 13 | Core Feature 7 and Non-Goals: every successful or failing Release Plan command leaves files, refs, tags, remotes, config, packages, releases, and Run state unchanged. | Maintainer / CLI | pass | Every `cli-flow-results.json` flow has `repoUnchanged: true` with file, refs, status, config, and remotes hashes. Flow `invalid-roundfix-config` confirms no `.roundfix` state directory was created. |
| 14 | Non-Goals: pre-release release bases are not supported by the first Release Plan release. | Maintainer / CLI | pass | Flow `pre-release-base` exits `2`, emits no stdout plan, and reports that pre-release tags are unsupported. |
| 15 | User Story 2 and Core Features 6, 9: release guidance starts with `roundfix release plan`, authorizes only conclusive patch plans generically, and gates minor, major, version-zero breaking, and manual classification outcomes. | Agent / docs | pass | `docs-command-grep.txt` and `docs-boundary-grep.txt` show the runbook, command index, root Agent pointer, and both Roundfix skill copies require the Release Plan Command and document approval boundaries. |
| 16 | Existing publication workflow remains intact after approval: tag validation, artifact version agreement, npm publication, GitHub Release assets, and Upgrade Command asset naming are preserved. | Maintainer / docs, cli | pass | `docs-boundary-grep.txt` shows retained publication terms; `docs-contract-go-test.txt` records `TestSelectPlatformAssetResolvesReleaseNamingScheme` passing alongside the documentation contract. |
| 17 | Command discoverability: root help and `release plan --help` describe command shape, flags, states, exit codes, defaults, and read-only boundary truthfully. | Automation caller / CLI | pass | `help-root.txt` lists `roundfix release plan`; `help-release-plan.txt` lists `--from`, `--to`, `--impact`, `--reason`, `--format`, states, exit codes, defaults, and read-only guarantees. |
| 18 | Non-Goal sweep: Release Plan does not create/push tags, edit changelog or package files, publish packages, upload assets, create GitHub Releases, read Roundfix config, open the Run Database, contact a network service, decide `1.0.0` readiness, or replace the release workflow. | Maintainer / CLI, docs | pass | `cli-flow-results.json` mutation hashes remain unchanged for all flows; `invalid-roundfix-config` ignores invalid Roundfix config and creates no Run state; docs evidence preserves post-approval publication workflow instead of replacing it. |

## Evidence inventory

- `docs/specs/0034-release-plan/qa/evidence/2026-07-17-release-plan/run_cli_flows.py` — executable QA harness for public CLI probes against real local Git repositories.
- `docs/specs/0034-release-plan/qa/evidence/2026-07-17-release-plan/cli-flow-results.json` — structured results for 14 Release Plan flows.
- `docs/specs/0034-release-plan/qa/evidence/2026-07-17-release-plan/cli-flow-summary.txt` — compact flow outcome summary.
- `docs/specs/0034-release-plan/qa/evidence/2026-07-17-release-plan/cli-outputs/` — captured stdout and stderr for each CLI flow.
- `docs/specs/0034-release-plan/qa/evidence/2026-07-17-release-plan/help-root.txt` — captured root help.
- `docs/specs/0034-release-plan/qa/evidence/2026-07-17-release-plan/help-release-plan.txt` — captured `release plan --help`.
- `docs/specs/0034-release-plan/qa/evidence/2026-07-17-release-plan/docs-command-grep.txt` — durable guidance surfaces naming `roundfix release plan`.
- `docs/specs/0034-release-plan/qa/evidence/2026-07-17-release-plan/docs-boundary-grep.txt` — approval-boundary and publication-workflow documentation sweep.
- `docs/specs/0034-release-plan/qa/evidence/2026-07-17-release-plan/skill-cmp.txt` — canonical and embedded Roundfix skills are byte-identical.
- `docs/specs/0034-release-plan/qa/evidence/2026-07-17-release-plan/skills-sync-check.txt` — `make skills-sync-check` passed.
- `docs/specs/0034-release-plan/qa/evidence/2026-07-17-release-plan/docs-contract-go-test.txt` — focused documentation and release asset compatibility tests passed.
- `docs/specs/0034-release-plan/qa/evidence/2026-07-17-release-plan/make-verify.txt` — final full verification gate passed.

## Findings

None.

## Blocked and skipped

None.

## Coverage

- PRD user stories covered: 5 / 5.
- PRD core features covered: 9 / 9.
- PRD success metrics covered: all declared fix-only, feature, breaking, ambiguous, read-only, documentation, and automation metrics.
- PRD Non-Goals covered: all declared release mutation, publication, network, Run Database, Roundfix configuration, pre-release, and `1.0.0` readiness exclusions.
- Task acceptance criteria covered: `task_01` through `task_05` all have passing task evidence and are revalidated through the static gate, CLI probes, docs contract, help capture, and skill sync checks.

## Final verdict

Pass. Every PRD story, acceptance criterion, task acceptance criterion, documented Non-Goal, and verification gate passed against the real CLI and repository documentation.
