---
spec: 0158-daemon-verification-and-access-readiness
date: 2026-09-24
build: 256fe7c347dc6aea7501b4603e35433f0ceb4bad
auditing_binary: "0.16.0 (e38c8561, built 2026-09-24 08:07:17 -0300)"
auditor_staleness: "current: commit ancestry: build commit does not predate audited tree"
status: closed
verdict: pass
rows_blocked_environment: 1
rows_blocked_finding: 0
rows_blocked_declared: 0
surfaces: [backend, cli, docs]
---

# QA Report

## Scope and environment

- Scope: full authored terminal gate for Task 05 on build
  `256fe7c347dc6aea7501b4603e35433f0ceb4bad`.
- Actors: a local Roundfix operator, the Daemon, a Spec author, and a reviewer.
- Entry points: focused public-contract Go runners, the public
  `roundfix spec check` command, local Git reads, documentation checks, and the
  daemon-owned repository Verification fact.
- Authoring precondition: `roundfix spec check
  0158-daemon-verification-and-access-readiness --strict` exited 0 with no
  findings. Authored Verification commands were not executed by that check.
- Pull Request: none open; Pull Request journeys are environment-blocked.
- Repository Verification: daemon-owned; the supplied result is `make verify`
  exit 0 with diagnostics removed on success. This gate will not rerun it.
- Parity: focused Go runners execute the same Daemon and CLI boundaries used by
  an Implement Run. No live adapter request, network call, credential, commit,
  push, or Pull Request mutation is authorized for this gate.

## Performed repairs

None.

## Assigned repair failures

None.

## Authorization audit inputs

None.

## Mechanical findings

None.

## Static gate

- `make verify` — pass (exit 0), run once by the Daemon outside the Agent
  sandbox; diagnostics were removed on success. Per the QA contract, the Agent
  did not rerun it.
- `roundfix spec check 0158-daemon-verification-and-access-readiness --strict`
  — pass (exit 0), no findings. The checker disclosed four skipped detectors:
  no findings rollup, no backlog, no TechSpec Vocabulary Contract, and no
  `references/_index.md`.

## Execution log

- `make verify` — pass (exit 0), supplied by the Daemon; not rerun by the
  Agent.
- `roundfix spec check 0158-daemon-verification-and-access-readiness --strict`
  — pass three times (exit 0), including once through the current-tree build;
  no findings, and authored Verification commands were not executed.
- `GOCACHE=/private/tmp/roundfix-0158-qa-gocache go test -count=1 -v -run
  '^(TestTaskParsesIndependentVerification|TestIndependentVerificationHandsEveryFailureToRepair|TestUndeclaredVerificationStopsAtFirstFailure)$'
  ./internal/spec ./internal/daemon` — pass (exit 0).
- `GOCACHE=/private/tmp/roundfix-0158-qa-gocache go test -count=1 -v -run
  '^(TestAuthorizationParsesPreconditionRepairs|TestNamedTaskRepairsKnownRedPrecondition|TestUnnamedTaskStaysBlockedByRedPrecondition|TestPreconditionRepairWithoutConfiguredCommandIsRefused|TestUnknownPreconditionRepairTaskIsRefused)$'
  ./internal/authorization ./internal/daemon ./internal/cli` — pass (exit 0).
- The sandboxed access-readiness command could not start its CLI cases because
  `api.github.com` was blocked. The same focused command in the full-access
  test environment passed (exit 0):
  `GOCACHE=/private/tmp/roundfix-0158-qa-gocache go test -count=1 -v -run
  '^(TestRuntimeReportsAccessPolicySupport|TestDisposableProofAppliesRequestedAccessMode|TestProfilePreflightRefusesUnhonouredAccessPolicy)$'
  ./internal/agent ./internal/cli`.
- `GOCACHE=/private/tmp/roundfix-0158-qa-gocache go test -count=1 -v -run
  '^TestImplementTaskContentChoosesVerificationByTaskType/supported_policy_is_proven_for_preferred_and_fallback_selections$'
  ./internal/cli` — pass (exit 0) in the full-access test environment.
- Task-aligned accepted-status, declaration, and configuration-guide assertions
  — pass (exit 0). One exploratory chain that additionally required literal
  internal predicate identifiers exited 1; the guide promises a named failed
  predicate and remedy, not those internal spellings.
- `git merge-base --is-ancestor c1ee13f9 99c9741b`, commit identity reads, and
  `git blame -L 6,10 -- .roundfixrc.yml` — pass (exit 0); the external
  measurement predates Spec 0158.
- `GOCACHE=/private/tmp/roundfix-0158-qa-gocache go build -o
  /private/tmp/roundfix-0158-qa ./cmd/roundfix` — pass. The built command
  reported `roundfix 0.16.0`, and its strict Spec check exited 0 with no
  findings.
- Pull Request equivalent-evidence reads proved authorization commit
  `99c9741b` is an ancestor of audited head `256fe7c3` and that no Spec review
  artifact tree exists.

## Results

| # | Status | Provenance |
| - | --- | --- |
| R1 | pass | Requirement 1; repository Verification |
| R2 | pass | Requirement 2 |
| R3 | pass | Requirement 3 |
| R4 | pass | Requirement 4 |
| R5 | pass | Requirement 5 |
| R6 | pass | Requirement 6 |
| R7 | pass | outside-evidence row |
| R8 | blocked (environment: no open Pull Request) | Pull Request row |

## Row evidence

### R1

```yaml
inputs:
  - kind: repository_path
    ref: "**"
```

- Actor and surface: daemon; repository gate.
- Entry point and steps: run `make verify` once for the audited tree.
- Expected observable: exit 0 and no retained success diagnostics.
- Independent confirmation: Daemon settlement supplies the exit code and
  diagnostic disposition.
- Persistence check: not applicable to a static gate.
- Evidence path: daemon-owned gate fact in this report.
- Result: pass. The supplied daemon fact records the exact command, exit 0,
  and successful diagnostic disposition for this audited tree.

### R2

```yaml
inputs:
  - kind: repository_path
    ref: internal/spec/task.go
  - kind: repository_path
    ref: internal/spec/task_test.go
  - kind: repository_path
    ref: internal/daemon/engine.go
  - kind: repository_path
    ref: internal/daemon/task_engine.go
  - kind: repository_path
    ref: internal/daemon/task_engine_test.go
```

- Actor and surface: Task author and Daemon; Task declaration and Verification
  Feedback boundary.
- Entry point and steps: execute the focused public-contract cases for a
  declared independent Task and an undeclared Task.
- Expected observable: the declared Task runs all three commands and hands both
  deterministic failures to its one repair turn; the undeclared Task stops at
  the first failure.
- Independent confirmation: inspect the exact named test cases and their
  command-count and feedback assertions.
- Persistence check: rerun the focused cases with a fresh Go test process.
- Evidence path: focused command output recorded in this report.
- Result: pass. A fresh focused process passed all three named cases. The
  declared fixture ran `verify first`, `verify second`, and `verify third` on
  both attempts; failures from commands one and three carried distinct
  diagnostic paths into the single repair prompt and both published Daemon
  Verification events. The undeclared fixture ran only `verify first` on each
  bounded attempt and used exactly one repair turn. Parser subcases also proved
  the declaration is recorded and `sequential-groups` is refused by name.

### R3

```yaml
inputs:
  - kind: repository_path
    ref: internal/authorization/authorization.go
  - kind: repository_path
    ref: internal/authorization/authorization_test.go
  - kind: repository_path
    ref: internal/daemon/task_engine.go
  - kind: repository_path
    ref: internal/daemon/task_engine_test.go
  - kind: repository_path
    ref: internal/cli/implement.go
  - kind: repository_path
    ref: internal/cli/implement_test.go
```

- Actor and surface: local operator; Implement planning, Daemon precondition,
  and Task settlement boundaries.
- Entry point and steps: execute the focused named, unnamed, frozen-record, and
  missing-verbatim-command cases around a red repository command.
- Expected observable: only the named Task enters, it settles only after the
  gate passes, the unnamed Task stays blocked, and the malformed grant is
  refused before Run creation.
- Independent confirmation: assert the Agent-turn count, Daemon Verification
  events, final Task state, refusal message, and absent Run artifacts.
- Persistence check: rerun the focused cases with fresh fixtures.
- Evidence path: focused command output recorded in this report.
- Result: pass. Five focused cases passed. The named Task observed a red
  `make verify` entry, received one Agent turn after the Run Worktree's
  authorization was stripped, reran `focused check` and `make verify`, and
  settled completed. The unnamed Task ran only the entry gate, started no
  Agent Session, published the precondition classification, and settled failed
  with `repository not green on entry`. Planning refused both a whitespace-only
  near-match for the configured command and an unknown Task identifier before
  creating a Run.

### R4

```yaml
inputs:
  - kind: repository_path
    ref: internal/agent/agent.go
  - kind: repository_path
    ref: internal/agent/agent_test.go
  - kind: repository_path
    ref: internal/agent/acpx_runner.go
  - kind: repository_path
    ref: internal/agent/acpx_runner_test.go
  - kind: repository_path
    ref: internal/cli/profiles_validate.go
  - kind: repository_path
    ref: internal/cli/implement_test.go
```

- Actor and surface: local operator; profile readiness and Implement preflight.
- Entry point and steps: execute the focused unsupported-runtime,
  adapter-refusal, and supported-mode cases for requested full access.
- Expected observable: unsupported or refused access fails readiness before a
  Run exists; supported access is applied and recorded as `full-access` for
  preferred and fallback selections.
- Independent confirmation: assert the mode call, failed predicate, remedy,
  effective policy, and absent Run Database, Worktree, and Agent work.
- Persistence check: rerun the focused cases with fresh disposable sessions.
- Evidence path: focused command output recorded in this report.
- Result: pass. The sandboxed attempt was prevented from starting by blocked
  access to `api.github.com`; the same focused command then passed in the
  full-access test environment. Codex and Claude reported their full-access
  modes, OpenCode reported the requested mode unavailable, and runtime-default
  access needed no mode. The disposable proof issued one
  `set-mode bypassPermissions`, recorded `full-access`, closed the session, and
  classified an adapter refusal at `adapter_accepts_access_mode`. Implement
  preflight refused both unavailable and rejected modes with the exact
  predicate and remedy while creating no Run Database, Run Worktree, stdout,
  or Agent work. The companion supported case recorded `full-access` for every
  preferred and fallback proof.

### R5

```yaml
inputs:
  - kind: repository_path
    ref: docs/adr/0159-verification-runs-past-a-failure-only-when-declared-independent.md
  - kind: repository_path
    ref: docs/adr/0160-only-the-frozen-authorization-opens-a-red-repository-gate.md
  - kind: repository_path
    ref: docs/user-guide/configuration.md
```

- Actor and surface: maintainer; ADRs and configuration guide.
- Entry point and steps: run assertions over accepted decision status, the
  declared-independence and frozen-authorization contracts, and the documented
  readiness refusal.
- Expected observable: both ADRs are accepted and the guide names
  `agent_full_access`, readiness, pre-Run refusal, the failed predicate, and
  the remedy.
- Independent confirmation: the strict Spec check reports no unsupported
  citation or promise finding.
- Persistence check: repeat the checks against the audited commit.
- Evidence path: focused command output recorded in this report.
- Result: pass. Task-aligned assertions exited 0: both ADRs carry accepted
  status, ADR-0159 names `verification: independent`, ADR-0160 names
  `precondition_repairs`, and the guide states that requested full access is
  applied during profile readiness and refused before Run creation with the
  failed predicate and the remedy to disable full access or select a supporting
  runtime. An exploratory assertion for literal internal predicate identifiers
  exited 1 because the user guide intentionally describes the predicate rather
  than exposing code identifiers; no acceptance criterion requires those
  literals.

### R6

```yaml
inputs:
  - kind: repository_path
    ref: docs/specs/0158-daemon-verification-and-access-readiness/_prd.md
  - kind: repository_path
    ref: docs/specs/0158-daemon-verification-and-access-readiness/_techspec.md
  - kind: repository_path
    ref: docs/specs/0158-daemon-verification-and-access-readiness/_tasks.md
  - kind: repository_path
    ref: docs/specs/0158-daemon-verification-and-access-readiness/task_05.md
```

- Actor and surface: Spec author; public `roundfix spec check` command.
- Entry point and steps: run the strict consistency check over this Spec's
  explicit promise mappings and consuming Task references.
- Expected observable: no promise-rule or coverage finding.
- Independent confirmation: inspect the checker output and explicit Task 05
  references at the audited commit.
- Persistence check: rerun the read-only command against the same build.
- Evidence path: strict-check output recorded in this report.
- Result: pass. The public strict checker exited 0 three times, including once
  through the current-tree build, with `No findings` and explicitly stated that it did not execute
  authored Verification commands. Task 05's references name PRD Goals 1-3,
  Core Features 1-3, Success Metrics 1-3, Acceptance evidence, TechSpec Testing
  Approach 1-4, and API Contracts 1-3 as the consumed promises. The checker
  reported only four inapplicable missing-artifact skips, not a promise or
  coverage finding.

### R7

```yaml
inputs:
  - kind: repository_path
    ref: .roundfixrc.yml
```

- Actor and surface: maintainer; pre-Spec access-policy measurement recorded in
  repository configuration and its Git history.
- Entry point and steps: establish the measured codex-acp refusal, its date,
  and its provenance before Spec 0158 was authored.
- Expected observable: independent evidence shows that requested full access
  reached `session/set_mode`, returned ACP `-32602`, and failed Tasks before
  Agent work.
- Independent confirmation: compare the blamed measurement commit with the
  Spec authoring commit and the fresh R4 behavior evidence.
- Persistence check: repeat the read-only blame and ancestry checks.
- Evidence path: focused Git output recorded in this report.
- Result: pass. The source is `.roundfixrc.yml` lines 6-10, a measured incident
  record that Spec 0158 did not author: codex-acp 1.1.9 returned ACP `-32602`
  from `session/set_mode "full-access"` on 2026-08-04 and failed every Task
  before Agent work. `git blame` attributes the complete record to commit
  `c1ee13f9c74a40435314d33fcd7b4ba93067a409` dated 2026-08-05, and
  `git merge-base --is-ancestor c1ee13f9 99c9741b` exited 0, proving it predates
  the 2026-09-24 Spec authoring commit. R4 independently reproduced the
  adapter-refusal classification and the pre-Run no-artifact boundary on the
  audited build. Git emitted a non-blocking fsmonitor IPC warning while the
  blame still completed with exit 0 and complete provenance.

### R8

```yaml
inputs:
  - kind: live_service
    ref: Pull Request state for feat/0158-daemon-verification-and-access
```

- Actor and surface: reviewer; Pull Request controls.
- Entry point and steps: observe approval, checks/status, unresolved threads,
  Merge-Ready acceptance, and review-artifact ancestry.
- Expected observable: all five controls are explicitly accounted for.
- Independent confirmation: compare each available artifact with the claimed
  head.
- Persistence check: fresh Pull Request read when one exists.
- Evidence path: supplied environment fact and equivalent evidence recorded in
  this report.
- Result: blocked (environment: no open Pull Request), with equivalent evidence
  for every control:
  - approval: authorization commit `99c9741b` records maintainer approval on
    2026-09-24 for the Spec's implementation and delivery operations; it is an
    ancestor of audited head `256fe7c3`. No Pull Request acceptance exists yet;
  - checks and status: daemon-owned `make verify` exited 0;
  - unresolved review threads: no Pull Request exists, so no thread set exists
    to leave unresolved;
  - Merge-Ready acceptance: no Review Source evidence exists yet;
  - review-artifact ancestry: the Spec has no `reviews/` artifact tree and no
    Pull Request head, so no ancestry claim was made.
  The unblocking action is to open the Pull Request on
  `feat/0158-daemon-verification-and-access` after this gate settles, then
  observe all five controls against the Pull Request's actual head.

## Findings

None.

## Blocked and skipped

- R8 is environment-blocked because the QA prompt proves that no Pull Request
  is open. All five Pull Request controls carry explicit equivalent evidence in
  the row. No row is finding-blocked, declared-blocked, or skipped.

## Coverage

Complete: seven rows passed and one row is environment-blocked with equivalent
evidence. All six declared verification Requirements and the mandatory
outside-evidence, Pull Request, and repository Verification sources appear in
provenance; no row covers another source. Requirement 7 constrains the evidence
for every row and is not a declared verification source. The PRD declares no
Unreachable Acceptance and no frontend surface. Counts:
`rows_blocked_environment: 1`, `rows_blocked_finding: 0`,
`rows_blocked_declared: 0`; failed: 0; skipped: 0.

## Mechanical skips

| Detector | Missing artifact |
| --- | --- |
| authorization bounded paths | tooling authorization |
| consequent-fix commit order | consequent-fix declaration |
| QA Report structure | QA Report |
| QA evidence paths | QA Report |

These pre-Agent mechanical skips are preserved exactly as seeded; the Agent
did not reinterpret or remove them after materializing this report.

## Final verdict

Pass. Every runnable row passed; the sole environment-blocked Pull Request row
records its cause, unblocking action, and equivalent evidence for all five
controls. There are no findings, declared blocks, finding blocks, skipped rows,
or unresolved evidence paths.
