---
spec: 0105-the-gates-own-economics
date: 2026-08-31
build: 2ba8ea560dc99b87ec19a81eb6d71f7073edf39c
auditing_binary: "0.10.0 (2ba8ea56)"
auditor_staleness: "current: binary build commit is the audited HEAD"
status: closed
verdict: pass
rows_blocked_environment: 1
rows_blocked_finding: 0
rows_blocked_declared: 0
surfaces: [backend, docs]
---

# QA report — The gate's own economics

## Scope and environment

Full authored-gate scope. The actor is a Roundfix Supervisor using the CLI and
shipped workflow skills from Run Worktree head
`2ba8ea560dc99b87ec19a81eb6d71f7073edf39c`. The current-head binary is
`/private/tmp/roundfix-qa-0105/roundfix`, built with commit identity
`2ba8ea56`. No credentials are required. Pull Request journeys are
environment-blocked because the prompt confirms no Pull Request is open; R07
records equivalent local evidence.

The precondition command
`roundfix spec check 0105-the-gates-own-economics --strict` passed with no
findings. It skipped the vocabulary-documentation detector because the
TechSpec has no Vocabulary Contract, and skipped `SC-REF-UNRESOLVED` because
the Spec has no `references/_index.md`; R10 and R01 retain those obligations.

## Performed repairs

None.

## Assigned repair failures

None.

## Mechanical findings

None.

## Static gate

`rtk make verify` first reached the complete Go suite but the managed sandbox
denied non-session process-table reads in two force-stop integration tests. The
focused reproduction failed identically at head and in a standalone unchanged
`origin/main` clone at `0a54d5dd`, both with `operation not permitted`; the Spec
does not change either test or its production process-tree code. The same exact
`rtk make verify` command was then rerun with process-table access and exited 0:
the full Go suite, skill sync/check, and final CLI build all passed. The sandbox
failure is an environment parity observation, not a blocked row.

## Results

| # | Story / criterion / sweep | Actor and surface | Status | Evidence |
| - | --- | --- | --- | --- |
| R01 | Authored graph and cited paths resolve despite the skipped reference detector | Supervisor · CLI/docs | pass | Strict check clean; all named guides, ADRs, and authorization paths are tracked; task_06 is terminal after completed task_08. |
| R02 | Roundfix derives QA Verification, selects only the newest valid report, accepts only `pass`, renders it, and refuses authored QA Verification | Supervisor · CLI/backend | pass | Focused four-package suite passed; current CLI refused a scratch authored command as `SC-QA-VERIFICATION-AUTHORED`. |
| R03 | Citation parser accepts the two conjunctions and obligations numbers, bounds bare numbers to that row, and names `ADR-NNNN` on failure | Spec author · CLI/backend | pass | Current CLI accepted English and Portuguese scratch Specs and rejected `ADR-29` with an `ADR-NNNN` fix; focused tests passed. |
| R04 | A finding blocks only named rows; unnamed rows remain measurable; naming every row still blocks all | Supervisor · backend | pass | Focused mechanical and Daemon tests passed for one-of-three and all-three row attribution. |
| R05 | A blocking machine fact before a matrix exists still withholds the Agent Session | Supervisor · backend | pass | `TestMechanicalStageWithholdsAgentSession` and precondition materialization tests passed. |
| R06 | Authoring and execution guidance require real-boundary characterization before dependent work | Spec author and Task Agent · docs | pass | Canonical skills state the trigger, real-boundary rule, fake prohibition, and dependency ordering; mirrors match. |
| R07 | A pre-PR gate records an environment block plus equivalent head, ancestry, changed-file, and local-check evidence | Supervisor · docs/CLI | blocked (environment: no open Pull Request) | Prompt cause plus equivalent evidence: head `2ba8ea56`, target `80983f05`, direct ancestry, complete changed-path list, clean strict check, and passing full Verification. |
| R08 | Skill edits stay within the grant, authorization precedes them, consequent repair follows its cause, and mirrors match sanctioned regeneration | Maintainer · Git/docs | pass | `git diff-tree` audited every Task commit; grant `634a644f` precedes skill commit `7f1a79ae`; consequent `2ba8ea56` follows the derived-command commits; all three pairs compare equal. |
| R09 | Outside evidence establishes the cost premise and counter-number independently of this Spec | Maintainer · docs | pass | Pre-Spec 2026-08-10/12 measurements name five repositories, Run Event Stream derivation, the vortex session, 123/201, six-of-eight, eleven contract failures, and four suite-missed defects. |
| R10 | Existing glossary terms cover the change and no competing term was introduced | Maintainer · docs | pass | Manual retained sweep found implementation continues to model Tasks, Task Types, QA Reports, verdict fields, and blocked counts under existing terms; characterization remains a Task obligation, not a new entity or Task Type. |
| R11 | Non-Goals remain intact | Maintainer · backend/docs | pass | Full changed-path and focused diff sweep found no gate reorder, typed-count contract change, data infrastructure, `internal/rounds` behavior, or cost-measurement feature; the review guardrail remains a backlog split. |
| R12 | Repository Verification passes on the audited tree | Maintainer · backend/docs | pass | Full-access `rtk make verify` exited 0 after the sandbox-only failure reproduced unchanged on `origin/main`. |
| R13 | Shipped skill mirrors and documentation commands work for their intended readers | Spec author, Task Agent, and QA Agent · docs | pass | `make skills-sync-check`, docs-contract tests, the full `skills` suite, and three byte comparisons passed. |

## Detailed evidence

### R01

```yaml
inputs:
  - kind: repository_path
    ref: docs/specs/0105-the-gates-own-economics/*.md
  - kind: repository_path
    ref: docs/adr/*.md
```

- Entry point and steps: load the Task Graph, every Task, PRD, TechSpec, and
  cited ADR and authorization paths; run the current binary's strict Spec check.
- Expected observable: one terminal `qa` Task depends on a completed leaf,
  every cited path resolves, and no strict finding appears.
- Independent confirmation and persistence: reload the files from Git and
  inspect the graph after the command.
- Evidence path: this report and the named repository paths.

### R02

```yaml
inputs:
  - kind: repository_path
    ref: internal/spec/**
  - kind: repository_path
    ref: internal/speccheck/**
  - kind: repository_path
    ref: docs/specs/0105-the-gates-own-economics/task_06.md
```

- Entry point and steps: use the current CLI on the real Spec and a scratch
  negative Spec; execute focused contract tests for report ordering, verdict
  enforcement, rendering, and authored-command refusal. Probe missing,
  malformed, duplicate-verdict, older-pass/newer-fail, and authored inputs.
- Expected observable: only the newest valid report controls settlement, only
  `pass` exits zero, the command stays visible, and authored QA commands are
  refused by name.
- Independent confirmation and persistence: rerun the current Spec check and
  reload `task_06.md` after the focused tests.
- Evidence path: this report and focused command output.

### R03

```yaml
inputs:
  - kind: repository_path
    ref: internal/speccheck/citations.go
  - kind: repository_path
    ref: internal/speccheck/citations_test.go
```

- Entry point and steps: run focused citation tests and the current CLI against
  positive and negative scratch forms: English `and`, Portuguese `e`, bare
  obligations numbers, a prose number, and an unsupported form.
- Expected observable: the obligations forms cite decisions; prose does not;
  an unsupported form reports the recognised `ADR-NNNN` syntax.
- Independent confirmation and persistence: rerun the positive scratch check
  from a fresh process.
- Evidence path: this report and focused command output.

### R04

```yaml
inputs:
  - kind: repository_path
    ref: internal/speccheck/mechanical.go
  - kind: repository_path
    ref: internal/daemon/task_engine.go
```

- Entry point and steps: exercise one named row in a three-row matrix, then all
  three named rows, through the mechanical-stage tests.
- Expected observable: one row blocks without withholding when two remain
  measurable; naming every row remains blocking.
- Independent confirmation and persistence: reload the materialized report and
  assert its blocked-row count and failing verdict.
- Evidence path: this report and focused command output.

### R05

```yaml
inputs:
  - kind: repository_path
    ref: internal/daemon/**
  - kind: repository_path
    ref: internal/speccheck/**
```

- Entry point and steps: exercise a pre-matrix blocking finding through the
  Daemon mechanical request path.
- Expected observable: no Agent Session starts and the refusal is retained.
- Independent confirmation and persistence: inspect the written refusal report
  after the request settles.
- Evidence path: this report and focused command output.

### R06

```yaml
inputs:
  - kind: repository_path
    ref: .agents/skills/write-tasks/SKILL.md
  - kind: repository_path
    ref: .agents/skills/implement-task/SKILL.md
```

- Entry point and steps: follow both skills as intended readers and locate the
  characterization trigger, real-boundary requirement, fake prohibition, and
  dependency ordering.
- Expected observable: dependent work follows a real-boundary characterization
  Task, and that Task cannot use a fake.
- Independent confirmation and persistence: compare canonical skills with
  their shipped mirrors.
- Evidence path: the four skill files and this report.

### R07

```yaml
inputs:
  - kind: repository_path
    ref: .agents/skills/qa-gate/SKILL.md
  - kind: external_repository
    ref: Pull Request state for ma/0105-the-gates-own-economics
```

- Entry point and steps: accept the prompt's no-open-Pull-Request fact, inspect
  the skill default, and record audited head, target-branch ancestry, changed
  files, strict Spec check, and full local Verification.
- Expected observable: the row remains typed environment-blocked, never passes
  silently, and equivalent evidence supports the overall verdict.
- Independent confirmation and persistence: a second Git read confirms head,
  ancestry, and changed paths after local checks.
- Evidence path: this report and Git/Verification output.

### R08

```yaml
inputs:
  - kind: repository_path
    ref: docs/workflow/authorizations/2026-08-31-the-gate-stops-charging-for-its-own-shape.md
  - kind: repository_path
    ref: .agents/skills/{qa-gate,write-tasks,implement-task}/SKILL.md
  - kind: repository_path
    ref: skills/{qa-gate,write-tasks,implement-task}/SKILL.md
  - kind: repository_path
    ref: internal/cli/implement_test.go
```

- Entry point and steps: resolve Task commits from trailers, inspect each with
  `git diff-tree`, compare the skill commit with exact bounded paths, prove the
  authorization is an ancestor, prove task_08 follows its cause, and run the
  sanctioned mirror check.
- Expected observable: no unauthorized path; correct authorization and repair
  chronology; byte-identical mirrors.
- Independent confirmation and persistence: repeat changed-path and ancestry
  queries directly from commit objects.
- Evidence path: Git objects, authorization record, skill files, and this report.

### R09

```yaml
inputs:
  - kind: repository_path
    ref: docs/findings/2026-08-12-a-queue-of-eight-specs-shows-where-the-loop-breaks.md
  - kind: repository_path
    ref: docs/findings/2026-08-12-three-consecutive-specs-measure-the-loop.md
  - kind: repository_path
    ref: docs/design/2026-08-12-the-spec-set-the-evidence-asks-for.md
  - kind: external_repository
    ref: source Runs named by the pre-Spec findings
```

- Entry point and steps: read the pre-Spec measurement and counter-number from
  the findings and design synthesis; compare their provenance with Spec history.
- Expected observable: prior evidence records 123/201 failed Tasks at QA, six
  of eight gate executions spent on one PR row, eleven contract-caused
  non-passes, and four defects missed by suites.
- Independent confirmation and persistence: corroborate claims across separate
  finding and design artifacts.
- Evidence path: the three documents and their source-Run provenance.

### R10

```yaml
inputs:
  - kind: repository_path
    ref: CONTEXT.md
  - kind: repository_path
    ref: docs/specs/0105-the-gates-own-economics/**
```

- Entry point and steps: compare emitted vocabulary with glossary entries for
  QA Report, Auditing Binary, Precondition Refusal, External Acceptance
  Evidence, Task, and Task Graph.
- Expected observable: existing canonical terms cover the change.
- Independent confirmation and persistence: search the changed diff for domain
  vocabulary and reload `CONTEXT.md`.
- Evidence path: `CONTEXT.md`, changed files, and this report.

### R11

```yaml
inputs:
  - kind: repository_path
    ref: docs/specs/0105-the-gates-own-economics/_prd.md
  - kind: repository_path
    ref: internal/**
  - kind: repository_path
    ref: .agents/skills/**
```

- Entry point and steps: inspect the complete Spec diff for verdict/count
  changes, graph reordering, data infrastructure, `internal/rounds`, and cost
  measurement.
- Expected observable: no excluded contract or subsystem changed; the gate
  remains strict.
- Independent confirmation and persistence: repeat the changed-path sweep from
  the authorization parent through audited head.
- Evidence path: Git diff, PRD Non-Goals, and this report.

### R12

```yaml
inputs:
  - kind: repository_path
    ref: '**'
```

- Entry point and steps: run `rtk make verify` exactly on the audited tree.
- Expected observable: exit 0 with all selected repository checks passing.
- Independent confirmation and persistence: run focused flows separately.
- Evidence path: this report and exact command output.

### R13

```yaml
inputs:
  - kind: repository_path
    ref: .agents/skills/{qa-gate,write-tasks,implement-task}/SKILL.md
  - kind: repository_path
    ref: skills/{qa-gate,write-tasks,implement-task}/SKILL.md
```

- Entry point and steps: read each changed canonical skill, run
  `make skills-sync-check`, and follow its stated grep/checker commands.
- Expected observable: readers can find and execute the new contract; mirrors
  match canonical skills.
- Independent confirmation and persistence: byte-compare all three pairs.
- Evidence path: six skill files, command output, and this report.

## Execution record

- R01 — `/private/tmp/roundfix-qa-0105/roundfix spec check
  0105-the-gates-own-economics --strict`: exit 0, no findings. A subsequent
  `git ls-files --error-unmatch` resolved every cited guide, ADR, and
  authorization. `_tasks.md` names task_06 as the only `qa` node, depending on
  completed task_08; all earlier Tasks are completed.
- R02 — the focused `go test -count=1` run across `internal/spec`,
  `internal/speccheck`, `internal/daemon`, and `internal/cli` exited 0. It
  exercised newest-report ordering, invalid calendar names, numeric rerun
  ordering, missing and duplicate verdicts, exact `pass` acceptance, rendered
  derivation, and QA fixture selection. Through the current binary, a cloned
  Task with an added `` `true` `` command exited 1 and named task_06 in
  `SC-QA-VERIFICATION-AUTHORED`; the unchanged real Spec then remained clean.
  After this report closed, the exact rendered command extracted from
  task_06.md exited 0 against this newest report.
- R03 — the current binary checked independent scratch Specs through its public
  CLI. `written-en` (`0026, 0029 and 0031`) and `written-pt` (`0026, 0029 e
  0031`) both exited 0 while an unrelated prose number remained inert.
  `written-bad` exited 1 and its fix said to use the recognised `ADR-NNNN`
  form. The focused parser tests independently passed.
- R04/R05 — the focused suite passed
  `TestFindingBlocksOnlyTheRowsItNames`,
  `TestMechanicalFindingsWithoutRowHintsBlockTheirRefusalCode`,
  `TestMechanicalStageWithholdsAgentSession`,
  `TestMechanicalStageSeedsReportBeforeAgentSession`, and
  `TestWriteMechanicalQAReportRecordsTheRefusal`. These cover the one-row,
  all-row, pre-matrix, report-reload, and Agent-withholding branches.
- R06/R13 — `rg` found the delivered clauses at write-tasks lines 82–87,
  implement-task lines 97–101, and qa-gate lines 177–187. `rtk make
  skills-sync-check`, `go test -count=1 -tags docscontract
  ./internal/docscontract`, and `go test -count=1 ./skills` exited 0. `cmp`
  returned equality for all three canonical/mirror skill pairs.
- R07 — the prompt states no Pull Request is open. Git reads established Run
  head `2ba8ea560dc99b87ec19a81eb6d71f7073edf39c`, target head and merge base
  `80983f05fb299230a1b512f8db3bcc4d5b054b03`, so the target is the direct
  ancestor of the audited head. `git diff --name-only origin/main...HEAD`
  recorded all 32 Spec-delivery paths. The strict Spec check and full
  `make verify` passed; this is the equivalent evidence ADR-0080 accepts.
- R08 — `git diff-tree --no-commit-id --name-only -r` ran for Task commits
  `9c0a79d9`, `45a9e21f`, `aa7efc38`, `ed1a5465`, `7f1a79ae`, and
  `2ba8ea56`. Only `7f1a79ae` edits protected skills, and its paths are the six
  grant-bounded skill paths plus task_05.md. `git merge-base --is-ancestor`
  proved authorization `634a644f` precedes `7f1a79ae`, and both the original
  derivation `9c0a79d9` and checker-compatible correction `ed1a5465` precede
  consequent fixture repair `2ba8ea56`. The repair commit contains only
  task_08.md and `internal/cli/implement_test.go`.
- R09 — `docs/backlog/2026-08-10-the-loop-is-measured-and-the-gate-is-where-it-costs.md`
  records the Run Event Stream query window and per-repository components for
  roundfix, vortex, oraculum, fluxus, and fiscus before this Spec's first commit.
  `docs/findings/2026-08-12-three-consecutive-specs-measure-the-loop.md` records
  the source vortex session and Pull Request-row/counter-number observations;
  `docs/design/2026-08-12-the-spec-set-the-evidence-asks-for.md` independently
  carries both totals into the pre-Spec design set.
- R10/R11 — `CONTEXT.md` defines Task, Task Type, Task Graph, QA Report,
  Auditing Binary, Precondition Refusal, and External Acceptance Evidence. The
  implementation adds no stored domain entity or new Task Type: it derives one
  existing `qa` Task field, scopes Mechanical Findings to report row IDs, and
  expands citation tokenization. The complete `origin/main...HEAD` path and
  source diff contains no database/data-fixture infrastructure,
  `internal/rounds` behavior, gate-order mutation, typed-count mutation, or
  measurement collector. `git diff --check origin/main...HEAD` passed.
- R12 — sandboxed `rtk make verify` reached the full suite and failed only the
  two force-stop tests on denied process-table access. The same focused pair
  failed identically in an unchanged `origin/main` clone at `0a54d5dd`. The
  full-access retry of the exact gate exited 0; `internal/cli` passed in 63.168s
  and every other package, skill check, sync check, and build passed.
  `rtk make verify-docs` then exited 0 with docs contracts, sanctioned
  regeneration boundaries, the Spec corpus budget, and both active strict Spec
  checks passing.

## Mechanical skips

| Detector | Missing artifact |
| --- | --- |
| authorization bounded paths | Task commits |
| consequent-fix commit order | consequent-fix declaration |
| QA Report structure | QA Report |
| QA evidence paths | QA Report |

## Findings

None.

## Blocked and skipped

- R07 — `blocked (environment: no open Pull Request)`. The prompt proves the
  cause. Unblocking action: open the Spec Pull Request from
  `ma/0105-the-gates-own-economics` after the authored gate and then exercise
  approval, checks, review threads, and Merge-Ready state. Equivalent evidence
  observed now: exact Run and target heads, direct ancestry, complete changed
  paths, a clean strict Spec check, focused public CLI flows, and passing full
  repository Verification.
- No row is finding-blocked, declared-blocked, or skipped. The initial sandbox
  denial did not leave R12 blocked because the exact gate passed with the
  required process-table access.

## Coverage

Thirteen of thirteen rows reached a terminal status: 12 pass and one
environment-blocked with equivalent evidence. All five user stories, five core
features, every task_06 acceptance item, the two strict-check skips, all six
Non-Goals, the outside-evidence requirement, tooling chronology, and both
declared surfaces were exercised. Probe coverage included older/newer report
ordering, malformed names and verdicts, authored input, one-row/all-row
attribution, pre-matrix withholding, both repository languages, an unsupported
citation form, unchanged-base reproduction, and canonical/mirror persistence.

Counts: `rows_blocked_environment: 1`, `rows_blocked_finding: 0`,
`rows_blocked_declared: 0`; failed 0; skipped 0; pending 0. Findings by impact:
Blocks-Completion 0, Data-Loss 0, Trust-Damage 0, Friction 0, Cosmetic 0.

## Final verdict

Pass — every runnable criterion passed, the sole environment-blocked Pull
Request row records its cause and equivalent evidence, and no row failed, was
finding- or declared-blocked, or was skipped.
