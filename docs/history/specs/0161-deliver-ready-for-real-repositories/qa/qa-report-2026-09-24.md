---
spec: 0161-deliver-ready-for-real-repositories
date: 2026-09-24
build: 94265a65119ccf144895eb6158b700c6c146d455
auditing_binary: "0.16.0 (e38c8561, built 2026-09-24 08:07:17 -0300)"
auditor_staleness: "current: commit ancestry: build commit does not predate audited tree"
status: closed
verdict: pass
rows_blocked_environment: 1
rows_blocked_finding: 0
rows_blocked_declared: 0
surfaces: [backend, cli]
---

# QA Report

## Scope and environment

- Scope: full authored terminal gate for Task 04 on build
  `94265a65119ccf144895eb6158b700c6c146d455`.
- Actor: a local Roundfix operator delivering Specs from a Git repository.
- Entry points: a binary built from the audited worktree, the public
  `roundfix deliver` CLI, real disposable Git repositories, focused executable
  contract checks, the strict Spec Consistency Check, and the daemon-owned
  repository Verification fact.
- Authoring precondition: `roundfix spec check
  0161-deliver-ready-for-real-repositories --strict` exited 0 with no findings.
  Authored Verification commands were not executed by that check.
- Pull Request: none open; Pull Request journeys are environment-blocked.
- Repository Verification: daemon-owned; the supplied result is `make verify`
  exit 0 with diagnostics removed on success. This gate will not rerun it.
- Parity: the behavior binary is `/private/tmp/roundfix-0161-qa`, built from
  the audited worktree. Git journeys use disposable repositories under
  `/private/tmp`; no production repository or remote is mutated. The
  independent Git fixture disabled inherited commit signing locally after the
  sandbox denied access to the user's GPG home; this changed fixture setup,
  not the branch behavior under observation.

## Performed repairs

None.

## Assigned repair failures

None.

## Authorization audit inputs

None.

## Mechanical findings

None.

## Static gate

- `make verify` — pass (exit 0), run once by the Daemon outside the Agent
  sandbox; diagnostics were removed on success. Per the QA contract, the Agent
  did not rerun it.
- `roundfix spec check 0161-deliver-ready-for-real-repositories --strict` —
  pass (exit 0), no findings. The checker disclosed four skipped detectors:
  no findings rollup, no backlog, no TechSpec Vocabulary Contract, and no
  `references/_index.md`.

## Execution log

- `GOCACHE=/private/tmp/roundfix-0161-qa-gocache go build -o
  /private/tmp/roundfix-0161-qa ./cmd/roundfix` — pass (exit 0).
- `/private/tmp/roundfix-0161-qa deliver --help` — pass (exit 0); the built CLI
  exposed `start`, `status`, `resume`, and `stop`.
- `/private/tmp/roundfix-0161-qa version` — pass (exit 0); reported
  `roundfix 0.16.0`.
- `GOCACHE=/private/tmp/roundfix-0161-qa-gocache go test -count=1 -v -run
  '^(TestNoChecksReportedIsAnEmptyReport|TestCheckCommandFailuresRemainErrors|TestDeliveryWaitsThroughUnreportedChecks|TestCheckReadErrorsAreRetriedUntilTheDeadline|TestPersistentCheckReadErrorsParkAtTheDeadline)$'
  ./internal/delivery` — pass (exit 0), 5/5 named cases.
- `GOCACHE=/private/tmp/roundfix-0161-qa-gocache go test -count=1 -v -run
  '^(TestDeliveryWorkflowCreatesAnItemBranchFromTheRefreshedDefault|TestItemBranchHasNoUpstream|TestEachDeliveryGetsItsOwnBranch|TestResumeReusesTheRecordedItemBranch|TestAParkLeavesACleanCheckout|TestResumeAcceptsTheArchiveCommit|TestResumeReleasesAStaleOwner)$'
  ./internal/cli` — pass (exit 0), 7/7 named cases and all subtests.
- `GOCACHE=/private/tmp/roundfix-0161-qa-gocache go test -count=1 -v -run
  '^TestRecordDeliveryQueueItemBranchKeepsTheFirstBranch$' ./internal/store` —
  pass (exit 0).
- Disposable Git fixture `/private/tmp/roundfix-0161-outside.JT5s42/repo` —
  pass: branches `roundfix/deliver-0161-outside-a` and
  `roundfix/deliver-0161-outside-b` both reported an empty upstream; a fresh
  switch reopened branch `a`; the final checkout was clean.
- `roundfix spec check 0161-deliver-ready-for-real-repositories --format json
  --strict` — pass (exit 0), `findings: []`, `repairInputs: []`, and authored
  Verification not executed.
- `git diff-tree --no-commit-id --name-only -r <task-commit>` over commits
  `2039edae`, `595fe0da`, and `94265a65` — pass: the changed paths match the
  three Task slices and contain no governed tooling path. Git emitted a
  non-blocking fsmonitor IPC diagnostic while still returning the complete
  path lists.

## Results

| # | Status | Provenance |
| - | --- | --- |
| R1 | pass | Requirement 1; repository Verification |
| R2 | pass | Requirement 2 |
| R3 | pass | Requirement 3; outside-evidence row |
| R4 | pass | Requirement 4 |
| R5 | pass | Requirement 5 |
| R6 | blocked (environment: no open Pull Request) | Pull Request row |

## Row evidence

### R1

```yaml
inputs:
  - kind: repository_path
    ref: "**"
```

- Actor and surface: daemon; repository gate.
- Entry point and steps: run `make verify` once for the audited tree.
- Expected observable: exit 0 and no retained success diagnostics.
- Independent confirmation: daemon settlement reports the exit code and
  diagnostic disposition.
- Persistence check: not applicable to a static gate.
- Evidence path: daemon-owned gate fact in this report.
- Result: pass. The supplied daemon fact records the exact command, exit 0,
  and successful diagnostic disposition for this audited tree.

### R2

```yaml
inputs:
  - kind: repository_path
    ref: internal/delivery/engine.go
  - kind: repository_path
    ref: internal/delivery/engine_test.go
  - kind: repository_path
    ref: internal/delivery/github.go
  - kind: repository_path
    ref: internal/delivery/github_test.go
```

- Actor and surface: delivery operator; CLI/backend.
- Entry point and steps: exercise the built delivery behavior with the exact
  `gh pr checks` no-report response, a transient read error, and a later
  passing report.
- Expected observable: no-report and transient-error states stay inside the
  bounded wait; a later passing report advances to merge; repeated errors stop
  at the deadline.
- Independent confirmation: inspect the exact check-call count, final queue
  stage, blocker, and merge observation.
- Persistence check: repeat from fresh test processes and fresh fixtures.
- Evidence path: focused command output recorded in this report.
- Result: pass. The exact gh 2.101.0 no-report stderr became an empty report;
  other command failures remained errors. The Delivery Engine waited through
  an empty report and a transient read error, then merged after a passing
  report. Repeated read errors stopped at the configured deadline and parked
  as `checks-timeout`, proving the retry is bounded.

### R3

```yaml
inputs:
  - kind: repository_path
    ref: internal/cli/deliver_workflow.go
  - kind: repository_path
    ref: internal/cli/deliver_test.go
  - kind: repository_path
    ref: internal/store/delivery.go
  - kind: repository_path
    ref: internal/store/delivery_test.go
  - kind: external_repository
    ref: disposable real-Git repository under /private/tmp
```

- Actor and surface: delivery operator; CLI backed by real Git.
- Entry point and steps: create item branches for two deliveries of one slug,
  inspect their upstreams and names, then repeat branch selection with a
  branch already recorded before the stage write.
- Expected observable: every item branch has no upstream, two deliveries have
  different suffixed names, and resume reuses the first recorded local branch.
- Independent confirmation: read the branch refs, `%(upstream:short)`, durable
  queue value, and current branch through fresh Git commands.
- Persistence check: reopen the repository and stored queue before the resume
  observation.
- Evidence path: focused command output and an independent disposable Git
  measurement recorded in this report.
- Result: pass. The product suite used real Git and observed no upstream for
  the created item branch, two different suffixed branch names for two
  deliveries of one slug, and reuse of the first recorded branch when the
  remote was unavailable. The store check proved a later proposal cannot
  overwrite that durable branch value. Independently, disposable repository
  `/private/tmp/roundfix-0161-outside.JT5s42/repo` created two branches with
  `git switch --no-track -c`; fresh `for-each-ref` reads reported
  `upstream=` for both, and a later `git switch` reopened branch `a` cleanly.

### R4

```yaml
inputs:
  - kind: repository_path
    ref: internal/cli/deliver.go
  - kind: repository_path
    ref: internal/cli/deliver_test.go
  - kind: repository_path
    ref: internal/cli/deliver_workflow.go
  - kind: repository_path
    ref: internal/delivery/engine.go
  - kind: repository_path
    ref: internal/delivery/engine_test.go
  - kind: external_repository
    ref: disposable real-Git repository under /private/tmp
```

- Actor and surface: delivery operator; CLI backed by real Git and process
  identity inspection.
- Entry point and steps: park after a non-exact archive with tracked and
  untracked dirt, resume after an exact archive commit, and resume a queue
  whose live PID has a different recorded identity.
- Expected observable: the starting branch is clean and the next item starts;
  the exact archive commit advances past archiving; the stale owner record is
  released while unreadable or matching identities remain fail-closed.
- Independent confirmation: fresh Git status/ref reads, persisted queue stage,
  and owner record reads after each journey.
- Persistence check: reopen the repository and Run Database before each resume
  observation.
- Evidence path: focused command output recorded in this report.
- Result: pass. `TestAParkLeavesACleanCheckout` dirtied tracked and untracked
  paths during a non-exact archive, observed the next queued Spec start, and
  finished on a clean starting branch. `TestResumeAcceptsTheArchiveCommit`
  advanced past an exact Spec-move commit and rejected the same move when the
  commit also touched an unrelated path. `TestResumeReleasesAStaleOwner`
  reclaimed a live PID with a mismatched identity; its negative subtest kept
  the owner and refused resume when identity was unreadable.

### R5

```yaml
inputs:
  - kind: repository_path
    ref: docs/specs/0161-deliver-ready-for-real-repositories/_prd.md
  - kind: repository_path
    ref: docs/specs/0161-deliver-ready-for-real-repositories/_techspec.md
  - kind: repository_path
    ref: docs/specs/0161-deliver-ready-for-real-repositories/_tasks.md
  - kind: repository_path
    ref: docs/specs/0161-deliver-ready-for-real-repositories/task_01.md
  - kind: repository_path
    ref: docs/specs/0161-deliver-ready-for-real-repositories/task_02.md
  - kind: repository_path
    ref: docs/specs/0161-deliver-ready-for-real-repositories/task_03.md
  - kind: repository_path
    ref: docs/specs/0161-deliver-ready-for-real-repositories/task_04.md
```

- Actor and surface: Spec maintainer; authored CLI contract.
- Entry point and steps: run the strict Spec Consistency Check and inspect its
  promise-rule result for numbered Success Metrics and API Contracts.
- Expected observable: every declared promise names a consuming Task and the
  checker reports no consistency finding.
- Independent confirmation: map every numbered Success Metric and API Contract
  citation to the Task References that consume it.
- Persistence check: rerun the checker from a fresh process.
- Evidence path: strict checker output and citation inventory recorded in this
  report.
- Result: pass. Two fresh strict-check processes reported no findings. The
  Task 04 References explicitly consume Success Metrics 1-5 and API Contracts
  1-3; Tasks 01-03 consume the matching Checks, Branches, and Park and resume
  sections. This is executable checker and citation evidence, not a file-read
  verdict.

### R6

```yaml
inputs:
  - kind: live_service
    ref: Pull Request for fix/0161-deliver-ready-for-real-repositories
  - kind: repository_path
    ref: "**"
```

- Actor and surface: reviewer and delivery operator; Pull Request controls.
- Entry point and steps: inspect approval, checks and status, unresolved review
  threads, Merge-Ready acceptance, and review-artifact ancestry for the Spec
  target branch.
- Expected observable: each control is resolved for the expected head.
- Independent confirmation: compare every control's head to the audited build
  and the supplied repository Verification fact.
- Persistence check: re-read the Pull Request state for the same head.
- Evidence path: prompt-supplied Pull Request fact and equivalent-evidence
  record in this report.
- Result: blocked (environment: no open Pull Request), with equivalent evidence
  for every control:
  - Approval: no acceptance was obtained because no Pull Request exists.
  - Checks and status: the daemon-owned `make verify` run passed at exit 0;
    the strict Spec check also passed with no findings.
  - Unresolved review threads: none can stand without a Pull Request or review
    thread set.
  - Merge-Ready acceptance: none exists yet; the authored gate runs before
    Pull Request creation under ADR-0091.
  - Review-artifact ancestry: no review artifact exists yet; the claimed QA
    head is `94265a65119ccf144895eb6158b700c6c146d455`, and no stale review head
    is being credited.

## Mechanical skips

| Detector | Missing artifact |
| --- | --- |
| authorization bounded paths | tooling authorization |
| consequent-fix commit order | consequent-fix declaration |
| QA Report structure | QA Report |
| QA evidence paths | QA Report |

## Findings

None.

## Blocked and skipped

- R6 is blocked by the supplied environment fact that no Pull Request is open.
  Unblock it after publication by observing approval, current-head checks,
  unresolved review threads, Merge-Ready acceptance, and review-artifact
  ancestry on `fix/0161-deliver-ready-for-real-repositories`. The equivalent
  pre-Pull-Request evidence for every control is recorded in R6, so this
  environment block does not cap the verdict.
- No row was skipped. No row is finding-blocked or declared-blocked.

## Coverage

- Coverage sources: 8/8 covered — Requirements 1-5, the outside-evidence row,
  the Pull Request row, and repository Verification, with Requirement 1 and
  repository Verification grouped in R1 and Requirement 3 grouped with outside
  evidence in R3.
- Results: 5 pass, 1 blocked by environment, 0 fail, 0 skipped, 0 pending.
- Blocked counts: `rows_blocked_environment: 1`,
  `rows_blocked_finding: 0`, `rows_blocked_declared: 0`.
- Unreachable Acceptance: the PRD declares no `## Unreachable Acceptance`
  entries. Frontend sweep: not applicable; the declared surfaces are backend
  and CLI.
- The declared matrix excludes PRD Goals, Core Features, Success Metrics,
  Non-Goals, and non-QA Task Acceptance Criteria except where Requirement 5
  verifies the promise rule; no row claims them as independent coverage
  sources.

## Final verdict

Pass. Every runnable row passed on build
`94265a65119ccf144895eb6158b700c6c146d455`; the sole environment-blocked row
records its cause and equivalent evidence for every Pull Request control, and
there are no findings, declared blocks, skips, or pending rows.
