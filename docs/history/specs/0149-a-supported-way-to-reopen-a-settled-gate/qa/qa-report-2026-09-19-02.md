---
spec: 0149-a-supported-way-to-reopen-a-settled-gate
date: 2026-09-19
build: 42e616eb4dcf7ec3012b399b96bf0344c3e4f5be
auditing_binary: "0.14.1 (b23ccba1, built 2026-09-19 15:08:39 -0300)"
auditor_staleness: "stale: commit ancestry: build commit predates audited tree"
status: closed
verdict: pass
rows_blocked_environment: 1
rows_blocked_finding: 0
rows_blocked_declared: 0
surfaces: [backend, cli, docs]
---

# QA Report

## Scope and environment

This full gate audits commit `42e616eb4dcf7ec3012b399b96bf0344c3e4f5be`
on the Run Worktree branch. The strict precondition `roundfix spec check
0149-a-supported-way-to-reopen-a-settled-gate --strict` exited 0 with no
findings and did not execute authored Verification commands. The intended
actors are a maintainer recovering a stale gate and a reviewer evaluating
pre-Pull-Request controls. The declared surfaces are backend, CLI, and docs.

The assignment confirms that no Pull Request is open and that Pull Request
journeys are environment-blocked. The Run Worktree branch is never pushed and
has no Pull Request of its own. No production data or credentials were used.
Some local Git reads emitted an fsmonitor IPC warning; each returned exit 0 and
the complete requested output, so the warning did not block an evidence row.

## Static gate

- `make verify`: pass, exit 0. The Roundfix Daemon ran this command outside the
  Agent sandbox and removed diagnostics on success. Per the assignment, this
  gate records that supervised result and did not rerun repository Verification.
- `roundfix spec check 0149-a-supported-way-to-reopen-a-settled-gate
  --strict`: pass, exit 0, `No findings. Authored Verification commands were
  not executed.`

## Performed repairs

None.

## Assigned repair failures

None.

## Authorization audit inputs

None.

## Mechanical findings

None.

## Results

| # | Status | Provenance |
| - | --- | --- |
| R1 | pass | Requirement 2; outside-evidence row |
| R2 | pass | Requirement 3; Requirement 4 |
| R3 | pass | Requirement 5 |
| R4 | pass | Requirement 6 |
| R5 | pass | Requirement 7 |
| R6 | pass | Requirement 8 |
| R7 | pass | Requirement 1; repository Verification |
| R8 | blocked (environment: no open Pull Request) | Pull Request row |

## Planned row evidence

### R1

```yaml
inputs:
  - kind: repository_path
    ref: cmd/roundfix/**
  - kind: repository_path
    ref: internal/cli/**
  - kind: repository_path
    ref: internal/spec/**
  - kind: repository_path
    ref: docs/history/specs/0148-*/**
```

- Actor: maintainer recovering a Spec whose settled QA gate is stale.
- Entry point and surface: built `roundfix reopen --spec <slug>` CLI.
- Steps: reproduce the loader's stale-gate refusal, run `reopen`, then load the
  same Spec from a new process.
- Expected observable: `reopen` exits 0, changes the QA Task to `pending`, and
  the fresh process no longer reports stale-gate invalidation.
- Independent confirmation: a second public command reads the Spec; Spec 0148
  repository history independently establishes the unsupported recovery gap.
- Persistence check: the reopened status remains visible to the fresh process.
- Evidence path: this report, R1 command transcript.
- Observed: before reopen, `archive` exited 2 with `QA gate result is
  invalidated ... dependencies are not completed: task_06`. `reopen` exited 0
  with `reopened task_04 pending — invalidated
  qa/qa-report-2026-09-19-01.md`. A new `archive` process reached the later
  eligibility check and refused only because `task_06` remained `pending`; the
  stale-gate refusal was gone. A fresh read found `status: pending` and the new
  invalidation naming `task_06`.
- Outside evidence: repository commit
  `796bdf7d3bb095357b61b85aea99bd4b9873b63d` records Spec 0148's QA Task as
  `failed`; corrective-target revision
  `eaae7abc10482c472f05695be4fc8628a50da56e` retains it as `pending`, while a
  repository-wide `git grep 'roundfix reopen --spec'` at that revision found no
  supported command. This evidence originates outside Spec 0149 and records
  the prior unsupported recovery path independently.
- Result: pass.

### R2

```yaml
inputs:
  - kind: repository_path
    ref: cmd/roundfix/**
  - kind: repository_path
    ref: internal/cli/**
  - kind: repository_path
    ref: internal/spec/**
```

- Actor: maintainer attempting to reopen a pending gate and a healthy settled
  gate.
- Entry point and surface: built `roundfix reopen --spec <slug>` CLI.
- Steps: invoke both refusals and invalid path-like slugs, then compare fixture
  bytes after each process exits.
- Expected observable: each refusal exits 2, names the failed condition, and
  leaves the Spec directory byte-identical.
- Independent confirmation: directory digests and fresh filesystem reads.
- Persistence check: comparisons run after each CLI process exits.
- Evidence path: this report, R2 command transcript.
- Observed, pending gate: exit 2 with `terminal QA Task "task_04" is not
  completed (status: pending)`; `git diff --exit-code` returned 0 and the
  working tree remained clean.
- Observed, healthy completed gate: exit 2 with `terminal QA Task "task_04" is
  not stale; every dependency is completed`; `git diff --exit-code` returned 0
  and the working tree remained clean.
- Invalid-input probes: `../other`, `a/b`, and `/private/tmp/other` each exited
  2 and named `invalid Spec slug` before repository loading.
- Result: pass.

### R3

```yaml
inputs:
  - kind: repository_path
    ref: cmd/roundfix/**
  - kind: repository_path
    ref: internal/cli/**
  - kind: repository_path
    ref: internal/spec/**
```

- Actor: maintainer preserving evidence of an invalidated verdict.
- Entry point and surface: built CLI and filesystem-visible Spec artifacts.
- Steps: force the atomic replacement to fail, compare both artifacts, then
  reopen successfully and re-read them.
- Expected observable: failure changes neither artifact; success preserves the
  report and prior Result and appends the dated invalidation.
- Independent confirmation: SHA-256 digests and focused fresh reads.
- Persistence check: evidence is re-read after each CLI process exits.
- Evidence path: this report, R3 command transcript.
- Observed failure probe: after the Spec directory became read-only, the
  command exited 1 because it could not create the temporary replacement. The
  QA Task digest remained
  `3cb9253f05b7f2f0fab55d8268ed9eb7d60dca1a1d6474840e3cf206a8c7069a`
  and the QA Report digest remained
  `43d26e298e1cc64803cfc79d2c04d523208665979632dc9db49c419252d6332e`.
- Observed success: after permissions were restored, `reopen` exited 0. A fresh
  read found `status: pending`, the prior `## Result` and text `The prior gate
  verified the original graph.`, plus a later `## Invalidation` naming
  `qa/qa-report-2026-09-19-01.md` and `task_06`. The QA Report digest remained
  `43d26e298e1cc64803cfc79d2c04d523208665979632dc9db49c419252d6332e`.
- Result: pass.

### R4

```yaml
inputs:
  - kind: repository_path
    ref: cmd/roundfix/**
  - kind: repository_path
    ref: internal/cli/**
  - kind: repository_path
    ref: internal/spec/**
  - kind: repository_path
    ref: internal/store/**
```

- Actor: maintainer using the local repair command outside a Run.
- Entry point and surface: built CLI in scratch repositories and the public
  active-Run refusal.
- Steps: compare Git and Roundfix state around a successful reopen; separately
  attempt reopen with a dead-owner Active Run and compare its record and journal.
- Expected observable: no Run or journal is created by success, Git HEAD and
  remote refs do not move, and an Active Run is refused without any mutation.
- Independent confirmation: fresh Git, filesystem, Run record, and journal reads.
- Persistence check: state inspection runs after each process exits.
- Evidence path: this report, R4 command transcript.
- Observed public flow: `reopen` exited 0. Before and after, fixture HEAD stayed
  `61a4b6d591c2a6c839edf85c4cc036ee86dae28f`; every remote ref was unchanged;
  no repository-local database appeared; and `git status --short` named only
  the QA Task. The command did not execute QA or publish Git state.
- Active-Run and confinement probes: `go test -count=1 -run '^TestReopen'
  ./internal/cli` passed, including the dead-owner Active Run case that compares
  the complete Run record and Run Event Journal before and after refusal, and
  the lexical manifest escape that compares the escaped QA Task bytes. The same
  suite retained the symlink-resolved Spec Root check and the success/refusal
  contracts. Focused Spec recovery tests also passed.
- Result: pass.

### R5

```yaml
inputs:
  - kind: repository_path
    ref: .agents/skills/roundfix/SKILL.md
  - kind: repository_path
    ref: skills/roundfix/SKILL.md
  - kind: repository_path
    ref: docs/user-guide/commands.md
  - kind: repository_path
    ref: docs/specs/0149-a-supported-way-to-reopen-a-settled-gate/_authorization.md
  - kind: repository_path
    ref: docs/specs/0149-a-supported-way-to-reopen-a-settled-gate/task_03.md
  - kind: repository_path
    ref: docs/specs/0149-a-supported-way-to-reopen-a-settled-gate/task_01.md
  - kind: repository_path
    ref: docs/specs/0149-a-supported-way-to-reopen-a-settled-gate/task_02.md
  - kind: repository_path
    ref: docs/specs/0149-a-supported-way-to-reopen-a-settled-gate/task_05.md
  - kind: repository_path
    ref: docs/specs/0149-a-supported-way-to-reopen-a-settled-gate/task_06.md
  - kind: repository_path
    ref: internal/cli/**
  - kind: repository_path
    ref: internal/spec/**
```

- Actor: maintainer relying on the shipped skill and bounded tooling authority.
- Entry point and surface: `skills check`, mirror comparison, command help, and
  Git commit-path audit.
- Steps: validate the skill contract and inspect every Task commit from Git
  against the operative authorization ancestry and bounded paths.
- Expected observable: docs match the public contract and all governed changes
  stay inside the earlier grant, with no folded or misordered tooling change.
- Independent confirmation: `git diff-tree` and ancestry checks, not Task prose.
- Persistence check: rerun read-only checks from the audited tree.
- Evidence path: this report, R5 command transcript.
- Observed: the audited binary's `skills check` exited 0; recursive comparison
  found the canonical skill and mirror byte-identical; the built command's help,
  both skill copies, and the user guide name the command, refusals, retained
  evidence, options, and exit codes.
- Git audit: `git diff-tree --no-commit-id --name-only -r` showed `b846aee0`
  changed Task 01 plus `internal/spec`; `5c5a7778` changed Task 02 plus
  `internal/cli` and `internal/spec`; `f71c6fb8` changed Task 03, the ordinary
  user guide, and only the two governed skill paths; `82894aee` changed Task 05
  plus ordinary `internal/cli` and `internal/spec`; and `42e616eb` changed Task
  06 plus ordinary `internal/cli`. Authorization commit `a86d5bb3` is an
  ancestor of `f71c6fb8`, the authorization record is byte-identical through
  HEAD, and it grants `implement` and `commit` for exactly the two governed
  paths. No prerequisite or consequent-fix declaration applies, no governed
  path falls outside the grant, and no authorization was folded into a Task
  commit.
- Re-observation: after the input declaration was expanded to every Task and
  source path this audit reads, the same skill, `diff-tree`, ancestry, and
  authorization-byte checks all exited 0 with the paths and chronology above.
- Result: pass.

### R6

```yaml
inputs:
  - kind: repository_path
    ref: docs/specs/0149-a-supported-way-to-reopen-a-settled-gate/**
```

- Actor: maintainer relying on the Spec's promise-to-Task traceability.
- Entry point and surface: strict Spec check and targeted inspection for its
  skipped detectors.
- Steps: record the strict result and inspect the absent-artifact boundaries.
- Expected observable: no promise-rule finding and no applicable broken local
  reference hidden by a skipped detector.
- Independent confirmation: Task Graph and artifact paths resolve from a fresh
  filesystem read.
- Persistence check: checks read the audited tree at HEAD.
- Evidence path: this report, R6 command transcript and Mechanical skips.
- Observed: the strict checker exited 0 with no findings. Its absent-artifact
  skips were followed up directly: no active rollup applies, no Spec-owned
  backlog entry applies, and no adopted `references/_index.md` exists. The
  linked authorization and TechSpec paths resolve. The absent Vocabulary
  Contract hides no competing domain term: `reopened`, `invalidated`, `not
  completed`, `not stale`, `Active Run`, `invalid Spec slug`, and `outside Spec
  directory` are used consistently across the command, tests, Task evidence,
  and user guide.
- Result: pass.

### R7

```yaml
inputs:
  - kind: repository_path
    ref: Makefile
  - kind: repository_path
    ref: '**/*.go'
  - kind: repository_path
    ref: go.mod
  - kind: repository_path
    ref: go.sum
```

- Actor: daemon verifying the assembled repository.
- Entry point and surface: repository Verification `make verify`.
- Steps: record the daemon-supplied result without rerunning it.
- Expected observable: exit 0 with diagnostics removed on success.
- Independent confirmation: supervised daemon result in the QA assignment.
- Persistence check: not applicable to a supervised static-gate fact.
- Evidence path: this report, Static gate.
- Observed: the assignment reports `make verify` exited 0 and diagnostics were
  removed on success. This Agent did not rerun the command.
- Result: pass.

### R8

```yaml
inputs:
  - kind: live_service
    ref: Pull Request for feat/0149-reopen-settled-gate
```

- Actor: reviewer evaluating Pull Request controls.
- Entry point and surface: equivalent-evidence path because the assignment
  confirms no Pull Request is open.
- Steps: account for approval, checks and status, unresolved review threads,
  Merge-Ready acceptance, and review-artifact ancestry.
- Expected observable: the environment cause is explicit and every unavailable
  control has equivalent observed or supervised evidence.
- Independent confirmation: assignment fact plus local head and artifact reads.
- Persistence check: live Pull Request state is unavailable before publication.
- Evidence path: this report, R8 command transcript and Blocked and skipped.
- Environment cause: the assignment states `Pull Request: none open; Pull
  Request journeys are environment-blocked.` The Run Worktree branch
  `roundfix/run-run_20260919T182944Z_bf311efa7a1801d0` is never pushed and has
  no Pull Request of its own, so this gate did not try to resolve a Pull Request
  from it.
- Equivalent controls: no approval has been obtained; repository Verification
  passed at exit 0; no Pull Request exists to carry unresolved review threads;
  no Merge-Ready acceptance exists yet; no review artifact exists under the
  Spec or repository `.roundfix` path; and the audited head is
  `42e616eb4dcf7ec3012b399b96bf0344c3e4f5be`, so there is no review-artifact
  ancestry claim to make.
- Unblocking action: after this gate and the configured pre-PR review complete,
  open the Pull Request on `feat/0149-reopen-settled-gate` and observe the live
  controls there.
- Result: blocked (environment: no open Pull Request), with equivalent evidence
  for every enumerated control.

## Mechanical skips

| Detector | Missing artifact |
| --- | --- |
| authorization bounded paths | Task commits |
| consequent-fix commit order | consequent-fix declaration |
| QA evidence paths | docs/specs/0149-a-supported-way-to-reopen-a-settled-gate/qa/evidence |

The manual Git audit in R5 compensates for the seeded authorization skip. The
strict precondition also skipped `SC-ROLLUP-MEMBER` because no active rollup
applies, `SC-BACKLOG-UNMOVED` because no Spec-owned backlog entry applies, the
vocabulary documentation detector because the TechSpec has no Vocabulary
Contract, and `SC-REF-UNRESOLVED` because the Spec has no
`references/_index.md`. R6 records the required follow-up.

## Findings

None.

## Blocked and skipped

- R8 is blocked by the confirmed absence of an open Pull Request. Equivalent
  observed and supervised evidence answers all five controls, so this
  environment block does not prevent a pass.
- No row was skipped, finding-blocked, or declared-blocked.

## Coverage

- Sources covered: Requirements 1-8, repository Verification,
  outside-evidence row, and Pull Request row. No row names another source.
- No PRD `Unreachable Acceptance` declaration or frontend sweep applies.
- Results: 7 pass, 1 blocked (environment), 0 fail, 0 finding-blocked, 0
  declared-blocked, 0 skipped, 0 pending.
- High-risk probes exercised: stale deep reopen in a fresh process, pending and
  healthy out-of-order refusals, path-like slug rejection, lexical and
  symlink-resolved confinement, atomic-write failure, retained evidence,
  dead-owner Active Run refusal without reclamation, and Git/Run side-effect
  isolation.
- Non-Goals checked through the public flow: reopen did not execute QA, decide
  whether corrective work was justified, change stale-gate detection, create a
  Run, write a Run Event Journal entry, commit, or push.

## Final verdict

Pass: every runnable criterion passed, the one environment-blocked Pull Request
row records its cause and equivalent evidence for all controls, and no row
failed, remained pending, was skipped, or was blocked by a finding or
declaration.
