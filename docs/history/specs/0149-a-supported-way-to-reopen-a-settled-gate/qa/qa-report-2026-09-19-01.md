---
spec: 0149-a-supported-way-to-reopen-a-settled-gate
date: 2026-09-19
build: 82894aee9c67bf6f9faf5b5541ecaf11474e5d27
auditing_binary: "0.14.1 (e0c22f11, built 2026-09-19 14:05:06 -0300)"
auditor_staleness: "stale: commit ancestry: build commit predates audited tree"
status: closed
verdict: pass
rows_blocked_environment: 1
rows_blocked_finding: 0
rows_blocked_declared: 0
surfaces: [backend, cli, docs]
---

# QA Report

## Scope and environment

This full gate audited commit `82894aee9c67bf6f9faf5b5541ecaf11474e5d27`
on the Run Worktree branch. The strict precondition `roundfix spec check
0149-a-supported-way-to-reopen-a-settled-gate --strict` exited 0 with no
findings. The CLI under test was built from the audited tree with `go build
-buildvcs=false -o <scratch>/roundfix ./cmd/roundfix` and exercised through new
processes in four committed scratch repositories.

The intended actors were a maintainer recovering a stale gate and a reviewer
evaluating the pre-Pull-Request controls. The declared surfaces were backend,
CLI, and docs. No production data or credentials were used. Pull Request
journeys were environment-blocked because the assignment confirms no Pull
Request is open; the equivalent controls are recorded in R8.

The sandbox denied Go's default cache under `~/Library/Caches/go-build`; the
same build passed with `GOCACHE` under `/private/tmp`, without changing source
or compiler inputs. Scratch Git commits initially inherited GPG signing and
were denied access to `~/.gnupg`; fixture-local `commit.gpgsign=false` allowed
the disposable repositories to be committed. Neither denial affected a product
journey.

## Static gate

- `make verify`: pass, exit 0. The Roundfix Daemon ran this command outside the
  Agent sandbox and removed diagnostics on success. Per the assignment, this
  gate recorded that supervised result and did not rerun repository
  Verification.
- `roundfix spec check 0149-a-supported-way-to-reopen-a-settled-gate
  --strict`: pass, exit 0, `No findings. Authored Verification commands were
  not executed.` The checker skipped the four absent-artifact detectors
  recorded in R6.
- `go test -count=1 -run '^TestReopen' ./internal/cli`: pass, exit 0.
- `go test -count=1 -run
  'Test(ReopenGate|AppendGateInvalidation|SetStatus|LoadForRecovery|LoadStillRefusesAStaleGate)'
  ./internal/spec`: pass, exit 0.

## Performed repairs

None.

## Assigned repair failures

None.

## Authorization audit inputs

None.

## Mechanical findings

None.

## Results

| # | Status | Provenance |
| - | --- | --- |
| R1 | pass | Requirement 2; outside-evidence row |
| R2 | pass | Requirement 3; Requirement 4 |
| R3 | pass | Requirement 5 |
| R4 | pass | Requirement 6 |
| R5 | pass | Requirement 7 |
| R6 | pass | Requirement 8 |
| R7 | pass | Requirement 1; repository Verification |
| R8 | blocked (environment: no open Pull Request) | Pull Request row |

## Planned row evidence

### R1

```yaml
inputs:
  - kind: repository_path
    ref: cmd/roundfix/**
  - kind: repository_path
    ref: internal/cli/**
  - kind: repository_path
    ref: internal/spec/**
  - kind: repository_path
    ref: docs/history/specs/0148-*/**
```

- Actor: maintainer recovering a Spec whose settled QA gate is stale.
- Entry point and surface: built `roundfix reopen --spec <slug>` CLI.
- Steps: invoke `archive` to reproduce the loader's stale-gate refusal, run
  `reopen`, then invoke `archive` again in a new process.
- Expected observable: `reopen` exits 0, changes the QA Task to `pending`, and
  the second public command passes the stale-gate validation point.
- Independent confirmation: a fresh process reads the Spec after reopen;
  repository history from Spec 0148 independently establishes the prior
  unsupported recovery gap.
- Persistence check: the reopened status remains visible to the fresh process.
- Evidence path: this report, R1 command transcript.
- Observed: before reopen, `roundfix archive
  0149-a-supported-way-to-reopen-a-settled-gate` exited 2 with `QA gate result
  is invalidated ... dependencies are not completed: task_05`. `roundfix
  reopen --spec 0149-a-supported-way-to-reopen-a-settled-gate` exited 0 with
  `reopened task_04 pending — invalidated
  qa/qa-report-2026-09-19-01.md`. A new `archive` process reached the later
  archive eligibility check and refused only because `task_05` remained
  `pending`; the stale-gate refusal was gone.
- Outside evidence: repository commit
  `796bdf7d3bb095357b61b85aea99bd4b9873b63d` records Spec 0148's terminal QA
  Task changing from `pending` to `failed` on a Run branch. The corrective
  target history at `eaae7abc10482c472f05695be4fc8628a50da56e` retains that
  Task as `pending`, while `git grep 'roundfix reopen --spec'` at that revision
  finds no supported command. This evidence originates outside Spec 0149 and
  independently records the operational gap the new command closes.
- Result: pass.

### R2

```yaml
inputs:
  - kind: repository_path
    ref: cmd/roundfix/**
  - kind: repository_path
    ref: internal/cli/**
  - kind: repository_path
    ref: internal/spec/**
```

- Actor: maintainer attempting to reopen a pending gate and a healthy completed
  gate.
- Entry point and surface: built `roundfix reopen --spec <slug>` CLI.
- Steps: commit each fixture, run the refused command, then compare each
  working tree with its committed bytes from a separate command.
- Expected observable: both calls exit 2, name the failed condition, and leave
  their Spec directories byte-identical.
- Independent confirmation: `git diff --exit-code` and `git status --short`
  after each refusal.
- Persistence check: the byte comparison runs after each CLI process exits.
- Evidence path: this report, R2 command transcript.
- Observed, pending gate: exit 2 with `terminal QA Task "task_04" is not
  completed (status: pending)`; the repository remained clean.
- Observed, healthy completed gate: exit 2 with `terminal QA Task "task_04" is
  not stale; every dependency is completed`; the repository remained clean.
- Invalid-input probes: `../other`, `a/b`, and `/private/tmp/other` each exited
  2 and named `invalid Spec slug` before repository loading. The focused current
  test run also passed the symlink-confinement and active-Run refusal cases.
- Result: pass.

### R3

```yaml
inputs:
  - kind: repository_path
    ref: cmd/roundfix/**
  - kind: repository_path
    ref: internal/cli/**
  - kind: repository_path
    ref: internal/spec/**
```

- Actor: maintainer preserving the evidence of an invalidated verdict.
- Entry point and surface: built `roundfix reopen --spec <slug>` CLI and
  filesystem-visible Spec artifacts.
- Steps: capture QA Report and QA Task digests, force the atomic replacement to
  fail through a read-only Spec directory, restore permissions, run a successful
  reopen, and re-read both artifacts.
- Expected observable: a write failure changes neither artifact; a successful
  reopen leaves the report digest unchanged, retains the prior Result, and adds
  a dated invalidation naming the report and stale dependency.
- Independent confirmation: SHA-256 digests plus focused text checks after
  fresh reads.
- Persistence check: all evidence is re-read after each CLI process exits.
- Evidence path: this report, R3 command transcript.
- Observed failure probe: the command exited 1 when it could not create the
  temporary replacement. The QA Task SHA-256 stayed
  `78bc2ca9998e8db49532e9d5716337911c41f95fd0a2e482e278c759116ae697`,
  its status stayed `completed`, and the QA Report SHA-256 stayed
  `d841d1dc7f322b2d40a9fc592b620c9372a9405d062be41385e4e25820b2268d`.
- Observed success: after permissions were restored, `reopen` exited 0. A fresh
  read found `status: pending`, `The prior gate verified the original graph.`,
  `## Invalidation`, `qa/qa-report-2026-09-19-01.md`, and `task_05`. The
  report SHA-256 remained
  `d841d1dc7f322b2d40a9fc592b620c9372a9405d062be41385e4e25820b2268d`.
- Result: pass.

### R4

```yaml
inputs:
  - kind: repository_path
    ref: cmd/roundfix/**
  - kind: repository_path
    ref: internal/cli/**
  - kind: repository_path
    ref: internal/spec/**
  - kind: repository_path
    ref: internal/store/**
```

- Actor: maintainer using the local repair command outside a Run.
- Entry point and surface: built `roundfix reopen --spec <slug>` CLI in a
  scratch Git repository with a local bare remote.
- Steps: snapshot HEAD, remote refs, working-tree state, and Roundfix state;
  reopen a stale gate; compare the snapshots.
- Expected observable: no Run database or journal appears, HEAD and remote refs
  do not move, and only the QA Task changes.
- Independent confirmation: Git and filesystem reads after the process exits.
- Persistence check: state inspection runs from fresh processes.
- Evidence path: this report, R4 command transcript.
- Observed: before and after reopen, HEAD stayed
  `3b3f38b0af5411a5393bbdb5425a594cbd158b99`; the bare remote stayed at the
  same `refs/heads/main`; no Roundfix database appeared under the isolated
  home; and `git status --short` named only
  `docs/specs/0149-a-supported-way-to-reopen-a-settled-gate/task_04.md`.
  The current focused suite also passed
  `TestReopenRefusesWhileARunIsActive`, which uses the shared `settle` guard
  and proves an active Run leaves the QA Task unchanged.
- Result: pass.

### R5

```yaml
inputs:
  - kind: repository_path
    ref: .agents/skills/roundfix/SKILL.md
  - kind: repository_path
    ref: skills/roundfix/SKILL.md
  - kind: repository_path
    ref: docs/user-guide/commands.md
  - kind: repository_path
    ref: docs/specs/0149-a-supported-way-to-reopen-a-settled-gate/_authorization.md
  - kind: repository_path
    ref: docs/specs/0149-a-supported-way-to-reopen-a-settled-gate/task_03.md
```

- Actor: maintainer relying on the shipped skill and bounded tooling authority.
- Entry point and surface: built `roundfix skills check`, skill mirror
  comparison, command help, and Git commit-path audit.
- Steps: validate the shipped skill, confirm both copies and the user guide name
  the command and refusals, and inspect Task commits against the operative
  authorization revision.
- Expected observable: skill checks pass, mirror bytes match, documentation
  names the public contract, and governed mutations stay inside the grant.
- Independent confirmation: Git ancestry and `diff-tree` path inspection rather
  than Task prose.
- Persistence check: rerun the read-only checks from the audited tree.
- Evidence path: this report, R5 command transcript.
- Observed: the built CLI's `skills check` exited 0; `diff -r
  .agents/skills/roundfix skills/roundfix` exited 0; both skill copies and the
  user guide name `roundfix reopen --spec <slug>`, unsettled and non-stale
  refusals, retained QA Report and Result evidence, options, and exit codes.
  The built command's `reopen --help` output states the same contract.
- Git audit: `b846aee0` changed Task 01 plus `internal/spec`; `5c5a7778`
  changed Task 02 plus `internal/cli` and `internal/spec`; `f71c6fb8` changed
  Task 03, the ordinary user guide, and only the two governed skill paths;
  `82894aee` changed Task 05 plus ordinary `internal/cli` and `internal/spec`
  paths. Authorization revision `e9dd66ff` is an ancestor of `f71c6fb8`, and
  its record is byte-identical through HEAD. No governed path falls outside the
  grant, and no consequent-fix declaration applies.
- Result: pass.

### R6

```yaml
inputs:
  - kind: repository_path
    ref: docs/specs/0149-a-supported-way-to-reopen-a-settled-gate/**
```

- Actor: maintainer relying on the Spec's promise-to-Task traceability.
- Entry point and surface: `roundfix spec check
  0149-a-supported-way-to-reopen-a-settled-gate --strict` plus targeted
  inspection for skipped detectors.
- Steps: run the strict checker, retain its skipped-detector list, and inspect
  the affected local artifact boundaries.
- Expected observable: no promise-rule finding; every declared promise names a
  consuming Task and no skipped detector hides an applicable broken reference.
- Independent confirmation: Task Graph and artifact paths resolve from a fresh
  filesystem read.
- Persistence check: rerun from the same audited tree.
- Evidence path: this report, R6 command transcript and Mechanical skips.
- Observed: the strict checker exited 0 with no findings. Manual follow-up found
  no active rollup, only the backlog placeholder, and no adopted reference
  index. The absent Vocabulary Contract hides no new competing domain term:
  the emitted `reopened`, `invalidated`, `not completed`, `not stale`, `Active
  Run`, and `invalid Spec slug` language is used consistently by the command,
  its tests, the Task evidence, and the user guide. The Spec's linked
  authorization and TechSpec paths resolve.
- Result: pass.

### R7

```yaml
inputs:
  - kind: repository_path
    ref: Makefile
  - kind: repository_path
    ref: '**/*.go'
  - kind: repository_path
    ref: go.mod
  - kind: repository_path
    ref: go.sum
```

- Actor: daemon verifying the assembled repository.
- Entry point and surface: repository Verification `make verify`.
- Steps: record the daemon-supplied execution fact without rerunning the
  command.
- Expected observable: exit 0 with diagnostics removed on success.
- Independent confirmation: supervised daemon result in the QA assignment.
- Persistence check: not applicable to a supervised static-gate fact.
- Evidence path: this report, Static gate.
- Observed: the assignment reports `make verify` exited 0 and that diagnostics
  were removed on success.
- Result: pass.

### R8

```yaml
inputs:
  - kind: live_service
    ref: Pull Request for feat/0149-reopen-settled-gate
```

- Actor: reviewer evaluating Pull Request controls.
- Entry point and surface: read-only Pull Request observation, replaced by
  equivalent evidence because the assignment confirms no Pull Request is open.
- Steps: account for approval, checks and status, unresolved review threads,
  Merge-Ready acceptance, and review-artifact ancestry.
- Expected observable: the environment cause is explicit and every unavailable
  Pull Request control has an equivalent observed or supervised fact.
- Independent confirmation: QA assignment fact plus local head and artifact
  inspection.
- Persistence check: live Pull Request state is unavailable in this
  pre-Pull-Request gate.
- Evidence path: this report, R8 command transcript and Blocked and skipped.
- Environment cause: the assignment states `Pull Request: none open; Pull
  Request journeys are environment-blocked.` The Run Worktree branch is never
  pushed and has no Pull Request of its own, so this gate did not try to resolve
  a Pull Request from it.
- Equivalent controls: no approval has been obtained; repository Verification
  passed at exit 0; no Pull Request exists to carry unresolved review threads;
  no Merge-Ready acceptance exists yet; no review artifact exists under the
  Spec or a repository `.roundfix/review` path; the audited head is
  `82894aee9c67bf6f9faf5b5541ecaf11474e5d27`, so there is no review-artifact
  ancestry claim to make.
- Unblocking action: after this gate and the configured pre-PR review complete,
  open the Pull Request on `feat/0149-reopen-settled-gate`, then observe the
  live controls there.
- Result: blocked (environment: no open Pull Request), with equivalent evidence
  for every enumerated control.

## Mechanical skips

| Detector | Missing artifact |
| --- | --- |
| authorization bounded paths | Task commits |
| consequent-fix commit order | consequent-fix declaration |
| QA evidence paths | docs/specs/0149-a-supported-way-to-reopen-a-settled-gate/qa/evidence |

The strict precondition also skipped `SC-ROLLUP-MEMBER` because no active
rollup exists, `SC-BACKLOG-UNMOVED` because no backlog entry exists, the
vocabulary documentation detector because the TechSpec has no Vocabulary
Contract, and `SC-REF-UNRESOLVED` because the Spec has no
`references/_index.md`. R6 records the required manual follow-up.

## Findings

None.

## Blocked and skipped

- R8 is blocked by the confirmed absence of an open Pull Request. The
  equivalent observed and supervised controls are complete, so this environment
  block does not prevent a pass.
- No matrix row was skipped, finding-blocked, or declared-blocked.

## Coverage

- Sources covered: Requirements 1-8, repository Verification,
  outside-evidence row, and Pull Request row.
- Results: 7 pass, 1 blocked (environment), 0 fail, 0 finding-blocked, 0
  declared-blocked, 0 skipped, 0 pending.
- No PRD `Unreachable Acceptance` declaration or frontend sweep applies.
- High-risk probes exercised: stale deep reopen in a new process, invalid
  out-of-order reopen against a pending gate, healthy-gate refusal, path-like
  slug rejection, symlink confinement, active-Run refusal, atomic-write
  failure, directory byte preservation, evidence persistence, and publication
  side-effect isolation.
- Non-Goals checked through the public flow: reopen did not execute QA, did not
  decide whether corrective work was justified, did not change stale-gate
  detection, and did not create a Run or publish Git state.

## Final verdict

Pass: every runnable criterion passed, the single environment-blocked Pull
Request row records its cause and equivalent evidence for all controls, and no
row failed, remained pending, was skipped, or was blocked by a finding or
declaration.
