---
spec: 0149-a-supported-way-to-reopen-a-settled-gate
date: 2026-09-19
build: f71c6fb80b9f0d39021f6b03e863bb786f3b14d4
auditing_binary: "0.14.1 (7541e463, built 2026-09-19 13:16:04 -0300)"
auditor_staleness: "current: commit ancestry: build commit does not predate audited tree"
status: closed
verdict: pass
rows_blocked_environment: 1
rows_blocked_finding: 0
rows_blocked_declared: 0
surfaces: [backend, cli, docs]
---

# QA Report

## Scope and environment

This full gate audited commit `f71c6fb80b9f0d39021f6b03e863bb786f3b14d4` on the Run Worktree branch. The strict precondition `roundfix spec check 0149-a-supported-way-to-reopen-a-settled-gate --strict` exited 0 with no findings. The CLI under test was built from the audited tree with `go build -buildvcs=false -o <scratch>/roundfix ./cmd/roundfix` and exercised in four committed scratch repositories through new processes.

The intended actors were a maintainer recovering a stale gate and a reviewer evaluating the pre-Pull-Request controls. The declared surfaces were backend, CLI, and docs. No production data or credentials were used. Pull Request journeys were environment-blocked because the assignment states that no Pull Request is open; the equivalent controls are recorded in R8.

## Static gate

- `make verify`: pass, exit 0. The Roundfix Daemon ran this command outside the Agent sandbox and removed diagnostics on success. Per the assignment, this gate recorded that supervised result and did not rerun repository Verification.
- `roundfix spec check 0149-a-supported-way-to-reopen-a-settled-gate --strict`: pass, exit 0, `No findings. Authored Verification commands were not executed.` The checker skipped the four absent-artifact detectors recorded in R6.

## Performed repairs

None.

## Assigned repair failures

None.

## Authorization audit inputs

| Task | Outcome | Record | Revision | Detail |
| --- | --- | --- | --- | --- |
| task_03 | granted | docs/specs/0149-a-supported-way-to-reopen-a-settled-gate/_authorization.md | e9dd66ff27fed038526249943295d88ad8493715 | operative grant |

## Mechanical findings

None.

## Results

| # | Status | Provenance |
| - | --- | --- |
| R1 | pass | Requirement 2; outside-evidence row |
| R2 | pass | Requirement 3; Requirement 4 |
| R3 | pass | Requirement 5 |
| R4 | pass | Requirement 6 |
| R5 | pass | Requirement 7 |
| R6 | pass | Requirement 8 |
| R7 | pass | Requirement 1; repository Verification |
| R8 | blocked (environment: no open Pull Request) | Pull Request row |

## Planned row evidence

### R1

```yaml
inputs:
  - kind: repository_path
    ref: cmd/roundfix/**
  - kind: repository_path
    ref: internal/cli/**
  - kind: repository_path
    ref: internal/spec/**
  - kind: repository_path
    ref: docs/history/specs/0148-*/**
```

- Actor: maintainer recovering a Spec whose settled QA gate is stale.
- Entry point and surface: built `roundfix reopen --spec <slug>` CLI.
- Steps: reproduce the pre-existing stale-gate refusal through a separate public command, run `reopen`, then invoke the refusing command again in a new process.
- Expected observable: `reopen` exits 0, changes the QA Task to `pending`, and the second public command passes the stale-gate validation point.
- Independent confirmation: a fresh process reads the Spec after reopen; repository history from Spec 0148 independently establishes the prior unsupported recovery gap.
- Persistence check: the reopened status remains visible to the fresh process.
- Evidence path: this report, R1 command transcript.
- Observed: before reopen, `roundfix archive demo` exited 2 with `QA gate result is invalidated ... dependencies are not completed: task_01`. `roundfix reopen --spec demo` then exited 0 with `reopened task_02 pending — invalidated qa/qa-report-2026-09-18.md`. A new `roundfix archive demo` process reached the later archive eligibility check and refused only because `task_01` remained `pending`; the stale-gate refusal was gone.
- Outside evidence: repository commit `eaae7abc10482c472f05695be4fc8628a50da56e` changed Spec 0148's settled `task_04` from `status: failed` to `status: pending` while adding corrective work. That historical diff is outside Spec 0149 and independently records the manual gate-state edit this command replaces.
- Result: pass.

### R2

```yaml
inputs:
  - kind: repository_path
    ref: cmd/roundfix/**
  - kind: repository_path
    ref: internal/cli/**
  - kind: repository_path
    ref: internal/spec/**
```

- Actor: maintainer attempting to reopen a pending gate and a healthy completed gate.
- Entry point and surface: built `roundfix reopen --spec <slug>` CLI.
- Steps: digest each fixture, run the refused command, and digest each fixture again.
- Expected observable: both calls exit 2, name the failed condition, and leave their Spec directories byte-identical.
- Independent confirmation: compare directory digests before and after each refusal.
- Persistence check: re-run the digest from a separate command after each process exits.
- Evidence path: this report, R2 command transcript.
- Observed, pending gate: the command exited 2 with `terminal QA Task "task_02" is not completed (status: pending)`; `git diff --exit-code -- docs/specs/demo` exited 0 and the repository stayed clean.
- Observed, healthy completed gate: the command exited 2 with `terminal QA Task "task_02" is not stale; every dependency is completed`; the same byte comparison exited 0 and the repository stayed clean.
- Result: pass.

### R3

```yaml
inputs:
  - kind: repository_path
    ref: cmd/roundfix/**
  - kind: repository_path
    ref: internal/cli/**
  - kind: repository_path
    ref: internal/spec/**
```

- Actor: maintainer preserving the evidence of an invalidated verdict.
- Entry point and surface: built `roundfix reopen --spec <slug>` CLI and filesystem-visible Spec artifacts.
- Steps: capture the QA Report bytes and prior Result text, reopen the stale gate, then re-read both artifacts.
- Expected observable: the report digest is unchanged; the prior Result remains; a dated invalidation record names the report and stale dependency.
- Independent confirmation: byte digest plus focused text checks after a fresh read.
- Persistence check: evidence is re-read after the CLI process exits.
- Evidence path: this report, R3 command transcript.
- Observed: the report's working-tree object id and committed object id were both `45bc7c115ea3c4b22f13b35c042e8343131f61c6`. A fresh read of the QA Task retained `The prior gate verified the original graph.` and added `## Invalidation`, `qa/qa-report-2026-09-18.md`, and `task_01`.
- Result: pass.

### R4

```yaml
inputs:
  - kind: repository_path
    ref: cmd/roundfix/**
  - kind: repository_path
    ref: internal/cli/**
  - kind: repository_path
    ref: internal/spec/**
```

- Actor: maintainer using the local repair command outside a Run.
- Entry point and surface: built `roundfix reopen --spec <slug>` CLI in a scratch Git repository with a local bare remote.
- Steps: snapshot HEAD, remote refs, repository paths, and Roundfix state; reopen a stale gate; compare the snapshots.
- Expected observable: no Run database or journal appears, HEAD and remote refs do not move, and only the QA Task changes.
- Independent confirmation: Git and filesystem reads after the process exits.
- Persistence check: repeat state inspection from fresh processes.
- Evidence path: this report, R4 command transcript.
- Observed: before and after the successful reopen, HEAD stayed `2fec4baeae55fdf173c246e661ee19a6b7edeaea`; the bare remote stayed at the same `refs/heads/main`; Run rows and joined Run Event rows for the scratch repository both stayed `0`; and no repository `.roundfix` path appeared. `git status --short` named only `docs/specs/demo/task_02.md`.
- Result: pass.

### R5

```yaml
inputs:
  - kind: repository_path
    ref: .agents/skills/roundfix/SKILL.md
  - kind: repository_path
    ref: skills/roundfix/SKILL.md
  - kind: repository_path
    ref: docs/specs/0149-a-supported-way-to-reopen-a-settled-gate/_authorization.md
  - kind: repository_path
    ref: docs/specs/0149-a-supported-way-to-reopen-a-settled-gate/task_03.md
```

- Actor: maintainer relying on the shipped skill and bounded tooling authority.
- Entry point and surface: `roundfix skills check`, skill mirror comparison, and Git commit-path audit.
- Steps: validate the shipped skill, confirm both copies name the command and refusals, and inspect Task commits against the operative authorization revision.
- Expected observable: skill checks pass, mirror bytes match, documentation names the public contract, and governed mutations stay inside the grant.
- Independent confirmation: Git ancestry and `diff-tree` path inspection rather than Task prose.
- Persistence check: not applicable to read-only repository evidence; rerun from the same audited tree.
- Evidence path: this report, R5 command transcript and preserved authorization audit inputs.
- Observed: the built CLI's `skills check` exited 0; `diff -r .agents/skills/roundfix skills/roundfix` exited 0; both copies and the user guide name `roundfix reopen --spec <slug>`, its unsettled and non-stale refusals, and retained evidence.
- Git audit: `b846aee0` changed Task 01 plus `internal/spec`; `5c5a7778` changed Task 02 plus `internal/cli` and `internal/spec`; `f71c6fb8` changed Task 03, the ordinary user guide, and only the two governed skill paths. Revision `e9dd66ff` is an ancestor of `f71c6fb8`, and the operative authorization record is byte-identical from that revision through HEAD. No governed path falls outside the grant.
- Result: pass.

### R6

```yaml
inputs:
  - kind: repository_path
    ref: docs/specs/0149-a-supported-way-to-reopen-a-settled-gate/**
```

- Actor: maintainer relying on the Spec's promise-to-Task traceability.
- Entry point and surface: `roundfix spec check 0149-a-supported-way-to-reopen-a-settled-gate --strict` plus targeted inspection for skipped detectors.
- Steps: run the strict checker, retain its skipped-detector list, and inspect the affected local artifact boundaries.
- Expected observable: no promise-rule finding; every declared promise names a consuming Task and no skipped detector hides an applicable broken reference.
- Independent confirmation: Task Graph and artifact paths resolve from a fresh filesystem read.
- Persistence check: not applicable to read-only repository evidence; rerun from the same audited tree.
- Evidence path: this report, R6 command transcript and Mechanical skips.
- Observed: the strict checker exited 0 with no findings. Manual follow-up found no active rollup, only the backlog placeholder, and no adopted reference index to resolve. The absent Vocabulary Contract hides no new competing domain term: the emitted `reopened`, `invalidated`, `not completed`, and `not stale` language is stated consistently in `internal/cli/reopen.go`, the TechSpec, and the user guide. The Spec's linked authorization and TechSpec paths both resolve.
- Result: pass.

### R7

```yaml
inputs:
  - kind: repository_path
    ref: Makefile
  - kind: repository_path
    ref: '**/*.go'
  - kind: repository_path
    ref: go.mod
  - kind: repository_path
    ref: go.sum
```

- Actor: daemon verifying the assembled repository.
- Entry point and surface: repository Verification `make verify`.
- Steps: record the daemon-supplied execution fact without rerunning the command.
- Expected observable: exit 0 with diagnostics removed on success.
- Independent confirmation: supervised daemon result in the QA assignment.
- Persistence check: not applicable to a supervised static-gate fact.
- Evidence path: this report, Static gate.
- Observed: the assignment reports `make verify` exited 0 and that diagnostics were removed on success.
- Result: pass.

### R8

```yaml
inputs:
  - kind: live_service
    ref: Pull Request for feat/0149-reopen-settled-gate
```

- Actor: reviewer evaluating Pull Request controls.
- Entry point and surface: read-only Pull Request observation, replaced by equivalent evidence because the assignment confirms no Pull Request is open.
- Steps: account for approval, checks and status, unresolved review threads, Merge-Ready acceptance, and review-artifact ancestry.
- Expected observable: the environment cause is explicit and every unavailable Pull Request control has an equivalent observed or supervised fact.
- Independent confirmation: QA assignment fact plus local head and review-artifact inspection.
- Persistence check: live Pull Request state is unavailable in this pre-Pull-Request gate.
- Evidence path: this report, R8 command transcript and Blocked and skipped.
- Environment cause: the assignment states `Pull Request: none open; Pull Request journeys are environment-blocked.` The Run Worktree branch is not a publication branch, so this gate did not attempt to resolve a Pull Request from it.
- Equivalent controls: no approval or acceptance has been obtained; repository Verification passed at exit 0; no Pull Request exists to carry unresolved review threads; no Merge-Ready acceptance exists yet; no review artifact exists under the Spec or `.roundfix/review`; the audited head is `f71c6fb80b9f0d39021f6b03e863bb786f3b14d4`.
- Unblocking action: open the Pull Request on `feat/0149-reopen-settled-gate` after the gate and configured pre-PR review complete, then observe the live controls there.
- Result: blocked (environment: no open Pull Request), with equivalent evidence for every enumerated control.

## Mechanical skips

| Detector | Missing artifact |
| --- | --- |
| consequent-fix commit order | consequent-fix declaration |
| QA Report structure | QA Report |
| QA evidence paths | QA Report |

The strict precondition also skipped `SC-ROLLUP-MEMBER` because no active rollup exists, `SC-BACKLOG-UNMOVED` because no backlog entry exists, the vocabulary documentation detector because the TechSpec has no Vocabulary Contract, and `SC-REF-UNRESOLVED` because the Spec has no `references/_index.md`. R6 records the required manual follow-up.

## Findings

None.

## Blocked and skipped

- R8 is blocked by the confirmed absence of an open Pull Request. The equivalent observed and supervised controls are complete, so this environment block does not prevent a pass.
- No matrix row was skipped, finding-blocked, or declared-blocked.

## Coverage

- Sources covered: Requirements 1-8, repository Verification, outside-evidence row, and Pull Request row.
- Results: 7 pass, 1 blocked (environment), 0 fail, 0 finding-blocked, 0 declared-blocked, 0 skipped, 0 pending.
- No PRD `Unreachable Acceptance` declaration or frontend sweep applies.
- High-risk probes exercised: stale deep reopen in a new process, invalid out-of-order reopen against a pending gate, healthy-gate refusal, directory byte preservation, evidence persistence, and publication-side-effect isolation.
- Non-Goals checked through the public flow: reopen did not execute QA, did not decide whether corrective work was justified, did not change stale-gate detection, and did not create a Run or publish Git state.

## Final verdict

Pass: every runnable criterion passed, the single environment-blocked Pull Request row records its cause and equivalent evidence for all controls, and no row failed, remained pending, was skipped, or was blocked by a finding or declaration.
