---
spec: 0090-a-gate-that-could-have-failed
date: 2026-08-09
build: e2b61c39a2833b013c7df6a6657bb1e23333347e
status: closed
verdict: fail
rows_blocked_environment: 0
rows_blocked_finding: 0
rows_blocked_declared: 0
surfaces: [backend, cli]
---

# QA report — A gate that could have failed

## Scope and environment

Full rerun of the authored terminal gate for `task_07` on Run Worktree build
`e2b61c39a2833b013c7df6a6657bb1e23333347e`. The intended actor is a Roundfix
maintainer or Supervisor using the built CLI and Run Event Stream. Runtime
journeys use isolated repositories under
`qa/evidence/2026-08-09-spec-0090-rerun-01/` and must not mutate another
repository. The prompt states that no Pull Request is open; the Spec declares
no Pull Request journey, so no promised row depends on one.

This rerun starts with prior finding F-002 and its adjacent positive-path canary,
then executes the remaining matrix against one build. Every row was opened as
`pending` before static or runtime execution. High-risk probes are: a gate that
passes unchanged, several offending commands around an observed non-zero
command, an unobservable executable, a command that fails before work and passes
after it, a no-op Agent, and post-terminal event reopening.

The authoring precondition `roundfix spec check
0090-a-gate-that-could-have-failed --strict` exited 0 with no findings.
`SC-REF-UNRESOLVED` was skipped because the Spec has no `references/_index.md`,
so outside-evidence reachability remains in Q12.

The isolated QA repositories used local `ma/` branches and disabled commit
signing only inside those scratch repositories. After execution, their nested
Git metadata and incidental runtime caches were moved outside the evidence root
so the Daemon sees regular artifacts rather than creating another Gitlink.

## Static gate

The required authoritative command is `make verify`; the two determinism rows
each clear the repository-local Go test cache first and run the gate unpiped.
Cold run 1 cleared the local cache and exited 2 at the test target. Every Go
package line was uncached. `internal/spec` repeated prior F-003's 27 additions
and two regressions in the coverage-equivalence corpus. `skills` found five
tracked QA evidence paths stored as Gitlinks (mode `160000`) and failed while
reading `failure-repo` as a file; this is F-006. All other packages, including
the corrected full `internal/cli`, passed. Cold run 2 cleared the same cache,
kept HEAD `e2b61c39a2833b013c7df6a6657bb1e23333347e`, reported no cached package,
and exited 2 with the same two failing packages and diagnostics.

## Results

| # | Story / criterion / sweep | Actor and surface | Steps, expected observable, confirmation, persistence, and evidence | Status |
| - | --- | --- | --- | --- |
| Q01 | Prior F-002 regression: corrected CLI fixtures retain Implement journeys | Maintainer; built test-facing CLI journeys; CLI/backend | Authorized full-access `GOCACHE="$PWD/.gocache" go test ./internal/cli -run '^(TestRunImplementExecutesSpecEndToEnd\|TestRunImplementAgentThatDoesNoWorkStillFailsVerification\|TestAgentSelectionProfilesMacro)$' -count=1 -v` exited 0: the end-to-end and no-op journeys passed, and all three mixed-profile macro subtests passed. A second full `go test ./internal/cli -count=1` exited 0 (`ok roundfix/internal/cli 53.994s`). The initial sandboxed attempt was denied access to `api.github.com`; the full-access executions removed that environment boundary. | pass |
| Q02 | Adjacent canary: positive control and ordinary path remain intact | Supervisor; built `roundfix implement`; CLI/backend | Isolated Run `run_20260809T223807Z_94d525063181dcf9` used `test -f "$(git rev-parse --git-path roundfix-qa-agent-task_01.done)"`: it failed on the unchanged tree, dispatched one controlled Agent prompt, passed post-Agent Verification attempt 1, committed `20ef838`, settled completed, and reached Clean. A fresh `roundfix events ... --filter task-status,verification,outcome` read returned cursors 2, 6–10, and 15–16 with started, command-passed, passed verdict, completed, and Clean. The prompt marker exists, the integrated task reads `status: completed`, and the isolated checkout is clean. Evidence: `qa/evidence/2026-08-09-spec-0090-rerun-01/{positive-repo,positive-home,positive-prompt-started}`. | pass |
| Q03 | Authored QA graph, completed dependencies, Project Constraints, and unresolved-reference fallback | Maintainer; Spec artifacts; backend/governance | `roundfix spec check 0090-a-gate-that-could-have-failed --strict` exited 0 with no findings. `_tasks.md` names `task_07` as QA and gives it all four leaves (`task_04`, `task_05`, `task_06`, `task_08`); tasks 01–06 and 08 read `completed`, while only the Daemon-owned QA task remains `pending`. Direct reads find all four required Project Constraint rows in both PRD and TechSpec and their operative agent-guide sources. `SC-REF-UNRESOLVED` was explicitly skipped for the absent references index, so Q12 retains that live obligation. | pass |
| Q04 | Protected-tooling authorization and changed-path audit | Maintainer; Git ancestry; backend/governance | `git diff-tree --no-commit-id --name-only -r` exited 0 for Task commits `d73ddc40`, `f145c3b5`, `2579d067`, `e7da557a`, `961e2fc8`, `b276882d`, and corrective implementation `e2b61c39`; every path is inside its Task's bounded list. Corrective authoring commits `365a8983`, `41f70d38`, and `6bd39870` touch only `CONTEXT.md` and Spec artifacts. No commit mutates protected tooling configuration, scripts, ignore files, plugin declarations, or version pins, so no tooling authorization choreography applies. Current status contains only this report and its isolated QA evidence root. | pass |
| Q05 | Goal 3: first genuinely cold authoritative gate | Maintainer; `make verify`; CLI/backend | `GOCACHE="$PWD/.gocache" go clean -testcache` exited 0, then unpiped `GOCACHE="$PWD/.gocache" make verify` exited 2. No package line reported `(cached)`. `internal/spec` repeated prior F-003's stale coverage-equivalence list; `skills` failed F-006 because commit `6bb1d709` recorded five isolated QA repositories as mode-`160000` Gitlinks and `TestOwnedSkillEditLeavesDerivedArtifactsByteIdentical` tried to read `failure-repo` as a file. All other packages, including `internal/cli`, passed. | fail |
| Q06 | Goal 3: same gate, same unchanged tree, second cold verdict | Maintainer; `make verify`; CLI/backend | A second local-cache clear exited 0 and a second unpiped `make verify` exited 2 with no `(cached)` package. HEAD remained `e2b61c39a2833b013c7df6a6657bb1e23333347e`; the same 27 coverage additions, two coverage regressions, and mode-`160000` `failure-repo` error failed exactly `internal/spec` and `skills`, while every other package passed. The gate is deterministic on the unchanged build but not green. See repeated F-003 and F-006. | fail |
| Q07 | Goal 1: vacuous Verification is refused before an Agent turn | Supervisor; built `roundfix implement`; CLI/backend | Run `run_20260809T224810Z_b1e9d9e2e0f4eee6` carried Spec 0089's exact `grep -q 'reasoning_effort: xhigh' .roundfixrc.yml`, an observed non-zero middle command, and a second vacuous command. It exited 1 before dispatch; a post-terminal event read records both offending commands in one `verification_vacuous` event at cursor 2, failed settlement at cursor 3, and Unresolved at cursor 5. The Run Worktree task reads `status: failed`, and `vacuous-prompt-started` is absent. | pass |
| Q08 | Goal 2: recorded evidence can contradict Task narration | Supervisor; built `roundfix events`; CLI | Q07's source task states that the Agent ran and completed every change. The retained Run Worktree preserves that prose, while a fresh public event read independently records work item `task_01`, both offending commands, `verification_vacuous`, failed settlement, and no prompt marker. Both contradictory records remained readable after process exit under `qa/evidence/2026-08-09-spec-0090-rerun-01/{journey-home,journey-worktrees}`. | pass |
| Q09 | Goal 4: unobserved Verification is `unknown`, distinct from observed non-zero | Supervisor; built `roundfix implement` and `events`; CLI/backend | Run `run_20260809T224940Z_43e961aecdbb7df2` withheld `sh` from PATH while retaining Git and the proved runtime. Its reopened cursor-2 event records `verification_unknown`, the exact command, `exec: "sh": executable file not found in $PATH`, and `batch-001-probe-1.log`; cursor 3 settles failed, and the Agent prompt marker is absent. Run `run_20260809T224950Z_cce888a8a24f44f1` instead dispatched two no-op Agent turns and recorded observed command failures/verdict failures at cursors 8–9 and 13–14 before failed settlement at cursor 15. Its prompt marker exists. Both reopened streams reach Unresolved with distinct terminal evidence. | pass |
| Q10 | Three controls and ADR-0110: negative control is authored and recorded, never synthesised | Maintainer/Supervisor; built CLI over isolated Spec; CLI/backend | The isolated vacuous task preserves two ordered `## Negative Control` declarations and Roundfix synthesises no mutation. Fresh `go test ./internal/spec -run '^TestNegativeControl' -count=1 -v` passes ordered parsing, absent-section validity, Verification preservation, and declaration count after Task reload. However, Q07's public Run Event carries neither declaration count nor whether a control was exercised, observed surface, or update time. Fresh source search finds `NegativeControl` only in `internal/spec` fields, parser, and tests. Prior F-005 therefore repeats. | fail |
| Q11 | Rubric predates implementation and probe chronology | Supervisor; Run Event Stream; backend | Reopened vacuous and unknown streams place the authored-command classification at cursor 2 and failed settlement at cursor 3, with no Agent prompt marker. The reopened positive stream instead records Task start at cursor 2, active Agent Selection at cursor 4, post-Agent Verification at cursors 6–9, and completed settlement at cursor 10; its prompt marker exists. The ordinary-failure stream similarly records post-Agent attempts at cursors 6–14. The persisted cursor order proves the probe reads the pre-authored rubric before Agent work and only cleared Tasks enter the Agent/post-Verification path. | pass |
| Q12 | ADR-0104 outside-evidence acceptance | Maintainer; Secondbrain source outside this Spec; docs/evidence | `/Users/marcio/dev/secondbrain/wiki/concepts/verificacao-adversarial-e-oraculos-de-agentes.md` is reachable outside the Spec, dated 2026-08-08, and names its own source trail. It supplies the imported claims that the mechanism deciding correctness must itself be tested and that explicit `unknown` is preferable to false confidence. It cites *Validation Evidence in LLM Repair Agents* (arXiv:2607.28871) and reports 3,730 events with 46.0% of comparable positives lacking bug-discriminating information. Q01–Q11's implementation/runtime claims remain supported only by fresh evidence from this repository, not by that external source. | pass |
| Q13 | Vocabulary Contract and `Negative Control` glossary check | Maintainer; `CONTEXT.md` and strict vocabulary checker; backend/docs | The strict Spec check exits 0, and direct reads find `verification_vacuous` at `CONTEXT.md:176` and `verification_unknown` at line 180. A fresh case-insensitive glossary search for the task_07-required coined term `Negative Control` exits 1 with no match. Prior F-004 is therefore narrowed but not resolved: emitted encodings are documented, while the coined concept remains absent. | fail |
| Q14 | CLI surface sweep: help, output, exit status, event filtering, reopen | Supervisor; built CLI; CLI | Built-binary root, `implement`, `events`, and `spec` help each exit 0 and document the exercised contracts. Positive Implement exits 0; vacuous, unknown, and ordinary failure exit 1; `events not-a-run` exits 2 with a concrete diagnostic. Reopened `events --filter task-status,verification,outcome` emits valid `roundfix-events/v1` JSONL with stable classifications, commands, diagnostics, cursors, and terminal outcomes. Q01's passing CLI regressions assert deterministic stdout reports and stderr diagnostics for the corrected Implement paths. All commands ran non-interactively with color/TUI disabled. | pass |
| Q15 | Backend sweep: classification, persistence, failure recovery | Supervisor; real Daemon Run lifecycle via CLI; backend | Four isolated real Runs persist distinct outcomes: vacuous and unknown fail before dispatch, ordinary no-op work fails after two observed attempts, and the positive control completes/Clean. Post-terminal event reads reproduce every classification and cursor sequence; retained failed Worktrees read `status: failed`, the integrated positive task reads `completed`, and the ordinary attempt-2 log resolves. The unknown process-start log is intentionally absent: `ExecVerifier` removes its temp file when no process starts, and `TestExecVerifierClassifiesProcessStartFailureAsUnknownVerdict` requires that absence while the event retains the reserved path and inline cause. No optimistic pass or vanished terminal state was observed. | pass |
| Q16 | Non-Goals and scope-creep audit | Maintainer; Git diff and public behavior; backend/CLI | Task-commit `diff-tree` evidence in Q04 shows Spec 0090 implementation touches only its bounded ordinary source/test/Spec paths; none retrofits an archived Spec, replaces the QA node, or introduces an independent checker. The later merge from `main` contains separately owned Spec 0093 authoring checks and is not attributed to this feature. Q07 mechanically refuses exit-zero commands without judging whether they test the right property, Q08 preserves inaccurate Result prose, and `_tasks.md` still names the terminal `task_07` QA node. All five Non-Goals remain intact. | pass |
| Q17 | QA report contract | Maintainer; this report; docs contract | The collision-safe same-day rerun path is `qa-report-2026-08-09-01.md`. Frontmatter closes with `verdict: fail` and exact zero counts for environment-, finding-, and declared-blocked rows. All 17 rows are terminal, every report-owned evidence root resolves as regular artifacts, and nested `.git` metadata was removed from the rerun root. Q15 explicitly records the product contract under which an unknown process-start destination is named but has no log file. The matrix covers every task_07 criterion. This child Agent made no commit, push, or Pull Request mutation in the Run Worktree. | pass |

## Findings

### F-003 (repeated) — Coverage-equivalence evidence remains stale

- Impact: Trust-Damage.
- Actor and journey: maintainer running Q05's authoritative cold gate.
- Expected: the coverage-equivalence corpus accounts for the current test
  identities introduced by the Spec and its corrective work.
- Actual: `TestCoverageEquivalence` still reports 27 additions and two
  regressions, including the renamed Verification probe cases, then fails
  `internal/spec`.
- Reproduction: clear the repository-local Go test cache and run unpiped
  `make verify` at `e2b61c39`.
- Affected rows: Q05 and Q06.

### F-006 — Committed QA evidence stores isolated repositories as Gitlinks

- Impact: Blocks-Completion.
- Actor and journey: maintainer running Q05's authoritative gate after the
  prior QA report was committed.
- Expected: evidence paths committed by the Daemon are regular readable
  artifacts, and the repository's skill-contract test can inspect every
  tracked file.
- Actual: commit `6bb1d709` records `failure-repo`, `positive-repo`,
  `unknown-path-repo`, `unknown-repo`, and `vacuous-repo` as mode-`160000`
  Gitlinks without a corresponding readable file contract.
  `TestOwnedSkillEditLeavesDerivedArtifactsByteIdentical` fails on the first
  path with `is a directory`.
- Reproduction: run `git ls-files --stage` for the five paths, then clear the
  local Go test cache and run `make verify`.
- Affected rows: Q05 and Q06.

### F-005 (repeated) — Negative-control health still stops at parsing

- Impact: Trust-Damage.
- Actor and journey: Supervisor reopening Q10's gate-health record after a
  real vacuous Run.
- Expected: ADR-0110 and the PRD require a public record of declared or
  exercised negative controls, observed surface, and evidence age beside the
  Task outcome.
- Actual: parser and reload tests retain two ordered declarations, but the
  `verification_vacuous` event exposes none of their count, exercise state,
  surface, or update time. Fresh source search finds `NegativeControl` only in
  `internal/spec` fields, parsing, and tests.
- Reproduction: run the Q07 isolated Spec with two declarations, reopen its
  events, run the focused `internal/spec` tests, and search `internal` for
  `NegativeControl|negative_control`.
- Affected rows: Q10.

### F-004 (repeated, narrowed) — `Negative Control` remains undocumented

- Impact: Trust-Damage.
- Actor and journey: maintainer reading the glossary required by Q13 and
  task_07.
- Expected: the coined `Negative Control` concept and both emitted Run Event
  encodings are documented in `CONTEXT.md`.
- Actual: `verification_vacuous` and `verification_unknown` now appear at
  lines 176 and 180, but a fresh case-insensitive search for `Negative Control`
  returns no match.
- Reproduction: run the strict Spec check, then search `CONTEXT.md` directly
  for all three vocabulary obligations.
- Affected rows: Q13.

## Blocked and skipped

None. The initial sandboxed focused CLI attempt was denied access to
`api.github.com`; authorized full-access reruns completed both the focused and
full package checks, so no row remains environment-blocked. The isolated
unknown-verdict journey intentionally withheld `sh` as its test input and is a
passed product row, not an environment block. No row is finding-blocked,
declared-blocked, or skipped. All three blocked-row counts are exactly zero.

## Coverage

All 17 planned rows are terminal: 13 pass and four fail. They cover all four PRD
goals, five Core Features, five Non-Goals, both declared surfaces, the
outside-evidence obligation, Project Constraints, commit-dependent tooling
scope, the prior F-002 regression, and all task_07 acceptance criteria. No row
is blocked or skipped. Findings comprise one Blocks-Completion and three
Trust-Damage defects.

## Final verdict

**Fail.** Corrective Task 08 resolves prior F-002 and every real vacuous,
unknown, ordinary-failure, and positive-control journey behaves as specified,
but the authoritative cold gate still fails on repeated F-003 and new F-006;
negative-control public evidence (F-005) and its glossary term (F-004) also
remain incomplete. Repair those four findings, keep future isolated QA
repositories out of Gitlink form, and rerun the full gate before Pull Request
preparation.
