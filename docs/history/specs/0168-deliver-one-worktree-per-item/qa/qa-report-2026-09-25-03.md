---
spec: 0168-deliver-one-worktree-per-item
date: 2026-09-25
build: 90cb19135a6a0bfac9ae30b7783ade097bdfbe97
auditing_binary: "0.16.0"
auditor_staleness: "current: declared tree version: auditing binary 0.16.0 does not predate 0.16.0"
status: closed
verdict: pass
rows_blocked_environment: 1
rows_blocked_finding: 0
rows_blocked_declared: 0
surfaces: [backend, cli, docs]
---

# QA Report

## Scope and environment

Full terminal rerun for Spec 0168 at
`90cb19135a6a0bfac9ae30b7783ade097bdfbe97` in the Run Worktree on macOS.
The intended actors are a delivery operator and a maintainer inspecting
persisted queue state, linked Git worktrees, CLI output and shipped
documentation. There is no open Pull Request; its row uses the
environment-blocked equivalent-evidence path.

The current-tree CLI is `/tmp/roundfix-qa0168-r3` (`roundfix 0.16.0`). The
installed `roundfix` on `PATH` is 0.15.0, so it is used only for the required
precondition check and not for feature acceptance. The seeded mechanical
result identifies the Auditing Binary as 0.16.0. The Daemon's repository
Verification result is recorded as a supervised gate fact and is not rerun.

## Static gate

- `roundfix spec check 0168-deliver-one-worktree-per-item --strict` with the
  installed 0.15.0 binary: exit 0, no findings, with four named skipped
  detectors retained in R05.
- `/tmp/roundfix-qa0168-r3 spec check
  0168-deliver-one-worktree-per-item --strict` with the current-tree 0.16.0
  binary: exit 0, no findings, with the same four named skipped detectors.
- Repository Verification: the Daemon ran `make verify-changed`; it passed
  with exit 0 and removed diagnostics on success. The gate did not rerun it.

## Performed repairs

None.

## Assigned repair failures

None.

## Authorization audit inputs

None.

## Mechanical findings

None.

## Results

| # | Story / criterion / sweep | Actor and surface | Status | Provenance | Evidence |
| - | --- | --- | --- | --- | --- |
| R01 | Persist one immutable branch/worktree pair, its provisioning state and migrate earlier schemas safely | maintainer; backend/store | pass | Requirement 2 | Three named store cases passed with persisted rereads and schema-18/schema-19 migration |
| R02 | Create one real-Git worktree per item from refreshed default, route every action there and leave a hostile dirty user checkout unchanged | delivery operator; CLI/backend | pass | Requirement 3; outside-evidence row | Three named cases passed against real temporary Git repositories; host Git 2.55.0 independently confirmed the worktree contract |
| R03 | Keep parked worktrees, print them, provision reuse/recreation, park missing branches and clean merged items without replay, queue blockage or data loss | delivery operator; CLI/backend | pass | Requirement 4 | Seventeen lifecycle, status, provisioning, migration, cleanup and data-preservation cases passed |
| R04 | Describe the item-worktree contract consistently in the canonical skill, mirror and user guide | delivery operator; docs/CLI | pass | Requirement 5 | Both reader surfaces contain every lifecycle promise; mirror identity and current-build skill check passed |
| R05 | Satisfy the Spec promise rule and manually close every detector skipped by strict Spec checking | maintainer; Spec governance | pass | Requirement 6 | Current-tree strict checker passed; direct queries closed promise, vocabulary, rollup, backlog and reference applicability |
| R06 | Record the Daemon's repository Verification result as a gate fact without rerunning it | maintainer; repository gate | pass | Requirement 1; repository Verification | Supervised fact: `make verify-changed` passed, exit 0, diagnostics removed on success |
| R07 | Prove governed skill changes stayed within prior authorization and valid commit chronology | maintainer; Git/tooling audit | pass | Requirement 6 | Current-ancestry commit paths, ordering and mirror identity match the prior grant; Tasks 06-08 add no governed path |
| R08 | Account for Pull Request approval, checks, threads, Merge-Ready evidence and review-artifact ancestry | maintainer; Pull Request | blocked (environment: no open Pull Request) | Pull Request row | Every control has equivalent observed or supervised evidence at `90cb19135a6a0bfac9ae30b7783ade097bdfbe97` |

## Row evidence

### R01

```yaml
inputs:
  - kind: repository_path
    ref: internal/store/delivery.go
  - kind: repository_path
    ref: internal/store/store.go
  - kind: repository_path
    ref: internal/store/delivery_test.go
```

- Steps: run the named store acceptance cases against the current build and
  require persistence to be confirmed after reopening the database.
- Expected: the first branch/worktree pair survives a second proposal;
  provisioning state survives ordinary stage updates; schema-18 and schema-19
  databases open at schema 20 with safe empty/false defaults.
- Independent confirmation: fresh public store reads after close/reopen.
- Observed: `go test -count=1 -v -run
  '^(TestDeliveryItemRecordsItsWorktreeWithItsBranch|TestOpenMigratesDeliveryQueueAddingItemWorktree|TestOpenMigratesDeliveryQueueAddingWorktreeProvisioning)$'
  ./internal/store` exited 0 and all three cases reported `PASS`. The cases
  reread the first immutable pair and provisioning marker, migrated schema 18
  with an empty worktree, migrated schema 19 with a false provisioning marker,
  and matched each migrated schema to schema 20.

### R02

```yaml
inputs:
  - kind: repository_path
    ref: internal/cli/deliver_workflow.go
  - kind: repository_path
    ref: internal/cli/deliver_test.go
  - kind: repository_path
    ref: internal/delivery/engine.go
  - kind: repository_path
    ref: internal/delivery/engine_test.go
  - kind: repository_path
    ref: internal/delivery/github.go
  - kind: external_repository
    ref: host Git public worktree and branch behavior exercised through independent temporary repositories
```

- Steps: run the real-Git creation and hostile-checkout cases and the action
  working-directory case; inspect branch, HEAD, porcelain status and sentinel
  contents after delivery actions.
- Expected: distinct linked worktrees start at refreshed default, item branches
  have no upstream, every action receives its item worktree, and the user's
  dirty non-default checkout is byte-for-byte unchanged.
- Independent confirmation: host Git porcelain and direct sentinel reads.
- Observed: the current-build focused run of
  `TestEachItemRunsInItsOwnWorktree`,
  `TestDeliverNeverTouchesTheUserCheckout` and
  `TestDeliveryActionsRunInTheItemWorktree` exited 0; all three named cases
  reported `PASS`. The CLI cases created real temporary Git repositories,
  compared branch, HEAD, porcelain status and tracked, ignored and nested-repo
  sentinel contents, and observed distinct item worktrees created from the
  refreshed default branch. The delivery case observed each action's working
  directory.
- Outside source: the host's independently maintained Git 2.55.0 executable.
  The real-Git cases used Git's own worktree, branch and porcelain operations
  as the oracle; the Spec did not author their implementation. `git --version`
  independently reported `git version 2.55.0`.

### R03

```yaml
inputs:
  - kind: repository_path
    ref: internal/cli/deliver.go
  - kind: repository_path
    ref: internal/cli/deliver_workflow.go
  - kind: repository_path
    ref: internal/cli/deliver_test.go
  - kind: repository_path
    ref: internal/delivery/engine.go
  - kind: repository_path
    ref: internal/delivery/engine_test.go
  - kind: repository_path
    ref: internal/worktree/worktree.go
  - kind: repository_path
    ref: internal/worktree/worktree_test.go
```

- Steps: run the real-Git park, status, reuse, recreation, provisioning,
  missing-branch, migrated-item, merged-cleanup, half-removed cleanup and
  registered-worktree preservation cases.
- Expected: lifecycle promises hold; registered worktrees on another branch or
  detached keep their uncommitted files; only a derived, truly half-removed
  item path is removed.
- Independent confirmation: fresh Git and CLI/store reads after transitions.
- Observed: the current-build run of
  `TestParkLeavesTheItemWorktreeInPlace`,
  `TestDeliverStatusPrintsTheItemWorktree`,
  `TestResumeUsesTheRecordedItemWorktree`,
  `TestResumeRecreatesAMissingItemWorktree`,
  `TestRecreatedItemWorktreeIsProvisioned`,
  `TestUnfinishedProvisioningIsCompletedOnReuse`,
  `TestResumeParksWhenTheItemBranchIsGone`,
  `TestAMergedItemLeavesNoWorktreeOrBranch`,
  `TestMigratedMergedItemDoesNotBlockResume`,
  `TestCleanupRecoversAHalfRemovedItemWorktree`,
  `TestCleanupPreservesAnUnregisteredPathWithoutThisRepositorysMarker`,
  `TestCleanupKeepsARegisteredItemWorktreeOnAnotherBranch`,
  `TestCleanupKeepsADetachedItemWorktree`,
  `TestCleanupPreservesAnUnregisteredPathWhileItsAdminDirectoryExists`,
  `TestCleanupPreservesAPathNotDerivedForTheItem`,
  `TestAFailedMergedCleanupDoesNotStopTheQueue` and
  `TestDeliveryEngineRetriesMergedItemCleanupWithoutReplayingMerge` exited 0;
  all seventeen named cases reported `PASS`. Fresh Git and store/CLI reads
  confirmed retained, recreated, provisioned and removed worktrees, guarded
  recovery of a truly half-removed derived path, preservation of registered,
  detached, live-admin and unowned paths with uncommitted content intact,
  queue progress after cleanup failure, and no stage or merge replay.

### R04

```yaml
inputs:
  - kind: repository_path
    ref: .agents/skills/roundfix/SKILL.md
  - kind: repository_path
    ref: skills/roundfix/SKILL.md
  - kind: repository_path
    ref: docs/user-guide/commands.md
```

- Steps: follow both reader surfaces, search for each promised state, compare
  skill copies byte-for-byte and run the current-build skill checker.
- Expected: both surfaces describe creation, untouched checkout, park, status,
  recreate-or-park and merged cleanup; the mirror is identical; skills pass.
- Independent confirmation: byte comparison and current-build CLI check.
- Observed: fixed-string searches found creation under `worktree.location`,
  the untouched and possibly dirty checkout, parked-item retention and status
  visibility, `item-worktree-missing`, and merged cleanup in both reader
  surfaces. `cmp -s .agents/skills/roundfix/SKILL.md
  skills/roundfix/SKILL.md` exited 0. `/tmp/roundfix-qa0168-r3 skills check`
  exited 0 and reported the Roundfix skill set passed.

### R05

```yaml
inputs:
  - kind: repository_path
    ref: docs/specs/0168-deliver-one-worktree-per-item/_prd.md
  - kind: repository_path
    ref: docs/specs/0168-deliver-one-worktree-per-item/_techspec.md
  - kind: repository_path
    ref: docs/specs/0168-deliver-one-worktree-per-item/_tasks.md
  - kind: repository_path
    ref: docs/specs/0168-deliver-one-worktree-per-item/task_*.md
  - kind: repository_path
    ref: docs/adr/*.md
```

- Steps: build and run the current-tree strict checker; inspect explicit
  promise-to-task references and Project Constraints; manually evaluate named
  skipped detectors.
- Expected: every declared promise has a consuming Task; applicable constraints
  and cited ADRs hold; skipped detectors are closed by direct repository facts.
- Independent confirmation: strict output plus direct cross-reference queries.
- Observed: the current-tree strict check exited 0 with no findings. Task 05's
  References name Goals 1-2, Core Features 1-7, Success Metrics 1-4, Testing
  Approach 1-5 and API Contracts 1-3, so every declared promise names a
  consuming Task. All ten cited ADRs carry accepted status, including ADR-0130
  whose inline frontmatter comment preserves `status: accepted`, and every
  Project Constraint source exists.
- Skipped-detector audit: no active or historical Finding, Rollup or Backlog
  Entry names Spec 0168; no adopted `references/` tree exists; the cited Spec
  0161 archive source resolves. The emitted blocker token
  `item-worktree-missing` appears in `internal/delivery/engine.go` and both
  public reader surfaces. These command-backed observations close
  `SC-ROLLUP-MEMBER`, `SC-BACKLOG-UNMOVED`, `SC-REF-UNRESOLVED` and the
  vocabulary detector that the strict checker explicitly skipped.

### R06

- Steps: transcribe the Daemon's supervised repository Verification command,
  exit status and diagnostics disposition exactly as supplied.
- Expected: `make verify-changed` passed with exit 0 and removed diagnostics.
- Independent confirmation: Daemon-owned prompt evidence; no QA rerun.
- Observed: the QA contract reports command `make verify-changed`, verdict
  `pass`, exit 0 and diagnostics removed on success. This is a supervised fact,
  not a QA rerun.

### R07

```yaml
inputs:
  - kind: repository_path
    ref: docs/specs/0168-deliver-one-worktree-per-item/_authorization.md
  - kind: repository_path
    ref: .agents/skills/roundfix/SKILL.md
  - kind: repository_path
    ref: skills/roundfix/SKILL.md
```

- Steps: resolve actual Task commit paths with `git diff-tree`; prove the
  authorization commit is earlier ancestry; classify governed paths; verify
  no late/folded prerequisite or consequent fix and reproduce mirror identity.
- Expected: the prior grant is separate and earlier; governed paths stay in
  its bounded set; corrective Tasks add no governed path; mirror copies are
  identical.
- Independent confirmation: Git commit objects and byte comparison.
- Observed: `git -c core.fsmonitor=false diff-tree --no-commit-id --name-only
  -r 67bc5617...` returned `.agents/skills/roundfix/SKILL.md`,
  `skills/roundfix/SKILL.md`, `docs/user-guide/commands.md` and `task_04.md`.
  The first two are exactly the authorized governed paths; the guide is
  ordinary documentation under ADR-0130, and the Task file is the assigned
  record. `git -c core.fsmonitor=false merge-base --is-ancestor a4485c3a...
  67bc5617...` exited 0, proving the separate current-ancestry authorization
  commit precedes the tooling change. Task 06's `11e3e34d...` commit contains
  its Task file and eight Go source/test paths; Task 07's `70c88992...` commit
  contains its Task file and four Go source/test paths; Task 08's
  `90cb1913...` commit contains its Task file and three Go source/test paths.
  No corrective Task touches a governed path. No prerequisite or
  consequent-fix declaration exists, and the skill copies are byte-identical
  after sanctioned regeneration.

### R08

```yaml
inputs:
  - kind: live_service
    ref: Pull Request state declared by the Roundfix QA prompt
```

- Steps: record the no-open-PR cause and account separately for approval,
  checks/status, unresolved review threads, Merge-Ready acceptance and
  review-artifact ancestry at the audited head.
- Expected: the row remains environment-blocked but carries equivalent
  observed or supervised evidence for every control.
- Independent confirmation: Run contract plus local authorization,
  verification and Git-head evidence.
- Cause: the Roundfix QA prompt states that no Pull Request is open and Pull
  Request journeys are environment-blocked. The Run Worktree branch is never a
  Pull Request branch, so the gate did not query or mutate GitHub.
- Equivalent controls:
  - Approval: no change acceptance has been obtained yet; the operative
    maintainer authorization at `a4485c3a...` grants this Spec's operations
    without fabricating review approval.
  - Checks and status: the Daemon-supervised `make verify-changed` result is
    pass, exit 0; the current-tree strict Spec check also exits 0.
  - Unresolved review threads: none can stand because the declared environment
    has no open Pull Request; no thread state beyond that cause is claimed.
  - Merge-Ready acceptance: none exists yet; it is recorded as absent, not as
    approval.
  - Review-artifact ancestry: no review artifact exists yet. The audited and
    claimed head is `90cb19135a6a0bfac9ae30b7783ade097bdfbe97`, confirmed by
    `git -c core.fsmonitor=false rev-parse HEAD`.
- Unblocking action: after the delivery workflow opens the Spec target branch's
  Pull Request, observe the same five controls read-only on its current head.
- Equivalent evidence conclusion: all five controls are explicitly accounted
  for, so this environment block qualifies for the pass policy without
  claiming review approval or Merge-Ready acceptance.

## Findings

None.

## Blocked and skipped

- R08 is blocked by the declared environment fact that no Pull Request is
  open. Equivalent observed or supervised evidence accounts for approval,
  checks/status, unresolved threads, Merge-Ready acceptance and
  review-artifact ancestry. The row is therefore eligible under the
  environment-blocked pass policy.
- No row is finding-blocked, declared-blocked or skipped.

## Coverage

Eight of eight rows reached terminal status: seven passed and one is
environment-blocked with complete equivalent evidence. The provenance is
closed over Requirements 1-6, the outside-evidence row, the Pull Request row
and repository Verification. No frontend sweep or PRD Unreachable Acceptance
source applies. `rows_blocked_environment: 1`, `rows_blocked_finding: 0`, and
`rows_blocked_declared: 0`.

## Mechanical skips

| Detector | Missing artifact |
| --- | --- |
| authorization bounded paths | Task commits |
| consequent-fix commit order | consequent-fix declaration |
| QA evidence paths | docs/specs/0168-deliver-one-worktree-per-item/qa/evidence |

## Final verdict

Pass: all seven runnable rows passed, R08 is environment-blocked with complete
equivalent evidence, and there are zero failed, finding-blocked,
declared-blocked or skipped rows and zero findings in every impact tier.
