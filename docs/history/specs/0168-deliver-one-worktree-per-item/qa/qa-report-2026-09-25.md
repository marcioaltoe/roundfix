---
spec: 0168-deliver-one-worktree-per-item
date: 2026-09-25
build: 682f5bece30a1a63904ec37df4e37f43dbc63a19
auditing_binary: "0.16.0"
auditor_staleness: "current: declared tree version: auditing binary 0.16.0 does not predate 0.16.0"
status: closed
verdict: pass
rows_blocked_environment: 1
rows_blocked_finding: 0
rows_blocked_declared: 0
surfaces: [backend, cli, docs]
---

# QA Report

## Scope and environment

Full terminal gate for Spec 0168 at `682f5bece30a1a63904ec37df4e37f43dbc63a19`
in the Run Worktree on macOS. The intended actors are a delivery operator and
a maintainer inspecting persisted queue state, linked Git worktrees, CLI
output and shipped documentation. There is no open Pull Request; its row uses
the environment-blocked equivalent-evidence path.

The built current-tree CLI is `/tmp/roundfix-qa0168` (`roundfix 0.16.0`). The
installed `roundfix` on `PATH` is 0.15.0, so it is not used for feature
acceptance. The Daemon's repository Verification result is recorded as a
supervised gate fact and is not rerun.

## Static gate

- `roundfix spec check 0168-deliver-one-worktree-per-item --strict` with the
  installed 0.15.0 binary: exit 0, no findings, with four named skipped
  detectors retained below.
- `/tmp/roundfix-qa0168 spec check 0168-deliver-one-worktree-per-item --strict`
  with the current-tree 0.16.0 binary: exit 0, no findings, with the same four
  named skipped detectors.
- Repository Verification: the Daemon ran `make verify-changed`; it passed with
  exit 0 and removed diagnostics on success. The gate did not rerun it.

## Performed repairs

None.

## Assigned repair failures

None.

## Authorization audit inputs

| Task | Outcome | Record | Revision | Detail |
| --- | --- | --- | --- | --- |
| task_04 | granted | docs/specs/0168-deliver-one-worktree-per-item/_authorization.md | b4de85f57ed4585fd9ac2e904dfd80cc54f503ba | operative grant |

## Mechanical findings

None.

## Results

| # | Story / criterion / sweep | Actor and surface | Status | Provenance | Evidence |
| - | --- | --- | --- | --- | --- |
| R01 | Persist one immutable branch/worktree pair and migrate the previous schema with an empty worktree | maintainer; backend/store | pass | Requirement 2 | Two named store cases passed with close/reopen persistence |
| R02 | Create one real-Git worktree per item from refreshed default, route every action there and leave a hostile dirty user checkout unchanged | delivery operator; CLI/backend | pass | Requirement 3; outside-evidence row | Three named cases passed against real temporary Git repositories; host Git 2.55.0 independently confirmed worktree/branch behavior and untouched checkout state |
| R03 | Keep parked worktrees, print them, recover missing worktrees, park missing branches and remove merged worktrees/branches | delivery operator; CLI/backend | pass | Requirement 4 | Seven lifecycle, public CLI status and cleanup-retry cases passed |
| R04 | Describe the item-worktree contract consistently in the canonical skill, mirror and user guide | delivery operator; docs/CLI | pass | Requirement 5 | Both reader surfaces contain every lifecycle promise; mirror identity and the current-build skill check passed |
| R05 | Satisfy the Spec promise rule and manually close every detector skipped by strict Spec checking | maintainer; Spec governance | pass | Requirement 6 | Current-tree strict checker passed; direct queries closed promise, vocabulary, rollup, backlog and reference applicability |
| R06 | Record the Daemon's repository Verification result as a gate fact without rerunning it | maintainer; repository gate | pass | Requirement 1; repository Verification | Supervised fact: `make verify-changed` passed, exit 0, diagnostics removed on success |
| R07 | Prove the governed skill changes stayed within prior authorization and valid commit chronology | maintainer; Git/tooling audit | pass | Requirement 6 | Commit paths, ancestry and mirror identity match the prior grant and sanctioned regeneration result |
| R08 | Account for Pull Request approval, checks, threads, Merge-Ready evidence and review-artifact ancestry | maintainer; Pull Request | blocked (environment: no open Pull Request) | Pull Request row | Every control has equivalent observed or supervised evidence at `682f5bece30a1a63904ec37df4e37f43dbc63a19` |

## Row evidence

### R01

```yaml
inputs:
  - kind: repository_path
    ref: internal/store/delivery.go
  - kind: repository_path
    ref: internal/store/store.go
  - kind: repository_path
    ref: internal/store/delivery_test.go
```

- Steps: execute the named store acceptance cases against the current build;
  require each named case to pass; independently reread the persisted pair and
  migrated empty value through the store's public package boundary.
- Expected: the first branch/worktree pair survives a second proposal and a
  schema-18 database opens at schema 19 with an empty worktree.
- Persistence: close and reopen the database in the acceptance cases.
- Observed: `go test -count=1 -v -run
  '^(TestDeliveryItemRecordsItsWorktreeWithItsBranch|TestOpenMigratesDeliveryQueueAddingItemWorktree)$'
  ./internal/store` exited 0. Both named cases reported `PASS`; the first
  reread the immutable pair and the second reopened a schema-18 fixture at the
  migrated schema with an empty worktree.

### R02

```yaml
inputs:
  - kind: repository_path
    ref: internal/cli/deliver_workflow.go
  - kind: repository_path
    ref: internal/cli/deliver_test.go
  - kind: repository_path
    ref: internal/delivery/engine.go
  - kind: repository_path
    ref: internal/delivery/engine_test.go
  - kind: repository_path
    ref: internal/delivery/github.go
  - kind: external_repository
    ref: independently-created temporary Git repository exercised by current-build real-Git acceptance cases
```

- Steps: run the real-Git creation and hostile-checkout cases and the action
  working-directory case against the current build; inspect the independent
  repository's branch, HEAD, porcelain status and sentinel contents after the
  operations.
- Expected: distinct linked worktrees start at refreshed default, item branches
  have no upstream, every action receives its item worktree, and the user's
  dirty non-default checkout is byte-for-byte unchanged.
- Independent confirmation: use Git porcelain and direct sentinel reads rather
  than the delivery engine's optimistic state.
- Observed: `go test -count=1 -v -run
  '^(TestEachItemRunsInItsOwnWorktree|TestDeliverNeverTouchesTheUserCheckout|TestDeliveryActionsRunInTheItemWorktree)$'
  ./internal/cli ./internal/delivery` exited 0. All three named cases passed.
  The CLI cases created real repositories outside the Spec artifacts, compared
  Git branch, HEAD and porcelain status plus tracked, ignored and nested-repo
  sentinel contents, and observed distinct item worktrees from refreshed
  default. The delivery case independently observed every action's working
  directory.
- Outside source: the host's independently maintained Git 2.55.0 executable.
  `git --version` reported `git version 2.55.0`, and `git worktree add -h`
  exposed the public `<path> [<commit-ish>]`, `-b <branch>` and `--no-track`
  contract used by the acceptance run. Git's own worktree list, branch and
  porcelain reads served as the oracle; the Spec did not author that
  implementation or its command contract.

### R03

```yaml
inputs:
  - kind: repository_path
    ref: internal/cli/deliver.go
  - kind: repository_path
    ref: internal/cli/deliver_workflow.go
  - kind: repository_path
    ref: internal/cli/deliver_test.go
  - kind: repository_path
    ref: internal/delivery/engine.go
  - kind: repository_path
    ref: internal/delivery/engine_test.go
```

- Steps: run the real-Git parked, status, reuse, recreation, missing-branch and
  merged-cleanup cases; invoke the built `deliver status` command against
  persisted scratch queue data when available.
- Expected: park retains the recorded worktree; status exposes the path;
  resume reuses or recreates it; missing branch records
  `item-worktree-missing` without stage replay; merge leaves no worktree or
  local branch and retries cleanup without replay.
- Independent confirmation: query `git worktree list`, `git branch` and a fresh
  `deliver status` process after state transitions.
- Observed: `go test -count=1 -v -run
  '^(TestParkLeavesTheItemWorktreeInPlace|TestDeliverStatusPrintsTheItemWorktree|TestResumeUsesTheRecordedItemWorktree|TestResumeRecreatesAMissingItemWorktree|TestResumeParksWhenTheItemBranchIsGone|TestAMergedItemLeavesNoWorktreeOrBranch|TestDeliveryEngineRetriesMergedItemCleanupWithoutReplayingMerge)$'
  ./internal/cli ./internal/delivery` exited 0. All seven cases reported
  `PASS`. Fresh CLI status showed the fourth field, Git confirmed retained,
  recreated and removed worktrees/branches, and a second resume/cleanup attempt
  observed zero stage or merge replay.

### R04

```yaml
inputs:
  - kind: repository_path
    ref: .agents/skills/roundfix/SKILL.md
  - kind: repository_path
    ref: skills/roundfix/SKILL.md
  - kind: repository_path
    ref: docs/user-guide/commands.md
```

- Steps: follow the delivery-queue documentation as an operator; search both
  documents for each promised state, compare the canonical and distributed
  skill byte-for-byte, and run the current-build skill checker.
- Expected: both reader surfaces describe creation, untouched checkout, park,
  status, recreate-or-park and merged cleanup; the mirror is identical; the
  skill checker passes.
- Independent confirmation: byte comparison and CLI skill check.
- Observed: independent fixed-string searches found creation under
  `worktree.location`, the untouched and possibly dirty checkout, parked-item
  retention and status visibility, `item-worktree-missing`, and merged cleanup
  in both reader surfaces. `cmp -s .agents/skills/roundfix/SKILL.md
  skills/roundfix/SKILL.md` exited 0. `/tmp/roundfix-qa0168 skills check`
  exited 0 and reported the Roundfix skill set passed.

### R05

```yaml
inputs:
  - kind: repository_path
    ref: docs/specs/0168-deliver-one-worktree-per-item/_prd.md
  - kind: repository_path
    ref: docs/specs/0168-deliver-one-worktree-per-item/_techspec.md
  - kind: repository_path
    ref: docs/specs/0168-deliver-one-worktree-per-item/_tasks.md
  - kind: repository_path
    ref: docs/specs/0168-deliver-one-worktree-per-item/task_*.md
  - kind: repository_path
    ref: docs/adr/*.md
```

- Steps: inspect the Spec's explicit promise-to-task references and Project
  Constraints; manually evaluate the four named skipped detectors without
  inventing absent Findings, Backlog entries, references or vocabulary.
- Expected: every declared promise names a consuming Task; applicable
  constraints and cited ADRs hold; the skipped detectors have no applicable
  artifact to validate or their contract is satisfied by the canonical domain
  vocabulary.
- Independent confirmation: strict checker output plus direct cross-reference
  queries over the current repository.
- Observed: the current-tree strict check exited 0 with no findings. Task 05's
  References name Goals 1-2, Core Features 1-7, Success Metrics 1-4, Testing
  Approach 1-5 and API Contracts 1-3, so every declared promise has a consuming
  Task. The applicable ADR files all carry `status: accepted`, and every
  Project Constraint cites an existing operative source.
- Skipped-detector audit: no active or historical Finding, Rollup or Backlog
  Entry names Spec 0168; no adopted `references/` tree exists; the cited local
  Spec 0161 archive source resolves. The new emitted blocker token
  `item-worktree-missing` appears in `internal/delivery/engine.go` and is
  documented verbatim in both public reader surfaces. These command-backed
  observations close `SC-ROLLUP-MEMBER`, `SC-BACKLOG-UNMOVED`,
  `SC-REF-UNRESOLVED` and the vocabulary detector that the strict checker
  explicitly skipped.

### R06

- Steps: transcribe the Daemon's supervised repository Verification command,
  exit status and diagnostics disposition exactly as supplied in the QA
  contract.
- Expected: `make verify-changed` passed with exit 0 and removed diagnostics.
- Independent confirmation: Daemon-owned prompt evidence; deliberately no
  second repository Verification run.
- Observed: the QA contract reports command `make verify-changed`, verdict
  `pass`, exit 0 and diagnostics removed on success. This is a supervised fact,
  not a QA rerun.

### R07

```yaml
inputs:
  - kind: repository_path
    ref: docs/specs/0168-deliver-one-worktree-per-item/_authorization.md
  - kind: repository_path
    ref: .agents/skills/roundfix/SKILL.md
  - kind: repository_path
    ref: skills/roundfix/SKILL.md
```

- Steps: resolve Task 04's actual commit paths with `git diff-tree`; prove the
  authorization commit is earlier ancestry; classify every actual path and
  require each governed path to be in the bounded set; check for prerequisite
  or consequent-fix commits and reproduce sanctioned mirror identity.
- Expected: the grant is separate and earlier; no governed path falls outside
  the bounded set; ordinary source and documentation remain outside
  `GovernedPath`; no folded or late corrective commit exists; the regenerated
  mirror is identical.
- Independent confirmation: Git commit objects, not Task prose.
- Observed: `git diff-tree --no-commit-id --name-only -r 682f5bec...` returned
  `.agents/skills/roundfix/SKILL.md`, `skills/roundfix/SKILL.md`,
  `docs/user-guide/commands.md` and `task_04.md`. The first two are exactly the
  authorized governed paths; the guide is ordinary documentation under
  ADR-0130, and the Task file is the assigned record. `git merge-base
  --is-ancestor b4de85f5... 682f5bec...` exited 0, proving the separate
  authorization commit precedes all four Task commits. No later or folded
  consequent-fix commit exists. The canonical and distributed skills compare
  byte-for-byte identical.

### R08

```yaml
inputs:
  - kind: live_service
    ref: Pull Request state declared by the Roundfix QA prompt
```

- Steps: record the no-open-PR cause and account separately for approval,
  checks/status, unresolved review threads, Merge-Ready acceptance and
  review-artifact ancestry at the audited head.
- Expected: the row remains environment-blocked but carries equivalent
  observed or supervised evidence for every control.
- Independent confirmation: Run contract plus local authorization,
  verification and Git-head evidence.
- Cause: the Roundfix QA prompt states that no Pull Request is open and Pull
  Request journeys are environment-blocked. The Run Worktree branch is never a
  Pull Request branch, so the gate did not query or mutate GitHub.
- Equivalent controls:
  - Approval: no change acceptance has been obtained yet; the operative
    maintainer authorization at `b4de85f5...` grants this Spec's implementation
    and delivery operations without fabricating review approval.
  - Checks and status: the Daemon-supervised `make verify-changed` result is
    pass, exit 0; the current-tree strict Spec check also exits 0.
  - Unresolved review threads: none can stand because the declared environment
    has no open Pull Request; no thread state is claimed beyond that cause.
  - Merge-Ready acceptance: none exists yet; this is recorded as absent, not as
    an approval.
  - Review-artifact ancestry: no review artifact exists yet. The audited and
    claimed head is `682f5bece30a1a63904ec37df4e37f43dbc63a19`, confirmed by
    `git -c core.fsmonitor=false rev-parse HEAD`.
- Unblocking action: after the delivery workflow opens the Spec target branch's
  Pull Request, observe its approval, required checks, unresolved threads,
  Merge-Ready evidence and current-head review-artifact ancestry there.

## Mechanical skips

| Detector | Missing artifact |
| --- | --- |
| consequent-fix commit order | consequent-fix declaration |
| QA Report structure | QA Report |
| QA evidence paths | QA Report |

## Findings

None.

## Blocked and skipped

- R08 is `blocked (environment: no open Pull Request)`. The prompt proves the
  cause, and the row records equivalent evidence for approval, checks/status,
  unresolved threads, Merge-Ready acceptance and review-artifact ancestry.
  The unblocking action is to observe those controls on the future Spec target
  Pull Request. No row is finding-blocked, declared-blocked or skipped.

## Coverage

All 8 rows reached a terminal status: 7 passed and 1 is environment-blocked
with complete equivalent evidence. The rows cover all six declared
verification Requirements plus the outside-evidence, Pull Request and
repository Verification sources. The PRD declares no Unreachable Acceptance
section and no frontend surface. Counts: `rows_blocked_environment: 1`,
`rows_blocked_finding: 0`, `rows_blocked_declared: 0`; skipped rows: 0.

## Final verdict

Pass: every runnable criterion passed, the sole environment-blocked Pull
Request row carries equivalent evidence for every control, and no finding,
declared block or skipped row remains. Statuses: 7 pass, 1 blocked
(environment), 0 fail, 0 skipped; findings by impact tier: none.
