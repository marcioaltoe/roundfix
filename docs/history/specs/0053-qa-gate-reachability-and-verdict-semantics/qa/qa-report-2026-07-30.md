---
spec: 0053-qa-gate-reachability-and-verdict-semantics
date: 2026-07-30
build: 9ebf6998eff3d576c84cb9aff6e02f20ed18ea4f
status: closed
verdict: pass
rows_blocked_environment: 1
rows_blocked_finding: 0
surfaces: [backend, cli, docs]
---

# QA report — QA gate reachability and verdict semantics

## Scope and environment

Full gate for the active Spec on the Run Worktree branch
`roundfix/run-run_20260730T171633Z_31fd3e20d148c9cc`. The Spec target is
`ma/spec-0053-qa-gate-reachability`; the prompt names Open Pull Request
`#51 (marcioaltoe/roundfix)`. The QA actor has read-only Pull Request authority
and no commit, push, Pull Request mutation, or review-thread mutation authority.

Build under test: `9ebf6998eff3d576c84cb9aff6e02f20ed18ea4f`.
Evidence root:
`docs/specs/0053-qa-gate-reachability-and-verdict-semantics/qa/evidence/2026-07-30-spec-0053/`.

## Planned matrix

| # | Story / criterion / sweep | Actor, entry point, surface | Steps and expected observable | Independent confirmation and persistence | Evidence | Status |
| - | --- | --- | --- | --- | --- | --- |
| 01 | Project Constraints and protected-tooling authorization | Maintainer; Spec artifacts and Git history; docs/backend | Read PRD, TechSpec, every Task Result, operative guides, and actual commit paths. Expect complete applicability statements, prior bounded authorization, sanctioned digest fallout, and no out-of-scope tooling mutation. | Re-read every commit with `git diff-tree`; rerun the sanctioned digest generator and confirm no worktree delta. | Authorization commit `397227f`; Task/corrective path audit through `9ebf699`; `rtk make baseline-digests` returned `ok: true`, `changed: false`. | pass |
| 02 | Repository Verification | Maintainer; repository root; backend/cli/docs | Run `rtk make verify`. Expect the authoritative pipeline to exit 0. | Recheck worktree status after the command. | `rtk make verify`: 2,907 passed, 5 environment-caused failures, 2 skipped; focused reproduction: `fork/exec /bin/ps: operation not permitted`; affected files have no `main...HEAD` diff. | blocked (environment: managed sandbox denies `/bin/ps`) |
| 03 | US1: environment-blocked `pass` remains readable and archivable | Maintainer; public `roundfix archive` path and QA reader; cli/backend | Supply a completed scratch Spec whose newest report is `pass` with `rows_blocked_environment: 3`. Expect archive success. | Reopen the moved Spec and stamped PRD; run the focused reader/archive matrix. | Public binary exited 0 and moved the fixture; reopened PRD has `status: archived`, date, and `source_slug`. Focused matrix included in the 45-pass reader/archive run. | pass |
| 04 | US5: finding-blocked `pass` is visibly distinct and refused | Maintainer; public `roundfix archive` path and QA reader; cli/backend | Supply `verdict: pass` with `rows_blocked_finding: 1`. Expect unreadable-report refusal and no archive move. | Reopen the source Spec; run the focused consistency case. | Public binary exited 2 with `rows_blocked_finding must be zero when verdict is "pass"`; source fixture remained active. | pass |
| 05 | Task 01 remaining count and verdict matrix | Maintainer; QA reader/archive tests; backend | Exercise absent counts, negative/non-integer counts, unknown keys, and `partial`/`fail`. Expect defaults, strict malformed-count refusal, unknown-key tolerance, and unchanged non-pass handling. | Repeat through archive matrix and current full gate. | `go test` reader/archive selection: 45 passing cases across `internal/spec` and `internal/cli`. | pass |
| 06 | US2: Open Pull Request fact resolves from the Spec target branch | QA Agent; Daemon QA prompt and PR #51; backend/cli | Inspect prompt construction and run the target-branch lookup contract. Expect `Pull Request: #51 (marcioaltoe/roundfix)` independent of the Run Worktree branch. | Query PR #51 read-only and compare head branch/repository to the prompt facts. | Prompt fact names PR #51; `gh pr view` proves head `ma/spec-0053-qa-gate-reachability` at build `9ebf699`, while this checkout is the per-Run branch. | pass |
| 07 | PR absence is not inferred from a failed lookup | QA Agent; prompt builder/Daemon failure cases; backend | Exercise successful empty lookup, failed lookup, malformed payload, timeout, and missing target branch. Expect only a completed empty lookup to say `none open`; unresolved cases say absence is unproven. | Re-run focused prompt/Daemon cases in both error classes. | Prompt/Daemon selection: 20 passing cases, including confirmed-empty and unresolved error/timeout/malformed/missing-target paths. | pass |
| 08 | US6: collision-safe same-day report naming is identical | QA Agent; Daemon prompt and canonical qa-gate Skill; docs/backend | Compare both contracts. Expect unsuffixed first report and numeric `-NN` reruns only. | Exercise the prompt contract test and confirm this run selected the first unused filename. | Canonical Skill and prompt test agree on unsuffixed first report plus numeric `-NN` only; this first report uses the unsuffixed name. | pass |
| 09 | Newest-report selection ranks every allowed numeric sibling correctly | Maintainer; QA report selector; backend | Exercise unsuffixed, `-00`, `-01`, `-02`, and `-10` in both input orders. Expect every numeric sibling to outrank the unsuffixed report and higher numeric suffixes to win. | Re-run the selector cases and inspect unchanged public archive selection behavior. | 45-pass reader/archive run includes `-00`, `-01` in both orders, `-02`, and `-10`; public archive selected the current allowed report. | pass |
| 10 | PR approval state is observed read-only | QA Agent; `gh pr view 51`; cli | Read reviews and approval decision. Expect an observed state for the current head, without mutations. | Re-read the current PR head SHA and decision in a second query. | Open PR #51 reports `reviewDecision: APPROVED`; latest CodeRabbit approval is on `531ba26`, the parent of the Daemon artifact commit. | pass |
| 11 | PR checks and status evidence are observed read-only | QA Agent; `gh pr checks/view 51`; cli | Read checks/status for the current head. Expect every required check conclusion to be attributable to that head. | Compare check rollup head context to the PR head SHA. | Head `9ebf699` reports completed `Validate PR title: SUCCESS` and `CodeRabbit: SUCCESS`; `mergeStateStatus: CLEAN`. | pass |
| 12 | Unresolved review threads are observed read-only | QA Agent; GitHub GraphQL read; cli | List review threads for PR #51. Expect an exact unresolved count with no thread mutation. | Compare thread heads/paths with the current review artifacts. | GraphQL returned 8 total threads, `hasNextPage: false`, all 8 `isResolved: true`; head OID is `9ebf699`. | pass |
| 13 | Merge-Ready acceptance is evaluated against the current head | Maintainer; PR #51 and Roundfix review contract; cli/docs | Combine approval, checks, current-head Review Source Evidence, and unresolved-thread count. Expect Merge-Ready only when all current-head conditions hold. | Compare with the latest review-round summary and artifact commit ancestry. | Current head `9ebf699`: PR open, approved, CLEAN, CodeRabbit success, and 0 unresolved threads; the approved parent/artifact descendant shape is exact. | pass |
| 14 | Daemon review-artifact descendant inherits verified parent evidence only in the exact shape | Maintainer; PR #51 Git ancestry and review artifacts; backend/docs | Inspect the review-artifact commit, its parent, subject, changed paths, and current head. Expect an exact Daemon-created artifact-only descendant under the resolved review root. | Use `git diff-tree` and PR head SHA; confirm zero mixed paths. | Current head `9ebf699` has subject `docs: review round 001 for pr 51`, parent `531ba26`, and exactly four paths under `docs/specs/_reviews/pr-51/round-001/`; all 3 issue artifacts are `resolved`. | pass |
| 15 | US3: newer target report classifies a QA-only retained Run as `superseded` | Operator; public `roundfix reconcile` plus classifier fixture; cli/backend | Dry-run reconciliation and exercise a scratch QA-only branch with a newer target report. Expect `superseded` and named newer-report evidence. | Repeat via text and `roundfix-reconcile/v1` JSON. | Public binary dry-run produced the stable text/JSON envelopes without mutation; 52 worktree and 36 CLI cases prove `superseded` with the newer-report path. Live user checkout dry-run classified all five Spec Runs as released. | pass |
| 16 | Preserve-by-default fallbacks remain `unintegrated` | Operator; reconciliation classifier; cli/backend | Exercise a non-QA commit, missing target report, older/equal target report, and unprovable Git evidence. Expect preserve with no release eligibility. | Confirm worktree/branch existence after each dry-run case. | Current-build worktree suite: 52 passing cases, including non-QA, missing, older/equal, malformed, dirty, stale, and unknown preservation. | pass |
| 17 | `reconcile --apply` releases only freshly re-proven `superseded` work and records why | Operator; safe scratch reconciliation fixture; cli/backend | Apply to an isolated superseded Run. Expect cleanup only after fresh proof and a release reason naming the superseding report; stale proof remains preserved. | Reopen Run reconciliation/event state and verify branch/worktree absence or preservation as applicable. | `TestRunReconcileSupersededJSONAndApply` and the 52-pass worktree suite exercise the public CLI runner in scratch Git/Run Database fixtures, re-open the event, and prove fresh cleanup versus stale preservation. | pass |
| 18 | Reconcile JSON adds `superseded` without a schema break | Operator; `roundfix reconcile --format json`; cli | Exercise the classification and summary. Expect schema `roundfix-reconcile/v1`, `classification: superseded`, and `summary.superseded`. | Parse the output and compare with text output. | Public binary emitted `roundfix-reconcile/v1`; current-build CLI suite passed schema, classification, counter, text parity, and apply cases. | pass |
| 19 | US4: Branch Integrity Preflight never auto-integrates a proven superseded QA report | User starting a review Run; branch-integrity preflight; cli/backend | Exercise a pending QA-only branch with a newer target report. Expect refusal with `roundfix reconcile --apply`, no merge command, and no fast-forward. | Re-read target head and integration calls after the preflight. | Current-build CLI suite: 36 passing reconciliation/preflight cases, including no integration and reconcile-only guidance. | pass |
| 20 | Task-work and unprovable branches retain existing integration behavior | User starting a review Run; branch-integrity preflight; cli/backend | Exercise task work and a failed proof. Expect the established fast-forward or `git merge --ff-only` path rather than a false supersession claim. | Compare target ancestry and recorded integration calls. | Same 36-pass CLI suite covers fast-forward task work and conservative unprovable handling. | pass |
| 21 | Mixed pending branch classes receive separate safe guidance | User starting a review Run; branch-integrity preflight; cli/backend | Exercise one task-work and one superseded branch. Expect each class under its own remedy with no unsafe suggestion. | Assert the QA-only branch has no merge command and the task branch keeps one. | Same 36-pass CLI suite covers the mixed-class refusal and distinct remedies. | pass |
| 22 | Explicit Branch Integrity bypass audit includes superseded work | Maintainer; bypass audit renderer; backend/cli | Exercise an audit containing superseded and task-work branches. Expect both ignored classes in the read-only audit payload. | Re-run the focused bypass case and inspect rendered output. | Same 36-pass CLI suite covers bypass audit publication, both ignored classes, and publish-failure refusal. | pass |
| 23 | Docs and Skill pairs describe the shipped contract and remain synchronized | Operator/QA Agent; canonical Skills, embedded copies, user guide, glossary; docs | Follow the QA naming/verdict and reconcile instructions. Expect identical Skill pairs, typed counts, six reconcile states, explicit `--apply`, and the QA-only exclusion. | Run Skill sync/check commands and inspect exactly the four authorized glossary definitions. | `make skills-sync-check`: 4 passed; public `roundfix skills check`: passed; both pairwise `cmp` checks: exit 0; canonical docs contain every required term. | pass |
| 24 | Non-Goals: no QA commit/push/PR mutation; preserve defaults and archive gate stay intact | Maintainer/QA Agent; Git status, public docs, focused gates; cli/docs | Confirm this run changes only QA artifacts; no automatic release, weakened preserve behavior, archive override, or Task-prose-only credit appears. | Compare worktree status before/after and inspect archive/reconcile negative cases. | Staged and tracked-unstaged path lists are empty; all 10 untracked paths are this report and its evidence. Public dry-runs applied nothing; PR reads mutated nothing; archive refusal and preserve cases stayed intact. | pass |
| 25 | CLI surface sweep: help, streams, exit codes, and recovery guidance | Operator; built `roundfix` CLI; cli | Run help and representative success/refusal flows. Expect deterministic stdout, diagnostics on stderr, documented exit codes, and actionable recovery text. | Repeat key commands and compare output shape; use the full gate as regression evidence. | Built current binary: archive success 0, contradictory pass refusal 2, reconcile dry-runs 0 in text/JSON, and help text with exact read-only/apply/exit contracts. | pass |

High-risk behavior probes: same-day `-01` collision and input-order reversal
(rows 08–09); malformed and contradictory frontmatter (rows 04–05); stale
heads/proof changed between dry-run and apply (rows 16–17); mixed pending
branch classes and explicit bypass (rows 21–22); current-head versus stale PR
evidence (rows 10–14). These probes target the defects and trust boundaries
named by the PRD.

## Static gate

`rtk make verify` reached the repository test command. It reported 2,907
passing tests, 5 failing tests, and 2 skipped tests, then exited 2. Every
failure reads live process identity through `/bin/ps`. The focused reproduction

```text
rtk proxy env GOCACHE=<worktree>/.gocache go test -count=1 \
  -run '^TestOwnerProcessIdentityIsStableForOneProcess$' -v ./internal/store
```

failed with:

```text
read current process identity: read start time for process 80249:
fork/exec /bin/ps: operation not permitted
```

`git diff --name-only main...HEAD` is empty for
`internal/store/process_unix.go`, `internal/store/process_unix_test.go`, and
`internal/cli/orphan_unix_test.go`. The failure is therefore environment-caused
and unchanged from the base branch. A full-access session that permits
`/bin/ps` must rerun the unchanged `rtk make verify`; the remaining QA rows
continue against the same build.

## Results

| # | Story / criterion / sweep | Actor and surface | Status | Evidence |
| - | --- | --- | --- | --- |
| 01 | Project Constraints and protected-tooling authorization | Maintainer; docs/backend | pass | Bounded authorization, ancestry, changed paths, and deterministic digest regeneration all match. |
| 02 | Repository Verification | Maintainer; backend/cli/docs | blocked (environment: managed sandbox denies `/bin/ps`) | Full gate reached 2,907 passes; all 5 failures share the unchanged `/bin/ps` denial. |
| 06 | Open Pull Request fact | QA Agent; backend/cli | pass | Prompt PR fact matches target-branch PR #51 and its current head. |
| 10–14 | Read-only Pull Request journeys | QA Agent/maintainer; cli/docs | pass | Approval, current-head checks, thread resolution, Merge-Ready, and artifact ancestry all pass. |
| 03–05, 07–09 | Verdict, prompt, and report-selection contracts | Maintainer/QA Agent; backend/cli/docs | pass | Public archive journey plus 65 current-build focused cases. |
| 15–23, 25 | Reconcile, preflight, docs, and CLI sweeps | Operator/maintainer; backend/cli/docs | pass | Public dry-run/help surfaces plus 88 current-build worktree/CLI cases and Skill checks. |
| 24 | Non-Goals and final mutation audit | Maintainer/QA Agent; cli/docs | pass | Only the 10 planned QA report/evidence paths are untracked; no tracked or staged delta exists. |

## Findings

None.

## Blocked and skipped

Row 02 is blocked by the managed sandbox's `/bin/ps` denial. Unblocking action:
rerun the unchanged `rtk make verify` in a full-access session. Equivalent
evidence: 2,907 passing tests in this run, a focused exact-denial reproduction,
no Spec diff in the affected process-identity files, 153 passing current-build
focused cases across the changed seams, and passing current-head Pull Request
checks.

No rows are finding-blocked or skipped.

## Coverage

All 25 rows are terminal: 24 pass and 1 is environment-blocked with equivalent
evidence. `rows_blocked_environment: 1`; `rows_blocked_finding: 0`; failed: 0;
skipped: 0; pending: 0.

All 6 PRD user stories, 24 Task acceptance criteria, 5 success metrics, and 5
Non-Goals map to the matrix. The selected high-risk probes passed: malformed
and contradictory frontmatter; `-00`/`-01`/`-02`/`-10` same-day ordering in
both input orders; failed, missing, and malformed Pull Request lookup; stale
reconciliation proof; mixed pending branch classes; explicit bypass audit; and
stale versus current-head Pull Request evidence.

The backend, CLI, and docs surfaces were exercised. No frontend or TUI surface
is declared, so responsive, browser, and terminal-layout sweeps do not apply.
Pull Request rows used read-only `gh` calls only; no review or thread state
changed.

## Final verdict

Pass — all 24 runnable rows passed, the single environment-blocked row records
its exact cause and equivalent observed evidence, and there are no failed,
finding-blocked, skipped, or pending rows. The Spec can proceed to the
Daemon-owned report commit and normal archive eligibility; rerun the unchanged
`rtk make verify` in a full-access session to remove the recorded sandbox
parity gap.
