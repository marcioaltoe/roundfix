---
spec: 0143-a-repository-says-who-reviews-before-the-pull-request
date: 2026-09-18
build: cdf02e011192cb079d4d5448432f7ece12767bc0
auditing_binary: "0.13.1 (3c914d8e, built 2026-09-18 06:32:45 -0300)"
auditor_staleness: "stale: commit ancestry: build commit predates audited tree"
status: closed
verdict: pass
rows_blocked_environment: 1
rows_blocked_finding: 0
rows_blocked_declared: 0
surfaces: [backend, cli, docs]
---

# QA Report

## Scope and environment

This full gate audited commit `cdf02e011192cb079d4d5448432f7ece12767bc0`
through the configuration loader, the public `doctor` command, repository
history, the Roundfix skill, and the user guide. The user-facing binary was
built from that commit at `/private/tmp/roundfix-0143-qa-bin`. The actor was a
repository maintainer resolving Project Config, User Config, and the built-in
default.

`/private/tmp/roundfix-0143-qa-bin spec check
0143-a-repository-says-who-reviews-before-the-pull-request --strict` exited 0
with no findings. Its skipped detectors named absent artifacts and did not
replace any matrix row. Every non-QA dependency of `task_04` is completed, and
`task_04` is the terminal QA node.

The first build attempt could not write the host Go cache under
`~/Library/Caches/go-build`. Repeating the build with
`GOCACHE=/private/tmp/roundfix-0143-go-cache` exited 0. This sandbox parity
deviation did not block a row.

The live `doctor` command was run with an isolated home. Its external readiness
probes did not finish within 40 seconds, so the command was interrupted. The
command still emitted every check, including
`pre-pr-review: ok (provider=codex; source=default)`. Focused command-boundary
tests supplied the complete provider/source, disabled-policy, no-provider-call,
and no-mutation evidence.

No Pull Request is open for the Spec target branch. Pull Request journeys are
environment-blocked and use the equivalent-evidence path in row 8.

## Performed repairs

None.

## Assigned repair failures

None.

## Authorization audit inputs

| Task | Outcome | Record | Revision | Detail |
| --- | --- | --- | --- | --- |
| task_05 | granted | docs/specs/0143-a-repository-says-who-reviews-before-the-pull-request/_authorization.md | 3c914d8eb4c2b00da143dd233a6be16babfc9b80 | operative grant |

## Mechanical findings

None.

## Static gate

- `make verify`: passed with exit 0 in the Daemon outside the Agent sandbox.
  Per the QA prompt, this result was recorded and the command was not rerun.
- `/private/tmp/roundfix-0143-qa-bin spec check
  0143-a-repository-says-who-reviews-before-the-pull-request --strict`: passed
  with exit 0 and no findings.
- `GOCACHE=/private/tmp/roundfix-0143-go-cache go build -o
  /private/tmp/roundfix-0143-qa-bin ./cmd/roundfix`: passed with exit 0.
- `GOCACHE=/private/tmp/roundfix-0143-go-cache go test -count=1 -v -run
  '^TestPrePRReview(PolicyResolution|ProviderRefusesNullValue)$'
  ./internal/config`: passed all 12 subtests.
- `GOCACHE=/private/tmp/roundfix-0143-go-cache go test -count=1 -v -run
  '^(TestDoctorReportsPrePRReviewPolicy|TestRunDoctorProfileReadinessProvesEffectiveCategoriesAndReportsCounts|TestRunDoctorRealRepositoryCheckDoesNotMutateState)$'
  ./internal/cli`: passed all selected cases and the mutation guard.
- `GOCACHE=/private/tmp/roundfix-0143-go-cache go test -count=1 -v -run
  '^(TestBuiltinReviewSourceExcludesNitpicks|TestLoadAppliesReviewSourceHierarchy|TestLoadAppliesReviewSourceIncludeNitpicksHierarchy)$'
  ./internal/config`: passed the built-in case and all three hierarchy subtests.
- `git diff --check c1d7ca11..HEAD`: passed with exit 0.
- `cmp -s .agents/skills/roundfix/SKILL.md skills/roundfix/SKILL.md`: passed
  with exit 0.

## Results

| # | Status | Provenance |
| - | --- | --- |
| 1 | pass | Requirement 1; repository Verification |
| 2 | pass | Requirement 2 |
| 3 | pass | Requirement 3 |
| 4 | pass | Requirement 4 |
| 5 | pass | Requirement 5 |
| 6 | pass | Requirement 6; outside-evidence row |
| 7 | pass | Requirement 7 |
| 8 | blocked (environment: no open Pull Request) | Pull Request row |

## Row evidence

### 1

- Actor and surface: Daemon, repository Verification.
- Entry point and steps: the Daemon ran `make verify` against the assembled Run
  Worktree.
- Expected observable: exit 0 with no verification diagnostics.
- Independent confirmation and persistence: the Daemon recorded exit 0 after
  all non-QA Task commits were assembled at `cdf02e01`.
- Evidence: the Repository Verification fact supplied in the QA prompt.

### 2

- Actor and surface: maintainer, configuration load boundary.
- Entry point and steps: `TestPrePRReviewPolicyResolution` and
  `TestPrePRReviewProviderRefusesNullValue` loaded fresh User and Project Config
  fixtures through the production loader.
- Expected observable: `codex`, `claude`, `coderabbit`, and `none` load;
  `gemini`, a valueless provider, explicit null, an empty string, a sequence,
  and a mapping fail with `pre_pr_review.provider`, the written value, and the
  full supported set.
- Independent confirmation: an invalid User Config remained invalid even when
  Project Config supplied a valid override, proving refusal occurs per loaded
  layer.
- Persistence: each of the 12 subtests used a fresh loader and filesystem
  fixture.
- Evidence: the focused configuration command passed every named case.

### 3

- Actor and surface: maintainer, layered local configuration.
- Entry point and steps: the production-loader suite exercised Project Config
  over User Config, User Config over the built-in value, and both layers silent.
- Expected observable: `coderabbit/project`, `claude/user`, and `codex/default`
  resolve in precedence order; silence never resolves to `none`.
- Independent confirmation: the built binary read this repository's silent
  Project Config and emitted `provider=codex; source=default`.
- Persistence: each resolution was reloaded from disk in its own fixture; the
  repository Project Config hash stayed
  `b47978d9638f05af9518b2ab6094128890da59f0a85719b58a75aa10b9201837`.
- Evidence: the focused configuration command and the built CLI observation.

### 4

- Actor and surface: maintainer, `roundfix doctor` CLI.
- Entry point and steps: the built binary ran `doctor` from the repository root
  with an isolated home; `TestDoctorReportsPrePRReviewPolicy` invoked the public
  command dispatcher for project, user, default, and explicit `none` policies.
- Expected observable: one `pre-pr-review` line names provider and source;
  explicit `none` is `skipped` with `review disabled by configuration`; no
  provider is invoked.
- Independent confirmation: the live command emitted
  `pre-pr-review: ok (provider=codex; source=default)`. The four command cases
  asserted zero Agent requests and unchanged existing health-check call counts.
- Persistence: fresh command environments reproduced each policy, and the real
  repository configuration hash remained unchanged.
- Evidence: the focused CLI command passed every selected case. The live
  command was interrupted only after the policy line was observed because its
  unrelated external readiness probes had not completed.

### 5

- Actor and surface: maintainer, existing configuration and Doctor diagnostics.
- Entry point and steps: focused regression cases exercised the built-in Review
  Source, its User/Project hierarchy and `include_nitpicks`; Doctor cases
  exercised the checks around the new line and compared repository state before
  and after.
- Expected observable: `review_source` retains its defaults and precedence;
  existing Doctor checks retain their behavior and Doctor mutates no state.
- Independent confirmation: the Daemon's full `make verify` passed on the
  assembled tree.
- Persistence: `TestRunDoctorRealRepositoryCheckDoesNotMutateState` compared
  complete before/after snapshots, while the live Project Config hash remained
  unchanged.
- Evidence: the focused Review Source and Doctor commands passed.

### 6

- Actor and surface: maintainer, repository policy and built CLI.
- Entry point and steps: read the setup-owned obligation in
  `docs/agents/agent-instructions.md`, searched this repository's Project Config
  for `pre_pr_review`, built the command from the audited commit, and ran
  `roundfix doctor` from the repository root.
- Expected observable: the guide names `codex`, `claude`, `coderabbit`, and
  explicit `none`, with Project over User over default; the silent repository
  resolves to `codex/default`.
- Independent confirmation: the operative clauses were found at lines 24 and
  68; `rg -n '^pre_pr_review:' .roundfixrc.yml` returned no match; the built
  command printed `provider=codex; source=default`.
- Persistence: the Project Config SHA-256 remained
  `b47978d9638f05af9518b2ab6094128890da59f0a85719b58a75aa10b9201837`,
  and the only worktree change remained this seeded QA Report.
- Evidence origin: the setup-owned agent guide and the repository's existing
  `.roundfixrc.yml`, neither authored by this Spec, plus observation through the
  built product.

### 7

- Actor and surface: QA auditor, Spec contract and Git history.
- Entry point and steps: ran the current-tree strict Spec checker, enumerated
  every Task commit with `git diff-tree --no-commit-id --name-only -r`, resolved
  the tooling grant from commit `c1d7ca11`, and compared the canonical and
  distributed Roundfix skills byte for byte.
- Expected observable: the promise map is consistent; any Governed Path change
  is traceable to an operative, earlier authorization and remains inside its
  exact bounded paths plus ordinary source and the Task file.
- Independent confirmation: only Task 05 changed Governed Paths. Its commit
  changed exactly `.agents/skills/roundfix/SKILL.md` and
  `skills/roundfix/SKILL.md` from the grant, its own Task file, and ordinary
  configuration source/tests. `cmp -s` confirmed the two skill copies match.
- Persistence: commit-specific path enumeration and the authorization commit's
  ancestry bind the audit to immutable commit objects.
- Evidence: strict Spec checking exited 0 with no findings; authorization
  commit `c1d7ca11` predates Task 05 commit `cdf02e01`; `git diff --check` and
  the skill comparison passed.

### 8

- Actor and surface: maintainer, Pull Request controls.
- Entry point and steps: used the Roundfix QA prompt's supervised fact that no
  Pull Request is open and Pull Request journeys are environment-blocked.
- Expected observable: approval, checks and status, unresolved threads,
  Merge-Ready acceptance, and review-artifact ancestry are each accounted for.
- Independent confirmation and equivalent evidence: no acceptance was
  obtained; the Daemon's `make verify` passed; no Pull Request exists to carry
  unresolved threads; no Merge-Ready Review Source evidence exists yet; and no
  review artifact exists whose ancestry could bind it to expected head
  `cdf02e01`.
- Persistence: this row depends on live service state and is ineligible for
  carry-forward.
- Evidence: the supplied Pull Request fact and the Daemon's repository
  Verification result. Unblocking action: open the Pull Request later in the
  delivery sequence and observe the five controls on the target-branch head.

## Findings

None.

## Blocked and skipped

- Row 8 is blocked by the supplied environment fact that no Pull Request is
  open. All five Pull Request controls have equivalent observed or supervised
  evidence, so this environment block does not prevent a pass verdict.
- No row is finding-blocked, declared-blocked, or skipped.

## Coverage

All seven declared verification Requirements are covered. Requirement 8
constrained the evidence and was met: no row passed from file reading alone.
The repository Verification, outside-evidence, and Pull Request rows are
present. The PRD has no Unreachable Acceptance declaration and declares no
frontend surface.

Seven rows passed and one row is environment-blocked with equivalent evidence.
`rows_blocked_environment=1`, `rows_blocked_finding=0`, and
`rows_blocked_declared=0`. No planned row, risk-based cut, or skipped source
remains.

## Final verdict

Pass: every runnable row passed, the no-Pull-Request row records its cause and
equivalent control evidence, and no finding-blocked, declared-blocked, or
skipped row remains.

## Mechanical skips

| Detector | Missing artifact |
| --- | --- |
| consequent-fix commit order | consequent-fix declaration |
| QA evidence paths | docs/specs/0143-a-repository-says-who-reviews-before-the-pull-request/qa/evidence |
