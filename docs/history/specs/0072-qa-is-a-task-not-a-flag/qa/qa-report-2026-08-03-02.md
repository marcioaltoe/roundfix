---
spec: 0072-qa-is-a-task-not-a-flag
date: 2026-08-03
build: 756b43133adcaa639428d79f305ee10e6162573f
status: closed
verdict: fail
rows_blocked_environment: 0
rows_blocked_finding: 15
surfaces: [backend, cli, docs]
---

# QA report — QA is a Task, not a flag

## Scope and environment

Full rerun of the authored terminal QA Task against Run Worktree build
`756b43133adcaa639428d79f305ee10e6162573f` on macOS, branch
`roundfix/run-run_20260803T160348Z_2bd085b059fa7bea`. The real user entry
point is a freshly built `roundfix` CLI; authoring and documentation journeys
use scratch repositories and the shipped repository guidance. Pull Request
#87 on target branch `ma/qa-task-not-flag` is supplied as open and will be
observed read-only. This gate writes only this report and QA evidence; it does
not commit, push, resolve review threads, open a Pull Request, or edit Task
status.

This rerun starts with the two blockers from
`qa/qa-report-2026-08-03-01.md`: F-001, exact Tooling Authority in both active
Spec artifacts; and F-003, Pull Request review readiness. The complete
governance audit still fails F-001 and therefore stops public scratch-flow QA.
F-003 changed during the gate: the first eight review threads are resolved,
but current-head CodeRabbit review opened four new unresolved threads.

## Static gate

`rtk make verify` ran alone against the report build and exited `0`:

```text
rtk go test -parallel 16 ./...
Go test: 3101 passed in 24 packages
rtk go test -count=1 ./skills -run 'TestNoPythonBaselineRuntime|TestThinSetupSkill|TestCheckRejectsExecutableSetupEngineArtifacts|TestRecommendedSkillsMatchLock'
Go test: 4 passed in 1 packages
rtk go run -buildvcs=false ./cmd/roundfix skills check
Roundfix skill check passed: roundfix, write-idea, write-prd, write-techspec, write-tasks, setup-context-driven, implement-task, implement-spec, brainstorming, council, business-analyst, archive-spec, qa-gate, evidence-gate
rtk go build -buildvcs=false ... -o bin/roundfix ./cmd/roundfix
```

This confirms prior F-002 remains resolved. The build metadata correctly says
`756b431-dirty` because this in-progress QA Report existed while the binary
was built; the implementation commit under test is the frontmatter `build`.

## Results

| # | Story / criterion / sweep | Actor, entry point, surface | Steps and expected observable | Independent confirmation and persistence | Evidence | Status |
| - | --- | --- | --- | --- | --- | --- |
| 1 | Authored-gate entry precondition: `task_07` is the manifest-named terminal QA Task and every dependency is completed | QA Agent; Spec files; docs/Task Graph | Load the real Spec graph; expect the named `type: qa` node to be terminal and cover every non-QA leaf | Re-read manifest and Task statuses from a second command; gate status remains Daemon-owned | `_tasks.md` names `qa: task_07`; the terminal node depends on completed task_04, task_05, and task_06, which cover every non-QA leaf. The Daemon-owned gate status is `failed` from the prior attempt and is not edited here. | pass |
| 2 | Project Constraint: identifier strategy accounts for Task Type `qa` without duplicating status or dependencies | Maintainer; active Spec artifacts; backend/docs | Compare PRD and TechSpec with domain guidance; expect explicit applicability and one owner per field | Load the graph through the public CLI and re-read repository vocabulary | Both active artifacts account for Task Type `qa`; `CONTEXT.md` and the graph preserve manifest-owned dependencies and Task-file-owned status. | pass |
| 3 | Project Constraint: authentication and HTTP are inapplicable and no credential or transport policy shipped | Maintainer; active Spec artifacts and Task commits; backend/docs | Read both applicability rows and exact Task diffs; expect a reason, operative source, and no auth/HTTP surface | Re-scan all Task commit paths | Both active artifacts give the inapplicability reason and cite `docs/agents/agent-instructions.md`; `git diff-tree` over the complete Spec ancestry found no auth, credential, HTTP, or transport path. | pass |
| 4 | Project Constraint: active ADR obligations preserve ADR-0080 and implement ADR-0088/ADR-0091 | Maintainer; ADRs and real graph/report; backend/docs | Read active ADRs; expect report/count semantics and authored-node shape to agree | Confirm through public graph and report reads after execution | ADR-0080, ADR-0088, and ADR-0091 are accepted. The real manifest and this report carry their required node and typed-count shapes; the full gate passes their automated suites. | pass |
| 5 | Prior F-001 and tooling audit: exact paths are authorized in both active artifacts; chronology, scope, mirrors, and sanctioned fallout are valid | Maintainer; authorization, Git ancestry, skills; docs/tooling | Inspect authorization and every relevant commit; expect traceable prior authority and exact bounded paths in PRD and TechSpec | Use `git diff-tree`, ancestry checks, mirror comparison, and sanctioned regeneration check | F-001 repeats and expands. The PRD still says only “the owned skill pair”; Spec-specific exact authority for Task 04 landed after the Task; the later `roundfix` skill authorization was folded into the same commit as the protected edit; active artifacts do not bound that path or the corrective golden/coverage paths. Mirrors and current derived pins are otherwise valid. | fail |
| 6 | Prior F-002 remains resolved: repository Verification passes | Maintainer; repository gate; backend/cli/docs | Run `rtk make verify`; expect exit 0 with format, tests, skill check, and build passing | Run focused public probes only on the same green build | `rtk make verify` exited 0: 3,101 tests passed across 24 packages, four skill tests passed, `roundfix skills check` passed, and the binary built. | pass |
| 7 | An author includes QA once: one manifest-named terminal `type: qa` Task covers every non-QA leaf | Spec author; scratch Spec through public CLI; docs/Task Graph | Create/load an authored graph; expect acceptance with one terminal gate | Repeat the load and inspect unchanged manifest/status bytes | Governance preflight F-001 prohibits public flow QA on this build; the scratch author journey was not run. | blocked (finding: F-001) |
| 8 | An author declines QA once with a non-empty reason; missing or explicit-empty reasons are rejected | Spec author; scratch Spec through public CLI; docs/Task Graph | Load valid decline, missing reason, and explicit-empty reason; expect only valid decline to load | Repeat each load and compare deterministic diagnostics | Governance preflight F-001 prohibits public flow QA; the valid, missing, and explicit-empty scratch probes were not run. | blocked (finding: F-001) |
| 9 | `roundfix implement --spec <slug>` runs an unsettled authored gate after implementation Tasks complete without a QA parameter | Operator; built CLI; cli/backend | Run an authored-gate scratch Spec; expect the gate to execute last and settle from its report | Read Task/report state and Run Event Stream through a fresh public command | Governance preflight F-001 prohibits the public end-to-end Run; task and test evidence is not substituted for the user journey. | blocked (finding: F-001) |
| 10 | Removed `--qa` fails as unknown, names Task Graph remediation, writes no requested output, and creates no Run | Operator; built CLI; cli | Invoke the removed flag; expect exit 2, diagnostic on stderr, and empty stdout | Compare `runs list` before/after and repeat invocation | Governance preflight F-001 prohibits the public CLI flow; the removed-flag and before/after Run probes were not run. | blocked (finding: F-001) |
| 11 | Appending work after a completed gate invalidates the result and names every inserted or unsettled dependency | Spec author; scratch Spec through public CLI; cli/backend | Load, append out of order, reload; expect invalidation naming inserted Tasks | Reload again and compare diagnostic/preserved files | Governance preflight F-001 prohibits the out-of-order public probe. | blocked (finding: F-001) |
| 12 | A non-terminal gate or gate missing leaf coverage is rejected with the offending Task named | Spec author; malformed scratch graphs through public CLI; cli/backend | Load both malformed shapes; expect preflight refusal naming dependent/uncovered Task | Repeat loads and confirm no Run exists | Governance preflight F-001 prohibits the malformed-graph public probes. | blocked (finding: F-001) |
| 13 | Declined and legacy graphs never start a gate; settled/no-gate graphs remain repeatable no-ops | Operator; built CLI; cli/backend | Invoke Implement on each scratch graph; expect no gate report/event and deterministic no-op output | Repeat invocation and inspect Runs/public filesystem state | Governance preflight F-001 prohibits repeated public invocation and independent Run reads. | blocked (finding: F-001) |
| 14 | Failed implementation dependency withholds the gate, leaves it resumable, and emits no QA report/event | Operator; controlled scratch Run; cli/backend | Run a failing dependency; expect gate not to start and its Task to remain unsettled | Read Task/report state and Run Event Stream after process exit | Governance preflight F-001 prohibits the controlled failure journey. Current review artifacts show the prior withheld-gate UX finding was resolved, but that is not live user-seat proof. | blocked (finding: F-001) |
| 15 | A hand-edited `type: qa` node cannot enter an ordinary Agent Session | Operator; malformed graph through built CLI; cli/backend | Present invalid routing shape; expect refusal before Agent work | Inspect Run events/state for absence of Agent work and repeat | Governance preflight F-001 prohibits the malformed public routing probe. | blocked (finding: F-001) |
| 16 | Report path, verdict vocabulary, typed counts, and `daemon.qa` events remain ADR-0080-compatible | QA Agent/operator; reports and scratch Runs; cli/backend | Exercise pass/fail/partial-compatible paths; expect stable report and event fields | Re-read reports/events through fresh commands and validate count totals | This report proves the schema and exact blocked-row totals; public pass/fail/partial Run and event probes were stopped by F-001. | blocked (finding: F-001) |
| 17 | Gate executions are countable as terminal Task history; Spec 0057's three closings remain legible | Maintainer; Git/spec history; docs/backend | Inspect historical reports/commits and current gate node; expect each closing to be countable | Re-run bounded history query and compare stable identities | Git history contains the current authored gate and prior reports, but the assembled-build public history journey was not run after F-001. | blocked (finding: F-001) |
| 18 | Legacy Specs load unchanged and archived artifacts remain byte-identical | Maintainer; public compatibility path; cli/backend/docs | Load archived manifests; expect legacy acceptance without mutation | Compare checksums/status before and after a second load | The current green gate includes the repaired characterization, but F-001 stopped the required public compatibility and persistence run. | blocked (finding: F-001) |
| 19 | Guides and shipped skills teach the authored gate and are followable without undocumented knowledge | Supervisor/Spec author; docs and built CLI; docs | Follow documented scratch flow; expect commands and graph shape to work and no stale guidance | Scan for stale `--qa` guidance and compare canonical/mirror skills | All three canonical/mirror directory comparisons exited 0; F-001 stopped the follow-the-docs public scratch journey. | blocked (finding: F-001) |
| 20 | CLI sweep: help omits `--qa`; output streams, exit codes, and non-interactive behavior are deterministic | Operator/automation; built CLI; cli | Exercise help, invalid flag, valid no-op, and repeated invocation | Capture stdout/stderr/exit separately and compare repeated results | The binary built under the green full gate, but F-001 stopped the public help/stream/exit/replay sweep. | blocked (finding: F-001) |
| 21 | Non-Goals: matrix/verdict semantics and withholding remain unchanged; no runtime skip path shipped | Maintainer/operator; public CLI plus bounded diff; backend/cli | Exercise dependency/verdict paths and inspect scope; expect unchanged semantics and no runtime skip | Re-read events/reports and search the bounded diff | Static scope and suites support the Non-Goals, but F-001 stopped live dependency and verdict paths. | blocked (finding: F-001) |
| 22 | Non-Goal: Spec 0071 test-execution-cost boundary is not widened | Maintainer; Task commit diff and full gate; backend/docs | Inspect changed paths and gate behavior; expect no removed/bypassed checks or Spec 0071 scope creep | Confirm full gate command and repository history | No `Makefile`, CI workflow, or Spec 0071 implementation path changed; the only 0071 path is the deliberately reconciled coverage record. The unchanged full gate passed. | pass |
| 23 | Gate-cycle attribution is measured, trim is semantics-preserving, and dominant untrimmable cost is explicit | Maintainer; attribution and focused behavior; backend/docs | Inspect committed measurements and reproduce focused behavior; expect evidence-backed delta with unchanged outputs | Compare current verdict suite and attribution source boundaries | `gate-cost-2026-08-03.md` records the 625.710s cycle, before/after medians, and 468.669s Agent-work share. Current Verification passes the unchanged verdict suite. | pass |
| 24 | Pull Request #87 is open on the target branch with current-head checks, review state, thread evidence, and Merge-Ready status observed read-only | Maintainer; GitHub PR #87; Pull Request | Read PR metadata, checks, reviews, and all threads; expect current head, passing checks, approval, and zero unresolved findings | Refresh all reads and compare the head/review/thread set | F-003 remains. The final refresh shows exact head `756b431`, OPEN, non-draft, all four checks successful, and `mergeStateStatus: CLEAN`; CodeRabbit's current-head review is `CHANGES_REQUESTED` with four new unresolved, non-outdated threads, so the Pull Request is not Merge-Ready. | fail |

## Findings

### F-001 — Tooling Authority remains incomplete and misordered

Impact: `Blocks-Completion`. Repeated from both prior reports and expanded by
the complete current-history audit.

The maintainer reaches the Project Constraint audit before public flow QA.
The active PRD still records only “the owned skill pair,” while the TechSpec
names `.agents/skills/write-tasks/**`, `.agents/skills/qa-gate/**`, and their
mirrors. The PRD therefore still lacks exact repository-relative paths in both
active artifacts.

The complete chronology adds three independent shape defects:

1. Task 04 commit `160d417` changed the canonical `write-tasks` and `qa-gate`
   skills, mirrors, and derived pins at 11:28. The Spec-specific exact-path
   addendum landed later in `44b1fd7` at 12:17. `git merge-base
   --is-ancestor 44b1fd7 160d417` exits 1. The addendum itself states that the
   earlier grant did not list 0072 and that a Spec not listed had no
   authorization, so the exact authorization was late even though the quoted
   maintainer direction makes its intent traceable.
2. Commit `44b1fd7` folds that late authorization record together with the
   coverage record, catalog diagnostic golden, and four plan-characterization
   goldens used to repair the Task 04 verification fallout. Those paths are
   not bounded in either active Spec artifact and are outside the exact
   ADR-0081 fallout list stated by the authorization record.
3. Commit `a12c1a6` adds the authorization for
   `.agents/skills/roundfix/**` and changes that canonical skill plus its
   mirror in the same commit. The authorization is therefore folded into the
   protected change instead of preceding it, and neither active artifact
   bounds the `roundfix` skill. Commit `bd581c4` later carries the consequent
   regenerated setup/catalog/parity/characterization paths inside the same
   multi-issue review-fix commit rather than as its own consequent-fix commit.

The remaining audit checks pass: `git diff-tree --no-commit-id --name-status
-r` was run for every Spec, QA, remediation, review-fix, and review-artifact
commit; all three canonical skill trees equal their shipped mirrors; the
current `HEAD` was archived to an isolated directory and `rtk make
baseline-digests` exited 0 with `changed:false`; and the review-artifact commit
`756b431` is an exact docs-only child of `bd581c4` under
`docs/specs/_reviews/pr-87/round-001/`.

Expected: exact bounded paths exist in both active Spec artifacts and in an
authorization commit before each protected edit; prerequisite and consequent
fixes occupy correctly ordered, separate commits. Actual: the PRD boundary is
still categorical, the Task 04 authorization is late, the later skill
authorization is folded into its edit, and corrective tooling fallout is
mixed with other changes.

Affected rows: 5 and 7–21.

### F-003 — Current-head CodeRabbit review has four unresolved findings

Impact: `Blocks-Completion`. Repeated from the prior report with fresh
current-head findings after the review-resolution Run.

Pull Request #87 is open, non-draft, and points to the exact report build on
`ma/qa-task-not-flag`. During this gate, CodeRabbit moved from pending to
success and submitted a current-head `CHANGES_REQUESTED` review. The final
complete thread read has twelve threads and no next page: the original eight
are resolved, while four new current, non-outdated threads remain unresolved:

- two stale or ambiguous authored-gate statements in
  `.agents/skills/roundfix/SKILL.md`;
- an unchecked withheld-gate progress write in
  `internal/daemon/task_engine.go`;
- missing `%w` context at the new manifest-decode and QA-validation boundaries
  in `internal/spec/spec.go`.

Both title checks, the Verification gate, and CodeRabbit report success, and
GitHub reports `mergeStateStatus: CLEAN`; those signals do not override the
four unresolved Review Issues or `reviewDecision: CHANGES_REQUESTED`.

Expected: accepted Review Source Evidence verifies the current head with zero
unresolved Review Issues. Actual: the check succeeds on the exact head but the
current review requests changes and four threads remain unresolved.

Affected row: 24.

## Blocked and skipped

- Rows 7–21 are `blocked (finding: F-001)`. The QA contract stops public flow
  testing after a failed complete Tooling Authority audit. Unblock by putting
  every exact protected path in both active Spec artifacts, then rewriting the
  branch history so each authorization precedes its protected change and each
  prerequisite or consequent fix has the required separate chronological
  shape. Rerun all public journeys afterward.
- No row is environment-blocked or skipped. No row remains pending.

## Coverage

Results: 7 passed, 2 failed, 15 finding-blocked, 0 environment-blocked, 0
skipped, and 0 pending. `rows_blocked_environment: 0` and
`rows_blocked_finding: 15` match the table exactly.

The matrix maps every Core Feature, Success Metric, Non-Goal, Project
Constraint class, declared surface, prior finding, and the supplied Pull
Request. Explicit-empty input, out-of-order graph growth, replay after
settlement, repeated no-op invocation, controlled dependency failure, and
fresh event confirmation remain explicitly unexecuted because F-001 stops
flow QA. Pull Request metadata was refreshed through the current-head review,
and the complete thread set was re-read after it changed. No frontend surface
is declared.

## Final verdict

Fail: repository Verification is green, but the Tooling Authority chronology
and active-artifact boundaries remain invalid and PR #87 has four unresolved
current-head review findings; counts are 7 pass, 2 fail, 15 finding-blocked, 0
environment-blocked, and no skipped or pending rows.
