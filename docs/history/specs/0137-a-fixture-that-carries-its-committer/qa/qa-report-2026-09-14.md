---
spec: 0137-a-fixture-that-carries-its-committer
date: 2026-09-14
build: 433d2280b96e21e8b6be7de2cf69300814d4a489
auditing_binary: "0.12.0 (433d2280-dirty, built 2026-09-14 12:47:54 -0300)"
auditor_staleness: "current: rebuilt from audited HEAD 433d2280; dirty suffix records the seeded QA report"
status: closed
verdict: pass
rows_blocked_environment: 1
rows_blocked_finding: 0
rows_blocked_declared: 0
surfaces: [backend]
---

# QA Report

## Performed repairs

None.

## Assigned repair failures

None.

## Authorization audit inputs

None.

## Mechanical findings

None.

## Results

| # | Status | Provenance |
| - | --- | --- |
| QA-01 | pass | Task 02 criterion 3; repository Verification |
| QA-02 | pass | PRD Goal 1; Task 01 criterion 1; Task 02 criteria 1-2 |
| QA-03 | pass | Task 01 criterion 2; Task 02 criterion 1 |
| QA-04 | pass | Task 01 criterion 3; Task 02 criterion 1 |
| QA-05 | pass | Task 02 criterion 4; package vetting |
| QA-06 | pass | Task 02 criterion 5; outside acceptance evidence |
| QA-07 | blocked (environment: Pull Request head predates audited build) | Pull Request approval, checks, review and ancestry controls |
| QA-08 | pass | PRD Non-Goals and scope-creep sweep |

## Mechanical skips

| Detector | Missing artifact |
| --- | --- |
| authorization bounded paths | tooling authorization |
| consequent-fix commit order | consequent-fix declaration |
| QA Report structure | QA Report |
| QA evidence paths | QA Report |

## Scope and environment

Full terminal QA of build `433d2280b96e21e8b6be7de2cf69300814d4a489`
from the Run Worktree on branch
`roundfix/run-run_20260914T152430Z_2caad77c6ce973f2`. The intended actor is a
Roundfix maintainer or CI operator exercising the Daemon Task-cycle fixture,
the repository gate, Git identity resolution, and Pull Request #188 through
their public command surfaces. No production data or service is involved.

The host default is Go 1.27.1. Every verdict-bearing Go, gofmt, build, test and
vet command in this gate selects the repository's Go 1.26 line explicitly with
`GOTOOLCHAIN=go1.26.7`. The gate rebuilt `bin/roundfix` from the assembled tree
with `GOCACHE=/private/tmp/roundfix-go-build-0137-qa`; `bin/roundfix --version`
reported `roundfix 0.12.0 (433d2280-dirty, built 2026-09-14 12:47:54 -0300)`
after the documentation boundary's rebuild.

`bin/roundfix spec check 0137-a-fixture-that-carries-its-committer --strict`
exited 0 before the matrix ran: no findings and authored Verification commands
were not executed. `_tasks.md` names `task_02` as the unique terminal QA node,
its sole dependency `task_01` is completed, and the Spec declares no
unreachable acceptance.

## Static gate

- `rtk proxy env GOTOOLCHAIN=go1.26.7 GOCACHE=/private/tmp/roundfix-go-build-0137-qa rtk make build`: pass; rebuilt the audited CLI binary.
- `rtk ./bin/roundfix spec check 0137-a-fixture-that-carries-its-committer --strict`: pass; no findings.
- `rtk proxy env GOTOOLCHAIN=go1.26.7 GOCACHE=/private/tmp/roundfix-go-build-0137-qa rtk make GOFMT=/Users/marcio/go/pkg/mod/golang.org/toolchain@v0.0.1-go1.26.7.darwin-arm64/bin/gofmt verify`: pass. The restricted attempt was interrupted when an existing integration test reached `api.github.com`; the unchanged full-access rerun exited 0, all Go packages and skill checks passed, and the binary rebuilt.
- `rtk proxy env GOTOOLCHAIN=go1.26.7 GOCACHE=/private/tmp/roundfix-go-build-0137-qa rtk make GOFMT=/Users/marcio/go/pkg/mod/golang.org/toolchain@v0.0.1-go1.26.7.darwin-arm64/bin/gofmt verify-docs`: pass; documentation contracts, repository regeneration contracts, corpus budget and the active-Spec consistency sweep exited 0.

## Planned row details

### QA-01

- Actor / entry / surface: CI operator; `make verify`; repository backend gate.
- Steps and expected observable: run the complete selected Verification on Go 1.26.7; expect exit 0 through formatting, tests, skill checks and build.
- Independent confirmation / persistence: re-read the final exit code and fresh Git status after the gate.
- Evidence: the complete command exited 0. `internal/cli` completed in 72.562s,
  `internal/daemon` completed in 8.170s, every other Go package passed or had no
  tests, the focused skill contract passed, `roundfix skills check` passed for
  all 14 owned skills, and the final CLI build succeeded. A fresh
  `git status --short --untracked-files=all` found only this seeded QA report;
  Git's optional fsmonitor query emitted an environment diagnostic but the
  status read exited 0.

```yaml
inputs:
  - kind: repository_path
    ref: "**/*.go"
  - kind: repository_path
    ref: Makefile
  - kind: repository_path
    ref: .agents/skills/**
  - kind: repository_path
    ref: skills/**
```

### QA-02

- Actor / entry / surface: CI operator; focused `go test` over the Daemon Task-cycle fixture; backend and real Git subprocesses.
- Steps and expected observable: copy the once-built seed, exclude host, global and system Git configuration, resolve the copy's persisted name and email, and let the Daemon's plain Git committer complete.
- Independent confirmation / persistence: rerun the journey in the same test process and read both identity keys from the copied repository after setup.
- Evidence: `rtk proxy env GOTOOLCHAIN=go1.26.7 GOCACHE=/private/tmp/roundfix-go-build-0137-qa GIT_CONFIG_GLOBAL=/dev/null GIT_CONFIG_SYSTEM=/dev/null GIT_CONFIG_NOSYSTEM=1 rtk go test -count=2 -v ./internal/daemon -run '^(TestTaskCycleFixtureSeedCarriesACommitterIdentity|TestTaskCycleRealRepoCommitsPerTaskExcludingPreexistingDirt)$'` exited 0 with 8 passing tests. Each of two invocations copied the package seed, read non-empty `user.name` and `user.email` from the copy under the isolated Git environment, and completed the Daemon's real `GitCommitter` journey under the process-level no-global/no-system controls. Repetition in one test binary exercised the inherited repository config after `sync.Once` had already fired.

```yaml
inputs:
  - kind: repository_path
    ref: internal/daemon/task_engine_test.go
  - kind: repository_path
    ref: internal/gittest/gittest.go
```

### QA-03

- Actor / entry / surface: CI operator; focused real-repository Task-cycle journey; backend and real Git repository.
- Steps and expected observable: settle two Tasks through the Daemon; expect one commit per Task and pre-existing dirt to remain uncommitted.
- Independent confirmation / persistence: fresh Git history and status reads inside the journey.
- Evidence: `rtk proxy env GOTOOLCHAIN=go1.26.7 GOCACHE=/private/tmp/roundfix-go-build-0137-qa GIT_CONFIG_GLOBAL=/dev/null GIT_CONFIG_SYSTEM=/dev/null GIT_CONFIG_NOSYSTEM=1 rtk go test -count=1 -v ./internal/daemon -run '^TestTaskCycleRealRepoCommitsPerTaskExcludingPreexistingDirt$'` exited 0. The public package journey ran the real `GitCommitter` twice, then independently read `HEAD~1`, `HEAD`, each commit's changed paths, Task status and porcelain status: each Task had its own correctly attributed commit and `user-wip.txt` remained untracked.

```yaml
inputs:
  - kind: repository_path
    ref: internal/daemon/task_engine_test.go
  - kind: repository_path
    ref: internal/daemon/task_engine.go
```

### QA-04

- Actor / entry / surface: CI operator; repeated shared-seed journey; backend and filesystem.
- Steps and expected observable: exercise the fixture twice in one test process; expect one seed creation while copy isolation, graph isolation, provenance and the inherited identity remain valid.
- Independent confirmation / persistence: the second invocation reuses the same once-only seed and independently checks a fresh copy.
- Evidence: `rtk proxy env GOTOOLCHAIN=go1.26.7 GOCACHE=/private/tmp/roundfix-go-build-0137-qa GIT_CONFIG_GLOBAL=/dev/null GIT_CONFIG_SYSTEM=/dev/null GIT_CONFIG_NOSYSTEM=1 rtk go test -count=2 -v ./internal/daemon -run '^(TestTaskCycleFixtureSeedIsCreatedOnce|TestTaskCycleFixtureSeedCarriesACommitterIdentity)$'` exited 0 with 8 passing tests. Both count invocations observed exactly one package-seed creation. Fresh copies kept distinct writable repositories and Task graphs, did not observe each other's marker, retained committed authorization provenance, and independently resolved both identity keys.

```yaml
inputs:
  - kind: repository_path
    ref: internal/daemon/task_engine_test.go
```

### QA-05

- Actor / entry / surface: Go maintainer; `go vet ./internal/daemon`; backend package.
- Steps and expected observable: vet the changed package on Go 1.26.7; expect no diagnostics.
- Independent confirmation / persistence: record the command exit and compare any diagnostic with the delivery target if one appears.
- Evidence: `rtk proxy env GOTOOLCHAIN=go1.26.7 GOCACHE=/private/tmp/roundfix-go-build-0137-qa rtk go vet ./internal/daemon` exited 0 with no diagnostics. No delivery-target comparison was needed because the current package was clean.

```yaml
inputs:
  - kind: repository_path
    ref: internal/daemon/**
```

### QA-06

- Actor / entry / surface: CI operator; GitHub Pull Request #188 check history and Git's public CLI; external repository and Git boundary.
- Steps and expected observable: read the failing Pull Request gate that reported `Author identity unknown`, then reproduce Git's no-identity refusal in a disposable repository with host, global and system configuration excluded.
- Independent confirmation / persistence: compare the GitHub job record with a fresh local Git invocation and preserve both exact exit results.
- Evidence: Pull Request #188's [Verification gate job](https://github.com/marcioaltoe/roundfix/actions/runs/34861114110/job/104033261234) ran `make verify` on head `e2b95b3545d9cc1cb06a65f41b8e4fe138d77ae6` with Go 1.26 and failed `TestTaskCycleRealRepoCommitsPerTaskExcludingPreexistingDirt` at 2026-09-14T15:18:29Z. The public job log records `git commit` exit 128, `Author identity unknown`, and `fatal: empty ident name`. Independent confirmation used a new repository at `/private/tmp/roundfix-qa-0137-no-identity.9OEJOM`: `git commit --allow-empty` under `GIT_CONFIG_GLOBAL=/dev/null`, `GIT_CONFIG_SYSTEM=/dev/null`, `GIT_CONFIG_NOSYSTEM=1`, and `user.useConfigOnly=true` exited 128 with `Author identity unknown` and `fatal: no email was given and auto-detection is disabled`. This evidence originates from GitHub Actions and the installed Git CLI, not from the Spec's own fixture.

```yaml
inputs:
  - kind: external_repository
    ref: marcioaltoe/roundfix#188
  - kind: live_service
    ref: GitHub Actions
```

### QA-07

- Actor / entry / surface: maintainer; GitHub Pull Request #188; Pull Request controls.
- Steps and expected observable: observe approval, checks and status, unresolved review threads, Merge-Ready acceptance, and review-artifact ancestry for the target branch head.
- Independent confirmation / persistence: compare the Pull Request head with local `origin/feat/spec-contained-authorization` and the Run's audited commit ancestry.
- Evidence: [Pull Request #188](https://github.com/marcioaltoe/roundfix/pull/188) is open from `feat/spec-contained-authorization` at `e2b95b3545d9cc1cb06a65f41b8e4fe138d77ae6`; local `origin/feat/spec-contained-authorization` resolves to that same commit, and Git proves it is an ancestor of audited build `433d2280b96e21e8b6be7de2cf69300814d4a489`. Approval: no reviews and no review decision. Checks: `Validate PR title` passed, while `Verification gate` failed on the identity defect this build repairs; the equivalent complete local `make verify` and required `make verify-docs` both passed on the audited build. Unresolved threads: GraphQL returned `totalCount: 0`, `hasNextPage: false`. Merge-Ready acceptance: none; GitHub reports `mergeStateStatus: BLOCKED` and no Review Source acceptance. Review-artifact ancestry: no `reviews/` artifact exists anywhere in this Spec's history, so none is claimed. The row cannot observe PR controls for `433d2280` until the Daemon integrates the QA report and the target branch is published; no push or PR mutation is authorized here.

```yaml
inputs:
  - kind: external_repository
    ref: marcioaltoe/roundfix#188
  - kind: live_service
    ref: GitHub
```

### QA-08

- Actor / entry / surface: maintainer; Git diff and repository paths; backend scope audit.
- Steps and expected observable: inspect the Task commit and current delta; expect no production code, Daemon commit behavior, staging behavior, authority behavior, unrelated fixture, dependency, or wall-clock-budget change.
- Independent confirmation / persistence: compare the Task commit's path list and source diff with its parent and re-read current status.
- Evidence: `git diff-tree --no-commit-id --name-status -r 433d2280` listed exactly `task_01.md` and `internal/daemon/task_engine_test.go`. The source diff adds one `gittest.PersistIdentity` call inside the existing `sync.Once` seed builder, one import, and one copied-repository identity test. A `git diff --quiet` control confirmed no change to `go.mod`, `go.sum`, `Makefile`, `internal/daemon/task_engine.go`, or `internal/gittest/gittest.go`. Fresh status contains only this seeded QA report. No production commit/staging/authority behavior, unrelated fixture, dependency, tool configuration or suite-budget path changed.

```yaml
inputs:
  - kind: repository_path
    ref: internal/daemon/task_engine_test.go
  - kind: repository_path
    ref: docs/specs/0137-a-fixture-that-carries-its-committer/task_01.md
  - kind: repository_path
    ref: go.mod
  - kind: repository_path
    ref: go.sum
```

## Findings

None.

## Blocked and skipped

- QA-07 — `blocked (environment: Pull Request head predates audited build)`.
  Cause: Pull Request #188 and `origin/feat/spec-contained-authorization` both
  point to `e2b95b35`, while the audited Task commit is its descendant
  `433d2280` on the unpushed per-Run branch. Unblocking action: the Daemon must
  commit this report, integrate the Run branch into the target branch and let
  the authorized delivery flow publish that head. Equivalent evidence records
  every control: no approval exists; both required local gates passed for the
  audited build; zero review threads exist; no Merge-Ready acceptance exists;
  and no review artifact or ancestry is claimed.
- The four entries under `Mechanical skips` are preserved Daemon
  materialization records. They are not skipped QA matrix rows and do not
  change the counters.

## Coverage

Closed: 8 rows — 7 pass, 0 fail, 1 blocked by environment, 0 blocked by
finding, 0 blocked by declaration, 0 pending and 0 skipped. Task 01's three
Acceptance Criteria, the PRD Goal, the complete repository gate, package vet,
outside evidence, Pull Request controls and every Non-Goal were covered. The
high-risk probes excluded global and system Git identity, disabled identity
auto-detection, repeated the fixture journeys after `sync.Once`, re-read Git
history/status/configuration, compared the Task commit with its parent, and
bound the live Pull Request evidence to its exact head.

## Final verdict

`pass`: 7 of 8 rows passed with no findings, declared blocks or skipped rows.
QA-07 remains environment-blocked because the Open Pull Request carries the
pre-fix ancestor; its cause, unblocking action and equivalent evidence answer
all five Pull Request controls for the audited build.
