---
spec: 0048-context-driven-project-decisions-and-spec-constraints
date: 2026-07-25
build: fd5068dfe12a2fac24aed74cedcfa5480cfe8f07
status: closed
verdict: pass
surfaces: [cli, infra, docs]
---

# QA report — Context-Driven project decisions and Spec constraints

## Scope and environment

This full rerun validates all six PRD user stories, all 17 Core Features, all
45 Task acceptance criteria, the active-Spec Project Constraint contract,
declared CLI, infra, and documentation surfaces, and every non-goal group that
needs a scope-creep check. The actor is a repository maintainer, automation
author, Spec author, or Agent using the built `roundfix` CLI and the shipped
repository guidance.

- Build: `fd5068dfe12a2fac24aed74cedcfa5480cfe8f07`
- Repository: the integrated `ma/context-driven-baseline-specs` checkout
- Application entry point: a fresh locally built `roundfix` binary
- Evidence directory:
  `qa/evidence/2026-07-25-project-decisions-rerun/`
- Real-user conditions: macOS, `America/Sao_Paulo`, terminal CLI, local
  disposable repositories, no browser surface
- Parity gaps: none. The formatter journey runs with a closed registry and a
  fresh fixture-owned cache.

All eight Task files are marked `completed` and carry a `## Result`. A row
passes only where fresh independent checks or live journeys prove it.

High-risk probes: missing and conflicting structured decisions, accepting all
compatible persisted defaults on update, wrong Plan Digest, empty reapply,
and attempted mutation outside a tooling authorization allowlist. These probe
the feature's fail-closed, persistence, and tooling-ownership contracts.

## Current rerun matrix

Every row was created as `pending` before execution. A Task row covers each
numbered acceptance criterion in that Task, in order; the evidence proves every
listed clause before the group passes.

| # | Story / criterion / sweep | Actor, entry point, and surface | Expected observable and independent confirmation | Evidence | Status |
| - | --- | --- | --- | --- | --- |
| PC-01 | PRD and TechSpec account for identifier strategy, authentication and HTTP, active ADR obligations, and tooling authority with applicability reasons and operative `docs/agents/` sources | Spec author; active artifacts; docs | Both artifacts contain all four sourced rows; reread from disk | Four sourced rows confirmed in both active artifacts. [Command evidence](evidence/2026-07-25-project-decisions-rerun/command-evidence.md) | pass |
| PC-02 | Every protected tooling mutation has express authorization, exact bounded files, and commit-path compliance | Maintainer; Task commits plus current delta; docs/infra | `git diff-tree` paths remain within explicit authorization, or no protected mutation exists; confirm through final diff | The reopened Task removed the temporary test-only pin; the final Spec delta adds no protected tooling artifact or version pin. [Command evidence](evidence/2026-07-25-project-decisions-rerun/command-evidence.md) | pass |
| US-01 / CF-01–04 | UUID version 7 is a visible explicit keep-or-change suggestion only for new project-owned Internal Identifiers; all four source-owned categories remain excluded | Maintainer; public root command and generated domain guide; CLI/docs | PTY prompt shows the suggestion; applied guide scopes it correctly; reopen generated file after apply | Both PTY flows and fresh generated domain reads passed; 18 Task 01 checks passed. [Command evidence](evidence/2026-07-25-project-decisions-rerun/command-evidence.md) | pass |
| US-02 / CF-05–07 | Better Auth conditionally proposes one typed provider-owned `GET`/`POST /api/auth/*` exception, requires an explicit answer, reuses compatible persisted values, and fails conflicts closed | Maintainer; root command and automation Plan; CLI | Greenfield and compatible update reach Plans; incompatible Decision Document fails before a Plan; rerun/read normalized Plan | Greenfield and keep-defaults update applied; explicit conflict named both decisions and left the clone clean. [Command evidence](evidence/2026-07-25-project-decisions-rerun/command-evidence.md) | pass |
| US-03 / CF-03,06–08 | Automation receives stable unresolved IDs, never accepts a suggestion implicitly, retains deterministic identities, and matches equivalent human answers | Automation author; `baseline plan`; CLI | Missing input exits action-required without mutation; equivalent answers produce identical Plan Digests; fresh repository status confirms no write | Missing input exited `3` with all stable IDs and no mutation; parity and identity checks passed. [Command evidence](evidence/2026-07-25-project-decisions-rerun/command-evidence.md) | pass |
| US-04 / CF-12–15 | New PRD and TechSpec artifacts carry complete Project Constraints before decomposition; legacy Specs remain byte-identical | Spec author; shipped authoring and downstream workflow skills; docs/CLI | Contract fixtures accept complete active artifacts, reject incomplete ones, and exempt proven legacy artifacts; reread fixtures | Active artifacts and complete/incomplete/legacy contract fixtures passed 12 focused checks. [Command evidence](evidence/2026-07-25-project-decisions-rerun/command-evidence.md) | pass |
| US-05 / CF-10–13 | Universal tooling authority has no opt-out and refuses missing, unbounded, or exceeded authorization before settlement | Maintainer and Agent; generated core guide and workflow skills; CLI/docs | Complete clause renders for every Profile; bounded fixture passes; missing and out-of-scope paths refuse; fresh guide read confirms persistence | Fifteen clause checks and ten authoring/tooling journeys passed; live generated clause remained complete. [Command evidence](evidence/2026-07-25-project-decisions-rerun/command-evidence.md) | pass |
| US-06 / CF-09,14–17 | Semantic guides and Spec artifacts are self-contained, safe, synchronized, and independent of ADR presence or thin-skill logic | Agent; generated guides, canonical assets, and thin setup skill; docs/CLI | No-ADR render remains operative; unsafe structured content fails; assets and skills agree; reread generated output | Render, safety, no-ADR, asset, documentation, thin-skill, and skill-sync checks passed. [Command evidence](evidence/2026-07-25-project-decisions-rerun/command-evidence.md) | pass |
| T01-AC01–05 | UUID suggestion; automation action-required; conditional complete Better Auth object; non-Better-Auth omission; compatible reuse and invalid-object refusal | Maintainer/automation; focused engine and CLI checks | Every Task 01 named test executes and passes; public missing-input and prompt observations confirm the seam | 18 focused checks plus public prompt and missing-input observations passed. [Command evidence](evidence/2026-07-25-project-decisions-rerun/command-evidence.md) | pass |
| T02-AC01–06 | One derived exception; idempotent reapply; conflict refusal; deterministic ordering; human/automation digest parity; compatible Fluxus-style keep-defaults update | Maintainer/automation; engine, CLI, and public update | Named tests pass; live update accepts both defaults and reaches a Plan; explicit conflict still fails | Nine focused checks, live update, conflict probe, and reapply passed. [Command evidence](evidence/2026-07-25-project-decisions-rerun/command-evidence.md) | pass |
| T03-AC01–05 | Identifier distinctions; complete backend contract; no-ADR operation; unsafe-marker refusal; formatter and empty-reapply zero delta | Agent; engine plus generated guides | Named render tests pass; fresh generated reads and reapply confirm persisted output | Ten focused checks, formatter, guide rereads, zero-change plan, and reapply passed. [Command evidence](evidence/2026-07-25-project-decisions-rerun/command-evidence.md) | pass |
| T04-AC01–05 | Complete clause in every Profile; no prompt; no exclusion; all categories; catalog accounting failure on removal | Maintainer; all-Profile generation and catalog mutation tests | Named tooling tests pass and live generated guide contains the clause | Fifteen focused checks and live generated-clause read passed. [Command evidence](evidence/2026-07-25-project-decisions-rerun/command-evidence.md) | pass |
| T05-AC01–05 | Four sourced constraint areas; applicability reasons; bounded authorization; no frontmatter field; upstream bytes unchanged | Spec author; authoring skill contract tests and file audit | Named tests pass; template and provenance reads independently confirm | Focused artifact, authorization, frontmatter, and provenance checks passed. [Command evidence](evidence/2026-07-25-project-decisions-rerun/command-evidence.md) | pass |
| T06-AC01–06 | Active-Spec decomposition refusal; tooling start refusal; exact allowlist; QA detects missing/exceeded scope; legacy bytes unchanged; release fixture is hermetic | Spec author/Agent; workflow skill tests and release journey | Named tests plus release journey pass; commit-path audit independently confirms no unauthorized final pin | Workflow and scope checks, final pin audit, and the offline release journey passed with the registry forced to a closed loopback endpoint. [Command evidence](evidence/2026-07-25-project-decisions-rerun/command-evidence.md) | pass |
| T07-AC01–05 | Correct Profile decisions; byte-identical canonical/distributed assets; examples parse; thin skill has no engine; upstream and branding guards pass | Maintainer; public help, asset audit, skill checks, docs | Focused tests, asset check, skill check, and branding search pass | 23 focused checks, public help, zero-drift asset audit, and all 14 skill checks passed. [Command evidence](evidence/2026-07-25-project-decisions-rerun/command-evidence.md) | pass |
| T08-AC01–08 | Plan parity; fail-closed inputs; complete Spec fixtures; bounded refusal; all-Profile lifecycle; fresh Fluxus QA rows; named tests execute meaningful assertions; no protected tooling pin | Maintainer/automation; macro tests and live Fluxus clones | Named tests are listed and pass; fresh greenfield/update transcripts and final diff satisfy every row | Required tests, the offline named suite, all-Profile lifecycle, and both fresh Fluxus journeys passed; no formatter pin or registry dependency remains. [Command evidence](evidence/2026-07-25-project-decisions-rerun/command-evidence.md) | pass |
| J-01 / SM-01–08 | Greenfield completes plan, exact-digest apply, formatter, repository Verification, fresh zero-change plan, and verified empty reapply | Fluxus maintainer; disposable clone; CLI/infra/docs | Apply reports verified; generated guides persist after reread; Fluxus gate passes; replan has zero file changes and reapply verifies | Fresh Plan/apply, 589-file formatter, Fluxus Verification, guide rereads, zero-change replan, and verified reapply passed. [Command evidence](evidence/2026-07-25-project-decisions-rerun/command-evidence.md) | pass |
| J-02 / SM-01–08 | Update reuses compatible decisions and completes the same lifecycle without manual constraint repair or drift | Fluxus maintainer; disposable clone; CLI/infra/docs | Accepting every displayed default reaches the Plan; apply, Verification, audit/replan, and empty reapply pass | PTY keep-defaults update, formatter, Fluxus Verification, zero-change rerun, and verified reapply passed. [Command evidence](evidence/2026-07-25-project-decisions-rerun/command-evidence.md) | pass |
| DOC-01 / SM-09 | Public docs and thin-skill instructions match the CLI; canonical and distributed assets have zero drift | Maintainer; help, docs examples, asset and skill checks; CLI/docs | Published examples parse and checks report zero drift; independent public help agrees | Documentation contracts, public help, asset audit, and thin-skill checks passed. [Command evidence](evidence/2026-07-25-project-decisions-rerun/command-evidence.md) | pass |
| NG-01 | No identifier migration or replacement of provider, protocol, natural, or business identities | Maintainer; Plan and generated domain guide; CLI/docs | Plan changes guidance only and generated scope preserves all exclusions | Both Plans changed Baseline guidance only; the generated domain guide preserves every excluded category. [Command evidence](evidence/2026-07-25-project-decisions-rerun/command-evidence.md) | pass |
| NG-02 | No Better Auth install/provider change/application route creation or policy on Profiles without Better Auth | Maintainer; all-Profile Plans and disposable clone status; CLI/infra | Changed paths contain no application routes or provider install; non-Better-Auth Profile omits decision | Live changes remained in managed guidance; conditional Profile checks passed. [Command evidence](evidence/2026-07-25-project-decisions-rerun/command-evidence.md) | pass |
| NG-03 | No ADR runtime dependency, Baseline tooling mutation, legacy rewrite, upstream-skill mutation, or Spec 0047 composition change | Maintainer/Agent; plans, tests, asset/provenance audits; docs/infra | No-ADR and legacy tests pass; Plans and diffs exclude protected/out-of-scope paths | No-ADR, legacy, provenance, final tooling-delta, asset, and composition checks passed. [Command evidence](evidence/2026-07-25-project-decisions-rerun/command-evidence.md) | pass |
| V-01 / SM-10 | Full Roundfix Verification passes on the current build after fresh live evidence is recorded | Maintainer; `rtk make verify`; infra | Exact repository gate exits 0 with no format, test, skill, sync, or build failure | Exact gate passed: 2,344 Go tests, 4 skill-runtime tests, all 14 skill checks, and build. | pass |

## Static gate

- `rtk env BUN_CONFIG_REGISTRY=http://127.0.0.1:1 go test -count=1
  ./internal/baseline ./internal/cli ./skills -run
  'TestProjectDecisionJourney|TestProjectConstraintJourney|TestToolingAuthorizationJourney|TestBaselineReleaseGate'`:
  passed in all three packages with a fresh local formatter fixture.
- `rtk make verify`: passed with 2,344 Go tests, 4 skill-runtime tests, all 14
  repository-owned skill checks, and the final build.
- `rtk go run -buildvcs=false ./cmd/roundfix skills check`: all 14
  repository-owned skills passed.
- Canonical asset audit: `ok: true`, zero findings and zero planned changes.
- `rtk git diff --check`: passed.
- Both disposable Fluxus clones passed their exact `rtk make verify` after
  dependency trees from the clean same-commit source checkout were
  pre-provisioned.

## Results summary

The Task 02 update-default conflict is fixed. A fresh public PTY update reused
the exact persisted Better Auth rationale, accepted every displayed default,
reached and applied a Plan, passed Fluxus Verification, and completed a
zero-change verified reapply. Greenfield, fail-closed, generated-guidance,
Project Constraint, tooling-authorization, asset, documentation, and non-goal
checks also passed.

No product-behavior row failed. The final repair replaced the prior
registry-backed formatter installer with repository-local fixture bytes; the
offline journey and full gate now pass with no blocked row.

## Findings

No current-build product finding was observed.

## Blocked and skipped

- Blocked: none.
- Skipped: none.

## Coverage

- User stories: 6 passed, 0 failed, 0 blocked
- PRD Core Features: 17 passed, 0 failed, 0 blocked
- Task acceptance criteria: 45 passed, 0 failed, 0 blocked
- Project Constraint audit rows: 2 passed, 0 failed
- Non-goal groups: 3 passed, 0 failed
- Matrix rows: 23 passed, 0 failed, 0 blocked, 0 skipped, 0 pending
- Behavior probes: missing input, wrong digest, explicit provider conflict,
  compatible keep-defaults update, formatter/Verification persistence, and
  empty reapply all passed

## Final verdict

Pass. Every PRD story, Core Feature, Task acceptance criterion, Project
Constraint row, live Fluxus journey, non-goal guard, offline formatter journey,
and the exact repository Verification gate passed.

## Prior build history — `10720c8`

The remainder of this report records the closed failing run that prompted the
Task 02, Task 06, and Task 08 repairs. It is historical evidence only and does
not determine the current verdict.

### Static gate

- `rtk make verify`: blocked after 2,291 passing tests and 2 failures in
  `TestGuidanceCompositionJourney/standard-typescript-monorepo`; Bun reported
  `error: bun is unable to write files to tempdir: PermissionDenied`.
- The focused journey reproduced the denial with `TMPDIR=/private/tmp`.
- Independent focused checks passed: 51 project-decision/rendering/tooling
  tests, 35 Project Constraint/documentation/asset/skill tests, 25 release-gate
  tests, all 14 repository-owned skill checks, 4 skill-sync checks, canonical
  asset audit, branding guard, and `git diff --check`.
- The disposable Fluxus greenfield repository passed its exact
  `rtk make verify` after lockfile-defined dependencies were restored.

### Results

| # | Story / criterion / sweep | Actor and surface | Status | Evidence |
| - | --- | --- | --- | --- |
| PC-01 | Active PRD and TechSpec account for identifier strategy, authentication and HTTP, active ADR obligations, tooling authority, applicability reasons, and operative `docs/agents/` sources | Spec author · docs | fail | The TechSpec cites operative sources; the active PRD cites none. [Command evidence](evidence/2026-07-25-project-decisions/command-evidence.md) |
| PC-02 | Tasks that mutate protected repository tooling have express maintainer authorization, exact bounded files, and commit-path compliance | Maintainer · docs/infra | fail | Task 06 commit `7cef20d` introduced the Oxfmt `0.59.0` version pin in `internal/cli/baseline_release_gate_test.go`; both active Spec artifacts expressly grant no tooling authorization. [Command evidence](evidence/2026-07-25-project-decisions/command-evidence.md) |
| US-01 | UUID version 7 is a visible keep-or-change suggestion for new project-owned Internal Identifiers; source-owned identifiers remain excluded | Maintainer · CLI/docs | pass | The public PTY displayed the exact suggestion; the applied domain guide scopes it to new project-owned IDs and lists all four exclusions. 51 focused checks passed. [Command evidence](evidence/2026-07-25-project-decisions/command-evidence.md) |
| US-02 | Better Auth selection exposes and derives one complete provider-owned `GET`/`POST /api/auth/*` exception; non-Better-Auth Profiles omit it; conflicts stop planning | Maintainer · CLI | fail | Greenfield derivation passed, but the public update's persisted HTTP default and suggested Better Auth default carry different reasons and conflict when both are kept; F-01. |
| US-03 | Automation receives every unresolved decision as stable input; missing or conflicting objects stop without mutation; equivalent human and automation answers produce the same Plan Digest | Automation author · CLI | fail | Missing automation input exited `3` with every stable ID and no mutation. The human update could not reach a Plan with the displayed defaults, so live human/automation Plan parity failed; F-01. |
| US-04 | Newly authored PRD and TechSpec fixtures carry all four Project Constraint areas, applicability reasons, and operative source paths without authorization frontmatter; incomplete active Specs cannot decompose | Spec author · docs | fail | Focused artifact-contract tests passed, but this active PRD has no operative source paths and its Task Graph completed; F-02. |
| US-05 | The universal tooling clause covers every protected mutation and artifact category without a prompt or opt-out; missing, unbounded, and exceeded authorization refuse before settlement | Maintainer/Agent · CLI/docs | fail | Generated guidance and focused refusal tests passed, but Task 06 introduced and settled an unauthorized Oxfmt version pin; F-03. |
| US-06 | Domain and backend guidance carry complete operative rules without ADR dependency; unsafe structured content fails before postimage assembly | Agent · docs/CLI | pass | Fresh generated domain/backend reads contain the complete rules; structured-safety and no-ADR focused checks passed. [Command evidence](evidence/2026-07-25-project-decisions/command-evidence.md) |
| J-01 | Every affected Profile completes plan, apply, repository Verification recommendation, audit, and empty reapply with zero managed delta | Maintainer · CLI/infra | pass | Fluxus greenfield plan/apply, formatter, repository Verification, zero-change replan, and verified reapply passed. The maintained-profile release gate passed 25 checks. [Command evidence](evidence/2026-07-25-project-decisions/command-evidence.md) |
| J-02 | Update reuses compatible decisions, retains values through formatting and Verification, and completes audit plus empty reapply without duplication or drift | Maintainer · CLI/infra | fail | The public update accepted both defaults and exited `1` before a Plan because the provider and persisted HTTP exceptions conflict; F-01. |
| J-03 | Fresh Fluxus greenfield and update journeys reproduce the accepted guide set with no manual constraint repair | Fluxus maintainer · CLI/infra/docs | fail | Greenfield passed end to end. Update requires manual repair of one product-proposed reason before planning; F-01. |
| DOC-01 | Canonical and distributed assets agree; public examples parse through public CLI contracts; the thin setup skill delegates execution; upstream-managed skill bytes and project-agnostic guards pass | Maintainer · CLI/docs | pass | Asset audit returned `ok: true` with 0 findings; 35 focused tests, 14 skill checks, 4 sync checks, and the no-branding search passed. [Command evidence](evidence/2026-07-25-project-decisions/command-evidence.md) |
| NG-01 | No existing identifier is migrated and no provider, protocol, natural, or business identifier is replaced | Maintainer · CLI/docs | pass | The applied plan changed only Baseline guidance; generated domain guidance preserves all source-owned categories. |
| NG-02 | Baseline does not install Better Auth, change providers, create application routes, or impose Better Auth policy on Profiles without that capability | Maintainer · CLI/infra | pass | Fluxus file changes were confined to managed guidance and backup output; conditional-selection checks passed. |
| NG-03 | No ADR becomes an operative dependency; Baseline adoption does not mutate tooling; completed/archived Specs and upstream-managed skills remain unchanged; Spec 0047 composition semantics remain intact | Maintainer/Agent · docs/infra | pass | No-ADR, legacy-exemption, upstream-immutability, asset, and composition checks passed; the live Plan changed no tooling path. |
| V-01 | Full repository Verification passes on this build | Maintainer · infra | blocked | Unmodified `rtk make verify` is sandbox-blocked by Bun tempdir `PermissionDenied`; focused checks and disposable Fluxus Verification passed. |

High-risk probes are invalid structured values and out-of-order decisions
(US-02/US-03), repeated apply and reopen through audit/reapply (J-01/J-02),
and mutation outside an authorization allowlist (US-05). These target the
feature's fail-closed, persistence, and tooling-ownership risks.

### Findings

### F-01 — Fluxus update defaults conflict before a Plan

- Impact: Blocks-Completion
- Actor and step: Fluxus maintainer; public root Baseline prompts 6–7.
- Expected: keeping the persisted HTTP contract and the product's Better Auth
  suggestion produces one derived exception and reaches the Change Plan
  without manual constraint repair.
- Actual: the two defaults use different rationale strings. After all defaults
  are accepted, normalization exits `1` and names an
  `auth.provider`/`http.contract` conflict for Better Auth at `/api/auth/*`.
- Reproduction: clone clean Fluxus commit `1aeed7e`, run the built
  `roundfix baseline`, select Preservation, reuse the TypeScript Profile, and
  accept every default.
- Evidence:
  [command-evidence.md](evidence/2026-07-25-project-decisions/command-evidence.md)
- Affected rows: US-02, US-03, J-02, J-03.

### F-02 — The active PRD omits operative Project Constraint sources

- Impact: Trust-Damage
- Actor and step: Spec author; final active artifact audit.
- Expected: each Project Constraint row cites its operative
  `docs/agents/` source before Task decomposition and completion.
- Actual: `_prd.md` contains the four applicability rows but no
  `docs/agents/` source. `_techspec.md` does cite them.
- Reproduction: inspect `_prd.md` → `Project Constraints`.
- Evidence:
  [command-evidence.md](evidence/2026-07-25-project-decisions/command-evidence.md)
- Affected rows: PC-01, US-04.

### F-03 — Task 06 settled an unauthorized tooling version pin

- Impact: Trust-Damage
- Actor and step: Maintainer; Task commit-path authorization audit.
- Expected: no tooling version pin changes because the PRD and TechSpec
  expressly authorize none.
- Actual: commit `7cef20d` adds
  `baselineReleaseOxfmtVersion = "0.59.0"` and uses it in a generated fixture
  `package.json`; no bounded authorization names
  `internal/cli/baseline_release_gate_test.go`.
- Reproduction:
  `rtk git diff-tree --no-commit-id --name-only -r 7cef20d`, then
  `rtk git show 7cef20d -- internal/cli/baseline_release_gate_test.go`.
- Evidence:
  [command-evidence.md](evidence/2026-07-25-project-decisions/command-evidence.md)
- Affected rows: PC-02, US-05.

### F-04 — Task 08 was marked completed without result evidence

- Impact: Trust-Damage
- Actor and step: Maintainer; Task completion audit.
- Expected: the Task records a `## Result`, terminal acceptance evidence, and
  the journey work required by its criteria.
- Actual: commit `10720c8` changes only `status: pending` to
  `status: completed`; the Result is absent and every checkbox remains open.
  Three named journey test functions do not exist.
- Reproduction: inspect `task_08.md`, `rtk git show 10720c8`, and search the
  named test functions under `internal/` and `skills/`.
- Evidence:
  [command-evidence.md](evidence/2026-07-25-project-decisions/command-evidence.md)
- Affected rows: J-02, J-03.

### Blocked and skipped

- Blocked: V-01. A full-access rerun must allow Bun to write its
  fixture-owned temp directory, then run `rtk make verify` unchanged.
- Skipped: none.

### Coverage

- User stories: 2 passed, 4 failed, 0 blocked
- Task acceptance criteria: 32 passed, 8 failed, 0 blocked
- Project Constraint audit rows: 0 passed, 2 failed
- Non-goal groups: 3 passed, 0 failed
- Matrix rows: 7 passed, 8 failed, 1 blocked, 0 skipped, 0 pending
- Behavior probes: missing input and wrong digest passed without mutation;
  update default conflict failed; empty reapply passed

### Final verdict

Fail. Eight rows fail across one Blocks-Completion and three Trust-Damage
findings; one static-gate row remains environment-blocked. Repair the Fluxus
update default conflict, add operative PRD sources, resolve the unauthorized
Task 06 version pin, reopen Task 08 with real Result evidence, and rerun the
unmodified repository gate before repeating QA.
