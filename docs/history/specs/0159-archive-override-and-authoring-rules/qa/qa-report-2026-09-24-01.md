---
spec: 0159-archive-override-and-authoring-rules
date: 2026-09-24
build: 61e9c167f323cb802898f7c3b4fd6d0fa94ec634
auditing_binary: "0.16.0 (e38c8561, built 2026-09-24 08:07:17 -0300)"
auditor_staleness: "current: commit ancestry: build commit does not predate audited tree"
status: closed
verdict: pass
rows_blocked_environment: 1
rows_blocked_finding: 0
rows_blocked_declared: 0
surfaces: [backend, cli, docs]
---

# QA Report

## Scope and environment

- Scope: full rerun of the authored terminal gate for Task 04 after corrective
  Tasks 05 and 06. The declared coverage sources are Requirements 1-6, the
  outside-evidence row, the Pull Request row, and repository Verification.
  Requirement 7 constrains evidence quality because it starts with `MUST NOT`.
- Actors and surfaces: maintainers using `roundfix archive`, Spec authors using
  `roundfix spec check`, and documentation authors consuming workflow skills.
- Audited tree: Run Worktree HEAD
  `61e9c167f323cb802898f7c3b4fd6d0fa94ec634` on the Run branch.
- Pull Request fact: none open; the Pull Request row uses the
  environment-blocked equivalent-evidence path.
- The PRD declares no Unreachable Acceptance and no frontend surface.
- Authoring precondition: `roundfix spec check
  0159-archive-override-and-authoring-rules --strict` exited 0 with `No
  findings`; authored Verification commands were not executed.

## Static gate

- The Daemon ran repository Verification `make verify-changed` outside the
  Agent sandbox. It passed with exit 0 and removed diagnostics on success.
  This is a supervised gate fact; the Agent did not rerun it.
- `/private/tmp/roundfix-0159-qa-rerun` was built from audited HEAD with
  `GOCACHE=/private/tmp/roundfix-0159-qa-rerun-gocache go build -o
  /private/tmp/roundfix-0159-qa-rerun ./cmd/roundfix`; the build exited 0,
  `--version` reported `roundfix 0.16.0`, and SHA-256 was
  `fd90d7557a502f3984452e35639f970b0b1a7120136c3208aa225e799da7ff51`.

## Performed repairs

None.

## Assigned repair failures

None.

## Authorization audit inputs

| Task | Outcome | Record | Revision | Detail |
| --- | --- | --- | --- | --- |
| task_05 | granted | docs/specs/0159-archive-override-and-authoring-rules/_authorization.md | f6f46b6db0cfb4f7d5c88f839e36edc94a94fb4e | operative grant |

## Mechanical findings

None.

## Results

| # | Story / criterion / sweep and provenance | Actor and surface | Status | Evidence |
| - | --- | --- | --- | --- |
| 1 | Repository gate — Requirement 1; repository Verification | Daemon; backend | pass | Supervised `make verify-changed`: exit 0, diagnostics removed on success; current-head public binary built successfully. |
| 2 | Authorized failed-QA archive — Requirement 2; outside-evidence row | Maintainer; CLI and repository filesystem | pass | Built binary archived a failed-QA fixture sourced from the user checkout, stamped every required field, and preserved the QA Task and report byte-identically. |
| 3 | Override refusal boundaries and ordinary archive compatibility — Requirement 3 | Maintainer; CLI | pass | Built binary returned the required refusal causes, accepted a pending-QA override despite a stale pass report, and preserved ordinary archive behavior without override metadata. |
| 4 | Claimed ADR ordinals — Requirement 4 | Spec author; CLI | pass | Built strict checks emitted `SC-ORDINAL-CLAIMED` for both collision classes, accepted distinct and fulfilled claims, and blamed the later claimant. |
| 5 | Settlement and authoring guidance — Requirement 5 | Documentation author; docs and repository contracts | pass | Executable contracts, canonical-to-mirror comparisons, skill sync, and commit-derived authorization scope and chronology all passed. |
| 6 | This Spec's promise mapping — Requirement 6 | Spec author; CLI | pass | Current-head strict Spec check returned `No findings`; rows 2-5 independently exercised the mapped public behavior and contracts. |
| 7 | Pull Request controls — Pull Request row | Maintainer/reviewer; Pull Request | blocked (environment: no open Pull Request) | Prompt-provided absence plus equivalent evidence for approval, checks/status, review threads, Merge-Ready acceptance, and review-artifact ancestry. |

### 1

- Expected observable: the assembled tree passes the selected repository gate.
- Evidence: the prompt records Daemon-supervised `make verify-changed`, exit 0,
  with diagnostics removed on success.
- Independent confirmation: the audited commit is recorded in frontmatter and
  a public binary built from that exact tree exited 0 from `go build`.
- Persistence check: the remaining public-flow rows use that same hashed binary.
- The Agent did not rerun repository Verification.

### 2

- Outside source: clean user checkout
  `/private/tmp/claude-501/-Users-marcio-dev-roundfix/0f9d9d2d-9b0a-4336-8d50-a94fb7322388/scratchpad/wt-0159`
  on `feat/0159-archive-override-and-authoring-rules` at
  `f6f46b6db0cfb4f7d5c88f839e36edc94a94fb4e`; the isolated clone lived at
  `/private/tmp/roundfix-0159-qa-rerun.u3Pfhj/failed-override`.
- Entry point: `/private/tmp/roundfix-0159-qa-rerun archive
  0159-archive-override-and-authoring-rules --qa-override --approval "Spec
  0159 QA rerun 2026-09-24" --reason "exercise failed-QA override after
  corrective tasks"`.
- Observable: exit 0 and `archived
  0159-archive-override-and-authoring-rules with QA override ->
  docs/history/specs/0159-archive-override-and-authoring-rules`.
- Independent confirmation: reopening archived `_prd.md` showed
  `qa_override: true`, the supplied approval and reason,
  `qa_override_qa_outcome: fail`, and
  `qa_override_revision: f6f46b6db0cfb4f7d5c88f839e36edc94a94fb4e`;
  the active path no longer existed.
- Persistence and byte identity: `task_04.md` stayed SHA-256
  `91ad6a81a9768e74fc83b66d36772121f6341612b3981878f0447669a4c4f4e8`
  with `status: pending`, and the failed report stayed SHA-256
  `e0499a71ac37029170c25a218d68aaa862ea9755a4d761b85b48c5af329d1daf`
  with `verdict: fail`, before and after the move.

### 3

- Entry point: the current-head binary exercised ten isolated copies of the
  user-checkout fixture below
  `/private/tmp/roundfix-0159-qa-rerun.u3Pfhj/`.
- The following cases exited 2 with the named public cause and left the active
  Spec directory in place:
  - pending `task_05`: every non-QA Task must be completed;
  - completed QA Task plus newest `pass`: the Spec already qualifies for normal
    archive;
  - `--qa-override` without both provenance values: approval and reason are
    required;
  - override missing only approval: `--approval <source>` is required;
  - override missing only reason: `--reason <text>` is required;
  - approval alone or reason alone: both flags require `--qa-override`;
  - completed QA Task plus newest `fail`, without override: no passing QA
    verdict.
- Corrective boundary: a pending QA Task with a stale newest `pass` report
  archived with exit 0 under the override. A fresh read showed
  `qa_override_qa_outcome: pass`, `qa_override_revision:
  f6f46b6db0cfb4f7d5c88f839e36edc94a94fb4e`, and preserved `status:
  pending` in the moved QA Task plus `verdict: pass` in the moved report.
- Ordinary compatibility: a completed QA Task with a newest `pass` report
  archived without the flag, output omitted `with QA override`, and a fresh
  search of archived `_prd.md` found no `qa_override` field.

### 4

- Tree-held negative: a claim for `docs/adr/0150-collision.md` made built
  `spec check --strict` exit 1 with `SC-ORDINAL-CLAIMED`, naming the existing
  `docs/adr/0150-work-branches-express-purpose-and-run-branches-express-ownership.md`.
- Cross-Spec negative: active Specs claiming `docs/adr/0999-primary.md` and
  `docs/adr/0999-peer.md` made the built command exit 1 with
  `SC-ORDINAL-CLAIMED`, naming both Specs and both paths.
- Distinct claims `0999` and `0998` returned exit 0 with `No findings`.
- Fulfilled chronology: the first Spec's exact existing ADR 0150 claim returned
  exit 0 despite a later active claim. Checking the later Spec exited 1 and
  named the existing tree path; the fulfilled first Spec was not blamed.
- Independent confirmation: focused `go test -count=1 -v` ran
  `TestOrdinalClaimedOnTree`, `TestOrdinalClaimedByAnotherActiveSpec`,
  `TestDistinctOrdinalsAreAccepted`,
  `TestFulfilledOrdinalClaimIsNotBlamedForALaterClaim`; all passed.

### 5

- `go test -count=1 -v -run
  '^(TestSettlementGuidanceIsOneTable|TestTaskAuthoringGuidanceNamesDeclarations)$'
  ./skills` exited 0 and passed both named contracts.
- `diff -r` exited 0 for every canonical/distributed pair: `qa-gate`,
  `archive-spec`, `roundfix`, and `write-tasks`. `make skills-sync-check` also
  exited 0.
- `git -c core.fsmonitor=false diff-tree --no-commit-id --name-only -r`
  inspected Task commits `6d300322`, `03fd89b7`, `d73be08c`, `fbeba0d7`, and
  `61e9c167`. Governed paths in Tasks 01-03 and 05 are inside the exact
  `_authorization.md` list; Task 06 changed only its Task file plus the
  authorization record's explicitly ordinary ordinal implementation and test.
- Authorization commit `c00ee185` is an ancestor of the first implementation
  commit. Target revision `f6f46b6` is an ancestor of corrective Task 05.
  Ancestry checks also proved the Task order `6d300322` → `03fd89b7` →
  `d73be08c` → `fbeba0d7` → `61e9c167`.
- No Task commit folded in `_authorization.md`; no prerequisite-fix or
  consequent-fix declaration applies. Exact mirror equality independently
  confirms the sanctioned `make skills-sync` result recorded by Tasks 03 and
  05.

### 6

- `/private/tmp/roundfix-0159-qa-rerun spec check
  0159-archive-override-and-authoring-rules --strict` exited 0 with `No
  findings` and did not execute authored Verification.
- This checks the current Spec's declared promise-to-Task relationships on the
  audited tree. Rows 2-5 independently exercised the archive CLI, ordinal
  checker, guidance contracts, mirrors, and governed commit scope, so this row
  does not rest on reading the Spec files alone.

### 7

- Cause: the QA prompt states that no Pull Request is open. The Run Worktree
  branch is never pushed and has no Pull Request of its own.
- Approval: `_authorization.md` records the maintainer's approved 2026-09-24
  implementation scope; no post-change Pull Request approval was obtained.
- Checks and status: Daemon-supervised `make verify-changed` passed with exit 0
  for the candidate, and focused public-flow and contract checks in rows 2-6
  passed.
- Unresolved review threads: none exists because there is no Pull Request or
  thread set.
- Merge-Ready acceptance: none exists yet because there is no Review Source
  object for a Pull Request.
- Review-artifact ancestry: no Pull Request review artifact exists yet. The
  audited candidate head is
  `61e9c167f323cb802898f7c3b4fd6d0fa94ec634`; the target branch and user
  checkout remain at `f6f46b6db0cfb4f7d5c88f839e36edc94a94fb4e` until
  Daemon integration.
- Unblocking action: integrate the Run into the target branch and open the
  Pull Request during the later delivery stage.

## Findings

None.

## Blocked and skipped

- Row 7 is blocked by `no open Pull Request`. Every Pull Request control has
  equivalent observed or supervised evidence, so this environment block does
  not prevent pass.
- No row is finding-blocked, declared-blocked, or skipped.

## Coverage

- Coverage sources: 9/9 — Requirements 1-6, outside evidence, Pull
  Request, and repository Verification.
- Rows: 6 pass, 0 fail, 1 environment-blocked, 0 finding-blocked, 0
  declared-blocked, 0 skipped, 0 pending.
- No frontend sweep applies. No PRD Unreachable Acceptance declaration exists.
- Requirement 7 was enforced: no row was accepted solely because a file was
  read.

## Final verdict

Pass: every runnable row passed, the one environment-blocked Pull Request row
records its cause and complete equivalent evidence, and there are no findings,
declared blocks, skips, or pending rows.

## Mechanical skips

| Detector | Missing artifact |
| --- | --- |
| consequent-fix commit order | consequent-fix declaration |
| QA evidence paths | docs/specs/0159-archive-override-and-authoring-rules/qa/evidence |
