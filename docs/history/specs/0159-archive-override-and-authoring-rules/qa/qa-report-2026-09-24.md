---
spec: 0159-archive-override-and-authoring-rules
date: 2026-09-24
build: d73be08cee0d63f3b7e230ceb81e5aa4249e76e2
auditing_binary: "roundfix 0.16.0, local QA build sha256:c4db8e585036f81dfc04f550d8d6b5be5f947998c24695f2c269e3eaf591baa9"
auditor_staleness: "current: built from audited HEAD d73be08cee0d63f3b7e230ceb81e5aa4249e76e2"
status: closed
verdict: pass
rows_blocked_environment: 1
rows_blocked_finding: 0
rows_blocked_declared: 0
surfaces: [backend, cli, docs]
---

# QA Report

## Scope and environment

- Scope: full authored gate for Task 04. Coverage sources are Requirements
  1-6, the outside-evidence row, the Pull Request row, and repository
  Verification. Requirement 7 constrains evidence quality and is not a source
  because it starts with `MUST NOT`.
- Actors and surfaces: maintainers using `roundfix archive`, Spec authors using
  `roundfix spec check`, and documentation authors consuming workflow skills.
- Audited tree: Run Worktree HEAD
  `d73be08cee0d63f3b7e230ceb81e5aa4249e76e2` on the Run branch.
- Public artifact: `/private/tmp/roundfix-0159-qa`, built from the audited tree
  with `GOCACHE=/private/tmp/roundfix-0159-qa-gocache go build -o
  /private/tmp/roundfix-0159-qa ./cmd/roundfix`; `--version` reported
  `roundfix 0.16.0`.
- Outside source: the user checkout on
  `feat/0159-archive-override-and-authoring-rules`, HEAD
  `c00ee1852625c391fe891d349002475e5f38e4fb`. Destructive journeys ran only in
  isolated Git clones below `/private/tmp/roundfix-0159-qa-flow.IAApcJ`.
- Pull Request fact: none open; Pull Request journeys use the
  environment-blocked equivalent-evidence path.
- The PRD declares no Unreachable Acceptance and no frontend surface.

## Static gate

- `/private/tmp/roundfix-0159-qa spec check
  0159-archive-override-and-authoring-rules --strict` exited 0 with `No
  findings`; it did not execute authored Verification commands.
- The Daemon already ran repository Verification `make verify-changed`
  outside the Agent sandbox. It passed with exit 0 and removed diagnostics on
  success. This is recorded as a static fact and was not rerun.
- Focused archive, ordinal, settlement, authoring-guidance, and skill-sync
  checks all exited 0.

## Performed repairs

None.

## Assigned repair failures

None.

## Authorization audit inputs

| Task | Outcome | Record | Revision | Detail |
| --- | --- | --- | --- | --- |
| task_01 | granted | docs/specs/0159-archive-override-and-authoring-rules/_authorization.md | c00ee1852625c391fe891d349002475e5f38e4fb | operative grant |
| task_02 | granted | docs/specs/0159-archive-override-and-authoring-rules/_authorization.md | c00ee1852625c391fe891d349002475e5f38e4fb | operative grant |
| task_03 | granted | docs/specs/0159-archive-override-and-authoring-rules/_authorization.md | c00ee1852625c391fe891d349002475e5f38e4fb | operative grant |

## Mechanical findings

None.

## Results

| # | Story / criterion / sweep and provenance | Actor and surface | Status | Evidence |
| - | --- | --- | --- | --- |
| 1 | Repository gate — Requirement 1; repository Verification | Daemon; backend | pass | Supervised `make verify-changed`: exit 0, diagnostics removed on success. |
| 2 | Authorized failed-QA archive — Requirement 2; outside-evidence row | Maintainer; CLI and repository filesystem | pass | Built binary archived an isolated clone sourced from the user checkout, stamped every override field, moved the Spec, and preserved QA bytes. |
| 3 | Override refusals and ordinary archive compatibility — Requirement 3 | Maintainer; CLI | pass | Built binary refused every invalid override shape and failed QA without an override; ordinary passing QA archived without override metadata. Focused negative-case tests passed. |
| 4 | Claimed ADR ordinals — Requirement 4 | Spec author; CLI | pass | Built `spec check --strict` emitted `SC-ORDINAL-CLAIMED` for both collisions and returned no findings for exact-path fulfillment and distinct claims. |
| 5 | Settlement and authoring guidance — Requirement 5 | Documentation author; docs and repository contracts | pass | Guidance contract tests, canonical-to-mirror comparisons, skill-sync check, and commit-scope authorization audit passed. |
| 6 | This Spec's promise mapping — Requirement 6 | Spec author; CLI | pass | Built strict Spec check returned no findings; executable journeys independently observed the promised CLI fields and refusal codes. |
| 7 | Pull Request controls — Pull Request row | Maintainer/reviewer; Pull Request | blocked (environment: no open Pull Request) | Prompt-provided absence plus equivalent evidence for approval, checks/status, review threads, Merge-Ready acceptance, and review-artifact ancestry. |

### 1 — Repository gate

- Expected observable: the selected repository gate accepts the assembled
  tree.
- Evidence: the Daemon-provided fact records `make verify-changed`, exit 0,
  with diagnostics removed on success.
- Independent confirmation and persistence: the audited commit is recorded in
  this report and later public-flow checks used a binary built from that HEAD.
- The Agent did not rerun repository Verification.

### 2 — Authorized failed-QA archive and outside evidence

- Entry point: `/private/tmp/roundfix-0159-qa archive
  0159-archive-override-and-authoring-rules --qa-override --approval "Spec
  0159 QA gate 2026-09-24" --reason "exercise failed-QA override in isolated
  clone"`.
- External source: an isolated Git clone based on the user checkout at
  `c00ee1852625c391fe891d349002475e5f38e4fb`, not a repository invented by the
  Spec. The QA overlay supplied the assembled Spec and a failed newest report;
  the committed fixture HEAD was
  `ed00a1ace85ed333eade23b648a65b8e32859eeb`.
- Observable: exit 0 and `archived ... with QA override ->
  docs/history/specs/0159-archive-override-and-authoring-rules`.
- Independent confirmation: reopening archived `_prd.md` showed
  `qa_override: true`, the supplied approval and reason,
  `qa_override_qa_outcome: fail`, and
  `qa_override_revision: ed00a1ace85ed333eade23b648a65b8e32859eeb`; the
  active directory no longer existed.
- Persistence and byte identity: SHA-256 for `task_04.md` stayed
  `31884dd101000f5967aa91a53ceb244e08ed82993667cd4b69a1ab3742f63bee` and
  SHA-256 for `qa-report-2026-09-24.md` stayed
  `627962d90fe902ffbe905a8447618d9c88f2a8f8726d7d0a57475558b7808aee`
  before and after the move.

### 3 — Refusals and ordinary archive compatibility

- Built-binary negative cases exited 2 and named the expected cause:
  - pending `task_01`: every non-QA Task must be completed;
  - already-qualifying report: an override is not allowed;
  - override without approval and reason: both are required;
  - approval without override: provenance flags require `--qa-override`;
  - failed QA without override after completing the QA Task: newest verdict
    `fail`, expected `pass`.
- Ordinary positive case: after completing the fixture QA Task, a passing
  report archived with exit 0 and output without `with QA override`; reopening
  `_prd.md` showed normal metadata and no override fields.
- Independent focused confirmation:
  `go test -count=1 -v -run
  '^(TestArchiveQAOverrideStampsProvenance|TestArchiveQAOverrideStillRequiresNonQATasks|TestArchiveQAOverrideRefusedWhenQAQualifies|TestArchiveCommandQAOverrideRequiresApprovalAndReason|TestRunArchiveQAOverrideReportsOverride)$'
  ./internal/spec ./internal/cli` passed every named case, including missing
  approval only, missing reason only, and stray reason only.

### 4 — Claimed ADR ordinals

- Tree-held negative: a fixture claim for
  `docs/adr/0154-qa-collision.md` made built `spec check --strict` exit 1 with
  `SC-ORDINAL-CLAIMED`, naming the existing ADR 0154 path.
- Cross-Spec negative: active Specs claimed `docs/adr/0999-primary.md` and
  `docs/adr/0999-peer.md`; built `spec check --strict` exited 1 with
  `SC-ORDINAL-CLAIMED`, naming both Specs and paths.
- Independent confirmations: changing the first claim to the existing exact
  ADR 0154 path produced exit 0 and no findings; changing the peer claim to
  ordinal 0998 produced exit 0 and no findings.
- `go test -count=1 -v -run
  '^(TestOrdinalClaimedOnTree|TestOrdinalClaimedByAnotherActiveSpec|TestDistinctOrdinalsAreAccepted)$'
  ./internal/speccheck` passed, including exact-path and distinct-claim
  subtests.

### 5 — Settlement, authoring guidance, and tooling scope

- `go test -count=1 -v -run
  '^(TestSettlementGuidanceIsOneTable|TestTaskAuthoringGuidanceNamesDeclarations)$'
  ./skills` passed.
- `cmp -s` passed for each canonical/distributed pair: `qa-gate`,
  `archive-spec`, `roundfix`, and `write-tasks`. `make skills-sync-check` also
  exited 0.
- `git diff-tree --no-commit-id --name-only -r` inspected Task commits
  `6d300322`, `03fd89b7`, and `d73be08c`. Every governed path was inside the
  exact `_authorization.md` list; ordinary paths matched its non-governed
  section.
- Authorization revision `c00ee185` is an ancestor of the first Task commit.
  Each Task commit is separate and chronological. No prerequisite-fix or
  consequent-fix declaration exists, no such change was folded into a Task
  commit, and no out-of-scope path was observed.

### 6 — Promise mapping

- `/private/tmp/roundfix-0159-qa spec check
  0159-archive-override-and-authoring-rules --strict` exited 0 with `No
  findings` and did not execute authored Verification.
- This actively checks the Spec's declared promise-to-Task relationships; the
  public CLI journeys separately exercised the behavior those promises name,
  so this row is not accepted from file reading alone.

### 7 — Pull Request controls

- Cause: the QA prompt states that no Pull Request is open. The Run Worktree
  branch is never pushed and has no Pull Request of its own.
- Approval: `_authorization.md` records the maintainer's approved 2026-09-24
  scope at `c00ee185`; no post-change Pull Request approval exists yet.
- Checks and status: Daemon-supervised `make verify-changed` passed with exit
  0 for the candidate.
- Unresolved review threads: none exists because there is no Pull Request or
  thread set.
- Merge-Ready acceptance: none exists yet because there is no Review Source
  object.
- Review-artifact ancestry: no review artifact exists yet. The claimed
  candidate head is `d73be08cee0d63f3b7e230ceb81e5aa4249e76e2`; the target
  branch remains at `c00ee1852625c391fe891d349002475e5f38e4fb` until Daemon
  integration.
- Unblocking action: integrate the Run into the target branch and open the
  Pull Request during the later delivery stage.

## Findings

None.

## Blocked and skipped

- Row 7 is blocked by `no open Pull Request`. Every Pull Request control has
  equivalent observed or supervised evidence, so this does not prevent pass.

## Coverage

- Coverage sources: 9/9 covered — Requirements 1-6, outside evidence, Pull
  Request, and repository Verification.
- Rows: 6 pass, 0 fail, 1 environment-blocked, 0 finding-blocked, 0
  declared-blocked, 0 skipped, 0 pending.
- No frontend sweep applies. No PRD Unreachable Acceptance declaration exists.
- Requirement 7 was enforced: no row was accepted solely because a file was
  read.

## Final verdict

Pass: every runnable row passed, the one environment-blocked Pull Request row
records its cause and complete equivalent evidence, and there are no findings,
declared blocks, skips, or pending rows.

## Mechanical skips

| Detector | Missing artifact |
| --- | --- |
| consequent-fix commit order | consequent-fix declaration |
| QA Report structure | QA Report |
| QA evidence paths | QA Report |
