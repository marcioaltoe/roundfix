---
spec: 0157-reconciliation-and-one-repository-identity
date: 2026-09-24
build: 972bbe8f42ebc60d78f22e2cb975448b0a090260
auditing_binary: "0.16.0 (e38c8561, built 2026-09-24 08:07:17 -0300)"
auditor_staleness: "current: commit ancestry: build commit does not predate audited tree"
status: closed
verdict: pass
rows_blocked_environment: 1
rows_blocked_finding: 0
rows_blocked_declared: 0
surfaces: [backend, cli]
---

# QA Report

## Scope and environment

Full terminal gate for commit `972bbe8f42ebc60d78f22e2cb975448b0a090260`
on the Run Worktree branch. The intended actor is a Roundfix operator using the
CLI against local Git repositories and linked worktrees. Scratch repositories,
homes and artifact directories are isolated below `/private/tmp`; no network,
credential, commit, push or Pull Request mutation is part of the run.
The durable command transcript is
`qa/evidence/2026-09-24-spec-0157/journeys.md`; its fixture source is beside it
under `fixture/main.go`.

The Daemon's mechanical stage used the auditing binary recorded in frontmatter.
The `roundfix` on `PATH` is 0.15.0 and is stale for this tree, so functional CLI
journeys use a binary built from the audited source. The current-source command
`go run ./cmd/roundfix spec check 0157-reconciliation-and-one-repository-identity --strict`
passed before the matrix was opened.

## Static gate

- Repository Verification: `make verify` passed with exit 0 in the Daemon's
  outside-sandbox run; diagnostics were removed on success. The gate did not
  rerun this command.
- Strict Spec check from the audited source: pass; no findings. The checker
  reported only its named missing-artifact skips for a rollup, backlog,
  Vocabulary Contract and references index.
- Focused regression checks: `go test -count=1` passed for the unproven
  candidate and archived-copy cases in `./internal/worktree`, the shared
  identity case in `./internal/config`, and linked/earlier Run discovery in
  `./internal/store`.

## Performed repairs

None.

## Assigned repair failures

None.

## Authorization audit inputs

None.

## Mechanical findings

None.

## Results

| # | Story / criterion / sweep | Actor and surface | Status | Provenance |
| - | --- | --- | --- | --- |
| R1 | Repository Verification passes for the assembled tree | Daemon; repository gate | pass | Requirement 1; repository Verification |
| R2 | An unproven cleanup candidate is refused without removing its Run Worktree or Run Branch | Operator; `roundfix reconcile --apply` | pass | Requirement 2 |
| R3 | A same-named active and archived QA Report is one report for supersession | Operator; reconciliation CLI | pass | Requirement 3 |
| R4 | Main and linked worktrees share the unchanged main identity, and a linked-worktree Run is listed from main | Operator; Git plus `roundfix runs list` | pass | Requirement 4; outside-evidence row |
| R5 | An earlier worktree-derived Artifact Directory remains listed and readable | Operator; `roundfix runs list` and artifact read | pass | Requirement 5 |
| R6 | The Spec's artifacts satisfy the promise rule through the current checker | Spec author; `roundfix spec check --strict` | pass | Requirement 6 |
| R7 | Pull Request controls are accounted for without an Open Pull Request | Reviewer; Pull Request journey | blocked (environment: no open Pull Request) | Pull Request row |

## Row evidence

### R1

```yaml
inputs:
  - kind: live_service
    ref: Daemon-owned repository Verification result for build 972bbe8f42ebc60d78f22e2cb975448b0a090260
```

- Steps: record the Daemon's exact command, exit status and diagnostics fact.
- Expected observable: `make verify` exits 0 with no retained success
  diagnostics.
- Independent confirmation: the prompt binds the fact to this QA run and the
  report build.
- Persistence: not applicable; this is a static gate fact for one build.
- Evidence: the QA prompt records `command: make verify`, `verdict: pass (exit
  0)` and `diagnostics: removed on success`. This is supervised evidence from
  the Daemon that owns repository Verification, recorded as a static fact and
  not re-derived in the Agent sandbox.

### R2

```yaml
inputs:
  - kind: repository_path
    ref: cmd/roundfix/**
  - kind: repository_path
    ref: internal/cli/reconcile.go
  - kind: repository_path
    ref: internal/worktree/worktree.go
```

- Steps: build the current CLI, create an isolated repository containing an
  absent-target cleanup candidate, run `roundfix reconcile --apply`, and query
  Git again for the candidate's worktree and branch.
- Expected observable: the CLI refuses because worktree revalidation returned
  no evidence, exits nonzero instead of panicking, and leaves both Git surfaces.
- Independent confirmation: fresh `git worktree list --porcelain` and
  `git show-ref` reads after the CLI exits.
- Persistence: repeat the public Git reads from a fresh process.
- Evidence: with `HOME=/private/tmp/roundfix-0157-r2.2U297C/home`,
  `/private/tmp/roundfix-0157-qa reconcile --apply --format json` exited 1
  without a panic. Its `runBranchCandidates[0]` recorded `action: preserve` and
  `refusalReason: apply Run Branch candidate ...: worktree revalidation returned
  no evidence`. A fresh `git worktree list --porcelain` still listed the Run
  Worktree at head `b4798c1`; `git show-ref --verify
  refs/heads/roundfix/run-run_20260924T131549Z_aad3266ff0df9bf6` exited 0 at
  `b4798c14c90abd6bdcd3867fc926c4879c35a365`; and the main checkout remained
  clean.

### R3

```yaml
inputs:
  - kind: repository_path
    ref: internal/cli/reconcile.go
  - kind: repository_path
    ref: internal/worktree/worktree.go
```

- Steps: create an isolated reconciliation fixture whose Run-side active QA
  Report and target-side archived QA Report have the same filename, then inspect
  through the reconciliation surface.
- Expected observable: the archived copy is accepted as the same report and its
  archived path is retained as evidence.
- Independent confirmation: inspect the emitted reconciliation evidence and
  query both report paths from Git.
- Persistence: rerun the read-only inspection from a fresh process.
- Evidence: with `HOME=/private/tmp/roundfix-0157-r3.FHe5hb/home`,
  `/private/tmp/roundfix-0157-qa reconcile
  run_20260924T131550Z_a11a0946c2ac10f0 --format json` exited 0 twice.
  Both fresh reads classified the Run `superseded`, reported
  `supersedingReport: docs/history/specs/qa-0157-fixture/qa/qa-report-2026-07-28.md`,
  and returned zero operational failures. `git show` independently read the
  Run-side active `qa-report-2026-07-28.md` with verdict `fail` and the
  target-side archived file of the same name with verdict `pass`.

### R4

```yaml
inputs:
  - kind: repository_path
    ref: cmd/roundfix/**
  - kind: repository_path
    ref: internal/config/config.go
  - kind: repository_path
    ref: internal/store/store.go
  - kind: external_repository
    ref: scratch Git repository observed independently with git worktree list and git rev-parse --git-common-dir
```

- Steps: create a real main checkout and linked worktree, let the current
  Roundfix binary resolve their default Artifact Directories, record a Run from
  the linked worktree, and run `roundfix runs list` from the main checkout.
- Expected observable: both worktrees resolve the main checkout's pre-change
  identity, and the main CLI listing includes the linked-worktree Run.
- Independent confirmation: native Git commands identify both paths as one
  repository through the shared common directory; this external Git evidence
  was not authored by the Spec.
- Persistence: reopen the Run store through a new CLI process from main.
- Evidence: in `/private/tmp/roundfix-0157-r45.EmtCfm`, a fresh backend read
  resolved the main and linked Artifact Directories to the same path,
  `.../artifacts/db203fe68217fa91`, equal to the independently computed
  pre-change main-root hash. From the main checkout,
  `HOME=... /private/tmp/roundfix-0157-qa runs list --state all --limit 0`
  exited 0 and listed both `run_20260924T131550Z_cb47c44565fbe4bb`
  (`qa-0157-current-identity`) and the earlier-identity Run. As the outside
  evidence, native Git reported both worktrees at the same commit in `git
  worktree list --porcelain`; from the linked checkout `git rev-parse
  --git-common-dir` returned `/private/tmp/roundfix-0157-r45.EmtCfm/main/.git`,
  independently proving that Git itself treats the two paths as one repository.

### R5

```yaml
inputs:
  - kind: repository_path
    ref: cmd/roundfix/**
  - kind: repository_path
    ref: internal/store/store.go
```

- Steps: seed a Run and marker in the earlier linked-path-derived Artifact
  Directory, list Runs from the main checkout, and read the marker through the
  listed Artifact Directory.
- Expected observable: the legacy Run remains listed and its artifact bytes are
  unchanged.
- Independent confirmation: compare the CLI-listed directory with the seeded
  legacy path and read the marker from a fresh process.
- Persistence: close and reopen the Run store before listing.
- Evidence: the same fresh main-checkout CLI listing included legacy Run
  `run_20260924T131550Z_fdb404ffe4395049`. After the setup writer closed, a new
  backend reader listed that Run from the main checkout while preserving its
  earlier linked-root identity and Artifact Directory
  `.../artifacts/9e452eeeea9249b6`; reading `earlier-run.txt` through that listed
  directory returned exactly `earlier worktree artifact\n`.

### R6

```yaml
inputs:
  - kind: repository_path
    ref: docs/specs/0157-reconciliation-and-one-repository-identity/**
  - kind: repository_path
    ref: internal/speccheck/**
```

- Steps: run the strict Spec checker from the audited source tree.
- Expected observable: no findings, including no unmapped Success Metric or API
  Contract promise.
- Independent confirmation: inspect the checker's exit status and named output,
  not merely the Spec files.
- Persistence: rerun the checker from a fresh process after the report closes.
- Evidence: `GOCACHE=/private/tmp/roundfix-0157-qa-gocache go run
  ./cmd/roundfix spec check 0157-reconciliation-and-one-repository-identity
  --strict` exited 0 three times from the audited source and printed `No findings`.
  This executes the promise-rule detector; it is not acceptance based only on
  reading the Spec files. The checker explicitly did not run authored Task
  Verification commands and reported only four named missing-artifact skips
  unrelated to the promise rule.

### R7

```yaml
inputs:
  - kind: live_service
    ref: Prompt-supplied Pull Request fact and Daemon-supervised repository Verification
```

- Steps: account for approval, checks and status, unresolved review threads,
  Merge-Ready acceptance and review-artifact ancestry for the expected head.
- Expected observable: no Open Pull Request is confirmed as the environmental
  cause, and every Pull Request control has explicit equivalent observed or
  supervised evidence.
- Independent confirmation: bind all evidence to the expected head
  `972bbe8f42ebc60d78f22e2cb975448b0a090260`.
- Persistence: not applicable while no Pull Request exists.
- Evidence: the QA assignment states `Pull Request: none open`; the Run
  Worktree branch is never pushed and has no Pull Request. Equivalent controls:
  approval — no change acceptance has been obtained yet, though the maintainer's
  implementation and delivery authority is recorded in `_authorization.md`;
  checks and status — the Daemon ran `make verify` for this build and recorded
  exit 0; unresolved review threads — none can stand because no Pull Request or
  thread collection exists; Merge-Ready acceptance — none exists yet because
  the configured pre-PR review stage follows this gate; review-artifact ancestry
  — no review artifact exists to misbind, while every gate observation is bound
  to expected head `972bbe8f42ebc60d78f22e2cb975448b0a090260` and its Task
  commits `380db2f9`, `e6f7907b` and `972bbe8f`.

## Findings

None.

## Blocked and skipped

- R7 is `blocked (environment: no open Pull Request)`. Proof: the prompt
  confirms no Open Pull Request and identifies the target branch separately
  from the never-pushed Run Worktree branch. Equivalent evidence for all five
  controls is recorded in R7. Unblocking action: after the Daemon integrates
  this Run, complete configured pre-PR review and open the Pull Request from
  `fix/0157-reconcile-and-repository-identity`; those later workflow steps do
  not belong to this gate.

## Coverage

Coverage is closed: 7/7 rows executed, 6 passed and 1 is environment-blocked
with equivalent evidence. Every declared Requirement and non-waivable source
appears in the matrix. Requirement 7 constrains evidence quality and is not a
coverage source. The PRD has no `Unreachable Acceptance` declarations and
declares no frontend surface. There are no failed, finding-blocked,
declared-blocked, skipped or pending rows.

## Final verdict

Pass. All six runnable rows passed; one Pull Request row is blocked only by the
proved no-open-Pull-Request environment and carries equivalent evidence for all
five controls. Counts: 6 pass, 1 environment-blocked, 0 fail, 0
finding-blocked, 0 declared-blocked, 0 skipped; findings by impact tier:
Blocks-Completion 0, Data-Loss 0, Trust-Damage 0, Friction 0, Cosmetic 0.

## Mechanical skips

| Detector | Missing artifact |
| --- | --- |
| authorization bounded paths | tooling authorization |
| consequent-fix commit order | consequent-fix declaration |
| QA Report structure | QA Report |
| QA evidence paths | QA Report |
