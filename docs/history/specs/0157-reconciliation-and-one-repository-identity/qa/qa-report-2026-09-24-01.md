---
spec: 0157-reconciliation-and-one-repository-identity
date: 2026-09-24
build: 934c775ff03f720f0e170d95c70afffcf798992a
auditing_binary: "0.16.0 (e38c8561, built 2026-09-24 08:07:17 -0300)"
auditor_staleness: "current: commit ancestry: build commit does not predate audited tree"
status: closed
verdict: pass
rows_blocked_environment: 1
rows_blocked_finding: 0
rows_blocked_declared: 0
surfaces: [backend, cli]
---

# QA Report

## Scope and environment

Full terminal rerun for commit `934c775ff03f720f0e170d95c70afffcf798992a`
on the Run Worktree branch. The actor is a Roundfix operator using the CLI with
local Git repositories and linked worktrees. Scratch repositories, homes and
artifact directories are isolated below `/private/tmp`; no network, credential,
commit to the audited repository, push or Pull Request mutation is in scope.
The durable command transcript is
`qa/evidence/2026-09-24-spec-0157-rerun-01/journeys.md`; fixture setup uses the
existing source under `qa/evidence/2026-09-24-spec-0157/fixture/main.go`.

The strict authoring precondition passed before this matrix was opened:
`roundfix spec check 0157-reconciliation-and-one-repository-identity --strict`
exited 0 with `No findings`. The command did not execute authored Verification.

## Static gate

- Repository Verification: the QA assignment records the Daemon-owned
  `make verify` run as passed with exit 0 and diagnostics removed on success.
  This gate will record that fact and will not rerun the command.
- Strict Spec check: passed with exit 0 and no findings. Its named skips were a
  missing rollup, backlog, Vocabulary Contract and references index.
- Focused regression selection: `go test -count=1` passed the named
  reconciliation, report-content identity, repository-identity,
  checkout-local Run-root and unreadable-worktree cases in
  `./internal/worktree`, `./internal/config` and `./internal/store`.

## Performed repairs

None.

## Assigned repair failures

None.

## Authorization audit inputs

None.

## Mechanical findings

None.

## Results

| # | Story / criterion / sweep | Actor and surface | Status | Provenance |
| - | --- | --- | --- | --- |
| R1 | Repository Verification passes for the assembled tree | Daemon; repository gate | pass | Requirement 1; repository Verification |
| R2 | An unproven cleanup candidate is refused without removing its Run Worktree or Run Branch | Operator; `roundfix reconcile --apply` | pass | Requirement 2 |
| R3 | Same-name QA Reports are one report only when the active and archived Git blobs are identical | Operator; reconciliation CLI | pass | Requirement 3 |
| R4 | Main and linked worktrees share the unchanged main identity, preserve checkout-local Run roots and expose linked-worktree Runs from main | Operator; Git plus `roundfix runs list` | pass | Requirement 4; outside-evidence row |
| R5 | An earlier worktree-derived Artifact Directory remains listed and readable | Operator; `roundfix runs list` and artifact read | pass | Requirement 5 |
| R6 | The Spec's artifacts satisfy the promise rule through the current checker | Spec author; `roundfix spec check --strict` | pass | Requirement 6 |
| R7 | Pull Request controls are accounted for without an Open Pull Request | Reviewer; Pull Request journey | blocked (environment: no open Pull Request) | Pull Request row |

## Row evidence

### R1

```yaml
inputs:
  - kind: live_service
    ref: Daemon-owned repository Verification result for build 934c775ff03f720f0e170d95c70afffcf798992a
```

- Steps: record the Daemon's command, exit status and diagnostics fact.
- Expected observable: `make verify` exits 0 with no retained success
  diagnostics.
- Independent confirmation: bind the prompt-supplied fact to this report and
  build.
- Persistence: not applicable; this is a static gate fact for one build.
- Evidence: the QA assignment binds `command: make verify`, `verdict: pass
  (exit 0)` and `diagnostics: removed on success` to this QA run at build
  `934c775ff03f720f0e170d95c70afffcf798992a`. This is supervised evidence
  from the Daemon that owns repository Verification, recorded as a static fact
  and not re-derived in the Agent sandbox.

### R2

```yaml
inputs:
  - kind: repository_path
    ref: cmd/roundfix/**
  - kind: repository_path
    ref: internal/cli/reconcile.go
  - kind: repository_path
    ref: internal/worktree/worktree.go
  - kind: repository_path
    ref: docs/specs/0157-reconciliation-and-one-repository-identity/qa/evidence/2026-09-24-spec-0157/fixture/main.go
```

- Steps: build the current CLI, create an isolated absent-target cleanup
  candidate, run `roundfix reconcile --apply`, then query Git again for its
  worktree and branch.
- Expected observable: the CLI refuses because revalidation returned no
  evidence, exits nonzero without panicking, and removes nothing.
- Independent confirmation: fresh `git worktree list --porcelain` and
  `git show-ref` reads after the CLI exits.
- Persistence: repeat the public Git reads from fresh processes.
- Evidence: the current-tree binary `roundfix 0.16.0
  (934c775f-dirty, built 2026-09-24 16:33:32 -0300)` ran with isolated HOME
  `/private/tmp/roundfix-0157-rerun-r2.rO8dzO/home`.
  `reconcile --apply --format json` exited 1 without a panic and recorded the
  Run Branch candidate as `action: preserve` with refusal `worktree
  revalidation returned no evidence`. A fresh `git worktree list --porcelain`
  still listed the Run Worktree at
  `4a2438dbec58fe1d99f0e4ba0d0ed66de03c9d5a`; a separate `git show-ref
  --verify refs/heads/roundfix/run-run_20260924T193358Z_68ec49712d0a41d5`
  resolved the branch to the same head; the main checkout remained clean.

### R3

```yaml
inputs:
  - kind: repository_path
    ref: cmd/roundfix/**
  - kind: repository_path
    ref: internal/cli/reconcile.go
  - kind: repository_path
    ref: internal/worktree/worktree.go
  - kind: repository_path
    ref: docs/specs/0157-reconciliation-and-one-repository-identity/qa/evidence/2026-09-24-spec-0157/fixture/main.go
```

- Steps: inspect an isolated candidate with same-name, different-content
  active and archived QA Reports; then replace the archived report with the
  active report's exact bytes and inspect again.
- Expected observable: different blobs do not prove supersession; identical
  blobs do, with the archived path retained as evidence.
- Independent confirmation: query both report blob IDs through Git before
  each CLI inspection.
- Persistence: rerun each read-only classification from a fresh CLI process.
- Evidence: fixture `/private/tmp/roundfix-0157-rerun-r3.ocjo9p` first
  produced active blob `0374cb8c68d83d9c7155d9af8f73f386b561c8c8`
  (`verdict: fail`) and archived blob
  `af54a1011967d076aa0e8c8a972509fc92348f44` (`verdict: pass`) at the
  same report name. Two fresh `reconcile
  run_20260924T193437Z_5a32d54e2f2b784a --format json` processes each
  classified the Run `unintegrated`, returned no `supersedingReport`, and
  preserved it. After the archived path was committed with the active report's
  exact bytes, both `git rev-parse <tree>:<report>` reads returned
  `0374cb8c68d83d9c7155d9af8f73f386b561c8c8`. Two more fresh CLI
  processes each classified the Run `superseded` and named
  `docs/history/specs/qa-0157-fixture/qa/qa-report-2026-07-28.md` as the
  superseding report.

### R4

```yaml
inputs:
  - kind: repository_path
    ref: cmd/roundfix/**
  - kind: repository_path
    ref: internal/config/config.go
  - kind: repository_path
    ref: internal/store/store.go
  - kind: repository_path
    ref: docs/specs/0157-reconciliation-and-one-repository-identity/qa/evidence/2026-09-24-spec-0157/fixture/main.go
  - kind: external_repository
    ref: scratch Git repository observed independently with git worktree list and git rev-parse --git-common-dir
```

- Steps: create a real main checkout and linked worktree, resolve their
  Artifact Directories, record a linked-worktree Run, and list Runs from main.
- Expected observable: both worktrees use the pre-change main identity, the Run
  keeps the linked checkout as `git_root`, and the main CLI lists it.
- Independent confirmation: native Git identifies both paths as one repository
  through the shared common directory; that repository is not a Spec artifact.
- Persistence: reopen the Run store and rerun the CLI from main.
- Evidence: fixture `/private/tmp/roundfix-0157-rerun-r45.b2AJaJ`
  resolved main, linked and independently computed pre-change main Artifact
  Directories to
  `/private/tmp/roundfix-0157-rerun-r45.b2AJaJ/home/.roundfix/artifacts/0a9c7690aaba1cef`.
  After the writer closed, a fresh reader listed current Run
  `run_20260924T193547Z_00f22f8b8bdf2abe` with `gitRoot` still equal to the
  linked checkout and its Artifact Directory equal to the shared main
  identity. Two fresh `runs list --state all --limit 0` CLI processes from the
  main checkout listed both current and legacy Runs; two more from the linked
  checkout did the same after an unreadable `.git/worktrees/unreadable/gitdir`
  entry was introduced. As outside evidence, native Git listed main and linked
  at the same commit `2c6fcc76f5e18385cc1fe0db885728d3ed2d5456`, and
  `git rev-parse --git-common-dir` from linked returned
  `/private/tmp/roundfix-0157-rerun-r45.b2AJaJ/main/.git`. Those Git
  observations come from a scratch repository the Spec did not author.

### R5

```yaml
inputs:
  - kind: repository_path
    ref: cmd/roundfix/**
  - kind: repository_path
    ref: internal/config/config.go
  - kind: repository_path
    ref: internal/store/store.go
  - kind: repository_path
    ref: docs/specs/0157-reconciliation-and-one-repository-identity/qa/evidence/2026-09-24-spec-0157/fixture/main.go
```

- Steps: seed a Run and marker in an earlier linked-path-derived Artifact
  Directory, list from main, then read the marker through the listed directory.
- Expected observable: the legacy Run remains listed and the marker bytes are
  unchanged.
- Independent confirmation: compare the listed path with the seeded legacy
  path and read from a fresh process.
- Persistence: close and reopen the Run store before listing.
- Evidence: the same fresh reader listed legacy Run
  `run_20260924T193547Z_618cd541aa6ee65b` from the main checkout while
  preserving its earlier linked-root Artifact Directory
  `/private/tmp/roundfix-0157-rerun-r45.b2AJaJ/home/.roundfix/artifacts/bcbb7e7c496ac383`.
  A separate process read `earlier-run.txt` through that listed path and
  returned exactly `earlier worktree artifact\n`; repeated CLI listings from
  both worktrees continued to include the legacy Run.

### R6

```yaml
inputs:
  - kind: repository_path
    ref: docs/specs/0157-reconciliation-and-one-repository-identity/**
  - kind: repository_path
    ref: internal/speccheck/**
```

- Steps: run the strict Spec checker from the audited tree.
- Expected observable: no findings, including no unmapped declared promise.
- Independent confirmation: use the checker's exit status and output rather
  than accepting a file read.
- Persistence: rerun after this report closes.
- Evidence: the current-tree binary `/private/tmp/roundfix-0157-rerun-qa`
  identifies itself as `roundfix 0.16.0 (934c775f-dirty, built 2026-09-24
  16:33:32 -0300)`. Its `spec check
  0157-reconciliation-and-one-repository-identity --strict` command exited 0
  and printed `No findings`, while explicitly reporting that authored
  Verification commands were not executed. Its only named skips were the
  absent rollup, backlog, Vocabulary Contract and references index, none of
  which is the promise-rule detector. The same current-tree command exited 0
  with `No findings` again after the report closed.

### R7

```yaml
inputs:
  - kind: live_service
    ref: Prompt-supplied Pull Request fact and Daemon-supervised repository Verification
```

- Steps: account for approval, checks and status, unresolved review threads,
  Merge-Ready acceptance and review-artifact ancestry for the expected head.
- Expected observable: the no-open-Pull-Request cause is explicit and every
  control has equivalent observed or supervised evidence.
- Independent confirmation: bind each control to expected head
  `934c775ff03f720f0e170d95c70afffcf798992a`.
- Persistence: not applicable while no Pull Request exists.
- Evidence: the QA assignment states `Pull Request: none open` and identifies
  `fix/0157-reconcile-and-repository-identity` as the target branch, distinct
  from the never-pushed Run Worktree branch. Equivalent controls: approval —
  no change acceptance has been obtained; `_authorization.md` grants bounded
  implementation and delivery authority but is not review approval; checks
  and status — the Daemon ran `make verify` for expected head
  `934c775ff03f720f0e170d95c70afffcf798992a` and recorded exit 0;
  unresolved review threads — none can stand because no Pull Request or thread
  collection exists; Merge-Ready acceptance — none exists because configured
  pre-PR review follows this authored gate; review-artifact ancestry — no
  review artifact exists to misbind, while this gate is bound to expected head
  `934c775ff03f720f0e170d95c70afffcf798992a` and Task commits
  `380db2f9`, `e6f7907b`, `972bbe8f`, `b4e0ac3f` and `934c775f`.

## Findings

None.

## Blocked and skipped

- R7 is `blocked (environment: no open Pull Request)`. The QA assignment
  confirms the absence and separates the target branch from the never-pushed
  Run Worktree branch. R7 records equivalent evidence for approval, checks and
  status, unresolved review threads, Merge-Ready acceptance and review-artifact
  ancestry. Unblocking action: after the Daemon integrates this Run, perform
  configured pre-PR review and open the Pull Request from
  `fix/0157-reconcile-and-repository-identity`; those later workflow steps are
  outside this gate.

## Coverage

Coverage is closed: 7/7 rows executed, 6 passed and 1 is environment-blocked
with equivalent evidence. Every declared Requirement and non-waivable source
appears in the matrix, and no row names another source. Requirement 7 constrains
evidence quality and is not a coverage source. The PRD has no `Unreachable
Acceptance` declarations and declares no frontend surface. There are no failed,
finding-blocked, declared-blocked, skipped or pending rows.

## Final verdict

Pass. Every runnable row passed, and the single environment-blocked Pull
Request row records its cause and equivalent evidence for all five controls.
Counts: 6 pass, 1 environment-blocked, 0 fail, 0 finding-blocked, 0
declared-blocked, 0 skipped; findings by impact tier: Blocks-Completion 0,
Data-Loss 0, Trust-Damage 0, Friction 0, Cosmetic 0.

## Mechanical skips

| Detector | Missing artifact |
| --- | --- |
| authorization bounded paths | tooling authorization |
| consequent-fix commit order | consequent-fix declaration |
