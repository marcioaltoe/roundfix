---
spec: 0135-a-rename-the-gate-sees-from-both-sides
date: 2026-09-14
build: "8354de7def75f2db6ea847b1a4ab799ce391be8e"
auditing_binary: "0.12.0"
auditor_staleness: "current: declared tree version: auditing binary 0.12.0 does not predate 0.12.0"
status: closed
verdict: pass
rows_blocked_environment: 1
rows_blocked_finding: 0
rows_blocked_declared: 0
surfaces: [backend, cli]
---

# QA Report

## Scope and environment

Full gate over both PRD Goals, all seven Acceptance Criteria in Tasks 01 and
02, all four Core Features, both declared intentional breaks, all four
Non-Goals, and the seven Acceptance Criteria of the authored terminal QA Task
at `8354de7def75f2db6ea847b1a4ab799ce391be8e`.

The actors are a Roundfix operator exercising the rebuilt command and a
maintainer auditing the Git-backed authorization readers. The PRD declares no
Unreachable Acceptance. Pull Request fact from the QA assignment: no Pull
Request is open; Pull Request journeys are environment-blocked.

The matrix uses a real temporary Git repository for rename, removal, staging,
and authorization journeys. The changed packages are `internal/daemon` and
`internal/spec`. Commands run from the Run Worktree unless a row names its
temporary repository.

## Static gate

- `rtk roundfix spec check 0135-a-rename-the-gate-sees-from-both-sides --strict`
  exited 0 with no findings. Authored Verification commands were not executed.
- `rtk env GOTOOLCHAIN=go1.26.7
  GOCACHE=/tmp/roundfix-go-build-0135-qa make GO=go
  GOFMT=/Users/marcio/go/pkg/mod/golang.org/toolchain@v0.0.1-go1.26.7.darwin-arm64/bin/gofmt
  verify` exited 0 on the unchanged full-access retry after a sandbox network
  denial. The gate completed every package, Skills checks, and a fresh build.
- `rtk env GOTOOLCHAIN=go1.26.7
  GOCACHE=/tmp/roundfix-go-build-0135-qa go vet ./internal/daemon
  ./internal/spec` exited 0 with no diagnostics.
- The rebuilt binary reported `roundfix 0.12.0 (8354de7d-dirty, built
  2026-09-14 09:44:47 -0300)` and its strict Spec check exited 0.

## Performed repairs

None.

## Assigned repair failures

None.

## Authorization audit inputs

None.

## Mechanical findings

None.

## Results

| # | Status | Provenance |
| - | --- | --- |
| Q-01 | pass | Authored QA precondition, dependency completion, pinned toolchain, and rebuilt binary |
| Q-02 | pass | Task 01 real-rename reader result, PRD Goal 1 premise, and outside Git evidence |
| Q-03 | pass | Task 01 governed-source rename refusal and intentional break 1 through the real reader |
| Q-04 | pass | Task 01 ordinary-removal preservation |
| Q-05 | pass | Task 01 real-repository staging preservation |
| Q-06 | pass | Task 02 unavailable-revision diagnostic, PRD Goal 2, and intentional break 2 |
| Q-07 | pass | Task 02 unresolvable-Spec-Root diagnostic preservation |
| Q-08 | pass | Task 02 unresolved and fail-closed outcomes |
| Q-09 | pass | Existing refusal tokens, operation vocabulary, and conditions |
| Q-10 | pass | Repository Verification on the pinned Go toolchain |
| Q-11 | pass | Scoped `go vet` over the packages this Spec changes |
| Q-12 | pass | Governed scope, vocabulary, changed-file boundaries, and Non-Goals |
| Q-13 | blocked (environment: no open Pull Request) | Pull Request controls and exact-head equivalent evidence |
| Q-14 | pass | QA report closure, exact counters, and evidence-path reread |

### Q-01

Actor and entry point: maintainer through the authored Task Graph, installed
strict Spec checker, pinned Go toolchain, and rebuilt CLI.

Expected observable: `task_03` is the unique terminal QA node, both non-QA
dependencies are completed, the strict checker returns no findings, and the
rebuilt binary identifies the audited head.

Independent confirmation and persistence: reread the Task Graph and Task
frontmatter after the build, then run the rebuilt binary's version and strict
check commands from a fresh process.

Observed: `_tasks.md` names `task_03` as the unique `qa` node and makes it
depend on both non-QA leaves; fresh Task-file reads found `task_01` and
`task_02` completed. The installed `roundfix 0.12.0` checker and the rebuilt
binary both returned `No findings` from the strict check. Go resolved to
`go1.26.7 darwin/arm64`. After the complete repository gate, a fresh process
reported `roundfix 0.12.0 (8354de7d-dirty, built 2026-09-14 09:44:47 -0300)`.

```yaml
inputs:
  - kind: repository_path
    ref: docs/specs/0135-a-rename-the-gate-sees-from-both-sides/_tasks.md
  - kind: repository_path
    ref: docs/specs/0135-a-rename-the-gate-sees-from-both-sides/task_0*.md
  - kind: repository_path
    ref: cmd/roundfix/**
  - kind: repository_path
    ref: internal/app/version.go
```

### Q-02

Actor and entry point: maintainer performing a real Git rename and reading it
through the Daemon's `GitWorktreeSnapshotter` boundary.

Expected observable: Git porcelain carries the destination and source, and the
reader returns both `notes.txt` and `Makefile`. This row carries the outside
evidence required by the Spec; the source is Git's own porcelain output from
the temporary repository, not bytes authored by the Spec.

Independent confirmation and persistence: inspect raw `git status
--porcelain=v1 -z` bytes separately, then rerun the reader regression against a
fresh temporary repository.

Observed: a new Git repository committed `Makefile`, then ran `git mv Makefile
notes.txt`. Git's raw NUL-delimited porcelain bytes decoded as `R  notes.txt`,
NUL, `Makefile`, NUL; `git diff --cached --name-status -M` independently
reported `R100 Makefile notes.txt`. On Go 1.26.7,
`TestSnapshotCarriesRenameSource` passed against another fresh temporary
repository and required the reader result to equal exactly `Makefile` and
`notes.txt` after sorting.

```yaml
inputs:
  - kind: repository_path
    ref: internal/daemon/daemon.go
  - kind: repository_path
    ref: internal/daemon/daemon_test.go
  - kind: external_repository
    ref: temporary Git repository porcelain v1 rename output observed 2026-09-14
```

### Q-03

Actor and entry point: Roundfix operator attempting to settle a failed Task
after renaming a tracked governed `Makefile` to ungoverned `notes.txt` without
the required operation authority.

Expected observable: the rebuilt public command refuses before Verification or
commit with the existing operation-authority diagnostic. The changed set comes
from the real Git reader and contains the governed source.

Independent confirmation and persistence: reopen the scratch repository and
confirm its HEAD, Task bytes, dirty rename, Verification marker, and Run state
remain unchanged.

Observed: the rebuilt public `roundfix settle --spec qa-rename-source --task
task_01` command consumed a real staged `R  Makefile -> notes.txt` rename and
exited 2 with `authorization operation "commit" is not permitted by record
"docs/specs/qa-rename-source/_authorization.md"`. The record permits
`implement`, which proves that the refusal is for the operation required by the
governed rename. stdout stayed empty, the authored `touch should-not-run`
Verification did not run, and independent post-command reads found the same
HEAD, the same Task digest, and the same dirty rename. No Run was created.

The first sandboxed fixture attempt stopped earlier at the default Artifact
Directory, and the next reached the machine-wide Run Database lock. The final
fixture kept its artifacts in the disposable root and ran with the required
filesystem access; neither earlier preflight reached the feature behavior.

```yaml
inputs:
  - kind: repository_path
    ref: bin/roundfix
  - kind: repository_path
    ref: internal/daemon/daemon.go
  - kind: repository_path
    ref: internal/daemon/task_engine.go
  - kind: repository_path
    ref: internal/spec/authorization.go
```

### Q-04

Actor and entry point: Roundfix operator settling a failed Task after removing
an ordinary tracked source file.

Expected observable: the ordinary removal settles without governed-operation
authority.

Independent confirmation and persistence: reopen the repository after the
command and verify the removal and settled Task state are committed with a
clean worktree.

Observed: in a separate temporary repository with no authorization record, the
rebuilt public `roundfix settle --spec qa-ordinary-removal --task task_01`
command exited 0. It reported the authored removal check `ok`, committed the
Task file and deleted `internal/ordinary.go`, and settled the Task. A fresh Git
reread found a new HEAD, `status: completed`, an empty worktree status, and a
commit containing only the Task-file modification and ordinary-source
deletion. The focused `TestOrdinaryRemovalDoesNotRequireOperation` regression
also passed on Go 1.26.7.

```yaml
inputs:
  - kind: repository_path
    ref: bin/roundfix
  - kind: repository_path
    ref: internal/cli/settle.go
  - kind: repository_path
    ref: internal/daemon/task_engine.go
  - kind: repository_path
    ref: internal/daemon/task_engine_test.go
```

### Q-05

Actor and entry point: Daemon Task-cycle staging against a real temporary Git
repository.

Expected observable: widening the changed set to both rename sides continues
to stage exactly the Agent's own changes.

Independent confirmation and persistence: inspect the resulting commit and
fresh worktree status from Git, then rerun the existing regression.

Observed: `TestSnapshotDiffCommitStagesOnlyAgentChangesInRealRepo` passed on Go
1.26.7. Its real repository commit contains only `agent-fix.go`, while the
pre-existing tracked edit and untracked note remain outside the commit and
remain visible in a fresh porcelain-status read.

```yaml
inputs:
  - kind: repository_path
    ref: internal/daemon/daemon.go
  - kind: repository_path
    ref: internal/daemon/daemon_test.go
```

### Q-06

Actor and entry point: Daemon resolving Spec-contained authorization against an
unknown delivery revision.

Expected observable: the result is `unavailable_revision`, field `revision`,
and the normalized unavailable revision value; the old Spec-Root diagnostic no
longer appears on this branch.

Independent confirmation and persistence: repeat the composed
`ReadSpecAuthorization` path in a fresh Git repository and compare its typed
result with the direct reader's established reason.

Observed: `TestSpecAuthorizationNamesUnavailableRevision` passed on Go 1.26.7
through `ReadSpecAuthorization` against a fresh Git repository. It requires
outcome `unresolved`, reason `unavailable_revision`, field `revision`, value
`refs/heads/unavailable`, and refusal of `implement`. The changed production
branch reuses `AuthorizationReasonUnavailableRevision`; the direct reader is
outside this Spec's diff.

```yaml
inputs:
  - kind: repository_path
    ref: internal/spec/authorization.go
  - kind: repository_path
    ref: internal/spec/authorization_test.go
```

### Q-07

Actor and entry point: Daemon resolving Spec-contained authorization through a
genuinely unresolvable Spec Root.

Expected observable: the existing unreadable-record reason and `spec_root`
field remain distinct from Q-06.

Independent confirmation and persistence: rerun both diagnostics together and
compare their typed reasons, fields, and values.

Observed: the same fresh Go 1.26.7 run passed
`TestOperationAuthorityReportsUnresolvableSpecRoot` together with Q-06. The
Spec-Root case remains outcome `unresolved`, reason `unreadable_record`, and
field `spec_root`; these values are distinct from Q-06's revision diagnostic.
`TestOperationAuthorityResolvesExternalSpecRoot` and
`TestOperationAuthorityDefaultRootUnchanged` also passed as adjacent location
controls.

```yaml
inputs:
  - kind: repository_path
    ref: internal/spec/authorization.go
  - kind: repository_path
    ref: internal/spec/authorization_test.go
```

### Q-08

Actor and entry point: Daemon evaluating operation authority after either
location-resolution failure.

Expected observable: both cases remain unresolved and neither permits
`implement`; the diagnostic correction grants no authority.

Independent confirmation and persistence: fresh result objects preserve
`AuthorizationUnresolved` and reject the operation on repeated reads.

Observed: Q-06 directly asserted that the unavailable revision cannot permit
`implement`; Q-07 directly asserted that the unresolvable Spec Root remains
unresolved. The two adjacent resolution controls passed in the same unchanged
process, so the diagnostic repair did not widen authority for default or
external Spec Roots.

```yaml
inputs:
  - kind: repository_path
    ref: internal/spec/authorization.go
  - kind: repository_path
    ref: internal/spec/authorization_test.go
```

### Q-09

Actor and entry point: maintainer exercising adjacent refusal paths and reading
the committed implementation range.

Expected observable: existing refusal tokens and their conditions remain
unchanged, and no operation-vocabulary or Governed Path definition changes.

Independent confirmation and persistence: run the refusal regressions, inspect
the exact committed diff, and compare the vocabulary owners with the pre-Task
base.

Observed: the fresh Go 1.26.7 Daemon run passed nine named refusal and
preservation tests, including missing `implement`, missing `commit`, governed
removal, injected governed-source rename, real-reader governed-source rename,
ordinary removal, unfiltered-snapshot delivery, and the separate commit/push
authority subcases. The full repository suite then passed every remaining
assertion. The implementation diff does not touch
`internal/authorization`, `internal/daemon/task_engine.go`, or
`internal/speccheck/governed.go`, which own the closed operation vocabulary,
the classifier, and the Governed Path set.

```yaml
inputs:
  - kind: repository_path
    ref: internal/authorization/**
  - kind: repository_path
    ref: internal/daemon/task_engine.go
  - kind: repository_path
    ref: internal/daemon/task_engine_test.go
  - kind: repository_path
    ref: internal/speccheck/governed.go
  - kind: repository_path
    ref: internal/spec/authorization.go
  - kind: repository_path
    ref: internal/spec/authorization_test.go
```

### Q-10

Actor and entry point: maintainer running `make verify` with Go 1.26.7 and that
toolchain's `gofmt`.

Expected observable: formatting, the full Go suite, Skills checks, and binary
build all exit clean.

Independent confirmation and persistence: run the gate without output piping,
record its exit code, and execute the rebuilt binary in a fresh process.

Observed: `rtk env GOTOOLCHAIN=go1.26.7
GOCACHE=/tmp/roundfix-go-build-0135-qa make GO=go
GOFMT=/Users/marcio/go/pkg/mod/golang.org/toolchain@v0.0.1-go1.26.7.darwin-arm64/bin/gofmt
verify` exited 0 on the one unchanged full-access retry. It completed
`go test -parallel 16 ./...`, the focused Skills suite, `roundfix skills
check`, and the binary build. The first attempt was stopped by sandbox network
denial at `api.github.com`; it did not report a test assertion failure. The
freshly rebuilt CLI version and strict check then exited 0 in separate
processes.

```yaml
inputs:
  - kind: repository_path
    ref: Makefile
  - kind: repository_path
    ref: go.mod
  - kind: repository_path
    ref: go.sum
  - kind: repository_path
    ref: "**/*.go"
  - kind: repository_path
    ref: .agents/skills/**
  - kind: repository_path
    ref: skills/**
```

### Q-11

Actor and entry point: maintainer running `go vet` on `internal/daemon` and
`internal/spec` with Go 1.26.7.

Expected observable: both changed packages emit no analyzer diagnostic.

Independent confirmation and persistence: run both packages together, then
reread the command result and package scope against the committed diff.

Observed: `rtk env GOTOOLCHAIN=go1.26.7
GOCACHE=/tmp/roundfix-go-build-0135-qa go vet ./internal/daemon
./internal/spec` exited 0 with no diagnostics. `git diff --name-only
81bf6938..8354de7d` independently limits implementation changes to those two
packages and their two completed Task files.

```yaml
inputs:
  - kind: repository_path
    ref: go.mod
  - kind: repository_path
    ref: internal/daemon/**
  - kind: repository_path
    ref: internal/spec/**
```

### Q-12

Actor and entry point: maintainer auditing the two non-QA Task commits and
their parent Spec commit.

Expected observable: only the seven declared source, test, and Task files
changed; no classifier, Governed Path set, operation vocabulary, protected
tooling, dependency, external/symlinked Spec Root, or repository-wide analyzer
work entered the Spec. The feature emits no new token.

Independent confirmation and persistence: use Git commit-tree path reads and
compare unchanged owners directly with the pre-Task base.

Observed: `git -c core.fsmonitor=false diff-tree --no-commit-id --name-only
-r 9bf96617` lists only `task_02.md`, `internal/spec/authorization.go`, and
`internal/spec/authorization_test.go`. The same command for `8354de7d` lists
only `task_01.md`, `internal/daemon/daemon.go`,
`internal/daemon/daemon_test.go`, and `internal/daemon/task_engine_test.go`.
The combined seven-path list exactly matches the assignment's prior changed
files.

The pre-Task-base comparison reports no change to
`internal/daemon/task_engine.go`, `internal/speccheck/governed.go`, `go.mod`,
`go.sum`, or `Makefile`. No tooling authorization or consequent-fix commit is
applicable. The accepted ADR-0130 Governed Path contract and ADR-0057 Daemon
status ownership remain intact. `CONTEXT.md` already owns the terms Governed
Path and Spec Root; the implementation adds no token, so no glossary update is
needed. External/symlinked Spec Root identity and repository-wide analyzer
coverage did not enter either Task commit.

```yaml
inputs:
  - kind: repository_path
    ref: CONTEXT.md
  - kind: repository_path
    ref: docs/adr/0057-daemon-exclusively-owns-implement-task-status.md
  - kind: repository_path
    ref: docs/adr/0130-the-audit-judges-governed-paths-and-history-keeps-the-set-honest.md
  - kind: repository_path
    ref: internal/authorization/**
  - kind: repository_path
    ref: internal/daemon/daemon*.go
  - kind: repository_path
    ref: internal/daemon/task_engine*.go
  - kind: repository_path
    ref: internal/spec/authorization*.go
  - kind: repository_path
    ref: internal/speccheck/governed.go
```

### Q-13

Actor and entry point: reviewer at the Pull Request boundary.

Expected observable: because no Pull Request is open, the row records the
environment cause plus equivalent evidence for approval, checks and status,
unresolved review threads, Merge-Ready acceptance, and review-artifact ancestry
against the exact audited head.

Independent confirmation and persistence: bind every available equivalent
control to the current Git head and state the unblocking action for the absent
Pull Request surface.

Observed environment cause: the QA assignment states that no Pull Request is
open and Pull Request journeys are environment-blocked. The Run Worktree branch
is never pushed and has no Pull Request of its own, so this row does not attempt
to resolve one from that branch.

Equivalent controls for audited head `8354de7d`:

- Approval: no Pull Request approval has been obtained; the committed Spec at
  `81bf6938` supplies the accepted requirements exercised here.
- Checks and status: the local pinned-toolchain `make verify`, scoped `go vet`,
  installed and rebuilt strict Spec checks, and rebuilt CLI version all passed.
- Unresolved review threads: no Pull Request surface exists on which a thread
  can stand; the assignment reports none open rather than an unresolved lookup.
- Merge-Ready acceptance: none exists yet. QA does not manufacture a Review
  Source verdict before the configured pre-Pull-Request review stage.
- Review-artifact ancestry: no review artifact exists yet. This evidence is
  bound directly to `8354de7d`, whose two Task commits and parent Spec commit
  were reread from Git.

Unblocking action: after the Daemon integrates the QA report, the delivery
owner must obtain the configured pre-Pull-Request review for the resulting
candidate, open the Pull Request on `feat/spec-contained-authorization`, and
observe its required checks, threads, and Merge-Ready evidence at that exact
head.

```yaml
inputs:
  - kind: live_service
    ref: GitHub Pull Request for feat/spec-contained-authorization
```

### Q-14

Actor and entry point: maintainer reopening the completed QA artifact.

Expected observable: one closed frontmatter block carries the mechanical
verdict and exact typed counters; no pending or skipped QA row remains; every
evidence locator resolves.

Independent confirmation and persistence: reread the report from disk and run
deterministic status/count/link checks after the final edit.

Observed: the final explicit-path reread found one closed frontmatter block,
`verdict: pass`, one environment-blocked row, zero finding- or
declared-blocked rows, 13 passing rows, and no pending or skipped QA status.
Every repository input path or glob resolves; Q-13 alone declares a live
service input and therefore remains non-carriable.

```yaml
inputs:
  - kind: repository_path
    ref: docs/specs/0135-a-rename-the-gate-sees-from-both-sides/qa/qa-report-2026-09-14.md
```

## Findings

None. The sandboxed public-fixture preflights and the first `make verify`
attempt stopped at filesystem or network boundaries before reaching a product
assertion. Their unchanged full-access runs reached the intended behavior and
passed.

## Blocked and skipped

Q-13 is blocked by the supplied environment fact `no open Pull Request`. All
five required controls have exact-head equivalent evidence. The delivery owner
must obtain the configured pre-Pull-Request review for the integrated candidate,
open the target-branch Pull Request, and observe its checks and review state.

No QA row was skipped, declared-blocked, or finding-blocked. The mechanical
skips materialized before this human QA run remain preserved below.

## Coverage

- PRD Goals: 2 passed / 2 total through the assembled reader paths.
- Core Features: 4 observed / 4 total; all passed.
- Tasks 01-02 Acceptance Criteria: 7 observed / 7 total; all passed.
- Task 03 Acceptance Criteria: 7 observed / 7 total; all passed.
- QA rows: 13 pass, 1 blocked (environment) with equivalent evidence, 0 fail,
  0 blocked (finding), 0 blocked (declared), 0 skipped, 0 pending.
- Findings: 0 Blocks-Completion, Data-Loss, Trust-Damage, Friction, or Cosmetic.
- Probes: raw NUL-delimited Git rename, governed-source rename without commit
  authority, post-refusal state persistence, ordinary-file removal without a
  record, pre-existing work isolation during staging, unknown revision,
  unresolvable Spec Root, default and external Spec Root controls, missing
  `implement`, missing `commit`, and separate commit/push authority.
- Non-Goals: classifier logic, external/symlinked Spec Root identity, Governed
  Path and operation vocabulary definitions, and repository-wide analyzer
  coverage did not enter the two non-QA Task commits.
- `rows_blocked_environment: 1`, `rows_blocked_finding: 0`, and
  `rows_blocked_declared: 0`.

## Final verdict

Pass. Every runnable row passed, the one environment-blocked Pull Request row
carries its cause and exact-head equivalent evidence, and no failed,
finding-blocked, declared-blocked, skipped, or pending row remains.

## Mechanical skips

| Detector | Missing artifact |
| --- | --- |
| authorization bounded paths | tooling authorization |
| consequent-fix commit order | consequent-fix declaration |
| QA Report structure | QA Report |
| QA evidence paths | QA Report |
