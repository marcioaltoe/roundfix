---
spec: 0068-spec-close-audit
date: 2026-08-04
build: 1346d83d4213e10b73a89bae6796d6d95dda6c18
status: closed
verdict: fail
rows_blocked_environment: 1
rows_blocked_finding: 0
rows_blocked_declared: 0
surfaces: [backend, cli, docs]
---

# QA report — Spec close audit

## Scope and environment

Full authored terminal QA Task on Run Worktree branch
`roundfix/run-run_20260804T233059Z_b56e1cad70138712`, build
`1346d83d4213e10b73a89bae6796d6d95dda6c18`, on macOS in the managed
workspace. The intended actor is a maintainer closing a Spec. The primary
public entry point is the built `roundfix spec audit <slug>` CLI against this
repository and disposable real-Git repositories. The documentation surface is
followed as an operator would read it.

The prompt proves that no Pull Request is open and states that Pull Request
journeys are environment-blocked. QA-05 therefore requires typed environment
blocking plus equivalent fixture evidence; the Run Worktree branch is never
used to infer a Pull Request. The PRD has no `## Unreachable Acceptance`
section, so this gate has no declaration it may match to a blocked row.

Evidence root:
`docs/specs/0068-spec-close-audit/qa/evidence/2026-08-04-spec-0068/`.

The initial matrix contained 17 rows. Before exercising unmatched-worktree
behavior, the PRD-to-Task comparison exposed a distinct pushed-and-merged
scratch-worktree promise, so QA-18 was added `pending` before that probe ran.
All 18 rows now have terminal status.

## Static gate

Pass. The required unpiped `rtk make verify` exited 0: 3,313 Go tests passed
across 26 packages; the isolated corpus-budget test and four Skill tests
passed; the Repository Skill Set passed; the binary built; and the Spec
Consistency Check reported no finding for Spec 0068. Evidence:
`evidence/2026-08-04-spec-0068/qa-02-make-verify.md`.

## Results

Every row is opened `pending`. Each row records the actor and public entry
point, expected observable, independent confirmation or persistence check,
and evidence destination.

| # | Story / criterion / sweep | Actor and entry point | Expected observable | Independent confirmation / persistence | Evidence | Status |
| - | --- | --- | --- | --- | --- | --- |
| QA-01 | Authored QA node, completed dependency closure, Project Constraints, and protected-tooling provenance | QA operator; Spec artifacts and Git history | `task_07` is the unique terminal `qa` node; every non-QA dependency is completed; both active Spec artifacts account for all four constraint categories; authorization and commit choreography cover every tooling path | Resolve implementation and authorization commits with `git diff-tree`; compare the canonical and mirrored Skill and validate generated fallout on the unchanged build | `evidence/2026-08-04-spec-0068/qa-01-governance.md` | pass |
| QA-02 | Repository Verification | QA operator; `rtk make verify` | The full gate exits 0 with no formatting, test, Skill, build, or Spec Consistency failure | Exit code and final gate summary from the unpiped command | `evidence/2026-08-04-spec-0068/qa-02-make-verify.md` | pass |
| QA-03 | A maintainer can discover and run the close audit for a real Spec without knowing its Runs or branches | Maintainer; built `roundfix --help`, `roundfix spec audit --help`, and `roundfix spec audit 0068-spec-close-audit` | Help lists the command; the slug alone produces a report whose survivors carry kinds and evidence | Rerun from a fresh process and parse the same current repository state through JSON | `evidence/2026-08-04-spec-0068/qa-03-real-spec.md` | pass |
| QA-04 | A clean Spec audit reports nothing needing attention | Maintainer; built CLI against a disposable committed Spec repository | Exit 0 and a clean text report | Fresh JSON invocation returns one empty `roundfix-specaudit/v1` result and a second run leaves state unchanged | `evidence/2026-08-04-spec-0068/qa-04-clean.md` | pass |
| QA-05 | A branch backing an Open Pull Request classifies `pull-request`, names the Pull Request, and is never reclaimable | Maintainer; Pull Request-backed close-audit journey | Real journey is unreachable because the prompt proves no Open Pull Request; equivalent fixture evidence must show the classification and empty reclaim command | Public CLI JSON over a real-Git fixture plus focused assembled verification; exact human unblocking action is to open a Pull Request on the Spec target branch and rerun read-only | `evidence/2026-08-04-spec-0068/qa-05-pull-request.md` | blocked (environment: no open Pull Request) |
| QA-06 | A branch with unintegrated work classifies `pending` with deciding evidence | Maintainer; built CLI against a disposable real-Git fixture | The survivor is `pending`, evidence names the unintegrated content, and no reclaim command is emitted | Rerun after a fresh process and compare the branch tip and working state | `evidence/2026-08-04-spec-0068/qa-06-pending.md` | pass |
| QA-07 | Merged local and stale remote branches classify `residue` with exact operator reclaim commands, but the audit executes neither | Maintainer; built CLI against disposable local and bare-remote fixtures | Exit 1; local and remote residue carry correct evidence and shell-safe reclaim commands | Snapshot refs, worktrees, and Run Database before and after two audit runs; all remain identical | `evidence/2026-08-04-spec-0068/qa-07-residue.md` | pass |
| QA-08 | An unmatched worktree stays `preserved` | Maintainer; built CLI against a disposable registered worktree with no matching Run | The worktree is `preserved`, evidence names the missing Run, and reclaim is empty | Fresh `git worktree list --porcelain` confirms the worktree persists after repeated audit | `evidence/2026-08-04-spec-0068/qa-08-preserved.md` | pass |
| QA-09 | An Active Run's branch and worktree are never classified `residue` or mutated | Maintainer; built CLI against the real active Run and an injected fixture | Both survivors stay preserved or pending with Active Run evidence and no reclaim command | Compare Run state, refs, worktree registration, and filesystem state before and after audit | `evidence/2026-08-04-spec-0068/qa-09-active-run.md` | pass |
| QA-10 | Delivery verification trusts the synced default tree, names a holding branch, and does not guess a missing holder | Maintainer; built CLI against delivered, branch-held, uncommitted-working-copy, and nowhere fixtures | Delivered claims are absent from the report; branch-only archive names its holder; working-copy-only and nowhere claims are undelivered with no guessed holder | Fresh Git-tree reads after each CLI run confirm the report against committed state | `evidence/2026-08-04-spec-0068/qa-10-delivery.md` | pass |
| QA-11 | A terminal Run whose target branch was deleted resolves by content, never by optimistic ancestry | Maintainer; built `roundfix reconcile` and `spec audit` against deleted-target fixtures | A fully represented Run Branch resolves `safe`; Run-only or different shared content resolves `unintegrated`; the reason names the evidence | Rerun inspection without `--apply` and confirm refs/worktrees persist; existing dirty and unknown refusals remain | `evidence/2026-08-04-spec-0068/qa-11-deleted-target.md` | pass |
| QA-12 | The motivating session replay reports both scratch worktrees, the orphaned Run Worktree, the remote backup branch, and both Pull Request branches | Maintainer; built CLI against the real-Git replay fixture tied to the source finding | All four residue categories appear with evidence; Pull Request branches are not reclaimable; the orphan resolves by content | Compare the fixture provenance and Git state after a second audit | `evidence/2026-08-04-spec-0068/qa-12-replay.md` | pass |
| QA-13 | Text, JSON, usage, stderr, and exit-code contracts are deterministic | Maintainer and automation; built CLI | Default text and `--format json` agree; JSON is one parseable object; clean exits 0, attention exits 1, and unknown slug, invalid format, or missing slug exits 2 with diagnostics on stderr | Decode JSON to EOF, rerun invalid and out-of-order inputs, and confirm no partial JSON or state mutation | `evidence/2026-08-04-spec-0068/qa-13-contract.md` | pass |
| QA-14 | Existing `spec check` and `reconcile` refusals retain their public behavior | Maintainer; built CLI regression journeys | `spec check` output/exit remain established; reconcile still refuses dirty, ambiguous, and unintegrated state | Fresh invocations over disposable fixtures confirm state persistence | `evidence/2026-08-04-spec-0068/qa-14-regression.md` | pass |
| QA-15 | The shipped Roundfix Skill accurately teaches the new command | Maintainer; canonical and mirrored Roundfix Skill plus built help | Required slug, both formats, four kinds, evidence, reclaim-as-operator-action, and exit codes match the binary | Follow the Skill commands against clean and attention fixtures; recursive comparison proves both Skill copies equal | `evidence/2026-08-04-spec-0068/qa-15-docs.md` | pass |
| QA-16 | The close audit is read-only across Git, Run Database, Spec artifacts, transport, and filesystem | Maintainer; repeated built CLI audit | No ref, worktree, database, Spec, or filesystem mutation; no network transport; ambiguity stays preserved | Byte/hash snapshots before and after plus repository-wide production import/call sweep | `evidence/2026-08-04-spec-0068/qa-16-read-only.md` | pass |
| QA-17 | Non-Goals remain absent: no automatic branch deletion, merge/review/close, process termination, compaction, or sanitation | QA operator; changed-path audit and public CLI behavior | The feature reports only; unrelated command behavior and owned subsystems do not change | Resolve every Task commit's changed paths and compare public state before and after attention reports | `evidence/2026-08-04-spec-0068/qa-17-non-goals.md` | pass |
| QA-18 | A pushed-and-merged Supervisor scratch worktree becomes reported residue while its branch remains | Maintainer; built CLI against a disposable pushed, merged, still-registered scratch worktree | The worktree classifies `residue` with an exact `git worktree remove --` command; the audit executes neither that command nor branch deletion | Repeat the audit and confirm the branch and worktree still exist, then compare the result with PRD Core Feature 4 and Task 02's stated coverage | `evidence/2026-08-04-spec-0068/qa-18-scratch-worktree.md` | fail |

Behavior probes selected for this destructive-safety boundary: repeat the same
audit, pass invalid and out-of-order input, audit a working-copy-only artifact,
inspect deleted-target content with a Run-only file, and retain unmatched or
Active worktrees. These probes target idempotency, invalid input, false
delivery, false integration, and accidental reclamation. Browser, viewport,
session, locale, accessibility, and offline probes do not apply to this
backend, CLI, and docs-only journey; transport absence is checked directly.

## Findings

### F-001 — Pushed-and-merged Supervisor scratch worktree is preserved without a reclaim path

- Impact: Blocks-Completion.
- Actor and journey: maintainer closing a Spec; QA-18 after the scratch branch
  is pushed and its content is merged to the default branch.
- Expected: the worktree classifies `residue` and carries the exact operator
  `git worktree remove --` command, while the audit itself mutates nothing.
- Actual: both local and remote branch refs classify `residue`, but the
  still-registered worktree classifies `preserved` because it has no matching
  Run and has no reclaim command. The emitted local branch deletion cannot
  close the journey while that branch remains checked out in the worktree.
- Reproduction: create a Spec-trailed scratch branch in a second worktree,
  push it, squash-merge it to `main`, push `main`, then run
  `bin/roundfix spec audit 0068-spec-close-audit` without removing the
  worktree.
- Evidence:
  `evidence/2026-08-04-spec-0068/qa-18-scratch-worktree.md`.
- Affected rows: QA-18.

## Blocked and skipped

QA-05 is `blocked (environment: no open Pull Request)` because the prompt
proves none is open. Equivalent observed evidence exists: fresh real-Git
fixtures classified Pull Request #42, #58, and #68 branches `pull-request`,
named each Pull Request, and emitted no reclaim command. A maintainer can
unblock the live journey by opening a Pull Request on
`ma/spec-0068-implementation` and rerunning this gate read-only. This single
environment block has sufficient equivalent evidence and would not by itself
cap the verdict. No row is finding-blocked, declared-blocked, or skipped.

## Coverage

18 of 18 planned rows reached terminal status: 16 pass, 1 fail, 1 blocked by
environment, 0 blocked by finding, 0 blocked by declaration, 0 skipped, and 0
pending. All three declared surfaces were exercised. Typed blocked counts:
environment 1, finding 0, declared 0. Findings by impact: Blocks-Completion 1;
Data-Loss 0; Trust-Damage 0; Friction 0; Cosmetic 0.

PRD traceability:

- Goals 1 and 4 and Core Features 1, 4, and 5 map to QA-03 and QA-05 through
  QA-09, QA-12, QA-16, and QA-18. QA-18 fails Core Feature 4.
- Goal 2 and Core Feature 2 map to QA-10.
- Goal 3 and Core Feature 3 map to QA-11 and QA-14.
- Core Feature 6 maps to QA-03 and QA-13.
- Success Metrics 1 through 4 map to QA-12, QA-11, QA-10, and QA-09.
- Every Non-Goal maps to QA-16 and QA-17.

Task acceptance traceability: Task 01 maps to QA-09, QA-11, and QA-14; Task
02 to QA-05 through QA-09, QA-16, and QA-18; Task 03 to QA-10 and QA-16; Task
04 to QA-03, QA-04, QA-07, QA-10, QA-13, and QA-14; Task 05 to QA-12 and
QA-17; Task 06 to QA-01, QA-02, and QA-15; Task 07 to the complete matrix and
this report. The pushed-and-merged scratch-worktree coverage statement in Task
02 is the only unmet criterion.

## Final verdict

Fail: 16 rows passed and the one environment-blocked Pull Request row has
equivalent evidence, but F-001 prevents the pushed-and-merged Supervisor
scratch-worktree journey from completing. The classifier must distinguish
that proven residue state from an ambiguous unmatched worktree and emit the
worktree reclaim action before this gate can pass on a new build.
