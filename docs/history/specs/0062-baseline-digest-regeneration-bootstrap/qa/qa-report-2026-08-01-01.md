---
spec: 0062-baseline-digest-regeneration-bootstrap
date: 2026-08-01
build: bb18c28981b22ae51e21d4b8ae246298edb6c29f
status: closed
verdict: pass
rows_blocked_environment: 1
rows_blocked_finding: 0
surfaces: [backend, infra, docs]
---

# QA report — Baseline digest regeneration bootstrap

## Scope and environment

Full Spec rerun against Run Worktree build
`bb18c28981b22ae51e21d4b8ae246298edb6c29f` on
`roundfix/run-run_20260801T165038Z_aeb47100d5549d9b`. The intended actors are a
Roundfix maintainer regenerating Baseline artifacts, an operator relying on
strict catalog validation, and a maintainer following the operating document.
The public entry points are `make baseline-digests`, ordinary Roundfix catalog
loads exercised by the production test boundary, and
`docs/workflow/baseline-digest-regeneration.md`.

Pull Request fact: no Pull Request is open for the Spec target branch
`ma/baseline-digest-regeneration-bootstrap`. Pull Request observation is
environment-blocked by contract; this per-Run branch is never pushed and is not
a Pull Request lookup target.

The high-risk regeneration journey uses four relevant probes: a stale pin on
the first invocation, an immediate second invocation for idempotence, an
inconsistency that survives rewriting, and a new manifest requirement supplied
out of order. Scratch copies isolate deliberate catalog edits from this QA
checkout.

The sandbox denied the host Go build cache on the first direct scratch check.
All scratch commands therefore used a writable case-local `GOCACHE` under
`/private/tmp`; no source, command semantics, or application input changed.

## Static gate

`rtk make verify` exited 0 on the assembled build: 2,954 Go tests passed in
24 packages; the four focused Skill tests passed; `roundfix skills check`
accepted all 14 required repository skills; and the production binary built
with `-buildvcs=false`. The build metadata recorded `bb18c28-dirty` because the
in-progress QA Report and evidence artifact already existed, not because an
implementation file changed.

## Results

| # | Story / criterion / sweep | Actor, entry point, and surface | Steps and probes | Expected observable, independent confirmation, and persistence | Evidence | Status |
| - | --- | --- | --- | --- | --- | --- |
| Q-01 | Project Constraints account for identifier strategy, authentication and HTTP, active ADR obligations, and tooling authority in both active Spec artifacts. | QA auditor; `_prd.md`, `_techspec.md`; docs | Inspect all four rows and operative `docs/agents/` sources; read ADR-0081 and ADR-0085. | Both artifacts independently state applicability, cite operative guidance, and agree with accepted ADRs; a second source inspection confirms the same contract. | Both snapshots contain all four rows, use operative sources, bind ADR-0081/ADR-0085, satisfy ADR-0077, and correctly make ADR-0063 inapplicable. [Audit evidence](evidence/2026-08-01-baseline-digest-bootstrap-rerun/command-results.md#project-constraints-and-tooling-authority). | pass |
| Q-02 | The authorized `Makefile` tooling change has exact bounded scope, authorization chronology, separate Task commit shape, consequent-fix ordering, and sanctioned derived-pin provenance. | QA auditor; Git ancestry and authorization record; infra | Resolve every Task commit, inspect each committed path with `git diff-tree`, compare ancestry to the authorization commit, and inspect the current delta. | Only the authorized `Makefile` path and assigned Task file occur in the tooling Task commit; authorization precedes it; no folded prerequisite/consequent fix or hand-edited pin exists. Re-read current status to confirm no later scope drift. | Authorization `e03fa07` precedes Task 04 `0775ad4`; `diff-tree` lists only `Makefile` and `task_04.md`; all other Task commits avoid protected tooling, and no pin or folded fix exists. [Audit evidence](evidence/2026-08-01-baseline-digest-bootstrap-rerun/command-results.md#project-constraints-and-tooling-authority). | pass |
| Q-03 | The repository Verification gate passes on the assembled build. | Maintainer; `rtk make verify`; backend, infra, docs | Run the exact repository gate once. | Exit 0 with formatting, tests, Skill checks, and build green; the captured exit/result independently confirms the build. | [`rtk make verify` exited 0](evidence/2026-08-01-baseline-digest-bootstrap-rerun/command-results.md#repository-verification): 2,954 Go tests, 4 Skill tests, 14 required Skill contracts, and the production build passed. | pass |
| Q-04 | Outside regeneration, the characterization corpus remains deterministic and strict, including the formatter mismatch diagnostic and unchanged unrelated diagnostics. | Operator; ordinary catalog test boundary; backend | Run the characterization twice; trigger a stale-pin ordinary load; compare the current golden and relevant implementation history. | Both corpus runs pass without rewriting; the stale pin still emits the same mismatch code; only the two guided detail changes are intentional. Hashing the golden before/after confirms persistence. | The focused set and second characterization passed; the golden retained SHA-256 `d1165372…81a4`; the scratch strict load emitted the unchanged mismatch code. [Contract evidence](evidence/2026-08-01-baseline-digest-bootstrap-rerun/command-results.md#focused-contract-checks). | pass |
| Q-05 | Regeneration mode is unreachable from public loads and defers exactly `catalog.profile.formatter.goldenDigest.mismatch`; a non-allowlisted diagnostic remains fatal. | Operator; public and package regeneration loaders; backend | Run mode-scoping and single-acquisition tests; inspect caller and allowlist boundaries. | Strict public load rejects the stale pin; regeneration defers only the one allowlisted code; another diagnostic remains fatal. Re-run an ordinary public path as the independent canary. | `TestCatalogRegenerationMode` and `TestRegenerationLoadsCatalogOnce` passed; lowercase regeneration entry points remain internal; the one-code allowlist and non-allowlisted failure are asserted. [Contract evidence](evidence/2026-08-01-baseline-digest-bootstrap-rerun/command-results.md#focused-contract-checks). | pass |
| Q-06 | One sanctioned invocation refreshes a stale formatter pin, succeeds, and threads one catalog through every regeneration stage. | Maintainer; `make baseline-digests` in a scratch archive; infra/backend | From a fresh archive, alter an existing module rule and its generated formatter fixture without updating the pin; prove the ordinary mismatch; invoke the target once; inspect exit/output and changed pins; run the single-acquisition guard. | First invocation exits 0 after rewriting; the strict close accepts the result; the guard records exactly one acquisition. A second invocation independently reports `"changed":false`, proving persisted convergence. | The strict precondition failed with the mismatch; the first target run exited 0 with `changed:true`, the strict read passed, the second run returned `changed:false`, and the one-load guard passed. [Retest evidence](evidence/2026-08-01-baseline-digest-bootstrap-rerun/command-results.md#one-invocation-regeneration-retest). | pass |
| Q-07 | Clean-tree regeneration is idempotent and retains the target step list, scans, comparison, structured output, and strict close. | Maintainer; `make baseline-digests` and dry-run recipe; infra | Run twice on a consistent scratch archive and inspect `make -n`. | Both invocations exit 0 with `"changed":false`; dry-run retains all declared stages and the non-update strict stage after rewrites. The second invocation is the persistence check. | Two clean-archive invocations exited 0 with `changed:false`; dry-run retained five updates, both scans, comparison, strict close, and success shape. [Idempotence evidence](evidence/2026-08-01-baseline-digest-bootstrap-rerun/command-results.md#clean-tree-idempotence-and-target-shape). | pass |
| Q-08 | A genuinely inconsistent post-rewrite catalog still fails strict validation in the target's structured failure shape. | Maintainer; strict-load and target recipe boundaries; infra/backend | Exercise a non-allowlisted/inconsistent catalog and inspect the final strict command plus trap fields. | Strict validation exits nonzero before success and reports `strict_validation_failed`, `strict-validation`, and `retryable:false`; an ordinary strict load independently confirms the inconsistency. | The final-stage strict command rejected a deliberately inconsistent scratch catalog; dry-run places it after comparison and binds the required structured fields before success. [Strict-close evidence](evidence/2026-08-01-baseline-digest-bootstrap-rerun/command-results.md#clean-tree-idempotence-and-target-shape). | pass |
| Q-09 | Missing Source Baseline clause and rule rows keep their codes and subjects, name the missing identity, and tell the human to add the row first because regeneration never creates it. | Maintainer; target/test diagnostic surface; backend/infra | Exercise missing-clause and missing-rule inputs, including a target invocation with the prerequisite omitted. | Both exact codes and subjects remain; each message names its identity and the manual first step; a filesystem check confirms no row was created. | The public target stopped on the new clause with the exact guided code/subject; no row appeared; focused clause/rule cases passed. [Missing-row evidence](evidence/2026-08-01-baseline-digest-bootstrap-rerun/command-results.md#missing-manifest-row). | pass |
| Q-10 | The operating document is executable without undocumented knowledge and accurately describes the repaired stale-pin path. | Maintainer; `docs/workflow/baseline-digest-regeneration.md`; docs | Follow both documented recovery branches against scratch inputs and resolve the ADR link. | The stale-pin branch succeeds once; the missing-row branch stops with the documented manual action; the document states the strict boundary, exact allowlist, strict close, and ADR-0085. Reopening the page confirms the durable instructions. | Both recovery branches matched public runs; the page and ADR link resolve and the focused contract scan passed. [Documentation evidence](evidence/2026-08-01-baseline-digest-bootstrap-rerun/command-results.md#documentation-and-scope). | pass |
| Q-11 | Non-Goals remain excluded: no automatic Source Baseline rows, parity marker, global relaxation, Verification weakening, retention change, or unrelated Baseline content. | QA auditor; Git scope plus real failure paths; backend/infra/docs | Inspect the Spec diff and deliberately exercise missing-row and strict-load paths. | No excluded artifact or behavior ships; missing rows remain manual and strict ordinary loads still reject drift. A final changed-path scan confirms the exclusion persists. | The Spec diff contains none of the excluded asset/policy changes; missing-row and strict-load probes preserved both exclusions; the full gate passed. [Scope evidence](evidence/2026-08-01-baseline-digest-bootstrap-rerun/command-results.md#documentation-and-scope). | pass |
| Q-12 | Pull Request checks, approval, unresolved review threads, Merge-Ready evidence, and review-artifact ancestry are observed read-only when an Open Pull Request exists. | Maintainer; target-branch Pull Request; Pull Request | Use the prompt's Pull Request fact; do not query the per-Run branch or mutate GitHub state. | With no open Pull Request, record the exact environment cause, local equivalent build evidence, and the target-branch action that unblocks the GitHub-only signals. | The prompt supplies supervised evidence that no target-branch Pull Request is open. The passing gate and local ancestry/path audit provide equivalent build-integrity evidence; GitHub-only signals become observable after the target-branch Pull Request opens. [Environment evidence](evidence/2026-08-01-baseline-digest-bootstrap-rerun/command-results.md#pull-request-environment). | blocked (environment: no open Pull Request) |

## Findings

No open findings. The previous report's F-01 is closed on build `bb18c28`: a
fresh stale-pin archive completed its first `make baseline-digests` invocation,
passed the immediate strict read, and converged to `changed:false` on the next
invocation.

## Blocked and skipped

- Q-12 — blocked by environment: no open Pull Request. The QA prompt is
  supervised evidence of the cause. Local Verification and ancestry/path
  inspection provide equivalent build-integrity evidence, but GitHub approval,
  checks, unresolved threads, and Merge-Ready state do not exist to observe.
  Open the Pull Request from `ma/baseline-digest-regeneration-bootstrap` to make
  those signals observable. No row was skipped.

## Coverage

All three PRD goals, four Core Features, four Success Metrics, four Non-Goal
groups, 38 Task acceptance criteria, three declared surfaces, and the Pull
Request journey were mapped. All runnable requirements passed on the current
build. Task-level evidence was credited only where it named a reproducible
check and the current repository gate passed; the assembled stale-pin,
idempotence, strict-close, missing-row, documentation, and scope journeys were
rerun directly.

Final row counts: 11 pass, 1 blocked by environment, 0 fail, 0 blocked by a
finding, 0 skipped, 0 pending. Impact totals: 0 Blocks-Completion, Data-Loss,
Trust-Damage, Friction, or Cosmetic findings. Probes executed: stale-pin first
invocation, immediate retry/idempotence, clean-state double invocation,
persistently inconsistent strict load, non-allowlisted diagnostic, and
out-of-order missing manifest row. No frontend or TUI exists in this Spec;
viewport, browser, console, network, and accessibility sweeps are not
applicable.

## Final verdict

Pass — every runnable row passed, the previous first-invocation failure is
closed on the current build, and the sole environment-blocked Pull Request row
has a prompt-supervised cause plus equivalent local build-integrity evidence;
there are no failed, finding-blocked, skipped, or pending rows.
