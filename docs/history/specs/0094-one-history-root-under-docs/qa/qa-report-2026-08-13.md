---
spec: 0094-one-history-root-under-docs
date: 2026-08-13
build: f5c1f5a5a485cdafc05eeb28ef57e14af409cc35
status: closed
verdict: pass
rows_blocked_environment: 1
rows_blocked_finding: 0
rows_blocked_declared: 0
surfaces: [cli, docs]
---

# QA report — One history root under `docs/`

## Scope and environment

Full terminal gate for all six PRD user stories, ten Core Features, Success
Metrics, explicit Non-Goals, task-level acceptance not safely credited from
focused evidence, and the mandatory commit-dependent tooling audit. The actor is
a Roundfix maintainer using a binary built from
`f5c1f5a5a485cdafc05eeb28ef57e14af409cc35` in this Run Worktree. Mutable flow
fixtures use disposable repositories under `/tmp`; the outside-evidence source
checkout is read-only. No Pull Request is open, so the live Review Source journey
is environment-blocked and is covered by equivalent repository evidence.

The strict authoring precondition passed:
`rtk roundfix spec check 0094-one-history-root-under-docs --strict` exited 0 with
`No findings.` The manifest names `task_09` as the terminal `qa` Task, and every
direct dependency is `completed`. The PRD has no `## Unreachable Acceptance`
declarations.

## Static gate

Passed. `rtk make verify` exited 0 after the full Go suite, focused Skill
contract, Repository Skill Set check, and production build. `rtk make
verify-docs` exited 0 after the docs-contract, repository-contract, declared
regeneration/frozen-boundary checks, corpus budget, and active Spec checks.

## Mechanical findings

None.

## Results

| # | Status | Provenance |
| - | --- | --- |

## Mechanical skips

| Detector | Missing artifact |
| --- | --- |
| authorization bounded paths | Task commits |
| consequent-fix commit order | consequent-fix declaration |
| QA Report structure | QA Report |
| QA evidence paths | QA Report |

## User-flow and contract results

| # | Story / criterion / sweep | Actor and surface | Status | Evidence |
| - | --- | --- | --- | --- |
| QA-01 | Stories 1 and 2; Archive Command and built-in history-root layout | Maintainer · CLI/docs | pass | `qa/evidence/2026-08-13-gate/final-command-evidence.md` |
| QA-02 | Stories 2 and 6; outside-evidence fleet migration, byte identity, and idempotence | Fleet maintainer · CLI | pass | `qa/evidence/2026-08-13-gate/final-command-evidence.md` |
| QA-03 | Story 3; retired ADR and declined Backlog Entry move while pending/active records stay live | Agent/maintainer · CLI/docs | pass | `qa/evidence/2026-08-13-gate/final-command-evidence.md` |
| QA-04 | Story 4; finished orphan Review Artifact retires, live/undecidable reviews stay, empty source shells disappear | Maintainer · CLI | pass | `qa/evidence/2026-08-13-gate/final-command-evidence.md` |
| QA-05 | Core Features 6–8; collision refusal names both paths, moves siblings, and remains non-current | Maintainer · CLI | pass | `qa/evidence/2026-08-13-gate/final-command-evidence.md` |
| QA-06 | Story 5; review excludes history and Spec-owned review trees and rule sources reach neither | Maintainer opening a Pull Request · Review configuration/docs | blocked (environment: no open Pull Request) | `qa/evidence/2026-08-13-gate/final-command-evidence.md` |
| QA-07 | Task 09 glossary closure and every-carrier documentation sweep | Agent/maintainer · docs/CLI help | pass | `qa/evidence/2026-08-13-gate/final-command-evidence.md` |
| QA-08 | Non-Goals: byte-preserving moves, local-Git-only liveness, unchanged Spec-owned Review path and Archive eligibility | Maintainer · CLI/docs | pass | `qa/evidence/2026-08-13-gate/final-command-evidence.md` |
| QA-09 | Mandatory tooling authorization, exact bounded paths, regeneration, and chronological commit shape | Maintainer · Git/CLI | pass | `qa/evidence/2026-08-13-gate/final-command-evidence.md` |

## Detailed evidence declarations

### QA-01

```yaml
inputs:
  - kind: repository_path
    ref: internal/spec/archive.go
  - kind: repository_path
    ref: internal/cli/archive.go
  - kind: repository_path
    ref: docs/history/**
```

Entry: built `roundfix archive` against a disposable completed Spec. Expected:
the command reports `docs/history/specs/<slug>`, stamps archive metadata, removes
the source, and a fresh read confirms the destination. Probe: run `archive
--help` independently and confirm it names the same built-in destination.

### QA-02

```yaml
inputs:
  - kind: repository_path
    ref: internal/baseline/**
  - kind: external_repository
    ref: /Users/marcio/dev/vortex@07826b268be7e77fbe2f6231fd5d151b54362cc8
```

Entry: `roundfix baseline update` against a disposable clone of an adopted
repository still holding the pre-0085 nested layout. Expected: unconfirmed plan
is read-only, approved apply moves every archived file under `docs/history/`,
the sorted content-identity set is unchanged, and a fresh rerun reports current
with zero relocations. Probe: confirm the source checkout remains unchanged.

### QA-03

```yaml
inputs:
  - kind: repository_path
    ref: internal/spec/retirement.go
  - kind: repository_path
    ref: internal/baseline/history_layout.go
```

Entry: Baseline update in a disposable repository containing superseded and
proposed ADRs plus declined and open Backlog Entries. Expected: only the
superseded and declined documents move, all four retain their identities, and a
fresh read confirms the active records stayed in their live directories. Probe:
rerun after apply and require no second relocation.

### QA-04

```yaml
inputs:
  - kind: repository_path
    ref: internal/spec/review_liveness.go
  - kind: repository_path
    ref: internal/baseline/history_layout.go
  - kind: repository_path
    ref: internal/baseline/transaction.go
```

Entry: Baseline update in a disposable local Git repository with finished,
reachable non-ancestor, and no-head orphan Review Artifacts plus a Spec-owned
Review Artifact. Expected: only finished moves; live and undecidable states are
reported distinctly; Spec-owned stays; source round/review directories are
pruned; a fresh rerun emits no finding for the retired review. Probe: inspect all
four paths after apply and rerun.

### QA-05

```yaml
inputs:
  - kind: repository_path
    ref: internal/baseline/apply.go
  - kind: repository_path
    ref: internal/baseline/transaction.go
```

Entry: approved Baseline update in a disposable dual-layout repository with one
occupied destination and one free sibling. Expected: non-success exit names
source, destination, and reason; sibling moves; colliding bytes remain distinct;
fresh text and JSON plans remain non-current and carry only identity metadata.
Probe: rerun twice and compare filesystem identities.

### QA-06

```yaml
inputs:
  - kind: repository_path
    ref: .coderabbit.yaml
  - kind: repository_path
    ref: internal/docscontract/publicdocs_test.go
  - kind: live_service
    ref: Pull Request stated absent by Roundfix QA environment
```

Entry: the Open Pull Request review surface. The environment states no Pull
Request is open. Expected blocked cause: `no open Pull Request`; equivalent
evidence must execute the repository-owned configuration checks, including
negative controls for missing exclusions and unsafe rule sources.

### QA-07

```yaml
inputs:
  - kind: repository_path
    ref: CONTEXT.md
  - kind: repository_path
    ref: .agents/skills/**/SKILL.md
  - kind: repository_path
    ref: docs/agents/**
  - kind: repository_path
    ref: internal/baseline/assets/**
```

Entry: follow the published docs and CLI help as their intended reader.
Expected: `History Root` and `History Relocation` own all emitted terms; active
carriers name `docs/history/specs/`; help output agrees with behavior; no stale
built-in `_archived/specs` carrier survives. Probe: positive and negative local
search plus built help output.

### QA-08

```yaml
inputs:
  - kind: repository_path
    ref: internal/spec/**
  - kind: repository_path
    ref: internal/baseline/**
  - kind: repository_path
    ref: internal/config/config.go
```

Entry: cross-check public-flow evidence and the implementation delta against
every Non-Goal. Expected: moved files retain identities; no provider/credential
path participates; Spec-owned review resolution and Archive eligibility remain
unchanged; no unrelated repository configuration changed.

### QA-09

```yaml
inputs:
  - kind: repository_path
    ref: docs/workflow/authorizations/2026-08-12-the-archive-root-under-docs.md
  - kind: repository_path
    ref: docs/specs/0094-one-history-root-under-docs/task_*.md
  - kind: repository_path
    ref: internal/baseline/assets/**
  - kind: repository_path
    ref: .agents/skills/**
```

Entry: `git diff-tree --no-commit-id --name-only -r <commit>` for every
tooling-changing Task commit plus ancestry checks for authorization,
prerequisite, and consequent fixes. Expected: grants precede changes, exact
bounded paths cover direct edits, sanctioned regeneration is reproducible, and
consequent fixes follow their cause in separate commits.

## Findings

None.

## Blocked and skipped

| Row | Classification | Cause | Equivalent observed evidence | Unblock action |
| --- | --- | --- | --- | --- |
| QA-06 | blocked (environment: no open Pull Request) | The supplied QA environment has no open Pull Request, and this gate may not create one. | The fresh focused docs-contract suite passed its positive configuration check and negative controls for both exclusions and unreachable rule sources; the full docs gate passed independently. | Open a Pull Request on `ma/0094-one-history-root-under-docs` and observe the CodeRabbit reviewed-file set. |

No row is finding-blocked, declared-blocked, or skipped. The four rows in
`Mechanical skips` are preserved Daemon materialization records, not QA matrix
rows skipped by this gate.

## Coverage

Eight rows passed through fresh application, CLI, Git, documentation, and
filesystem evidence. One Pull Request row is environment-blocked with its cause,
equivalent observed evidence, and unblock action recorded above. Together they
cover all six user stories, ten Core Features, eight Success Metrics, eight
Non-Goals, the outside-evidence obligation, and the mandatory tooling audit.

## Final verdict

Pass. Every runnable criterion passed; the sole environment-blocked row has
equivalent fresh evidence, and there are zero finding-blocked, declared-blocked,
or skipped QA rows.
