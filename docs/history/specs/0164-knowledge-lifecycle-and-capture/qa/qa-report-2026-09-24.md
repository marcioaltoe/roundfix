---
spec: 0164-knowledge-lifecycle-and-capture
date: 2026-09-24
build: 001d109bcce4a885150fdc5a084d9776d1dc775b
auditing_binary: "roundfix 0.16.0 (built from audited tree with -buildvcs=false)"
auditor_staleness: "current: binary built from audited commit 001d109bcce4a885150fdc5a084d9776d1dc775b"
status: closed
verdict: pass
rows_blocked_environment: 1
rows_blocked_finding: 0
rows_blocked_declared: 0
surfaces: [backend, docs]
---

# QA Report

## Scope and environment

Full terminal gate for commit `001d109bcce4a885150fdc5a084d9776d1dc775b` on the isolated Run Worktree branch. The intended actors are a Roundfix operator exercising the built CLI and a documentation reader following the rendered repository contracts. No Pull Request is open for the Spec target branch; the Pull Request journey uses the prompt-supplied environment fact and equivalent evidence path. Repository Verification was already run by the Daemon and will be recorded without rerunning it.

## Static gate

- Authoring precondition: `/tmp/roundfix-qa-0164 spec check 0164-knowledge-lifecycle-and-capture --strict` passed with `No findings`; the checker explicitly skipped `SC-ROLLUP-MEMBER`, `SC-BACKLOG-UNMOVED`, the vocabulary documentation detector, and `SC-REF-UNRESOLVED` because their optional artifacts are absent.
- Repository Verification: the Daemon ran `make verify-changed`; it passed with exit 0 and removed diagnostics on success. QA did not rerun it.

## Performed repairs

None.

## Assigned repair failures

None.

## Authorization audit inputs

- Authorizing revision: `59501c2a`, which contains `_authorization.md` and is an ancestor of every Task commit.
- Task 03: `35f0eafc`; governed changes are `internal/speccheck/backlog.go` and `internal/speccheck/backlog_test.go`.
- Task 04: `f505feeb`; bounded governed source changes are the context-workflow module, rendered guide/manifest, docs-contract test, and archive resolver/test named by `_authorization.md`; the remaining `internal/baseline/assets/**` and baseline testdata changes are sanctioned `make baseline-digests` outputs.
- Task 05: `001d109b`; bounded governed source changes are the Secondbrain module and rendered guide/manifest named by `_authorization.md`; the remaining baseline asset/testdata changes are sanctioned `make baseline-digests` outputs.
- No Task commit changes `_authorization.md`; no prerequisite-fix or consequent-fix declaration exists.

## Mechanical findings

None.

## Results

| # | Story / criterion / sweep | Actor and surface | Status | Provenance |
| - | --- | --- | --- | --- |
| R1 | Repository Verification gate fact | Daemon / repository | pass | Requirement 1; repository Verification |
| R2 | ADR-0163 acceptance and byte-preserving retirement of ADR-0123 and ADR-0152 | Maintainer / docs | pass | Requirement 2 |
| R3 | Recorded Review outcome is stable across real Git object-store states | Operator / backend | pass | Requirement 3 |
| R4 | Legacy Review Artifact relocation is independent of liveness | Operator / backend | pass | Requirement 4 |
| R5 | Finding closure and terminal Backlog disposition contracts | Maintainer / backend | pass | Requirement 5 |
| R6 | Rendered history-family and capture-publication contracts | Documentation reader / docs | pass | Requirement 6 |
| R7 | Governed-path authorization, chronology, and regeneration reproducibility | Maintainer / repository tooling | pass | Requirement 7 |
| R8 | This Spec's artifacts satisfy the promise rule | Spec author / docs | pass | Requirement 8 |
| R9 | Outside evidence corroborates the Review-retirement defect | Maintainer / docs | pass | outside-evidence row |
| R10 | Pull Request controls for the expected target head | Reviewer / Pull Request | blocked (environment: no open Pull Request) | Pull Request row |

## Row evidence

### R1

```yaml
inputs:
  - kind: repository_path
    ref: .
```

Expected observable: the declared repository gate completes successfully on the assembled tree. Observed supervised evidence: the Daemon reported `make verify-changed` passed with exit 0 and removed diagnostics on success. Independent confirmation: the current-tree `roundfix 0.16.0` binary built successfully and its strict Spec check returned `No findings`; this does not replace the repository gate. Persistence: the fact is bound to build `001d109bcce4a885150fdc5a084d9776d1dc775b` in this report. Status: pass.

### R2

```yaml
inputs:
  - kind: repository_path
    ref: docs/adr/0163-review-artifact-retirement-reads-recorded-evidence.md
  - kind: repository_path
    ref: docs/history/adr/0123-a-review-retires-on-local-reachability-and-never-writes-into-history.md
  - kind: repository_path
    ref: docs/history/adr/0152-review-artifact-retirement-uses-stable-evidence.md
```

Expected observable: ADR-0163 is accepted, names both superseded decisions and the history-root boundary; both earlier ADRs are superseded under `docs/history/adr/`, their former paths are absent, and their bodies match the authoring baseline. Observed: lifecycle and required-language scans passed; both former paths are absent. Independent confirmation: body-only SHA-256 pairs matched byte-for-byte — ADR-0123 `2d6cb85c35497686f4c9becfcef2d9fd261fa915761247252e214e27c03ef5c9`, ADR-0152 `757976b007fd9140ee9c59019962453b92bbb680a85d11327ec15026819fca2b`. Persistence: the comparison used the bodies at authoring commit `59501c2a` and current build `001d109b`. Status: pass.

### R3

```yaml
inputs:
  - kind: repository_path
    ref: internal/spec/review_liveness.go
  - kind: repository_path
    ref: internal/spec/review_liveness_test.go
```

Expected observable: the same recorded-open Review stays live when its head is on a local branch, absent, or reachable only from a fetched ref; a recorded hexadecimal squash receipt finishes without a local merge object; absent and malformed outcomes remain explicitly undecidable. Observed: `go test -count=1 -v -run '^(TestClassifyReviewIgnoresObjectStoreAvailability|TestClassifyReviewAcceptsARecordedSquashReceipt|TestClassifyReviewWithoutARecordedOutcomeIsUnknown|TestClassifyReviewRejectsMalformedRecordedOutcome)$' ./internal/spec` passed all named real-Git and invalid-record cases. Independent confirmation: the verbose output separately reported each of the three object-store states and six malformed-record branches. Persistence: every case constructs fresh repositories and records on the current build; `-count=1` disabled cached results. Status: pass.

### R4

```yaml
inputs:
  - kind: repository_path
    ref: internal/baseline/history_layout.go
  - kind: repository_path
    ref: internal/baseline/history_layout_test.go
```

Expected observable: legacy `_reviews` artifacts move to `docs/specs/reviews/` when live or undecidable and to `docs/history/reviews/` when finished, preserving retained reports and collision safety. Observed: `go test -count=1 -v -run '^TestHistoryLayoutRelocatesLegacyReviewRootWhateverItsLiveness$' ./internal/baseline` passed. Independent confirmation: the named case exercises all three liveness results and asserts their distinct destination trees and retained report files. Persistence: the case used fresh fixtures on build `001d109b` with test caching disabled. Status: pass.

### R5

```yaml
inputs:
  - kind: repository_path
    ref: internal/spec/retirement.go
  - kind: repository_path
    ref: internal/spec/retirement_test.go
  - kind: repository_path
    ref: internal/speccheck/backlog.go
  - kind: repository_path
    ref: internal/speccheck/backlog_test.go
  - kind: repository_path
    ref: internal/speccheck/citations.go
  - kind: repository_path
    ref: internal/speccheck/citations_test.go
```

Expected observable: closure reason plus evidence licenses an absorber-less archived Finding without masking an invalid absorber; every declared terminal Backlog status retires only with its required evidence; active, promoted, unknown, missing-evidence, and blank-evidence cases stay live; a terminal entry left active raises `SC-BACKLOG-UNMOVED`. Observed: the four named focused tests passed across `internal/speccheck` and `internal/spec`. Independent confirmation: verbose output reported the invalid-absorber and two-route-fix branches plus all eleven Backlog status/evidence branches separately. Persistence: fresh test fixtures ran with `-count=1`. Status: pass.

### R6

```yaml
inputs:
  - kind: repository_path
    ref: internal/spec/archive.go
  - kind: repository_path
    ref: internal/baseline/assets/modules/context-workflow.json
  - kind: repository_path
    ref: internal/baseline/assets/modules/secondbrain.json
  - kind: repository_path
    ref: docs/agents/docs-layout.md
  - kind: repository_path
    ref: docs/agents/secondbrain.md
  - kind: repository_path
    ref: internal/docscontract/publicdocs_test.go
```

Expected observable: `ArchiveKinds()` enumerates all retired families, the rendered layout guide names each resolved history directory, the capture module and rendered guide name the publication owner and all three evidence levels, and neither contains the old unconditional commit command. Observed: `TestArchiveKindsNameEveryRetiredFamily`, `TestDocsLayoutGuideNamesEveryHistoryFamily`, and `TestCatalogCompatibility` passed; literal scans found `publication owner`, `captured locally`, `committed locally`, and `confirmed on the remote` in both sources and did not find the removed sentence. Independent confirmation through the public CLI: `/tmp/roundfix-qa-0164 baseline update --repo . --no-skills --format text` reported `Baseline update: current`, identical prior/current catalog digests, and `File changes: 0`. Persistence: the reader-facing guides and their module sources agree at build `001d109b`. Status: pass.

### R7

```yaml
inputs:
  - kind: repository_path
    ref: docs/specs/0164-knowledge-lifecycle-and-capture/_authorization.md
  - kind: repository_path
    ref: docs/specs/0164-knowledge-lifecycle-and-capture/task_03.md
  - kind: repository_path
    ref: docs/specs/0164-knowledge-lifecycle-and-capture/task_04.md
  - kind: repository_path
    ref: docs/specs/0164-knowledge-lifecycle-and-capture/task_05.md
  - kind: repository_path
    ref: internal/baseline/**
  - kind: repository_path
    ref: internal/docscontract/**
  - kind: repository_path
    ref: internal/spec/**
  - kind: repository_path
    ref: internal/speccheck/**
  - kind: repository_path
    ref: docs/agents/**
```

Expected observable: every governed path changed by Tasks 03-05 is either an exact bounded path or a sanctioned regeneration output; the authorization is separate and earlier; a second regeneration is byte-identical. Observed: `git diff-tree --no-commit-id --name-only -r` inventories for commits `35f0eafc`, `f505feeb`, and `001d109b` matched the bounded/source and derived-output sets above; `git merge-base --is-ancestor 59501c2a <task-commit>` passed for all five Task commits. `TestGovernedPath` and `TestOutputsForCommand` passed, including the historical governed set and the `make baseline-digests` output enumeration. Independent confirmation: `make baseline-digests` returned `changed:false` and `baseline-digests: no changes`; `git diff --exit-code` then passed, leaving only this untracked QA directory. Persistence: the authorization commit predates the Task series and the reproduction ran against final build `001d109b`. Status: pass.

### R8

```yaml
inputs:
  - kind: repository_path
    ref: docs/specs/0164-knowledge-lifecycle-and-capture/_prd.md
  - kind: repository_path
    ref: docs/specs/0164-knowledge-lifecycle-and-capture/_techspec.md
  - kind: repository_path
    ref: docs/specs/0164-knowledge-lifecycle-and-capture/_tasks.md
  - kind: repository_path
    ref: docs/specs/0164-knowledge-lifecycle-and-capture/task_*.md
```

Expected observable: each numbered Success Metric maps into the TechSpec and each Success Metric and API Contract names a consuming Task, with no undeclared promise section. Observed through the public authoring surface: the already-recorded current-tree strict Spec check returned `No findings`, including no `SC-COVERAGE-UNMAPPED` or `SC-COVERAGE-UNTASKED` error; the check was not rerun as a matrix command. Independent confirmation: `TestPromiseCoverageReachesItsTask` passed its unmapped, unnamed, resolved-range, and no-TechSpec branches with `-count=1`. Persistence: the check read this Spec's current PRD, TechSpec, graph, and Tasks at build `001d109b`. Status: pass.

### R9

```yaml
inputs:
  - kind: repository_path
    ref: docs/history/findings/2026-08-14-a-review-retires-on-whatever-the-object-store-happens-to-hold.md
```

Outside source: `docs/history/findings/2026-08-14-a-review-retires-on-whatever-the-object-store-happens-to-hold.md`, a measured Finding created before this Spec. It records fifty orphan Review Artifacts producing three classifications in one session as refs changed, plus the squash-merge ancestry failure and the stranded legacy root. Provenance check: commit `1106d154f149bc7c5a8109d9a470f5d98700a599` containing the archived Finding is an ancestor of authoring commit `59501c2a`; `git diff --exit-code 59501c2a..HEAD -- <finding>` passed, so the Spec did not rewrite its outside evidence. Independent current-build corroboration: a separate uncached run of `TestClassifyReviewIgnoresObjectStoreAvailability` passed the present, absent, and fetched-ref states. Persistence: the outside Finding remains a pre-Spec Git object and the reproduction is bound to build `001d109b`. Status: pass.

### R10

```yaml
inputs:
  - kind: live_service
    ref: Pull Request for feat/0164-knowledge-lifecycle-and-capture
```

Environment cause: the Roundfix QA assignment states that no Pull Request is open for `feat/0164-knowledge-lifecycle-and-capture`; the per-Run branch is never pushed and has no Pull Request of its own. Equivalent supervised or observed evidence covers every control:

- Approval: no Pull Request acceptance has been obtained.
- Checks and status: Daemon-supervised `make verify-changed` passed with exit 0; current-tree strict Spec check and rows R2-R9 passed.
- Unresolved review threads: no Pull Request exists, so no Pull Request thread set exists to resolve; none was claimed as reviewed.
- Merge-Ready acceptance: no Review Source acceptance exists yet; none was claimed.
- Review-artifact ancestry: no Spec-local `reviews/` artifact exists. Both `HEAD` and `feat/0164-knowledge-lifecycle-and-capture` resolve to `001d109bcce4a885150fdc5a084d9776d1dc775b`, which is the head this row records.

Unblocking action: open the Pull Request later in the delivery sequence and run the configured current-head review/check policy there. Status: blocked (environment: no open Pull Request), with complete equivalent evidence for this pre-PR gate.

## Findings

None.

## Blocked and skipped

- R10 is blocked by the expected pre-PR environment. All five Pull Request controls have explicit equivalent evidence or an explicit supervised absence, so the environment block does not prevent `pass`.
- No row is finding-blocked, declared-blocked, or skipped.

## Coverage

All 10 planned rows are terminal: 9 passed and 1 is environment-blocked with equivalent evidence. The matrix covers Requirements 1-8, the repository Verification, the outside-evidence row, and the Pull Request row. There are no PRD Unreachable Acceptance declarations and no frontend sweep. `rows_blocked_environment: 1`; `rows_blocked_finding: 0`; `rows_blocked_declared: 0`. No risk-based probes were added because the QA Task declares the matrix sources explicitly.

## Final verdict

Pass. Every runnable criterion passed on build `001d109b`; the sole environment-blocked Pull Request row records its cause, exact unblocking action, and equivalent evidence for every required control, with no finding-blocked, declared-blocked, skipped, or pending row.

## Mechanical skips

| Detector | Missing artifact |
| --- | --- |
| authorization bounded paths | Task commits |
| consequent-fix commit order | consequent-fix declaration |
| QA Report structure | QA Report |
| QA evidence paths | QA Report |

These rows preserve the seeded mechanical-stage record. R7 subsequently supplied the missing Task-commit authorization audit, and the final evidence-gate inspection supplied the report structure and evidence-path checks; no consequent-fix declaration exists.
