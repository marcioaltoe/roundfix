---
spec: 0118-a-task-proved-once-does-not-run-twice
date: 2026-08-27
build: 4315553db6b81a8a7ae9a8b6169c80871442f7c3
status: closed
verdict: pass
rows_blocked_environment: 0
rows_blocked_finding: 0
rows_blocked_declared: 0
surfaces: [backend, cli, docs]
---

# QA Report

## Scope and environment

Full gate against build `4315553db6b81a8a7ae9a8b6169c80871442f7c3` on macOS in the Run Worktree. The actor was a Supervisor or maintainer using the built CLI, its public command runner, isolated temporary Git repositories, isolated Run databases, and temporary Run Worktrees. No production Run record, user checkout state, branch, Pull Request, credential, or network service was mutated. No Pull Request is open by design: this authored gate precedes Pull Request creation, and the Spec declares no Pull Request journey.

The production-like binary was built with `go build -buildvcs=false -o /tmp/roundfix-qa-0118 ./cmd/roundfix`. Stateful reconcile and implement journeys used the public CLI runner with real command arguments, Git repositories, Run databases, Run Worktrees, stdout, stderr, and exit codes. The Implement failure-injection and selection probes used the command's existing collaborator seam; those rows record supervised evidence where a production failure cannot be safely induced.

`roundfix spec check 0118-a-task-proved-once-does-not-run-twice --strict` passed before the matrix ran. `_tasks.md` names `task_07` as the unique terminal QA node, and its only dependency, `task_06`, is completed; all preceding non-QA nodes are completed.

## Static gate

- `rtk make verify` first ran in the restricted sandbox and reached the test suite, where two unrelated Force Stop process-table tests failed because process enumeration returned `operation not permitted`. The same two tests failed with the same diagnostic on an unchanged `main` archive at `7f1fb815`, classifying the first result as environmental rather than code-caused.
- `rtk make verify` reran with process-enumeration permission and exited `0`: every Go package passed, the skill checks passed, and the CLI rebuilt.
- `rtk make verify-docs` exited `0`: docs-contract tests, repository ownership/sync contracts, corpus budget, and `roundfix spec check` all passed; Spec 0118 reported no findings.
- Focused feature command: `rtk env GOCACHE=/tmp/roundfix-qa-feature-gocache go test -count=1 -v ./internal/cli -run '^(TestCarryForward.*|TestInspectSpecCarryForwards|TestImplementCarryForward.*|TestImplementRefusesWhenCarryForwardIsAvailable|TestImplementProceedsWhenNothingIsCarriable|TestRunCommandHelp)$'` exited `0` with every listed journey and subtest passing.
- The authored `task_07` Verification exited `0` after this report closed: the newest report carried `verdict: pass`, the strict Spec check reported no findings, and `go test -count=1 ./internal/cli ./internal/spec ./internal/store` passed all three packages.

## Performed repairs

None.

## Assigned repair failures

None.

## Mechanical findings

None.

## Results

| # | Status | Provenance |
| - | --- | --- |
| QA-01 | pass | PRD stories 1 and 4; core feature 1 |
| QA-02 | pass | PRD core feature 1; Stopped parity control |
| QA-03 | pass | PRD story 4; core feature 2 |
| QA-04 | pass | PRD core features 1 and 4; proof-preservation probes |
| QA-05 | pass | PRD goal 4; corrective task 08 |
| QA-06 | pass | PRD story 2; core feature 3 |
| QA-07 | pass | PRD story 3; core feature 4 |
| QA-08 | pass | PRD story 4; core feature 5 |
| QA-09 | pass | PRD user experience; corrective task 09 |
| QA-10 | pass | PRD story 5; core feature 7 |
| QA-11 | pass | PRD core feature 6; vocabulary contract |
| QA-12 | pass | project tooling authority constraint |
| QA-13 | pass | outside acceptance evidence; ADR-0104 |
| QA-14 | pass | PRD Non-Goals and scope-creep sweep |

## Planned row details

### QA-01

- Actor / entry / surface: Supervisor; `roundfix reconcile <run-id> --carry-forward`; CLI and Git checkout.
- Steps and expected observable: carry an Unresolved Run's proved Task; expect exit 0 and the Task commit on the checkout.
- Independent confirmation / persistence: fresh Git reads confirm the commit and Task provenance after command exit.
- Evidence: `TestCarryForwardAcceptsAnUnresolvedRun/Unresolved` passed through the public reconcile runner. The command exited `0`, stderr stayed empty, HEAD advanced, the implementation file appeared, and a fresh Task-file read found `status: completed`, the source Run id, and the settlement commit.

```yaml
inputs:
  - kind: repository_path
    ref: internal/cli/carryforward.go
  - kind: repository_path
    ref: internal/cli/reconcile.go
  - kind: repository_path
    ref: internal/cli/reconcile_test.go
```

### QA-02

- Actor / entry / surface: Supervisor; the same reconcile command against a Stopped Run; CLI and Git checkout.
- Steps and expected observable: repeat QA-01 with the pre-existing accepted outcome; expect the same success and proof behavior.
- Independent confirmation / persistence: fresh Git and Task-file reads after command exit.
- Evidence: `TestCarryForwardAcceptsAnUnresolvedRun/Stopped` passed the same assertions as QA-01. The Stopped control confirms the accepted-outcome widening did not change the pre-existing success path.

```yaml
inputs:
  - kind: repository_path
    ref: internal/cli/carryforward.go
  - kind: repository_path
    ref: internal/cli/reconcile_test.go
```

### QA-03

- Actor / entry / surface: Supervisor; reconcile a Run with another terminal outcome; CLI diagnostics.
- Steps and expected observable: try a Clean control and inspect the exhaustive accepted set; expect refusal naming the actual outcome plus Stopped and Unresolved, with Failed excluded by the same membership guard.
- Independent confirmation / persistence: a fresh Run-database read confirms no outcome or checkout mutation.
- Evidence: `TestCarryForwardRefusesAnUnacceptedRunOutcomeByName` passed with a Clean Run and required the diagnostic to contain `Clean`, `Stopped`, and `Unresolved`. `carryForwardAcceptedStates` contains only Stopped and Unresolved, and both selection guards use membership in that set, so Failed and every other terminal outcome follow the same named-refusal path.

```yaml
inputs:
  - kind: repository_path
    ref: internal/cli/reconcile.go
  - kind: repository_path
    ref: internal/cli/reconcile_test.go
```

### QA-04

- Actor / entry / surface: Supervisor; reconcile Runs whose proof inputs are invalid; CLI and Git checkout.
- Steps and expected observable: moved input, mixed ready/refusing set, missing Run Worktree, and report-without-mutation probes must refuse without a partial carry.
- Independent confirmation / persistence: fresh HEAD, status, and Task-file reads confirm no mutation.
- Evidence: the focused suite passed moved-input probes for both Stopped and Unresolved, mixed ready/refusing sets for both outcomes, read-only report probes for both outcomes, and the explicit missing-Run-Worktree refusal. Each probe independently re-read HEAD, status, Task files, and implementation paths to prove no partial or hidden carry.

```yaml
inputs:
  - kind: repository_path
    ref: internal/cli/carryforward.go
  - kind: repository_path
    ref: internal/cli/reconcile_test.go
  - kind: repository_path
    ref: internal/cli/carryforward_test.go
```

### QA-05

- Actor / entry / surface: Supervisor; carry a serial Task graph; CLI and Git checkout.
- Steps and expected observable: prove later inputs against staged earlier carries; a genuine unrelated drift control must still refuse the whole set.
- Independent confirmation / persistence: fresh Git history and Task-file reads confirm dependency-order carries only.
- Evidence: both `TestCarryForwardInputsResolveAgainstStagedCarries` subtests passed. The serial graph carried task_01 then task_02 against the accumulating staged state; the genuine shared-input drift control returned Preflight refusal, named the moved PRD, left HEAD unchanged, and carried neither Task.

```yaml
inputs:
  - kind: repository_path
    ref: internal/cli/carryforward.go
  - kind: repository_path
    ref: internal/cli/carryforward_test.go
```

### QA-06

- Actor / entry / surface: Supervisor; `roundfix implement --spec <slug>`; CLI, Git, Run Database, and Agent boundary.
- Steps and expected observable: present a complete carriable prior set; expect Preflight exit 2 naming Run, Tasks, and exact recovery command, with empty stdout and no Run, Agent, Git, or database mutation.
- Independent confirmation / persistence: fresh HEAD, status, Run-list, and Agent-call reads after refusal.
- Evidence: `TestImplementRefusesWhenCarryForwardIsAvailable/a_real_prior_Run_refuses_before_another_Run_or_Task_Agent_turn_exists` passed against a real prior Unresolved Run. The public implement runner returned Preflight exit `2`, named the Run, task_01, and the exact backticked recovery command, spent zero Agent turns, retained one Run row only, and left HEAD and status unchanged. The injected zero-row cases also proved empty stdout and no Run Database creation.

```yaml
inputs:
  - kind: repository_path
    ref: internal/cli/implement.go
  - kind: repository_path
    ref: internal/cli/implement_test.go
```

### QA-07

- Actor / entry / surface: Supervisor; Implement Command with non-carriable stranded work and with an inspection failure; CLI and Run Database.
- Steps and expected observable: both probes report the reason and proceed to normal Run creation instead of refusing.
- Independent confirmation / persistence: a fresh Run-list read and recorded Agent call confirm progress.
- Evidence: `TestImplementCarryForwardRefusesOnlyWhenTheSetWouldCarry` and both `TestImplementProceedsWhenNothingIsCarriable` cases passed. A mixed set reported the refusing Task and proceeded; a moved-input set and a forced inspection error each emitted their reason, created one Run, and reached one supervised Task Agent call.

```yaml
inputs:
  - kind: repository_path
    ref: internal/cli/implement.go
  - kind: repository_path
    ref: internal/cli/implement_test.go
```

### QA-08

- Actor / entry / surface: Supervisor; Implement Command with several prior Runs; CLI diagnostic.
- Steps and expected observable: choose the largest complete carriable set, then newest creation time on a tie.
- Independent confirmation / persistence: fresh fixture Run-list and refusal text identify the selected Run and Tasks.
- Evidence: the public implement runner passed `largest_carriable_set_wins_over_recency` and `newest_Run_wins_a_carriable-count_tie`. Each refusal named only the selected Run, its Tasks, and its exact recovery command; Run count, Git state, and Agent calls remained unchanged.

```yaml
inputs:
  - kind: repository_path
    ref: internal/cli/implement.go
  - kind: repository_path
    ref: internal/cli/implement_test.go
```

### QA-09

- Actor / entry / surface: Supervisor; `roundfix reconcile --help`; public CLI help and stream/exit contract.
- Steps and expected observable: help names Stopped and Unresolved for `--carry-forward`; no flag, default, or sibling option changes.
- Independent confirmation / persistence: repeat the built command and compare the public help assertion.
- Evidence: the freshly built `/tmp/roundfix-qa-0118 reconcile --help` exited `0`, wrote 829 bytes to stdout and zero bytes to stderr, and showed `--carry-forward Hand a Stopped or Unresolved Run's settled Tasks back to the checkout`. `TestRunCommandHelp/reconcile` also passed.

```yaml
inputs:
  - kind: repository_path
    ref: internal/cli/cli.go
  - kind: repository_path
    ref: internal/cli/cli_test.go
```

### QA-10

- Actor / entry / surface: Supervisor and Agent; user guide and shipped Roundfix skill; docs surface.
- Steps and expected observable: follow both documents to the recovery command, accepted outcomes, complete-set rule, staged-carry proof, and all reconcile mutation switches.
- Independent confirmation / persistence: canonical and generated skills compare byte-identically; docs-contract tests re-read the files.
- Evidence: `rtk make verify-docs` passed the docs contracts and ownership/sync contracts. Searches found Stopped, Unresolved, `--apply`, `--discard-superseded`, `--carry-forward`, `complete candidate set`, and `staged carries` in the intended surfaces; `rtk cmp .agents/skills/roundfix/SKILL.md skills/roundfix/SKILL.md` exited `0`.

```yaml
inputs:
  - kind: repository_path
    ref: docs/user-guide/commands.md
  - kind: repository_path
    ref: .agents/skills/roundfix/SKILL.md
  - kind: repository_path
    ref: skills/roundfix/SKILL.md
```

### QA-11

- Actor / entry / surface: maintainer; glossary and vocabulary detector; docs surface.
- Steps and expected observable: Task Carry-Forward names Stopped and Unresolved, every other terminal outcome refusal, and the Reconcile Command mutation switches; the detector passes.
- Independent confirmation / persistence: fresh implementation search matches the glossary and no second term is needed.
- Evidence: the strict Spec check and `make verify-docs` vocabulary detector passed. `CONTEXT.md` defines Task Carry-Forward with Stopped and Unresolved and says every other terminal outcome is refused; implementation search found the two-value accepted set and both membership guards. The glossary audit found no additional introduced, changed, or retired term requiring a new entry.

```yaml
inputs:
  - kind: repository_path
    ref: CONTEXT.md
  - kind: repository_path
    ref: docs/specs/0118-a-task-proved-once-does-not-run-twice/_techspec.md
  - kind: repository_path
    ref: internal/cli/carryforward.go
```

### QA-12

- Actor / entry / surface: maintainer; Git commit chronology; repository tooling audit.
- Steps and expected observable: resolve authorization, each Task commit's actual paths, generated fallout, and ancestry; expect no governed path outside the bounded grant and separate prior authorization commits.
- Independent confirmation / persistence: `git diff-tree` and ancestry checks against immutable commit ids.
- Evidence: authorization commits `7e42d6bc` and `dc006772` precede both Task commits `61bb1467` and `4315553d`. `git diff-tree --no-commit-id --name-only -r` showed each authorization commit changed only the authorization record; each Task commit changed only `.agents/skills/roundfix/SKILL.md`, `skills/roundfix/SKILL.md`, and `task_06.md`. Both skill diffs stay inside the reconcile/implement guidance, and the two copies compare byte-identically. No consequent-fix declaration exists, matching the preserved mechanical skip.

```yaml
inputs:
  - kind: repository_path
    ref: docs/workflow/authorizations/2026-08-27-carry-forward-reaches-an-unresolved-run.md
  - kind: repository_path
    ref: docs/specs/0118-a-task-proved-once-does-not-run-twice/task_06.md
  - kind: repository_path
    ref: .agents/skills/roundfix/SKILL.md
  - kind: repository_path
    ref: skills/roundfix/SKILL.md
```

### QA-13

- Actor / entry / surface: Supervisor; adopted `fiscus` measurement and provenance index; outside evidence.
- Steps and expected observable: verify that five Unresolved Runs and 5h21m predate this Spec and establish the repeated-work requirement independently.
- Independent confirmation / persistence: provenance index resolves to the adopted Finding; current repository Run-branch history supplies a second independent repeated-set measurement.
- Evidence: the adopted source resolves through `references/_index.md` and records the 2026-08-07/08 `fiscus` session's five Unresolved Runs and 5h21m before this Spec existed. Independent local Run Branch logs from Spec 0098 show settlement trailers `{task_01, task_06}`, then `{task_01, task_02, task_03, task_04, task_06}`, then `{task_01, task_02, task_03, task_04, task_05, task_06, task_07}`, directly demonstrating repeated proved Tasks across terminal Runs.

```yaml
inputs:
  - kind: external_repository
    ref: fiscus session 2026-08-07/08
  - kind: repository_path
    ref: docs/specs/0118-a-task-proved-once-does-not-run-twice/references/_index.md
  - kind: repository_path
    ref: docs/specs/0118-a-task-proved-once-does-not-run-twice/references/2026-08-12-five-unresolved-runs-to-deliver-one-spec.md
```

### QA-14

- Actor / entry / surface: maintainer; CLI diff, tests, and docs; scope sweep.
- Steps and expected observable: no automatic carry, Clean-only integration unchanged, Failed remains refused, one Run per invocation, no force switch, and no Settle or discard behavior change.
- Independent confirmation / persistence: changed-path and focused symbol/test sweeps confirm the exclusions after a fresh build.
- Evidence: the complete changed-path and CLI diff is confined to carry-forward inspection/reconciliation, Implement Preflight, help, tests, docs, and the authorized skill. The exhaustive accepted set excludes Failed; no force flag exists; carry-forward still accepts exactly one Run id; the command never performs carry-forward from Implement. Four adjacent discard/read-only reconcile canaries passed, and the full `make verify` pass covers unchanged Clean-only integration and Settle behavior.

```yaml
inputs:
  - kind: repository_path
    ref: internal/cli/**
  - kind: repository_path
    ref: docs/user-guide/commands.md
  - kind: repository_path
    ref: docs/specs/0118-a-task-proved-once-does-not-run-twice/_prd.md
```

## Mechanical skips

| Detector | Missing artifact |
| --- | --- |
| consequent-fix commit order | consequent-fix declaration |
| QA Report structure | QA Report |
| QA evidence paths | QA Report |

These are preserved mechanical-stage skips, not skipped QA rows. The mechanical stage ran before this seeded report had an agent-authored structure or evidence paths; the completed report below resolves those two report artifacts for final verification. No consequent-fix declaration exists, so that detector remains inapplicable.

## Findings

None. The restricted-sandbox process-enumeration denial was environmental and reproduced unchanged on `main`; the required full verification passed when rerun with the necessary permission.

## Blocked and skipped

No QA row is blocked or skipped. No Pull Request journey exists in this Spec, and the prompt confirms that no Pull Request is open; the authored QA gate runs before Pull Request creation under the repository workflow.

## Coverage

- User stories: 5/5 passed.
- Core features: 7/7 passed.
- Planned rows: 14 passed, 0 failed, 0 blocked, 0 skipped, 0 pending.
- Blocked counts: environment 0, finding 0, declared 0.
- High-risk probes: moved declared input, mixed ready/refusing set, missing Run Worktree, read-only report, genuine serial-graph drift, complete-set versus individually carriable distinction, inspection failure, largest-set selection, newest-tie selection, and repeated help invocation.
- Non-Goals: all eight checked; no automatic carry, integration-outcome widening, Failed acceptance, multi-Run carry, proof bypass/force flag, discard behavior change, Settle change, Run Database recreation, or release work shipped.
- Surfaces: backend, CLI, and docs covered. Frontend, TUI, data migration, HTTP, authentication, network, and live-service sweeps are not declared surfaces for this Spec.

## Final verdict

Pass: all 14 planned rows passed on build `4315553db6b81a8a7ae9a8b6169c80871442f7c3`, with zero blocked, skipped, pending, or finding rows and zero findings at every impact tier.
