---
spec: 0103-a-suite-that-leaks-nothing
date: 2026-08-14
build: 7dade8d856a1e672a19584865e105a23c1021301
status: closed
verdict: fail
rows_blocked_environment: 4
rows_blocked_finding: 0
rows_blocked_declared: 0
surfaces: [backend, infra]
---

# QA Report

## Scope and environment

Full rerun of the authored terminal gate against commit
`7dade8d856a1e672a19584865e105a23c1021301` in the Run Worktree on branch
`roundfix/run-run_20260814T231441Z_0915098b591a82e2`. The Pull Request fact is
`none open`; the Spec declares no Pull Request journey, and this per-Run branch
must never be pushed. The intended actors are a maintainer running the Go and
repository gates and an operator using the built Roundfix CLI. Scratch state and
binaries stay under `/tmp`; only this report may be written under the Spec.

`task_09` is the manifest's unique terminal `qa` node. Its dependencies
`task_02`, `task_05`, `task_07`, `task_08`, `task_10`, `task_11`, and `task_12`
are completed. This is a full gate, not a risk-based partial run. The PRD has no
`Unreachable Acceptance` declaration.

The mandatory precondition passed:

```text
$ rtk roundfix spec check 0103-a-suite-that-leaks-nothing --strict
Spec 0103-a-suite-that-leaks-nothing
No findings.
Skipped:
  SC-REF-UNRESOLVED: missing docs/specs/0103-a-suite-that-leaks-nothing/references/_index.md
```

The named skip means that detector did not run; it does not replace any QA row.

## Static gate

`rtk make verify` ran the repository's authoritative gate and exited `2` at
`make test`. The former `TestFixtureBinarySurvivesConcurrentExec` failure is
gone, but three other ACPX fixture journeys failed under the repository-wide
load:

```text
--- FAIL: TestACPXRunCancelsPromptCooperatively
    spawned fixture "fake acpx prompt fixture" exited with exit status unavailable
    before prompt start milestone
--- FAIL: TestACPXProbeSelectionRejectionClosesDisposableSession
    expected SelectionRejectedError, got AdapterLineageError
--- FAIL: TestACPXProbeValidatesSelectionWithDisposableSession
    close disposable Agent Session: acpx infrastructure error after exit code -1
FAIL roundfix/internal/agent
```

The exact three-test selection passed five fresh repetitions, and the complete
`internal/agent` package passed separately at `-parallel 16`, including the
fixture stress and all three failed journeys. The current delta against the
previous report head changes only the stress sizing/identity assertion in that
file. The smallest observed reproduction remains the repository-wide test
sweep: real helper children return killed/incomplete evidence only under global
package load. This is code-caused finding F-01 because Task 12 explicitly
requires the stress repair to pass inside `make verify`; it affects R01 and R02.

The same gate also repeated the two environment-caused Force Stop integration
failures documented in R08: macOS denied `kern.proc.all` before owned-tree
enumeration. No other package failed.

## Mechanical findings

None.

## Results

| # | Story / criterion / sweep | Actor and surface | Status | Evidence |
| - | --- | --- | --- | --- |
| R01 | Stories 1–2: the authoritative suite completes without changing the repository or leaving a test process | Maintainer · infra | fail | F-01: `make verify` exited 2 after three ACPX fixture journeys failed under repository-wide load; host process confirmation was also denied. See **Static gate** and **F-01**. |
| R02 | Story 3 / Core Feature 4: compiled fixtures survive dense concurrent execution with no written executable, empty probe, or `ETXTBSY` | Maintainer · infra | fail | F-01: the dedicated stress and focused regressions pass, but three real ACPX fixture journeys fail only inside the authoritative loaded suite. See **R02** and **F-01**. |
| R03 | Story 3 / Core Feature 3: an already-dead fixture fails in under one second and names its fixture and exit status without changing the live-child budget | Maintainer · infra | pass | Dead fixture completed in 0.34s; asserted diagnostic names the fixture and exit 23; budget remains 90s. See **R03**. |
| R04 | Story 1 / Core Feature 1: the suite guard attributes created, modified, and removed repository paths while allowing isolated and ignored writes | Maintainer · infra | pass | Fresh subprocesses named all three changes, allowed isolated/ignored writes, and emitted measured cost. See **R04**. |
| R05 | Story 1: every process-spawning package installs the per-package guard and the repository contract names a missing installation | Maintainer · infra | pass | Every test-bearing internal package emitted the named pass; the synthetic missing package was named. See **R05**. |
| R06 | Story 2 / Core Feature 2: detach tests preserve the survival assertion but kill and reap every proven survivor, including a child that ignores the sentinel | Maintainer · infra | blocked (environment: host process-table enumeration denied) | All detach and teardown flows passed; `pgrep` could not access sandboxed `sysmond`. Equivalent evidence: subprocess teardown proves PID absence. See **R06**. |
| R07 | Story 4 / Core Feature 5: Doctor reports found, empty, and partial Process Residue without changing exit status or writing a Run record | Operator · backend CLI | blocked (environment: Go build cache denied outside workspace) | Fresh build was denied before compilation; five current-tree command scenarios pass and prior built-CLI evidence is unchanged. See **R07**. |
| R08 | Core Feature 6: Force Stop fails and names a surviving descendant, preserves clean-tree output, and leaves an unrelated process untouched | Operator · backend CLI | blocked (environment: macOS `kern.proc.all` denied) | Command-level tree cases and the real unrelated-process canary passed; two host integrations were denied before enumeration. See **R08**. |
| R09 | Story 5 / Core Feature 8: the mechanical gate refuses a binary and Gitlink, accepts readable evidence, and skips an absent evidence directory | Maintainer · backend CLI | pass | Four independent detector cases passed and the public strict checker reported no finding. See **R09**. |
| R10 | Core Feature 7: both declared regeneration commands complete under the guard; undeclared and wrong-command writes remain violations | Maintainer · infra | pass | Both sanctioned commands and the frozen-boundary contract passed with no diff; wrong/absent declarations remained violations. See **R10**. |
| R11 | Outside evidence: the repair rests on upstream golang/go#22315 and the independent 2026-08-06 measurement of four survivors consuming 2h40m CPU | Maintainer · backend/docs | blocked (environment: GitHub unreachable) | The pre-Spec process measurement resolves; both web and `gh` failed to obtain golang/go#22315, so the upstream half has no fresh equivalent evidence. See **R11**. |
| R12 | Non-Goals: no retry, sleep, deadline widening, concurrency, or survival-property weakening was introduced | Maintainer · infra | pass | Commit/source audit found no shortcut or suite-parallelism change; the 90s budget and survival assertion remain. See **R12**. |
| R13 | Non-Goals: no unrelated process is killed and garbage collection or retention behavior is unchanged | Operator · backend | pass | Real unrelated-process canary passed; no GC or retention path changed. See **R13**. |
| R14 | Commit-dependent tooling audit: no Task mutated protected repository tooling, and sanctioned derived output remains deterministic fallout only | Maintainer · infra | pass | Every implementation commit was audited with `git diff-tree`; no protected tooling file or pin changed. See **R14**. |
| R15 | Vocabulary closure: Process Residue is present in the glossary with the emitted Doctor semantics | Maintainer · docs/backend | pass | Strict checking passed; `CONTEXT.md` and Doctor source agree on the reporting-only term. See **R15**. |
| R16 | Report and evidence hygiene: the QA tree contains no committed binary, Gitlink, scratch repository, or unresolved repository evidence path | Maintainer · infra | pass | Tracked QA entries are mode-100644 Markdown; this report is UTF-8 text and the only untracked path. See **R16**. |

## Mechanical skips

| Detector | Missing artifact |
| --- | --- |
| authorization bounded paths | tooling authorization |
| consequent-fix commit order | consequent-fix declaration |
| QA evidence paths | docs/specs/0103-a-suite-that-leaks-nothing/qa/evidence |

## Detailed evidence

### R01

```yaml
inputs:
  - kind: repository_path
    ref: Makefile
  - kind: repository_path
    ref: internal/**
  - kind: live_service
    ref: host process table
```

- Entry point: repository root; run `rtk make verify` as a maintainer.
- Expected: exit zero, unchanged tracked repository state, and no surviving test process.
- Independent confirmation/persistence: fresh Git status and process-table read after exit.
- Evidence path: this row in this report; command logs remain scratch under `/tmp`.

- Observed: `make verify` exited 2 with F-01's three loaded ACPX fixture
  failures plus R08's two environment-denied integrations. A fresh Git status
  after the gate showed no tracked change; only this untracked seeded report is
  present. The independent host process read was denied as recorded in R06.
  The expected authoritative-suite completion therefore did not occur.

### R02

```yaml
inputs:
  - kind: repository_path
    ref: internal/agent/**
  - kind: repository_path
    ref: internal/cli/baseline_release_gate_test.go
  - kind: repository_path
    ref: internal/testfixture/**
```

- Entry point: Go test runner at gate parallelism plus the existing adapter and formatter journeys.
- Expected: concurrent non-empty fixture probes with no exec error; deterministic refusal of a written executable; unchanged adapter/formatter behavior.
- Independent confirmation/persistence: fresh focused test processes at two machine parallelism settings.
- Evidence path: this row in this report; focused logs remain scratch under `/tmp`.

- Observed: `go test -parallel 16` passed the stress in 0.03s; fresh
  `GOMAXPROCS=2` and `GOMAXPROCS=8` runs each passed twice. The existing ACPX
  selection/package-version journeys and standard TypeScript formatter journey
  passed in fresh test processes. The stress derives workers from
  `runtime.GOMAXPROCS(0)`, keeps a two-worker floor, and rejects any fixture for
  which `os.SameFile` does not prove hard-link identity with the compiled test
  binary; every execution still rejects exec errors and empty output. No retry,
  sleep, or skip occurs in the stress block.
- Divergence: `make verify` nevertheless failed three other real ACPX fixture
  journeys under repository-wide load. The exact trio passed five focused
  repetitions and the complete agent package passed independently, which
  isolates the failure to global suite load but does not satisfy Task 12's
  authoritative-gate acceptance criterion.

### R03

```yaml
inputs:
  - kind: repository_path
    ref: internal/agent/acpx_integration_test.go
  - kind: repository_path
    ref: internal/agent/acpx_runner_test.go
```

- Entry point: dead-fixture regression through the Go test binary.
- Expected: pass in under one second while asserting fixture name and `exit status 23`.
- Independent confirmation/persistence: inspect the 90-second live-child upper bound and absence of a retry.
- Evidence path: this row in this report.

- Observed: `TestSpawnedFixtureDeathFailsFast` passed in 0.34s. Its outer
  process accepts the behavior only when output contains `immediate-exit acpx
  fixture` and `exit status 23`; current source retains
  `agentWaitBudget = 90 * time.Second` and no retry path was introduced.

### R04

```yaml
inputs:
  - kind: repository_path
    ref: internal/suiteguard/**
```

- Entry point: suiteguard's real guarded `TestMain` subprocesses.
- Expected: created/modified/removed paths are named; isolated and ignored writes pass; cost is emitted.
- Independent confirmation/persistence: each direction runs in a fresh subprocess with a new fingerprint.
- Evidence path: this row in this report.

- Observed: all three top-level directions passed. The violating subprocess
  returned nonzero and named `created: created.txt`, `modified: modified.txt`,
  and `removed: removed.txt`. Isolated and ignored writes returned zero. The
  live clean fingerprint measured 4,903 paths and emitted separate before/after
  durations.

### R05

```yaml
inputs:
  - kind: repository_path
    ref: internal/**/*_test.go
  - kind: repository_path
    ref: internal/suiteguardcontract/**
```

- Entry point: repository-contract-tagged Go test sweep across `./internal/...`.
- Expected: every test-bearing package runs the named contract and the synthetic unguarded package is named.
- Independent confirmation/persistence: compare emitted package count with a fresh source inventory.
- Evidence path: this row in this report.

- Observed: the `repocontract` sweep exited zero with a named
  `TestEverySpawningPackageInstallsTheSuiteGuard` pass in every test-bearing
  internal package. The suiteguard package additionally passed
  `names_an_unenumerated_and_unguarded_spawning_package` and the live
  `repository_installations` inventory; no package emitted `no tests to run`.

### R06

```yaml
inputs:
  - kind: repository_path
    ref: internal/cli/detach_test.go
  - kind: live_service
    ref: host process table
```

- Entry point: detach and non-cooperative teardown tests through the Go test binary.
- Expected: survival is observed before cleanup; every recorded child is killed, reaped, and absent afterward.
- Independent confirmation/persistence: fresh host process-table query after the test process exits.
- Evidence path: this row in this report.

- Observed: all eight detach command/review cases and
  `TestDetachedChildIsTerminatedAtTeardown` passed. The non-cooperative case
  records its PID, proves survival, kills and waits for it, and fails unless the
  PID becomes absent. The independent host query was unavailable: `pgrep -af
  'roundfix.*test|cli.test|agent.test|store.test'` exited 3 with `sysmon request
  failed: sysmond service not found` and `Cannot get process list`.
- Unblocking action: repeat the post-test process query in a full-access macOS
  session that permits host process-table enumeration. Equivalent observed
  evidence is the subprocess's own kill, reap, and PID-absence assertion.

### R07

```yaml
inputs:
  - kind: repository_path
    ref: internal/cli/doctor.go
  - kind: repository_path
    ref: internal/cli/doctor_test.go
  - kind: repository_path
    ref: internal/store/process*.go
  - kind: live_service
    ref: host process table and local Run Database
```

- Entry point: built `roundfix doctor` plus command-facing scenario tests.
- Expected: found/empty/partial wording, unchanged residue exit semantics, and no Run mutation.
- Independent confirmation/persistence: rerun the binary and compare Run listings around the read-only scenario.
- Evidence path: this row in this report; the attempted `/private/tmp` binary
  target was never created.

- Observed: `go build -buildvcs=false -o /private/tmp/roundfix-0103-qa03
  ./cmd/roundfix` exited 1 before compilation because the default Go build cache
  under `/Users/marcio/Library/Caches/go-build` returned `operation not
  permitted`. Per the sandbox contract the denied build was not retried with a
  relocated cache. The installed `roundfix doctor` was reachable but is an
  older parity-deviating build: it exited 1 on existing profile/signature checks
  and did not contain the new residue line.
- Equivalent evidence: all five current-tree command-facing cases passed,
  covering PID/age/CPU/Run output, live-Run exclusion, explicit empty and
  partial states, unchanged `exitOK`, and byte-equivalent Run listings. The
  prior report's built CLI printed `residue: ok (no process residue found)`, and
  `git diff 50044869..HEAD` proves no Doctor or process-reader input changed
  after that observed build.
- Unblocking action: build and run this head in a session where Go may access
  its configured cache, or where that cache is already configured inside a
  writable boundary before QA begins.

### R08

```yaml
inputs:
  - kind: repository_path
    ref: internal/cli/cli.go
  - kind: repository_path
    ref: internal/cli/cli_test.go
  - kind: repository_path
    ref: internal/store/process*.go
  - kind: live_service
    ref: host process table
```

- Entry point: Force Stop through the CLI dispatcher and the real owner-process controller.
- Expected: surviving descendant keeps the Run Active and is named; clean tree stays byte-compatible; unrelated process remains alive.
- Independent confirmation/persistence: reload Run state and re-read the unrelated PID after termination.
- Evidence path: this row in this report.

- Observed: `TestForceStopProvesTheTree` passed the surviving-descendant
  failure/state case, and `TestForceStopProvesTheTreeWhenTreeExits` passed the
  byte-compatible success/state case. The real
  `TestOwnerProcessControllerTerminateTreeLeavesUnrelatedProcessRunning`
  canary passed. The broader Force Stop sweep failed only in
  `TestRunForceStopOwnerProcessIntegrationProvesExitBeforeStoreCompletion` and
  `TestRunForceStopLegacyRunWithoutOwnerIdentityStillStopsOwner`; both stopped
  before termination because `kern.proc.all` returned `operation not
  permitted` while enumerating the owned tree.
- Unblocking action: rerun the two named host integrations in a full-access
  macOS session. Equivalent observed evidence comprises both public CLI
  directions, fresh Run-state reloads embedded in them, and the real
  unrelated-process canary.

### R09

```yaml
inputs:
  - kind: repository_path
    ref: internal/speccheck/mechanical.go
  - kind: repository_path
    ref: internal/speccheck/mechanical_test.go
  - kind: repository_path
    ref: docs/specs/0103-a-suite-that-leaks-nothing/qa/**
```

- Entry point: four top-level evidence-refusal tests plus public `roundfix spec check --strict`.
- Expected: binary and Gitlink refused by path/kind, JSON accepted, absent evidence directory skipped.
- Independent confirmation/persistence: inspect tracked Git modes and rerun strict checking after closure.
- Evidence path: this row in this report.

- Observed: binary, Gitlink, ordinary JSON, and absent-directory cases each
  passed as an independent top-level test. The public strict Spec check returned
  `No findings`; its missing-reference-index detector skip remains preserved
  because that detector did not run.

### R10

```yaml
inputs:
  - kind: repository_path
    ref: Makefile
  - kind: repository_path
    ref: docs/workflow/authorizations/**
  - kind: repository_path
    ref: internal/baseline/**
  - kind: repository_path
    ref: internal/suiteguard/**
  - kind: repository_path
    ref: internal/suiteguardcontract/**
```

- Entry point: sanctioned regeneration commands and guard subprocess directions.
- Expected: both declared commands exit zero under the guard; wrong/absent declaration still names violations; bytes are deterministic.
- Independent confirmation/persistence: rerun commands and compare repository bytes after each execution.
- Evidence path: this row in this report; command logs remain scratch under `/tmp`.

- Observed: `make baseline-digests` exited zero and reported `changed:false`
  with canonical artifacts already current. The exact
  `TestBaselinePlanCharacterization -update-baseline-plan-characterization`
  command regenerated all five goldens and exited zero; the repository retained
  no diff. `TestDeclaredStepRegenerationAndFrozenBoundaries` passed its six
  dedicated, failure, sanctioned, and frozen cases. The three suiteguard
  subprocess directions passed: the in-process declaration allowed its exact
  output, while wrong and absent declarations each named `created:
  declared.txt`. A source scan found no `ps`, `kern.proc.all`, or `pgrep` token
  under either guard package, and Git found no Makefile or derived-output delta.

### R11

```yaml
inputs:
  - kind: external_repository
    ref: https://github.com/golang/go/issues/22315
  - kind: repository_path
    ref: docs/findings/2026-08-10-a-fake-adapter-goes-silent-under-a-dense-start.md
  - kind: repository_path
    ref: docs/findings/2026-08-06-the-detach-tests-leak-the-process-they-prove-survives.md
```

- Entry point: upstream issue and pre-Spec repository Findings.
- Expected: independent sources support the `ETXTBSY` race and four survivors/2h40m measurement.
- Independent confirmation/persistence: fresh reads resolve outside this Spec and retain dated details.
- Evidence path: source URL and repository paths named above and in this row.

- Observed: the independent pre-Spec Finding
  `docs/findings/2026-08-06-the-detach-tests-leak-the-process-they-prove-survives.md`
  resolves and records PIDs 33864, 55351, 54551, and 87902, ages from 1d22h to
  3d14h, and 2h40m total CPU. The separate 2026-08-10 Finding records the
  write-then-execute `ETXTBSY` signature before this Spec.
- Block: a fresh web observation of
  `https://github.com/golang/go/issues/22315` returned no page content, and
  `gh issue view 22315 --repo golang/go` exited 1 with `error connecting to
  api.github.com`. Because an external-repository input is never carriable and
  the Spec's prior report cannot replace its source, the upstream half has no
  equivalent current evidence.
- Unblocking action: open golang/go#22315 in a network-enabled QA session and
  confirm that it documents the concurrent write/fork/exec `ETXTBSY` hazard.

### R12

```yaml
inputs:
  - kind: repository_path
    ref: internal/agent/**
  - kind: repository_path
    ref: internal/cli/detach_test.go
  - kind: repository_path
    ref: .git
```

- Entry point: implementation-commit and source audit correlated with R02, R03, and R06.
- Expected: no retry, sleep, skip, deadline widening, concurrency change, or weakened survival assertion.
- Independent confirmation/persistence: commit-level diffs and current-source scans agree.
- Evidence path: this row in this report.

- Observed: the prior implementation audit remains applicable, and the only
  code delta after the previous report head is Task 12's stress sizing and
  hard-link identity assertion. It adds no retry, sleep, skip, or deadline and
  leaves `agentWaitBudget` at 90 seconds. The repository's `-parallel 16`
  contract is unchanged. The detach sweep still proves each child survives its
  caller before non-cooperative teardown.

### R13

```yaml
inputs:
  - kind: repository_path
    ref: internal/cli/**
  - kind: repository_path
    ref: internal/store/**
  - kind: repository_path
    ref: .git
  - kind: live_service
    ref: unrelated-process canary
```

- Entry point: real lineage canary and implementation changed-path audit.
- Expected: unrelated PID survives; no garbage-collection or retention behavior changed.
- Independent confirmation/persistence: second process read and commit-level path inspection.
- Evidence path: this row in this report.

- Observed: the real
  `TestOwnerProcessControllerTerminateTreeLeavesUnrelatedProcessRunning` canary
  passed and re-read the unrelated PID after owned-tree termination. The
  base-to-head path audit contains no garbage-collection or retention
  implementation path; the Spec changes process inspection/termination only.

### R14

```yaml
inputs:
  - kind: repository_path
    ref: .git
  - kind: repository_path
    ref: docs/specs/0103-a-suite-that-leaks-nothing/task_*.md
```

- Entry point: `git diff-tree` for every implementation/corrective commit plus current delta.
- Expected: no protected tooling path or version pin changed; no tooling authorization row is applicable.
- Independent confirmation/persistence: base-to-head name-status and per-commit path lists agree.
- Evidence path: this row in this report.

- Observed: `git diff-tree --no-commit-id --name-only -r` was executed for all
  eleven implementation/corrective commits: `3925cbce`, `96423c2b`, `298acc85`,
  `ddb7a6bd`, `646bbd98`, `a9a4ca00`, `bb9cb40d`, `8f074d05`, `de3a80ad`,
  `db5c57fa`, and `7dade8d8`. Their actual paths are Go source/tests and their
  assigned Task files. None is a linter, formatter, test-runner/build
  configuration, script, ignore file, plugin declaration, dependency manifest,
  or version pin. `make baseline-digests` and plan characterization left no
  derived-output delta; the current worktree delta is only this report.

### R15

```yaml
inputs:
  - kind: repository_path
    ref: CONTEXT.md
  - kind: repository_path
    ref: internal/cli/doctor.go
  - kind: repository_path
    ref: docs/specs/0103-a-suite-that-leaks-nothing/_techspec.md
```

- Entry point: glossary read and built Doctor output.
- Expected: Process Residue is defined with the same reporting-only semantics the CLI emits.
- Independent confirmation/persistence: strict checker and fresh CLI output agree.
- Evidence path: this row in this report.

- Observed: `CONTEXT.md` defines Process Residue as a Roundfix-started process
  with no live owning Run and states that Doctor reports age, CPU, and
  originating Run without settling or creating a Run record. `doctor.go`
  renders `no process residue found` for the empty state and found/partial
  results otherwise. The strict Spec check returned no vocabulary finding.

### R16

```yaml
inputs:
  - kind: repository_path
    ref: docs/specs/0103-a-suite-that-leaks-nothing/qa/**
  - kind: repository_path
    ref: .git
```

- Entry point: QA-tree Git-mode/file-type audit after every other row settles.
- Expected: no binary, Gitlink, scratch repository, missing evidence reference, or unowned change.
- Independent confirmation/persistence: public strict check rerun after the report closes.
- Evidence path: this report itself.

- Observed: `git ls-files -s` reports each committed QA entry as mode `100644`;
  no mode `160000` entry exists. `file` identifies this report as UTF-8 text,
  and the QA tree contains only four Markdown reports with no `evidence/`
  directory, binary, or nested repository. `git status --short` reports only
  this seeded report as untracked. Every repository evidence path named in a
  terminal row resolves; R11 separately and honestly records its unreachable
  external source.

## Findings

### F-01 — ACPX fixture family still fails under repository-wide load

- Impact: **Blocks-Completion**. A maintainer cannot complete the authoritative
  gate, and fixture-dependent tests return misleading adapter, close, or
  milestone failures instead of their intended assertions.
- History: repeated dense-load family from F-01 in
  `qa-report-2026-08-14-02.md`; the original dedicated stress no longer fails,
  but the same loaded suite now kills or truncates three real ACPX fixture
  journeys.
- Expected: `rtk make verify` exits zero, with compiled fixtures surviving the
  suite's real spawn density.
- Actual: the command exits 2 after a prompt fixture dies before its milestone,
  a rejection fixture returns unproven adapter lineage, and a disposable
  session close returns exit code -1. The exact trio passes 5/5 in isolation,
  and the complete agent package passes separately at `-parallel 16`.
- Reproduction: from repository root, run `rtk make verify` at build
  `7dade8d856a1e672a19584865e105a23c1021301`.
- Evidence: **Static gate**, R01, and R02 in this report.
- Affected rows: R01 and R02.

## Blocked and skipped

- R06 is blocked by sandboxed host process-table enumeration. A full-access
  macOS session must run the independent post-teardown query; the subprocess's
  kill/reap/PID-absence assertion is equivalent observed evidence.
- R08 is blocked by macOS `kern.proc.all` denial in two host integrations. A
  full-access macOS session must rerun those cases; the CLI state/output cases
  and real unrelated-process canary are equivalent observed evidence.
- R07 is blocked because a fresh current-head CLI build could not access Go's
  configured cache. The five current-tree command scenarios and unchanged prior
  built-CLI observation are equivalent evidence; a full-access build must close
  the parity gap.
- R11 is blocked because GitHub was unreachable through both available read
  paths. The independent process measurement resolves locally, but the upstream
  issue needs a fresh network-enabled observation and has no credited equivalent.
- The mechanical detector skips above are preserved exactly as seeded.

## Coverage

All 16 planned rows are terminal: 10 pass, 2 fail, and 4 are environment-
blocked; no row is finding-blocked, declared-blocked, skipped, or pending. All
five user stories were exercised: one is fully satisfied, three contain a
failed authoritative journey, and one is environment-blocked. Of eight core
features, four pass, one fails under repository-wide load, and three retain an
environment-blocked independent confirmation. All five Non-Goals pass their
scope-creep checks. The outside-evidence row resolves the pre-Spec process
measurement but is blocked on fresh access to its upstream issue.

High-risk probes covered global versus focused spawn load, two explicit
`GOMAXPROCS` settings, immediate child death, non-cooperative teardown, wrong
and absent regeneration declarations, unreadable process tables, surviving
descendants, clean process trees, and an unrelated-process canary. There is no
frontend or TUI surface in this Spec.

Counts: `rows_blocked_environment: 4`, `rows_blocked_finding: 0`, and
`rows_blocked_declared: 0`. Finding impacts: one **Blocks-Completion**; zero
Data-Loss, Trust-Damage, Friction, or Cosmetic findings.

## Final verdict

**Fail.** Fix F-01 so real ACPX fixtures survive the repository-wide
`make verify` load, then rerun the full gate in a network-enabled, full-access
session that can build the current CLI, read the macOS process table, and
re-observe golang/go#22315; Pull Request preparation remains blocked.
