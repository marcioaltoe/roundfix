---
spec: 0103-a-suite-that-leaks-nothing
date: 2026-08-14
build: e266055d8ccdfa1972cfca4bbc2f1bcff72af551
status: closed
verdict: fail
rows_blocked_environment: 2
rows_blocked_finding: 0
rows_blocked_declared: 0
surfaces: [backend, infra]
---

# QA report — A suite that leaks nothing

## Scope and environment

Full rerun of the authored terminal gate against commit
`e266055d8ccdfa1972cfca4bbc2f1bcff72af551` in the Run Worktree on branch
`roundfix/run-run_20260814T225040Z_fa7178b18c2de202`. The Pull Request fact is
`none open`; the Spec declares no Pull Request journey, and this per-Run branch
must never be pushed. The intended actors are a maintainer running the Go and
repository gates and an operator using the built Roundfix CLI. Scratch state and
binaries stay under `/tmp`; only this report may be written under the Spec.

`task_09` is the manifest's unique terminal `qa` node. Its dependencies
`task_02`, `task_05`, `task_07`, `task_08`, and `task_10` are completed. This is
a full gate, not a risk-based partial run.

The mandatory precondition passed:

```text
$ rtk roundfix spec check 0103-a-suite-that-leaks-nothing --strict
Spec 0103-a-suite-that-leaks-nothing
No findings.
Skipped:
  SC-REF-UNRESOLVED: missing docs/specs/0103-a-suite-that-leaks-nothing/references/_index.md
```

The named skip means that detector did not run; it does not replace any QA row.

## Static gate

`rtk make verify` ran the repository's authoritative gate and exited `2` at
`make test`. Twenty-four packages passed; `internal/agent` and `internal/cli`
failed:

```text
go test -parallel 16 ./...
--- FAIL: TestFixtureBinarySurvivesConcurrentExec (2.20s)
    acpx_runner_test.go:104: exec worker 15 execution 2: signal: killed:
FAIL roundfix/internal/agent 8.018s

--- FAIL: TestRunForceStopOwnerProcessIntegrationProvesExitBeforeStoreCompletion
    ... read process table for non-session owner: operation not permitted
--- FAIL: TestRunForceStopLegacyRunWithoutOwnerIdentityStillStopsOwner
    ... read process table for non-session owner: operation not permitted
FAIL roundfix/internal/cli 107.167s
make: *** [test] Error 1
```

The fixture failure is code-caused for this gate: the acceptance stress passed
when isolated, including three fresh repetitions in 0.41–0.44s, but did not
survive the repository's actual `-parallel 16` load. Finding F-01 affects R01
and R02.

The two Force Stop integration failures are environment-caused. A focused rerun
reproduced macOS `kern.proc.all` returning `operation not permitted`, and a
direct `pgrep` confirmation failed with `sysmond service not found`. The
command-level tree cases and the real unrelated-process controller canary still
ran; R08 records those as equivalent evidence. No other row depends on the
denied host-wide table.

## Mechanical findings

None.

## Results

| # | Story / criterion / sweep | Actor and surface | Status | Evidence |
| - | --- | --- | --- | --- |
| R01 | Stories 1–2: the authoritative suite completes without changing the repository or leaving a test process | Maintainer · infra | fail | F-01: `make verify` exited 2 after the fixture stress child was killed; host process-table confirmation was also denied. See **Static gate** and **F-01**. |
| R02 | Story 3 / Core Feature 4: compiled fixtures survive dense concurrent execution with no written executable, empty probe, or `ETXTBSY` | Maintainer · infra | fail | F-01: isolated stress and adapter/formatter regressions passed, but the same stress failed under the authoritative suite's dense load. See **R02**. |
| R03 | Story 3 / Core Feature 3: an already-dead fixture fails in under one second and names its fixture and exit status without changing the live-child budget | Maintainer · infra | pass | `TestSpawnedFixtureDeathFailsFast` passed in 0.35s; source asserts the fixture name and `exit status 23`; budget remains 90s. See **R03**. |
| R04 | Story 1 / Core Feature 1: the suite guard attributes created, modified, and removed repository paths while allowing isolated and ignored writes | Maintainer · infra | pass | Guard subprocess named all three changes, passed isolated and ignored writes, and emitted measured cost. See **R04**. |
| R05 | Story 1: every process-spawning package installs the per-package guard and the repository contract names a missing installation | Maintainer · infra | pass | All 26 test-bearing internal packages emitted the named contract pass; the synthetic missing package was named. See **R05**. |
| R06 | Story 2 / Core Feature 2: detach tests preserve the survival assertion but kill and reap every proven survivor, including a child that ignores the sentinel | Maintainer · infra | blocked (environment: host process-table enumeration denied) | All detach and teardown flows passed; `pgrep` could not obtain the sandboxed process list. Equivalent evidence: the subprocess teardown itself proves PID absence. See **R06**. |
| R07 | Story 4 / Core Feature 5: Doctor reports found, empty, and partial Process Residue without changing exit status or writing a Run record | Operator · backend CLI | pass | Built CLI printed `residue: ok (no process residue found)`; five command-facing cases passed found/live/empty/partial/read-only behavior. See **R07**. |
| R08 | Core Feature 6: Force Stop fails and names a surviving descendant, preserves clean-tree output, and leaves an unrelated process untouched | Operator · backend CLI | blocked (environment: macOS `kern.proc.all` denied) | Core command cases and real unrelated-process canary passed; two host-level Force Stop integrations were denied before enumeration. See **R08**. |
| R09 | Story 5 / Core Feature 8: the mechanical gate refuses a binary and Gitlink, accepts readable evidence, and skips an absent evidence directory | Maintainer · backend CLI | pass | Four independent top-level cases passed; the strict Spec check reported no finding. See **R09**. |
| R10 | Core Feature 7: both declared regeneration commands complete under the guard; undeclared and wrong-command writes remain violations | Maintainer · infra | fail | F-02: plan characterization and all guard directions passed, but `make baseline-digests` was refused for its own two outputs and exited 2. See **R10** and **F-02**. |
| R11 | Outside evidence: the repair rests on upstream golang/go#22315 and the independent 2026-08-06 measurement of four survivors consuming 2h40m CPU | Maintainer · backend/docs | pass | Fresh upstream observation and the pre-Spec Finding both resolve and support the two claims. See **R11**. |
| R12 | Non-Goals: no retry, sleep, deadline widening, concurrency, or survival-property weakening was introduced | Maintainer · infra | pass | Commit/source audit found no retry, parallelism, or 90s-budget change; survival remains asserted. See **R12**. |
| R13 | Non-Goals: no unrelated process is killed and garbage collection or retention behavior is unchanged | Operator · backend | pass | Real unrelated-process canary passed; no GC or retention path changed. See **R13**. |
| R14 | Commit-dependent tooling audit: no Task mutated protected repository tooling, and sanctioned derived output remains deterministic fallout only | Maintainer · infra | pass | Every Task commit's actual paths were audited with `git diff-tree`; no protected tooling file or pin changed. See **R14**. |
| R15 | Vocabulary closure: Process Residue is present in the glossary with the emitted Doctor semantics | Maintainer · docs/backend | pass | Strict Spec check passed; `CONTEXT.md` and Doctor agree on the reporting-only term. See **R15**. |
| R16 | Report and evidence hygiene: the QA tree contains no committed binary, Gitlink, scratch repository, or unresolved evidence path | Maintainer · infra | pass | Tracked QA entries are mode 100644 Markdown; this report is UTF-8 text; only this report is untracked. See **R16**. |

## Detailed evidence

### R01

```yaml
inputs:
  - kind: repository_path
    ref: Makefile
  - kind: repository_path
    ref: internal/**
  - kind: live_service
    ref: host process table
```

- Entry point and steps: run the repository's full `make verify` target from the
  repository root, compare repository status before and after, then inspect the
  host process table for a surviving test binary from this checkout.
- Expected observable: Verification exits zero, tracked state is unchanged, and
  no test process remains.
- Independent confirmation and persistence: a fresh `git status` and a separate
  process-table read after the command exits.
- Observed: `make verify` failed as quoted in **Static gate**. A post-command
  `git status` contained only this seeded report, but host process enumeration
  was denied. The repository state did persist cleanly; the suite outcome did
  not meet the expected observable.

### R02

```yaml
inputs:
  - kind: repository_path
    ref: internal/agent/**
  - kind: repository_path
    ref: internal/cli/baseline_release_gate_test.go
  - kind: repository_path
    ref: internal/testfixture/**
```

- Entry point and steps: invoke the stress and existing adapter/formatter flows
  through `go test`, then scan the two formerly racy call sites for executable
  writes.
- Expected observable: concurrent execution returns a non-empty version every
  time with no exec error, existing behavior passes, and no test writes then
  executes the same file.
- Independent confirmation and persistence: rerun the focused adapter and
  formatter flows in fresh test processes and inspect the current source.
- Observed: the focused stress passed once, then three fresh repetitions passed
  in 0.41–0.44s; `TestACPXRunAppliesSelectionBeforePrompt`,
  `TestCheckAdapterProvesOfficialClaudePackageAndVersion`, and the standard
  TypeScript monorepo formatter journey also passed. Current source writes only
  mode-0600/0644 behavior data at the former call sites. Under `make verify`,
  however, worker 15 execution 2 received `signal: killed`; the high-risk dense-
  load promise therefore failed.

### R03

```yaml
inputs:
  - kind: repository_path
    ref: internal/agent/acpx_integration_test.go
  - kind: repository_path
    ref: internal/agent/acpx_runner_test.go
```

- Entry point and steps: run the dead-fixture regression through the Go test
  binary and read its elapsed result and asserted diagnostic.
- Expected observable: completion in under one second; the test proves the
  user-facing failure names the fixture and `exit status 23`.
- Independent confirmation and persistence: confirm the live-child upper bound
  remains `90 * time.Second` and no retry path was added.
- Observed: `TestSpawnedFixtureDeathFailsFast` passed in 0.35s. Its outer process
  rejects runs that do not contain both `immediate-exit acpx fixture` and
  `exit status 23`; current source keeps `agentWaitBudget = 90 * time.Second`
  and adds no retry.

### R04

```yaml
inputs:
  - kind: repository_path
    ref: internal/suiteguard/**
```

- Entry point and steps: run the violating, isolated, and ignored-write guarded
  subprocesses from the package's public test-runner entry point.
- Expected observable: a violation returns nonzero and names each path and
  change kind; isolated and ignored writes return zero; cost is printed.
- Independent confirmation and persistence: a fresh second subprocess receives
  a clean before/after fingerprint rather than reusing optimistic state.
- Observed: all three top-level cases passed. The violation subprocess returned
  a repository-boundary diagnostic naming `created: created.txt`, `modified:
  modified.txt`, and `removed: removed.txt`. Isolated and ignored writes passed;
  the live repository measurement covered 4,900 paths with separate before and
  after fingerprint durations.

### R05

```yaml
inputs:
  - kind: repository_path
    ref: internal/**/*_test.go
  - kind: repository_path
    ref: internal/suiteguardcontract/**
```

- Entry point and steps: run the repository contract across `./internal/...`,
  including its synthetic missing-package negative, then run all guarded
  packages through the full gate.
- Expected observable: every spawning package produces a named contract pass,
  the negative fixture names the missing package, and no live package emits a
  repository-boundary violation.
- Independent confirmation and persistence: current-tree installation count and
  the later full-suite result agree.
- Observed: `go test -tags repocontract ... ./internal/...` exited zero. Every
  one of the 26 test-bearing internal packages emitted
  `TestEverySpawningPackageInstallsTheSuiteGuard`; the suiteguard package also
  passed `names_an_unenumerated_and_unguarded_spawning_package` and the live
  repository inventory. The full gate later reached every package, though R01
  failed for other reasons.

### R06

```yaml
inputs:
  - kind: repository_path
    ref: internal/cli/detach_test.go
  - kind: live_service
    ref: host process table
```

- Entry point and steps: run every detach flow and the non-cooperative teardown
  case, then query the host process table after the test binary exits.
- Expected observable: survival remains asserted before cleanup; teardown kills,
  reaps, and proves absence even when the sentinel is ignored.
- Independent confirmation and persistence: no matching detached test child is
  visible in a fresh post-run process-table read.
- Observed: the detach sweep and `TestDetachedChildIsTerminatedAtTeardown`
  passed. The test records the PID, proves it alive after detachment, kills it,
  waits, and fails unless `ProcessAlive` becomes false. The planned independent
  host query was blocked: `pgrep` returned exit 3 with `sysmond service not
  found` and `Cannot get process list`.

### R07

```yaml
inputs:
  - kind: repository_path
    ref: internal/cli/doctor.go
  - kind: repository_path
    ref: internal/cli/doctor_test.go
  - kind: repository_path
    ref: internal/store/process*.go
  - kind: live_service
    ref: host process table and local Run Database
```

- Entry point and steps: build Roundfix under `/tmp`, run `doctor` as an operator,
  then run the command-facing found, live-Run, empty, unreadable, and read-only
  scenarios.
- Expected observable: the live command states its inventory; scenario output
  carries PID, age, CPU, and originating Run when found, names unreadability when
  partial, stays `exitOK`, and leaves Run records byte-equivalent.
- Independent confirmation and persistence: rerun the built command and compare
  Run listings before and after the write-refusal scenario.
- Observed: `/tmp/0103-qa-roundfix doctor` reached the public CLI and printed
  `residue: ok (no process residue found)`. Its overall exit was 1 because the
  existing Codex binary signature/profile checks failed; residue itself remained
  non-blocking. The five focused command-facing cases all passed, including PID
  5252 with age/CPU/Run, live-Run exclusion, explicit empty and partial text, and
  a before/after Run listing equality check.

### R08

```yaml
inputs:
  - kind: repository_path
    ref: internal/cli/cli.go
  - kind: repository_path
    ref: internal/cli/cli_test.go
  - kind: repository_path
    ref: internal/store/process*.go
```

- Entry point and steps: exercise the CLI command dispatcher against a Run with
  a surviving descendant and a clean tree, then run the real process-controller
  unrelated-process canary.
- Expected observable: surviving PID is named and the Run remains Active; a clean
  tree preserves stdout, stderr, exit status, and Stopped state; the unrelated
  process remains alive.
- Independent confirmation and persistence: reload Run state after each command
  and observe the unrelated PID through a second process-table read.
- Observed: `TestForceStopProvesTheTree` and
  `TestForceStopProvesTheTreeWhenTreeExits` passed, as did the real
  `TestOwnerProcessControllerTerminateTreeLeavesUnrelatedProcessRunning` canary.
  The broader command sweep passed the state/output cases but failed its two
  host integrations before termination because macOS denied `kern.proc.all`.
  Unblocking action: rerun those two tests in a full-access macOS session that
  permits process-table enumeration.

### R09

```yaml
inputs:
  - kind: repository_path
    ref: internal/speccheck/mechanical.go
  - kind: repository_path
    ref: internal/speccheck/mechanical_test.go
  - kind: repository_path
    ref: docs/specs/0103-a-suite-that-leaks-nothing/qa/**
```

- Entry point and steps: run all four evidence-refusal directions, then run the
  public strict Spec check against this Spec.
- Expected observable: binary and Gitlink are refused by path and kind, JSON is
  accepted, an absent evidence directory skips, and this readable report causes
  no mechanical finding.
- Independent confirmation and persistence: inspect tracked Git modes and rerun
  the public checker after the report closes.
- Observed: binary, Gitlink, ordinary JSON, and absent-directory cases each
  passed as an independent top-level test. The pre-run public strict Spec check
  returned `No findings`; its missing-reference-index detector skip was retained
  because it did not run.

### R10

```yaml
inputs:
  - kind: repository_path
    ref: Makefile
  - kind: repository_path
    ref: docs/workflow/authorizations/**
  - kind: repository_path
    ref: internal/baseline/**
  - kind: repository_path
    ref: internal/suiteguard/**
  - kind: repository_path
    ref: internal/suiteguardcontract/**
```

- Entry point and steps: run `make baseline-digests` and the exact plan-
  characterization update command, execute declared/undeclared/wrong-command
  guard cases, and inspect the repository diff.
- Expected observable: both sanctioned commands exit zero without a boundary
  violation; wrong-command and undeclared writes remain named violations; no
  private exemption list or unexpected diff appears.
- Independent confirmation and persistence: rerun the deterministic commands and
  compare tracked bytes after completion.
- Observed: the plan-characterization command passed all five cases and left no
  diff. The three guard directions passed: declared output accepted, wrong
  command refused `declared.txt`, and undeclared output refused `undeclared.txt`.
  `make baseline-digests` failed at
  `TestReadoptionCompatibilityMaintainedFixture` because the guard named its own
  declared `catalog.digest` and `catalog.normalized.json` writes. The failed
  command's two changes were restored to HEAD through `apply_patch`; a fresh
  `git diff --exit-code` over both outputs and plan characterization exited zero.

### R11

```yaml
inputs:
  - kind: external_repository
    ref: https://github.com/golang/go/issues/22315
  - kind: repository_path
    ref: docs/findings/2026-08-10-a-fake-adapter-goes-silent-under-a-dense-start.md
  - kind: repository_path
    ref: docs/findings/**
```

- Entry point and steps: inspect the upstream issue independently of this Spec,
  then locate the pre-Spec 2026-08-06 process-table measurement in repository
  Finding history.
- Expected observable: upstream documents the write-then-execute `ETXTBSY`
  family; independent dated evidence records four survivors and 2h40m CPU.
- Independent confirmation and persistence: source URL and repository path are
  both reachable from a fresh read and neither sits under this Spec.
- Observed: [golang/go#22315](https://github.com/golang/go/issues/22315) is an
  upstream Go issue opened in 2017. It documents the Unix race where one thread
  writes a program while concurrent `fork`/`exec` retains a writable descriptor,
  producing intermittent `ETXTBSY`. The independent repository Finding
  `docs/findings/2026-08-06-the-detach-tests-leak-the-process-they-prove-survives.md`
  records PIDs 33864, 55351, 54551, and 87902, ages from 1d22h to 3d14h, and
  2h40m total CPU before this Spec existed. Neither source is authored under
  this Spec.

### R12

```yaml
inputs:
  - kind: repository_path
    ref: internal/agent/**
  - kind: repository_path
    ref: internal/cli/detach_test.go
  - kind: repository_path
    ref: .git
```

- Entry point and steps: inspect every implementation commit and current source
  for retry, sleep, deadline, test-parallelism, or removed-survival assertions;
  correlate with R02, R03, and R06 behavior.
- Expected observable: no forbidden shortcut or parallelism change; the 90-second
  live budget and detach survival assertion remain.
- Independent confirmation and persistence: commit-level path/diff inspection and
  current-source scan agree.
- Observed: the base-to-head diff adds no retry, changes no `t.Parallel`, and
  leaves `agentWaitBudget` at 90 seconds. The old fixed 75ms detach child exit is
  replaced by an explicit sentinel wait plus non-cooperative teardown; the
  `time.Sleep(time.Hour)` occurs only in the negative fixture whose purpose is to
  ignore the sentinel. R02, R03, and R06 confirm the behavior canaries.

### R13

```yaml
inputs:
  - kind: repository_path
    ref: internal/cli/**
  - kind: repository_path
    ref: internal/store/**
  - kind: repository_path
    ref: .git
```

- Entry point and steps: run the real process-controller lineage canary and audit
  implementation commit paths for garbage collection or retention changes.
- Expected observable: an unrelated process remains running and no GC or
  retention implementation changed.
- Independent confirmation and persistence: a second process read observes the
  canary before its own cleanup; the commit audit names the complete changed-path
  set.
- Observed: the real process-controller canary passed and asserted the unrelated
  PID remained live after tree termination. `git diff --name-only
  60036423..HEAD` includes no GC, retention, or user-guide retention path.

### R14

```yaml
inputs:
  - kind: repository_path
    ref: .git
  - kind: repository_path
    ref: docs/workflow/authorizations/**
  - kind: repository_path
    ref: docs/specs/0103-a-suite-that-leaks-nothing/task_*.md
```

- Entry point and steps: resolve the Daemon-owned implementation commits and run
  `git diff-tree --no-commit-id --name-only -r` for each; classify every actual
  changed path against the protected-tooling boundary and sanctioned outputs.
- Expected observable: no Task commit changes protected tooling configuration,
  scripts, ignore files, plugin declarations, or version pins; any regenerated
  derived byte is reproducible by its declared command.
- Independent confirmation and persistence: R10's post-regeneration diff matches
  the commit audit.
- Observed: `git diff-tree --no-commit-id --name-only -r <commit>` ran for Tasks
  01–08 and 10. Changes are bounded to assigned Task files, Go production/test
  files, and the new Go fixture/guard packages. No Makefile, workflow, ignore
  file, linter/formatter/typechecker/test-runner configuration, plugin
  declaration, dependency manifest, lockfile, or version pin changed. The
  authorization and consequent-fix mechanical detectors remain skipped because
  those artifacts are inapplicable; R10 separately records the behavioral
  regeneration failure rather than disguising it as an authorization problem.

### R15

```yaml
inputs:
  - kind: repository_path
    ref: CONTEXT.md
  - kind: repository_path
    ref: internal/cli/doctor.go
  - kind: repository_path
    ref: docs/specs/0103-a-suite-that-leaks-nothing/_techspec.md
```

- Entry point and steps: run the strict Spec checker and directly compare the
  emitted Doctor token with the glossary definition and Vocabulary Contract.
- Expected observable: Process Residue is documented with the same reporting-
  only, no-Run-record semantics and no competing synonym was introduced.
- Independent confirmation and persistence: fresh checker output and direct
  source reads agree.
- Observed: the strict checker exited zero with `No findings`.
  `internal/cli/doctor.go` emits `residue`; `CONTEXT.md` defines Process Residue
  as a reporting-only readiness fact that creates no Run record and records the
  preferred term and avoided synonyms. The TechSpec Vocabulary Contract points
  to the same emission.

### R16

```yaml
inputs:
  - kind: repository_path
    ref: docs/specs/0103-a-suite-that-leaks-nothing/qa/**
```

- Entry point and steps: after all rows, enumerate tracked QA modes, content
  types, and every report evidence reference.
- Expected observable: no mode `160000`, binary, scratch repository, unresolved
  path, or file not authored by this gate is present.
- Independent confirmation and persistence: the final public strict Spec check
  reads the closed report successfully.
- Observed: `git ls-files -s` reports only mode-100644 Markdown under the QA
  tree; `file` identifies all three reports as UTF-8 text; `rg --files` finds no
  evidence directory, binary, or scratch repository. Every external or local
  source named by a row was read successfully. The worktree contains only this
  seeded untracked report after the failed regeneration outputs were restored.

## Mechanical skips

| Detector | Missing artifact |
| --- | --- |
| authorization bounded paths | tooling authorization |
| consequent-fix commit order | consequent-fix declaration |
| QA evidence paths | docs/specs/0103-a-suite-that-leaks-nothing/qa/evidence |

## Findings

### F-01 — Compiled fixture stress is killed under the authoritative suite load

- Impact: `Blocks-Completion`.
- Actor and journey: maintainer running the authoritative repository gate, R01
  and R02.
- Expected: `TestFixtureBinarySurvivesConcurrentExec` completes all 512
  provision-and-exec cycles under the suite's real dense start without an exec
  error or empty probe, and `make verify` exits zero.
- Actual: under `go test -parallel 16 ./...`, worker 15 execution 2 returned
  `signal: killed`; `internal/agent` and `make verify` failed. The focused test
  passed once and then passed three fresh repetitions, so this is a load-
  dependent gate failure rather than a deterministic single-package failure.
- Reproduction: from the repository root at the recorded build, run
  `rtk make verify`.
- Evidence: **Static gate**; focused comparison in **R02**.
- Affected rows: R01, R02.
- Required before rerun: find why a compiled fixture child receives SIGKILL
  under the repository's actual spawn density, repair that root cause without a
  retry or relaxed assertion, and rerun the full gate.

### F-02 — Baseline digest regeneration is still refused by the suite guard

- Impact: `Blocks-Completion`.
- Actor and journey: maintainer running the declared derived-artifact
  regeneration, R10.
- Expected: `make baseline-digests` completes while the guard is installed,
  because the authorization record declares the command's exact outputs.
- Actual: the command exits 2 at
  `TestReadoptionCompatibilityMaintainedFixture`; the guard reports
  `modified: internal/baseline/testdata/catalog.digest` and
  `modified: internal/baseline/testdata/catalog.normalized.json`.
- Reproduction: from the repository root at the recorded build, run
  `rtk make baseline-digests`.
- Evidence: exact output and restored-tree confirmation in **R10**. The separate
  plan-characterization command and all three guard direction tests passed.
- Affected row: R10.
- Required before rerun: make the guard recognize these outputs only under the
  declared command, regenerate the deterministic catalog artifacts, and prove
  the command leaves the repository at its expected bytes.

## Blocked and skipped

- R06 — `blocked (environment: host process-table enumeration denied)`.
  `pgrep` exited 3 with `sysmond service not found` and `Cannot get process
  list`. Equivalent observed evidence exists: every detach flow passed, the
  teardown negative fixture ignores the sentinel, and the cleanup waits and
  proves its PID absent. Unblock with a full-access macOS process-table read
  immediately after the same detach sweep.
- R08 — `blocked (environment: macOS kern.proc.all denied)`. Both real
  host-level Force Stop integrations reproduced `operation not permitted`
  while enumerating a non-session owner's process tree. Equivalent evidence
  exists: the command-facing surviving-descendant and clean-tree cases passed,
  Run state was reloaded, and the real process controller left an unrelated PID
  running. Unblock by rerunning the two named integration tests in a full-access
  macOS session.
- No flow row was declared-blocked or skipped. The three preserved Mechanical
  skips are detector applicability records, not flow statuses.

## Coverage

- PRD user stories executed: `5/5`. Story 1 has passing attribution and
  installation rows but the assembled full gate fails in R01. Story 2 has
  equivalent teardown evidence but its host-table confirmation is environment-
  blocked. Story 3 has one pass and one load-dependent failure. Stories 4 and 5
  pass their public surfaces.
- Core features exercised: `8/8` — `4 pass`, `2 fail`, `2 blocked
  (environment)` with equivalent evidence.
- Non-Goals checked: `5/5`; no retry/deadline/parallelism weakening, no lost
  survival assertion, no unrelated process termination, and no GC or retention
  change was found.
- Flow rows after R16 closes: `11 pass`, `3 fail`, `2 blocked (environment)`,
  `0 blocked (finding)`, `0 blocked (declared)`, `0 skipped`, `0 pending`.
- Findings: `2 Blocks-Completion`; `0 Data-Loss`, `0 Trust-Damage`, `0
  Friction`, `0 Cosmetic`.
- `rows_blocked_environment: 2`; `rows_blocked_finding: 0`;
  `rows_blocked_declared: 0`.
- Outside evidence: passed from upstream Go issue #22315 and the independent
  2026-08-06 process-table Finding; neither is authored by this Spec.
- Pull Request journeys: none declared. The prompt confirms no Pull Request is
  open, and the Run Worktree branch was not used to infer one.

## Final verdict

`fail` — 11 rows passed, 3 failed, and 2 were environment-blocked with
equivalent evidence; F-01 and F-02 are both `Blocks-Completion` findings, so the
compiled-fixture load failure and sanctioned-regeneration refusal must be fixed
before this full gate can pass.
