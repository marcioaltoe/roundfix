---
spec: 0103-a-suite-that-leaks-nothing
date: 2026-08-15
build: 4482c01907014f7bbdbd10c5217c28a7873007fb
head: acc1635080449f5a23c4e1867520149640a642a8
status: closed
verdict: pass
rows_blocked_environment: 3
rows_blocked_finding: 0
rows_blocked_declared: 0
surfaces: [backend, infra]
---

# QA report — A suite that leaks nothing

## Scope and environment

Full rerun of the authored terminal gate. Product source is byte-identical to
build `4482c01907014f7bbdbd10c5217c28a7873007fb`; repository head
`acc1635080449f5a23c4e1867520149640a642a8` adds only Spec documentation and
committed QA evidence. Pull Request journeys are environment-blocked because no
Pull Request is open, but this Spec declares none. `task_09` is the unique
terminal QA node and every dependency is completed.

`rtk roundfix spec check 0103-a-suite-that-leaks-nothing --strict` exited zero
with no findings. It skipped only `SC-REF-UNRESOLVED` because the Spec has no
`references/_index.md`.

## Static gate

`rtk make verify` ran from the repository root and exited `2` at `make test`.
Every package passed except `internal/agent` and `internal/cli`:

- `TestACPXRunCodexUsesConfiguredCleanPathOnDarwin` received an ACPX selection
  rejection with exit `-1`, and
  `TestACPXRunCancellationCommandFailuresWarnAndContinue` lost its fixture
  before the prompt-start event. Both tests then passed three fresh focused
  repetitions. The complete supervised host gate at the same product build
  also exited zero, so Task 09 classifies this Agent Session-only process
  density as environmental.
- `TestRunForceStopLegacyRunWithoutOwnerIdentityStillStopsOwner` and
  `TestRunForceStopOwnerProcessIntegrationProvesExitBeforeStoreCompletion`
  both failed before termination because macOS returned `operation not
  permitted` while enumerating the owned process tree. The same denial
  reproduced in the focused Force Stop sweep; public tree-state directions and
  the unrelated-process canary passed.

The same-product-build supervised result is
`qa/evidence/2026-08-15-the-authoritative-gate-on-the-host.md`. A fresh status
after the gate and after both sanctioned regeneration journeys names only this
seeded report; the product tree stayed unchanged.

## Mechanical findings

None.

## Results

| # | Story / criterion / sweep | Actor and surface | Status | Evidence |
| - | --- | --- | --- | --- |
| R01 | Stories 1–2: authoritative suite, unchanged repository, and no surviving test process | Maintainer · infra | blocked (environment: Agent Session process density and macOS process-table denial) | Session `make verify` exited 2 on two ACPX fixture/selection failures and two `kern.proc.all` denials; every other package passed. Same-product-build supervised host gate exited zero and repository state stayed unchanged. |
| R02 | Story 3 / Core Feature 4: compiled fixtures survive dense concurrent execution | Maintainer · infra | pass | Focused stress passed at `-parallel 16`; adapter and formatter journeys passed; former call sites write only non-executable data. |
| R03 | Story 3 / Core Feature 3: dead fixture fails fast with a named exit | Maintainer · infra | pass | Fresh regression passed in 0.36s; its assertions require the fixture name and exit status while the live-child budget remains unchanged. |
| R04 | Story 1 / Core Feature 1: suite guard attributes repository writes | Maintainer · infra | pass | Fresh guarded subprocesses named created, modified, and removed paths; isolated and ignored writes passed; cost was emitted. |
| R05 | Story 1: every spawning package installs the guard | Maintainer · infra | pass | Fresh repository-contract sweep passed in every test-bearing internal package and named the synthetic missing installation. |
| R06 | Story 2 / Core Feature 2: detach survivors are killed and reaped | Maintainer · infra | blocked (environment: host process-table enumeration denied) | Fresh detach and non-cooperative teardown flows passed; independent `pgrep` exited 3 because sandboxed `sysmond` is unavailable. Equivalent subprocess evidence proves survival, kill, reap, and PID absence. |
| R07 | Story 4 / Core Feature 5: Doctor reports Process Residue without mutation | Operator · backend CLI | pass | Fresh built CLI printed `residue: ok (no process residue found)`; five command-facing found/live/empty/partial/read-only cases passed. |
| R08 | Core Feature 6: Force Stop proves the spawned tree and preserves unrelated processes | Operator · backend CLI | blocked (environment: macOS `kern.proc.all` denied) | Public surviving-descendant and clean-tree cases passed, as did the real unrelated-process canary; two host integrations stopped at `operation not permitted` before enumeration. |
| R09 | Story 5 / Core Feature 8: evidence gate refuses binary and Gitlink scratch state | Maintainer · backend CLI | pass | Four fresh top-level cases passed for binary refusal, Gitlink refusal, readable JSON acceptance, and absent-directory skip; strict Spec checking passed. |
| R10 | Core Feature 7: sanctioned regeneration works without weakening other violations | Maintainer · infra | pass | `make baseline-digests`, plan-characterization regeneration, and six frozen-boundary cases passed; wrong/absent declarations stayed violations and no repository diff remained. |
| R11 | Outside evidence: golang/go#22315 and the independent 2026-08-06 residue measurement | Maintainer · backend/docs | pass | The committed operator observation of golang/go#22315 and the pre-Spec 2026-08-06 process-table Finding resolve and support the ETXTBSY diagnosis plus four survivors consuming 2h40m CPU. |
| R12 | Non-Goals: no retry, sleep, widened deadline, concurrency change, or weakened survival assertion | Maintainer · infra | pass | Fresh source audit found a concurrent two-worker floor, hard-link identity check, unchanged 90s live-child budget, no shortcut in the stress block, and preserved survival-before-teardown assertion. |
| R13 | Non-Goals: no unrelated process is killed; GC and retention stay unchanged | Operator · backend | pass | Fresh real controller canary left the unrelated process alive; base-to-product path audit found no GC, retention, prune, or journal implementation change. |
| R14 | Commit-dependent tooling audit | Maintainer · infra | pass | `git diff-tree` audited all eleven implementation/corrective commits; only Go source/tests and assigned Task files changed, with no protected tooling path or version pin. |
| R15 | Vocabulary closure for Process Residue | Maintainer · docs/backend | pass | Fresh strict check passed; `CONTEXT.md` defines Process Residue with Doctor's reporting-only, no-Run-write semantics, and the built CLI emitted the term. |
| R16 | Report and evidence hygiene | Maintainer · infra | pass | Final audit found only ordinary text evidence and mode-100644 tracked entries, no Gitlink or nested repository, no unresolved evidence path, and no unowned worktree change. |

## Mechanical skips

| Detector | Missing artifact |
| --- | --- |
| authorization bounded paths | tooling authorization |
| consequent-fix commit order | consequent-fix declaration |

## Detailed evidence

### R01

```yaml
inputs:
  - kind: repository_path
    ref: Makefile
  - kind: repository_path
    ref: internal/**
  - kind: repository_path
    ref: docs/specs/0103-a-suite-that-leaks-nothing/qa/evidence/2026-08-15-the-authoritative-gate-on-the-host.md
  - kind: live_service
    ref: host process table
```

- Entry point and steps: run `rtk make verify`, inspect fresh Git status, and
  query the post-suite host process table.
- Expected observable: exit zero, no repository mutation, and no surviving test
  process. Independent confirmation is a separate host process read and the
  same-build supervised gate.
- Observed: the Session gate reached only the two dense ACPX failures and two
  process-table denials described above. The ACPX failures passed 3/3 focused;
  the product tree stayed unchanged. The supervised host gate at product build
  `4482c01907014f7bbdbd10c5217c28a7873007fb` exited zero.
- Environment proof and equivalent evidence: independent `pgrep` exited `3`
  with `sysmond service not found`; the committed host record supplies the
  complete gate. Unblock by running in a full-access macOS process context.

### R02

```yaml
inputs:
  - kind: repository_path
    ref: internal/agent/**
  - kind: repository_path
    ref: internal/cli/baseline_release_gate_test.go
  - kind: repository_path
    ref: internal/testfixture/**
```

- Entry point: Go test runner at `-parallel 16`, followed by the adapter and
  formatter journeys and a scan of the former written-executable call sites.
- Expected: concurrent probes stay non-empty with no exec failure; a written
  executable is rejected; existing behavior persists in fresh processes.
- Observed: `TestFixtureBinarySurvivesConcurrentExec` passed in `0.03s`;
  adapter-selection regressions and the standard TypeScript formatter journey
  passed. The former call sites write only mode-0600/0644 data; the stress case
  requires `os.SameFile` with the compiled test binary.

### R03

```yaml
inputs:
  - kind: repository_path
    ref: internal/agent/acpx_integration_test.go
  - kind: repository_path
    ref: internal/agent/acpx_runner_test.go
```

- Entry point: `TestSpawnedFixtureDeathFailsFast` through the real Go test
  binary. Expected: under one second, naming the fixture and exit status, with
  the live-child ceiling unchanged.
- Observed: the fresh run passed in `0.36s`; its outer assertion requires
  `immediate-exit acpx fixture` and `exit status 23`.
- Independent confirmation: source retains
  `agentWaitBudget = 90 * time.Second` and no retry path was added.

### R04

```yaml
inputs:
  - kind: repository_path
    ref: internal/suiteguard/**
```

- Entry point: suiteguard's guarded `TestMain` subprocesses.
- Expected: created, modified, and removed paths are named; isolated and ignored
  writes pass; each run reports fingerprint cost.
- Observed: all directions passed. The negative subprocess named `created:
  created.txt`, `modified: modified.txt`, and `removed: removed.txt`; fresh
  accepted subprocesses reported their measured path counts and timings.

### R05

```yaml
inputs:
  - kind: repository_path
    ref: internal/**/*_test.go
  - kind: repository_path
    ref: internal/suiteguardcontract/**
```

- Entry point: repository-contract-tagged Go test sweep across
  `./internal/...`.
- Expected: every test-bearing package emits the named contract pass and the
  synthetic unguarded package is named.
- Observed: the complete fresh sweep exited zero; every applicable package
  emitted `TestEverySpawningPackageInstallsTheSuiteGuard`, and the suiteguard
  package passed the missing-installation negative.

### R06

```yaml
inputs:
  - kind: repository_path
    ref: internal/cli/detach_test.go
  - kind: live_service
    ref: host process table
```

- Entry point: all detach flows plus the sentinel-ignoring teardown case;
  independent confirmation is a post-process host query.
- Expected: the child first survives its caller, then cleanup kills, reaps, and
  proves its PID absent even without sentinel cooperation.
- Observed: the detach sweep and `TestDetachedChildIsTerminatedAtTeardown`
  passed. The test itself reads the PID after survival and again after teardown.
  `pgrep` could not obtain the sandboxed process list.
- Environment proof and equivalent evidence: `pgrep` exited `3` with `Cannot
  get process list`; the fresh subprocess lifecycle assertions supply the
  equivalent observable. Unblock the independent confirmation with host
  process-table access.

### R07

```yaml
inputs:
  - kind: repository_path
    ref: internal/cli/doctor.go
  - kind: repository_path
    ref: internal/cli/doctor_test.go
  - kind: repository_path
    ref: internal/store/process*.go
  - kind: live_service
    ref: host process table and local Run Database
```

- Entry point: a freshly built `/private/tmp/roundfix-0103-qa01 doctor` plus
  five command-facing residue scenarios.
- Expected: found, live-Run-excluded, empty, partial, and no-write behavior;
  residue alone does not change Doctor's exit status.
- Observed: the fresh binary printed `residue: ok (no process residue found)`.
  Doctor exited `1` only for unrelated profile and Codex signature failures.
  All five named residue cases passed, including complete Run-list comparison
  around the read-only command. The committed host observation resolves at
  `qa/evidence/2026-08-15-the-residue-check-on-the-host.md` as a second public
  read path.

### R08

```yaml
inputs:
  - kind: repository_path
    ref: internal/cli/cli.go
  - kind: repository_path
    ref: internal/cli/cli_test.go
  - kind: repository_path
    ref: internal/store/process*.go
  - kind: live_service
    ref: host process table
```

- Entry point: Force Stop through the CLI dispatcher and the real process
  controller. Expected: a survivor is named and keeps the Run Active; a clean
  tree preserves output; an unrelated process remains alive after termination.
- Observed: `TestForceStopProvesTheTree`, its clean-tree peer, and
  `TestOwnerProcessControllerTerminateTreeLeavesUnrelatedProcessRunning`
  passed. The two host integrations stopped before termination only because
  `kern.proc.all` returned `operation not permitted`.
- Environment proof and equivalent evidence: the denial reproduced in the
  focused sweep. The public state reloads and real unrelated-process canary are
  equivalent evidence. Unblock by granting macOS process-table access.

### R09

```yaml
inputs:
  - kind: repository_path
    ref: internal/speccheck/mechanical.go
  - kind: repository_path
    ref: internal/speccheck/mechanical_test.go
  - kind: repository_path
    ref: docs/specs/0103-a-suite-that-leaks-nothing/qa/**
```

- Entry point: four evidence-refusal cases and public strict Spec checking.
- Expected: binary and Gitlink are refused by path and kind, readable JSON is
  accepted, and an absent evidence directory is skipped.
- Observed: all four top-level cases passed. The strict checker returned no
  findings and retained only its explicit missing-index skip.

### R10

```yaml
inputs:
  - kind: repository_path
    ref: Makefile
  - kind: repository_path
    ref: docs/workflow/authorizations/**
  - kind: repository_path
    ref: internal/baseline/**
  - kind: repository_path
    ref: internal/suiteguard/**
  - kind: repository_path
    ref: internal/suiteguardcontract/**
```

- Entry point: both sanctioned regeneration commands, the frozen-boundary
  contract, and all guard declaration directions.
- Expected: declared outputs regenerate, wrong/absent declarations remain
  violations, and a rerun leaves canonical bytes unchanged.
- Observed: `make baseline-digests` exited zero with `changed:false`; plan
  characterization regenerated and exited zero; all six frozen-boundary cases
  passed. Guard negatives named `declared.txt`. Git found no remaining diff and
  no guard code contains a host process-table lookup.

### R11

```yaml
inputs:
  - kind: external_repository
    ref: https://github.com/golang/go/issues/22315
  - kind: repository_path
    ref: docs/specs/0103-a-suite-that-leaks-nothing/qa/evidence/2026-08-15-the-upstream-diagnosis-observed.md
  - kind: repository_path
    ref: docs/findings/2026-08-10-a-fake-adapter-goes-silent-under-a-dense-start.md
  - kind: repository_path
    ref: docs/findings/2026-08-06-the-detach-tests-leak-the-process-they-prove-survives.md
```

- Entry point: the operator-observed upstream record required by Task 09 and
  the two pre-Spec Findings.
- Expected: independent evidence supports the concurrent write/fork/exec
  `ETXTBSY` hazard and the four-survivor, 2h40m process measurement.
- Observed: the committed operator record names golang/go#22315 and its
  mechanism. The 2026-08-06 Finding names PIDs 33864, 55351, 54551, and 87902,
  ages from 1d22h through 3d14h, and 2h40m total CPU. Neither source was created
  by this Spec; this row uses the supplied observation rather than attempting
  unavailable Session network access.

### R12

```yaml
inputs:
  - kind: repository_path
    ref: internal/agent/**
  - kind: repository_path
    ref: internal/cli/detach_test.go
  - kind: repository_path
    ref: .git
```

- Entry point: current-source and implementation-history audit correlated with
  R02, R03, and R06.
- Expected: no retry, sleep, skip, deadline widening, fixed suite concurrency,
  or weakened survival assertion.
- Observed: the stress block derives workers from `GOMAXPROCS`, keeps a
  two-worker floor and barrier, contains no retry/sleep/skip, and rejects
  non-hard-linked fixtures. The 90-second live-child bound and the
  survival-before-teardown assertion remain.

### R13

```yaml
inputs:
  - kind: repository_path
    ref: internal/cli/**
  - kind: repository_path
    ref: internal/store/**
  - kind: repository_path
    ref: .git
  - kind: live_service
    ref: unrelated-process canary
```

- Entry point: the real unrelated-process canary and base-to-product changed
  path audit.
- Expected: the unrelated PID survives and GC/retention behavior is untouched.
- Observed: the real controller canary passed. The changed-path inventory has no
  GC, retention, prune, journal, or artifact-lifecycle implementation path; the
  only `config` match is the guard contract test installed in that package.

### R14

```yaml
inputs:
  - kind: repository_path
    ref: .git
  - kind: repository_path
    ref: docs/specs/0103-a-suite-that-leaks-nothing/task_*.md
```

- Entry point: `git diff-tree --no-commit-id --name-only -r` for each Task and
  corrective commit, plus the current delta.
- Expected: no protected tooling file, script, ignore file, dependency
  manifest, plugin declaration, or version pin changed.
- Observed: commits `3925cbce`, `96423c2b`, `298acc85`, `ddb7a6bd`,
  `646bbd98`, `a9a4ca00`, `bb9cb40d`, `8f074d05`, `de3a80ad`, `db5c57fa`,
  and `7dade8d8` contain only Go source/tests and their assigned Task files.
  The current tracked delta is empty; only this seeded report is untracked.

### R15

```yaml
inputs:
  - kind: repository_path
    ref: CONTEXT.md
  - kind: repository_path
    ref: internal/cli/doctor.go
  - kind: repository_path
    ref: docs/specs/0103-a-suite-that-leaks-nothing/_techspec.md
```

- Entry point: glossary read, built Doctor output, and public strict checker.
- Expected: Process Residue has the same reporting-only semantics in domain and
  product vocabulary.
- Observed: `CONTEXT.md` defines the term as a process with no live Run owner,
  includes age/CPU/origin, and says reporting creates no Run record. The built
  CLI emitted `residue`; strict checking returned no vocabulary finding.

### R16

```yaml
inputs:
  - kind: repository_path
    ref: docs/specs/0103-a-suite-that-leaks-nothing/qa/**
  - kind: repository_path
    ref: .git
```

- Entry point: final QA-tree Git-mode, file-type, nested-repository, evidence
  reference, and strict-report audit.
- Expected: ordinary readable evidence only, no Gitlink or binary, every path
  resolves, and no unowned worktree change exists.
- Observed: every committed QA entry is mode `100644`; the current report and
  all evidence files are text; no mode `160000` or nested `.git` directory
  exists. `git diff --check` passed, and `git status --short` names only this
  seeded report. Every repository evidence path in the report resolves.

## Findings

None. The Agent Session-only ACPX failures passed three fresh focused
repetitions and the same-product-build supervised host gate; the Force Stop
integrations stop at a proved macOS permission boundary before product behavior.

## Blocked and skipped

- R01 is environment-blocked on Agent Session process density and macOS
  process-table access. The supervised same-product-build `make verify` pass,
  fresh focused 3/3 ACPX passes, unchanged repository state, and subprocess
  lifecycle assertions supply equivalent evidence.
- R06 is environment-blocked on the independent host process query. Fresh
  detach subprocesses prove survival followed by kill, reap, and PID absence.
- R08 is environment-blocked on `kern.proc.all`. Fresh public Force Stop state
  cases and the real unrelated-process canary supply equivalent evidence.
- No row is finding-blocked, declared-blocked, skipped, or pending. The two
  preserved mechanical detector skips are applicability records, not QA row
  statuses.

## Coverage

All 16 planned rows are terminal: 13 pass and 3 are environment-blocked; none
fail, are finding-blocked, declared-blocked, skipped, or pending. Every PRD user
story, all eight core features, all five Non-Goals, the outside-evidence
obligation, the glossary closure, and the tooling/evidence audits are covered.

High-risk probes covered repository-wide versus focused load; three fresh
repetitions of the Agent Session failures; gate-parallel compiled execution;
immediate fixture death; created, modified, and removed writes; a missing guard
installation; non-cooperative teardown; unreadable process tables; found,
live-Run-excluded, empty, partial, and read-only residue states; surviving and
clean Force Stop trees; an unrelated-process canary; wrong and absent
regeneration declarations; deterministic regeneration; and binary/Gitlink
evidence refusal. There is no frontend or TUI surface in this Spec.

The glossary check found Process Residue already defined with Doctor's
reporting-only semantics, so this rerun introduced, changed, and retired no
domain term. Counts: `rows_blocked_environment: 3`,
`rows_blocked_finding: 0`, and `rows_blocked_declared: 0`. Finding impacts are
zero at every tier.

## Final verdict

**Pass.** Every runnable criterion passed; each of the three
environment-blocked rows records its cause, unblocking action, and equivalent
observed or supervised evidence; no row failed, was finding- or
declared-blocked, skipped, or left pending.
