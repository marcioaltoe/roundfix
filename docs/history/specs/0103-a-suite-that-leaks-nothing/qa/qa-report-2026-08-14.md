---
spec: 0103-a-suite-that-leaks-nothing
date: 2026-08-14
build: 8f074d055f93a4b13c1c18e2431ce61b08aac684
status: closed
verdict: fail
rows_blocked_environment: 0
rows_blocked_finding: 0
rows_blocked_declared: 0
surfaces: [backend, infra]
---

# QA report — A suite that leaks nothing

## Scope and environment

The full authored gate was requested against commit
`8f074d055f93a4b13c1c18e2431ce61b08aac684` in the Run Worktree on branch
`roundfix/run-run_20260814T212843Z_1085ae5aa10dc59b`. The Pull Request fact was
`none open`; any required Pull Request journey would therefore be
environment-blocked, but the gate stopped before building any flow matrix rows.

`task_09` is the manifest's terminal `qa` node. Its dependencies `task_02`,
`task_05`, `task_07`, and `task_08` are completed, as are Tasks 01–08. The
seeded mechanical stage below was preserved. No scratch repository, built
binary, or additional evidence artifact was created.

## Static gate

The mandatory clean-authoring precondition failed:

```text
$ roundfix spec check 0103-a-suite-that-leaks-nothing --strict
Spec 0103-a-suite-that-leaks-nothing
[error] SC-VOCABULARY-UNDOCUMENTED: internal/cli/doctor.go emits undocumented token "residue" absent from CONTEXT.md
  at internal/cli/doctor.go:30
  at CONTEXT.md:1
  fix: Document "residue" in CONTEXT.md or narrow the declared pattern.
Skipped:
  SC-REF-UNRESOLVED: missing docs/specs/0103-a-suite-that-leaks-nothing/references/_index.md
```

Exit code: `1`. A focused repository search independently confirmed that
`internal/cli/doctor.go` emits `residue` and the TechSpec declares Process
Residue, while `CONTEXT.md` has no matching term. Under the QA gate contract, a
failed strict Spec check stops execution before matrix construction and before
`make verify`; neither the static verification pipeline nor live backend and
infra journeys ran on this build.

## Mechanical findings

None.

## Results

| # | Status | Provenance |
| - | --- | --- |
| QA-STATIC | fail | static gate: `roundfix spec check --strict` reported SC-VOCABULARY-UNDOCUMENTED for "residue", so the run stopped before building any matrix row |

This row was added by hand on 2026-08-14. The gate wrote this report with an
empty Results table, which its own contract calls malformed, and the malformed
report then refused the next run of the same Spec with a fix that run could not
perform. Rather than delete the evidence — the one exit this repository's rules
single out — the row records what actually happened, which is the shape Spec 0113
proposes for a precondition refusal. See
`docs/backlog/2026-08-14-a-spec-that-coins-a-term-cannot-pass-its-own-gate.md`.

## Mechanical skips

| Detector | Missing artifact |
| --- | --- |
| authorization bounded paths | tooling authorization |
| consequent-fix commit order | consequent-fix declaration |
| QA Report structure | QA Report |
| QA evidence paths | QA Report |

## Findings

### QA-001 — Process Residue is emitted but absent from the glossary

- Impact: `Blocks-Completion`.
- Actor and journey: maintainer running the authored final QA gate, strict
  authoring precondition.
- Expected: the declared `residue` vocabulary emitted from
  `internal/cli/doctor.go` resolves in `CONTEXT.md`, and the strict Spec check
  exits zero.
- Actual: `SC-VOCABULARY-UNDOCUMENTED` names `internal/cli/doctor.go:30` and
  `CONTEXT.md:1`; the command exits `1`.
- Reproduction: from the repository root, run
  `roundfix spec check 0103-a-suite-that-leaks-nothing --strict`.
- Evidence: the exact command output in **Static gate** and the current-tree
  search `rg -n -i 'process residue|residue' CONTEXT.md internal/cli/doctor.go
  docs/specs/0103-a-suite-that-leaks-nothing/_techspec.md`, which returned
  matches only in `internal/cli/doctor.go` and `_techspec.md`.
- Affected rows: none were materialized; the failed authoring precondition
  forbids building the flow matrix.

## Blocked and skipped

No QA row was marked blocked or skipped. The gate stopped before row planning,
as required by the clean-authoring precondition. To unblock a new QA run,
document the emitted Process Residue term in `CONTEXT.md` (or intentionally
narrow the Vocabulary Contract), then make the strict Spec check pass.

The four preserved mechanical skips above are detector applicability records,
not flow-matrix `skipped` statuses, and therefore do not change the blocked-row
counts or verdict mechanics.

## Coverage

- PRD user stories executed: `0/5`.
- Core features exercised through live surfaces: `0/7`.
- Flow rows: `0 pass`, `0 fail`, `0 blocked`, `0 skipped`, `0 pending`.
- Findings: `1 Blocks-Completion`; `0 Data-Loss`, `0 Trust-Damage`, `0 Friction`,
  `0 Cosmetic`.
- `rows_blocked_environment: 0`; `rows_blocked_finding: 0`;
  `rows_blocked_declared: 0`.
- Not tested: compiled fixtures, bounded wait, suite guard, detach teardown,
  residue inventory, Force Stop tree proof, evidence refusal, Non-Goals, and
  the outside-evidence obligation comprising upstream `golang/go#22315` and the
  external 2026-08-06 process-table measurement of four surviving processes
  consuming two hours and forty minutes of CPU. The strict authoring failure
  prevented matrix construction, source confirmation, and all live-flow
  execution.

## Final verdict

`fail` — the authored final QA gate cannot start until Process Residue is
documented consistently and the strict Spec check exits zero; no flow verdict
was inferred from Task evidence or prior focused tests.
