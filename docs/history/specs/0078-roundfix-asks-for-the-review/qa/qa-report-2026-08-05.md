---
spec: 0078-roundfix-asks-for-the-review
date: 2026-08-05
build: bdf6ff8d4d680188a97986ee1340ab9ff052a499
status: closed
verdict: pass
rows_blocked_environment: 11
rows_blocked_finding: 0
rows_blocked_declared: 0
surfaces: [backend, cli]
---

# QA report — Roundfix asks for the review

## Scope and environment

Full authored gate for task_06 on the retained Run Worktree branch
`roundfix/run-run_20260805T184945Z_b449bc66d45239fd`, build
`bdf6ff8d4d680188a97986ee1340ab9ff052a499`. The intended actors are an
unattended Roundfix operator and a maintainer configuring Review Source
behaviour. Entry points are the built `roundfix` CLI, its documented Project
Config, the Run Event Stream, and the Review Source boundary reached through
the CLI. No Pull Request is open for the Spec target branch, so the live hosted
Pull Request journey is planned as environment-blocked and requires equivalent
observed or supervised evidence before the report can pass.

Evidence directory:
`qa/evidence/2026-08-05-spec-0078/`.

## Static gate

The initial unpiped `rtk make verify` exited 0: 3,452 Go tests, the isolated
Spec corpus budget, Skill checks, build, and Spec Consistency Check passed. A
final-worktree rerun exited 2 only because the one-second wall-clock corpus
budget measured 1.489615292s; all 3,452 product tests passed. The unchanged
focused budget reproduction then exited 0, classifying the final attempt as
environmental timing variance. Evidence and exact unblocking action:
[`make-verify.md`](evidence/2026-08-05-spec-0078/make-verify.md).

## Planned matrix detail

Every row starts `pending`. Each row records the actor, real entry point,
steps, expected observable, independent confirmation, persistence or replay
check, and intended evidence.

| # | Actor, entry point, and surface | Steps and behavior probe | Expected observable | Independent confirmation and persistence | Evidence |
| - | --- | --- | --- | --- | --- |
| R01 | QA Agent; Spec task files; docs | Read `_tasks.md` and every Task status before running the gate. | `task_06` is the authored terminal QA node and every non-QA dependency is `completed`. | Re-read task_05 and graph from disk; do not alter status. | Report plus task paths. |
| R02 | Maintainer; Git history and active Spec; docs | Audit all four Project Constraints, active ADRs, protected-tooling authorization, exact Task commit paths, and chronological commit choreography. Probe out-of-scope paths. | Both active artifacts cite applicable or reasoned non-applicable constraints; Task 05 stays within its authorization and derived fallout. | `git diff-tree` for the authorization, Task, prerequisite, and consequent commits; compare to the grant and sanctioned regeneration paths. | Focused command transcript under evidence. |
| R03 | Operator; repository root; backend/CLI | Run the exact repository gate `rtk make verify` unpiped. | Exit 0 with format, tests, Skill check, and build passing. | Record command exit status and inspect final output. | `make-verify.txt`. |
| R04 | Maintainer; built CLI plus repository Project Config; CLI | Run `doctor` against the committed `.roundfixrc.yml` and `.coderabbit.yaml`. Probe a fresh invocation. | Effective pair is coherent: push-triggered review false, asking true, Max Rounds 2; command exits 0. | Second public config/read path or focused Preflight output confirms the values without changing files. | `repository-config.txt`. |
| R05 | Operator; built `resolve`, `watch`, and `fetch`; CLI | Exercise all four `pushTriggersReview` × `request_review` rows. Use invalid/out-of-order pairs as a Garbage Tour. | The two unequal pairs proceed past coherence; both equal pairs refuse before mutation with exit 2 and row-specific next action; `fetch` is exempt in all four. | Re-run refused rows and inspect that no Run/Review Source mutation occurred. | `preflight-table.txt`. |
| R06 | Unattended operator; built `watch --until-clean`; CLI/backend | Drive a Round that Final Pushes a new head with asking enabled. Probe the exact order around the async seam. | Exactly one request for the pushed head occurs after Final Push and before head-bound Evidence wait. | Review Source readback lists one marked request; event/read path confirms the same head and ordering. | `watch-one-request.txt`. |
| R07 | Unattended operator; built `watch --until-clean`; CLI/backend | Drive the same Round through the artifact-only docs descendant. Reopen/read after descendant publication. | Still one request, addressed to the fix head; the artifact descendant produces none. | Review Source comment readback and Run Event replay remain at one request after the descendant exists. | `artifact-descendant.txt`. |
| R08 | Recovering operator; Review Source request path reached by the real CLI; backend | Replay/resume the same pushed head and attempt the request twice. | One published comment; the replay reports deduplicated and does not publish again. | Fresh complete comment-list read after the second attempt returns one same-head marker. | `same-head-idempotency.txt`. |
| R09 | Operator; built `resolve`; CLI/backend | Resolve and Final Push once with asking enabled. Probe request-before-push ordering as invalid. | One request follows a successful Final Push; skipped/no-op push produces none. | Read Review Source comments and push log separately. | `resolve-request.txt`. |
| R10 | Operator; built `fetch`; CLI/backend | Run `fetch` under every coherence pair, including configurations that would refuse `resolve`/`watch`. | Exit follows existing fetch behavior and publishes zero requests in every pair. | Compare Review Source comment ledger before and after each invocation. | `fetch-read-only.txt`. |
| R11 | Unattended operator; built `watch`; CLI/backend | Make the Review Source explicitly refuse the request. Probe replay and delayed capacity as disallowed recovery paths. | Run ends Review Skipped naming the refusal; no retry, backoff, capacity wait, or second request occurs. | Event Stream reaches terminal refusal and the mutation ledger remains at one attempt. | `request-refusal.txt`. |
| R12 | Operator; built `watch`; CLI/backend | Drive repeated Rounds to the configured cap and try a completion replay. | At most one request per Round and never more than Max Rounds; this repository exposes Max Rounds 2. | Count request events and marked comments independently. | `round-cap.txt`. |
| R13 | Operator; `roundfix events <run-id>`; CLI | Replay the request lifecycle. | Each request outcome records head, command, and published or deduplicated result; asking never appears as Review Source Evidence. | Compare event JSON with Review Source readback and subsequent Evidence state. | `request-events.txt`. |
| R14 | Maintainer; configuration and Review Source boundary; backend/CLI | Check defaults and precedence; exercise different-head marker, command-only text, foreign author, pagination, list failure, publish failure, and blank command. | Only an exact Roundfix-authored same-head marker deduplicates; complete pagination is read; failures return without success event or retry; defaults stay false and `@coderabbitai review`. | Fresh focused checks plus source-bound public output where available. | `boundary-contract.txt`. |
| R15 | Existing operator; built CLI with asking disabled; CLI/backend | Run the existing watch, resolve, and fetch journeys with nil/disabled requester. | Existing control flow and outputs remain unchanged. | Full repository gate and focused regression checks agree. | `disabled-regression.txt`. |
| R16 | Skill reader; canonical and mirrored Roundfix Skill; docs | Follow the Skill contract for request timing, keys/defaults, refusals, Evidence boundary, fetch exemption, and no-retry behavior. | Documentation matches the shipped CLI and mirror is byte-identical. | `skills-sync-check` plus focused text comparison. | `skill-contract.txt`. |
| R17 | Maintainer; Git diff and public behavior; backend/CLI/docs | Sweep Non-Goals: no Evidence reinterpretation, fetch/fingerprinting/thread-resolution change, PR creation/body mutation, Review Source selection/limits, retry/backoff/capacity policy, or QA-verdict change. | No out-of-scope behavior shipped. | Changed-path and focused source/test sweep, cross-checked with `make verify`. | `non-goals.txt`. |
| R18 | Operator; hosted Open Pull Request and real Review Source; CLI/backend | Run the request journey against the Spec target branch's Open Pull Request. | Hosted comment, review, Evidence, and refusal behavior match the equivalent gate evidence. | Fresh hosted Pull Request readback. | Planned `blocked (environment: no open Pull Request)` plus equivalent observed or supervised evidence from R05–R13. |

## Results

| # | Story / criterion / sweep | Actor and surface | Status | Evidence |
| - | --- | --- | --- | --- |
| R01 | Authored terminal gate and completed prerequisites | QA Agent · docs | pass | Fresh `rtk rg` shows `_tasks.md` names `qa: task_06`, task_06 depends on task_05, task_05 is the sole non-QA leaf, tasks 01–05 are `completed`, and task_06 remains Daemon-owned `pending`. |
| R02 | Project Constraints and protected-tooling audit | Maintainer · docs/Git | pass | [`constraint-audit.md`](evidence/2026-08-05-spec-0078/constraint-audit.md): both active artifacts account for every constraint; accepted ADRs agree; authorization commit `f253cc92` is a separate ancestor of Task 05; `diff-tree` shows only the Skill pair, assigned Task file, and sanctioned derived fallout. |
| R03 | Authoritative repository Verification | Operator · backend/CLI | blocked (environment: host-load timing variance exceeded one-second corpus budget) | [`make-verify.md`](evidence/2026-08-05-spec-0078/make-verify.md): initial full gate passed; final full attempt measured 1.489615292s and exited 2; unchanged focused reproduction passed, while 3,452 product tests stayed green. Exact unblocking action recorded. |
| R04 | Repository's committed pair is coherent | Maintainer · CLI | blocked (environment: sandbox denied Agent state and adapter lineage was unproven) | [`repository-config.md`](evidence/2026-08-05-spec-0078/repository-config.md): `doctor` could not finish unrelated machine readiness, but equivalent built-`watch` evidence proves the committed false/true pair reaches the next Preflight check; Max Rounds is 2 and the keyword is explicit. Full-access unblocking action is recorded below. |
| R05 | Four-row Preflight table and both exit-2 refusals | Operator · CLI | pass | [`preflight-table.md`](evidence/2026-08-05-spec-0078/preflight-table.md): four scratch repositories exercised the built CLI; unequal pairs advanced, both equal pairs refused `resolve` and `watch` at exit 2 with exact values, next actions, and no-side-effects output; 13 fresh boundary tests independently passed. |
| R06 | One request after a Round's Final Push before Evidence | Unattended operator · CLI/backend | blocked (environment: no open Pull Request) | [`equivalent-pr-evidence.md`](evidence/2026-08-05-spec-0078/equivalent-pr-evidence.md): supervised watch seam observes old-head Evidence, one new-head request, then new-head Evidence; exact unblocking action recorded. |
| R07 | Artifact-only descendant produces no second request | Unattended operator · CLI/backend | blocked (environment: no open Pull Request) | [`equivalent-pr-evidence.md`](evidence/2026-08-05-spec-0078/equivalent-pr-evidence.md): one request for fix head `def456`, none for artifact descendant `artifact789`; exact unblocking action recorded. |
| R08 | Same-head replay is idempotent | Recovering operator · backend | blocked (environment: no open Pull Request) | [`equivalent-pr-evidence.md`](evidence/2026-08-05-spec-0078/equivalent-pr-evidence.md): two supervised calls publish once, deduplicate once, retain one same-head comment, and record both outcomes. |
| R09 | `resolve` requests only after a successful push | Operator · CLI/backend | blocked (environment: no open Pull Request) | [`equivalent-pr-evidence.md`](evidence/2026-08-05-spec-0078/equivalent-pr-evidence.md): CLI integration observes Final Push before one request; skipped Final Push produces zero. |
| R10 | `fetch` remains read-only under every pair | Operator · CLI/backend | blocked (environment: no open Pull Request) | [`equivalent-pr-evidence.md`](evidence/2026-08-05-spec-0078/equivalent-pr-evidence.md): all four public scratch invocations bypass coherence; fresh four-row Preflight and CLI integration evidence observe zero requests. |
| R11 | Explicit refusal ends Review Skipped with no retry | Unattended operator · CLI/backend | blocked (environment: no open Pull Request) | [`equivalent-pr-evidence.md`](evidence/2026-08-05-spec-0078/equivalent-pr-evidence.md): one request, skipped Evidence, named terminal refusal, and no retry/second request; failure paths perform one attempt. |
| R12 | Request count is bounded by Round cap | Operator · CLI/backend | blocked (environment: no open Pull Request) | [`equivalent-pr-evidence.md`](evidence/2026-08-05-spec-0078/equivalent-pr-evidence.md): request seam is once inside a Round; independent Max Rounds evidence stops at exactly 2, matching Project Config. |
| R13 | Request outcome is observable and distinct from Evidence | Operator · CLI/Event Stream | blocked (environment: no open Pull Request) | [`equivalent-pr-evidence.md`](evidence/2026-08-05-spec-0078/equivalent-pr-evidence.md): both outcomes emit head/command/outcome `review_source.request` events; 45 Run Event tests pass and Evidence remains a later read. |
| R14 | Request/config boundary, pagination, near misses, and failures | Maintainer · backend/CLI | pass | [`boundary-contract.md`](evidence/2026-08-05-spec-0078/boundary-contract.md): 18 fresh focused checks cover defaults, precedence, validation, marker near misses, full pagination, and no-event/no-retry failures. |
| R15 | Asking-disabled compatibility | Existing operator · CLI/backend | pass | [`equivalent-pr-evidence.md`](evidence/2026-08-05-spec-0078/equivalent-pr-evidence.md) and [`make-verify.md`](evidence/2026-08-05-spec-0078/make-verify.md): nil requester preserves Clean watch flow; no-op resolve and fetch remain mutation-free; full suite passes. |
| R16 | Canonical Skill and mirror match shipped contract | Skill reader · docs | pass | [`skill-contract.md`](evidence/2026-08-05-spec-0078/skill-contract.md): sync check, Skill validation, byte comparison, and focused contract inspection pass. |
| R17 | Non-Goals and scope-creep sweep | Maintainer · backend/CLI/docs | pass | [`non-goals.md`](evidence/2026-08-05-spec-0078/non-goals.md): commit/path and product diff sweep found no Evidence reinterpretation, PR creation, retry/backoff/capacity, issue-resolution, plan/limit, or QA-semantics expansion. |
| R18 | Hosted Open Pull Request journey | Operator · CLI/backend | blocked (environment: no open Pull Request) | Prompt-proved cause and exact unblocking action plus complete equivalent observed/supervised evidence are recorded in [`equivalent-pr-evidence.md`](evidence/2026-08-05-spec-0078/equivalent-pr-evidence.md). |

## Findings

None. The run found zero Blocks-Completion, Data-Loss, Trust-Damage, Friction,
or Cosmetic findings.

## Blocked and skipped

- R03 is `blocked (environment: host-load timing variance exceeded one-second
  corpus budget)`. The final unpiped full gate measured 1.489615292s; the same
  build's initial full gate and the unchanged focused reproduction passed.
  Unblock it by rerunning the exact full gate on a host/session without the
  transient load. QA did not retry the whole gate until green.
- R04 is `blocked (environment: sandbox denied Agent state and adapter lineage
  was unproven)`. `doctor` could not initialize Codex state under `~/.codex`
  and could not prove the configured Claude adapter lineage. Unblock it in a
  full-access session after installing/proving the required Claude adapter,
  then rerun `roundfix doctor`. Equivalent feature-specific evidence exists:
  the built `watch` command loaded this repository's configuration, accepted
  the false/true pair, and reached the later upstream check; focused config and
  Preflight tests passed.
- R06–R13 and R18 are `blocked (environment: no open Pull Request)`. The prompt
  proves no Pull Request is open for `ma/0078-roundfix-asks-for-the-review`, and
  the per-Run branch is never pushed. Unblock them by opening that target-branch
  Pull Request outside this QA gate, then perform read-only hosted comment,
  Evidence, refusal, and Event Stream confirmation. Every row links complete
  equivalent observed or supervised evidence in
  [`equivalent-pr-evidence.md`](evidence/2026-08-05-spec-0078/equivalent-pr-evidence.md).
- No row is finding-blocked, declared-blocked, or skipped.

## Coverage

18/18 rows reached a terminal status: 7 pass and 11 environment-blocked with
equivalent observed or supervised evidence. Stories and criteria: all seven
Core Features, all Goals and Success Metrics, every task-level criterion, and
all six Non-Goal groups covered. `rows_blocked_environment: 11`,
`rows_blocked_finding: 0`, `rows_blocked_declared: 0`; zero fail and zero
skipped. The gate exercised the public CLI, backend Review Source boundary,
persistence/replay, invalid-pair Garbage Tour, refusal/interrupt behavior,
configuration precedence, complete pagination, and the hosted Pull Request
equivalent path.

## Final verdict

Pass: every runnable row passed, all 11 environment-blocked rows record their
cause, exact unblocking action, and equivalent observed or supervised evidence,
and there are zero failed, finding-blocked, declared-blocked, or skipped rows.
