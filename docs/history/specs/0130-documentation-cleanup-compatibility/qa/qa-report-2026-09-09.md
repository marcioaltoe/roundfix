---
spec: 0130-documentation-cleanup-compatibility
date: 2026-09-09
build: c6a5ea156c70d33d59e9e5e1d98fcc2fe24cb318
auditing_binary: "roundfix 0.12.0"
auditor_staleness: "current: declared tree version 0.12.0 equals the auditing binary version"
status: closed
verdict: pass
rows_blocked_environment: 1
rows_blocked_finding: 0
rows_blocked_declared: 0
surfaces: [infra, docs]
---

# QA Report

## Performed repairs

None.

## Assigned repair failures

None.

## Mechanical findings

None.

## Scope and environment

Full terminal QA of build `c6a5ea156c70d33d59e9e5e1d98fcc2fe24cb318`
from the Run Worktree on branch
`roundfix/run-run_20260909T100849Z_71ecd38c8a6128b9`. The intended actors are a
maintainer authorizing a bounded tooling repair and an operator running the
repository's regeneration and verification paths. The Pull Request fact supplied
to this gate says no Pull Request is open, so Pull Request observation uses the
required environment-blocked equivalent-evidence path.

The gate writes only this Spec-local report and evidence. Regeneration will run in
an isolated temporary clone so the audited checkout remains unchanged.

## Static gate

- `rtk roundfix spec check 0130-documentation-cleanup-compatibility --strict`:
  pass; no findings, with the reported skipped detectors retained where relevant.
- The initial `rtk make verify` failed with the formatter resolved by the QA
  environment. The full `make verify verify-docs` control subsequently exited 0
  at the exact audited commit with Go and gofmt 1.26.7. See the dated resolution
  of F-001 and the retained [control transcript](evidence/2026-09-09-run-run_20260909T100849Z/05-toolchain-control.txt).
- `rtk make verify-docs`: pass; build, documentation contracts, tagged
  regeneration contracts, corpus budget and active Spec checks completed.

## Results

| # | Story / criterion / sweep | Actor and surface | Status | Evidence |
| - | --- | --- | --- | --- |
| QA-01 | CF-1: approved Spec and compatible legacy grants are discovered; unrelated, unapproved, malformed, mismatched and symlinked records grant no authority | Maintainer · infra/filesystem | pass | `TestCleanupRegenerationDiscovery` and all five named subtests passed. [Feature evidence](evidence/2026-09-09-run-run_20260909T100849Z/02-feature-contracts.md). |
| QA-02 | CF-2: command-only grants resolve through tree ownership with frozen, dedicated, sidecar, nested and invalid/conflicting boundaries preserved | Operator · infra/regeneration | pass | The full ownership group passed after the unchanged cache-denied command ran with cache access; the isolated public regeneration command also passed twice with `changed:false`. [Feature evidence](evidence/2026-09-09-run-run_20260909T100849Z/02-feature-contracts.md), [operator evidence](evidence/2026-09-09-run-run_20260909T100849Z/03-cleanup-regeneration.md). |
| QA-03 | CF-3: immutable main history supplies real non-empty grant bytes and bounded paths; missing historical Task objects remain explicit skips while controlled accepted/refused Git cases execute | Maintainer · infra/Git | pass | The ancestor resolves with 42 records. Independent `cat-file` checks confirm four unavailable objects; the two dependent historical cases remain explicitly skipped, while controlled accepted/refused cases pass. [History evidence](evidence/2026-09-09-run-run_20260909T100849Z/04-history-scope-and-outside-evidence.md). |
| QA-04 | CF-4 and OE-1: cleanup-compatible fixtures reproduce the original PR #180 boundary without restoring `docs/workflow`, while successful regeneration and strict/frozen refusals remain observable | Operator · infra/regeneration | pass | The cited external job reproduces missing proof-cost authority and stale derived outputs. With 45 workflow files staged for deletion in a disposable clone, two regeneration runs passed, left the directory absent and created no unstaged delta. [Cleanup evidence](evidence/2026-09-09-run-run_20260909T100849Z/03-cleanup-regeneration.md), [outside evidence](evidence/2026-09-09-run-run_20260909T100849Z/04-history-scope-and-outside-evidence.md). |
| QA-05 | Goals 1–3 and assembled repository behavior pass the complete selected Verification | Operator · infra | pass | Full `make verify verify-docs` exited 0 in a clean clone of the exact audited commit with Go and gofmt 1.26.7; an independent Git reread remained clean. No source or formatter configuration changed. [Static evidence](evidence/2026-09-09-run-run_20260909T100849Z/01-static-gates.md), [control transcript](evidence/2026-09-09-run-run_20260909T100849Z/05-toolchain-control.txt). |
| QA-06 | The repository documentation boundary, strict Spec checks and derived-artifact gates pass before Pull Request preparation | Maintainer · docs | pass | `rtk make verify-docs` exited 0, including the tagged regeneration gates and active Spec checks. [Static evidence](evidence/2026-09-09-run-run_20260909T100849Z/01-static-gates.md). |
| QA-07 | The separate maintainer authorization precedes the Task commit and the Task commit changes only the five bounded tooling paths plus its assigned Task evidence | Maintainer · Git/governance | pass | `54614682` is an ancestor of `c6a5ea15`; diff-tree lists exactly the five granted paths plus `task_01.md`. No descendant commit exists for a consequent fix. [Scope evidence](evidence/2026-09-09-run-run_20260909T100849Z/04-history-scope-and-outside-evidence.md). |
| QA-08 | Non-Goals: no public CLI/config behavior, workflow directory, exemption list, broader Spec work, dependency or unapproved implementation/test path entered the repair | Maintainer · docs/Git | pass | The Task commit's complete path set is the approved five-file slice plus its Task evidence; `go.mod` and `go.sum` are unchanged. The disposable cleanup flow proves the repair does not restore `docs/workflow`. [Scope evidence](evidence/2026-09-09-run-run_20260909T100849Z/04-history-scope-and-outside-evidence.md). |
| QA-09 | Vocabulary Contract: no glossary term was added, changed or retired, and canonical terms remain unchanged | Maintainer · docs | pass | `CONTEXT.md` is byte-unchanged from the authorization parent through the audited Task commit; the TechSpec declares no vocabulary change. [Scope evidence](evidence/2026-09-09-run-run_20260909T100849Z/04-history-scope-and-outside-evidence.md). |
| QA-10 | Pull Request approval, checks, unresolved threads, Merge-Ready acceptance and review-artifact ancestry controls | Maintainer · Pull Request | blocked (environment: no open Pull Request) | Supplied Pull Request fact: none open. Equivalent evidence records accepted authorization, both complete local gates passed at the audited commit, and independent Codex Standards/Spec reviews with zero remaining findings for that commit. No Pull Request threads or Merge-Ready acceptance existed at this gate; no PR-review artifact ancestry is claimed. Delivery still requires review and GitHub checks for its final candidate. |

## Findings

### F-001 — initial formatter failure; resolved as an execution-environment mismatch

- Impact: `Blocks-Completion` — a maintainer cannot satisfy the repository's
  required pre-delivery gate.
- Actor and step: operator runs `rtk make verify` from the audited Run Worktree.
- Expected: the selected complete Verification exits 0.
- Actual: exit 2 at `fmt-check`; it names
  `internal/cli/baseline_skills_restore_test.go` and
  `internal/cli/baseline_assets_sync_test.go`.
- Reproduction: `rtk make verify`.
- Initial interpretation, superseded below: `rtk gofmt -d` shows indentation differences. Both files are
  byte-unchanged from authorization commit `54614682` through Task commit
  `c6a5ea15`, so the failure predates and lies outside this Spec's five-file
  tooling authority.
- Affected rows: QA-05. QA-10 records the same red check as equivalent Pull
  Request control evidence.
- Evidence: [static gate evidence](evidence/2026-09-09-run-run_20260909T100849Z/01-static-gates.md).
- Initial suggested action, withdrawn: format the two files in an authorized
  repair and rerun the complete QA gate.

#### 2026-09-09 — independent toolchain control and resolution

The Supervisor reproduced the formatter discrepancy without changing either
file: the system formatter listed both paths, while gofmt 1.26.7 listed none.
The complete `make verify verify-docs` command then exited 0 in a separate clean
clone of `c6a5ea156c70d33d59e9e5e1d98fcc2fe24cb318`, explicitly selecting Go and
gofmt 1.26.7. An independent `git status --porcelain` reread returned no paths.
The exact command, toolchain, transcript and checksum are retained in the static
evidence. This resolves QA-05 through executed evidence; it grants no waiver and
requires no source formatting change. The original failed invocation remains
recorded above and in the earlier committed report.

The independently reviewed implementation is unchanged: both Codex
`gpt-5.6-luna`/`max` review axes reported zero remaining findings for `c6a5ea15`.
This Spec result covers the bounded repair at that commit. The subsequent
canonical-policy cleanup and its separate documentation-test failure remain
delivery work; this report does not declare the combined cleanup PR merge-ready.

## Blocked and skipped

- QA-10 — `blocked (environment: no open Pull Request)`. Cause: the QA prompt
  confirms no Pull Request exists and the per-Run branch never has one.
  Unblocking action: after the final delivery candidate passes its review and
  repository checks, open the target-branch Pull Request.
  Equivalent controls: maintainer authorization is commit `54614682`; checks are
  the complete Go/gofmt 1.26.7 `make verify verify-docs` control passed at
  `c6a5ea15`. Both independent Codex review axes cleared that implementation.
  No Pull Request means no unresolved review threads, no Merge-Ready acceptance
  and no PR-review artifact whose ancestry could be claimed. These observations
  answer every pre-PR control without claiming publication or merge approval.
- The four entries under `Mechanical skips` are preserved Daemon materialization
  records. They are not skipped QA matrix rows and do not change the row counters.

## Coverage

Closed: 10 rows — 9 pass, 0 fail, 1 blocked by environment, 0 blocked by
finding, 0 blocked by declaration, 0 pending and 0 skipped. Core Features 1–4
and OE-1 passed. Goals 1–3 and the assembled repository gate passed at the audited
repair commit after the formatter environment was made explicit. Both declared surfaces, tooling chronology and
scope, Non-Goals and vocabulary were exercised. The probes covered
malformed/unapproved grants, symlink and unsafe-path rejection, frozen and
wrong-command output refusal, unavailable Git objects, controlled Git changes,
post-cleanup regeneration idempotence and an independent Git reread.

## Final verdict

`pass`: 9 of 10 rows passed, with zero unresolved findings. QA-10 remains
environment-blocked with each pre-PR control answered by recorded equivalent
evidence. F-001 was resolved by the complete same-commit toolchain control; no
product repair, skipped assertion or QA override was used.

## Mechanical skips

| Detector | Missing artifact |
| --- | --- |
| authorization bounded paths | tooling authorization |
| consequent-fix commit order | consequent-fix declaration |
| QA Report structure | QA Report |
| QA evidence paths | QA Report |
