---
spec: 0082-the-manifest-already-answered-that
date: 2026-08-07
build: f3b2f0634fddce3cc712615e32b977630fe675b1
status: closed
verdict: fail
rows_blocked_environment: 1
rows_blocked_finding: 1
rows_blocked_declared: 0
surfaces: [cli, docs]
---

# QA report — Baseline update (rerun 02)

## Scope and environment

Full authored terminal QA gate for `task_08`, rerun after the repository gate
was made fail-closed. The build under test is the Run Worktree commit named in
frontmatter on branch
`roundfix/run-run_20260807T203932Z_c1861264e77b2e60`; the Spec target branch is
`ma/baseline-update-command`. No Pull Request is open, so Pull Request journeys
are environment-blocked by prompt fact and require equivalent local evidence.

Actors: fleet maintainer, one-off repository maintainer, first-adoption
maintainer, documentation reader, and QA operator. Public entry point: a fresh
`roundfix` binary built from this checkout. Mutable rehearsal repositories must
be gate-created copies under the operating-system temporary directory with the
fixed `roundfix-qa-0082-` prefix. No source repository outside this checkout may
be mutated.

This is a same-day rerun of `qa-report-2026-08-07.md`. No earlier row is carried
forward: the prior build was `cda415f177144ebb6be60c95410ae144343392ce`, so all
rows require fresh observation against the current build.

The QA binary was built from this checkout with an isolated temporary Go cache.
The initial build attempt was denied by the host Go cache permissions; the
isolated-cache build exited 0. The nine adopted source repositories and
OnionCry were read-only inputs to local Git clones. The historical Fiscus copy
was checked out at real repository commit `689e381` to reproduce the sixth
fleet catalog identity that was current when this Spec was authored; no fixture
manifest was constructed.

### Fleet reality

| Copy | Profile | Prior catalog suffix | First `--yes --no-skills` outcome |
| --- | --- | --- | --- |
| `conexus` | `standard-typescript-monorepo` | `edd99b3d…78a1` | exit 3, 14 unaccounted clauses |
| `fiscus` | `standard-typescript-monorepo` | `3e67a4f1…8dbb` | exit 3, 14 unaccounted clauses |
| `tax_poc` | `standard-typescript-monorepo` | `daa5be17…b69` | exit 3, 14 unaccounted clauses |
| `vortex` | `standard-typescript-monorepo` | `11d71526…4bb` | exit 3, 14 unaccounted clauses |
| `gss` | `gss-apps-monorepo` | `c0435d25…a014` | exit 0, five file changes |
| `oraculum` | `oraculum-backend` | `11d71526…4bb` | exit 0, two file changes |
| `oraculum_wt` | `oraculum-backend` | `11d71526…4bb` | exit 0, two file changes |
| `oraculum_run` | `oraculum-backend` | `11d71526…4bb` | exit 0, two file changes |
| `fluxus` | `oraculum-backend` | `11d71526…4bb` | exit 2, repository-owned Profile does not resolve |

These copies cover three Profile identities and six catalog identities:
`edd99b3d…78a1`, `3e67a4f1…8dbb`, `daa5be17…b69`, `11d71526…4bb`,
`c0435d25…a014`, and current `6707239b…193f`. The never-adopted copy came from
OnionCry and had no Setup Manifest.

### Temporary-copy ledger

Every mutable repository was created below the exact root
`/private/tmp/roundfix-qa-0082-rerun.6OuCGN` and removed with that exact root
before close:

- `/private/tmp/roundfix-qa-0082-rerun.6OuCGN/conexus`
- `/private/tmp/roundfix-qa-0082-rerun.6OuCGN/fiscus`
- `/private/tmp/roundfix-qa-0082-rerun.6OuCGN/fluxus`
- `/private/tmp/roundfix-qa-0082-rerun.6OuCGN/tax_poc`
- `/private/tmp/roundfix-qa-0082-rerun.6OuCGN/vortex`
- `/private/tmp/roundfix-qa-0082-rerun.6OuCGN/gss`
- `/private/tmp/roundfix-qa-0082-rerun.6OuCGN/oraculum`
- `/private/tmp/roundfix-qa-0082-rerun.6OuCGN/oraculum_wt`
- `/private/tmp/roundfix-qa-0082-rerun.6OuCGN/oraculum_run`
- `/private/tmp/roundfix-qa-0082-rerun.6OuCGN/never_adopted`
- `/private/tmp/roundfix-qa-0082-rerun.6OuCGN/oraculum_required_skill`
- `/private/tmp/roundfix-qa-0082-rerun.6OuCGN/oraculum_optional_skill`
- `/private/tmp/roundfix-qa-0082-rerun.6OuCGN/oraculum_owned_skill`
- `/private/tmp/roundfix-qa-0082-rerun.6OuCGN/oraculum_confirmation`
- `/private/tmp/roundfix-qa-0082-rerun.6OuCGN/oraculum_marker`
- `/private/tmp/roundfix-qa-0082-rerun.6OuCGN/oraculum_decision`
- `/private/tmp/roundfix-qa-0082-rerun.6OuCGN/oraculum_profile`
- `/private/tmp/roundfix-qa-0082-rerun.6OuCGN/oraculum_interactive`
- `/private/tmp/roundfix-qa-0082-rerun.6OuCGN/roundfix_regen`

The same exact root owned the moved-skill holding directories, QA binary, and
isolated Go caches. `rm -rf` targeted only the realpath-validated root. The
exact-root nonexistence check and `/private/tmp` prefix sweep both exited 0;
the worktree status contains only this QA Report.

## Static gate

`rtk make verify` was run un-piped from the repository root. The sandboxed
attempt was denied before the gate by blocked access to `api.github.com`; the
authorized full-access rerun exited 0. Its output reports every Go package
green, the focused corpus budget and Skill tests green, `roundfix skills check`
green, the binary build green, and `bin/roundfix spec check` with no findings
for Spec 0082. This is fresh evidence that the coverage regression reported as
F-001 in the first report is repaired on this build.

## Results

Every row is `pending` before flow execution. Rows are ordered by governance
and user impact. CLI persistence is confirmed through a second public command,
a fresh manifest read, or an exact tree/region digest comparison.

| # | Story / criterion / sweep | Actor, entry point, and surface | Steps and expected observable | Independent confirmation and persistence | Evidence path | Status |
| - | --- | --- | --- | --- | --- | --- |
| 1 | Authored gate, dependency completion, and Project Constraints | QA operator; Spec artifacts; docs/Git | Validate `qa: task_08`, terminality, completed non-QA dependencies, PRD/TechSpec constraint rows, operative sources, and active ADR accounting. | Re-read Task frontmatter, guidance, authorization, and accepted ADRs. | `_tasks.md`, `_prd.md`, `_techspec.md`, `task_01.md` through `task_08.md` | pass — `task_08` is the unique terminal `qa` node; its dependency and all non-QA Tasks are completed. Both active artifacts account for identifier, authentication/HTTP, active ADR, and tooling obligations with operative sources. |
| 2 | Protected-tooling authorization, chronological commit shape, exact paths, and regenerated-pin provenance | QA operator; Git/tooling | Resolve authorization, prerequisite, Task, and consequent commits; enumerate committed paths with `git diff-tree`; expect exact bounded scope and reproducible sanctioned pins. | Run regeneration in an isolated copy and compare canonical/mirrored skills byte-for-byte. | Authorization record; this report | pass — authorization `5c0e57f3` and scope prerequisite `9340a52f` precede Task commit `cda415f1`; fresh `diff-tree` enumerates the authorized sources, Task/test/doc paths, and derived pins. At `cda415f1`, isolated `make baseline-digests` returned `changed:false`; its otherwise unauthorized Profile-path delta is exactly one generated `goldenDigest`. Both canonical/mirror skill pairs are byte-identical; no consequent fix commit exists. |
| 3 | Repository Verification | QA operator; `rtk make verify`; CLI | Expect the full formatter, test, Repository Skill Set, and build gate to exit 0. | Read the un-piped exit status and final summary; diagnose any failure. | This report, Static gate | pass — authorized full-access rerun exited 0 after the sandbox-only GitHub denial; every package and the complete build/skills/spec-check pipeline passed. |
| 4 | Public update help and structured contract | Fleet maintainer; `roundfix baseline update --help`; CLI/docs | Expect seven documented flags, schema, and exits `0/1/2/3/130`; reject mutually exclusive confirmations. | Follow the user guide independently and compare stdout/stderr and exit categories. | `docs/user-guide/context-driven-development.md`; this report | pass — fresh built help exits 0 and names all seven flags, result schema, and five exits; `--yes` plus `--confirm-plan` exits 2/category `invalid`. |
| 5 | Story 1 — stale adopted repository refresh | Fleet maintainer; `baseline update --repo <copy> --yes --format json`; CLI | On a real adopted copy, expect no questions, managed refresh, structured result, and exit 0. | Reload the Setup Manifest and current catalog identity after apply. | This report; temporary-copy ledger | pass — real GSS and Oraculum copies apply without input at exits 0, report five/two managed changes and approved Plan Digests, and reload at catalog `6707239b…193f`. Fleet-wide success is assessed separately in row 17. |
| 6 | Idempotence success metric | Fleet maintainer; repeat update; CLI | Expect the second invocation to exit 0 with zero file changes. | Compare the complete repository tree before and after the second invocation. | This report; temporary-copy ledger | pass — second GSS and Oraculum runs exit 0 with `fileChanges: []` and `idempotence: verified`; their exact binary-diff digests remain `35ed03fb…eb40` and `34362d66…5766`. |
| 7 | Story 2 — interactive adopted update asks only what is new | One-off maintainer; interactive `roundfix baseline`; CLI | Expect direct Plan confirmation, zero settled-decision prompts, and no ACP runtime child; decline the Plan. | Observe process creation and compare the copied tree before/after rejection. | This report; temporary-copy ledger | pass — real Oraculum announces `update` and reaches `Prompt 1: Final confirmation`; read-only process inspection shows tested PID `42089` has zero children. Decline exits 3 and `git diff --quiet HEAD --` remains clean. |
| 8 | Story 3 — repository-authored bytes remain byte-identical | Repository maintainer; update a real adopted copy; CLI | Refresh a copy containing authored prose and Repository-Specific Normative Rules. | Compare exact pre/post non-managed region and authored-block digests after reload. | This report; temporary-copy ledger | pass — exact `HEAD`/working-tree diff on real GSS shows changes only inside setup-owned marker blocks, while `AGENTS.md` and `docs/agents/specific-repository.md` are byte-identical (`git diff --exit-code`, exit 0). The second update preserves the complete diff digest. |
| 9 | Story 4 — owned and external skills refresh with guidance | Repository maintainer; drift/remove skills then update; CLI | Expect installed/restored names and a skills status independent from apply status. | Run Repository Skill Set check and inspect restored files through the repository surface. | This report; temporary-copy ledger | fail — owned-skill reinstall passes with `installedCount: 14`, but F-002 blocks a missing required external skill before restore and F-003 makes optional external restore exit 1 after guidance apply. |
| 10 | External skill upstream unreachable degrades to warning | Repository maintainer; update with unreachable immutable source; CLI | Expect guidance apply exit 0, named drift and reason, verified apply, and skills warning. | Reload manifest and skill state after the command. | This report; temporary-copy ledger | blocked (finding: F-003 — waits on repository-owned Profile restoration) — optional `humanizer` fails at Profile resolution before immutable-source acquisition, so the warning path is unreachable. |
| 11 | Story 5 — new decision stops without writes; explicit suggestion applies | Fleet maintainer; adopted repository copy; CLI | Remove one required decision; expect exact ID, exit 3, and no writes, then `--adopt-suggested --yes` applies and reports the value. | Whole-tree digest on refusal and manifest read after explicit adoption. | This report; temporary-copy ledger | pass — removing only `secondbrain.enabled` exits 3 naming exactly that ID and preserves diff digest `7f3184b3…5b874`; explicit adoption exits 0, lists the suggestion, and a fresh manifest read returns `true`. The real standard-TypeScript sweep independently exercises that Profile's decision/retention set. |
| 12 | Story 6 — unaccounted managed clause fails closed | Repository maintainer; stale adopted copy; CLI | Expect action-required refusal, named clause delta, and no writes. | Whole-tree digest plus a fresh structured result read. | This report; temporary-copy ledger | pass — Conexus, historical real Fiscus, Tax PoC, and Vortex each exit 3/category `classification`, name the same 14 unaccounted clauses with `ClauseDelta`, and report zero file changes. |
| 13 | No manifest remains first adoption | First-adoption maintainer; update then interactive Baseline; CLI | Expect update exit 3 with first-adoption direction and no writes; interactive path starts the full interview. | Whole-tree digest and observed first prompt. | This report; temporary-copy ledger | pass — real OnionCry copy exits 3/category `adoption`, directs `roundfix baseline`, remains clean, and interactive entry announces `adoption` at `Prompt 1: Instruction preservation`. |
| 14 | Hand-edited managed marker blocks safely | Repository maintainer; edit copied marker then update; CLI | Expect refusal naming the offending path and no command-added writes. | Compare the deliberate-diff tree digest before/after the command. | This report; temporary-copy ledger | pass — an edited `skill-dispatch.md` marker exits 3 with `baseline.preservation.managed-marker.modified`, names the path, and preserves deliberate-diff digest `f907d831…9386e`. |
| 15 | Confirmation-gated apply and invalid/out-of-order probes | One-off maintainer; preview/confirm CLI | Preview without confirmation, confirm exact digest, reject stale digest, and pass both confirmation forms. | Tree digests around each step and fresh JSON reads. | This report; temporary-copy ledger | pass — preview exits 3/category `approval` with digest `83275d94…1ef6` and empty-tree digest unchanged; exact digest applies at exit 0; reusing it against current digest `f7c93973…cef58` exits 3 without changing the applied-tree digest; mutually exclusive forms exit 2/category `invalid`. |
| 16 | Moved profile digest remains an update | Repository maintainer; alter copied stored digest then update; CLI | Expect update rather than adoption with no re-interview. | Reload the Setup Manifest/Profile identity after apply. | This report; temporary-copy ledger | pass — zeroing only the stored Profile digest still applies at exit 0; reload resolves `oraculum-backend` and restores Profile digest `dcc06a50…50cd8` at the current catalog. |
| 17 | Fleet-shape sweep across real copies and all named Profiles | Fleet maintainer; scripted CLI loop | Exercise nine adopted copies across three Profile identities, six prior catalog digests, and one never-adopted copy; expect branchable per-repository outcomes and stops only for genuinely new decisions. | Parse each result schema/category and inspect each copy's post-state. | This report; temporary-copy ledger | fail — F-004. GSS and three Oraculum-family copies exit 0; Conexus, historical Fiscus, Tax PoC, and Vortex exit 3 on the same 14 unaccounted clauses; Fluxus exits 2 because its repository-owned Profile no longer resolves; OnionCry exits 3/category `adoption`. Results are structured and branchable, but the promised fleet refresh cannot complete. |
| 18 | Docs journey and published examples | Documentation reader; user guide and CLI | Follow update instructions without undocumented knowledge; expect every published example to parse through the update parser. | Compare fresh CLI help and focused example-contract behavior with the guide. | `docs/user-guide/context-driven-development.md`; this report | pass — the focused `go test ./internal/cli` run for `TestBaselineDocumentationContract` and `TestBaselineExamplesParse` exits 0; fresh help agrees with the guide. F-002/F-003 concern the behavior documented behind those examples, not parsing. |
| 19 | Non-Goals and scope-creep sweep | QA operator; CLI/docs | Check first adoption unchanged, no implicit Profile change/remediation, no authored-rule re-filing, no fleet driver, unchanged immutable acquisition, and no marker repair. | Inspect help/results and copied repositories after fresh reads. | `_prd.md`; this report | pass — first adoption enters the full interview; update retains its Profile or refuses an unresolved one; marker drift and unaccounted retention refuse without repair; authored rules remain byte-identical; help remains one-repository-only and keeps the immutable/offline-source contract. |
| 20 | Pull Request observation | Maintainer; Pull Request/Git | Prompt fact says no Open Pull Request; approval/check/thread/Merge-Ready observation is unreachable. | Use local Verification, complete ancestry/scope audit, and CLI journeys as equivalent evidence where applicable. | This report | blocked (environment: no open Pull Request) — prompt fact proves the cause. Equivalent local evidence is the passing authoritative gate, complete Git ancestry/scope audit, fresh public CLI journeys, and read-only process observation; no PR state is inferred from the Run Branch. |
| 21 | Temporary-copy containment and cleanup | QA operator; filesystem/Git | Track every created `roundfix-qa-0082-*` path and delete only the exact created roots before close. | Exact-path nonexistence, prefix sweep, and worktree status must prove no copy remains. | This report, temporary-copy ledger | pass — realpath resolved the exact created root; exact deletion exited 0, exact-root nonexistence exited 0, the `/private/tmp/roundfix-qa-0082-*` sweep returned no path, and worktree status names only this report. |

## Findings

### F-002 — A missing required external skill blocks before update can restore it

- Impact: **Blocks-Completion**. A maintainer cannot use the promised one-act
  refresh to repair a repository whose required Repository Skill Set member is
  missing.
- Actor and step: move real external `context7` from an adopted Oraculum copy,
  then run update with the local offline skills source.
- Expected: guidance applies and the skills stage restores `context7` through
  preview and exact Plan confirmation.
- Actual: exit 3 before Plan/apply, category `decision`, message `required
  profile alignment is unresolved: capability.context7`; skills are `not_run`.
- History: repeated from `qa-report-2026-08-07.md`, F-002.
- Affected rows: 9.

### F-003 — External restore rejects a repository-owned Profile after guidance apply

- Impact: **Blocks-Completion**. Guidance applies, but the missing external
  skill remains absent and the documented warning/continuation contract cannot
  run.
- Actor and step: move optional external `humanizer` from an adopted Oraculum
  copy and run update.
- Expected: unreachable source becomes a named drift warning with exit 0, or a
  reachable source restores the skill.
- Actual: exit 1 after guidance apply; apply state is `verified`, category is
  `skills`, skills status is `failed`, and the error is `Unknown built-in
  Baseline Profile "oraculum-backend"`.
- History: repeated from `qa-report-2026-08-07.md`, F-003.
- Affected rows: 9 and 10.

### F-004 — The real fleet cannot reach the current catalog through update

- Impact: **Blocks-Completion**. The fleet sweep promised by the PRD cannot
  bring the standard-TypeScript repositories current.
- Actor and step: run the built update command with `--yes --no-skills
  --format json` over nine real adopted repository copies.
- Expected: each compatible adopted repository applies, or stops only for a
  genuinely new decision and names it.
- Actual: four standard-TypeScript copies stop on the same 14 unaccounted
  clauses. Fluxus separately reports its unresolved repository-owned Profile.
  Only GSS and the three valid Oraculum-family copies apply.
- History: repeated from `qa-report-2026-08-07.md`, F-004, with Fluxus now
  exposing an additional incompatible local Profile state.
- Affected rows: 17. Row 12 passes because fail-closed retention is correct;
  this finding is the unmet fleet-success promise.

## Blocked and skipped

- Row 10 is `blocked (finding: F-003 — waits on repository-owned Profile
  restoration)`. Resolve repository-owned Profiles in the restoration boundary,
  then rerun the reachable and unreachable immutable-source cases.
- Row 20 is `blocked (environment: no open Pull Request)`. The prompt proves
  the cause. The passing gate, ancestry/scope audit, CLI journeys, and process
  observation provide equivalent local evidence; no Pull Request state exists.
- No rows are declared-blocked or skipped.

## Coverage

All 21 rows are terminal: 17 pass, 2 fail, 1 environment-blocked, and 1
finding-blocked; zero are pending, declared-blocked, or skipped. User Stories
2, 3, 5, and 6 pass; Story 1 fails its fleet outcome under F-004; Story 4
fails external-skill restoration under F-002/F-003. Core Features are 8/9
passed, with Core Feature 5 failing.

The ten authored rehearsal cases close as 7 pass, 2 fail, and 1
finding-blocked. The CLI and docs surfaces, three Profile identities, six prior
catalog identities, nine adopted copies, one never-adopted copy, all Non-Goals,
the authoritative gate, Project Constraints, tooling authority, and cleanup
were exercised. Probes covered idempotent resubmission, Plan preview/reopen,
stale and mutually exclusive confirmation, missing decisions, marker edits,
missing/unreachable skills, Profile-digest drift, read-only child-process
observation, and first-adoption fallback.

Blocked counts: `rows_blocked_environment: 1`, `rows_blocked_finding: 1`,
`rows_blocked_declared: 0`. Finding impact counts: 3 Blocks-Completion, 0
Data-Loss, 0 Trust-Damage, 0 Friction, and 0 Cosmetic.

## Final verdict

**Fail.** Make required and optional external skill restoration work through
repository-owned Profiles, including the unreachable-source warning path, and
supply a retention path that lets the real standard-TypeScript fleet meet the
authored update promise; then rerun the full gate. Counts: 17 pass, 2 fail, 1
environment-blocked, 1 finding-blocked; findings are 3 Blocks-Completion.
