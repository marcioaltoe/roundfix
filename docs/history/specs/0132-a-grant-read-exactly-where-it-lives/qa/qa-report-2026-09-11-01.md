---
spec: 0132-a-grant-read-exactly-where-it-lives
date: 2026-09-11
build: 8d13355339325480e7bf52d12a1479d39c7a5590
status: closed
verdict: pass
auditing_binary: "0.12.0 (8d133553, built 2026-09-11 12:48:24 -0300)"
auditor_staleness: "current: rebuilt from audited HEAD 8d133553 before the gate"
rows_blocked_environment: 2
rows_blocked_finding: 0
rows_blocked_declared: 0
surfaces: [backend, cli]
---

# QA Report

## Performed repairs

None.

## Assigned repair failures

None.

## Authorization audit inputs

None.

## Mechanical findings

None.

## Scope and environment

Full terminal QA rerun at audited head
`8d13355339325480e7bf52d12a1479d39c7a5590` in the Run Worktree. The actors
are the maintainer reading local authority, the Supervisor invoking Spec Check,
Implement, and Settle, and the reviewer auditing committed scope. The rerun
starts with prior finding F-001 and its blocked rows, then re-observes every
remaining row on the rebuilt current tree.

The supplied Pull Request fact says no Pull Request is open for
`feat/spec-contained-authorization`; the Run Worktree branch has no Pull
Request of its own. Scratch repositories, build outputs, and Go caches use
`/private/tmp`. No credential or live network surface is required for the
product journeys.

## Static gate

- `/private/tmp/roundfix-0132-rerun-bin/roundfix spec check 0132-a-grant-read-exactly-where-it-lives --strict`: pass (exit 0); no findings and no authored Verification execution. The checker skipped absent Finding, Rollup, Backlog, Vocabulary Contract emission, and references-index artifact classes; applicable promises remain represented in the matrix.
- `rtk make verify GO=/Users/marcio/go/pkg/mod/golang.org/toolchain@v0.0.1-go1.26.7.darwin-arm64/bin/go GOFMT=/Users/marcio/go/pkg/mod/golang.org/toolchain@v0.0.1-go1.26.7.darwin-arm64/bin/gofmt GOCACHE=/private/tmp/roundfix-0132-rerun-cache BIN=/private/tmp/roundfix-0132-rerun-bin/roundfix BUILD_COMMIT=8d133553`: pass (exit 0) on the unchanged access-enabled retry; all packages, skill checks, and the rebuilt CLI passed. The sandboxed first attempt was stopped by denied read-only access to `api.github.com` inside the skill check before it produced a gate verdict.
- `rtk make verify-docs GO=/Users/marcio/go/pkg/mod/golang.org/toolchain@v0.0.1-go1.26.7.darwin-arm64/bin/go GOCACHE=/private/tmp/roundfix-0132-rerun-cache BIN=/private/tmp/roundfix-0132-rerun-bin/roundfix BUILD_COMMIT=8d133553`: pass (exit 0); documentation contracts, derived-artifact checks, the corpus budget, and every active-Spec consistency check passed.
- Disposable archive layout at scratch commit `36fe9ce`: the first identical `make verify` run failed when one live ACP adapter invocation returned a transient config-surface error and six concurrent Daemon tests timed out under fresh-clone load. All seven exact tests then passed independently in both active and archived layouts. One unchanged full-gate retry passed (exit 0), including `internal/authorization`, `internal/spec`, `internal/speccheck`, `internal/daemon`, skill checks, and the rebuilt archived-layout CLI.

## Results

| # | Story / criterion / sweep | Actor and surface | Status | Evidence |
| - | --- | --- | --- | --- |
| QA-01 | Goal 1, US-1, CF-1, and declared break 1: malformed delimiter lines refuse and name the line while valid, CRLF, missing-record, and missing-revision distinctions remain exact | Maintainer · CLI/backend | pass | The rebuilt CLI refused committed `---evil` input by name; the independent reader matrix passed every preserved distinction. |
| QA-02 | Goal 2, US-2, and CF-2: a separate Git repository holds the configured Spec Root while project paths are judged in the project repository; the default root is unchanged | Maintainer and Supervisor · CLI/Git | pass | Public text and JSON strict checks passed from a committed external Spec repository with the project grant removed; fresh resolver controls passed. |
| QA-03 | CF-3 and Task 08: citation resolution, mechanical consumption, root separation, escape refusal, and unresolved-is-not-skip behavior | Supervisor and reviewer · CLI/backend/Git | pass | Fresh real-Git citation and mechanical audit cases passed, including both escape paths and unresolved refusal. |
| QA-04 | CF-5 and outside evidence: the 2026-09-08 `authorized`-named record is discovered while unrelated Markdown remains outside the candidate set | Reviewer · Git/backend | pass | Git proves the record predates this Spec at `81a6afb4`; fresh discovery and bounded-read controls passed. |
| QA-05 | Goal 3, US-3, CF-4, and declared break 2: active-to-archive movement leaves record readers and the complete suite green, including an empty active corpus | Supervisor · CLI/backend/Git | pass | Current-tree and committed disposable archive-layout `make verify` gates passed; exact transient controls passed in both layouts. |
| QA-06 | Success metrics and regression locks: both declared breaks occur and no other characterized answer moves | Maintainer and reviewer · backend/CLI | pass | The characterization matrix and all three Task 09 Daemon journeys passed on the current build; the complete gate passed. |
| QA-07 | Tooling authority: every governed changed path is inside the five-path grant and the grant predates every consuming commit | Reviewer · Git | pass | Clean `diff-tree` inventories, ancestry checks, record-byte comparison, and repository contract cases passed. |
| QA-08 | Non-Goals: execution trust boundary, archive-traversal cost, Governed Path set, grant semantics, operation vocabulary, and Daemon status ownership remain unchanged | Reviewer · Git/backend | pass | Owner-path diffs were empty, the discovery change remained one line, and operation/status controls passed. |
| QA-09 | Public external-root operations: Implement and Settle read authority from the configured Spec Root without a duplicate project record and preserve side-effect boundaries | Supervisor · CLI/Git | blocked (environment: sandbox denies ACP Session and Run Database writes) | Direct commands stopped before mutation; current public command-runner cases and external strict checks provide equivalent observed evidence. |
| QA-10 | Vocabulary and documentation: no token is coined, refusal tokens retain their meanings, and no glossary update is required | Maintainer · CLI/docs | pass | Strict checking passed, `CONTEXT.md` stayed unchanged, and no refusal token owner changed. |
| QA-11 | Pull Request approval, checks/status, unresolved threads, Merge-Ready acceptance, and review-artifact ancestry | Maintainer · Pull Request | blocked (environment: no open Pull Request) | All five controls have exact-head equivalent evidence for `8d133553`. |

## Row evidence

### QA-01

```yaml
inputs:
  - kind: repository_path
    ref: internal/authorization/authorization.go
  - kind: repository_path
    ref: internal/authorization/authorization_test.go
```

Passed. A disposable clone committed a closing delimiter of `---evil` at
scratch commit `31b0160`. The rebuilt public CLI ran
`spec check 0132-a-grant-read-exactly-where-it-lives --strict`, exited 1, and
emitted `SC-TOOLING-UNTYPED` with `malformed YAML frontmatter delimiter line
"---evil"`.

A separate current-tree process passed `TestAuthorizationParseCharacterization`,
`TestMalformedDelimiterRefuses`,
`TestDelimiterWithTrailingCarriageReturnGrants`, and
`TestMissingAuthorizationEvidenceRemainsUnresolved`. It confirmed both
malformed positions name the line, the well-formed record grants unchanged,
CRLF markers remain valid, and `unreadable_record` remains distinct from
`unavailable_revision`.

### QA-02

```yaml
inputs:
  - kind: repository_path
    ref: internal/spec/authorization.go
  - kind: repository_path
    ref: internal/speccheck/constraints.go
  - kind: repository_path
    ref: internal/cli/implement.go
  - kind: repository_path
    ref: internal/cli/settle.go
```

Passed. A project clone at `8d133553` pointed `specs.root` at a separate Spec
repository whose committed scratch head was `4741570`. The project copy of
this Spec's `_authorization.md` was absent, and the external PRD and TechSpec
cited the adjacent record as `[_authorization.md](_authorization.md)`. The
rebuilt CLI's text and JSON strict checks both exited 0 with no findings and no
authorization skip; the JSON confirmation returned an empty `findings` array.

Fresh `TestOperationAuthorityResolvesExternalSpecRoot`,
`TestOperationAuthorityDefaultRootUnchanged`, and
`TestOperationAuthorityReportsUnresolvableSpecRoot` executions passed. They
read the external repository's committed record, preserve the default path and
revision, and keep a genuine non-Git root `unresolved`.

### QA-03

```yaml
inputs:
  - kind: repository_path
    ref: internal/speccheck/constraints.go
  - kind: repository_path
    ref: internal/speccheck/mechanical.go
  - kind: repository_path
    ref: internal/daemon/task_engine.go
```

Passed. Fresh current-head execution passed
`TestCitationResolvesInExternalSpecRoot`, both
`TestCitationRejectsEscapeFromSpecRoot` branches,
`TestMechanicalAuditConsumesResolvedReference`,
`TestMechanicalAuditJudgesTheProjectRoot`, and
`TestUnresolvedReferenceIsNotASkip`.

Those cases use distinct committed project and Spec repositories. They
observed a nonempty Task-commit audit sourced from the external record,
project-root judgment where the Task commit does not exist in the Spec
repository, refusal for upward and symlink escape, and `QA-AUTH-PATHS` rather
than a skip for an unresolved operative reference. A genuine
no-authorization declaration retained its presence-aware skip.

### QA-04

```yaml
inputs:
  - kind: repository_path
    ref: docs/specs/0122-verified-content-and-terminal-settlement/references/2026-09-08-authorized-qa-archive-override.md
  - kind: repository_path
    ref: internal/suiteguardcontract/regeneration.go
```

Passed. `git log --diff-filter=A` locates the exact outside-evidence record in
commit `81a6afb48f4a3683d0e5fad52f3919cf1bdfbbf4`, authored on
2026-09-08 before Spec 0132. A fresh committed-object read confirmed its
approved state and `make baseline-digests` declaration.

`TestDiscoveryRecognizesAuthorizedNaming` found that declaration.
`TestSanctionedRegenerationReadsOnlyCandidateRecords` preserved the bounded
read count, while every proposed, null-dated, malformed, and unrelated branch
of `TestSanctionedRegenerationRejectsNonOperativeRecords` contributed nothing.

### QA-05

```yaml
inputs:
  - kind: repository_path
    ref: docs/specs/0132-a-grant-read-exactly-where-it-lives/**
  - kind: repository_path
    ref: internal/authorization/authorization_test.go
  - kind: repository_path
    ref: internal/spec/spec_test.go
  - kind: repository_path
    ref: internal/speccheck/governed_repocontract_test.go
```

Passed. The current tree passed `make verify`. A disposable clone moved this
Spec from `docs/specs/` to `docs/history/specs/`, committed the move as scratch
commit `36fe9ce`, and passed the same complete gate on one unchanged retry.
The first archive-layout run exposed transient ACP adapter state and six
concurrency timeouts; every exact failed case passed independently in both the
active and archived layouts before the unchanged full retry passed. The result
therefore establishes the archive behavior while retaining the first-run
environment diagnostics.

### QA-06

```yaml
inputs:
  - kind: repository_path
    ref: internal/authorization/**
  - kind: repository_path
    ref: internal/spec/**
  - kind: repository_path
    ref: internal/speccheck/**
  - kind: repository_path
    ref: internal/suiteguardcontract/**
  - kind: repository_path
    ref: internal/daemon/task_engine_test.go
```

Passed. The reader, root-resolution, citation, mechanical-audit, discovery,
archive, and corpus characterization selections all passed on the current
tree. The former F-001 reproductions
`TestTaskCycleSettlesCompletedWithoutCommitWhenOnlyExternalTaskFileChanged`,
`TestTaskCycleQAReportExternalProceedsWithoutStaging`, and
`TestTaskCommitDropsSymlinkCrossingTaskFileAndCommitsRepositoryPaths` also
passed, reaching their original settlement, staging, and commit-content
assertions. The current and archived complete gates passed, so the two declared
breaks occur without an additional observed regression.

### QA-07

```yaml
inputs:
  - kind: repository_path
    ref: docs/specs/0132-a-grant-read-exactly-where-it-lives/_authorization.md
  - kind: repository_path
    ref: docs/specs/0132-a-grant-read-exactly-where-it-lives/task_*.md
  - kind: repository_path
    ref: internal/speccheck/constraints.go
  - kind: repository_path
    ref: internal/speccheck/constraints_characterization_test.go
  - kind: repository_path
    ref: internal/speccheck/governed_repocontract_test.go
  - kind: repository_path
    ref: internal/suiteguardcontract/regeneration.go
  - kind: repository_path
    ref: internal/suiteguardcontract/regeneration_test.go
```

Passed. Clean `git -c core.fsmonitor=false diff-tree --no-commit-id
--name-only -r <commit>` inventories produced these governed subsets:

- `9d1e573d`: `constraints_characterization_test.go` and
  `regeneration_test.go`;
- `eaa04070`: `regeneration.go` and `regeneration_test.go`;
- `b534bf3a`: none;
- `760c02a5`: `constraints_characterization_test.go`;
- `9ca163d9`: `constraints.go` and
  `constraints_characterization_test.go`;
- `9cbc29bf`: `constraints_characterization_test.go`;
- `1ffcc25c`: `governed_repocontract_test.go`;
- `cb03618a`: none; this commit authored Task 09 and its graph edge;
- `8d133553`: none; Task 09 changed only its own file and the ungoverned
  `internal/daemon/task_engine_test.go` fixture.

Every governed subset is inside the exact five-path grant. The separate grant
commit `fdc51a22` is an ancestor of every listed Task commit, and
`git diff --exit-code fdc51a22..HEAD -- <record>` confirmed that the operative
record bytes are unchanged. No Task declares a prerequisite or consequent
fix, and no sanctioned regeneration applies. Fresh
`TestCleanupHistoricalGrantEvidence` and the operative branches of
`TestEveryBoundedPathIsGoverned` passed.

### QA-08

```yaml
inputs:
  - kind: repository_path
    ref: docs/specs/0132-a-grant-read-exactly-where-it-lives/_prd.md
  - kind: repository_path
    ref: docs/specs/0132-a-grant-read-exactly-where-it-lives/_techspec.md
  - kind: repository_path
    ref: docs/agents/domain.md
  - kind: repository_path
    ref: internal/**
```

Passed. `git diff --exit-code 8c0422f5..HEAD` was empty for
`internal/speccheck/verification.go`, `internal/speccheck/governed.go`, and
`CONTEXT.md`. The complete `regeneration.go` diff is one line, changing the
filename candidate from `authorization` to `authoriz`; archive traversal and
the candidate filter remain present.

`TestAuthorizationReaderTypesPermittedOperations` passed and continued to
refuse `tag` and `deploy`. Four fresh Daemon status cases passed: the Daemon
writes `in_progress`, rewrites Agent-authored variants, preserves `## Result`,
and owns Verification settlement. The citation, bounded-read, and
non-operative discovery controls above preserve grant semantics and the
execution-trust exclusion.

### QA-09

```yaml
inputs:
  - kind: external_repository
    ref: disposable project and external Spec Git repositories
```

Environment-blocked with equivalent current-build evidence. The direct
Implement command used a writable scratch Artifact Directory but stopped in
Preflight when the sandbox denied a temporary write below the local ACP Session
store. The direct Settle command stopped when the sandbox denied the Roundfix
Run Database lock. Both public reports stated that no Run, Agent, commit, or
push occurred.

Fresh `TestRunImplementUsesConfiguredExternalSpecRootEndToEnd` and
`TestRunSettleUsesConfiguredExternalSpecRoot` executions passed through the
public command runners. Both use separate committed Spec repositories, omit
the project duplicate, and confirm effects through fresh file, Git, and Run
state reads. The independent public strict checks in QA-02 also resolved the
external adjacent record without a project copy. After both direct refusals,
the project remained at `8d133553` with only the planned config edit and held
project record, the external Spec repository remained clean at `4741570`, and
the scratch Artifact Directory stayed empty.

### QA-10

```yaml
inputs:
  - kind: repository_path
    ref: CONTEXT.md
  - kind: repository_path
    ref: internal/authorization/**
  - kind: repository_path
    ref: internal/speccheck/**
```

Passed. The rebuilt strict check emitted no findings. `CONTEXT.md` is
byte-unchanged from pre-implementation merge `8c0422f5`, and the Task 09 delta
does not touch any authorization or Spec-check token owner. The current
reader matrix reuses `malformed_record`, `unreadable_record`,
`unavailable_revision`, `SC-TOOLING-UNTYPED`, and `QA-AUTH-PATHS`. No domain
term was introduced, changed, or retired, so the domain close check requires
no glossary edit.

### QA-11

```yaml
inputs:
  - kind: live_service
    ref: GitHub Pull Request for feat/spec-contained-authorization
```

Environment-blocked because the supplied fact says no Pull Request is open for
the Spec target branch, and the Run Worktree branch never has one. Equivalent
controls for audited head `8d133553`:

- approval: no Pull Request acceptance was obtained;
- checks and status: strict Spec Check and the required current-tree
  `make verify` passed, and the disposable archived layout passed the same
  complete gate;
- unresolved review threads: no Pull Request exists, so no PR thread stands;
- Merge-Ready acceptance: none exists yet;
- review-artifact ancestry: no review artifact exists for this candidate; this
  QA evidence is bound directly to `8d133553`.

## Findings

None.

## Blocked and skipped

- QA-09 is blocked by the sandbox's ACP Session and Run Database write
  boundaries. A full-access session over disposable repositories would make
  the direct commands reachable; current public command-runner and independent
  repository evidence cover both operations.
- QA-11 is blocked by the supplied no-open-Pull-Request fact. All five required
  controls have equivalent exact-head evidence above; the later delivery flow
  creates the Pull Request only after archive and pre-PR review.
- No matrix row was skipped or declared-blocked. The Daemon-provided mechanical
  skips remain preserved below.

## Coverage

Completed: 11 of 11 rows covering 3 Goals, 3 User Stories, 5 Core Features, 2
declared breaks, 3 Non-Goals, the outside-evidence obligation, exact tooling
authority, both declared surfaces, and the Pull Request control journey.
Statuses: 9 pass, 2 environment-blocked with equivalent evidence, 0 fail, 0
finding-blocked, 0 declared-blocked, 0 skipped, and 0 pending. Impact totals:
0 Blocks-Completion, Data-Loss, Trust-Damage, Friction, or Cosmetic findings.

## Final verdict

Pass. Every runnable row passed, both environment-blocked rows carry their
cause and equivalent observed evidence, and no finding-blocked,
declared-blocked, skipped, failed, or pending row remains.

## Mechanical skips

| Detector | Missing artifact |
| --- | --- |
| authorization bounded paths | Task commits |
| consequent-fix commit order | consequent-fix declaration |
| QA evidence paths | docs/specs/0132-a-grant-read-exactly-where-it-lives/qa/evidence |
