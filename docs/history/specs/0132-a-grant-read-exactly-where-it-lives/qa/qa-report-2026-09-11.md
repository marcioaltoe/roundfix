---
spec: 0132-a-grant-read-exactly-where-it-lives
date: 2026-09-11
build: 1ffcc25cdfe0961bb7e5035c0300fe5741040bad
verdict: fail
auditing_binary: "0.12.0 (1ffcc25c, built 2026-09-11 12:08:42 -0300)"
auditor_staleness: "current: rebuilt from audited HEAD 1ffcc25c before the gate"
status: closed
rows_blocked_environment: 2
rows_blocked_finding: 1
rows_blocked_declared: 0
surfaces: [backend, cli]
---

# QA Report

## Performed repairs

None.

## Assigned repair failures

None.

## Authorization audit inputs

| Task | Outcome | Record | Revision | Detail |
| --- | --- | --- | --- | --- |
| task_04 | granted | docs/specs/0132-a-grant-read-exactly-where-it-lives/_authorization.md | 1c23eaeb2cd2c548637dc17cfcd6ce8e1d155d2c | operative grant |
| task_08 | granted | docs/specs/0132-a-grant-read-exactly-where-it-lives/_authorization.md | 1c23eaeb2cd2c548637dc17cfcd6ce8e1d155d2c | operative grant |
| task_06 | granted | docs/specs/0132-a-grant-read-exactly-where-it-lives/_authorization.md | 1c23eaeb2cd2c548637dc17cfcd6ce8e1d155d2c | operative grant |

## Mechanical findings

None.

## Scope and environment

Full terminal QA at audited head
`1ffcc25cdfe0961bb7e5035c0300fe5741040bad` in the Run Worktree. The actors are
the maintainer reading and granting local authority, the Supervisor invoking
Spec Check, Implement, and Settle, and the reviewer auditing committed scope.
The authored `task_07` QA node is terminal and its sole dependency, `task_06`,
is `completed`; every non-QA Task is `completed`.

The public CLI entry point is
`/private/tmp/roundfix-0132-qa-bin/roundfix`, rebuilt from audited HEAD with Go
1.26.7. Scratch Git repositories and build caches use `/private/tmp`. The
supplied Pull Request fact says no Pull Request is open for
`feat/spec-contained-authorization`; the Run Worktree branch has no Pull
Request of its own.

The Daemon's authorization inputs, empty mechanical findings, and mechanical
skips below are preserved. The matrix exercises malformed and missing records,
a separate Git repository as the resolved Spec Root, default-root compatibility,
archive movement, the historical `authorized`-named record, exact changed-file
scope, and the declared Non-Goals.

## Static gate

- `/private/tmp/roundfix-0132-qa-bin/roundfix spec check 0132-a-grant-read-exactly-where-it-lives --strict`: pass (exit 0); no findings and no authored Verification execution. The checker skipped absent Finding, Rollup, Backlog, Vocabulary Contract emission, and references-index artifact classes, which remain covered below where applicable.
- `GOCACHE=/private/tmp/roundfix-0132-qa-cache make GO=/Users/marcio/go/pkg/mod/golang.org/toolchain@v0.0.1-go1.26.7.darwin-arm64/bin/go GOFMT=/Users/marcio/go/pkg/mod/golang.org/toolchain@v0.0.1-go1.26.7.darwin-arm64/bin/gofmt BIN=/private/tmp/roundfix-0132-qa-bin/roundfix BUILD_COMMIT=1ffcc25c verify`: fail (exit 2). The first identical attempt was environment-blocked at the read-only `api.github.com` skill check; the access-enabled rerun reached the full suite and failed three `internal/daemon` cases because their external Spec Root fixtures are not Git repositories. All other packages passed.
- `GOCACHE=/private/tmp/roundfix-0132-qa-cache make GO=/Users/marcio/go/pkg/mod/golang.org/toolchain@v0.0.1-go1.26.7.darwin-arm64/bin/go GOFMT=/Users/marcio/go/pkg/mod/golang.org/toolchain@v0.0.1-go1.26.7.darwin-arm64/bin/gofmt BIN=/private/tmp/roundfix-0132-qa-bin/roundfix BUILD_COMMIT=1ffcc25c verify-docs`: pass (exit 0); documentation contracts, repository regeneration contracts, corpus budget, build, and the full active-Spec check completed.

## Results

| # | Story / criterion / sweep | Actor and surface | Status | Evidence |
| - | --- | --- | --- | --- |
| QA-01 | Goal 1, US-1, CF-1, and declared break 1: malformed delimiter lines refuse and name the offending line while well-formed, CRLF, missing-record, and missing-revision distinctions remain exact | Maintainer · CLI/backend | pass | Public strict checking refused `---evil` by name; a fresh reader matrix independently passed all valid, malformed, CRLF, missing-record, and missing-revision cases. |
| QA-02 | Goal 2, US-2, and CF-2: a separate Git repository can hold the configured Spec Root while authority is read there and project paths are judged in the project repository; the default root is unchanged | Maintainer and Supervisor · CLI/Git | pass | Public strict checking passed from the external root with the project grant removed; fresh operation-resolution cases passed for external, default, and unresolvable roots. |
| QA-03 | CF-3 and Task 08: the citation resolves against its carrying artifact, the mechanical audit consumes that exact reference, escape stays refused, project-root judgment stays separate, and unresolved input is not skipped | Supervisor and reviewer · CLI/backend/Git | pass | Fresh real-Git audit cases passed for external citation, project-root judgment, upward/symlink escape, unresolved refusal, and genuine no-authorization skip. |
| QA-04 | CF-5 and outside evidence: the record written on 2026-09-08 under the prior `authorized` naming is discovered while unrelated Markdown remains outside the candidate set | Reviewer · Git/backend | pass | Git proves the record predates this Spec at `81a6afb4`; fresh discovery found its sanctioned command while read-count and non-operative controls passed. |
| QA-05 | Goal 3, US-3, CF-4, and declared break 2: moving this Spec from active to archived layout leaves record readers and the suite green, including an empty active corpus | Supervisor · CLI/backend/Git | blocked (finding: F-001 — waits on `make verify`) | The unchanged-layout full gate is already red in three external-Spec-root Daemon journeys, so an archive-layout green claim cannot be established on this build. |
| QA-06 | Success metrics and regression locks: both declared breaks occur and no other characterized parse, resolution, discovery, default-root, or archive answer moves | Maintainer and reviewer · backend/CLI | fail | F-001 is an undeclared regression: three pre-existing non-Git external-Spec-root Daemon journeys now refuse before their original settlement/staging observable. |
| QA-07 | Tooling authority: every governed changed path is inside the five-path grant, the grant precedes each consuming commit, and no authorization, prerequisite, or consequent repair is folded into the wrong commit | Reviewer · Git | pass | Clean `git diff-tree` inventories bound every governed edit to the five-path grant; `fdc51a22` is a separate ancestor of all seven Task commits and the grant is unchanged at HEAD. |
| QA-08 | Non-Goals: the execution trust boundary, residual archive-traversal cost, Governed Path set, grant semantics, operation vocabulary, and Daemon Task-status ownership remain unchanged | Reviewer · Git/backend | pass | Git leaves the trust and governed-set sources unchanged; focused operation, status-ownership, and candidate-read canaries passed, and the discovery diff is the one-line stem change. |
| QA-09 | Public external-root operations: Implement dispatch and Settle read the grant from the configured Spec Root without a duplicate project record and preserve refusal/no-side-effect behavior | Supervisor · CLI/Git | blocked (environment: sandbox denies the local Roundfix Run Database write lock) | Public Implement passed authority and reached the later default-branch refusal with no side effects. Public Settle was blocked before authority; fresh public-runner cases provide equivalent external-root evidence for both commands. |
| QA-10 | Vocabulary and documentation detector: no token was coined, existing refusal tokens retain their meanings, and no glossary update is required | Maintainer · CLI/docs | pass | The source diff adds no token declaration, `CONTEXT.md` is byte-unchanged, and strict checking classified the absent Vocabulary Contract as a skip rather than a finding. |
| QA-11 | Pull Request approval, checks/status, unresolved threads, Merge-Ready acceptance, and review-artifact ancestry controls | Maintainer · Pull Request | blocked (environment: no open Pull Request) | All five controls have exact-head equivalent observations: no PR acceptance, failing local Verification, no PR threads, no Merge-Ready acceptance, and no review artifact for HEAD `1ffcc25c`. |

## Row evidence

### QA-01

```yaml
inputs:
  - kind: repository_path
    ref: internal/authorization/authorization.go
  - kind: repository_path
    ref: internal/authorization/authorization_test.go
```

Passed. In a disposable clone, the rebuilt public CLI read a closing `---evil`
line through `spec check ... --strict`, exited 1, and emitted
`SC-TOOLING-UNTYPED` with `malformed YAML frontmatter delimiter line
"---evil"`. The working-tree check did not need the attempted fixture commit,
which the sandbox refused because the user's GPG key store is outside its
writable boundary.

The independent current-build reader command passed ten cases across
`TestAuthorizationParseCharacterization`, `TestMalformedDelimiterRefuses`,
`TestDelimiterWithTrailingCarriageReturnGrants`, and
`TestMissingAuthorizationEvidenceRemainsUnresolved`. It proved both malformed
positions name the offending line, the well-formed record grants unchanged,
CRLF markers remain valid, and `unreadable_record` stays distinct from
`unavailable_revision`.

### QA-02

```yaml
inputs:
  - kind: repository_path
    ref: internal/spec/authorization.go
  - kind: repository_path
    ref: internal/speccheck/constraints.go
  - kind: repository_path
    ref: internal/cli/implement.go
  - kind: repository_path
    ref: internal/cli/settle.go
```

Passed. A project clone pointed `specs.root` at a second committed Git clone and
removed its own copy of this Spec's `_authorization.md`. The rebuilt CLI ran
`spec check 0132-a-grant-read-exactly-where-it-lives --strict` from the project,
exited 0, and reported no findings. Its authorization-discovery detector noted
that the project-local record was absent, while the cited external operative
record still satisfied the strict constraint row.

Fresh `internal/spec` execution independently passed
`TestOperationAuthorityResolvesExternalSpecRoot`,
`TestOperationAuthorityDefaultRootUnchanged`, and
`TestOperationAuthorityReportsUnresolvableSpecRoot`. The cases read the
external repository's committed HEAD, preserve the default path and revision,
and classify a non-Git root as `unresolved` rather than granted or refused.

### QA-03

```yaml
inputs:
  - kind: repository_path
    ref: internal/speccheck/constraints.go
  - kind: repository_path
    ref: internal/speccheck/mechanical.go
  - kind: repository_path
    ref: internal/daemon/task_engine.go
```

Passed. The fresh current-head `internal/speccheck` selection passed
`TestCitationResolvesInExternalSpecRoot`, both
`TestCitationRejectsEscapeFromSpecRoot` subtests,
`TestMechanicalAuditConsumesResolvedReference`,
`TestMechanicalAuditJudgesTheProjectRoot`, and
`TestUnresolvedReferenceIsNotASkip`.

Those cases use separate committed Git repositories. They observed a non-empty
Task-commit audit from the external record, an out-of-grant project path that
could not be found in the Spec repository, refusal for upward and symlink
escape, refusal rather than skip for an unresolved operative citation, and the
preserved presence-aware skip only for a genuine no-authorization declaration.

### QA-04

```yaml
inputs:
  - kind: repository_path
    ref: docs/specs/0122-verified-content-and-terminal-settlement/references/2026-09-08-authorized-qa-archive-override.md
  - kind: repository_path
    ref: internal/suiteguardcontract/regeneration.go
```

Passed. Git locates the exact source at
`docs/specs/0122-verified-content-and-terminal-settlement/references/2026-09-08-authorized-qa-archive-override.md` in commit
`81a6afb48f4a3683d0e5fad52f3919cf1bdfbbf4`, authored on
2026-09-08 before Spec 0132. A fresh committed-object read confirms its
approved record and `make baseline-digests` sanctioned regeneration.

`TestDiscoveryRecognizesAuthorizedNaming` found that exact declaration.
`TestSanctionedRegenerationReadsOnlyCandidateRecords` preserved the bounded
read count, and all proposed, null-dated, malformed, and unrelated cases in
`TestSanctionedRegenerationRejectsNonOperativeRecords` contributed nothing.

### QA-05

```yaml
inputs:
  - kind: repository_path
    ref: docs/specs/0132-a-grant-read-exactly-where-it-lives/**
  - kind: repository_path
    ref: internal/authorization/authorization_test.go
  - kind: repository_path
    ref: internal/spec/spec_test.go
  - kind: repository_path
    ref: internal/speccheck/governed_repocontract_test.go
```

Blocked by F-001. The unchanged-layout full gate fails before an archive-layout
comparison can establish the required suite-green observable. Unblocking
requires the corrective slice to reconcile the three Daemon fixtures with the
committed external-Spec-root contract, then rerun the full gate before and after
the disposable archive move.

Narrow layout evidence remains positive but cannot replace the failed full
gate. In a disposable clone, fresh executions of
`TestRecordDiscoveredInEitherRoot`, `TestRepositorySpecCorpusStillLoads`, and
`TestEveryBoundedPathIsGoverned` passed before the move and passed again after
the same Spec directory was committed under
`docs/history/specs/0132-a-grant-read-exactly-where-it-lives/`. The corpus case
also passed its archived-only and unreadable-root branches; the governed case
retained its intentional empty-record skip.

### QA-06

```yaml
inputs:
  - kind: repository_path
    ref: internal/authorization/**
  - kind: repository_path
    ref: internal/spec/**
  - kind: repository_path
    ref: internal/speccheck/**
  - kind: repository_path
    ref: internal/suiteguardcontract/**
```

Failed. The full gate and the isolated public-engine reproduction
`TestTaskCycleSettlesCompletedWithoutCommitWhenOnlyExternalTaskFileChanged`
both refuse at `ReadSpecAuthorization`: `useExternalSpecRoot` writes a plain
directory, while commit `760c02a5` changed `taskCycleFixture.plan` to pass that
directory into a resolver that requires `git rev-parse --show-toplevel`.
Working CLI external-root fixtures use `newExternalSpecsRoot`, which initializes
and commits a separate Git repository. The mismatch also breaks
`TestTaskCycleQAReportExternalProceedsWithoutStaging` and
`TestTaskCommitDropsSymlinkCrossingTaskFileAndCommitsRepositoryPaths` before
their settlement or staging assertions.

### QA-07

```yaml
inputs:
  - kind: repository_path
    ref: docs/specs/0132-a-grant-read-exactly-where-it-lives/_authorization.md
  - kind: repository_path
    ref: docs/specs/0132-a-grant-read-exactly-where-it-lives/task_*.md
  - kind: repository_path
    ref: internal/speccheck/constraints.go
  - kind: repository_path
    ref: internal/speccheck/constraints_characterization_test.go
  - kind: repository_path
    ref: internal/speccheck/governed_repocontract_test.go
  - kind: repository_path
    ref: internal/suiteguardcontract/regeneration.go
  - kind: repository_path
    ref: internal/suiteguardcontract/regeneration_test.go
```

Passed. Clean `git -c core.fsmonitor=false diff-tree --no-commit-id --name-only
-r <commit>` reads produced these governed subsets:

- `9d1e573d` (task_01): `constraints_characterization_test.go` and
  `regeneration_test.go`;
- `eaa04070` (task_05): `regeneration.go` and `regeneration_test.go`;
- `b534bf3a` (task_02): no governed path;
- `760c02a5` (task_03): `constraints_characterization_test.go`;
- `9ca163d9` (task_04): `constraints.go` and
  `constraints_characterization_test.go`;
- `9cbc29bf` (task_08): `constraints_characterization_test.go`;
- `1ffcc25c` (task_06): `governed_repocontract_test.go`.

Every subset is inside the exact five-path record. The intervening
`1c23eaeb` split changed only `_tasks.md`, `task_04.md`, and new `task_08.md`.
`git merge-base --is-ancestor fdc51a22 <task-commit>` exited 0 for every Task
commit; the grant commit is separate from all implementation commits and
`git diff --exit-code fdc51a22 HEAD -- <record>` confirms identical operative
bytes. No Task declares a prerequisite or consequent repair, no sanctioned
regeneration applies, and the preserved mechanical skip records the absent
consequent-fix declaration.

Fresh repository-contract execution passed
`TestCleanupHistoricalGrantEvidence` and the operative-record cases in
`TestEveryBoundedPathIsGoverned`; its intentionally empty-corpus subtest kept
its existing skip.

### QA-08

```yaml
inputs:
  - kind: repository_path
    ref: docs/specs/0132-a-grant-read-exactly-where-it-lives/_prd.md
  - kind: repository_path
    ref: docs/specs/0132-a-grant-read-exactly-where-it-lives/_techspec.md
  - kind: repository_path
    ref: docs/agents/domain.md
  - kind: repository_path
    ref: internal/**
```

Passed. The clean path diff from pre-implementation merge `8c0422f5` through
audited HEAD contains neither `internal/speccheck/verification.go` nor
`internal/speccheck/governed.go`; an exit-code comparison over those two paths
confirmed no byte change. The `regeneration.go` diff is exactly one line,
changing the filename candidate from `authorization` to `authoriz`; it does not
remove the candidate filter or change the archive traversal.

The authorization implementation diff changes only complete-line delimiter
parsing. Fresh `TestAuthorizationReaderTypesPermittedOperations` execution kept
the closed vocabulary and refused `tag` and `deploy`. Four Daemon status cases
passed, proving the Daemon still writes `in_progress`, normalizes Agent-authored
variants, preserves `## Result`, and owns final verification and settlement.
The bounded-read and non-operative discovery controls recorded under QA-04
cover the remaining cost and grant-semantics exclusions. The strict check's
Finding, Rollup, and Backlog skips correspond to empty live directories; direct
directory reads confirmed no artifact exists whose lifecycle or relocation
could change in this Spec.

### QA-09

```yaml
inputs:
  - kind: repository_path
    ref: internal/cli/implement.go
  - kind: repository_path
    ref: internal/cli/settle.go
  - kind: repository_path
    ref: internal/spec/authorization.go
```

Blocked by the sandbox before the direct Settle journey could read authority.
The public Implement command used the separate committed Spec repository with
the project record removed, passed its `implement` authority check, and reached
the later default-branch refusal. It exited 2 with its documented no-side-effect
report; project HEAD `54fcde44` and a clean status were unchanged after a fresh
reread.

The direct Settle probe selected the external failed Task but could not create
`/Users/marcio/.roundfix/roundfix.db.lock`; it exited 2 before authority and left
project HEAD/status and external Spec HEAD/status byte-for-byte unchanged. A
full-access retry was rejected because Settle is mutation-capable. Equivalent
current-head evidence passed in `TestRunSettleUsesConfiguredExternalSpecRoot`
and `TestRunImplementUsesConfiguredExternalSpecRootEndToEnd`; both public command
runners use separate committed Spec repositories with project duplicates
removed and assert the expected effects through fresh reads.

### QA-10

```yaml
inputs:
  - kind: repository_path
    ref: CONTEXT.md
  - kind: repository_path
    ref: internal/authorization/**
  - kind: repository_path
    ref: internal/speccheck/**
```

Passed. `git diff --exit-code 8c0422f5..HEAD -- CONTEXT.md` exited 0. The
production diff adds no `SC-*` or `AuthorizationReason*` declaration; it reuses
`malformed_record`, `unreadable_record`, `unavailable_revision`, and
`SC-TOOLING-UNTYPED`. The rebuilt strict check emitted no vocabulary finding
and explicitly skipped the Vocabulary Contract detector because this TechSpec
declares that no token is coined. No term was introduced, changed, or retired,
so the domain close check requires no glossary edit. The skipped references
index is also inapplicable: the Spec has no `references/` directory, and every
Markdown file link in its Task files targets the present `_authorization.md`;
the outside-evidence path in QA-04 was read directly from committed Git.

### QA-11

```yaml
inputs:
  - kind: live_service
    ref: GitHub Pull Request for feat/spec-contained-authorization
```

Blocked because the supplied fact says no Pull Request is open for the Spec
target branch, and the Run Worktree branch never has one. Equivalent controls:

- approval: no Pull Request acceptance was obtained;
- checks and status: strict Spec Check passed at HEAD `1ffcc25c`, while the
  required `make verify` failed F-001 on the same head;
- unresolved review threads: no Pull Request exists, so no PR thread stands;
- Merge-Ready acceptance: none exists yet;
- review-artifact ancestry: no review artifact exists for this candidate; the
  claimed QA evidence is bound directly to audited HEAD `1ffcc25c`.

Unblocking requires correcting F-001, obtaining a passing exact-head
Verification, and creating the later Pull Request after the normal archive and
pre-PR review steps.

## Findings

### F-001 — Daemon external-Spec-root fixtures refuse before settlement

- Impact: Trust-Damage.
- Actor and step: Supervisor, assembled full-gate regression at the Daemon
  external-Spec-root boundary.
- Expected: existing external Task-file, external QA-report, and symlinked
  Spec-root journeys reach their settlement/staging assertions.
- Actual: all three refuse Implement or commit authority because the fixture
  Spec Root is a plain directory and cannot supply the Git provenance now
  required by `ReadSpecAuthorization`.
- Reproduction: run `GOCACHE=/private/tmp/roundfix-0132-qa-debug-cache /Users/marcio/go/pkg/mod/golang.org/toolchain@v0.0.1-go1.26.7.darwin-arm64/bin/go test -count=1 ./internal/daemon -run '^TestTaskCycleSettlesCompletedWithoutCommitWhenOnlyExternalTaskFileChanged$'`; it exits 1 with `git rev-parse --show-toplevel: fatal: not a git repository`.
- Root cause: commit `760c02a5` correctly threads `fixture.specsRoot` into the
  authorization resolver but leaves `useExternalSpecRoot` and the symlink
  fixture non-Git. The production external-root helper used by CLI journeys
  creates a committed repository, so the test data contract diverged at this
  boundary.
- Affected rows: QA-05 and QA-06.

## Blocked and skipped

- QA-05 is blocked by F-001 until `make verify` passes on the same build and the
  archive-layout comparison can run.
- QA-09 is blocked by the sandboxed Run Database lock. Unblocking requires a
  supervised full-access Settle run against the disposable repositories; the
  current-head public-runner equivalent evidence passed.
- QA-11 is blocked by the supplied no-open-Pull-Request fact; all required
  equivalent controls are recorded above.

## Coverage

Completed: 11 of 11 rows covering 3 Goals, 3 User Stories, 5 Core Features, 2
declared breaks, 3 Non-Goals, the outside-evidence obligation, exact tooling
authority, both declared surfaces, and the Pull Request control journey.
Statuses: 7 pass, 1 fail, 1 finding-blocked, 2 environment-blocked, 0
declared-blocked, 0 skipped, 0 pending. Impact totals: 1 Trust-Damage; 0
Blocks-Completion, Data-Loss, Friction, or Cosmetic.

## Final verdict

Fail. Correct F-001 at the shared Daemon external-Spec-root fixture boundary,
then rerun `make verify`, the archive-layout comparison, and one adjacent
public Implement/Settle canary before repeating this gate.

## Mechanical skips

| Detector | Missing artifact |
| --- | --- |
| consequent-fix commit order | consequent-fix declaration |
| QA Report structure | QA Report |
| QA evidence paths | QA Report |
