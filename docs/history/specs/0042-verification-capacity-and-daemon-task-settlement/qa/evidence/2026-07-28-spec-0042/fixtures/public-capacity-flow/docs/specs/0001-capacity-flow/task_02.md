---
task: task_02
spec: 0001-capacity-flow
status: pending
type: backend
---

# Task 02: Second public capacity slice

## Verification

- `printf 'start task_02\n' >> "$QA_COORD_DIR/verification.log"; sleep 0.25; printf 'end task_02\n' >> "$QA_COORD_DIR/verification.log"` — expected: the complete real shell command runs under Daemon Verification.
