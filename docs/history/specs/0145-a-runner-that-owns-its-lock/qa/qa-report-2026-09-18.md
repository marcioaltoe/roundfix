---
spec: 0145-a-runner-that-owns-its-lock
date: 2026-09-18
build: c2410cf51235dc08b7b0c66435c76d1a9be7c7c2
auditing_binary: "0.14.1 (1ee8d8b1, built 2026-09-18 13:54:21 -0300)"
auditor_staleness: "stale: commit ancestry: build commit predates audited tree"
status: closed
verdict: pass
rows_blocked_environment: 1
rows_blocked_finding: 0
rows_blocked_declared: 0
surfaces: [backend]
---

# QA Report

## Scope and environment

Full authored QA scope ran against commit
`c2410cf51235dc08b7b0c66435c76d1a9be7c7c2` on the Run Worktree branch. The
actor was a Roundfix maintainer using the package's normal Go analyzer and test
entry points. The unchanged comparison tree was target-branch commit
`1ee8d8b19778ece840baa4fd32cd757cbf0c8610`, extracted from Git into a scratch
directory. No credentials or live service were required.

The authored graph names `task_02` as its terminal QA node, and its only
dependency, `task_01`, is `completed`. The strict precondition passed:

```text
$ roundfix spec check 0145-a-runner-that-owns-its-lock --strict
Spec 0145-a-runner-that-owns-its-lock
No findings. Authored Verification commands were not executed.
```

The auditing binary predates the audited commit, as the preserved mechanical
header records. That staleness does not replace or weaken the live checks below.

## Static gate

The Daemon ran the repository Verification outside this Agent sandbox. Per the
QA prompt, it is recorded as a supplied gate fact and was not rerun:

```text
$ make verify
exit 0
diagnostics: removed on success
```

Focused live checks on the audited build:

```text
$ env GOCACHE=<scratch>/gocache-current go vet -copylocks ./internal/agent
exit 0; no output

$ env GOCACHE=<scratch>/gocache-tests go test -count=1 ./internal/agent
ok  roundfix/internal/agent  1.943s

$ rg -n '^func \(runner ACPXRunner\)' internal/agent
exit 1; no matches

$ git diff --check 1ee8d8b19778ece840baa4fd32cd757cbf0c8610..c2410cf51235dc08b7b0c66435c76d1a9be7c7c2
exit 0; no output
```

## Performed repairs

None.

## Assigned repair failures

None.

## Authorization audit inputs

None.

## Mechanical findings

None.

## Results

| # | Status | Provenance |
| - | --- | --- |
| Q1 | pass | Requirement 1; repository Verification |
| Q2 | pass | Requirement 2; Requirement 5; outside-evidence row |
| Q3 | pass | Requirement 3 |
| Q4 | pass | Requirement 4 |
| Q5 | pass | Requirement 6 |
| Q6 | blocked (environment: no open Pull Request) | Pull Request row |

## Mechanical skips

| Detector | Missing artifact |
| --- | --- |
| authorization bounded paths | tooling authorization |
| consequent-fix commit order | consequent-fix declaration |
| QA Report structure | QA Report |
| QA evidence paths | QA Report |

## Row evidence

### Q1

- Actor and surface: maintainer, repository verification entry point.
- Steps and expected observable: the Daemon runs `make verify`; the command
  must exit zero.
- Independent confirmation and persistence: the Daemon supplied exit `0` for
  the audited Run and removed diagnostics on success. The QA contract forbids
  rerunning this command in the Agent sandbox, so this row records that gate
  fact once.
- Evidence: the Repository Verification block in the QA prompt and the exact
  result recorded under `Static gate`.

### Q2

- Actor and surface: maintainer, `go vet -copylocks ./internal/agent`.
- Steps and expected observable: run the analyzer first on unchanged commit
  `1ee8d8b1`, then on audited commit `c2410cf5`; observe 31 diagnostics before
  and zero after.
- Independent confirmation and persistence: the unchanged-tree command exited
  `1` with 31 lines, all containing `passes lock by value`. The current-tree
  command exited `0` with no output. Extracting and sorting the 31 method names
  from the unchanged analyzer output and the 31 removed value-receiver
  declarations produced an empty `diff -u`, proving that the recorded
  diagnostics are the receiver changes in this Spec.
- Outside evidence: Spec 0123's owned finding at
  `docs/specs/0123-runtime-readiness-and-model-capabilities/references/2026-09-08-acpx-runner-value-receivers-copy-its-mutex.md`
  predates this Spec and records the same 31-diagnostic command result. The
  fresh replay against the unchanged Git commit independently reproduced it.

### Q3

- Actor and surface: maintainer, repository source scan.
- Steps and expected observable: search all changed agent-package source for
  `func (runner ACPXRunner)`; expect no match.
- Independent confirmation and persistence: `rg` exited `1` with no matches,
  and the independent current-tree `go vet -copylocks` run exited `0` with no
  diagnostics. Reopening the committed diff shows 31 receiver declarations
  changed from `ACPXRunner` to `*ACPXRunner`.
- Evidence: the receiver scan and analyzer output recorded above.

### Q4

- Actor and surface: maintainer, package test entry point and committed Git
  diff.
- Steps and expected observable: run the full package tests once without
  cache reuse, then inspect every source and test change from `1ee8d8b1` to
  `c2410cf5`.
- Independent confirmation and persistence: `go test -count=1
  ./internal/agent` passed. The zero-context diff contains receiver-kind edits,
  one redundant address-of removal, and eight address-of additions at
  non-addressable runner literals. It contains no assertion edit and no added,
  removed, or renamed exported identifier. Re-reading the committed diff after
  the test run confirmed the same source-only state.
- Evidence: package test output and `git diff --unified=0
  1ee8d8b1..c2410cf5 -- 'internal/agent/*.go'`.

### Q5

- Actor and surface: maintainer, Spec checker and Git changed-path audit.
- Steps and expected observable: run the strict Spec checker and resolve every
  path in the Task commit with `git diff-tree`; expect no promise-rule finding
  and no governed tooling path.
- Independent confirmation and persistence: the strict check returned `No
  findings`. `git diff-tree --no-commit-id --name-only -r c2410cf5` listed the
  assigned Task file and nine ordinary files under `internal/agent`; no linter,
  analyzer, Verification, build, package-manager, generator, plugin, ignore,
  or version-pin file changed. `git diff --check` also passed.
- Evidence: strict-check output, Task commit `c2410cf5`, and its ten resolved
  changed paths.

### Q6

- Actor and surface: maintainer, Pull Request controls.
- Steps and expected observable: inspect approval, checks and status,
  unresolved review threads, Merge-Ready acceptance, and review-artifact
  ancestry for the Spec target branch.
- Environment cause: the QA prompt states that no Pull Request is open and
  that Pull Request journeys are environment-blocked. The Run Worktree branch
  is never pushed and has no Pull Request of its own.
- Equivalent supervised evidence: no acceptance has been obtained; the Daemon
  reports `make verify` passed; no Pull Request exists on which a review thread
  could stand; no Review Source Merge-Ready acceptance exists yet; and no
  review artifact exists whose ancestry could be bound to `c2410cf5`. These
  facts come from the Supervisor's QA context and the Daemon's supplied
  Verification result.
- Unblocking action: after the Daemon integrates the Run into
  `feat/0145-runner-lock`, open the Spec Pull Request and observe those controls
  on its actual head.

## Findings

None.

## Blocked and skipped

Q6 is environment-blocked because no Pull Request exists at the authored QA
stage. Its five equivalent control facts are recorded in the row. No row is
finding-blocked, declared-blocked, or skipped. The mechanical detector skips
above remain preserved as materialized by the Daemon.

## Coverage

- Declared Requirement sources: 6 of 6 covered; all passed.
- Nonwaivable sources: repository Verification passed; outside evidence passed;
  Pull Request row environment-blocked with equivalent supervised evidence.
- Rows: 5 passed, 0 failed, 1 environment-blocked, 0 finding-blocked, 0
  declared-blocked, 0 skipped, 0 pending.
- Unreachable Acceptance declarations: none.
- Frontend sweep: not applicable; the PRD declares only `backend`.
- Provenance closure: Requirements 1-6, the outside-evidence row, the Pull
  Request row, and repository Verification each appear in at least one row;
  no other source appears in row provenance.

## Final verdict

Pass: every runnable criterion passed, the sole environment-blocked row records
its cause and equivalent supervised evidence, and no finding-blocked,
declared-blocked, skipped, failed, or pending row remains.
