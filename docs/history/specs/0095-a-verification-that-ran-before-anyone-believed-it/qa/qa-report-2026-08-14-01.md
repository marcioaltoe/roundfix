---
spec: 0095-a-verification-that-ran-before-anyone-believed-it
date: 2026-08-14
build: 0748172313d2fad6d6e447635f20dc94ab43a0ab
status: closed
verdict: pass
rows_blocked_environment: 0
rows_blocked_finding: 0
rows_blocked_declared: 0
surfaces: [backend, cli, docs]
---

# QA report — A Verification that ran before anyone believed it

## Scope and environment

Full rerun of the authored terminal `task_09` gate on Run Worktree build
`0748172313d2fad6d6e447635f20dc94ab43a0ab`. The actor is a Supervisor
authoring a Task Graph or a maintainer auditing the resulting repository. The
backend surface has no HTTP entry point; users reach it through the public
`roundfix spec check` CLI. The prompt reports no open Pull Request, and the Spec
declares no Pull Request journey. The PRD has no `## Unreachable Acceptance`
declarations.

`_tasks.md` names `task_09` as the terminal `qa` node and makes it depend on
every non-QA leaf. `task_03`, `task_06`, `task_07`, and `task_08` are completed.
The current QA Task remains Daemon-owned and is failed from the prior gate while
this rerun is in progress.

Scratch repositories and the operator binary will live under the managed
temporary directory. No credential, network service, database, deployment, or
customer data is required. High-risk probes cover a mixed honest/vacuous Task,
an unavailable command, omission of the opt-in flag, multiple requested Specs,
invalid shell forms beside permitted counterparts, a missing declared output
beside a missing input, command-local temporary state, repeat execution, and
tooling authorization chronology.

## Static gate

The authoring precondition passed before the matrix was opened:

```text
rtk roundfix spec check 0095-a-verification-that-ran-before-anyone-believed-it --strict
exit: 0
Spec 0095-a-verification-that-ran-before-anyone-believed-it
No findings.
Skipped:
  SC-REF-UNRESOLVED: missing docs/specs/0095-a-verification-that-ran-before-anyone-believed-it/references/_index.md
```

The skipped reference detector is retained in row Q-07 rather than credited as
equivalent coverage.

## Mechanical findings

None.

## Mechanical skips

| Detector | Missing artifact |
| --- | --- |
| authorization bounded paths | Task commits |
| consequent-fix commit order | consequent-fix declaration |

These daemon-materialized skips are preserved. Row Q-08 performs the
commit-backed tooling audit from repository Git evidence; no Task declares a
consequent fix.

## Results

| # | Story / criterion / sweep | Actor, entry point, and surface | Expected observable and independent confirmation | Status |
| - | --- | --- | --- | --- |
| Q-01 | Story 1: every authored command executes against `HEAD` before a Run | Supervisor; `roundfix spec check --run-verification`; CLI/backend | A mixed honest/vacuous graph names both verdicts, `HEAD`, and exits non-zero; an honest-only graph exits zero; unknown execution is distinct; a second invocation confirms stable results. | pass |
| Q-02 | Story 2: the authoring contract states exit-zero semantics and usable empty-result forms | Supervisor; canonical authoring Skill; docs/CLI | The canonical and generated Skill copies name the rule and three working forms; each form executes with its documented exit behavior; the sync check confirms persistence. | pass |
| Q-03 | Story 3: a Task can declare a file it creates without hiding a mistyped input | Supervisor; `roundfix spec check`; CLI/backend | A nonexistent `creates:` output is accepted while a nonexistent `interface:` remains `SC-REF-UNRESOLVED`; a corrected input passes on a fresh invocation. | pass |
| Q-04 | Story 4 and Core Feature 3: non-hermetic Verification forms are refused | Supervisor; `roundfix spec check`; CLI/backend | An undeclared variable, presence guard, and external path are each named by `SC-VERIFY-NON-HERMETIC`; command-local state and Task-created paths are spared on a fresh read. | pass |
| Q-05 | Core Features 2 and 4: inverted-exit and vacuity refusals reject measured forms and spare correct counterparts | Supervisor; `roundfix spec check`; CLI/backend | All three inverted forms and a pre-work-vacuous command are refused; `grep -q`, compared `test`, and honest pre-work commands remain accepted; text and JSON independently agree. | pass |
| Q-06 | External Acceptance Evidence: authored graphs outside Spec 0095 expose what a clean static check missed | Supervisor; repository-authored Specs other than 0095 through `--run-verification`; CLI/backend | The report names each external Spec checked and its per-command verdicts; this Spec's own fixtures or prose cannot satisfy the row. | pass |
| Q-07 | Coined refusal vocabulary and skipped reference fallback satisfy the domain contract | Maintainer; strict checker, `CONTEXT.md`, and Git tree; docs/CLI | Both coined codes resolve in the glossary, no undocumented-vocabulary finding exists, and the missing reference index corresponds to no adopted source. | pass |
| Q-08 | Protected-tooling change obeys exact authorization scope, sanctioned regeneration, and chronology | Maintainer; Git ancestry and real commit paths; docs/infra | Every Task commit path is read from Git; Task 08 stays within its exact canonical path plus assigned Task and byte-identical sanctioned mirror; authorization ancestry and current delta are valid. | pass |
| Q-09 | Non-Goals and scope boundaries remain intact | Maintainer/Supervisor; public CLI, Git range, and Daemon canary; CLI/backend/docs | Opt-in omission executes nothing; the checker reports rather than invents Verification content; Daemon settlement and QA semantics do not change; refusal remains measured rather than a general shell linter; only the bounded authoring Skill changed. | pass |
| Q-10 | Repository Verification and docs boundary pass on the assembled build | Maintainer; `make verify` and `make verify-docs`; backend/CLI/docs | Both unpiped repository gates exit zero; any environment denial is recorded with equivalent current-build evidence rather than retried. | pass |
| Q-11 | QA Report closes with exact row counts, evidence, and no committed scratch artifacts | Roundfix Daemon; report and Git tree; docs | Every row is terminal; verdict and typed blocked counts match; report evidence resolves; no scratch repository, built binary, or gitlink is tracked. | pass |

## Detailed evidence

Each row below records current-build evidence from its public or operational
entry point.

### Q-01

The current-build binary `/tmp/roundfix-0095-qa-roundfix` ran from committed
scratch repository `/tmp/roundfix-0095-qa.PAkoQF`:

- `spec check exec-mixed --run-verification` exited 1 and reported
  `Verification tree: HEAD`, the committed `docs/agents/domain.md` predicate as
  `vacuous`, and the absent Task output as `honest`.
- `spec check exec-honest --run-verification` exited 0 with two `honest`
  commands and no finding.
- `spec check exec-unknown --run-verification` exited 1 and reported the missing
  executable as `unknown` with exit 127, never as honest or vacuous.
- `spec check no-opt-in` exited 0 and printed `Verification: not run (use
  --run-verification).`; `qa-should-not-exist` remained absent.
- A two-Spec invocation emitted two `Verification tree: HEAD` sections and all
  four expected command records. Repeating the mixed invocation reproduced the
  same verdicts, and `git status --short` remained empty after all probes.

### Q-02

`rtk rg -n -C 4` found the exit-zero rule and all three copyable forms on line
85 of both `.agents/skills/write-tasks/SKILL.md` and its generated
`skills/write-tasks/SKILL.md` mirror. `rtk cmp -s` confirmed the copies are
byte-identical, and `rtk make skills-sync-check` exited 0. In the scratch
repository, concrete substitutions of the documented `test -z`, `! grep -rq`,
and redirected-output-plus-`|| { cat ...; exit 1; }` forms each exited 0 for an
empty or absent result. Reopening the generated mirror through the sync check
confirmed persistence independently of the canonical read.

### Q-03

The public checker exited 0 for `creates-clean`, where
`generated/client.go` was declared under `creates:` but did not exist. It exited
1 for `creates-bad`, naming only `missing/guide.md` as
`SC-REF-UNRESOLVED`; the same Task also declared the absent generated output.
After the scratch repository committed `missing/guide.md`, a fresh
`spec check creates-bad` exited 0 with no finding while
`generated/client.go` remained absent. This proves the exemption is limited to
declared outputs and persists across a fresh Git `HEAD`.

### Q-04

`spec check non-hermetic` exited 1 with exactly three
`SC-VERIFY-NON-HERMETIC` findings. The public text named undeclared variable
`TASK_04_BASELINE`, presence guard `VORTEX_POSTGRES_INTEGRATION_URL`, and
external path `/tmp/task-before.tree`, with a specific repair for each.
`spec check hermetic` exited 0 for a command-local assignment, a temporary
directory created before it was read, and a redirected file created before its
grep. A six-Spec JSONL invocation independently returned three typed findings
for `non-hermetic` and an empty finding list for `hermetic`.

### Q-05

`spec check inverted` exited 1 with three distinct
`SC-VERIFY-INVERTED-EXIT` findings: `grep -c` count-and-exit,
`grep -v ... | wc -l`, and `test $(...)` without a comparison. Each report
named a working replacement. `inverted-correct` exited 0 for `grep -q` and two
explicit comparisons. `vacuous` exited 1 and named
`SC-VERIFY-VACUOUS-COMMAND` for `git diff --name-only HEAD | cat`, while
`nonvacuous` exited 0 for the adjacent `... | grep -q .` predicate. JSONL
repeated all refusal identities and returned empty finding lists for all three
correct-counterpart Specs.

### Q-06

Evidence origin is the authored Task Graphs under
`docs/history/specs/0093-a-spec-that-validates-itself/` and
`docs/history/specs/0094-one-history-root-under-docs/`. Both predate Spec 0095
and are outside its artifacts. A separate clone copied each archived graph into
the active root because the public checker intentionally rejects archived
status; `_tasks.md` and every `task_*.md` remained byte-identical to their
historical sources, proved by `rtk cmp -s` across both sets.

- Spec 0093's ordinary public check reported `No findings.`. The same graph
  with `--run-verification` exited 1 and classified all 33 authored commands:
  25 vacuous and 8 honest against `HEAD`, with no unknown result. Examples
  include already-present citation tests, source tokens, Skill text, QA report
  globs, cache cleaning, and `make verify`.
- Spec 0094's execution mode classified all 63 authored commands: 36 vacuous
  and 27 honest, with no unknown result. Its current ordinary check also
  reported three unrelated glossary findings after the archived graph was
  reactivated, so this row does not describe that graph as currently
  checker-clean. A second complete invocation reproduced the same 36/27/0
  counts.

This independent corpus evidence exceeds the PRD's six-defect success metric:
the execution mode found 61 commands already satisfied before work across two
graphs that Spec 0095 did not author.

```yaml
inputs:
  - kind: repository_path
    ref: docs/history/specs/0093-a-spec-that-validates-itself/_tasks.md
  - kind: repository_path
    ref: docs/history/specs/0093-a-spec-that-validates-itself/task_*.md
  - kind: repository_path
    ref: docs/history/specs/0094-one-history-root-under-docs/_tasks.md
  - kind: repository_path
    ref: docs/history/specs/0094-one-history-root-under-docs/task_*.md
  - kind: repository_path
    ref: internal/cli/spec_check.go
  - kind: repository_path
    ref: internal/daemon/verification_probe.go
```

### Q-07

The current-build strict JSON command exited 0 with an empty finding list and
only the declared missing-reference-index skip. `CONTEXT.md:187` defines
Inverted Verification Exit and its stable `SC-VERIFY-INVERTED-EXIT` token;
`CONTEXT.md:191` defines Non-Hermetic Verification and
`SC-VERIFY-NON-HERMETIC`. The worktree has no Spec `references/` directory and
`git ls-tree -r HEAD` returned no adopted reference path, so the skip does not
hide an unresolved source. The glossary check found that the two coined terms
are already present and the existing Vacuous Verification term remains valid;
no domain-context update is needed in this rerun.

### Q-08

Task 08 is the only Task that changed protected repository tooling. Fresh
`git diff-tree --no-commit-id --name-only -r 28acf39c` resolved exactly the
canonical `.agents/skills/write-tasks/SKILL.md`, assigned `task_08.md`, and
generated `skills/write-tasks/SKILL.md` mirror. Authorization commit
`385b5f85` predates `28acf39c`; the pre-Task authorization names Spec 0095,
bounds the canonical file, and states that mirrors rewritten by
`make skills-sync` are sanctioned ADR-0081 fallout. The Task-commit canonical
and mirror blobs are byte-identical, the same paths have no later byte delta,
and current `rtk make skills-sync-check` passes.

Correction `07481723` changes only the authorization record. It adds the parsed
`## Sanctioned regeneration` representation after the first QA report exposed
that the audit could not read the already-granted prose; it does not add a new
Task path or modify the protected output. The seeded mechanical rerun therefore
reports no finding. There is no declared prerequisite or consequent-fix commit,
and the only current repository delta is this untracked seeded QA Report.

### Q-09

The no-flag Q-01 probe executed nothing and reported that omission. Every
execution record reproduced the author-written command verbatim rather than
selecting content. A deliberately unmeasured shell form, `false || true`,
received no static finding and exited 0 without the flag; the opt-in execution
mode then classified it vacuous and exited 1. This proves the static detector
remains bounded to measured forms while execution closes the wider gap.

Fresh `-count=1` Daemon canaries ran nine pre-work and settlement tests and all
passed, including vacuous refusal before Agent work, ordinary handling of an
honest failure, and unknown-result settlement. The original
`task_engine_test.go` has no diff from `origin/main`. Git range inspection found
no change to either QA Skill copy, and the only Skill paths changed by this Spec
are the authorized canonical `write-tasks` Skill and its generated mirror.

### Q-10

Unpiped `rtk make verify` exited 0. It ran all Go packages, including
`internal/baseline` in 73.156 seconds, `internal/cli` in 98.154 seconds,
`internal/spec` in 46.107 seconds, `internal/speccheck` in 6.730 seconds, and
`internal/store` in 45.706 seconds; the focused Skill suite, public
`roundfix skills check`, and binary build also passed.

Unpiped `rtk make verify-docs` exited 0. The docs contract, measured sanctioned
ownership, declared regeneration/frozen-boundary, owned-Skill byte-identity,
corpus-budget, binary build, and active-corpus Spec checks all passed. Spec 0095
reported `No findings.` with only the already-audited missing-reference-index
skip. Neither gate encountered an environment denial.

### Q-11

The closed report records 11 pass rows, zero failures, zero blocked rows of any
type, zero skipped rows, and zero pending rows. Frontmatter records
`verdict: pass` with all three blocked counters at 0. Repository status names
only this seeded QA Report; both scratch repositories and the QA binary remain
under `/tmp`, and no Spec QA path is tracked as a gitlink. The report names no
separate evidence file, so there is no unresolved evidence path.

## Findings

None.

## Blocked and skipped

None. Every planned row ran. The prompt confirms no Pull Request is open, but
the PRD declares no Pull Request acceptance journey, so no product row depends
on that unavailable surface. The daemon's two preserved mechanical skips are
closed by Q-08 or are inapplicable because no consequent fix is declared.

## Coverage

All 11 rows passed. Counts are `rows_blocked_environment: 0`,
`rows_blocked_finding: 0`, and `rows_blocked_declared: 0`; no row failed or was
skipped. All four user stories, six Core Features, five Goals/Success Metrics,
the outside-evidence acceptance, glossary closure, tooling audit, Non-Goals,
and both repository gates have current evidence.

The external corpus classified 96 authored commands from two earlier Task
Graphs and found 61 vacuous before work. Behavior probes covered mixed verdicts,
honest-only success, unknown execution, flag omission, multiple Specs, missing
output versus missing input, all six measured static forms and their correct
counterparts, command-local state, an unmeasured shell form, repeated execution,
Git chronology, sanctioned regeneration, and unchanged Daemon/QA behavior.
There is no frontend, TUI, HTTP, data, live-service, credential, deployment, or
Pull Request surface in this Spec.

## Final verdict

Pass: every runnable story and criterion passed on build `07481723`; there are
no findings, blocked rows, declared-blocked rows, or skipped rows. Result: 11
pass, 0 fail, 0 blocked, 0 skipped.
