---
spec: 0136-a-rename-the-committer-can-stage
date: 2026-09-14
build: 896c4ad2699592653afa68bb422a2cc3f777509c
auditing_binary: "roundfix 0.12.0 (896c4ad2-dirty, built 2026-09-14 11:58:38 -0300)"
auditor_staleness: "current: binary and public-flow artifact were rebuilt from audited HEAD 896c4ad2"
status: closed
verdict: pass
rows_blocked_environment: 1
rows_blocked_finding: 0
rows_blocked_declared: 0
surfaces: [backend, cli]
---

# QA Report — A rename the committer can stage

The rebuilt CLI commits staged and unstaged renames, keeps deletions
stageable, preserves governed-mutation refusals, pushes ordinary changes
without unused authority, and reports resolvable authorization paths. The
corrective governed-rename journey now refuses final push and leaves its bare
remote unchanged.

## Scope and environment

Full terminal rerun for Tasks 01–03, corrective Task 05, and PRD Goals 1–3.
The actor is a Roundfix operator using a binary rebuilt from HEAD `896c4ad2`
against disposable local Git repositories. The run uses Git 2.55.0 and the
repository-pinned Go 1.26.7 toolchain. No credential, production data, or user
repository participates in a functional fixture.

The authored terminal node is `task_04`; it depends on completed `task_01`,
`task_02`, `task_03`, and `task_05`. The rebuilt binary's
`spec check 0136-a-rename-the-committer-can-stage --strict` precondition exited
zero with no findings. The checker skipped its absent-artifact lifecycle,
backlog, vocabulary-documentation, and reference-index detectors; obligations
those skips did not decide remain in the matrix. The PRD has no Unreachable
Acceptance declaration.

Pull Request fact from the QA assignment: no Pull Request is open for target
branch `feat/spec-contained-authorization`, and Pull Request journeys are
environment-blocked. Equivalent control state is recorded in R-13. The Run
Worktree branch was not used to resolve a Pull Request.

The committed evidence harness stores fake executable adapters without their
executable bits. The first attempt therefore stopped at public preflight with
exit 2 and no side effects. The functional rerun used a byte-identical
`/private/tmp` harness copy with executable bits restored only on the three
adapter programs. The failed setup attempt and the parity adjustment are both
preserved as evidence.

## Static gate

- `rtk ./bin/roundfix spec check
  0136-a-rename-the-committer-can-stage --strict` exited 0 with no findings.
- `rtk make verify
  GO=/Users/marcio/go/pkg/mod/golang.org/toolchain@v0.0.1-go1.26.7.darwin-arm64/bin/go
  GOFMT=/Users/marcio/go/pkg/mod/golang.org/toolchain@v0.0.1-go1.26.7.darwin-arm64/bin/gofmt`
  passed on one unchanged authorized retry after the sandbox stopped the first
  process at `api.github.com` without a command exit status. Every Go package,
  the focused Skills suite, `roundfix skills check`, and the binary build
  passed.
- Go 1.26.7 `vet ./internal/daemon ./internal/cli ./internal/spec
  ./internal/worktree` exited 0 with empty output after the ambient Go cache
  returned `operation not permitted` and the unchanged check used the writable
  private temporary cache.
- Fresh focused regressions for staged and unstaged rename, deletion,
  unfiltered classification, final-push authority, prior changed paths,
  unresolved authorization paths, operation vocabulary, and changed-path
  auditing all exited 0. The CLI-focused command required one unchanged
  network-enabled retry at its existing GitHub boundary.

Evidence: [static gates](evidence/2026-09-14-run-aa3f4fef-rerun/static-gates.txt).

## Performed repairs

None.

## Assigned repair failures

None.

## Authorization audit inputs

None.

## Mechanical findings

None.

## Results

| # | Status | Provenance |
| - | --- | --- |
| R-01 | pass | Repository `make verify` on pinned Go 1.26.7 |
| R-02 | pass | Scoped `go vet` on every package changed by the Spec |
| R-03 | pass | Staged `git mv` through rebuilt `roundfix implement`; committed A/D reread |
| R-04 | pass | Unstaged rename through rebuilt `roundfix implement`; committed A/D reread |
| R-05 | pass | Tracked deletion through rebuilt `roundfix implement`; committed deletion reread |
| R-06 | pass | Governed rename without `implement`; stable public refusal and no commit |
| R-07 | pass | Executable drop, real-repository staging, and unfiltered classification regressions |
| R-08 | pass | Ordinary auto-push without `push` operation; local and bare-remote heads match |
| R-09 | pass | Direct governed edit and governed rename both refuse final push; remotes remain unchanged |
| R-10 | pass | Public unresolved-location failures plus typed reason and outcome checks |
| R-11 | pass | Outside Git 2.55.0 pathspec measurement |
| R-12 | pass | Intentional breaks, vocabulary, scope boundaries, and Non-Goals |
| R-13 | blocked (environment: no open Pull Request) | Assignment fact and complete equivalent-control state |
| R-14 | pass | Report closure, exact counters, zero pending/skipped rows, and evidence-path reread |

## Mechanical skips

| Detector | Missing artifact |
| --- | --- |
| authorization bounded paths | tooling authorization |
| consequent-fix commit order | consequent-fix declaration |

## Findings

None. Prior report F-001 is resolved at this build: the same governed-rename
public journey now reaches the missing-`push` refusal and does not advance its
remote.

## Detailed evidence

### R-01

Actor and entry point: maintainer through the repository gate with the pinned
Go 1.26.7 toolchain. Expected and observed: formatting, every Go package, the
shipped Skills checks, and the binary build exited clean. A fresh
`./bin/roundfix --version` reread reported
`roundfix 0.12.0 (896c4ad2-dirty, built 2026-09-14 11:58:38 -0300)`.

```yaml
inputs:
  - kind: repository_path
    ref: Makefile
  - kind: repository_path
    ref: "**/*.go"
  - kind: repository_path
    ref: .agents/skills/**
  - kind: repository_path
    ref: skills/**
```

### R-02

Actor and entry point: maintainer running pinned `go vet` over
`internal/daemon`, `internal/cli`, `internal/spec`, and `internal/worktree`,
the four implementation packages changed by this Spec. The command exited 0
with empty output. No diagnostic needed the Spec 0123/0124 ownership exception.

```yaml
inputs:
  - kind: repository_path
    ref: internal/daemon/**
  - kind: repository_path
    ref: internal/cli/**
  - kind: repository_path
    ref: internal/spec/**
  - kind: repository_path
    ref: internal/worktree/**
```

### R-03

Actor and entry point: operator running rebuilt `roundfix implement` over a
fresh work branch whose Agent performed `git mv before.txt after.txt`.
Expected and observed: the command exited 0, the Daemon logged that the absent
source was omitted from staging, and the Task committed. An independent
user-checkout reread reported `A after.txt`, `D before.txt`, completed Task
metadata, and empty porcelain status.

Evidence: [stderr](evidence/2026-09-14-run-aa3f4fef-rerun/observed/staged_rename/stderr.txt),
[changed paths](evidence/2026-09-14-run-aa3f4fef-rerun/observed/staged_rename/changed.txt),
and [status](evidence/2026-09-14-run-aa3f4fef-rerun/observed/staged_rename/status.txt).

```yaml
inputs:
  - kind: repository_path
    ref: internal/daemon/daemon.go
  - kind: repository_path
    ref: internal/daemon/task_engine.go
  - kind: repository_path
    ref: internal/daemon/daemon_test.go
  - kind: external_repository
    ref: disposable Git repository recorded under observed/staged_rename
```

### R-04

The operator repeated the public flow with plain `mv` in a fresh repository.
`roundfix implement` exited 0, emitted no dropped-path event, and the
independent checkout reread found the source deletion, destination addition,
completed Task metadata, and clean status.

Evidence: [stderr](evidence/2026-09-14-run-aa3f4fef-rerun/observed/unstaged_rename/stderr.txt),
[changed paths](evidence/2026-09-14-run-aa3f4fef-rerun/observed/unstaged_rename/changed.txt),
and [status](evidence/2026-09-14-run-aa3f4fef-rerun/observed/unstaged_rename/status.txt).

```yaml
inputs:
  - kind: repository_path
    ref: internal/daemon/task_engine.go
  - kind: external_repository
    ref: disposable Git repository recorded under observed/unstaged_rename
```

### R-05

The operator removed tracked `before.txt` without staging it first.
`roundfix implement` exited 0, created the Task commit, and the independent
commit reread reported `D before.txt`; porcelain status was empty. The result
persists in the user branch after the Run Worktree closes.

Evidence: [changed paths](evidence/2026-09-14-run-aa3f4fef-rerun/observed/delete/changed.txt)
and [status](evidence/2026-09-14-run-aa3f4fef-rerun/observed/delete/status.txt).

```yaml
inputs:
  - kind: repository_path
    ref: internal/daemon/task_engine.go
  - kind: external_repository
    ref: disposable Git repository recorded under observed/delete
```

### R-06

The governed-rename probe staged `git mv Makefile notes.txt` under a record
that permitted `commit` but omitted `implement`. Verification passed, then the
public CLI exited 1 with the unchanged
`authorization operation "implement" is not permitted` diagnostic. A fresh
user-checkout reread remained on the seed head with no Task commit and clean
status.

Evidence: [stdout](evidence/2026-09-14-run-aa3f4fef-rerun/observed/governed_rename/stdout.txt),
[stderr](evidence/2026-09-14-run-aa3f4fef-rerun/observed/governed_rename/stderr.txt),
and [head](evidence/2026-09-14-run-aa3f4fef-rerun/observed/governed_rename/head.txt).

```yaml
inputs:
  - kind: repository_path
    ref: internal/daemon/task_engine.go
  - kind: repository_path
    ref: internal/speccheck/governed.go
```

### R-07

Fresh Go 1.26.7 focused checks passed the existing executable-mode drop,
real-repository staging, unfiltered snapshot classification, operation
vocabulary, and authoritative changed-path audit assertions. The full
repository suite passed their packages again. This independently re-observed
the adjacent contracts after corrective Task 05.

```yaml
inputs:
  - kind: repository_path
    ref: internal/daemon/daemon_test.go
  - kind: repository_path
    ref: internal/daemon/task_engine_test.go
  - kind: repository_path
    ref: internal/authorization/**
  - kind: repository_path
    ref: internal/speccheck/**
```

### R-08

An ordinary Task changed `ordinary.txt` under a record with `implement` and
`commit`, but no `push`. The rebuilt CLI completed the Task, executed the
separate final push, exited 0, and printed
`pushed origin/feat/qa-ordinary_push`. The bare remote independently resolved
to the same SHA as the fresh local head.

Evidence: [stdout](evidence/2026-09-14-run-aa3f4fef-rerun/observed/ordinary_push/stdout.txt),
[head](evidence/2026-09-14-run-aa3f4fef-rerun/observed/ordinary_push/head.txt),
and [remote head](evidence/2026-09-14-run-aa3f4fef-rerun/observed/ordinary_push/remote-head.txt).

```yaml
inputs:
  - kind: repository_path
    ref: internal/cli/implement.go
  - kind: external_repository
    ref: disposable local bare remote recorded under observed/ordinary_push
```

### R-09

The direct control modified governed `Makefile` under the same no-`push`
record. The Task committed, final push exited 1 with the unchanged
missing-`push` refusal, and the bare remote stayed on the seed SHA while the
local branch advanced.

The former F-001 journey then renamed governed `Makefile` to ordinary
`notes.txt`. The committed diff independently reported `D Makefile` and
`A notes.txt`; final push exited 1 with the same refusal; the local head
advanced to `8bd36237`, while the bare remote remained on seed `24b02c54`.
This is the required persistence check and proves both sides of the rename
reach the final-push authority decision.

Evidence: [direct-edit stderr](evidence/2026-09-14-run-aa3f4fef-rerun/observed/governed_modify_push/stderr.txt),
[direct-edit remote](evidence/2026-09-14-run-aa3f4fef-rerun/observed/governed_modify_push/remote-head.txt),
[rename stderr](evidence/2026-09-14-run-aa3f4fef-rerun/observed/governed_push/stderr.txt),
[rename paths](evidence/2026-09-14-run-aa3f4fef-rerun/observed/governed_push/changed.txt),
[rename local head](evidence/2026-09-14-run-aa3f4fef-rerun/observed/governed_push/head.txt),
and [rename remote head](evidence/2026-09-14-run-aa3f4fef-rerun/observed/governed_push/remote-head.txt).

```yaml
inputs:
  - kind: repository_path
    ref: internal/cli/implement.go
  - kind: repository_path
    ref: internal/worktree/worktree.go
  - kind: external_repository
    ref: disposable local bare remotes recorded under observed/governed*_push
```

### R-10

Two public CLI flows injected failures only at the authorization resolver's
Git boundary. An unavailable revision kept the Spec-root-relative record path
`docs/specs/0001-rename-fixture/_authorization.md`; an unresolvable Spec Root
reported the absolute path beneath that configured root. Both exited 1,
remained Unresolved, created no Task commit, and retained clean checkout state.
Fresh package tests retained `unavailable_revision`/`revision` and
`unreadable_record`/`spec_root`.

Evidence: [unavailable revision](evidence/2026-09-14-run-aa3f4fef-rerun/observed/unavailable_revision/stdout.txt),
[unavailable head](evidence/2026-09-14-run-aa3f4fef-rerun/observed/unavailable_revision/head.txt),
[unresolvable root](evidence/2026-09-14-run-aa3f4fef-rerun/observed/unresolvable_spec_root/stdout.txt),
and [unresolvable head](evidence/2026-09-14-run-aa3f4fef-rerun/observed/unresolvable_spec_root/head.txt).

```yaml
inputs:
  - kind: repository_path
    ref: internal/spec/authorization.go
  - kind: repository_path
    ref: internal/spec/authorization_test.go
```

### R-11

Outside evidence came from Git 2.55.0 in two repositories the Spec did not
build. `git add -f -- before.txt` exited 128 with `pathspec ... did not match`
after `git mv`; the same source-path command exited 0 after plain `mv`, where
the source remained in the index. This is Git's measured behavior rather than
a premise authored by the Spec.

Evidence: [Git measurement](evidence/2026-09-14-run-aa3f4fef-rerun/git-pathspec.txt).

```yaml
inputs:
  - kind: external_repository
    ref: disposable Git 2.55.0 pathspec measurements recorded in git-pathspec.txt
```

### R-12

R-03, R-08, and R-10 observed all three declared intentional breaks. R-06
and R-09 retained refusal text and operation conditions. Fresh tests retained
the operation vocabulary and Governed Path predicate. Commit-by-commit
`git diff-tree` rereads found only the declared ordinary source, test, and Task
paths; no Task touched external or symlinked Spec Root identity policy, grant
semantics, the Governed Path set, operation vocabulary, or Daemon-owned Task
status. Corrective Task 05 changed the prior-changed reader only for the
Spec-authored final-push seam.

Evidence: [scope audit](evidence/2026-09-14-run-aa3f4fef-rerun/scope-audit.txt).

```yaml
inputs:
  - kind: repository_path
    ref: internal/authorization/**
  - kind: repository_path
    ref: internal/speccheck/governed.go
  - kind: repository_path
    ref: internal/worktree/worktree.go
  - kind: repository_path
    ref: internal/daemon/**
  - kind: repository_path
    ref: internal/cli/**
  - kind: repository_path
    ref: internal/spec/**
```

### R-13

The assignment proves no Pull Request is open for target branch
`feat/spec-contained-authorization`; the Run Worktree branch was not queried.
Equivalent control state is complete: no approval was obtained; local
`make verify` passed for HEAD `896c4ad2`; no Pull Request means no unresolved
review thread stands; no Merge-Ready acceptance or review artifact exists yet;
and no review-artifact ancestry is claimed. Opening the target branch's Pull
Request and observing those controls unblocks the journey. This environmental
row does not prevent pass because its cause and every equivalent control are
recorded.

```yaml
inputs:
  - kind: live_service
    ref: GitHub Pull Request for feat/spec-contained-authorization
```

### R-14

The closed report carries one `verdict: pass`, exact typed counters, fourteen
terminal rows, and no pending or skipped QA row. `git diff --check` exits 0,
all Markdown evidence links resolve to regular files, and the report preserves
the seeded mechanical sections and skips.

Evidence: [harness setup](evidence/2026-09-14-run-aa3f4fef-rerun/harness-setup.txt).

```yaml
inputs:
  - kind: repository_path
    ref: docs/specs/0136-a-rename-the-committer-can-stage/qa/qa-report-2026-09-14-01.md
  - kind: repository_path
    ref: docs/specs/0136-a-rename-the-committer-can-stage/qa/evidence/2026-09-14-run-aa3f4fef-rerun/**
```

## Blocked and skipped

R-13 is blocked by the assignment's proved absence of an open Pull Request.
Its exact unblocking action and equivalent observed or supervised control
state are recorded in the row. No QA row was skipped. The preserved mechanical
skips above are detector facts from the Daemon-seeded report and do not stand
in for acceptance evidence.

The initial harness preflight stop is not a blocked acceptance row: it occurred
before Run creation, made no side effect, and the same public flow completed
after executable bits were restored in a temporary copy. Evidence remains
under [initial attempt](evidence/2026-09-14-run-aa3f4fef-rerun/results/governed_push/stderr.txt)
and [harness setup](evidence/2026-09-14-run-aa3f4fef-rerun/harness-setup.txt).

## Coverage

- Task 01 criteria map: staged rename → R-03; unstaged rename → R-04;
  deletion → R-05; governed rename refusal → R-06; existing drops and
  real-repository staging → R-07.
- Task 02 criteria map: ordinary final push → R-08; governed final-push
  refusal → R-09; separate explicit final push → R-08 and its Run journal.
- Task 03 criteria map: Spec-Root-relative record paths, unchanged typed reason
  fields, and unresolved outcomes → R-10.
- Task 05 criteria map: both rename paths and governed-rename final-push
  refusal → R-09; ordinary push → R-08; direct governed-edit refusal → R-09.
- PRD Goals: Goal 1 → R-03 through R-07; Goal 2 → R-08 and R-09; Goal 3 →
  R-10.
- Task 01–03 Acceptance Criteria: 11 of 11 pass.
- Corrective Task 05 Acceptance Criteria: 4 of 4 pass.
- PRD Goals: 3 of 3 pass.
- Planned rows: 13 pass and 1 environment-blocked; zero failed, pending,
  finding-blocked, declared-blocked, or skipped rows.
- Probes: staged and unstaged rename, deletion, governed implement refusal,
  ordinary push, direct governed-edit push refusal, governed-rename push
  refusal, unavailable revision, and unresolvable Spec Root.
- Non-Goals: external and symlinked Spec Root identity, Governed Paths, grant
  semantics, operation vocabulary, and Daemon-owned Task status were checked
  against committed diffs and fresh adjacent regressions.

## Final verdict

Pass: 13 runnable rows pass, and R-13 is environment-blocked by the confirmed
absence of an open Pull Request with its cause, unblocking action, and complete
equivalent-control state recorded; no row failed, was skipped, or is blocked
by a finding or declaration.
