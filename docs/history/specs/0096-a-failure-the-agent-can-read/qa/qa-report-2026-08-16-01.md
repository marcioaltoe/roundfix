---
spec: 0096-a-failure-the-agent-can-read
date: 2026-08-16
build: f09e521b76e97877027c31b418bc330fcfa36d4a
status: closed
verdict: pass
rows_blocked_environment: 0
rows_blocked_finding: 0
rows_blocked_declared: 0
surfaces: [backend, docs]
---

# QA report — A failure the Agent can read

## Scope and environment

This full rerun exercises the terminal `task_08` QA node after corrective
`task_09`, against commit `f09e521b76e97877027c31b418bc330fcfa36d4a` on
the isolated Run Worktree branch
`roundfix/run-run_20260816T175818Z_92fc4ad2bce84de2`. The intended actors are an
Agent receiving Verification Feedback, a Supervisor reading Run Events and
recovery output, and a maintainer reading the authoring and configuration
surfaces. No Pull Request is open, and the Spec declares no Pull Request
journey.

`rtk roundfix spec check 0096-a-failure-the-agent-can-read --strict` exited 0
with no findings. It skipped `SC-REF-UNRESOLVED` because this Spec has no
`references/_index.md`; QA-10 retains the corresponding literal-path audit.

## Static gate

`rtk make verify` reached the repository Go suite and exited 2 at `make test`.
Every package passed except two `internal/cli` force-stop integration tests:
`TestRunForceStopLegacyRunWithoutOwnerIdentityStillStopsOwner` and
`TestRunForceStopOwnerProcessIntegrationProvesExitBeforeStoreCompletion`.
Both failed while enumerating the owner process tree because the sandbox
returned `operation not permitted`; the error names the denied host operation,
and the only change since the previous QA head is `internal/daemon/engine.go`
plus `task_09.md`. This is an environment-caused static-gate gap. A full-access
session with process-table visibility is the exact unblocking action. Neither
test covers this Spec's declared backend or documentation journeys.

The same full run passed `internal/agent`, `internal/config`, `internal/daemon`,
`internal/runevent`, and every other package. The prior nondeterministic
`TestACPXRunWarmSessionIsIdempotent` failure did not recur in the full gate and
then passed five fresh focused repetitions.

## Performed repairs

None.

## Assigned repair failures

None.

## Mechanical findings

None.

## Results

| # | Story / criterion / sweep | Actor and surface | Status | Evidence |
| - | --- | --- | --- | --- |
| QA-01 | Story 1 / Core Feature 1 — empty diagnostics say the command produced no output, name a redirect target when present, and preserve non-empty feedback | Agent; backend Task and Batch repair flows | pass | Production Batch and Task repair cycles passed; prior F-001 regressions passed with the verifier cause restored |
| QA-02 | Story 2 / Core Feature 2 / outside evidence — a matching failure is named in the Run Event and Agent prompt; first and pruned failures stay new | Supervisor and Agent; backend plus external Finding | pass | Four production-cycle cases and seven signature probes passed; outside Finding §4 remains readable |
| QA-03 | Story 3 / Core Feature 3 — the authoring contract names both corrective-ceiling exits and keeps the choice inside the loop's authority | Supervisor; docs | pass | Canonical and shipped skills name both exits, the two-Task ceiling, and no-human continuation; copies match |
| QA-04 | Core Feature 4 — recovery output names the Task and Task-file surface on one line without changing its other fields | Supervisor; backend recovery output | pass | Production Task cycle passed the same-line and unchanged-fields cases |
| QA-05 | Core Feature 5 — a vacuity refusal names offenders, carries every probed command with verdict and probe log, and preserves classification and phase | Supervisor; backend Run Event | pass | Production Task cycle passed summary, payload, probe-log, classification, and phase cases |
| QA-06 | Core Feature 6 — rendered config explains what `max_run_duration` bounds and when it is evaluated, and remains loadable | Maintainer; product config/docs surface | pass | Built command created and reloaded a fresh project config carrying the budget contract; focused loader test passed |
| QA-07 | Non-Goals — repair count, ceiling value, diagnostic handling, verdict semantics, Task settlement, and measured commands do not regress | Maintainer; backend and docs | pass | Corrective diff is limited to the shadow fix and task record; focused regressions pass and policy values remain unchanged |
| QA-08 | Vocabulary close — `Repeated Failure` remains in `CONTEXT.md` with the semantics emitted by the Daemon | Maintainer and future Agent; docs | pass | Glossary, Agent prompt, and Run Event use the same canonical term and earlier-Run/attempt semantics |
| QA-09 | Commit-dependent tooling audit — task_07 follows bounded authority, regeneration, and chronological ancestry | Maintainer; Git/tooling boundary | pass | Task commit contains the bounded canonical skill, Task file, and sanctioned mirror; grant precedes it; sync check passes |
| QA-10 | Strict-check skip retention — active Spec repository paths resolve despite skipped `SC-REF-UNRESOLVED` | Maintainer; docs | pass | Explicit audit resolved all 30 operative, implementation, external-evidence, skill, and ADR paths |

## Detailed evidence

### QA-01

```yaml
inputs:
  - kind: repository_path
    ref: internal/agent/**
  - kind: repository_path
    ref: internal/daemon/**
```

`rtk env GOCACHE=/tmp/roundfix-0096-qa-rerun-gocache go test -count=1
./internal/daemon ./internal/cli -run
'^(TestResolveCycleVerificationFailureRepairsSameSessionAndAvoidsDuplicateBatchStart|TestTaskCycleVerificationFailureRepairsSameSessionAndRerunsFullSequence|TestResolveCycleVerificationFailureFailsBatchAndContinues|TestRunOutcomeDerivedFromUnresolvedIssues|TestRunResolveVerificationFailureDoesNotCommit|TestRepeatedFailure|TestDiagnosticSignature|TestRecoveryNamesTheSurface|TestVacuousPreWorkEvent)$'
-v` exited 0.
Both zero-byte diagnostic journeys returned to the same Agent Session and
completed. The adjacent non-empty canaries that previously reported
`verification failed: <nil>` now passed, including the public Resolve journey;
the failure cause is again retained.

### QA-02

```yaml
inputs:
  - kind: repository_path
    ref: internal/agent/**
  - kind: repository_path
    ref: internal/daemon/**
  - kind: repository_path
    ref: internal/runevent/**
  - kind: external_repository
    ref: fiscus Spec 0004 Task task_07 Runs observed on 2026-08-08 and recorded in docs/findings/2026-08-12-five-unresolved-runs-to-deliver-one-spec.md
```

The outside record
`docs/findings/2026-08-12-five-unresolved-runs-to-deliver-one-spec.md` §4 remains
readable and records two consecutive Fiscus `task_07` Runs with the same
`timestamptz` assertion, noticed only by comparing diagnostic files. The same
focused command passed the first-failure, retained-match, prompt-match, and
pruned-history cases plus timestamp, duration, temporary-path, PID, Run-ID,
different-assertion, and different-command signature probes.

### QA-03

```yaml
inputs:
  - kind: repository_path
    ref: .agents/skills/write-tasks/SKILL.md
  - kind: repository_path
    ref: skills/write-tasks/SKILL.md
```

Lines 135–145 of the canonical and shipped `write-tasks` skills keep the ceiling
at two corrective Tasks, name amend-and-recut and promote-to-own-Spec as the
sanctioned exits, and say the loop continues without a human. `rtk cmp -s
.agents/skills/write-tasks/SKILL.md skills/write-tasks/SKILL.md` exited 0.

### QA-04

```yaml
inputs:
  - kind: repository_path
    ref: internal/daemon/**
```

The focused production Task cycle passed both subtests: the recovery reason
names the Task and surface on one line, and removing only that inserted phrase
leaves the existing category, preferred attempt, fallback attempt, and action
unchanged.

### QA-05

```yaml
inputs:
  - kind: repository_path
    ref: internal/daemon/**
  - kind: repository_path
    ref: internal/runevent/**
```

The focused production Task cycle used three shell probes and passed all four
subtests. The refused Run Event named both offenders, retained all three
ordered verdicts and numbered probe logs, and kept classification `vacuous`
and phase `failed`.

### QA-06

```yaml
inputs:
  - kind: repository_path
    ref: internal/config/**
```

`rtk env GOCACHE=/tmp/roundfix-0096-qa-rerun-gocache go build
-buildvcs=false -o /tmp/roundfix-0096-qa-rerun ./cmd/roundfix` exited 0. In a
fresh Git repository under `/tmp`, the built command's `init --scope project`
surface created `.roundfixrc.yml` with:

```text
budget:
  enabled: true
  max_run_duration: 2h
  # Bounds wall-clock time from Run start; evaluated before each review Round and during Review Source waits.
  # Active Work Item resolution is not interrupted when the budget expires.
```

The independent built-command read `profiles show --category general --json`
loaded that config and exited 0. `TestRenderedConfig` also passed its contract
and loader-round-trip cases.

### QA-07

```yaml
inputs:
  - kind: repository_path
    ref: internal/**
  - kind: repository_path
    ref: docs/agents/autonomous-work.md
  - kind: repository_path
    ref: .agents/skills/write-tasks/SKILL.md
```

`git diff 948ca676..f09e521b` contains only `task_09.md` and the three-line
`publishErr` correction in `internal/daemon/engine.go`; it changes no repair
count, ceiling, store, migration, budget-evaluation path, verdict, settlement,
or measured Verification command. The prior F-001 Daemon and CLI reproductions
now pass, one Verification repair remains in ADR-0038, and the corrective ceiling
remains two Tasks.

### QA-08

```yaml
inputs:
  - kind: repository_path
    ref: CONTEXT.md
  - kind: repository_path
    ref: internal/agent/agent.go
  - kind: repository_path
    ref: internal/daemon/engine.go
```

`rtk rg` located `Repeated Failure` at `CONTEXT.md:327`, in the Agent prompt at
`internal/agent/agent.go:327`, and in the Run Event summary at
`internal/daemon/engine.go:520`. Each surface names a matching earlier Run and
attempt; no glossary repair is needed on this rerun.

### QA-09

```yaml
inputs:
  - kind: repository_path
    ref: docs/workflow/authorizations/2026-08-12-the-authoring-and-baseline-corrections.md
  - kind: repository_path
    ref: .agents/skills/write-tasks/SKILL.md
  - kind: repository_path
    ref: skills/write-tasks/SKILL.md
```

`rtk git -c core.fsmonitor=false diff-tree --no-commit-id --name-only -r
cd429177...` returned exactly the authorized canonical skill, `task_07.md`, and
the sanctioned shipped mirror. Authorization commits `385b5f85` and
`60036423` are ancestors of the Task commit; the latter carries the sanctioned
`make skills-sync` output declaration. The corrective `task_09` commit changes
only its Task file and `internal/daemon/engine.go`. `rtk make
skills-sync-check` and the byte comparison both exited 0. No prerequisite or
consequent-fix declaration exists, so the seeded consequent-order skip remains.

### QA-10

```yaml
inputs:
  - kind: repository_path
    ref: docs/specs/0096-a-failure-the-agent-can-read/**
  - kind: repository_path
    ref: docs/adr/**
  - kind: repository_path
    ref: docs/agents/**
  - kind: repository_path
    ref: docs/workflow/authorizations/**
```

After the strict check skipped `SC-REF-UNRESOLVED`, an explicit `rtk proxy test
-e` audit resolved all 30 cited paths: the glossary; operative guides and
authorization; implementation interfaces; both skill copies; the outside
Finding; and ADRs 0014, 0020, 0038, 0056, 0057, 0081, 0091, 0096, 0104, 0111,
0117, 0127, 0129, 0135, 0136, and 0137. Zero paths were missing.

## Mechanical skips

| Detector | Missing artifact |
| --- | --- |
| authorization bounded paths | Task commits |
| consequent-fix commit order | consequent-fix declaration |
| QA evidence paths | docs/specs/0096-a-failure-the-agent-can-read/qa/evidence |

## Findings

No current findings. Prior F-001 is repaired: all three original regressions now
pass and the public Resolve progress retains the verifier cause. Prior F-002 did
not recur in the full gate and its focused test passed five consecutive fresh
runs.

## Blocked and skipped

No matrix row is blocked or skipped. The full static gate retains one host-parity
gap: two out-of-scope force-stop integration tests need process-table visibility
that this sandbox denies. Their exact unblocking action is a full-access
`rtk make verify` rerun.

## Coverage

- User stories: 3 passed, 3 total.
- Core features: 6 passed, 6 total.
- Matrix rows: 10 passed, 0 failed, 0 blocked, 0 skipped, 0 pending.
- Outside-evidence obligations: 1 passed, sourced to the Fiscus 2026-08-08
  repeated-failure record.
- Non-Goal sweeps: 1 passed.
- `rows_blocked_environment`: 0.
- `rows_blocked_finding`: 0.
- `rows_blocked_declared`: 0.
- Findings by impact: 0 Blocks-Completion, 0 Data-Loss, 0 Trust-Damage, 0
  Friction, 0 Cosmetic.

## Final verdict

Pass: all ten runnable rows passed on build `f09e521b`, the prior product
regression is repaired, the earlier nondeterministic check did not recur, and no
row is failed, blocked, skipped, or pending. The sandbox-only process-table gap
does not cover a declared Spec journey and is recorded with its full-access
unblocking action.
