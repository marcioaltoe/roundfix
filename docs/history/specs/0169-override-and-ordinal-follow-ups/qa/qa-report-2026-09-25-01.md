---
spec: 0169-override-and-ordinal-follow-ups
date: 2026-09-25
build: a27b0a157bf7fc05cffa80453596d90114c89859
auditing_binary: "roundfix 0.16.0 (built from a27b0a157bf7fc05cffa80453596d90114c89859)"
auditor_staleness: "current: built from the audited HEAD and reports the declared tree version 0.16.0"
status: closed
verdict: pass
rows_blocked_environment: 1
rows_blocked_finding: 0
rows_blocked_declared: 0
surfaces: [backend, cli, docs]
---

# QA Report — Override and ordinal follow-ups

## Scope and environment

Full terminal rerun for build `a27b0a15` on macOS after corrective Task 04.
The intended actors are a maintainer invoking the local CLI and a Spec author
running strict consistency checks. The production-like entry point is a
`roundfix 0.16.0` binary built from the audited HEAD at
`/tmp/roundfix-qa-0169-rerun.UzSqus/roundfix`; disposable Git repositories and
Spec fixtures remained outside the source tree. The installed
`/Users/marcio/go/bin/roundfix` reports `0.15.0` and was used only as explicitly
stale outside evidence.

The strict authoring precondition passed with the built binary: `roundfix spec
check 0169-override-and-ordinal-follow-ups --strict` reported no findings and
did not execute authored Verification commands. The authored QA node is
`task_03`, it is terminal, and all three non-QA dependencies are completed. The
Daemon owns Task status and repository Verification. This gate did not commit,
push, or open a Pull Request.

## Static gate

- Repository Verification: the Daemon ran `make verify-changed`; exit `0`,
  verdict `pass`, diagnostics removed on success. The gate did not rerun it.
- Built binary: `/tmp/roundfix-qa-0169-rerun.UzSqus/roundfix version` reported
  `roundfix 0.16.0`.
- Authoring precondition: the built binary's strict check exited `0` with `No
  findings` before the matrix ran and again after the behavior rows.

## Performed repairs

None.

## Assigned repair failures

None.

## Authorization audit inputs

| Task | Outcome | Record | Revision | Detail |
| --- | --- | --- | --- | --- |
| task_01 | granted | docs/specs/0169-override-and-ordinal-follow-ups/_authorization.md | 407fef9ec26f30f37b7710305c2f51daac64b209 | operative grant |
| task_04 | granted | docs/specs/0169-override-and-ordinal-follow-ups/_authorization.md | c2d662e10ff46f743481a7186d1605d14cdb132d | operative grant |

## Mechanical findings

None.

## Results

| # | Story / criterion / sweep | Actor and surface | Status | Provenance and evidence |
| - | --- | --- | --- | --- |
| R1 | Repository Verification | Daemon; repository gate | pass | Requirement 1; repository Verification — supervised `make verify-changed` exited 0 and success diagnostics were removed. |
| R2 | Override records an incomplete QA Task status and omits it for a completed QA Task | Maintainer; CLI/backend | pass | Requirement 2 — two public CLI archive flows exited 0; fresh archived reads found `failed` only for the failed Task case. |
| R3 | Skills, mirrors, command guide, and executable override contract agree | Maintainer/agent; CLI/docs | pass | Requirement 3 — skill validation and mirror comparisons passed; help and both public archive flows matched the command-only refusal/stamp contract; both tooling commits stayed within prior grants. |
| R4 | Same-Spec ordinal collisions fail once with renumbering guidance, distinct numbers clear the collision, and a missing Task Graph exposes the skipped detector | Spec author; CLI/backend | pass | Requirement 4 — public Spec checks emitted one applicable collision, the pure same-Spec case named renumbering, a distinct ordinal removed it, and the missing-graph check listed the detector. |
| R5 | This Spec's artifacts satisfy the promise rule | Spec author; CLI/docs | pass | Requirement 5 — current-tree strict Spec check exited 0 with no findings after the behavior rows ran. |
| R6 | Spec 0159 independently recorded the five follow-up gaps | Maintainer/reviewer; repository evidence | pass | outside-evidence row — archived Spec 0159 names all five gaps at committed provenance; a pre-feature binary reproduced the absent same-Spec detector. |
| R7 | Pull Request controls | Maintainer/reviewer; Pull Request | blocked (environment: no open Pull Request) | Pull Request row — prompt-provided absence plus equivalent evidence for approval, checks/status, threads, Merge-Ready state, and ancestry. |

## Row evidence

### R1

```yaml
inputs:
  - kind: live_service
    ref: Roundfix Daemon repository Verification result
```

The Roundfix Daemon reported `make verify-changed` exit `0` and removed the
success diagnostics. This is supervised evidence supplied outside the Agent
sandbox, recorded as a fact and not re-derived.

### R2

```yaml
inputs:
  - kind: repository_path
    ref: cmd/roundfix/**
  - kind: repository_path
    ref: internal/spec/**
```

A disposable clone at HEAD supplied two isolated Spec fixtures. For each
fixture, the built command entered through `roundfix archive <slug>
--qa-override --approval <source> --reason <text>`:

- failed QA Task plus a `pass` report: exit `0`; the archived `_prd.md` records
  `qa_override_qa_outcome: pass` and `qa_override_qa_task_status: failed`;
- completed QA Task plus a `fail` report: exit `0`; the archived `_prd.md`
  records `qa_override_qa_outcome: fail` and omits
  `qa_override_qa_task_status`.

Fresh filesystem reads confirmed both active Spec paths disappeared, both
archive destinations exist, and each archived QA Task and report remains
present. The complete assertion chain exited `0`.

### R3

```yaml
inputs:
  - kind: repository_path
    ref: .agents/skills/archive-spec/**
  - kind: repository_path
    ref: .agents/skills/qa-gate/**
  - kind: repository_path
    ref: .agents/skills/roundfix/**
  - kind: repository_path
    ref: skills/archive-spec/**
  - kind: repository_path
    ref: skills/qa-gate/**
  - kind: repository_path
    ref: skills/roundfix/**
  - kind: repository_path
    ref: docs/user-guide/commands.md
  - kind: repository_path
    ref: docs/specs/0169-override-and-ordinal-follow-ups/_authorization.md
```

The built `roundfix skills check` command exited `0`. Exact `cmp` checks proved
the canonical and distributed copies match for `archive-spec`, `qa-gate`, and
`roundfix`. Built `archive --help` names the only override command, requires
approval and reason, preserves QA evidence, and refuses an override only when
QA qualifies for normal archive. R2 independently proved the command's
status-stamp behavior rather than accepting documentation reads as evidence.

Focused inspection found `qa_override_qa_task_status` in every override field
list, the current refusal rule in both Roundfix copies, and command-only,
never-hand-stamp instructions in both archive-spec copies. The command guide
also names the conditional status field.

The governed-path audit resolved Task 01 to `c2834d28` and Task 04 to
`a27b0a15`. `git diff-tree --no-commit-id --name-only -r <commit>` showed only
the exact governed paths in `_authorization.md`, ordinary source/test/docs
paths, and each assigned Task file. Authorization revisions `407fef9e` and
`92c69b66` respectively precede those Task commits in ancestry; the seeded
Task 04 audit records the same operative grant as visible at `c2d662e1`.
Sanctioned `make skills-sync` regeneration is recorded in both Task Results,
and the fresh byte comparisons prove its current output. No consequent-fix
declaration exists, as retained in Mechanical skips.

### R4

```yaml
inputs:
  - kind: repository_path
    ref: cmd/roundfix/**
  - kind: repository_path
    ref: internal/speccheck/**
```

The built CLI checked disposable Specs through the public `roundfix spec check`
surface:

- two Tasks claiming ordinal `9998` at different paths, with the first path
  already held on the tree, emitted exactly one `SC-ORDINAL-CLAIMED` error;
- after removing the held path, a fresh invocation emitted exactly one
  same-Spec `SC-ORDINAL-CLAIMED` error naming both paths and the fix `Renumber
  one Task's creates: path to an unclaimed ADR ordinal`;
- changing the second claim to ordinal `9997` removed every
  `SC-ORDINAL-CLAIMED` diagnostic on a fresh invocation; the deliberately
  minimal fixture continued to report unrelated authoring findings;
- a Spec containing `_prd.md` but no `_tasks.md` listed
  `SC-ORDINAL-CLAIMED: missing
  docs/specs/qa-missing-task-graph/_tasks.md` under `Skipped`.

Each invocation reparsed the scratch files, providing an independent public
read after every input change.

### R5

```yaml
inputs:
  - kind: repository_path
    ref: docs/specs/0169-override-and-ordinal-follow-ups/**
```

A fresh `/tmp/roundfix-qa-0169-rerun.UzSqus/roundfix spec check
0169-override-and-ordinal-follow-ups --strict` invocation exited `0`, reported
`No findings`, and explicitly did not execute authored Verification commands.
This public check confirms the PRD promises map into the TechSpec and Task
Graph; R2-R4 independently executed the mapped behavior. The announced skips
concern absent optional rollup, backlog, Vocabulary Contract, and reference
index artifacts, not the promise rule.

### R6

```yaml
inputs:
  - kind: repository_path
    ref: docs/history/specs/0159-archive-override-and-authoring-rules/_prd.md
```

The outside source is archived Spec 0159's `## Recorded limits`, committed at
`ae47621cef7eae56af2e0a85537f9a519ecd6ba0` (`feat: archive override and
authoring rules (#247)`). It names the five gaps before this Spec's
implementation: incomplete-QA status provenance, stale Roundfix wording,
archive-spec hand-stamping, same-Spec duplicate ordinals, and the missing-Task-
Graph skip.

This row does not rest on file reading alone. The installed pre-feature
`/Users/marcio/go/bin/roundfix` reports `0.15.0`; against the same disposable
duplicate-ordinal fixture used by R4, its public strict Spec check emitted no
`SC-ORDINAL-CLAIMED`. The built `0.16.0` binary emitted the same-Spec error for
the identical input. The older binary is explicitly stale and is not evidence
for current behavior.

### R7

```yaml
inputs:
  - kind: live_service
    ref: Pull Request for fix/0169-override-and-ordinal-follow-ups
```

- Cause: the QA prompt states that no Pull Request is open. The Run Worktree
  branch is never pushed and has no Pull Request of its own.
- Approval: `_authorization.md` records approved implementation and delivery
  scope; no post-change Pull Request approval was obtained.
- Checks and status: Daemon-supervised `make verify-changed` passed with exit
  `0`, and R2-R6 record the current candidate's public-flow, contract,
  governance, and outside-evidence checks.
- Unresolved review threads: none exists because no Pull Request or thread set
  exists.
- Merge-Ready acceptance: none exists because no Pull Request Review Source
  object exists yet.
- Review-artifact ancestry: no Pull Request review artifact exists yet. The
  audited candidate head is `a27b0a157bf7fc05cffa80453596d90114c89859`;
  `fix/0169-override-and-ordinal-follow-ups` is at
  `c2d662e10ff46f743481a7186d1605d14cdb132d`, proven as an ancestor of the
  candidate until Daemon integration.
- Unblocking action: integrate the Run into the target branch and open the
  Pull Request during the later delivery stage.

## Findings

None.

## Blocked and skipped

R7 is blocked by the declared environment fact that no Pull Request is open.
Its evidence block records all five equivalent-control states and the later
delivery action that unblocks the live journey. No row is finding-blocked,
declared-blocked, or skipped.

## Coverage

All 7 rows settled: 6 passed and 1 is environment-blocked with equivalent
observed or supervised evidence. The matrix covers Requirements 1-5 plus the
outside-evidence, Pull Request, and repository Verification sources.
`rows_blocked_environment: 1`, `rows_blocked_finding: 0`, and
`rows_blocked_declared: 0`. The PRD has no Unreachable Acceptance declaration,
`frontend` is not declared, and the declared matrix adds no default behavior
probe, Non-Goal observable, or risk-based skip.

## Final verdict

Pass: every runnable row passed, R7 records its environment cause and all five
equivalent Pull Request controls, and no row failed, remained pending, or was
finding-blocked, declared-blocked, or skipped.

## Mechanical skips

| Detector | Missing artifact |
| --- | --- |
| consequent-fix commit order | consequent-fix declaration |
| QA evidence paths | docs/specs/0169-override-and-ordinal-follow-ups/qa/evidence |
