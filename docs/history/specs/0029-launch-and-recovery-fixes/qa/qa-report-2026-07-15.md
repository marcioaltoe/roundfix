---
spec: 0029-launch-and-recovery-fixes
date: 2026-07-15
verdict: pass
surfaces: [cli, docs]
---

# QA Report — Launch and Recovery Fixes

## Summary

The QA gate validated the completed CLI and docs surfaces for the launch and recovery fixes. The PRD has no explicit user-story section, so this report maps coverage to PRD goals, core features, non-goals, and every task acceptance criterion. Static verification, focused feature tests, the built CLI, and shipped skill checks all passed. Verdict: pass.

## Results

| # | Criterion / user story | Verdict | Evidence |
| - | ---------------------- | ------- | -------- |
| 1 | All task files are completed before QA starts. | pass | `rtk proxy grep -n "^status:\|^# Task\|^- \[.\]\|^## Result\|Evidence:" docs/specs/0029-launch-and-recovery-fixes/task_0*.md` showed `status: completed` for `task_01` through `task_06` and checked acceptance criteria. |
| 2 | PRD goal: Detached Run start produces either the four-line report or a diagnostic naming phase, child exit/signal, and output presence. | pass | `rtk go test ./internal/cli/ -run 'Detach\|Doctor\|Settle'` passed 50 tests, including detached report and failure-diagnostic tests. |
| 3 | PRD goal: slow but healthy Preflight Validation does not fail Detached Run start. | pass | `TestRunDetachedCommandAllowsRunCreationPastLivenessDeadline` passed under `rtk go test ./internal/cli/ -run 'Detach\|Doctor\|Settle'`. |
| 4 | PRD goal: Batch-time Agent Model rejection reports rejected model, advertised models, and recovery paths. | pass | `rtk go test ./internal/agent/... -run 'ModelNotAdvertised\|NotAdvertised'` passed 3 tests; `rtk go test ./internal/daemon/... -run 'ModelNotAdvertised\|not advertised\|NotAdvertised'` passed 2 tests. |
| 5 | PRD goal: Doctor Command shows the effective model probe result. | pass | `TestRunDoctorReportsReadinessChecks`, `TestRunDoctorAcceptsConfiguredEmptyReasoningEffort`, and `TestRunDoctorReportsModelRejectionWithNextAction` passed in the focused CLI test run. |
| 6 | PRD goal: Settle chooses a failed Task surface and always names the chosen surface. | pass | `TestRunSettleSkipsStaleKeptRunWorktreeAndUsesFailedCheckout`, kept-worktree settle tests, and surface-line assertions passed in the focused CLI test run. |
| 7 | Core feature 1: two-phase Detached Run handshake with liveness and Run-creation phases. | pass | `rtk proxy grep -R "liveness" -n internal/cli/detach.go internal/cli/detach_test.go` found implementation and tests; focused CLI tests passed. |
| 8 | Core feature 2: Batch-time model rejections classify as selection failures, not generic protocol errors. | pass | Agent and daemon focused tests passed; `rtk proxy grep -R "not advertised by runtime" -n internal/agent internal/daemon internal/cli` found shipped reason wiring. |
| 9 | Core feature 3: Doctor reports configured Agent Model outcome and advertised list on failure. | pass | Focused CLI tests passed; `rtk proxy grep -R "model: ok" -n internal/cli skills .agents/skills` found CLI tests and skill docs. |
| 10 | Core feature 4: Settle prefers failed surfaces, reports selected surface, and enumerates candidate statuses on refusal. | pass | Focused CLI tests passed; `rtk proxy grep -R "Settle surface:" -n internal/cli skills .agents/skills` found implementation, tests, and docs. |
| 11 | Core feature 5: Roundfix Skill ships the Detached Run, Doctor, and Settle behavior. | pass | `rtk go run -buildvcs=false ./cmd/roundfix skills check` passed; `rtk ./bin/roundfix skills check` passed. |
| 12 | Non-goal: no upstream acpx/codex-acp advertised-list fix is included. | pass | Diff and grep evidence showed classification in Roundfix code only, with no upstream package changes. |
| 13 | Non-goal: settle pathspec scoping remains deferred. | pass | Scope inspection showed no task-specific pathspec feature work; existing pathspec code remains outside this spec's changed behavior. |
| 14 | Non-goal: four-line Detached Run success report and success-path public output stay unchanged. | pass | `TestRunImplementDetachPrintsReportAndCompletesRun` passed in the focused CLI test run. |
| 15 | Task 01 AC: child stub that sleeps past the liveness deadline is killed and prints the liveness-timeout diagnostic naming exit or signal. | pass | `TestRunDetachedCommandTimesOutWaitingForLiveness` passed in the focused CLI test run. |
| 16 | Task 01 AC: child that signals liveness and takes longer than the old 10 seconds but under the Run-creation ceiling produces the normal four-line success report. | pass | `TestRunDetachedCommandAllowsRunCreationPastLivenessDeadline` passed in the focused CLI test run. |
| 17 | Task 01 AC: child exit before handshake names exit code and relays output; silent child states produced no output. | pass | `TestRunDetachedCommandReportsChildExitBeforeHandshakeWithOutput` and `TestRunDetachedCommandReportsSilentChildExitBeforeHandshake` passed in the focused CLI test run. |
| 18 | Task 01 AC: success path output is byte-identical to the current contract. | pass | `TestRunImplementDetachPrintsReportAndCompletesRun` passed in the focused CLI test run. |
| 19 | Task 01 AC: full test suite passes. | pass | `rtk make verify` passed, including `rtk go test ./...` with 1249 tests. |
| 20 | Task 02 AC: fixture stderr tail yields typed error with correct model, advertised list, and recovery paths. | pass | `TestModelNotAdvertisedPromptExitYieldsTypedError` passed in the focused agent test run. |
| 21 | Task 02 AC: garbage stderr tail yields the current infrastructure error. | pass | `TestModelNotAdvertisedGarbageStderrKeepsInfrastructureError` passed in the focused agent test run. |
| 22 | Task 02 AC: `errors.As` extracts the typed error through the existing wrap chain. | pass | `TestModelNotAdvertisedSelectionPreflightYieldsTypedErrorThroughWrapChain` passed in the focused agent test run. |
| 23 | Task 02 AC: full test suite passes. | pass | `rtk make verify` passed. |
| 24 | Task 03 AC: spec Task Batch failure on model rejection carries the model-rejection terminal reason in journal payload and report line. | pass | `TestTaskCycleModelNotAdvertisedFailureSettlesAndReportsReason` passed in the focused daemon test run. |
| 25 | Task 03 AC: review Batch failure on model rejection settles Review Issues with that reason in artifacts. | pass | `TestResolveCycleModelNotAdvertisedFailureSettlesReviewIssueReason` passed in the focused daemon test run. |
| 26 | Task 03 AC: unrelated agent failures keep existing reason text. | pass | `rtk go test ./internal/daemon/... -run 'ModelNotAdvertised\|not advertised\|NotAdvertised'` passed the focused regression set; task result evidence records unchanged unrelated-failure coverage. |
| 27 | Task 03 AC: full test suite passes. | pass | `rtk make verify` passed. |
| 28 | Task 04 AC: Doctor passing probe prints `model: ok (<model>)`. | pass | `TestRunDoctorReportsReadinessChecks` and `TestRunDoctorAcceptsConfiguredEmptyReasoningEffort` passed in the focused CLI test run. |
| 29 | Task 04 AC: Doctor rejected model prints failure line with advertised list and `next:` action, and exits nonzero. | pass | `TestRunDoctorReportsModelRejectionWithNextAction` passed in the focused CLI test run. |
| 30 | Task 04 AC: pre-existing Doctor lines are unchanged. | pass | `TestRunDoctorReportsReadinessChecks` passed in the focused CLI test run. |
| 31 | Task 04 AC: full test suite passes. | pass | `rtk make verify` passed. |
| 32 | Task 05 AC: stale kept worktree with Task `pending` and checkout Task `failed` settles from the checkout and prints the checkout in `Settle surface:`. | pass | `TestRunSettleSkipsStaleKeptRunWorktreeAndUsesFailedCheckout` passed in the focused CLI test run. |
| 33 | Task 05 AC: Task `failed` only in a kept Run Worktree still selects that worktree. | pass | `TestRunSettleRetargetsKeptRunWorktreeAndCleansUpAfterIntegration` passed in the focused CLI test run. |
| 34 | Task 05 AC: no failed surface refusal names every candidate path and status. | pass | `TestRunSettleRefusalEnumeratesCandidateStatuses` passed in the focused CLI test run. |
| 35 | Task 05 AC: full test suite passes. | pass | `rtk make verify` passed. |
| 36 | Task 06 AC: skills check passes with updated anchors. | pass | `rtk go run -buildvcs=false ./cmd/roundfix skills check` and `rtk ./bin/roundfix skills check` passed. |
| 37 | Task 06 AC: skill documents detach failure diagnostics and no longer implies a silent failure mode. | pass | `rtk proxy grep -qi "liveness" skills/roundfix/SKILL.md` passed; report inspection found explicit Detached Run diagnostics in the generated skill. |
| 38 | Task 06 AC: skill documents Doctor `model:` line and Settle surface reporting. | pass | `rtk proxy grep -q "Settle surface:" skills/roundfix/SKILL.md` and `rtk proxy grep -q "model: ok" skills/roundfix/SKILL.md` passed. |
| 39 | Task 06 AC: full test suite passes. | pass | `rtk make verify` passed. |
| 40 | CLI smoke: built application is runnable. | pass | `rtk ./bin/roundfix --help` printed the command list, including `doctor`, `settle`, `implement`, and `skills check`. |

## Failures and blocked items

None.

## Environment

- Repository: `bfcfcaf` (`rtk git rev-parse --short HEAD`).
- OS/toolchain: Darwin arm64; `go version go1.26.5 darwin/arm64`.
- Static gate: `rtk make verify` passed, running `rtk go test ./...`, `rtk go run -buildvcs=false ./cmd/roundfix skills check`, and `rtk go build -buildvcs=false -o bin/roundfix ./cmd/roundfix`.
- Focused CLI gate: `rtk go test ./internal/cli/ -run 'Detach|Doctor|Settle'` passed with 50 tests.
- Focused Agent Model classification gate: `rtk go test ./internal/agent/... -run 'ModelNotAdvertised|NotAdvertised'` passed with 3 tests.
- Focused Daemon reason wiring gate: `rtk go test ./internal/daemon/... -run 'ModelNotAdvertised|not advertised|NotAdvertised'` passed with 2 tests.
- Docs gates: `rtk go run -buildvcs=false ./cmd/roundfix skills check`, `rtk ./bin/roundfix skills check`, `rtk proxy grep -q "Settle surface:" skills/roundfix/SKILL.md`, `rtk proxy grep -qi "liveness" skills/roundfix/SKILL.md`, and `rtk proxy grep -q "model: ok" skills/roundfix/SKILL.md` passed.
- CLI smoke: `rtk ./bin/roundfix --help` passed.
- Not tested: no frontend, browser, database, or network flow exists in the PRD surfaces. No live acpx/codex-acp network behavior was tested; the spec deliberately handles upstream advertised-list drift through classification, Doctor rendering, and Daemon reason wiring.
