---
spec: 0113-a-gate-report-that-does-not-block-its-successor
date: 2026-08-15
build: 57b7d2f6bd29cdc3b455e308b9280763c6833901
status: closed
verdict: pass
rows_blocked_environment: 1
rows_blocked_finding: 0
rows_blocked_declared: 0
surfaces: [cli, backend, docs]
---

# QA report — A gate report that does not block its successor

## Scope and environment

Full same-day rerun of the authored terminal `task_07` gate. The rerun starts
with R03, the failure in `qa-report-2026-08-15-02.md`, then R07, the prior
environment-blocked row. The Task Graph names `task_07` as its unique terminal
`qa` node, and every dependency is completed. Task status remains Daemon-owned.

No Pull Request is open, and the Spec declares no Pull Request journey. The
per-Run branch is not a publication surface. Scratch repositories, Go caches,
and binaries stay under `/private/tmp`; no credential or live service is
required.

The PATH binary is stale: its strict check exits `1` with the pre-Task-10
`SC-TOOLING-UNBOUNDED` reading. The current source built as
`/private/tmp/roundfix-0113-qa03` at version `0.6.0`; its text and JSON strict
checks both exit `0`, with no findings or repair inputs and only the optional
missing `references/_index.md` skip. The current-source binary is the CLI under
test.

## Seeded mechanical evidence

The Daemon created this report and started the QA Agent, proving its mechanical
stage was nonblocking. The materialized sections below are preserved as
pre-Agent audit evidence.

### Performed repairs

None.

### Assigned repair failures

None.

### Mechanical findings

None.

### Materialized mechanical rows

| # | Status | Provenance |
| - | --- | --- |

## Static gate

The installed PATH binary predates Task 10 and exited `1` with
`SC-TOOLING-UNBOUNDED`. `roundfix --version` in the previous rerun identified
that binary as `0.4.0`; it is not the artifact under test. The current source
built as `/private/tmp/roundfix-0113-qa03` at version `0.6.0`. Both of these
current-source commands exited `0`, with no findings or repair inputs:

```text
$ /private/tmp/roundfix-0113-qa03 spec check 0113-a-gate-report-that-does-not-block-its-successor --strict
$ /private/tmp/roundfix-0113-qa03 spec check 0113-a-gate-report-that-does-not-block-its-successor --strict --format json
```

`rtk make verify` exited `2`. All packages except `internal/agent` and
`internal/cli` passed. The CLI failures were the two Force Stop integrations
that need macOS process-table enumeration; both failed with the same sandbox
denial at previous QA head `615ce12f`. The two ACP fixture cases that failed in
the full parallel run passed in a fresh complete `internal/agent` rerun.

Equivalent current-build evidence passed:

```text
$ go test -count=1 ./internal/cli -run '^TestRunImplement'
ok roundfix/internal/cli

$ go test -count=1 ./internal/daemon ./internal/speccheck ./internal/spec
ok roundfix/internal/daemon
ok roundfix/internal/speccheck
ok roundfix/internal/spec

$ go test -count=1 ./internal/agent
ok roundfix/internal/agent
```

The focused feature and Task 10 boundary groups passed in fresh runs. After
report closure, `rtk make verify-docs` exited `0`; documentation contracts,
repository contracts, corpus budget, current-source build, and active-Spec
checks all passed.

## Results

| # | Story / criterion / sweep | Actor and surface | Status | Evidence |
| - | --- | --- | --- | --- |
| R01 | Stories 1 and 2 / Core Features 1–3 and 5: truthful refusal reports are readable by successors and diagnostics name the required literal | Maintainer · CLI/backend gate | pass | Blocking and nonblocking writer cases, successor rereads, literal diagnostics, malformed controls, and all public Implement journeys passed. See **R01**. |
| R02 | Story 3 / Core Feature 4: one unparsed row reports one cause while a genuinely wrong count remains distinct | Maintainer · backend gate | pass | Parse-only, genuine-count, and combined probes passed. See **R02**. |
| R03 | Story 4 / Core Feature 6: the full-sweep strict gate receives its Spec-owned coined term as repair input without weakening authoring checks | Supervisor · CLI/backend/docs | pass | Current-source CLI separates the authoring error from the gate repair input in text and JSON; the undeclared-term and Implement controls passed. See **R03**. |
| R04 | Story 5 / Core Feature 7: Task-declared repairs reach the mechanical request, remain bounded, are performed, verified, and recorded | Supervisor · CLI/backend/docs | pass | On-disk Task declarations reached the Daemon request; performed, skipped, unassigned, and out-of-bounds probes passed. See **R04**. |
| R05 | Outside evidence: replay the three 2026-08-14 and 2026-08-15 refusals from artifacts this Spec did not author | Maintainer · docs/backend | pass | All four historical artifacts resolve unchanged; the current writer, successor, repair stage, and full-sweep boundary now close their recorded failure modes. See **R05**. |
| R06 | Non-Goals and changed-path scope: the mechanical stage stays present, verdict/count semantics and write bounds stay intact, prior reports remain unchanged, and no Governed Path changes | Maintainer · backend/Git | pass | Negative controls and all nine Task-commit `git diff-tree` audits passed; no tooling row applies. See **R06**. |
| R07 | Repository regression gate and assembled public Implement journeys remain green | Maintainer · CLI/backend | blocked (environment: macOS process-table enumeration denied) | Full Verification reached the tree; only two Force Stop integrations remain reproducibly denied by the sandbox. Public Implement and all affected packages pass. See **R07**. |
| R08 | Vocabulary closure: document every introduced, changed, or retired domain term | Maintainer · docs | pass | `Assigned Repair` remains the one canonical term; Task 10's repair-input field labels that existing concept and introduces no second domain term. See **R08**. |
| R09 | Report and evidence hygiene: terminal matrix, resolving evidence paths, and no binary, Gitlink, scratch repository, or unauthored evidence | Maintainer · docs/CLI | pass | This Markdown report is the only visible worktree artifact; all inputs resolve, blocked counts match, and no binary or Gitlink is tracked as evidence. See **R09**. |

## Detailed evidence

### R01

```yaml
inputs:
  - kind: repository_path
    ref: internal/daemon/task_engine.go
  - kind: repository_path
    ref: internal/daemon/task_engine_test.go
  - kind: repository_path
    ref: internal/cli/implement_test.go
  - kind: repository_path
    ref: internal/speccheck/mechanical.go
```

- Entry point: writer-to-report integration, successor mechanical reads,
  blocked-cause diagnostics, and every public `TestRunImplement` journey.
- Expected: only a blocking zero-row result records `QA-PRECONDITION`; a
  nonblocking zero-row result records no refusal; a fresh successor accepts the
  writer's terminal refusal row; malformed rows remain refused; the missing
  finding literal is quoted as `" — waits on "`.
- Independent confirmation: writer outputs were reread by the mechanical stage,
  negative controls independently changed terminal status and table shape, and
  public Implement journeys materialized separate Specs.
- Observed: all six writer cases, three successor cases, five literal cases, and
  every public Implement journey passed. The seeded report's empty materialized
  table truthfully matches its nonblocking stage.

### R02

```yaml
inputs:
  - kind: repository_path
    ref: internal/speccheck/mechanical.go
  - kind: repository_path
    ref: internal/speccheck/mechanical_test.go
```

- Entry point: `TestCountDisagreementReportsItsCause` through the report-shape
  detector.
- Expected: one unparsed row that explains the declared total yields one
  row-specific finding; a genuinely wrong total remains distinct; both causes
  together remain two findings.
- Independent confirmation: parse-only, count-only, and combined reports were
  materialized and read separately.
- Observed: all three probes passed and named the intended rows and count.

### R03

```yaml
inputs:
  - kind: repository_path
    ref: internal/cli/spec_check.go
  - kind: repository_path
    ref: internal/cli/spec_check_test.go
  - kind: repository_path
    ref: internal/speccheck/mechanical.go
  - kind: repository_path
    ref: internal/speccheck/testdata/repo/docs/specs/vocabulary-missing/**
```

- Entry point: current-source `roundfix spec check` against a fresh Git copy of
  the established `vocabulary-missing` fixture, followed by the focused public
  CLI, classifier, and unchanged Implement-contract tests.
- Expected: `--stage techspec --strict` retains
  `SC-VOCABULARY-UNDOCUMENTED` as an error; full-sweep `--strict` exits `0`,
  removes it from findings, and emits one severity-free repair input naming
  `publish:`; an undeclared token remains an error; Implement gains no Spec-check
  precondition.
- Independent confirmation: text and JSON ran as separate CLI processes from
  the scratch repository. The six focused test groups then exercised the CLI
  boundary, lower classifier, undeclared control, renderers, and public
  Implement contract in fresh package processes.
- Observed: authoring exited `1` with the vocabulary error; full-sweep text and
  JSON exited `0`, each naming one `SC-VOCABULARY-UNDOCUMENTED` repair input and
  no finding. Every focused test passed. `GatePrecondition` resolves from
  production `internal/cli/spec_check.go` as well as its declaration and tests.

### R04

```yaml
inputs:
  - kind: repository_path
    ref: internal/spec/task.go
  - kind: repository_path
    ref: internal/daemon/task_engine.go
  - kind: repository_path
    ref: internal/speccheck/mechanical.go
  - kind: repository_path
    ref: internal/speccheck/report.go
```

- Entry point: `spec.Load` from on-disk QA Task files through the Daemon's
  `qaMechanicalRequest`, followed by the real assigned-repair stage.
- Expected: exact paths and replacements survive parsing and request mapping;
  assigned work writes, rereads, and records only the named path; skipped,
  unassigned, and out-of-bounds work cannot silently pass or write.
- Independent confirmation: the positive case reread its changed target and
  report record; the out-of-bounds case reread the protected target unchanged;
  the undeclared Task kept request fields nil.
- Observed: both production-request cases and all four repair-stage probes
  passed. This QA Task declares no repair, matching the seeded `None` records.

### R05

```yaml
inputs:
  - kind: repository_path
    ref: docs/backlog/2026-08-14-a-spec-that-coins-a-term-cannot-pass-its-own-gate.md
  - kind: repository_path
    ref: docs/history/specs/0103-a-suite-that-leaks-nothing/qa/qa-report-2026-08-14.md
  - kind: repository_path
    ref: docs/history/specs/0103-a-suite-that-leaks-nothing/qa/qa-report-2026-08-14-01.md
  - kind: repository_path
    ref: docs/history/specs/0114-an-audit-that-reads-the-authorization-it-was-given/qa/qa-report-2026-08-15.md
  - kind: repository_path
    ref: internal/daemon/task_engine.go
  - kind: repository_path
    ref: internal/speccheck/mechanical.go
  - kind: repository_path
    ref: internal/cli/spec_check.go
```

- Entry point: read the pre-Spec backlog record, both Spec 0103 reports, and the
  Spec 0114 report; replay their recorded behaviors against the current writer,
  successor detector, assigned-repair stage, and current-source strict CLI.
- Expected: the sources resolve outside this Spec and the current node states
  what it does with the empty-table refusal, successor shape refusal,
  declared-term deadlock, and named repairs that the old gate only reported.
- Independent confirmation: `git diff --quiet` proves all four source artifacts
  are byte-unchanged from previous QA head `615ce12f`; the sources predate this
  rerun and this Spec did not author them.
- Observed: the current writer emits a truthful terminal row only for a blocking
  refusal and its successor accepts it; malformed controls still refuse; the
  full-sweep strict boundary turns the Spec-owned term into a repair input while
  authoring retains the error; assigned repairs parse, map, write, reread, and
  record within their Task bounds.

### R06

```yaml
inputs:
  - kind: repository_path
    ref: internal/daemon/task_engine.go
  - kind: repository_path
    ref: internal/speccheck/**
  - kind: repository_path
    ref: internal/spec/**
  - kind: repository_path
    ref: internal/cli/spec_check.go
  - kind: repository_path
    ref: docs/specs/0113-a-gate-report-that-does-not-block-its-successor/_prd.md
  - kind: repository_path
    ref: .git
```

- Entry point: feature negative controls plus
  `git diff-tree --no-commit-id --name-only -r` for the commits owning Tasks 01,
  02, 03, 04, 05, 06, 08, 09, and 10.
- Expected: malformed reports, undeclared terms, skipped repairs, unassigned
  observations, and out-of-bounds writes stay refused; the mechanical stage,
  verdict and typed counts retain their contracts; no Task commit changes a
  Governed Path or unrelated surface.
- Independent confirmation: exact-byte report and post-write rereads passed;
  Git supplied each changed path independently of the Task Results. The diff
  from previous QA head to current head contains only Task 10, its CLI source,
  and its CLI test.
- Observed: every negative control passed. Task commits contain their own Task
  file and ordinary Go source/tests under `internal/daemon`,
  `internal/speccheck`, `internal/spec`, or `internal/cli`. No configuration,
  script, ignore file, dependency, plugin declaration, version pin, or other
  Governed Path changed, so no commit-dependent tooling row applies.

### R07

```yaml
inputs:
  - kind: repository_path
    ref: Makefile
  - kind: repository_path
    ref: '**/*.go'
  - kind: repository_path
    ref: go.mod
  - kind: repository_path
    ref: go.sum
  - kind: live_service
    ref: macOS process-table access in the managed QA sandbox
```

- Entry point: required `rtk make verify`, followed by fresh public Implement,
  affected backend, and complete `internal/agent` package runs. The two Force
  Stop failures were replayed from the unchanged previous QA head
  `615ce12fba541a5bd3f0328d64cb2566ed5f4a91`.
- Expected: the full repository gate exits `0`; when the sandbox prevents that,
  the report records the denial, the exact unblocking action, and equivalent
  evidence for the changed and adjacent surfaces.
- Independent confirmation: both Force Stop tests fail at the previous head with
  the same `read process table for non-session owner: operation not permitted`
  diagnostic. Current `TestRunImplement`, `internal/daemon`,
  `internal/speccheck`, `internal/spec`, and a fresh complete `internal/agent`
  run exit `0`.
- Observed: `rtk make verify` exited `2`. The two Force Stop integrations remain
  unreachable because the managed sandbox denies process-table enumeration.
  Two concurrent ACP fixture cases also failed in that full parallel run, but a
  fresh complete `internal/agent` rerun passed and the previous head's focused
  parents passed; they are not a persistent current-build failure. A full-access
  macOS run that permits process-tree reads is the exact unblocking action.

### R08

```yaml
inputs:
  - kind: repository_path
    ref: CONTEXT.md
  - kind: repository_path
    ref: docs/specs/0113-a-gate-report-that-does-not-block-its-successor/_techspec.md
  - kind: repository_path
    ref: internal/speccheck/mechanical.go
  - kind: repository_path
    ref: internal/cli/spec_check.go
```

- Entry point: compare the emitted vocabulary in the mechanical stage and the
  Task 10 CLI boundary with the TechSpec Vocabulary Contract and `CONTEXT.md`,
  then rerun the current-source strict JSON check.
- Expected: `Assigned Repair` has one canonical definition matching the declared
  emitted pattern; this rerun identifies any introduced, changed, or retired
  term and updates its semantic owner only when needed.
- Independent confirmation: exact searches resolve the glossary definition,
  TechSpec contract, mechanical-stage language, and Task 10's output fields.
  Strict JSON checking reports no finding.
- Observed: `Assigned Repair` remains documented at `CONTEXT.md:31`. The CLI's
  `repairInputs` and `[repair input]` labels expose the existing classifier input
  for that concept; they do not establish a competing domain term. Task 10
  introduces, changes, or retires no glossary term, so this gate needs no
  `CONTEXT.md` edit.

### R09

```yaml
inputs:
  - kind: repository_path
    ref: docs/specs/0113-a-gate-report-that-does-not-block-its-successor/qa/qa-report-2026-08-15-03.md
  - kind: repository_path
    ref: .git
```

- Entry point: inspect the closed report, QA-tree index modes, worktree status,
  frontmatter counters, row statuses, and every declared repository input; run
  `rtk make verify-docs` against the closed artifact.
- Expected: nine terminal rows, exact blocked counts, no unresolved input, and
  no binary, Gitlink, scratch repository, or unauthored evidence added by this
  rerun.
- Independent confirmation: Git index modes, filesystem paths, report text,
  current-source strict checking, and the documentation gate are read
  independently after closure.
- Observed: this rerun's only visible worktree artifact is this UTF-8 Markdown
  report. Prior reports remain unchanged; scratch repositories and the QA binary
  remain under `/private/tmp`; the ignored `bin/roundfix` built by
  `make verify-docs` is not evidence or a tracked change. No tracked mode
  `160000` exists below the Spec. All repository inputs resolve, all nine rows
  are terminal, the single environment-blocked row matches
  `rows_blocked_environment: 1`, and `rtk make verify-docs` exits `0`.

## Findings

None.

## Blocked and skipped

R07 is `blocked (environment: macOS process-table enumeration denied)`. The
unchanged previous-head reproduction and passing current-source public Implement,
affected backend, and complete Agent-package runs provide equivalent observed
evidence. A full-access macOS run that permits the two Force Stop integrations to
enumerate their owned process trees is the exact unblocking action. No row is
finding-blocked, declared-blocked, or skipped.

The mechanical detector skips below remain byte-preserved from the seeded
report. They are presence records, not flow-row `skipped` statuses.

## Mechanical skips

| Detector | Missing artifact |
| --- | --- |
| authorization bounded paths | tooling authorization |
| consequent-fix commit order | consequent-fix declaration |
| QA evidence paths | docs/specs/0113-a-gate-report-that-does-not-block-its-successor/qa/evidence |

## Coverage

All nine matrix rows are terminal: eight pass and one is environment-blocked
with equivalent evidence. Every PRD user story, all seven Core Features, all
five Non-Goals, and the outside-evidence obligation map to an executed row.

High-risk probes cover blocking and nonblocking zero-row reports, optional
skips, successor rereads, non-terminal and unreadable tables, missing literals,
unrecognised causes, all three typed totals, parse-only versus genuine count
disagreement, stage-scoped versus full-sweep strict checking, text and JSON
repair inputs, undeclared vocabulary, omitted repair declarations, skipped
repairs, unassigned observations, out-of-bounds writes, and public Implement
regression. There is no frontend, TUI, network, database, authentication, HTTP,
or Pull Request journey in scope.

Counts: `rows_blocked_environment: 1`, `rows_blocked_finding: 0`, and
`rows_blocked_declared: 0`. Finding impacts: zero `Blocks-Completion`,
`Data-Loss`, `Trust-Damage`, `Friction`, or `Cosmetic` findings.

## Final verdict

`pass` — every runnable criterion passed, the one environment-blocked repository
row records its cause and equivalent current-build evidence, and no row is
finding-blocked, declared-blocked, failed, or skipped.
