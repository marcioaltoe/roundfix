---
spec: 0113-a-gate-report-that-does-not-block-its-successor
date: 2026-08-15
build: 4cb6fdec30d7c3e47c72bd487249c09840d84dd3
status: closed
verdict: fail
rows_blocked_environment: 1
rows_blocked_finding: 0
rows_blocked_declared: 0
surfaces: [cli, backend, docs]
---

# QA report — A gate report that does not block its successor

## Scope and environment

Full authored terminal gate for `task_07` against Run Worktree commit
`4cb6fdec30d7c3e47c72bd487249c09840d84dd3`. The Task Graph names
`task_07` as its unique terminal `qa` node. Dependencies `task_02`, `task_04`,
`task_06`, `task_08`, and `task_09` are completed; Task status remains
Daemon-owned.

No Pull Request is open, and the Spec declares no Pull Request journey. The
per-Run branch is not a publication surface. Scratch repositories, Go caches,
and binaries remain under `/private/tmp`; no credential or live service is
required.

The PATH binary is Roundfix `0.4.0` and predates this build. Its strict check
reports stale `SC-TOOLING-UNBOUNDED` behavior. The current source builds as
`/private/tmp/roundfix-0113-qa02` at version `0.6.0`; that binary is the CLI
under test.

## Seeded mechanical evidence

The Daemon created this report and started the QA Agent, proving its mechanical
stage was nonblocking. The materialized sections below are preserved as
pre-Agent audit evidence. Because the Implement Run's Daemon binary predates
Task 09's current-source repair, its materialized refusal row is also a parity
deviation to re-observe through the current-source CLI.

### Performed repairs

None.

### Assigned repair failures

None.

### Mechanical findings

None.

### Materialized mechanical rows

| # | Status | Provenance |
| - | --- | --- |
| QA-PRECONDITION | fail | mechanical refusal: authorization bounded paths: missing tooling authorization; consequent-fix commit order: missing consequent-fix declaration; QA evidence paths: missing docs/specs/0113-a-gate-report-that-does-not-block-its-successor/qa/evidence; no other checks executed |

## Static gate

The required PATH command ran first and exposed a stale-tool mismatch:

```text
$ roundfix spec check 0113-a-gate-report-that-does-not-block-its-successor --strict
[error] SC-TOOLING-UNBOUNDED: .../_techspec.md makes Tooling authority applicable without bounded files
```

`roundfix --version` identified that binary as `0.4.0` at commit `9e85a41`.
The current source built successfully as `/private/tmp/roundfix-0113-qa02` at
version `0.6.0`; its strict check exited `0` with no findings and only the
optional missing `references/_index.md` skip. This proves the PATH result is an
environment/tool-version mismatch, not a current authoring defect.

`rtk make verify` exercised the full repository and exited `2`. Every package
except `internal/cli` passed. The CLI package's only failures were
`TestRunForceStopLegacyRunWithoutOwnerIdentityStillStopsOwner` and
`TestRunForceStopOwnerProcessIntegrationProvesExitBeforeStoreCompletion`; both
reported `read process table for non-session owner: operation not permitted`.
The same two tests at previous QA head
`022482b07853cb79c9b973d4551159cabfdb143a` failed with the identical sandbox
denial, proving the cause is environment-owned and unchanged by Task 09.

Equivalent current-build evidence then passed:

```text
$ go test -count=1 ./internal/cli -run '^TestRunImplement'
ok roundfix/internal/cli 9.904s

$ go test -count=1 ./internal/daemon ./internal/speccheck ./internal/spec
ok roundfix/internal/daemon 9.060s
ok roundfix/internal/speccheck 6.774s
ok roundfix/internal/spec 21.511s
```

The seven named feature groups and their behavior probes passed in a separate
fresh run. `git diff --check` also exited `0`. After report closure,
`rtk make verify-docs` exited `0`: the documentation contract, repository
contract, corpus budget, current build, and active-Spec checks all passed.

## Results

| # | Story / criterion / sweep | Actor and surface | Status | Evidence |
| - | --- | --- | --- | --- |
| R01 | Stories 1 and 2 / Core Features 1–3 and 5: truthful refusal reports are readable by successors and diagnostics name the required literal | Maintainer · CLI/backend gate | pass | Current-source writer/successor cases and both Task 09 public Implement journeys passed; the preserved seed row came from the pre-Task-09 Daemon. See **R01**. |
| R02 | Story 3 / Core Feature 4: one unparsed row reports one cause while a genuinely wrong count remains distinct | Maintainer · backend gate | pass | Parse-only, genuine-count, and both-causes probes passed. See **R02**. |
| R03 | Story 4 / Core Feature 6: the gate receives its Spec-owned coined term as repair input without weakening authoring checks | Supervisor · CLI/backend/docs | fail | F-001: the classifier passes only as a direct caller; the production tree has no caller and the real strict gate command still exits `1` on its Spec's declared term. See **R03**. |
| R04 | Story 5 / Core Feature 7: Task-declared repairs reach the mechanical request, remain bounded, are performed, verified, and recorded | Supervisor · CLI/backend/docs | pass | A scratch current-source probe drove Task parsing → Daemon request → mechanical write/readback → seeded report; negative controls passed. See **R04**. |
| R05 | Outside evidence: replay the three 2026-08-14 and 2026-08-15 refusals from artifacts this Spec did not author | Maintainer · docs/backend | pass | All four outside artifacts resolved and each recorded failure was compared with the current node; the vocabulary case remains exposed by F-001. See **R05**. |
| R06 | Non-Goals and changed-path scope: the mechanical stage stays present, verdict/count semantics and write bounds stay intact, prior reports remain unchanged, and no Governed Path changes | Maintainer · backend/Git | pass | Negative controls and all eight Task-commit `git diff-tree` reads passed; no tooling audit row applies. See **R06**. |
| R07 | Repository regression gate and assembled public Implement journeys remain green | Maintainer · CLI/backend | blocked (environment: macOS process-table enumeration denied) | Full Verification reached the tree but two unchanged Force Stop integrations could not read the sandboxed process table; every current public Implement journey and affected backend package passed. See **R07**. |
| R08 | Vocabulary closure: document every introduced, changed, or retired domain term | Maintainer · docs | pass | `Assigned Repair` resolves in `CONTEXT.md` and the TechSpec Vocabulary Contract; Task 09 introduced no new term. See **R08**. |
| R09 | Report and evidence hygiene: terminal matrix, resolving evidence paths, and no binary, Gitlink, scratch repository, or unauthored evidence | Maintainer · docs/CLI | pass | The rerun adds only this Markdown report; its inputs resolve, blocked counts match, and no tracked Gitlink exists. See **R09**. |

## Detailed evidence

### R01

```yaml
inputs:
  - kind: repository_path
    ref: internal/daemon/task_engine.go
  - kind: repository_path
    ref: internal/daemon/task_engine_test.go
  - kind: repository_path
    ref: internal/cli/implement_test.go
```

- Entry point: current-source writer and successor integration, followed by the
  public Implement command-runner journeys
  `TestRunImplementHasNoSpecCheckPrecondition` and
  `TestRunImplementQAPromptStatesSpecTargetBranchFromRunRecord`.
- Expected: only a blocking zero-row result writes `QA-PRECONDITION`; a
  nonblocking zero-row result keeps `verdict: pass`, contains no refusal marker,
  and optional skips remain skips. A blocking report survives a fresh successor
  read, while non-terminal and unreadable rows remain refused.
- Independent confirmation and persistence: six writer cases reread reports
  from disk, three successor cases start fresh repositories, and the two public
  Implement journeys run from separately materialized Specs.
- Observed: every case passed. The seed row above was created by the Run's
  already-running Daemon before Task 09 existed; the current-source writer did
  not reproduce it.

### R02

```yaml
inputs:
  - kind: repository_path
    ref: internal/speccheck/mechanical.go
  - kind: repository_path
    ref: internal/speccheck/mechanical_test.go
```

- Entry point: `TestCountDisagreementReportsItsCause` through the real
  report-shape detector.
- Expected: an unparsed row that explains the declared total produces one
  row-specific parse finding; a genuinely wrong total remains a separate
  finding; both independent causes remain two findings.
- Independent confirmation: parse-only, count-only, and combined reports are
  materialized separately and reread in a fresh process.
- Observed: all three probes passed. The adjacent five-case literal diagnostic
  also passed and kept all three typed totals intact.

### R03

```yaml
inputs:
  - kind: repository_path
    ref: internal/speccheck/mechanical.go
  - kind: repository_path
    ref: internal/speccheck/testdata/repo/docs/specs/vocabulary-missing/**
  - kind: repository_path
    ref: .agents/skills/qa-gate/SKILL.md
  - kind: repository_path
    ref: internal/daemon/task_engine.go
```

- Entry point: build the current CLI, run `spec check vocabulary-missing
  --stage techspec --strict` from a fresh uncommitted fixture repository, run
  `TestGateAcceptsItsOwnDeclaredTerm`, and enumerate production callers of
  `GatePrecondition`.
- Expected: authoring continues to report the undocumented `publish:` token,
  while the authored terminal gate classifies the same Spec-owned token as
  named repair input and proceeds to its assigned repair.
- Independent confirmation: the isolated classifier covers own-declared,
  undeclared, and non-gate authoring cases; the public CLI reads the on-disk
  fixture in another process.
- Observed: the public CLI exited `1` with
  `SC-VOCABULARY-UNDOCUMENTED` for `publish:`. `GatePrecondition` appears only
  at its declaration and in `mechanical_test.go`; Task 09 removed the sole
  production call from `qaMechanicalRequest`. The active QA skill requires the
  strict command and stops before matrix construction on that exit. F-001 is a
  regression of the first report's production-boundary finding.

### R04

```yaml
inputs:
  - kind: repository_path
    ref: internal/spec/task.go
  - kind: repository_path
    ref: internal/daemon/task_engine.go
  - kind: repository_path
    ref: internal/speccheck/mechanical.go
  - kind: repository_path
    ref: internal/speccheck/report.go
```

- Entry point: a scratch copy of current head loaded an on-disk QA Task with an
  exact repair path and replacement, built the request through
  `qaMechanicalRequest`, ran `RunMechanicalStage`, reread the repaired PRD, and
  wrote/reread the seeded QA Report through `writeMechanicalQAReport`.
- Expected: the exact declaration crosses every production boundary, writes
  only the named path, persists the replacement, and records the verified
  repair under `## Performed repairs`.
- Independent confirmation: `TestQAProbeAssignedRepairRunsThroughDaemonBoundary`
  passed in the scratch source tree. The repository's skipped, unassigned, and
  out-of-bounds cases separately proved no unauthorized write.
- Observed: one repair was performed and verified after write; its identifier
  and verification record survived in the seeded report. The scratch tree and
  binary remain under `/private/tmp` and are not repository evidence.

### R05

```yaml
inputs:
  - kind: repository_path
    ref: docs/backlog/2026-08-14-a-spec-that-coins-a-term-cannot-pass-its-own-gate.md
  - kind: repository_path
    ref: docs/history/specs/0103-a-suite-that-leaks-nothing/qa/qa-report-2026-08-14.md
  - kind: repository_path
    ref: docs/history/specs/0103-a-suite-that-leaks-nothing/qa/qa-report-2026-08-14-01.md
  - kind: repository_path
    ref: docs/history/specs/0114-an-audit-that-reads-the-authorization-it-was-given/qa/qa-report-2026-08-15.md
  - kind: repository_path
    ref: internal/daemon/task_engine.go
  - kind: repository_path
    ref: internal/speccheck/mechanical.go
```

- Entry point: read the pre-Spec backlog record, both Spec 0103 reports, and the
  Spec 0114 report; replay their described behaviors against the current writer,
  successor detector, classifier, Daemon request builder, and repair stage.
- Expected: the sources resolve outside this Spec and the report states what the
  current node does with each, including any remaining defect.
- Independent confirmation: Git history places all four source artifacts before
  this Spec's implementation, and none changed between previous QA head
  `022482b0` and current head.
- Observed: the Spec 0103 empty-table successor is repaired; the Spec 0114 Task
  declaration now maps, writes, verifies, and reports repairs; the Spec 0103
  coined-term deadlock returns after Task 09 removed the production classifier,
  as F-001 records.

### R06

```yaml
inputs:
  - kind: repository_path
    ref: internal/daemon/task_engine.go
  - kind: repository_path
    ref: internal/speccheck/**
  - kind: repository_path
    ref: internal/spec/**
  - kind: repository_path
    ref: docs/specs/0113-a-gate-report-that-does-not-block-its-successor/_prd.md
  - kind: repository_path
    ref: .git
```

- Entry point: current-source negative controls plus
  `git diff-tree --no-commit-id --name-only -r` for Task commits `c9c7b761`,
  `d4d01a0d`, `57da94ce`, `2430a536`, `40076dbf`, `d607098d`, `24379a99`,
  and `4cb6fdec`.
- Expected: the mechanical stage remains; malformed reports, undeclared terms,
  skipped repairs, unassigned observations, and out-of-bounds paths remain
  refused; report/count meanings and prior evidence remain unchanged; no Task
  commit changes a Governed Path.
- Independent confirmation: exact-byte report and post-repair rereads passed.
  Git, not Task prose, supplied every committed path.
- Observed: each Task commit contains its Task file and ordinary Go source/tests
  under `internal/daemon`, `internal/speccheck`, or `internal/spec`. No
  configuration, script, ignore file, dependency, plugin declaration, version
  pin, or other Governed Path changed, so no commit-dependent tooling row
  applies.

### R07

```yaml
inputs:
  - kind: repository_path
    ref: Makefile
  - kind: repository_path
    ref: '**/*.go'
  - kind: repository_path
    ref: go.mod
  - kind: repository_path
    ref: go.sum
  - kind: live_service
    ref: macOS process-table access in the managed QA sandbox
```

- Entry point: required `rtk make verify`, unchanged-head reproduction of its
  two failures, then fresh complete affected-package and public Implement runs.
- Expected: the full repository gate exits `0`; when the sandbox prevents that,
  the exact environmental cause and equivalent observed evidence are required.
- Independent confirmation: the two Force Stop tests fail identically at
  previous QA head `022482b0`; Task 09 changes no Force Stop path. Every
  `TestRunImplement` journey and complete fresh runs of `internal/daemon`,
  `internal/speccheck`, and `internal/spec` exit `0` at current head.
- Observed: only process-table enumeration is unreachable. Unblocking requires a
  full-access macOS run that permits those two tests to enumerate the owned
  process tree. Equivalent current-build evidence covers every changed and
  adjacent surface, so this environment-blocked row is fully evidenced.

### R08

```yaml
inputs:
  - kind: repository_path
    ref: CONTEXT.md
  - kind: repository_path
    ref: docs/specs/0113-a-gate-report-that-does-not-block-its-successor/_techspec.md
  - kind: repository_path
    ref: internal/speccheck/mechanical.go
```

- Entry point: compare the emitted repair vocabulary with the TechSpec
  Vocabulary Contract and glossary, then rerun the current strict check.
- Expected: `Assigned Repair` has one canonical definition and matching emitted
  pattern; Task 09 introduces, changes, or retires no other domain term.
- Independent confirmation: exact searches resolve `CONTEXT.md:31`, the
  contract pattern `[Aa]ssigned [Rr]epair`, and the emitted source language.
- Observed: the current source strict check is clean. Task 09 changes no
  vocabulary owner, so no domain-context edit is required in this gate.

### R09

```yaml
inputs:
  - kind: repository_path
    ref: docs/specs/0113-a-gate-report-that-does-not-block-its-successor/qa/qa-report-2026-08-15-02.md
  - kind: repository_path
    ref: .git
```

- Entry point: inspect the closed report, QA-tree file modes, current worktree
  status, frontmatter counters, flow-row statuses, and every declared input.
- Expected: nine terminal flow rows, exact blocked counters, no missing input,
  and no binary, Gitlink, scratch repository, or unauthored evidence added by
  this rerun.
- Independent confirmation: Git index modes, filesystem paths, report text, and
  the current-source strict checker are read separately after closure.
- Observed: this rerun adds only `qa-report-2026-08-15-02.md`, a UTF-8 Markdown
  file. Prior reports remain unchanged. Scratch repositories, caches, and
  binaries stay under `/private/tmp`; no mode `160000` exists below the Spec.
  All repository inputs resolve, all nine flow rows are terminal, and the one
  environment-blocked row matches `rows_blocked_environment: 1`.

## Findings

### F-001 — Gate-only vocabulary classification again has no production boundary

- Impact: `Blocks-Completion`.
- History: regression of F-001 in
  `qa-report-2026-08-15.md`; Task 08 connected the classifier, and Task 09
  removed that production call without placing it at another gate boundary.
- Actor and journey: Supervisor running an authored terminal gate for a Spec
  whose complete Vocabulary Contract declares a term not yet in its glossary.
- Expected: authoring reports the omission, while that Spec's gate classifies the
  finding as a named repair input, performs the Task-assigned glossary repair,
  verifies it, and proceeds.
- Actual: the real strict command exits `1` on the Spec-owned token and the
  active QA contract stops before matrix construction. `GatePrecondition` has
  no production caller, so no request carries the repair input.
- Reproduction: from a fresh uncommitted copy of
  `internal/speccheck/testdata/repo`, run
  `/private/tmp/roundfix-0113-qa02 spec check vocabulary-missing --stage
  techspec --strict`; it reports `SC-VOCABULARY-UNDOCUMENTED` for `publish:`.
  Then run `rg -n 'GatePrecondition\\(' --glob '*.go' .`; only the declaration
  and tests resolve.
- Required change: place gate-only classification at the strict-check boundary
  the authored QA flow actually uses, without turning Spec consistency into an
  Implement-command precondition, and prove the full coined-term journey.
- Affected row: R03.

## Blocked and skipped

R07 is `blocked (environment: macOS process-table enumeration denied)`.
Evidence is the identical previous-head failure plus clean current-source public
Implement and backend runs. A full-access macOS process table unblocks the two
Force Stop integrations. No row is finding-blocked, declared-blocked, or
skipped.

The mechanical detector skips below remain byte-preserved from the seeded
report. They are presence records, not flow-row `skipped` statuses.

## Mechanical skips

| Detector | Missing artifact |
| --- | --- |
| authorization bounded paths | tooling authorization |
| consequent-fix commit order | consequent-fix declaration |
| QA evidence paths | docs/specs/0113-a-gate-report-that-does-not-block-its-successor/qa/evidence |

## Coverage

All nine rows are terminal: seven pass, one fails, and one is
environment-blocked with equivalent evidence. Every PRD user story, all seven
Core Features, all five Non-Goals, and the outside-evidence obligation map to an
executed row.

High-risk probes cover blocking and nonblocking zero-row reports, optional
skips, successor rereads, non-terminal and unreadable tables, missing literals,
unrecognised blocked causes, all three typed totals, parse-only versus genuine
count disagreements, undeclared versus Spec-owned vocabulary, absent repair
declarations, skipped repairs, unassigned observations, out-of-bounds writes,
and the no-Spec-check public Implement contract. There is no frontend, TUI,
network, database, or Pull Request journey in scope.

Current counts: `rows_blocked_environment: 1`,
`rows_blocked_finding: 0`, and `rows_blocked_declared: 0`. Findings by impact:
one `Blocks-Completion`; zero `Data-Loss`, `Trust-Damage`, `Friction`, or
`Cosmetic`.

## Final verdict

`fail` — Task 09 restores truthful refusal rows and the public Implement
journeys, and the assigned-repair chain works end to end, but a Spec-owned
undocumented term still stops its own authored gate because
`GatePrecondition` again has no production caller. Place that classification at
the gate's real strict-check boundary, prove the complete coined-term journey,
and rerun this gate. The full repository gate also needs a full-access macOS
process table to re-observe its two unchanged Force Stop integrations.
