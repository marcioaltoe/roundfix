---
spec: 0119-spec-contained-authorization
date: 2026-09-10
build: 026b191262ec3cd4a5ec5a4cbd1ac12f097829c6
verdict: pass
auditing_binary: "0.12.0 (026b1912-dirty, built 2026-09-10 16:29:24 -0300)"
auditor_staleness: "current: rebuilt from audited HEAD 026b1912; dirty state is this seeded QA report"
status: closed
rows_blocked_environment: 3
rows_blocked_finding: 0
rows_blocked_declared: 0
surfaces: [backend, cli, docs]
---

# QA Report

## Performed repairs

None.

## Assigned repair failures

None.

## Authorization audit inputs

| Task | Outcome | Record | Revision | Detail |
| --- | --- | --- | --- | --- |
| task_16 | granted | docs/specs/0119-spec-contained-authorization/_authorization.md | 52a9989c1f13081b00bd4329d9362292beff0ea8 | operative grant |

## Mechanical findings

None.

## Scope and environment

Full terminal QA rerun at audited head
`026b191262ec3cd4a5ec5a4cbd1ac12f097829c6` in the Run Worktree. The actors
are the maintainer approving one Spec-contained grant, the Supervisor deciding
whether work may dispatch, and the reviewer auditing committed scope and
delivery authority. The authored `task_10` QA node is terminal and all fifteen
non-QA Tasks are marked `completed`.

The public CLI entry point is
`/private/tmp/roundfix-0119-qa-04-bin/roundfix`, rebuilt from audited HEAD with
the repository's Go 1.26.1 toolchain. Its `-dirty` identity records this seeded,
untracked QA report; no implementation path was dirty when the run began.
Scratch repositories, build caches, and generated-state checks use
`/private/tmp`.

The supplied Pull Request fact says no Pull Request is open for
`feat/spec-contained-authorization`; the per-Run branch has no Pull Request of
its own. QA-12 therefore uses the required environment-blocked
equivalent-evidence path. Release, deployment, credential, destructive, and
paid-service actions are outside this Spec's authority and will not run.

The Daemon's seeded authorization input, empty mechanical findings, and
mechanical skips are preserved. QA-02 is carried because its prior report is in
current ancestry and its declared reader/checker inputs are byte-identical.
Every row whose input changed under Tasks 15 or 16 was re-observed.

## Static gate

- `/private/tmp/roundfix-0119-qa-04-bin/roundfix spec check 0119-spec-contained-authorization --strict`: pass (exit 0); no findings, no authored Verification execution, and only absent Finding, Rollup, and Backlog artifact classes skipped.
- `PATH=/usr/local/go/bin:/Users/marcio/.cargo/bin:/opt/homebrew/bin:/usr/bin:/bin GOCACHE=/private/tmp/roundfix-0119-qa-04-cache make GO=/usr/local/go/bin/go BIN=/private/tmp/roundfix-0119-qa-04-bin/roundfix BUILD_COMMIT=026b1912-dirty verify`: pass (exit 0) on the one authorized unchanged rerun after the sandboxed attempt was interrupted by blocked `api.github.com` access. Every package, skill check, skill-sync check, and CLI build completed; `internal/daemon` passed as one concurrent package run in 9.556 seconds.
- `PATH=/usr/local/go/bin:/Users/marcio/.cargo/bin:/opt/homebrew/bin:/usr/bin:/bin GOCACHE=/private/tmp/roundfix-0119-qa-04-cache make GO=/usr/local/go/bin/go BIN=/private/tmp/roundfix-0119-qa-04-bin/roundfix BUILD_COMMIT=026b1912-dirty verify-docs`: pass (exit 0); documentation contracts, repository regeneration contracts, corpus budget, and the full active-Spec check completed.

## Results

| # | Story / criterion / sweep | Actor and surface | Status | Evidence |
| - | --- | --- | --- | --- |
| QA-01 | Goals 1-2, US-1/US-2 and CF-1: the current Spec-contained typed grant is operative, exact, self-contained, and accepted by strict authoring validation | Maintainer and Supervisor · CLI/docs | pass | The rebuilt strict check passed; the fresh current-record test accepted the 25-path through-merge grant, and Git rereads prove the amendment predates every Task. |
| QA-02 | Task 02/03 and declared break 1: proposed, null-dated, withdrawn, wrong-consumer, contradictory, unknown-operation, and unsafe-path records do not become grants; an honest proposal remains inspectable | Maintainer · CLI/Git | carried (established by: qa-report-2026-09-10-02.md; head: 026b1912) | The prior public refusal matrix remains bound to byte-identical reader and checker inputs; its report commit is an ancestor of this head. |
| QA-03 | CF-5 and Task 13: the closed operation vocabulary is enforced independently at strict checking, Implement dispatch, Task commit, push, and Settle | Maintainer and Supervisor · CLI/Git | blocked (environment: default Artifact Directory is outside the writable sandbox) | Public strict checking refused missing `implement`; Implement then hit the sandbox denial before authority. Fresh current-head public-boundary tests supply equivalent evidence for Implement, commit, push, and Settle with no side effects. |
| QA-04 | Goal 4, CF-3, and outside evidence: preserved authorization records at revision `6b8ea487...` remain readable with original provenance, including legacy multi-Spec scope | Reviewer · Git/backend | pass | Fresh Git inventory returned 42 records, and the current historical-reader test passed against those immutable objects. |
| QA-05 | US-3, CF-2, and Tasks 05/14/16: every governed Task change stays within the grant at its authorizing ancestor, both canonical citation forms drive a non-empty audit, and sanctioned regeneration remains reproducible | Reviewer · Git/CLI | pass | The 25-path amendment is an ancestor of all 15 non-QA Task commits; all changed paths were enumerated, Task 16 touches only its Task plus two granted files, and two digest runs changed nothing. |
| QA-06 | Goal 3, CF-6, and declared break 3: read-only checking remains available and this Spec ships no authored-command execution gate | Supervisor · CLI/Git | pass | Public read-only checking left an external marker absent; opted-in Verification executed the modified authored command, and an independent marker and HEAD reread confirmed the final compatibility behavior. |
| QA-07 | Task 15: every source-trust caller and token is withdrawn, authored commands execute as before, the record half remains intact, and no per-command Git subprocess remains | Supervisor and reviewer · CLI/backend/docs | blocked (environment: default Artifact Directory is outside the writable sandbox) | Public Settle hit the sandbox denial before Verification. The public Spec Check execution, complete gate, fresh Settle integration test, source sweep, glossary reread, and record-contract canaries provide equivalent evidence. |
| QA-08 | Declared break 2 and Tasks 04/09/11/16: Governed Path and sanctioned-regeneration discovery include active, archived, and legacy grants while ordinary paths and one parser remain preserved | Reviewer · backend/docs | pass | Race-enabled current-head suite-guard packages passed; decoder ownership remains singular, repeated resolution uses the new cache contract, and sanctioned regeneration stayed stable. |
| QA-09 | CF-3 and Tasks 07/08/12: canonical Baseline guides, authoring skills, and templates place the record inside the Spec while preserving prior wording; generated guides, shipped skills, and digest pins converge | Maintainer · docs/CLI | pass | Two public managed refreshes each reported `File changes: 0`; two digest runs reported `changed:false`, skill synchronization passed, and the clean clone stayed clean. |
| QA-10 | Vocabulary Contract and glossary currency: the checker runs the contract, `SC-TOOLING-UNAPPROVED` is owned and defined, the withdrawn token is absent, and no additional term needs a glossary update | Maintainer · docs/CLI | pass | Strict current-Spec and active-corpus checks ran vocabulary validation; source and glossary searches find only the remaining documented refusal code. |
| QA-11 | Task 16, Non-Goals, and regression lock: the full concurrent gate passes after reducing archive-scan cost, without deadline, parallelism, skip, dependency, auth, release, deployment, paid-service, or Task-status ownership changes | Maintainer and reviewer · CLI/Git | pass | `make verify` and `make verify-docs` passed; `internal/daemon` passed concurrently in 9.556s, all deadlines remain two seconds, no skip or dependency change exists, and fresh status-ownership cases passed. |
| QA-12 | Pull Request approval, checks/status, unresolved threads, Merge-Ready acceptance, and review-artifact ancestry controls | Maintainer · Pull Request | blocked (environment: no open Pull Request) | The supplied fact blocks the PR surface; all five controls have exact-head equivalent evidence below. |

## Row evidence

### QA-01

```yaml
inputs:
  - kind: repository_path
    ref: docs/specs/0119-spec-contained-authorization/_authorization.md
  - kind: repository_path
    ref: docs/specs/0119-spec-contained-authorization/_prd.md
  - kind: repository_path
    ref: docs/specs/0119-spec-contained-authorization/_techspec.md
```

Passed. The rebuilt CLI returned no strict finding. The fresh cross-package
selection passed `TestCurrentRecordPermitsItsDeclaredOperations`, accepting the
record as operative and confirming `implement`, `commit`, and `push`. Direct
Git reads show that commit `3e833b006ed08e685ab3598562db6834ac81ae69`
carries the final 25-path amendment, is an ancestor of every non-QA Task commit,
and has no authorization-file difference from audited HEAD. Release remains
absent from the operation list.

### QA-02

```yaml
inputs:
  - kind: repository_path
    ref: internal/speccheck/constraints.go
  - kind: repository_path
    ref: internal/authorization/authorization.go
```

Carried from `qa-report-2026-09-10-02.md`, whose commit `81ba806f` is an
ancestor of audited HEAD. A fresh Git diff from the carried report head through
`026b1912` found no change in `internal/speccheck/constraints.go` or
`internal/authorization/authorization.go`. The carried public matrix covers
contradictory approval state, null grant date, withdrawal, wrong consumer,
absolute path, traversal, glob, symlink, duplicate path, unknown operation, and
an honest proposal. The current active-Spec strict sweep independently passed.

### QA-03

```yaml
inputs:
  - kind: repository_path
    ref: internal/authorization/authorization.go
  - kind: repository_path
    ref: internal/cli/implement.go
  - kind: repository_path
    ref: internal/cli/settle.go
  - kind: repository_path
    ref: internal/daemon/task_engine.go
```

Blocked by the sandbox before both public mutation-capable commands could reach
their operation boundary. In a committed scratch history without `implement`,
public strict checking exited 1 on `SC-TOOLING-UNAPPROVED` and named both the
missing operation and record. Public Implement then exited 2 because creating
the default Artifact Directory below `/Users/marcio/.roundfix/artifacts/` is
not permitted in this sandbox; HEAD and status remained unchanged. The public
Settle attempt reached the same environment refusal in its separate scratch
repository and left its marker absent and HEAD unchanged.

Equivalent current-head evidence passed in the fresh five-package selection:
`TestStrictCheckRefusesMissingImplementAuthority`,
`TestRunImplementRefusesMissingImplementAuthorityBeforeRun`,
`TestDispatchRefusesMissingImplementAuthority`,
`TestCommitAndPushAuthorityAreSeparate`, and
`TestSettleRefusesMissingCommitAuthority`. These public-runner and Daemon
contracts assert no Run, Agent, shell, Task Worktree, commit, or push side
effect at the applicable refusal. Unblocking action: rerun the public Implement
and Settle probes in a full-access session where the default Artifact Directory
is writable.

### QA-04

```yaml
inputs:
  - kind: external_repository
    ref: roundfix Git revision 6b8ea48725cbca13974eee0b400b3482202874f6
```

Passed. Fresh `git ls-tree -r --name-only 6b8ea487... --
docs/workflow/authorizations` returned 42 records authored before this Spec.
The fresh `TestAuthorizationReaderResolvesPreservedHistoricalRecords` run
passed against those immutable objects, retaining the multi-Spec consumer,
classifying no record as an accidental grant to Spec 0119, and distinguishing
an unavailable revision from both grant and refusal. This Git history is the
required outside evidence rather than a fixture authored by this Spec.

### QA-05

```yaml
inputs:
  - kind: repository_path
    ref: docs/specs/0119-spec-contained-authorization/_authorization.md
  - kind: repository_path
    ref: docs/specs/0119-spec-contained-authorization/task_*.md
  - kind: repository_path
    ref: internal/speccheck/governed.go
  - kind: repository_path
    ref: internal/suiteguardcontract/regeneration.go
```

Passed. Fresh `git diff-tree --no-commit-id --name-status -r <commit>` output
was inspected for every commit between the final amendment and audited HEAD.
The current `Roundfix-Task` trailers identify all 15 non-QA Task commits, and
`git merge-base --is-ancestor 3e833b00 <task-commit>` exited 0 for each. The
final record is byte-identical at audited HEAD.

The previously replayed Tasks 01-14 retain the clean chronology established by
`qa-report-2026-09-10-02.md`. The new Task 16 commit `026b1912` changes only
`task_16.md`, `internal/suiteguardcontract/regeneration.go`, and
`internal/suiteguardcontract/regeneration_test.go`; the two governed paths are
in the exact grant. The Daemon's preserved authorization row independently
records the operative record at `52a9989c`, which is an ancestor of Task 16.
Two clean-clone `make baseline-digests` runs returned `changed:false`, covering
the sanctioned generated outputs. The resolved-reference and both-citation
audit tests also passed in the fresh selection.

### QA-06

```yaml
inputs:
  - kind: repository_path
    ref: internal/cli/spec_check.go
  - kind: repository_path
    ref: internal/speccheck/verification.go
```

Passed through the rebuilt public CLI. In a committed copy of the repository's
`coverage-range` fixture, Task 01 was modified to run
`touch /private/tmp/roundfix-0119-qa04.Omxw94/qa-source-ran-nonzero; false`.
Read-only strict checking exited 0, reported that authored Verification was not
executed, and an independent shell read found no marker. The same command with
`--run-verification` executed one authored command and classified its nonzero
pre-work exit as honest; an independent read found the marker. Git HEAD stayed
`3079a62c`, proving the public check did not write history. This observes the
final declared break: the withdrawn source gate does not refuse the modified
artifact, while read-only checking remains available.

### QA-07

```yaml
inputs:
  - kind: repository_path
    ref: internal/daemon/**
  - kind: repository_path
    ref: internal/cli/settle.go
  - kind: repository_path
    ref: internal/speccheck/**
  - kind: repository_path
    ref: CONTEXT.md
```

Blocked only at the public Settle subjourney by the same Artifact Directory
permission error recorded under QA-03. Settle exited 2 before Verification in a
scratch repository, left its external marker absent, kept Task status `failed`,
and left HEAD unchanged. Unblocking action: rerun that public probe with a
writable default Artifact Directory in a full-access session.

Equivalent evidence is complete. QA-06 observes public modified-source
execution through Spec Check. The fresh current-head selection passed
`TestSpecCheckRunVerification` and
`TestSettleExecutesCommandFromModifiedSource`, and the complete repository gate
passed. A source sweep found no `AuthorizeAuthoredCommands`,
`ProbeAuthoredCommands`, or `SC-SOURCE-UNTRUSTED` below `internal/` or in
`CONTEXT.md`; `internal/speccheck/verification_source.go` is absent. The
authorization, operation, constraint, resolved-reference, and suite-guard
canaries passed, so withdrawing the execution boundary did not weaken the
record half.

### QA-08

```yaml
inputs:
  - kind: repository_path
    ref: internal/speccheck/governed.go
  - kind: repository_path
    ref: internal/suiteguardcontract/regeneration.go
  - kind: repository_path
    ref: internal/authorization/authorization.go
```

Passed. The race-enabled, shuffled current-head run of
`./internal/suiteguardcontract ./internal/suiteguard` passed. It covers active,
archived, multi-Spec, frontmatter-free legacy, proposed, null-dated, malformed,
wrong-consumer, candidate-file-count, and once-per-process cache cases. The
fresh authorization and resolved-reference selection also passed. A source
sweep found `parseAuthorizationRecord`, `splitAuthorizationFrontmatter`, and
`authorizationFrontmatterMapping` only in
`internal/authorization/authorization.go`. The two public digest runs in the
clean clone changed nothing.

### QA-09

```yaml
inputs:
  - kind: repository_path
    ref: internal/baseline/assets/modules/*.json
  - kind: repository_path
    ref: .agents/skills/write-*/**
  - kind: repository_path
    ref: skills/write-*/**
  - kind: repository_path
    ref: docs/agents/setup-context.json
```

Passed through the rebuilt public CLI in a clean current-head clone. The first
and second `roundfix baseline update --repo . --no-skills --yes --format text`
runs both reported `Baseline update: verified`, identical prior/current catalog
digest `sha256:136f8ff3...`, `File changes: 0`, and verified idempotence. Git
stayed clean after both. Two later `make baseline-digests` runs each returned
`ok:true, changed:false`; Git remained clean, and `make skills-sync-check`
passed. The complete `make verify-docs` gate independently validated the
documentation, ownership, regeneration, and active-Spec contracts.

### QA-10

```yaml
inputs:
  - kind: repository_path
    ref: CONTEXT.md
  - kind: repository_path
    ref: docs/specs/0119-spec-contained-authorization/_techspec.md
  - kind: repository_path
    ref: internal/speccheck/constraints.go
```

Passed. Neither the strict current-Spec check nor the complete active-Spec sweep
listed vocabulary validation among its skips. Source inspection finds
`SC-TOOLING-UNAPPROVED` emitted by
`internal/speccheck/constraints.go`, and `CONTEXT.md` defines that token under
`Grant Refusal Code`. A negative search found no `SC-SOURCE-UNTRUSTED` in code
or glossary. Task 16 adds no user-facing term, so the glossary currency check
found no further update due.

### QA-11

```yaml
inputs:
  - kind: repository_path
    ref: '**/*.go'
  - kind: repository_path
    ref: go.mod
  - kind: repository_path
    ref: go.sum
  - kind: repository_path
    ref: docs/specs/0119-spec-contained-authorization/_prd.md
```

Passed. The authorized unchanged full `make verify` rerun exited 0 after the
initial sandboxed attempt was interrupted by blocked `api.github.com` access.
Every package completed; `internal/daemon` passed as one concurrent package run
in 9.556 seconds. `make verify-docs` also exited 0. Eight fresh `time.After`
reads in `internal/daemon/task_engine_test.go` all remain
`2 * time.Second`, and a diff search found no added `t.Skip(` call. The
race-enabled suite-guard packages passed, so the once-per-process cache also
survives concurrent access.

`go.mod` and `go.sum` have no difference from the independently committed
grant amendment. The change adds no HTTP endpoint, authentication layer,
credential policy, dependency, deployment, release, paid-service path, or
different Task-status owner. Five fresh Daemon status/Result ownership cases
passed, preserving `in_progress` before Agent work, normalization before
Verification, and Result content through repair.

### QA-12

```yaml
inputs:
  - kind: live_service
    ref: GitHub Pull Request for feat/spec-contained-authorization
```

Blocked by the supplied environment fact: no Pull Request is open for
`feat/spec-contained-authorization`, and the Run Worktree branch never has one.

Equivalent controls for audited head `026b1912`:

- approval: the maintainer's final 25-path through-merge amendment is commit
  `3e833b00`, an ancestor of every non-QA Task; this QA report supplies the
  terminal gate evidence, while no pre-Pull-Request review acceptance exists;
- checks and status: `make verify` and `make verify-docs` pass on audited HEAD;
- unresolved review threads: no Pull Request exists, so no PR thread exists or
  is claimed resolved;
- Merge-Ready acceptance: none exists for this head;
- review-artifact ancestry: the Spec has no `reviews/` directory and no
  accepted review artifact is claimed for this head.

Unblocking action: integrate the passing QA report through the Daemon, obtain
the configured pre-Pull-Request review for the resulting candidate, open the
target-branch Pull Request, and observe its required checks and review state.

## Findings

None. The previous F-005 full-suite timeout did not reproduce after Task 16:
the complete current-head gate passed, including `internal/daemon` as one
concurrent run, and the race-enabled cache contract passed.

## Blocked and skipped

- QA-03 is `blocked (environment: default Artifact Directory is outside the
  writable sandbox)`. The public strict refusal ran, both mutation-capable
  commands recorded no side effects, and fresh public-runner/Daemon tests cover
  every operation boundary on current HEAD.
- QA-07 is blocked by the same environment cause for its public Settle
  subjourney. Public Spec Check observed the final modified-source behavior;
  the complete gate, fresh Settle integration test, source sweep, glossary
  reread, and record canaries provide equivalent evidence.
- QA-12 is `blocked (environment: no open Pull Request)`. The supplied fact
  proves the cause, and the row answers approval, checks/status, unresolved
  threads, Merge-Ready acceptance, and review-artifact ancestry for exact head
  `026b1912`.
- No matrix row is finding-blocked, declared-blocked, or skipped. The two
  entries under `Mechanical skips` are preserved Daemon materialization, not
  skipped matrix rows, and do not change the counters.

## Coverage

Closed: 12 rows — 8 fresh passes, 1 carried pass, 3 blocked by environment
with equivalent observed evidence, 0 failed, 0 blocked by finding, 0 blocked by
declaration, 0 skipped, and 0 pending. The matrix maps Goals 1-4, User Stories
1-3, Core Features 1-6, all three declared intentional breaks, the historical
outside-evidence obligation, changed-file chronology, Vocabulary Contract,
glossary currency, Non-Goals, and every Task acceptance group.

Behavior probes covered missing operation authority, modified authored command
execution with independent marker rereads, Artifact Directory sandbox denial,
all 42 historical records, exact Task commit ancestry and paths, active and
archived grant discovery, once-per-process caching under the race detector, two
managed refreshes, two digest regenerations, the full concurrent gate, fixed
two-second deadlines, and Task-status ownership. The carried refusal matrix
adds proposed, null-dated, withdrawn, wrong-consumer, contradictory,
unknown-operation, absolute, traversal, glob, symlink, and duplicate-path
inputs. No Pull Request, release, deployment, credential, destructive, or paid
service journey ran.

Finding totals: 0 `Blocks-Completion`, 0 `Data-Loss`, 0 `Trust-Damage`, 0
`Friction`, and 0 `Cosmetic`.

## Final verdict

`pass`: every runnable row passed, the one repository-only carried row retains
byte-identical inputs, and all three environment-blocked rows record their
cause, unblocking action, and equivalent current-head evidence. No row failed,
is finding-blocked or declared-blocked, or was skipped.

## Mechanical skips

| Detector | Missing artifact |
| --- | --- |
| consequent-fix commit order | consequent-fix declaration |
| QA evidence paths | docs/specs/0119-spec-contained-authorization/qa/evidence |
