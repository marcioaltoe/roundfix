---
spec: 0119-spec-contained-authorization
date: 2026-09-10
build: 3eb6f85b3ff07e3c9748cbdc95782a7a0709546c
auditing_binary: "0.12.0 (3eb6f85b-dirty, rebuilt from audited HEAD; dirty state is this seeded QA report)"
auditor_staleness: "current: QA binary was rebuilt from audited HEAD 3eb6f85b"
status: closed
verdict: fail
rows_blocked_environment: 1
rows_blocked_finding: 6
rows_blocked_declared: 0
surfaces: [backend, cli, docs]
---

# QA Report

## Performed repairs

None.

## Assigned repair failures

None.

## Mechanical findings

### QA-AUTH-PATHS

- location: `docs/specs/0119-spec-contained-authorization/_authorization.md:1`
- detail: Task task_13 commit 9b7f9615136ce47ec7968c1abc60500d0585f685 changes internal/speccheck/coherence.go outside authorization grant docs/specs/0119-spec-contained-authorization/_authorization.md's exact bounded files
- fix: Move the path into an expressly authorized Task or narrow the commit to the written authorization and assigned Task file.
- blocked row: `task_13`

## Scope and environment

Full rerun of the terminal QA gate at
`3eb6f85b3ff07e3c9748cbdc95782a7a0709546c`, three commits after the
previous report head `afb90f668cb8fe167973e8dd20701f511c47959b`.
The intended actors are the maintainer approving a Spec-contained grant, the
Supervisor deciding whether work may dispatch, and the reviewer auditing
committed scope and delivery authority.

The public CLI entry point is
`/private/tmp/roundfix-0119-qa-rerun-bin/roundfix`, rebuilt from the audited
tree. The `-dirty` identity reflects this seeded, untracked QA report; no
implementation path was dirty when the run began. The Daemon seeded the
mechanical stage with `0.12.0 (c4bcff44, built 2026-09-10 09:50:56 -0300)`;
its materialized finding and skips remain below.

The supplied Pull Request fact says no Pull Request is open, so QA-12 uses the
environment-blocked equivalent-evidence path. Scratch repositories and build
caches use `/private/tmp`. This checkout may receive only this report and its
Spec-local QA evidence.

## Static gate

- `rtk proxy /private/tmp/roundfix-0119-qa-rerun-bin/roundfix spec check 0119-spec-contained-authorization --strict`: pass (exit 0); no findings, and authored Verification commands were not executed. The checker skipped only absent Finding, Rollup, and Backlog artifact classes.
- `rtk make verify`: the host Go 1.27.1 run exited 2 at `fmt-check` because that newer formatter rewrites two files unchanged since `8d333a6c`. `/usr/local/go/bin/gofmt` from the repository's Go 1.26.1 toolchain reports both files clean. The unchanged target rerun as `PATH=/usr/local/go/bin:$PATH rtk make verify` passed after all packages, skill checks and the CLI build completed. Its first sandboxed attempt was interrupted by blocked `api.github.com` access and produced no terminal verdict; the authorized unchanged rerun is the evidence-bearing result.
- `PATH=/usr/local/go/bin:$PATH rtk make verify-docs`: pass (exit 0); docs contracts, regeneration contracts, corpus budget and the full active-Spec check passed.

## Results

| # | Story / criterion / sweep | Actor and surface | Status | Evidence |
| - | --- | --- | --- | --- |
| QA-01 | Goals 1-2, US-1/US-2 and CF-1: the current Spec-contained typed grant is operative, exact, self-contained and accepted by strict authoring validation | Maintainer and Supervisor · CLI/docs | pass | Rebuilt strict checking passed; a fresh reader run accepted the unchanged 23-path, through-merge record. |
| QA-02 | Task 02/03 refusal criteria and declared break 1: proposed, null-dated, withdrawn, wrong-consumer, contradictory and unsafe-path records do not become grants; an honest proposal remains inspectable | Maintainer · CLI/Git | blocked (finding: F-003 — waits on authorized consuming history) | Flow stopped after the complete changed-path audit; the fresh focused refusal selection passes but is not a user-flow substitute. |
| QA-03 | CF-5 and Task 13 operation boundaries: the closed operation vocabulary is enforced independently at strict checking, Implement dispatch, Task commit, push and Settle | Maintainer and Supervisor · CLI/Git | blocked (finding: F-003 — waits on authorized consuming history) | Flow stopped before public dispatch/Settle journeys; all named action-boundary checks pass as static equivalent evidence. |
| QA-04 | Goal 4, CF-3 and outside evidence: preserved authorization records at revision `6b8ea487...` remain readable with original provenance, including legacy multi-Spec scope | Reviewer · Git/backend | pass | Git exposes 42 preserved records; the fresh full-corpus reader run passed against that immutable revision. |
| QA-05 | US-3, CF-2 and Tasks 05/14: every governed Task change stays within the ancestor grant, and both canonical citation forms drive a non-empty changed-path audit | Reviewer · Git/CLI | fail | F-003 repeats and widens: Tasks 05, 13 and 14 change governed paths outside the exact grant; the seeded `QA-AUTH-PATHS` finding independently reports Task 13. |
| QA-06 | Goal 3, CF-6 and declared break 3: read-only checking remains available while modified or untracked authored commands refuse execution without an ancestor approval | Supervisor · CLI/Git | blocked (finding: F-003 — waits on authorized consuming history) | Flow stopped before the public marker journey; source-refusal and approved-source checks pass as static equivalent evidence. |
| QA-07 | Task 06 execution seams: Implement dispatch and Settle apply the same source refusal before Agent work, shell execution or commit; Daemon-owned status and Result remain outside the authored projection | Supervisor · CLI/Git | blocked (finding: F-003 — waits on authorized consuming history) | Flow stopped before public Settle; probe, post-Agent, Settle and authored-projection checks pass as static equivalent evidence. |
| QA-08 | Declared break 2 and Tasks 04/09/11: Governed Path and sanctioned-regeneration discovery grow to Spec-contained and archived grants while legacy declarations, ordinary paths and one parser remain preserved | Reviewer · backend/docs | blocked (finding: F-003 — waits on authorized consuming history) | Flow stopped before public regeneration journeys; fresh governed-set and regeneration contract selections pass. |
| QA-09 | CF-3 and Tasks 07/08/12: canonical Baseline guides, authoring skills and templates place the record inside the Spec while preserving prior wording; generated guides, shipped skills and digest pins converge | Maintainer · docs/CLI | blocked (finding: F-003 — waits on authorized consuming history) | Flow stopped before public managed refresh; templates, skill sync, digest identity and repository docs gates pass statically. |
| QA-10 | Vocabulary Contract and glossary currency: both Grant Refusal Codes are defined by their emitting owners, the checker runs the contract, and no additional term needs a glossary update | Maintainer · docs/CLI | pass | Strict checking ran the detector; both constants match the glossary and the term predates their emitters. |
| QA-11 | Non-Goals and regression lock: no authentication service, global permission database, arbitrary-shell authority, deployment/release grant, paid call, Task-status ownership change or undeclared public behavior entered the change | Reviewer · CLI/Git | pass | Dependencies are unchanged, the complete gate passes, and fresh Daemon ownership cases preserve status authority. |
| QA-12 | Pull Request approval, checks/status, unresolved threads, Merge-Ready acceptance and review-artifact ancestry controls | Maintainer · Pull Request | blocked (environment: no open Pull Request) | The supplied fact blocks the PR surface; all five controls have head-bound equivalent evidence below. |

## Row evidence

### QA-01

```yaml
inputs:
  - kind: repository_path
    ref: docs/specs/0119-spec-contained-authorization/_authorization.md
  - kind: repository_path
    ref: docs/specs/0119-spec-contained-authorization/_prd.md
  - kind: repository_path
    ref: docs/specs/0119-spec-contained-authorization/_techspec.md
```

Passed. The rebuilt public CLI exited 0 for the strict current-Spec check with
no findings. `git diff --exit-code 454d6784..HEAD --
docs/specs/0119-spec-contained-authorization/_authorization.md` also exited 0,
so the read was against the same committed record that predates the consuming
work. A fresh `TestCurrentRecordPermitsItsDeclaredOperations` run accepted that
record as operative and confirmed `implement`, `commit` and `push`; the record
also carries its approved date, consuming Spec, 23 exact paths and explicit
through-merge operation list.

### QA-02

```yaml
inputs:
  - kind: repository_path
    ref: internal/speccheck/constraints.go
  - kind: repository_path
    ref: internal/authorization/authorization.go
```

Blocked by F-003 before the public scratch journeys. The fresh focused
selection for Spec-relative resolution, non-operative grant refusals and honest
proposal declarations passed under Go 1.26.1. That evidence preserves the
known field and unsafe-path contracts, but Task fixtures do not establish a
current user-flow pass. Rerun the proposed, null, withdrawn, wrong-consumer,
contradictory, absolute, traversal, glob, symlink and duplicate-path journeys
after the consuming history satisfies the grant.

### QA-03

```yaml
inputs:
  - kind: repository_path
    ref: internal/authorization/authorization.go
  - kind: repository_path
    ref: internal/cli/implement.go
  - kind: repository_path
    ref: internal/cli/settle.go
  - kind: repository_path
    ref: internal/daemon/task_engine.go
```

Blocked by F-003 before public Implement and Settle execution. Fresh static
checks passed for missing-`implement` strict refusal, dispatch before Agent
work, independent commit and push authority, public Settle's missing-`commit`
refusal, and the current record's permitted operations. Those checks answer
the repaired F-002 contract but cannot turn an unauthorized consuming commit
into a passing end-to-end journey.

### QA-04

```yaml
inputs:
  - kind: external_repository
    ref: roundfix Git revision 6b8ea48725cbca13974eee0b400b3482202874f6
```

Passed. `git ls-tree -r --name-only 6b8ea487... --
docs/workflow/authorizations` returned 42 preserved records. The fresh
`TestAuthorizationReaderResolvesPreservedHistoricalRecords` run passed against
those immutable Git objects, including the historical multi-Spec consumer and
the unavailable-revision distinction. This history predates Spec 0119 and is
the required outside acceptance evidence rather than a fixture authored by
the Spec.

### QA-05

```yaml
inputs:
  - kind: repository_path
    ref: docs/specs/0119-spec-contained-authorization/_authorization.md
  - kind: repository_path
    ref: docs/specs/0119-spec-contained-authorization/task_*.md
  - kind: repository_path
    ref: internal/speccheck/governed.go
```

Failed. Fresh `git diff-tree --no-commit-id --name-only -r <commit>` output
was inspected for all thirteen non-QA Task commits. The operative record at
`454d678464a50e88e736d7df98ee16370a097ae5` is an ancestor of audited HEAD and
is byte-identical there and now. Its exact 23-path list omits three governed
changes:

- Task 05 commit `546b8800ced45c1ba1fafad12ea2191d52f0f706`
  changes `internal/speccheck/mechanical_test.go`;
- Task 13 commit `9b7f9615136ce47ec7968c1abc60500d0585f685`
  changes `internal/speccheck/coherence.go`;
- Task 14 commit `3eb6f85b3ff07e3c9748cbdc95782a7a0709546c`
  changes `internal/speccheck/mechanical_test.go` again.

`internal/speccheck/governed.go` names both paths in the historical Governed
Path set. Neither path is a declared output of `make baseline-digests`. The
Task 13 occurrence is independently materialized above as `QA-AUTH-PATHS`;
the full audit preserves the earlier report's F-003 identity and records the
additional consuming commits instead of splitting the same defect.

### QA-06

```yaml
inputs:
  - kind: repository_path
    ref: internal/speccheck/verification.go
  - kind: repository_path
    ref: internal/cli/spec_check.go
```

Blocked by F-003 before the public marker journey. Fresh
`TestVerificationRefusesUntrustedSource` and
`TestVerificationExecutesOnCommittedProvenance` runs passed, including the
modified, untracked, out-of-tree, bound-approval and expiry cases. They are
equivalent static evidence only; the marker absence and independent public
reread remain required after the changed-path defect is corrected.

### QA-07

```yaml
inputs:
  - kind: repository_path
    ref: internal/daemon/verification_probe.go
  - kind: repository_path
    ref: internal/daemon/engine.go
  - kind: repository_path
    ref: internal/cli/settle.go
```

Blocked by F-003 before public Settle. Fresh probe, post-Agent, Settle and
authored-projection checks all passed and preserved the shared
`SC-SOURCE-UNTRUSTED` decision while allowing Daemon-owned status and Result
changes. The next QA run must still confirm the marker, Task bytes and Git head
through the public command after the authority history is repaired.

### QA-08

```yaml
inputs:
  - kind: repository_path
    ref: internal/speccheck/governed.go
  - kind: repository_path
    ref: internal/suiteguardcontract/regeneration.go
  - kind: repository_path
    ref: internal/authorization/authorization.go
```

Blocked by F-003 before public regeneration flow. Fresh governed-set tests
passed for shipped templates, monotonicity and every discovered record. Fresh
regeneration tests passed for archived, proposed, malformed, multi-Spec and
frontmatter-free legacy records. A new source search found the frontmatter
decoder only in `internal/authorization/authorization.go`. `make verify-docs`
also passed the repository regeneration contracts, but these static gates do
not replace the blocked public flow.

### QA-09

```yaml
inputs:
  - kind: repository_path
    ref: internal/baseline/assets/modules/*.json
  - kind: repository_path
    ref: .agents/skills/write-*/**
  - kind: repository_path
    ref: skills/write-*/**
  - kind: repository_path
    ref: docs/agents/setup-context.json
```

Blocked by F-003 before the public managed-refresh journey. Both template
contract tests passed in the fresh focused selection. Canonical and shipped
templates compare byte-identically and carry `express maintainer
authorization`, `bounded files` and `_authorization.md` together;
`make skills-sync-check` passed. The manifest catalog digest equals
`internal/baseline/testdata/catalog.digest`, and `make verify-docs` passed the
derived-artifact gates. The first-refresh zero-diff and second-refresh
persistence observations remain for the corrected consuming history.

### QA-10

```yaml
inputs:
  - kind: repository_path
    ref: CONTEXT.md
  - kind: repository_path
    ref: internal/speccheck/constraints.go
  - kind: repository_path
    ref: internal/speccheck/verification.go
```

Passed. Neither strict-check output nor the full active-Spec sweep listed the
Vocabulary Contract detector among its skips. The emitted constants are
exactly `SC-TOOLING-UNAPPROVED` and `SC-SOURCE-UNTRUSTED`, both defined under
`Grant Refusal Code` in `CONTEXT.md`. Commit `fed19418` introduced the glossary
term before the emitting Task commits `d62b9358` and `4a0783cc`; the rerun adds
no public term that needs another glossary edit.

### QA-11

```yaml
inputs:
  - kind: repository_path
    ref: go.mod
  - kind: repository_path
    ref: go.sum
  - kind: repository_path
    ref: docs/specs/0119-spec-contained-authorization/_prd.md
```

Passed. `go.mod` and `go.sum` are byte-identical to the grant commit. The
post-report diff changes only the operation-authority and shared-reference
boundaries declared by corrective Tasks 13 and 14; it adds no HTTP,
authentication, credential, deployment, release or paid-service surface. The
complete repository gate passed. A fresh four-case Daemon selection preserved
`in_progress` before Agent work, normalization before Verification, and Result
content across that normalization. F-003 is an authority-scope failure, but it
does not add an undeclared public behavior or alter the listed Non-Goals.

### QA-12

```yaml
inputs:
  - kind: live_service
    ref: GitHub Pull Request for feat/spec-contained-authorization
```

Blocked by the supplied environment fact: no Pull Request is open for
`feat/spec-contained-authorization`, and the Run Worktree branch never has one.
Equivalent controls for audited head `3eb6f85b`:

- approval: the maintainer's through-merge grant at `454d6784` predates the
  work, but F-003 proves this candidate exceeds its exact path scope and no QA
  acceptance exists;
- checks and status: `make verify` under Go 1.26.1 and `make verify-docs` pass,
  while terminal QA remains failed on F-003;
- unresolved review threads: no Pull Request exists, so no Pull Request thread
  stands or is claimed resolved;
- Merge-Ready acceptance: none exists for this head;
- review-artifact ancestry: no accepted review artifact is claimed for this
  head, and the prior QA report belongs to `60e05eac`, not `3eb6f85b`.

Unblocking action: repair F-003 through an ancestor grant or remove the
out-of-scope governed changes, rerun this full gate, obtain the configured
pre-Pull-Request review for the resulting head, then open the target branch
Pull Request and observe its required checks and review state.

## Findings

### F-003 — governed changes exceed Spec 0119's exact bounded paths (repeated and widened)

- Impact: `Trust-Damage` — a reviewer cannot reproduce the promised
  grant-to-change relationship from the consuming Spec.
- Actor and step: the reviewer enumerates every non-QA Task commit with
  `git diff-tree --no-commit-id --name-only -r <commit>` and compares each
  Governed Path with the authorization record already present in target
  ancestry.
- Expected: each governed change is one of the exact 23 granted paths or a
  reproducible output of `make baseline-digests`; the grant exists before the
  consuming commit.
- Actual: Task 05 and Task 14 change
  `internal/speccheck/mechanical_test.go`, and Task 13 changes
  `internal/speccheck/coherence.go`. Both paths are governed and neither is in
  Spec 0119's grant or the regeneration output set.
- Reproduction:
  `git diff-tree --no-commit-id --name-only -r 546b8800ced45c1ba1fafad12ea2191d52f0f706`,
  the same command for `9b7f9615136ce47ec7968c1abc60500d0585f685`
  and `3eb6f85b3ff07e3c9748cbdc95782a7a0709546c`, followed by
  `git show 454d6784:docs/specs/0119-spec-contained-authorization/_authorization.md`.
- Evidence: the complete command audit under QA-05; the preserved seeded
  `QA-AUTH-PATHS` finding independently identifies Task 13; and
  `internal/speccheck/governed.go` names both omitted paths.
- History: repeated from the September 9 report's F-003 for Task 05 and widened
  by the Task 13 and Task 14 commits.
- Affected rows: QA-02, QA-03, QA-05, QA-06, QA-07, QA-08 and QA-09. QA-05
  directly fails; the other rows wait because the QA contract stops flow
  execution after a post-commit authorization-shape defect.
- Required correction: obtain an independently committed maintainer grant for
  the two exact paths and replay the consuming work after that ancestor, or
  remove the paths from the consuming commits without weakening the repaired
  contracts. A later amendment cannot authorize the existing commits
  retroactively.

## Blocked and skipped

- QA-02, QA-03, QA-06, QA-07, QA-08 and QA-09 are
  `blocked (finding: F-003 — waits on authorized consuming history)`. Fresh
  static evidence is recorded under each row, but their real command journeys
  did not run after the complete command audit found the authorization-shape
  defect.
- QA-12 is `blocked (environment: no open Pull Request)`. The supplied fact
  proves the cause, and the row records approval, checks/status, unresolved
  threads, Merge-Ready acceptance and review-artifact ancestry for audited head
  `3eb6f85b`.
- No matrix row was skipped or declared-blocked. The two entries under
  `Mechanical skips` remain the Daemon's materialized detector record; they are
  not skipped matrix rows and do not change the counters.

## Coverage

Closed: 12 rows — 4 pass, 1 fail, 6 blocked by finding, 1 blocked by
environment, 0 blocked by declaration, 0 skipped and 0 pending. The matrix
maps Goals 1-4, User Stories 1-3, Core Features 1-6, the three declared
intentional breaks, the outside-evidence obligation, changed-file chronology,
Vocabulary Contract and Non-Goals. F-003 prevents current real-flow evidence
for six rows, so their focused checks remain explicitly non-terminal.

Static and read-only evidence covered the strict Spec check, complete
repository Verification, documentation and regeneration gates, all 42
historical records, current operation parsing, refusal contracts, one shared
grant parser, shipped-template identity, catalog digest identity, glossary
ownership, dependency stability and Daemon Task-status ownership. Public
modified-source, operation-refusal, regeneration and managed-refresh probes
were not executed after F-003 triggered the mandatory flow stop. No Pull
Request, release, deployment, credential or paid-service journey ran.

Finding totals: 0 `Blocks-Completion`, 0 `Data-Loss`, 1 `Trust-Damage`, 0
`Friction` and 0 `Cosmetic`.

## Final verdict

`fail`: 4 of 12 rows passed, QA-05 failed, six rows are finding-blocked and
the no-Pull-Request row is environment-blocked with equivalent evidence. Put
the exact governed paths in independently committed target ancestry before
replaying the consuming commits, or remove those paths without weakening the
contracts, then rerun the complete gate and all six blocked public journeys.

## Mechanical skips

| Detector | Missing artifact |
| --- | --- |
| consequent-fix commit order | consequent-fix declaration |
| QA evidence paths | docs/specs/0119-spec-contained-authorization/qa/evidence |
