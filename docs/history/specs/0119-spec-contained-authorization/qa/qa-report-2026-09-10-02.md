---
spec: 0119-spec-contained-authorization
date: 2026-09-10
build: 339e6deedacdf489fe8623935b26d85dbd0e781b
verdict: fail
auditing_binary: "0.12.0 (339e6dee-dirty, built 2026-09-10 14:39:47 -0300)"
auditor_staleness: "current: rebuilt from audited HEAD 339e6dee; dirty state is this seeded QA report"
status: closed
rows_blocked_environment: 1
rows_blocked_finding: 0
rows_blocked_declared: 0
surfaces: [backend, cli, docs]
---

# QA Report

## Performed repairs

None.

## Assigned repair failures

None.

## Authorization audit inputs

None.

## Mechanical findings

None.

## Scope and environment

Full terminal QA rerun at
`339e6deedacdf489fe8623935b26d85dbd0e781b` on the Run Worktree branch.
The actors are the maintainer approving a Spec-contained grant, the Supervisor
deciding whether work may dispatch, and the reviewer auditing committed scope
and delivery authority. All non-QA dependencies of authored gate `task_10`
are `completed`, and the Task Graph makes that gate terminal.

The public CLI was rebuilt from audited HEAD as
`/private/tmp/roundfix-0119-qa-02-bin/roundfix` with Go 1.26.1. Its `-dirty`
identity records only this seeded, untracked report. Scratch clones, Git
repositories, build caches, and generated-state checks lived under
`/private/tmp`; no implementation path in the audited checkout changed.

The supplied Pull Request fact says no Pull Request is open for
`feat/spec-contained-authorization`, and the per-Run branch never has a Pull
Request. QA-12 therefore uses the required environment-blocked
equivalent-evidence path. Release, deployment, credential, destructive, and
paid-service actions were not exercised.

Four repository-input journeys were carried from
`qa-report-2026-09-10-01.md`. That report is committed at current head
`339e6dee`; its audited tree `d4e03f29` and current HEAD differ only by the
prior report itself. A fresh path-excluding Git diff returned empty, and each
carried row has only repository-path inputs. The failed chronology row, the
external-evidence row, generated-state rows, static gate, and Pull Request row
were re-observed.

## Static gate

- `/private/tmp/roundfix-0119-qa-02-bin/roundfix spec check 0119-spec-contained-authorization --strict`: pass (exit 0). It reported no findings, did not execute authored Verification, and skipped only absent Finding, Rollup, and Backlog artifact classes.
- `PATH=/usr/local/go/bin:/Users/marcio/.cargo/bin:/opt/homebrew/bin:/usr/bin:/bin GOCACHE=/private/tmp/roundfix-0119-qa-02-cache make GO=/usr/local/go/bin/go BIN=/private/tmp/roundfix-0119-qa-02-bin/roundfix BUILD_COMMIT=339e6dee-dirty verify`: fail twice at `go test -parallel 16 ./...`. The first valid run timed out in one CLI and eight Daemon concurrency cases; the unchanged rerun timed out in four of the same Daemon cases. The exact failed selections passed unchanged in isolation, but focused results do not replace the full gate.
- The same `make verify` command at unchanged target `main` revision `3e833b00`, in a fresh local clone under the same host and Go toolchain, passed every package and completed the build. This comparison, plus two candidate failures, classifies F-005 as current-candidate behavior rather than an environment block.
- An earlier attempt pinned `GO=/usr/local/go/bin/go` but left `gofmt` on PATH at Go 1.27.1. It reported two unchanged target files as needing formatting. Go 1.26.1 `gofmt` reported both clean, and the corrected gate moved past formatting, so that attempt is excluded as a toolchain mismatch.
- `PATH=/usr/local/go/bin:/Users/marcio/.cargo/bin:/opt/homebrew/bin:/usr/bin:/bin GOCACHE=/private/tmp/roundfix-0119-qa-02-cache make GO=/usr/local/go/bin/go BIN=/private/tmp/roundfix-0119-qa-02-bin/roundfix BUILD_COMMIT=339e6dee-dirty verify-docs`: pass (exit 0). Documentation contracts, regeneration contracts, corpus budget, and the full active-Spec check passed.

## Results

| # | Story / criterion / sweep | Actor and surface | Status | Evidence |
| - | --- | --- | --- | --- |
| QA-01 | Goals 1-2, US-1/US-2 and CF-1: the current Spec-contained typed grant is operative, exact, self-contained and accepted by strict authoring validation | Maintainer and Supervisor · CLI/docs | pass | The rebuilt strict check passed; the fresh current-record test accepted the 25-path through-merge grant, and target ancestry carries the same record byte for byte. |
| QA-02 | Task 02/03 refusal criteria and declared break 1: proposed, null-dated, withdrawn, wrong-consumer, contradictory and unsafe-path records do not become grants; an honest proposal remains inspectable | Maintainer · CLI/Git | carried (established by: qa-report-2026-09-10-01.md; head: 339e6dee) | The prior public refusal matrix is valid at the same source tree; its declared reader and checker inputs are byte-identical. |
| QA-03 | CF-5 and Task 13 operation boundaries: the closed operation vocabulary is enforced independently at strict checking, Implement dispatch, Task commit, push and Settle | Maintainer and Supervisor · CLI/Git | carried (established by: qa-report-2026-09-10-01.md; head: 339e6dee) | The prior public strict, Implement and Settle journeys remain bound to byte-identical operation-boundary inputs. |
| QA-04 | Goal 4, CF-3 and outside evidence: preserved authorization records at revision `6b8ea487...` remain readable with original provenance, including legacy multi-Spec scope | Reviewer · Git/backend | pass | Fresh Git evidence exposed all 42 records, and the current historical-reader test passed against those immutable objects. |
| QA-05 | US-3, CF-2 and Tasks 05/14: every governed Task change stays within the grant at its authorizing ancestor, and both canonical citation forms drive a non-empty changed-path audit | Reviewer · Git/CLI | pass | Fresh `git diff-tree` output for all thirteen Task commits was compared with the record at target `3e833b00`; the 25-path grant precedes every Task, including replayed Tasks 05, 13 and 14. |
| QA-06 | Goal 3, CF-6 and declared break 3: read-only checking remains available while modified authored commands refuse execution without an ancestor approval | Supervisor · CLI/Git | carried (established by: qa-report-2026-09-10-01.md; head: 339e6dee) | The prior public marker journey remains bound to byte-identical verification and CLI inputs. |
| QA-07 | Task 06 execution seams: Implement probing, post-Agent Verification and Settle apply the same source refusal; Daemon-owned status and Result remain outside the authored projection | Supervisor · CLI/Git | carried (established by: qa-report-2026-09-10-01.md; head: 339e6dee) | The prior three-entry-point journey remains bound to byte-identical probe, engine and Settle inputs. |
| QA-08 | Declared break 2 and Tasks 04/09/11: Governed Path and sanctioned-regeneration discovery grow to Spec-contained and archived grants while legacy declarations, ordinary paths and one parser remain preserved | Reviewer · backend/docs | pass | Fresh historical and regeneration selections passed; two sanctioned digest runs changed nothing, and a source sweep found one authorization decoder. |
| QA-09 | CF-3 and Tasks 07/08/12: canonical Baseline guides, authoring skills and templates place the record inside the Spec while preserving prior wording; generated guides, shipped skills and digest pins converge | Maintainer · docs/CLI | pass | Two public managed refreshes each reported `File changes: 0`; both digest runs reported `changed:false`, the scratch clone stayed clean, and skill synchronization passed. |
| QA-10 | Vocabulary Contract and glossary currency: both Grant Refusal Codes are defined by their emitting owners, the checker runs the contract, and no additional term needs a glossary update | Maintainer · docs/CLI | pass | Current-Spec and full-corpus checks passed without skipping vocabulary validation; both emitted codes remain defined under `Grant Refusal Code`. |
| QA-11 | Non-Goals and regression lock: no authentication service, global permission database, arbitrary-shell authority, deployment/release grant, paid call, Task-status ownership change or undeclared public behavior entered the change | Reviewer · CLI/Git | fail | F-005: the required candidate `make verify` failed twice on suite-level Task-cycle timeouts while unchanged `main` passed the same gate on the same host. |
| QA-12 | Pull Request approval, checks/status, unresolved threads, Merge-Ready acceptance and review-artifact ancestry controls | Maintainer · Pull Request | blocked (environment: no open Pull Request) | The supplied fact blocks the live PR surface; all five controls have current-head equivalent evidence below. |

## Row evidence

### QA-01

```yaml
inputs:
  - kind: repository_path
    ref: docs/specs/0119-spec-contained-authorization/_authorization.md
  - kind: repository_path
    ref: docs/specs/0119-spec-contained-authorization/_prd.md
  - kind: repository_path
    ref: docs/specs/0119-spec-contained-authorization/_techspec.md
```

Passed. The rebuilt CLI returned no strict finding. A fresh
`TestCurrentRecordPermitsItsDeclaredOperations` run passed. The target record
at `3e833b00` is byte-identical to current HEAD and carries `status: approved`,
grant date `2026-09-09`, the consuming Spec, 25 exact paths, and
`implement`, `commit`, `push`, `pull_request`, and `merge`. Release remains
absent.

### QA-02

```yaml
inputs:
  - kind: repository_path
    ref: internal/speccheck/constraints.go
  - kind: repository_path
    ref: internal/authorization/authorization.go
```

Carried from `qa-report-2026-09-10-01.md` at current report head `339e6dee`.
That public matrix exercised contradictory approval state, null grant date,
withdrawal, wrong consumer, absolute path, traversal, glob, symlink, duplicate
path, and unknown operation. The changed tree between its audited build and
this build contains only the prior QA Report, outside these declared inputs.

### QA-03

```yaml
inputs:
  - kind: repository_path
    ref: internal/authorization/authorization.go
  - kind: repository_path
    ref: internal/cli/implement.go
  - kind: repository_path
    ref: internal/cli/settle.go
  - kind: repository_path
    ref: internal/daemon/task_engine.go
```

Carried from `qa-report-2026-09-10-01.md` at current report head `339e6dee`.
Its public strict, Implement and Settle journeys proved missing `implement`
and `commit` authority refuse before their actions and leave HEAD unchanged;
fresh current-record evidence confirms the operative grant still permits the
operations this Spec uses.

### QA-04

```yaml
inputs:
  - kind: external_repository
    ref: roundfix Git revision 6b8ea48725cbca13974eee0b400b3482202874f6
```

Passed. Fresh
`git ls-tree -r --name-only 6b8ea487... -- docs/workflow/authorizations`
returned the 42 records dated from 2026-07-30 through 2026-09-08. A fresh
`TestAuthorizationReaderResolvesPreservedHistoricalRecords` run passed,
including the historical multi-Spec consumer, no accidental grant to Spec
0119, and an unavailable revision distinct from grant or refusal. These
immutable pre-Spec records satisfy the outside-evidence obligation.

### QA-05

```yaml
inputs:
  - kind: repository_path
    ref: docs/specs/0119-spec-contained-authorization/_authorization.md
  - kind: repository_path
    ref: docs/specs/0119-spec-contained-authorization/task_*.md
  - kind: repository_path
    ref: internal/speccheck/governed.go
```

Passed. Fresh `git diff-tree --no-commit-id --name-only -r <commit>` output
was read for every Task commit. `git merge-base --is-ancestor 3e833b00
<task-commit>` exited 0 for all thirteen. The authorization at `3e833b00`
contains the final 25-path amendment and has no byte difference at HEAD.

This fixes the prior chronology failure without retroactive authority:

- Task 05 is now `76fb1953` and its governed
  `internal/speccheck/mechanical_test.go` change follows `3e833b00`;
- Task 13 is now `0dbde510` and its governed
  `internal/speccheck/coherence.go` change follows `3e833b00`;
- Task 14 is now `ac4c4ed3` and its governed
  `internal/speccheck/mechanical_test.go` change follows `3e833b00`.

Every other governed Task path is listed directly or is a deterministic output
of sanctioned `make baseline-digests`; two fresh runs changed nothing. The
Daemon's preserved mechanical skip records that no current-Run Task commit was
supplied to its seed stage; this independent history audit supplies the QA row.

### QA-06

```yaml
inputs:
  - kind: repository_path
    ref: internal/speccheck/verification.go
  - kind: repository_path
    ref: internal/cli/spec_check.go
```

Carried from `qa-report-2026-09-10-01.md` at current report head `339e6dee`.
That public journey changed a tracked Task's command to
`touch qa-should-not-run`: read-only checking passed, execution refused with
`SC-SOURCE-UNTRUSTED: modified-artifact`, the marker stayed absent, and HEAD
stayed unchanged. Both declared inputs are byte-identical here.

### QA-07

```yaml
inputs:
  - kind: repository_path
    ref: internal/daemon/verification_probe.go
  - kind: repository_path
    ref: internal/daemon/engine.go
  - kind: repository_path
    ref: internal/cli/settle.go
```

Carried from `qa-report-2026-09-10-01.md` at current report head `339e6dee`.
Its public Settle refusal and independent probe, post-Agent, projection,
status, and Result checks remain bound to byte-identical inputs. F-005 concerns
suite-level concurrent completion, not a changed result from these source-trust
journeys.

### QA-08

```yaml
inputs:
  - kind: repository_path
    ref: internal/speccheck/governed.go
  - kind: repository_path
    ref: internal/suiteguardcontract/regeneration.go
  - kind: repository_path
    ref: internal/authorization/authorization.go
```

Passed. Fresh archived, proposed, null-dated, malformed, unrelated,
set-only-grows, and frontmatter-free legacy regeneration selections passed.
Two public `make baseline-digests` runs returned `changed:false`. A fresh source
search found `parseAuthorizationRecord`, `splitAuthorizationFrontmatter`, and
`authorizationFrontmatterMapping` only in
`internal/authorization/authorization.go`.

### QA-09

```yaml
inputs:
  - kind: repository_path
    ref: internal/baseline/assets/modules/*.json
  - kind: repository_path
    ref: .agents/skills/write-*/**
  - kind: repository_path
    ref: skills/write-*/**
  - kind: repository_path
    ref: docs/agents/setup-context.json
```

Passed in a clean current-head clone. The first and second public
`roundfix baseline update --repo . --no-skills --yes --format text` runs both
reported `Baseline update: verified`, identical catalog digest
`sha256:136f8ff3...`, `File changes: 0`, and `Idempotence: verified`. Two
later digest runs reported no changes, `make skills-sync-check` passed, and Git
remained clean.

### QA-10

```yaml
inputs:
  - kind: repository_path
    ref: CONTEXT.md
  - kind: repository_path
    ref: internal/speccheck/constraints.go
  - kind: repository_path
    ref: internal/speccheck/verification.go
```

Passed. Neither strict current-Spec checking nor the full active-Spec sweep
skipped the Vocabulary Contract detector. `constraints.go` emits
`SC-TOOLING-UNAPPROVED`, `verification.go` emits
`SC-SOURCE-UNTRUSTED`, and `CONTEXT.md` defines both under
`Grant Refusal Code`. This rerun introduced no term, so no glossary edit is
due.

### QA-11

```yaml
inputs:
  - kind: repository_path
    ref: '**/*.go'
  - kind: repository_path
    ref: go.mod
  - kind: repository_path
    ref: go.sum
  - kind: repository_path
    ref: docs/specs/0119-spec-contained-authorization/_prd.md
```

Failed on F-005. The remaining scope checks passed: `go.mod` and `go.sum` are
unchanged from target; no HTTP endpoint, authentication layer, credential
policy, dependency, deployment, release, or paid-service path entered the
candidate. The current-head full gate nevertheless failed twice under its
authored package concurrency, while unchanged `main` passed the same command
and every failed current-head selection passed in isolation.

### QA-12

```yaml
inputs:
  - kind: live_service
    ref: GitHub Pull Request for feat/spec-contained-authorization
```

Blocked by the supplied environment fact: no Pull Request is open for
`feat/spec-contained-authorization`, and the Run Worktree branch never has
one.

Equivalent controls for audited head `339e6dee`:

- approval: the 25-path maintainer amendment is target commit `3e833b00`, an ancestor of every non-QA Task commit; no QA or review acceptance exists for the failing candidate;
- checks and status: `make verify-docs` passed, while `make verify` failed twice on F-005;
- unresolved review threads: no Pull Request exists, so no Pull Request thread stands or is claimed resolved;
- Merge-Ready acceptance: none exists for this head;
- review-artifact ancestry: the previous QA Report is committed at this head, but no accepted pre-Pull-Request review artifact exists or is claimed.

Unblocking action: correct F-005, rerun the full terminal gate to pass, obtain
the configured pre-Pull-Request review for the resulting head, then open the
target branch Pull Request and observe its required checks and review state.

## Findings

### F-005 — current candidate times out in the required concurrent full suite

- Impact: `Blocks-Completion` — the maintainer cannot pass the repository's required `make verify` gate reliably.
- Actor and step: maintainer runs the complete Verification with the repository's authored `-parallel 16` package test command.
- Expected: every package completes and `make verify` exits 0 on the candidate.
- Actual: two corrected current-head runs exited 2. The first timed out in one CLI and eight Daemon Task-cycle cases; the unchanged rerun timed out in four of those Daemon cases. The unchanged target passed the same gate under the same host and toolchain.
- Reproduction: `PATH=/usr/local/go/bin:/Users/marcio/.cargo/bin:/opt/homebrew/bin:/usr/bin:/bin GOCACHE=/private/tmp/roundfix-0119-qa-02-cache make GO=/usr/local/go/bin/go BIN=/private/tmp/roundfix-0119-qa-02-bin/roundfix BUILD_COMMIT=339e6dee-dirty verify`.
- Diagnostic evidence: the exact one-case CLI selection and eight-case Daemon selection passed unchanged in isolation. That proves the assertions can complete but does not satisfy the full gate. The second candidate run still timed out in `TestTaskCycleSchedulesIndependentWaveWithConcurrencyCap`, `TestTaskCycleVerificationCapacityTwoOverlapsReadyAttemptsWithoutPermitLoss`, `TestTaskCycleIntegratedVerificationCapacityOneBoundsConcurrentTaskWorktrees`, and `TestTaskCycleRepairReacquiresVerificationCapacityAfterFeedback`.
- Unchanged-base evidence: the same full gate passed at `main` revision `3e833b00` in a fresh local clone immediately after the candidate failures.
- Affected row: QA-11.
- Required correction: identify and repair the current-head Task-cycle scheduling or synchronization cost that misses its authored start/completion deadlines under the full suite, then rerun the unchanged full gate. Do not replace the gate with isolated tests or suppress its timeout signal.

## Blocked and skipped

- QA-12 is `blocked (environment: no open Pull Request)`. The supplied fact
  proves the cause, and the row records approval, checks/status, unresolved
  threads, Merge-Ready acceptance, and review-artifact ancestry for audited
  head `339e6dee`.
- No matrix row is finding-blocked, declared-blocked, or skipped. The three
  entries under `Mechanical skips` remain the Daemon's materialized detector
  record; they are not skipped matrix rows and do not change the counters.

## Coverage

Closed: 12 rows — 6 fresh passes, 4 carried passes, 1 fail, 1 blocked by
environment, 0 blocked by finding, 0 blocked by declaration, 0 skipped, and 0
pending. The matrix maps Goals 1-4, User Stories 1-3, Core Features 1-6, all
three declared intentional breaks, the outside-evidence obligation,
changed-file chronology, Vocabulary Contract, glossary currency, and
Non-Goals.

Behavior probes cover contradictory approval state, null date, withdrawal,
wrong consumer, unsafe and duplicate paths, unknown and absent operations,
Implement and Settle operation refusals, edited authored commands with marker
rereads, historical and archived records, two managed refreshes, two digest
regenerations, two full candidate gates, exact failing selections, and an
unchanged-base full gate. No Pull Request, release, deployment, credential, or
paid-service journey ran.

Finding totals: 1 `Blocks-Completion`, 0 `Data-Loss`, 0 `Trust-Damage`, 0
`Friction`, and 0 `Cosmetic`.

## Final verdict

`fail`: ten rows passed or carried, QA-11 failed on F-005, and the
no-Pull-Request row is environment-blocked with equivalent evidence. Repair
the current-head full-suite timeout behavior and rerun the complete terminal
gate before Pull Request preparation.

## Mechanical skips

| Detector | Missing artifact |
| --- | --- |
| authorization bounded paths | Task commits |
| consequent-fix commit order | consequent-fix declaration |
| QA evidence paths | docs/specs/0119-spec-contained-authorization/qa/evidence |
