---
spec: 0119-spec-contained-authorization
date: 2026-09-09
build: 60e05eac3ca9dbd69ca2e0b4878b0955993e0999
auditing_binary: "0.12.0 (60e05eac, built 2026-09-09 16:17:45 -0300)"
auditor_staleness: "current: build commit 60e05eac is the audited HEAD"
status: closed
verdict: fail
rows_blocked_environment: 1
rows_blocked_finding: 0
rows_blocked_declared: 0
surfaces: [backend, cli, docs]
---

# QA Report

## Performed repairs

None.

## Assigned repair failures

None.

## Mechanical findings

None.

## Scope and environment

Full terminal QA of build `60e05eac3ca9dbd69ca2e0b4878b0955993e0999`
from the Run Worktree on branch
`roundfix/run-run_20260909T164137Z_37e4c04dd34403b7`. The intended actors are
the maintainer approving one Spec-contained grant, the Supervisor deciding
whether work may dispatch, and the reviewer auditing committed scope and
delivery authority.

The gate uses `/private/tmp/roundfix-0119-qa-bin/roundfix`, rebuilt from the
audited tree. The Daemon seeded this report with mechanical evidence from
`0.12.0 (454d6784, built 2026-09-09 12:07:01 -0300)` and classified that
binary as stale because its build commit predates the audited tree. The
mechanical stage reported no findings and materialized the detector skips
preserved at the end of this report.

The Pull Request fact supplied to this gate says no Pull Request is open. Pull
Request controls therefore use the required environment-blocked
equivalent-evidence path. Destructive, release, deploy, paid-service and
credentialed actions are outside this Spec's authority. Scratch Git journeys
run only under `/private/tmp`; this checkout receives only this report and QA
evidence.

## Static gate

- `PATH=/private/tmp/roundfix-0119-qa-bin:$PATH roundfix spec check 0119-spec-contained-authorization --strict`: pass; no findings. The checker reported only missing-artifact skips for Finding lifecycle, Rollup membership and Backlog relocation, and stated that authored Verification commands were not executed.
- `rtk make verify`: fail (exit 2). The first run under Go 1.27.1 stopped at
  `fmt-check` on two files unchanged since `8d333a6c`; Go 1.26.1's
  formatter reports those files clean. The exact gate rerun with the repository's
  declared Go 1.26 toolchain reached the suite and failed reproducibly in
  `TestWritePRDProjectConstraints` and
  `TestWriteTechSpecProjectConstraints`: both templates omit the preserved
  phrases `express maintainer authorization` and `bounded files`. Seven
  concurrent Daemon cases also timed out in that run, but the exact seven-case
  selection passed unchanged in 1.089 seconds and is recorded as an execution
  contention observation rather than a product finding.
- `rtk make verify-docs`: pass (exit 0) with Go 1.26.1. The docs contract,
  repository regeneration contracts, corpus budget and the whole active Spec
  check all passed.

## Results

| # | Story / criterion / sweep | Actor and surface | Status | Evidence |
| - | --- | --- | --- | --- |
| QA-01 | Goals 1–2, US-1/US-2 and CF-1: the current Spec-contained typed grant is operative, exact, self-contained and accepted by strict authoring validation | Maintainer and Supervisor · CLI/docs | pass | The rebuilt CLI accepted the current record with its approved state, grant date, consuming Spec, 23 exact paths and typed operations; the whole active Spec check also accepted honest proposal records. |
| QA-02 | Task 02/03 refusal criteria and declared break 1: proposed, null-dated, withdrawn, wrong-consumer, contradictory and unsafe-path records do not become grants; an honest proposal remains inspectable | Maintainer · CLI/Git | pass | Eleven independent scratch worktrees produced the expected field-specific refusals; the active corpus retains honest proposals without findings. |
| QA-03 | CF-5 and Task 02 operation criteria: only the six closed operations parse, absence grants none, through-merge authority excludes release, tag and deploy | Maintainer and reviewer · CLI/docs | fail | Unknown `tag` refuses, but F-002: a missing operations list passes strict authoring validation and Implement advances to profile proof instead of refusing absent implement authority. |
| QA-04 | Goal 4, CF-3 and outside evidence: preserved authorization records at revision `6b8ea487...` remain readable with original provenance, including legacy multi-Spec scope | Reviewer · Git/backend | pass | Git listed all 42 preserved records at the cited revision; the fresh full-corpus reader case passed and retained multi-Spec scope. |
| QA-05 | US-3, CF-2 and Task 05: actual governed paths stay within the operative ancestor grant; self-approval, sibling approval and retroactive approval refuse | Reviewer · Git/CLI | fail | The grant commit precedes every Task commit, but F-003: Task 05 changed historically governed `internal/speccheck/mechanical_test.go` outside Spec 0119's 23-path grant. |
| QA-06 | Goal 3, CF-6 and declared break 3: read-only checking remains available while modified or untracked authored commands refuse execution without an ancestor approval | Supervisor · CLI/Git | pass | Public read-only checking exited 0; execution returned `SC-SOURCE-UNTRUSTED: modified-artifact` and never created its marker. The full source/approval boundary selection passed. |
| QA-07 | Task 06 execution seams: Implement dispatch and Settle apply the same source refusal before Agent work, shell execution or commit; Daemon-owned status and Result remain outside the authored projection | Supervisor · CLI/Git | pass | Public Settle refused the modified Task before shell or commit; focused real-Git probe, post-Agent, Settle and projection contracts all passed. |
| QA-08 | Declared break 2 and Tasks 04/09/11: Governed Path and sanctioned-regeneration discovery grow to Spec-contained and archived grants while legacy declarations, ordinary paths and one parser remain preserved | Reviewer · backend/docs | pass | Governed-set and regeneration suites passed, including archived, legacy, multi-Spec, ordinary-path and mutation-sensitive cases; the parser search has one implementation. |
| QA-09 | CF-3 and Tasks 07/08: canonical Baseline guides, authoring skills and templates place the record inside the Spec; generated guides, shipped skills and digest pins converge | Maintainer · docs/CLI | fail | F-001 breaks preserved template guidance; F-004 makes the first public Baseline refresh rewrite the committed catalog digest. Skill sync and repeated digest regeneration pass. |
| QA-10 | Vocabulary Contract and glossary currency: both Grant Refusal Codes are defined by their emitting owners, the checker runs the contract, and no additional term needs a glossary update | Maintainer · docs/CLI | pass | Strict checking ran the Vocabulary Contract; both emitted constants match the glossary's new `Grant Refusal Code` term and its two tokens. |
| QA-11 | Non-Goals and regression lock: no authentication service, global permission database, arbitrary-shell authority, deployment/release grant, paid call, Task-status ownership change or undeclared public behavior entered the change | Reviewer · CLI/Git | fail | Dependencies and Daemon status ownership are preserved, but F-001 is an undeclared authoring-contract regression; F-002–F-004 violate the promised authority and generated-state boundaries. |
| QA-12 | Pull Request approval, checks/status, unresolved threads, Merge-Ready acceptance and review-artifact ancestry controls | Maintainer · Pull Request | blocked (environment: no open Pull Request) | The supplied fact confirms no PR. Equivalent evidence records maintainer approval, one passing and one failing local gate, no thread/artifact state, and no Merge-Ready acceptance. |

## Row evidence

### QA-01

```yaml
inputs:
  - kind: repository_path
    ref: docs/specs/0119-spec-contained-authorization/_authorization.md
  - kind: repository_path
    ref: docs/specs/0119-spec-contained-authorization/_prd.md
  - kind: repository_path
    ref: docs/specs/0119-spec-contained-authorization/_techspec.md
```

Passed through the rebuilt public CLI. The current
`_authorization.md` carries `status: approved`, `granted: 2026-09-09`,
`consuming: 0119-spec-contained-authorization`, 23 exact paths, and the
`implement`, `commit`, `push`, `pull_request`, and `merge`
operations. The strict current-Spec check exited 0 with no findings. The
complete `make verify-docs` run independently checked all active Specs and
accepted the proposal-only records for Specs 0120–0129 without
`SC-TOOLING-UNAPPROVED`.

### QA-02

```yaml
inputs:
  - kind: repository_path
    ref: internal/speccheck/constraints.go
  - kind: repository_path
    ref: internal/authorization/authorization.go
```

Passed through eleven independent scratch Git worktrees at audited HEAD. The
rebuilt public `roundfix spec check 0119-spec-contained-authorization
--strict` command exited 1 and named the exact withholding field for:
`status: proposed` with a contradictory date, `granted: null`,
`status: withdrawn`, a wrong consumer, an absolute path, `../` traversal,
a glob, the existing `CLAUDE.md` symlink, a duplicate path, and unknown
operation `tag`. Proposal/null/withdrawn and unsafe-path cases emitted
`SC-TOOLING-UNAPPROVED`; the wrong consumer preserved
`SC-TOOLING-UNAUTHORIZED`. The whole active corpus check exited 0 while
honest proposal rows remained present, confirming that inspectable proposals
do not become false failures.

### QA-03

```yaml
inputs:
  - kind: repository_path
    ref: internal/authorization/authorization.go
  - kind: repository_path
    ref: docs/specs/0119-spec-contained-authorization/_authorization.md
```

Failed. The public unknown-operation case emitted
`SC-TOOLING-UNAPPROVED` with
`operations contains unsupported token "tag"`. Removing the complete
`operations` block, however, made the same strict command exit 0 with no
finding. Running public `roundfix implement` with a local
`/usr/bin/false` adapter advanced to Agent-profile lineage proof and returned
the same adapter failure for both the approved-operation and missing-operation
repositories. No Run, Agent, command, commit or push occurred, but the
comparison proves absent `implement` authority does not stop dispatch at the
authorization boundary.

### QA-04

```yaml
inputs:
  - kind: external_repository
    ref: roundfix Git revision 6b8ea48725cbca13974eee0b400b3482202874f6
```

Passed. Public Git command
`git ls-tree -r --name-only 6b8ea48725cbca13974eee0b400b3482202874f6 -- docs/workflow/authorizations`
returned 42 records dated from 2026-07-30 through 2026-09-08. A fresh
`TestAuthorizationReaderResolvesPreservedHistoricalRecords` run passed
against those immutable objects. Its observable contract reads all 42 without
an unresolved result or accidental grant to Spec 0119, preserves the
eight-Spec `consuming` form, and distinguishes an unavailable revision. The
source is repository history authored before this Spec; it is external
acceptance evidence rather than a fixture created by Spec 0119.

### QA-05

```yaml
inputs:
  - kind: repository_path
    ref: docs/specs/0119-spec-contained-authorization/_authorization.md
  - kind: repository_path
    ref: docs/specs/0119-spec-contained-authorization/task_*.md
```

Failed on changed-file scope. Commit
`454d678464a50e88e736d7df98ee16370a097ae5` introduced the operative
`_authorization.md` and is an ancestor of every non-QA Task commit; the
record is byte-identical at audited HEAD. Fresh
`git diff-tree --no-commit-id --name-only -r <commit>` output was inspected
for each Task commit. Task 05 commit
`546b8800ced45c1ba1fafad12ea2191d52f0f706` includes
`internal/speccheck/mechanical_test.go`. The current Governed Path set and
archived Spec 0130's record both name that exact path, while Spec 0119's
23-path record does not. No sanctioned regeneration covers it. The remaining
governed Task paths are bounded directly or are deterministic
`make baseline-digests` outputs still being checked under QA-09.

### QA-06

```yaml
inputs:
  - kind: repository_path
    ref: internal/speccheck/verification.go
  - kind: repository_path
    ref: internal/cli/spec_check.go
```

Passed against a tracked fixture in a disposable clone. After changing its
Verification from `true` to `touch qa-should-not-run`, public read-only
`roundfix spec check coverage-range` exited 0, reported that authored
commands were not executed, and left the marker absent. The same command with
`--run-verification` exited 1 on
`SC-SOURCE-UNTRUSTED: modified-artifact`, named the differing authored
projection, and again left the marker absent.

The fresh focused selection
`TestVerificationRefusesUntrustedSource`,
`TestVerificationExecutesOnCommittedProvenance`, and
`TestAuthoredProjectionIgnoresDaemonOwnedFields` passed. These real-Git
cases cover modified, untracked, out-of-tree and unresolved sources; a
repository/revision/artifact/command-digest-bound approval; expiry when any
identity changes; and the continued exclusion of Daemon-owned `status` and
`## Result` fields.

### QA-07

```yaml
inputs:
  - kind: repository_path
    ref: internal/daemon/verification_probe.go
  - kind: repository_path
    ref: internal/daemon/engine.go
  - kind: repository_path
    ref: internal/cli/settle.go
```

Passed. Public `roundfix settle --spec coverage-range --task task_01`
selected the scratch repository surface, returned the same
`SC-SOURCE-UNTRUSTED: modified-artifact` refusal, left
`qa-should-not-run` absent, and created no commit. The fresh
`TestProbeRefusesUntrustedCommandSource`,
`TestPostAgentVerificationRefusesUntrustedCommandSource`, and
`TestSettleRefusesUntrustedCommandSource` selection passed across
`internal/daemon` and `internal/cli`; each uses its own entry path and
asserts no verifier or shell call after refusal. This row concerns command
source trust; F-002 separately records that operation authority is not
enforced before dispatch.

### QA-08

```yaml
inputs:
  - kind: repository_path
    ref: internal/speccheck/governed.go
  - kind: repository_path
    ref: internal/suiteguardcontract/regeneration.go
```

Passed. The fresh tagged governed-set selection covered both shipped
templates, monotonicity, ordinary source/Spec exclusions, all records reached
by current discovery, and the real repository-records subtest. The fresh
suiteguard selection passed archived approved records, historical multi-Spec
consumers, frontmatter-free legacy declarations, and proposed/null/malformed
or unrelated refusals. A source search finds
`parseAuthorizationRecord`, `splitAuthorizationFrontmatter`, and
`authorizationFrontmatterMapping` only in
`internal/authorization/authorization.go`.

### QA-09

```yaml
inputs:
  - kind: repository_path
    ref: internal/baseline/assets/modules/*.json
  - kind: repository_path
    ref: .agents/skills/write-*/**
  - kind: repository_path
    ref: skills/write-*/**
```

Failed at the repository Verification boundary. A focused rerun of
`go test -count=1 ./skills -run '^(TestWriteTechSpecProjectConstraints|TestWritePRDProjectConstraints)$'`
reproduced both failures. The template diff adds the Spec-contained record but
removes the prior literal guidance `express maintainer authorization` and
`bounded files`, although Task 07 requires existing clauses not to weaken and
the PRD declares every unlisted observable change a regression.

In a disposable clone, the first public
`roundfix baseline update --repo . --no-skills --yes --format text` run
rewrote `docs/agents/setup-context.json` from catalog digest `5b12ccc2…`
to `136f8ff3…`; the second run reported zero changes. Two subsequent
`make baseline-digests` runs both reported `changed:false`, and
`make skills-sync-check` passed. This proves the generated corpus and skill
bundle are stable after refresh, but the assembled commit did not carry the
current manifest pin.

### QA-10

```yaml
inputs:
  - kind: repository_path
    ref: CONTEXT.md
  - kind: repository_path
    ref: docs/specs/0119-spec-contained-authorization/_techspec.md
```

Passed. The strict current-Spec and full active-corpus checks did not list the
Vocabulary Contract detector among their skips. The declared owner
`internal/speccheck/constraints.go` emits
`SC-TOOLING-UNAPPROVED`; `internal/speccheck/verification.go` emits
`SC-SOURCE-UNTRUSTED`; and `CONTEXT.md` defines both under the new
`Grant Refusal Code` entry. Commit `fed19418` introduced that term before
the emitting Tasks in this Run. The Spec introduced no other public vocabulary
that lacks a glossary owner, so no further domain-context edit is due.

### QA-11

```yaml
inputs:
  - kind: repository_path
    ref: go.mod
  - kind: repository_path
    ref: go.sum
  - kind: repository_path
    ref: docs/specs/0119-spec-contained-authorization/_prd.md
```

Failed on the same preserved-contract regression as QA-09. The remaining
scope checks found `go.mod` and `go.sum` byte-identical to the authorization
commit, no new HTTP/authentication/credential import, and no release, deploy or
paid-service execution. The fresh Daemon-status selection passed four
ownership cases, preserving `in_progress` before Agent work and normalization
before Daemon Verification. The QA journeys used only the rebuilt local binary,
Git and local test processes; the deliberately invalid adapter stopped before
any Agent Session. F-001 remains an observable regression outside the PRD's
three declared breaks, while F-002, F-003 and F-004 violate the action,
bounded-path and generated-state promises.

### QA-12

```yaml
inputs:
  - kind: live_service
    ref: GitHub Pull Request for feat/spec-contained-authorization
```

Blocked by the supplied environment fact: no Pull Request is open for
`feat/spec-contained-authorization`, and the per-Run branch has no Pull
Request of its own.

Equivalent controls:

- approval: the maintainer's typed through-merge authorization is committed at
  `454d6784`, an ancestor of every Task commit; no QA or review acceptance was
  obtained for the failing assembled candidate;
- checks and status: `make verify-docs` passed at audited HEAD, while
  `make verify` failed reproducibly on F-001, so the candidate is not
  check-clean;
- unresolved review threads: no Pull Request exists, so there are no PR threads
  to inspect or claim resolved;
- Merge-Ready acceptance: none exists for this candidate;
- review-artifact ancestry: no PR review artifact exists, and none is claimed.

Unblocking action: correct F-001–F-004, rerun terminal QA to pass, obtain the
configured Codex pre-PR review for the resulting head, then open the target
branch Pull Request and observe its required checks and review state.

## Findings

### F-001 — authoring templates drop required tooling-authority guidance

- Impact: `Blocks-Completion` — the maintainer cannot pass the repository's
  required `make verify` gate.
- Actor and step: maintainer runs the complete Verification after the authoring
  template changes.
- Expected: both templates retain the existing tooling-authority contract while
  adding the Spec-contained record placement.
- Actual: `TestWritePRDProjectConstraints` and
  `TestWriteTechSpecProjectConstraints` each report that the template is
  missing `express maintainer authorization` and `bounded files`.
- Reproduction:
  `rtk env GOCACHE=/private/tmp/roundfix-0119-qa-go126-cache /usr/local/go/bin/go test -count=1 ./skills -run '^(TestWriteTechSpecProjectConstraints|TestWritePRDProjectConstraints)$'`.
- Evidence: the changed template comments replace both required phrases with
  new placement wording. The focused command exits nonzero with four exact
  missing-guidance diagnostics.
- Affected rows: QA-09 and QA-11.
- Required correction: preserve the existing authorization and bounded-file
  wording while adding the Spec-contained record contract, synchronize the
  shipped copies, then rerun the complete gate.

### F-002 — absent operation authority does not stop Implement dispatch

- Impact: `Trust-Damage` — the record says an absent operations list grants
  no operation, but the authoring and dispatch surfaces treat a record without
  that list like the through-merge record until a later, unrelated preflight.
- Actor and step: the maintainer removes `operations`; the Supervisor runs
  strict Spec checking and then public Implement dispatch.
- Expected: authoring or dispatch refuses missing `implement` authority
  before Agent-profile proof.
- Actual: strict checking exits 0 with no finding. Both the approved and
  missing-operation repositories reach the identical
  `adapter_lineage_unknown` profile-proof refusal for the intentionally local
  `/usr/bin/false` adapter.
- Reproduction: remove `operations` from
  `docs/specs/0119-spec-contained-authorization/_authorization.md`, run
  `roundfix spec check 0119-spec-contained-authorization --strict`, then run
  `roundfix implement --spec 0119-spec-contained-authorization --agent codex --model gpt-5.6-sol --reasoning-effort high --agent-command /usr/bin/false --no-input`
  from a non-default scratch branch.
- Evidence: the first command exits 0; the second reaches profile proof and
  explicitly reports no Run, Agent, commit or push side effects. A fresh source
  search finds `AuthorizationResolution.Permits` called only from tests.
- Affected row: QA-03.
- Required correction: enforce the operation required by each action boundary
  from the ancestor grant and add a public refusal contract for a missing list.

### F-003 — Task 05 changes a Governed Path outside the consuming grant

- Impact: `Trust-Damage` — a reviewer cannot rely on the consuming Spec's
  exact bounded paths to account for every governed change.
- Actor and step: reviewer runs `git diff-tree` for each Task commit and
  compares governed paths with Spec 0119's ancestor grant.
- Expected: every governed path is in the exact 23-path list or is a proven
  output of the sanctioned regeneration command.
- Actual: Task 05 commit `546b8800` changes
  `internal/speccheck/mechanical_test.go`. That path is governed by the
  current declared set and archived Spec 0130's authorization, but Spec 0119
  does not bound it.
- Reproduction:
  `git diff-tree --no-commit-id --name-only -r 546b8800ced45c1ba1fafad12ea2191d52f0f706`;
  compare the result with both authorization records and
  `internal/speccheck/governed.go`.
- Evidence: the path appears in all three governing sources except Spec 0119's
  grant; `git diff 454d6784..HEAD -- <Spec-0119-authorization>` is empty, so
  no later grant amendment supplies it.
- Affected row: QA-05.
- Required correction: obtain an ancestor grant amendment for this exact path
  and replay the consuming work under that authority, or remove the path from
  the consuming change without weakening its tests.

### F-004 — assembled Baseline manifest carries a stale catalog digest

- Impact: `Trust-Damage` — a maintainer applying the public managed refresh
  sees an unexpected tracked manifest change on the supposedly regenerated
  assembled tree.
- Actor and step: maintainer runs
  `roundfix baseline update --repo . --no-skills --yes --format text` in a
  clean disposable clone of audited HEAD.
- Expected: the first refresh reports the repository current and changes zero
  files; Task 08 requires derived pins to match fresh regeneration.
- Actual: the first refresh changes
  `docs/agents/setup-context.json` from catalog digest `5b12ccc2…` to
  `136f8ff3…`. The second run is idempotent.
- Reproduction: run the public command above twice at `60e05eac`, then
  inspect `git diff -- docs/agents/setup-context.json`.
- Evidence: both refreshes exit 0; the first reports one managed-file change,
  and the diff contains only the catalog digest. Two later
  `make baseline-digests` runs report `changed:false`.
- Affected row: QA-09.
- Required correction: regenerate and commit the current catalog digest after
  the final canonical/skill source change, then prove the first refresh is
  already current.

## Blocked and skipped

- QA-12 — `blocked (environment: no open Pull Request)`. The supplied fact
  proves the cause. The equivalent controls above answer approval, local
  checks/status, thread state, Merge-Ready acceptance and review-artifact
  ancestry without claiming publication or merge approval.
- The three entries under `Mechanical skips` are preserved Daemon
  materialization records. They are not skipped QA matrix rows and do not
  change the row counters.

## Coverage

Closed: 12 rows — 7 pass, 4 fail, 1 blocked by environment, 0 blocked by
finding, 0 blocked by declaration, 0 pending and 0 skipped. Goals 1–4, User
Stories 1–3, Core Features 1–6, the three declared breaks, the outside-evidence
obligation, every Task acceptance group, changed-file chronology, Non-Goals and
Vocabulary Contract were measured. Four findings remain: 1
`Blocks-Completion`, 3 `Trust-Damage`, 0 `Data-Loss`, 0 `Friction` and
0 `Cosmetic`.

Behavior probes covered contradictory approval state, null date, withdrawn and
wrong-consumer records; absolute, traversal, glob, symlink and duplicate
paths; unknown and absent operations; edited authored commands with independent
marker rereads; approved versus missing-operation dispatch; legacy and archived
records; Baseline refresh idempotence; and concurrent Daemon timeout isolation.
No Pull Request, release, deployment, credential or paid-service journey ran.

## Final verdict

`fail`: 7 of 12 rows passed, 4 failed, and the no-Pull-Request row is
environment-blocked with equivalent evidence. Correct F-001–F-004, rerun the
complete repository gate and public journeys on the repaired head, and require
a new report with zero failed, finding-blocked, declared-blocked or skipped
rows before Pull Request preparation.

## Mechanical skips

| Detector | Missing artifact |
| --- | --- |
| consequent-fix commit order | consequent-fix declaration |
| QA Report structure | QA Report |
| QA evidence paths | QA Report |
