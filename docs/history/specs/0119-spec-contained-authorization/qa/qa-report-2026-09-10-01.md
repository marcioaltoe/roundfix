---
spec: 0119-spec-contained-authorization
date: 2026-09-10
build: d4e03f29828915d7a8ee7f8a12ba79e129895c7e
auditing_binary: "0.12.0 (d4e03f29-dirty, built 2026-09-10 13:16:29 -0300)"
auditor_staleness: "current: rebuilt from audited HEAD d4e03f29; dirty state is this seeded QA report"
status: closed
verdict: fail
rows_blocked_environment: 1
rows_blocked_finding: 0
rows_blocked_declared: 0
surfaces: [backend, cli, docs]
---

# QA Report

## Performed repairs

None.

## Assigned repair failures

None.

## Authorization audit inputs

None.

## Mechanical findings

None.

## Scope and environment

Full rerun of the terminal QA gate at
`d4e03f29828915d7a8ee7f8a12ba79e129895c7e`, whose second parent is target
`main` at `3e833b006ed08e685ab3598562db6834ac81ae69`. The intended
actors are the maintainer approving a Spec-contained grant, the Supervisor
deciding whether work may dispatch, and the reviewer auditing committed scope
and delivery authority.

The public CLI entry point was
`/private/tmp/roundfix-0119-qa-final-bin/roundfix`, rebuilt from audited HEAD
through the repository's `make build` target with Go 1.26.1. Its `-dirty`
identity records this seeded, untracked report; no implementation path was
dirty. Scratch Git repositories and generated caches lived under
`/private/tmp`. The audited checkout received only this report and the
ignored binary produced by the repository Verification.

The Daemon seeded the mechanical sections and skips preserved in this report.
Because this rerun began after every implementation Task commit, the mechanical
stage received no current-Run Task commits and skipped the bounded-path audit.
The gate therefore reconstructed all thirteen non-QA Task commits from their
`Roundfix-Task` trailers and ran the shipped mechanical audit over the
actual Git history. That independent audit produced F-003.

One discarded probe changed only the working-tree authorization and therefore
did not change committed authority. Implement correctly ignored that edit and
started a scratch QA Run. The probe was interrupted and credited to no row;
`roundfix stop` reclaimed its dead owner and reported no repository edit,
commit, push, fetch, or Review Source mutation. The valid operation probes used
separate ephemeral commits in scratch repositories so the ancestor reader saw
the intended record. No scratch commit or Run touched this checkout or the
Spec target branch.

The supplied Pull Request fact says no Pull Request is open for
`feat/spec-contained-authorization`; the per-Run branch has no Pull Request
of its own. QA-12 uses the required environment-blocked equivalent-evidence
path. Release, deployment, credential, destructive, and paid-service actions
were not exercised.

## Static gate

- `rtk make GO=/usr/local/go/bin/go BIN=/private/tmp/roundfix-0119-qa-final-bin/roundfix GOCACHE=/private/tmp/roundfix-0119-qa-final-cache BUILD_COMMIT=d4e03f29-dirty build`: pass (exit 0). The binary reported `roundfix 0.12.0 (d4e03f29-dirty, built 2026-09-10 13:16:29 -0300)`.
- `rtk proxy /private/tmp/roundfix-0119-qa-final-bin/roundfix spec check 0119-spec-contained-authorization --strict`: pass (exit 0); no findings, no authored Verification execution, and only the absent Finding, Rollup, and Backlog artifact classes skipped.
- `PATH=/usr/local/go/bin:$PATH rtk make verify`: pass (exit 0) on the fresh evidence-bearing rerun; every package, skill check, skill-sync check, and CLI build completed. The first sandboxed run was invalidated by denied process-table and GPG access, a terminated Git fsmonitor, and a reduced PATH that omitted `rustfmt`. The first full-access run then exposed four non-reproducible Daemon integration timeouts; the exact four-case selection passed unchanged in 1.429 seconds, and the fresh complete gate passed without an edit.
- `PATH=/usr/local/go/bin:$PATH rtk make verify-docs`: pass (exit 0); documentation contracts, regeneration contracts, corpus budget, and the full active-Spec check passed.
- The fresh authorization and historical selection passed in `internal/authorization`, `internal/spec`, and `internal/suiteguardcontract`. The fresh cross-boundary selection passed in `internal/speccheck`, `internal/daemon`, and `internal/cli`, covering refusal selection, operation authority, authored-source trust, Daemon status ownership, and Settle.

## Results

| # | Story / criterion / sweep | Actor and surface | Status | Evidence |
| - | --- | --- | --- | --- |
| QA-01 | Goals 1-2, US-1/US-2 and CF-1: the current Spec-contained typed grant is operative, exact, self-contained and accepted by strict authoring validation | Maintainer and Supervisor · CLI/docs | pass | The rebuilt strict check passed; a fresh reader run accepted the committed 25-path through-merge record, and an independent Git reread matched the target copy byte for byte. |
| QA-02 | Task 02/03 refusal criteria and declared break 1: proposed, null-dated, withdrawn, wrong-consumer, contradictory and unsafe-path records do not become grants; an honest proposal remains inspectable | Maintainer · CLI/Git | pass | Ten independent scratch worktrees produced the expected field-specific public refusals, while the active-Spec sweep accepted honest proposal-only records. |
| QA-03 | CF-5 and Task 13 operation boundaries: the closed operation vocabulary is enforced independently at strict checking, Implement dispatch, Task commit, push and Settle | Maintainer and Supervisor · CLI/Git | pass | Public strict checking refused an unknown token and absent list; committed scratch grants without `implement` or `commit` stopped Implement and Settle before the named action, with unchanged HEADs. Fresh Daemon checks covered independent commit and push decisions. |
| QA-04 | Goal 4, CF-3 and outside evidence: preserved authorization records at revision `6b8ea487...` remain readable with original provenance, including legacy multi-Spec scope | Reviewer · Git/backend | pass | Git exposed all 42 preserved records, and the fresh full-corpus reader run passed against those immutable objects. |
| QA-05 | US-3, CF-2 and Tasks 05/14: every governed Task change stays within the grant at its authorizing ancestor, and both canonical citation forms drive a non-empty changed-path audit | Reviewer · Git/CLI | fail | F-003 persists: the shipped mechanical audit reads pre-amendment grant revisions for Tasks 05, 13, and 14, so their changes to `mechanical_test.go` and `coherence.go` remain outside the exact bounded paths. |
| QA-06 | Goal 3, CF-6 and declared break 3: read-only checking remains available while modified authored commands refuse execution without an ancestor approval | Supervisor · CLI/Git | pass | Public read-only checking passed; `--run-verification` refused with `SC-SOURCE-UNTRUSTED: modified-artifact`, and a fresh independent reread confirmed the marker stayed absent and HEAD unchanged. |
| QA-07 | Task 06 execution seams: Implement probing, post-Agent Verification and Settle apply the same source refusal; Daemon-owned status and Result remain outside the authored projection | Supervisor · CLI/Git | pass | Public Settle refused the modified Task before shell or commit; the fresh cross-boundary selection passed the probe, post-Agent, Settle, projection, status, and Result-preservation contracts. |
| QA-08 | Declared break 2 and Tasks 04/09/11: Governed Path and sanctioned-regeneration discovery grow to Spec-contained and archived grants while legacy declarations, ordinary paths and one parser remain preserved | Reviewer · backend/docs | pass | Fresh historical and regeneration selections passed; two public `make baseline-digests` runs returned `changed:false`, and a source sweep found one authorization decoder. |
| QA-09 | CF-3 and Tasks 07/08/12: canonical Baseline guides, authoring skills and templates place the record inside the Spec while preserving prior wording; generated guides, shipped skills and digest pins converge | Maintainer · docs/CLI | pass | Two public managed refreshes each reported `File changes: 0`; both digest regenerations reported no changes, and `make skills-sync-check` passed. |
| QA-10 | Vocabulary Contract and glossary currency: both Grant Refusal Codes are defined by their emitting owners, the checker runs the contract, and no additional term needs a glossary update | Maintainer · docs/CLI | pass | Strict checking did not skip vocabulary validation; both constants match the glossary's `Grant Refusal Code` entry, and this rerun introduced no term. |
| QA-11 | Non-Goals and regression lock: no authentication service, global permission database, arbitrary-shell authority, deployment/release grant, paid call, Task-status ownership change or undeclared public behavior entered the change | Reviewer · CLI/Git | pass | Dependencies remain unchanged, the complete gates pass, public refusal probes left their scratch HEADs stable, and fresh Daemon cases preserve status and Result ownership. F-003 is recorded separately as the failed authority chronology. |
| QA-12 | Pull Request approval, checks/status, unresolved threads, Merge-Ready acceptance and review-artifact ancestry controls | Maintainer · Pull Request | blocked (environment: no open Pull Request) | The supplied fact blocks the live PR surface; all five controls have head-bound equivalent evidence below. |

## Row evidence

### QA-01

```yaml
inputs:
  - kind: repository_path
    ref: docs/specs/0119-spec-contained-authorization/_authorization.md
  - kind: repository_path
    ref: docs/specs/0119-spec-contained-authorization/_prd.md
  - kind: repository_path
    ref: docs/specs/0119-spec-contained-authorization/_techspec.md
```

Passed. The rebuilt CLI returned no strict finding. The fresh
`TestCurrentRecordPermitsItsDeclaredOperations` run accepted the record as
operative and confirmed `implement`, `commit`, and `push`. A Git read at
target commit `3e833b00` showed `status: approved`, grant date, consuming
Spec, 25 exact paths, and the five through-merge operations; a diff from that
copy to audited HEAD exited 0. This row establishes the current record's shape
and role. QA-05 separately tests whether each consuming Task commit could read
that scope at its own authorizing revision.

### QA-02

```yaml
inputs:
  - kind: repository_path
    ref: internal/speccheck/constraints.go
  - kind: repository_path
    ref: internal/authorization/authorization.go
```

Passed through the rebuilt public CLI in independent scratch worktrees. The
strict command exited 1 and named the withholding field for a contradictory
`status: proposed` plus date, `granted: null`, `status: withdrawn`, a
wrong consumer, an absolute path, `../` traversal, a glob, the repository's
`CLAUDE.md` symlink, a duplicate path, and unknown operation `tag`.
Wrong-consumer input preserved `SC-TOOLING-UNAUTHORIZED`; the remaining
grant-shape cases emitted `SC-TOOLING-UNAPPROVED`. The full active-Spec
check passed while proposal-only Specs remained present, independently
confirming that an honest proposal is inspectable without becoming a grant or
a false finding.

### QA-03

```yaml
inputs:
  - kind: repository_path
    ref: internal/authorization/authorization.go
  - kind: repository_path
    ref: internal/cli/implement.go
  - kind: repository_path
    ref: internal/cli/settle.go
  - kind: repository_path
    ref: internal/daemon/task_engine.go
```

Passed. Public strict checking rejected the unknown `tag` token and rejected
an absent operations list because the executable graph lacked
`implement`. In an isolated scratch repository where that absence was
committed, public Implement exited 2 with
`authorization operation "implement" is not permitted`, reported no Run,
Agent, commit, or push side effect, and left HEAD at the scratch commit. In a
second committed scratch history, public Settle exited 2 before Verification
with `authorization operation "commit" is not permitted` and left HEAD
unchanged. Fresh `TestCommitAndPushAuthorityAreSeparate` evidence confirms
the Daemon asks `commit` and `push` independently; no CLI refusal exists
for the policy-only `pull_request`, `merge`, or `release` operations.

### QA-04

```yaml
inputs:
  - kind: external_repository
    ref: roundfix Git revision 6b8ea48725cbca13974eee0b400b3482202874f6
```

Passed. A fresh
`git ls-tree -r --name-only 6b8ea487... -- docs/workflow/authorizations`
read returned 42 records dated from 2026-07-30 through 2026-09-08. The fresh
`TestAuthorizationReaderResolvesPreservedHistoricalRecords` run read the
immutable corpus, retained the historical multi-Spec consumer, classified no
record as an accidental grant to Spec 0119, and kept unavailable revision
distinct from granted and refused. Those records predate this Spec and provide
its required outside acceptance evidence.

### QA-05

```yaml
inputs:
  - kind: repository_path
    ref: docs/specs/0119-spec-contained-authorization/_authorization.md
  - kind: repository_path
    ref: docs/specs/0119-spec-contained-authorization/task_*.md
  - kind: repository_path
    ref: internal/speccheck/governed.go
```

Failed. Fresh `git diff-tree --no-commit-id --name-only -r <commit>`
output was inspected for all thirteen non-QA Task commits. The final
25-path amendment is target commit `3e833b00`, and that commit is an ancestor
of final candidate merge `d4e03f29`. The Task audit does not read the final
record retroactively: it resolves the merge base between the delivery target
and each Task commit's first parent.

The shipped `RunMechanicalStage` logic was invoked against those exact Task
commits and target revision. It read `454d6784` for Task 05 and
`11376668` for Tasks 13 and 14. Neither authorizing revision contains the
2026-09-10 amendment, so it emitted three `QA-AUTH-PATHS` findings:

- Task 05 commit `546b8800` changes governed
  `internal/speccheck/mechanical_test.go`;
- Task 13 commit `9b7f9615` changes governed
  `internal/speccheck/coherence.go`;
- Task 14 commit `3eb6f85b` changes
  `internal/speccheck/mechanical_test.go` again.

The target's later amendment names both paths, but Core Feature 2 and the
mechanical audit explicitly reject later authorization of earlier consuming
work. The remaining governed Task changes are either exact paths in the grant
their audit read or outputs of the sanctioned `make baseline-digests`
regeneration.

### QA-06

```yaml
inputs:
  - kind: repository_path
    ref: internal/speccheck/verification.go
  - kind: repository_path
    ref: internal/cli/spec_check.go
```

Passed in a scratch checkout whose Task 01 command was changed to
`touch qa-should-not-run`. Public read-only strict checking exited 0,
reported that authored Verification commands were not executed, and left the
marker absent. The same command with `--run-verification` exited 1 on
`SC-SOURCE-UNTRUSTED: modified-artifact`, named the differing authored
projection, and again left the marker absent. A fresh Git reread confirmed
HEAD stayed `d4e03f29`.

### QA-07

```yaml
inputs:
  - kind: repository_path
    ref: internal/daemon/verification_probe.go
  - kind: repository_path
    ref: internal/daemon/engine.go
  - kind: repository_path
    ref: internal/cli/settle.go
```

Passed. Public Settle selected the modified-source scratch repository, exited
1 with the same `SC-SOURCE-UNTRUSTED: modified-artifact` decision, left
`qa-should-not-run` absent, and created no commit. Fresh probe,
post-Agent, Settle, and authored-projection selections passed through their
own paths. Four fresh Daemon ownership cases confirmed `in_progress` before
Agent work, normalization before Verification, and Result preservation across
normalization and repair handoff.

### QA-08

```yaml
inputs:
  - kind: repository_path
    ref: internal/speccheck/governed.go
  - kind: repository_path
    ref: internal/suiteguardcontract/regeneration.go
  - kind: repository_path
    ref: internal/authorization/authorization.go
```

Passed. The fresh authorization and suite-guard selection covered preserved
historical records, archived multi-Spec grants, frontmatter-free legacy
declarations, and proposed, null-dated, malformed, or unrelated refusals. Both
public `make baseline-digests` runs in the clean scratch checkout returned
`ok:true, changed:false`, and Git remained clean after each. A source sweep
found `parseAuthorizationRecord`, `splitAuthorizationFrontmatter`, and
`authorizationFrontmatterMapping` only in
`internal/authorization/authorization.go`.

### QA-09

```yaml
inputs:
  - kind: repository_path
    ref: internal/baseline/assets/modules/*.json
  - kind: repository_path
    ref: .agents/skills/write-*/**
  - kind: repository_path
    ref: skills/write-*/**
  - kind: repository_path
    ref: docs/agents/setup-context.json
```

Passed through the rebuilt public CLI in a clean scratch checkout. The first
and second
`roundfix baseline update --repo . --no-skills --yes --format text` runs
both reported `Baseline update: verified`, identical prior/current catalog
digest `sha256:136f8ff3...`, `File changes: 0`, and
`Idempotence: verified`. Git stayed clean after both runs. Two later
`make baseline-digests` runs each reported no changes, and
`make skills-sync-check` passed. The complete docs gate independently
validated the template phrases, generated corpus, and ownership contracts.

### QA-10

```yaml
inputs:
  - kind: repository_path
    ref: CONTEXT.md
  - kind: repository_path
    ref: internal/speccheck/constraints.go
  - kind: repository_path
    ref: internal/speccheck/verification.go
```

Passed. Neither the current-Spec strict check nor the full active-Spec sweep
listed the Vocabulary Contract detector among its skips.
`internal/speccheck/constraints.go` emits
`SC-TOOLING-UNAPPROVED`, `internal/speccheck/verification.go` emits
`SC-SOURCE-UNTRUSTED`, and `CONTEXT.md` defines both under
`Grant Refusal Code`. The rerun changes no product term, so the required
glossary-currency check found no update.

### QA-11

```yaml
inputs:
  - kind: repository_path
    ref: go.mod
  - kind: repository_path
    ref: go.sum
  - kind: repository_path
    ref: docs/specs/0119-spec-contained-authorization/_prd.md
```

Passed for scope. `go.mod` and `go.sum` are byte-identical to the original
grant commit. The implementation adds no HTTP endpoint, authentication layer,
credential policy, dependency, deployment, release, or paid-service path. The
complete repository gates passed, and fresh Daemon status/Result ownership
cases passed. The public negative probes used only scratch Git state and the
rebuilt local binary. F-003 is an authority-history failure and is not hidden
as a Non-Goal regression; QA-05 and the finding below carry it directly.

### QA-12

```yaml
inputs:
  - kind: live_service
    ref: GitHub Pull Request for feat/spec-contained-authorization
```

Blocked by the supplied environment fact: no Pull Request is open for
`feat/spec-contained-authorization`, and the Run Worktree branch never has
one.

Equivalent controls for audited head `d4e03f29`:

- approval: the maintainer's two-path amendment is target commit
  `3e833b00`, an ancestor of the candidate merge, but F-003 proves it does
  not precede the three consuming Task commits under the shipped audit;
- checks and status: `make verify` and `make verify-docs` pass, while
  terminal QA remains failed on F-003;
- unresolved review threads: no Pull Request exists, so no Pull Request thread
  stands or is claimed resolved;
- Merge-Ready acceptance: none exists for this head;
- review-artifact ancestry: no accepted review artifact is claimed for this
  head; the prior closed reports cover `60e05eac` and `3eb6f85b`.

Unblocking action: replay the three consuming Task commits after the amendment
is in their authorizing target ancestry, rerun the complete gate, obtain the
configured pre-Pull-Request review for the resulting head, then open the target
branch Pull Request and observe its required checks and review state.

## Findings

### F-003 — the two-path amendment remains retroactive for three Task commits

- Impact: `Trust-Damage` — a reviewer following the shipped audit cannot
  reproduce the claimed grant-to-change relationship from the consuming Spec.
- Actor and step: the reviewer enumerates every non-QA Task commit with
  `git diff-tree --no-commit-id --name-only -r <commit>` and asks the
  mechanical audit which ancestor grant each commit can read.
- Expected: the exact bounded path exists in the grant at the Task commit's
  authorizing revision; a later target amendment cannot authorize earlier
  consuming work.
- Actual: Task 05 reads `454d6784`, while Tasks 13 and 14 read
  `11376668`. Those record versions omit
  `internal/speccheck/mechanical_test.go` and
  `internal/speccheck/coherence.go`. The amendment adding them appears later
  at `3e833b00`.
- Reproduction: run `git merge-base 3e833b00 <task-commit>^1` for
  `546b8800`, `9b7f9615`, and `3eb6f85b`; inspect the authorization at
  each returned revision; then run `RunMechanicalStage` with those Task
  commits and delivery target `3e833b00`.
- Evidence: the exact mechanical run emitted three `QA-AUTH-PATHS` findings
  naming Task 05's `mechanical_test.go`, Task 13's `coherence.go`, and
  Task 14's `mechanical_test.go`.
- History: repeated from both prior QA reports. The latest amendment fixes the
  current record's path list but does not satisfy the earlier report's required
  replay of the consuming commits.
- Affected row: QA-05.
- Required correction: replay the three consuming Task commits on history
  whose delivery-target ancestry already contains `3e833b00`, without
  changing their behavior or widening the grant again, then rerun the complete
  gate.

## Blocked and skipped

- QA-12 is `blocked (environment: no open Pull Request)`. The supplied fact
  proves the cause, and the row records approval, checks/status, unresolved
  threads, Merge-Ready acceptance, and review-artifact ancestry for audited
  head `d4e03f29`.
- No matrix row was finding-blocked, declared-blocked, or skipped. The three
  entries under `Mechanical skips` remain the Daemon's materialized detector
  record; they are not skipped matrix rows and do not change the counters.

## Coverage

Closed: 12 rows — 10 pass, 1 fail, 1 blocked by environment, 0 blocked by
finding, 0 blocked by declaration, 0 skipped, and 0 pending. The matrix maps
Goals 1-4, User Stories 1-3, Core Features 1-6, all three declared intentional
breaks, the outside-evidence obligation, changed-file chronology, Vocabulary
Contract, glossary currency, and Non-Goals.

Behavior probes covered contradictory approval state, null date, withdrawal,
wrong consumer, absolute/traversing/glob/symlink/duplicate paths, unknown and
absent operations, committed Implement and Settle operation refusals, edited
authored commands with marker rereads, historical and archived records,
regeneration idempotence, and repeated managed refresh. No Pull Request,
release, deployment, credential, or paid-service journey ran.

Finding totals: 0 `Blocks-Completion`, 0 `Data-Loss`, 1
`Trust-Damage`, 0 `Friction`, and 0 `Cosmetic`.

## Final verdict

`fail`: 10 of 12 rows passed, QA-05 failed, and the no-Pull-Request row is
environment-blocked with equivalent evidence. The two added paths must exist
in delivery-target ancestry before the three commits that consume them; replay
those commits on the amended target, then rerun the complete gate before Pull
Request preparation.

## Mechanical skips

| Detector | Missing artifact |
| --- | --- |
| authorization bounded paths | Task commits |
| consequent-fix commit order | consequent-fix declaration |
| QA evidence paths | docs/specs/0119-spec-contained-authorization/qa/evidence |
