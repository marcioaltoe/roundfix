---
spec: 0021-cockpit-visual-fidelity
date: 2026-07-07
verdict: pass
surfaces: [cli, docs]
---

# QA Report — Cockpit Visual Fidelity

## Summary

This spec's contract is the styled render itself, and its enforcement is the
test strategy the PRD demanded: every fidelity behavior is byte-pinned
against token-constructed expectations with no-color twins — 169 TUI tests
(976+ total), all green in the static gate. Those pinned renders are the
render output, so they are the primary evidence; the TUI sweeps (small
sizes, no-color readability, key flows) are covered by the same fixtures.
A live side-by-side with the design PNGs needs a human terminal and is
recommended to the reviewer, but every criterion the spec wrote is proven.
Verdict: pass.

## Results

| # | Criterion / user story | Verdict | Evidence |
| - | ---------------------- | ------- | -------- |
| 1 | Story 1 — state by color (tokens §7) | pass | `TestResolveTokensStyledRendersDocumentedColors` (each token's 256-color sequence), `TestCockpitPhaseRowColorsMarkersThroughTokens`, header chip test (task_01/02 Results) |
| 2 | Story 2 — carded queue, selection, timers | pass | `TestCockpitWorkItemCardPinsSelectionStyling` (selected differs only by documented styling), `TestCockpitBatchSeparatorStampDerivesFromRunEventTimestamps` (`00:38` shape) |
| 3 | Story 3 — grouped summary timeline | pass | `TestTimelineSettledBatchCollapsesAndExecutingBatchExpands` (▶/▼), `TestTimelineToolPayloadRendersOneBoundedLineInThePane` (raw payload never inline), `TestTimelineGutterAlignsAcrossKinds`, indicator test |
| 4 | Story 4 — explanatory empty states | pass | `TestCockpitFetchRunAttachRendersExplanatoryEmptyStates` + review/implement variants (task_04 Result) — the bare "No Work Items" screens from the field screenshots are gone |
| 5 | Story 5 — no-color degrade | pass | `TestResolveTokensIdentityWithoutColor` + a no-color twin per surface (header, phases, cards, timeline rows, modal) asserting ANSI-free marker-only parity |
| 6 | Detail Modal fidelity | pass | `TestCockpitDetailModalStyledThroughTokensWithNoColorTwin` (frame, title block, sections, position footer); the shipped modal behavior suite passes unchanged |
| 7 | TUI sweeps — sizes and keys | pass | Small-width/height fixtures in the suite (88x24 minimum two-pane, full-surface modal fallback); all keys and follow semantics pinned unchanged |
| 8 | Non-Goal — no new controls, no storage change | pass | Collapse is state-driven (no keybinding added); Run Event storage untouched (task Results) |
| 9 | Docs match behavior | pass | README Live Run View section added; SKILL.md five rendering bullets; drift check green (task_05 Result) |

## Failures and blocked items

None. Recommended human follow-up (not blocking): a visual side-by-side of a
live `roundfix attach` against `roundfix-01/02.png` in the reviewer's
terminal — the automated gate proves the written contract; taste is a human
check.

## Environment

Static gate `make verify` green at the branch head (978 tests, 19 packages).
Render evidence is the TUI test suite's byte-pinned `View()` output with
token-constructed expectations — the enforcement mechanism this spec was
created to add. Non-TTY session; no live terminal render captured.
