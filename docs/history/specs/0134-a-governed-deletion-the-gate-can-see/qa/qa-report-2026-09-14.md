---
spec: 0134-a-governed-deletion-the-gate-can-see
date: 2026-09-14
build: "6ebb40e4f7eedb53c6c153a02461be1302578f67"
auditing_binary: "0.12.0"
auditor_staleness: "current: declared tree version: auditing binary 0.12.0 does not predate 0.12.0"
status: closed
verdict: fail
rows_blocked_environment: 1
rows_blocked_finding: 0
rows_blocked_declared: 0
surfaces: [backend, cli]
---

# QA Report

The governed-deletion and enumerated-regeneration repairs pass their assembled
reader paths. The gate fails because Task 04 requires a clean pinned-toolchain
linter, while `go vet ./...` reports pre-existing `copylocks` diagnostics.

## Scope and environment

This was a full gate over both PRD Goals, all 13 Acceptance Criteria in Tasks
01-03, both declared intentional breaks, the three Non-Goals, and the terminal
Task 04 report contract. The actors were a Roundfix operator using Settle and a
maintainer auditing authorization and regeneration behavior. The PRD declares
no Unreachable Acceptance.

The assembled head was
`6ebb40e4f7eedb53c6c153a02461be1302578f67`. The gate resolved and used Go
1.26.7, including that toolchain's `gofmt`, rather than ambient Go 1.27.1.
`make verify` rebuilt `bin/roundfix` as
`roundfix 0.12.0 (6ebb40e4-dirty, built 2026-09-14 07:06:38 -0300)`; the dirty
marker records the untracked Daemon-seeded QA directory, while tracked
implementation bytes were clean.

The first full-gate attempt was stopped by the sandbox when a test process
reached `api.github.com`. One unchanged full-access retry passed. The built CLI
then ran against isolated Git repositories and isolated Roundfix homes under
`/private/tmp`. No production service, credential, user repository, Run
Database, branch, or Pull Request was mutated.

Pull Request fact from the QA assignment: no Pull Request is open and Pull
Request journeys are environment-blocked. The per-Run branch was not queried
for a Pull Request.

## Static gate

- `rtk roundfix spec check 0134-a-governed-deletion-the-gate-can-see --strict`
  exited 0 with no findings. The rebuilt binary repeated that result.
- `rtk env GOTOOLCHAIN=go1.26.7 GOCACHE=/tmp/roundfix-go-build-0134-qa make
  GO=go
  GOFMT=/Users/marcio/go/pkg/mod/golang.org/toolchain@v0.0.1-go1.26.7.darwin-arm64/bin/gofmt
  verify` exited 0 on the unchanged full-access retry. It ran the repository
  suite, Skills checks, and build.
- The corresponding pinned `make ... fmt-check` exited 0.
- `rtk env GOTOOLCHAIN=go1.26.7
  GOCACHE=/tmp/roundfix-go-build-0134-qa go vet ./...` exited 1 with 31
  `copylocks` diagnostics in `internal/agent`; see F-001. The pre-Task base
  `e9746a45` reproduces the same diagnostics.

Evidence: [static and build](evidence/2026-09-14-run-run_20260914T095106Z/01-static-and-build.md)
and [lint finding](evidence/2026-09-14-run-run_20260914T095106Z/04-lint-finding.md).

## Performed repairs

None.

## Assigned repair failures

None.

## Authorization audit inputs

None.

## Mechanical findings

None.

## Results

| # | Status | Provenance |
| - | --- | --- |
| Q-01 | pass | Authored QA precondition, dependency completion, pinned toolchain, and rebuilt binary; static/build evidence |
| Q-02 | pass | Task 01's addition/removal/rename characterization at `c03a9f30`, plus the assembled-tree two-row transition; governed-mutation evidence |
| Q-03 | pass | PRD Goal 1, Task 02 removal and Settle parity, and intentional break 1 through rebuilt CLI exit 2 plus independent reread; governed-mutation evidence |
| Q-04 | pass | Task 02 governed-source rename and shared Daemon/Settle classifier readers; fresh focused reader checks |
| Q-05 | pass | Task 02 ordinary-removal control and preserved refusal tokens/conditions; rebuilt CLI success plus fresh focused checks |
| Q-06 | pass | PRD Goal 2, Task 03 enumerated-list authority, and intentional break 2 through the mechanical reader; regeneration evidence |
| Q-07 | pass | Task 03 command-only resolution, audit/suite-guard parity, and outside Baseline ownership evidence; regeneration evidence |
| Q-08 | pass | Repository suite, formatting check, Skills checks, and rebuilt binary on Go 1.26.7; static/build evidence |
| Q-09 | fail | F-001 — the required lint-clean criterion is false: pinned `go vet ./...` exits 1 |
| Q-10 | pass | Refusal vocabulary, operation vocabulary, Governed Path set, authorization ancestry/scope, and all three Non-Goals remain bounded; regeneration/scope evidence |
| Q-11 | blocked (environment: no open Pull Request) | Assignment fact; local checks passed, but no current approval, Merge-Ready acceptance, review threads, or review-artifact ancestry can be observed |
| Q-12 | pass | This report is closed with one verdict, exact typed counters, zero pending/skipped QA rows, one finding, and resolving repository evidence paths |

### Q-01

Actor and entry point: maintainer through `roundfix spec check` and the authored
Task Graph. Expected and observed: `task_04` is the terminal QA node, Tasks 02
and 03 are completed, strict checking returns no findings, and the gate uses a
binary rebuilt from the assembled head. A second strict check through that
rebuilt binary independently confirmed the result.

```yaml
inputs:
  - kind: repository_path
    ref: docs/specs/0134-a-governed-deletion-the-gate-can-see/_tasks.md
  - kind: repository_path
    ref: docs/specs/0134-a-governed-deletion-the-gate-can-see/task_0*.md
  - kind: repository_path
    ref: cmd/roundfix/**
  - kind: repository_path
    ref: internal/app/version.go
```

### Q-02

Actor and entry point: maintainer reading and executing the Task 01
characterization. A fresh Go 1.26.7 run at commit `c03a9f30` passed the three
pre-change answers: addition `true`, removal `false`, and governed-source rename
`false`. The current-tree test passed with all three `true`; `git show
b8869c7a -- internal/daemon/task_engine_test.go` confirms that only removal and
rename moved. The full current suite proves every surviving assertion still
runs.

```yaml
inputs:
  - kind: repository_path
    ref: internal/daemon/task_engine_test.go
  - kind: repository_path
    ref: internal/daemon/task_engine.go
```

### Q-03

Actor and entry point: operator running the rebuilt `roundfix settle` command
against a failed Task with a deleted tracked `Makefile` and `implement` but no
`commit` authority. The command exited 2 with
`authorization operation "commit" is not permitted`. A fresh reread preserved
HEAD, the failed Task checksum, and ` D Makefile`; the Verification marker was
absent. This covers Task 02 criteria 1 and 3, PRD Goal 1, and intentional break
1 through the reader an operator uses.

Evidence: [governed mutation](evidence/2026-09-14-run-run_20260914T095106Z/02-governed-mutation.md).

```yaml
inputs:
  - kind: repository_path
    ref: internal/cli/settle.go
  - kind: repository_path
    ref: internal/cli/settle_test.go
  - kind: repository_path
    ref: internal/daemon/task_engine.go
```

### Q-04

Actor and entry point: Daemon executing Task-cycle commit preflight, with
Settle as the adjacent public reader. Fresh focused tests passed the removal,
governed-source rename, public Settle removal, and characterization cases. The
rename input contained `Makefile` only before the turn and an ordinary
destination only after it; the Daemon refused from the removed source. Both
Daemon and Settle call `HasGovernedSnapshotMutation`.

```yaml
inputs:
  - kind: repository_path
    ref: internal/daemon/task_engine.go
  - kind: repository_path
    ref: internal/daemon/task_engine_test.go
  - kind: repository_path
    ref: internal/cli/settle.go
  - kind: repository_path
    ref: internal/cli/settle_test.go
```

### Q-05

Actor and entry point: operator settling an ordinary deletion, with invalid and
out-of-order authority probes in the adjacent reader suite. The rebuilt CLI
settled a deleted `internal/ordinary.go` without an authorization record,
committed only that deletion and its Task status, and left a clean worktree.
Fresh checks also kept the existing missing-`implement`, missing-`commit`,
out-of-grant `QA-AUTH-PATHS`, same-commit grant, and retroactive-grant
refusals. This covers Task 02 criteria 4 and 5.

```yaml
inputs:
  - kind: repository_path
    ref: internal/daemon/task_engine_test.go
  - kind: repository_path
    ref: internal/cli/implement_test.go
  - kind: repository_path
    ref: internal/cli/settle_test.go
  - kind: repository_path
    ref: internal/speccheck/mechanical_test.go
```

### Q-06

Actor and entry point: maintainer through the mechanical changed-path audit.
The enumerated-output reader allowed the one listed output and emitted
`QA-AUTH-PATHS` for an owner-derived sibling outside the enumeration. The
exact-output positive control still passed. This covers Task 03 criteria 1-2,
PRD Goal 2, and intentional break 2.

```yaml
inputs:
  - kind: repository_path
    ref: internal/speccheck/mechanical.go
  - kind: repository_path
    ref: internal/speccheck/mechanical_test.go
```

### Q-07

Actor and entry point: maintainer through the mechanical audit, Baseline
ownership resolver, and suite guard. Fresh tests passed command-only ownership
resolution and exact audit/suite-guard parity for both declaration shapes.
Outside evidence came from the pre-existing ownership records and the
2026-08-06 enumeration at commit
`81a6afb48f4a3683d0e5fad52f3919cf1bdfbbf4`, path
`docs/workflow/authorizations/2026-08-06-proof-cost.md`; the current resolver's
output matched that independently authored list.

Evidence: [regeneration and scope](evidence/2026-09-14-run-run_20260914T095106Z/03-regeneration-and-scope.md).

```yaml
inputs:
  - kind: repository_path
    ref: internal/baseline/assets/**/_ownership.yml
  - kind: repository_path
    ref: internal/baseline/testdata/**/_ownership.yml
  - kind: repository_path
    ref: internal/baseline/derived_ownership.go
  - kind: repository_path
    ref: internal/baseline/derived_ownership_test.go
  - kind: repository_path
    ref: internal/suiteguardcontract/regeneration.go
  - kind: repository_path
    ref: internal/suiteguardcontract/regeneration_test.go
  - kind: repository_path
    ref: internal/speccheck/mechanical.go
  - kind: repository_path
    ref: internal/speccheck/mechanical_test.go
```

### Q-08

Actor and entry point: maintainer through the repository Verification and
formatter targets. The fresh pinned-toolchain retry exited 0 for the full
repository gate and built the tested CLI. A separate pinned `fmt-check` exited
0. The first sandbox denial is recorded as an environment parity deviation,
not as a product pass or failure.

```yaml
inputs:
  - kind: repository_path
    ref: Makefile
  - kind: repository_path
    ref: go.mod
  - kind: repository_path
    ref: go.sum
  - kind: repository_path
    ref: "**/*.go"
  - kind: repository_path
    ref: .agents/skills/**
  - kind: repository_path
    ref: skills/**
```

### Q-09

Actor and entry point: maintainer running the Task 04 lint gate on Go 1.26.7.
The repository declares no `lint` Make target or `.golangci.yml`; the Go-native
`go vet ./...` check exited 1 with 31 `copylocks` diagnostics. An unchanged
pre-Task checkout reproduced them in `internal/agent`, which this Spec did not
touch. The result is still a failed Task 04 acceptance criterion.

Evidence: [lint finding](evidence/2026-09-14-run-run_20260914T095106Z/04-lint-finding.md).

```yaml
inputs:
  - kind: repository_path
    ref: Makefile
  - kind: repository_path
    ref: go.mod
  - kind: repository_path
    ref: internal/agent/**
```

### Q-10

Actor and entry point: maintainer auditing scope through Git commits and fresh
refusal tests. The Task range changed only the three Task files and the three
named implementation/test areas. `internal/spec/authorization.go` and
`internal/speccheck/governed.go` are byte-unchanged, so the operation
vocabulary and Governed Path set did not move. The authorization commit
`f2d3f0ce` precedes the consuming commits, and the authorized governed test
change stayed within its bounded path. No external/symlinked Spec Root work or
Daemon wall-clock work entered the slice. Because the strict checker skipped
the vocabulary detector, this row also independently confirms that the feature
reuses existing tokens and that the TechSpec's Vocabulary Contract coins none.

```yaml
inputs:
  - kind: repository_path
    ref: internal/spec/authorization.go
  - kind: repository_path
    ref: internal/speccheck/governed.go
  - kind: repository_path
    ref: docs/specs/0134-a-governed-deletion-the-gate-can-see/_authorization.md
  - kind: repository_path
    ref: docs/specs/0134-a-governed-deletion-the-gate-can-see/_techspec.md
  - kind: repository_path
    ref: internal/cli/settle*.go
  - kind: repository_path
    ref: internal/daemon/task_engine*.go
  - kind: repository_path
    ref: internal/speccheck/mechanical*.go
```

### Q-11

Actor and entry point: reviewer at the Pull Request boundary. Cause: the QA
assignment confirms that no Pull Request is open. Equivalent control evidence
is incomplete: the local repository Verification passed, but no approval was
obtained, no Pull Request exists on which review threads could stand, no
Merge-Ready acceptance exists, and no review artifact is tied to a Pull Request
head. A later delivery stage must open the Pull Request on
`feat/spec-contained-authorization` and observe all five controls for its
current head.

```yaml
inputs:
  - kind: live_service
    ref: GitHub Pull Request for feat/spec-contained-authorization
```

### Q-12

Actor and entry point: maintainer reopening the QA artifact. The final reread
found one closed frontmatter block, `verdict: fail`, counters matching the one
environment-blocked row and zero finding/declared-blocked rows, no `pending` or
`skipped` QA status, one F-001 block, and all four relative evidence links on
disk.

```yaml
inputs:
  - kind: repository_path
    ref: docs/specs/0134-a-governed-deletion-the-gate-can-see/qa/qa-report-2026-09-14.md
  - kind: repository_path
    ref: docs/specs/0134-a-governed-deletion-the-gate-can-see/qa/evidence/2026-09-14-run-run_20260914T095106Z/**
```

## Findings

### F-001: the pinned Go linter is not clean

- Impact: Blocks-Completion.
- Actor and journey: maintainer, Task 04 static gate.
- Expected: the repository suite, formatter, and linter all exit 0 on the
  pinned Go toolchain.
- Actual: `go vet ./...` exits 1 with 31 `copylocks` diagnostics because value
  receiver methods copy `ACPXRunner`, which contains `sync.Mutex`.
- Reproduction: run the Q-09 command from the repository root on Go 1.26.7.
- Independent confirmation: the pre-Task base `e9746a45` produces the same
  diagnostics under `go vet ./internal/agent`, and the Spec's Task range has no
  `internal/agent` change.
- Affected row: Q-09 only. The repository Verification and every feature flow
  remain independently measured and passing.
- Evidence: [04-lint-finding.md](evidence/2026-09-14-run-run_20260914T095106Z/04-lint-finding.md).

## Blocked and skipped

Q-11 is blocked by the proved environment fact `no open Pull Request`.
Unblocking requires a later authorized delivery stage to open the Pull Request
on `feat/spec-contained-authorization`, then observe approval, current-head
checks/status, unresolved threads, Merge-Ready acceptance, and review-artifact
ancestry. Local Verification is the only equivalent control established in
this gate.

No QA row was skipped. The mechanical skips materialized before the human QA
run are preserved below.

## Coverage

- PRD Goals: 2 passed / 2 total.
- Tasks 01-03 Acceptance Criteria: 13 observed / 13 total; all feature
  criteria passed.
- QA rows: 10 pass, 1 fail, 1 blocked (environment), 0 blocked (finding),
  0 blocked (declared), 0 skipped, 0 pending.
- Findings: 1 Blocks-Completion; 0 Data-Loss, 0 Trust-Damage, 0 Friction,
  0 Cosmetic.
- Probes: governed addition, deletion, governed-source rename, ordinary-file
  deletion, missing operation, missing record, out-of-grant path, same-commit
  grant, retroactive grant, enumerated output, command-only output, invalid
  regeneration declaration, and independent reader parity.
- Non-Goals: external/symlinked Spec Root identity, Governed Path and operation
  vocabulary changes, and residual Daemon wall-clock work were checked and did
  not enter the Task range.
- `rows_blocked_environment: 1`, `rows_blocked_finding: 0`,
  `rows_blocked_declared: 0`.

## Final verdict

Fail: 10 rows passed, Q-09 failed, and Q-11 is environment-blocked without
complete equivalent Pull Request evidence. A corrective Task must make the
declared pinned-toolchain lint gate clean or name the repository's intended
lint command, then the full gate must rerun; no implementation repair belongs
in this QA Task.

## Mechanical skips

| Detector | Missing artifact |
| --- | --- |
| authorization bounded paths | tooling authorization |
| consequent-fix commit order | consequent-fix declaration |
| QA Report structure | QA Report |
| QA evidence paths | QA Report |
