---
spec: 0146-a-gate-that-runs-the-analyzer
date: 2026-09-18
build: 4917bed8a4ce2c60de808e4064942ca650b8845d
auditing_binary: "0.14.1 (b9dc0072, built 2026-09-18 15:27:02 -0300)"
auditor_staleness: "stale: commit ancestry: build commit predates audited tree"
status: closed
verdict: pass
rows_blocked_environment: 1
rows_blocked_finding: 0
rows_blocked_declared: 0
surfaces: [backend, infra]
---

# QA Report — A gate that runs the analyzer

## Scope and environment

Full authored terminal QA for commit `4917bed8a4ce2c60de808e4064942ca650b8845d` in the Daemon Run Worktree. The actor is a repository maintainer using the Go toolchain and the repository's public Make targets. `roundfix spec check 0146-a-gate-that-runs-the-analyzer --strict` exited 0 with no findings before the matrix was built. The checker skipped four inapplicable or absent-artifact detectors already listed under Mechanical skips.

The prompt states that no Pull Request is open and Pull Request journeys are environment-blocked. The Daemon already ran the repository Verification outside the Agent sandbox; its supervised result is the required static-gate fact and is not rerun here.

## Static gate

- `make verify` — passed, exit 0. The Daemon ran this outside the Agent sandbox and removed its diagnostics artifact on success. The QA Agent did not rerun it. The current composition invokes `go vet ./...` before tests in both Verification tiers.

## Performed repairs

None.

## Assigned repair failures

None.

## Authorization audit inputs

| Task | Outcome | Record | Revision | Detail |
| --- | --- | --- | --- | --- |
| task_02 | granted | docs/specs/0146-a-gate-that-runs-the-analyzer/_authorization.md | b9dc00725d9fc0ffdbce20264d9a4773d2f790e8 | operative grant |

## Mechanical findings

None.

## Results

| # | Story / criterion / sweep | Actor and surface | Status | Evidence |
| - | --- | --- | --- | --- |
| 1 | Repository Verification includes the analyzer and passes | Maintainer; `make verify`; backend and infra | pass | This report, row 1 |
| 2 | Negative control fails with its named diagnostic while the module remains silent; prerequisite outside evidence is satisfied | Maintainer; Go analyzer; backend | pass | This report, row 2 |
| 3 | Full and incremental Verification tiers compose the analyzer | Maintainer; Make targets; infra | pass | This report, row 3 |
| 4 | Existing gate steps retain their names, commands, and order; CI invocation is unchanged | Maintainer; Makefile and CI contract; infra | pass | This report, row 4 |
| 5 | No dependency, analyzer configuration, suppression mechanism, or third-party analyzer was added | Maintainer; repository diff; backend and infra | pass | This report, row 5 |
| 6 | Spec promise coverage and every Task commit stay within the operative authority | Maintainer; Spec checker and Git evidence; infra | pass | This report, row 6 |
| 7 | Pull Request controls through required equivalent evidence | Maintainer; Pull Request boundary; infra | blocked (environment: no open Pull Request; equivalent evidence recorded) | This report, row 7 |

## Row evidence

### 1

```yaml
inputs:
  - kind: repository_path
    ref: Makefile
  - kind: repository_path
    ref: "**/*.go"
```

- Provenance: Requirement 1; repository Verification.
- Steps: record the Daemon-supervised `make verify` result without rerunning it.
- Expected observable: exit 0 with the analyzer target included in the gate.
- Independent confirmation: inspect the direct `vet` target and the gate composition in row 3.
- Persistence check: both complete and incremental compositions are checked in row 3.
- Observed: the Daemon-supervised `make verify` exited 0. `make -n vet` printed `go vet ./...`, and the direct composition inspection in row 3 confirms the command belongs to the gate.
- Status: pass.

### 2

```yaml
inputs:
  - kind: repository_path
    ref: internal/baseline/analyzer/testdata/printfdiagnostic/diagnostic.go
  - kind: repository_path
    ref: internal/baseline/repository_gate_repocontract_test.go
  - kind: repository_path
    ref: docs/specs/0123-runtime-readiness-and-model-capabilities/references/2026-09-08-acpx-runner-value-receivers-copy-its-mutex.md
```

- Provenance: Requirement 2; Requirement 6; outside-evidence row.
- Steps: run the analyzer over the fixture and the full module, execute the focused repository contract, and compare the current module result with Spec 0123's owned finding.
- Expected observable: fixture exits nonzero and names `fmt.Printf format %d`; module exits 0 silently; the focused contract passes; the earlier 31 copied-lock diagnostics are absent.
- Independent confirmation: focused repository-contract test plus the finding's independently recorded prior state.
- Persistence check: repeat the module observation through the complete focused contract.
- Observed fixture command: `GOCACHE=/private/tmp/roundfix-0146-qa-gocache go vet ./internal/baseline/analyzer/testdata/printfdiagnostic` exited 1 and reported `fmt.Printf format %d has arg "not a number" of wrong type string` at `diagnostic.go:6:14`.
- Observed module command: `GOCACHE=/private/tmp/roundfix-0146-qa-gocache go vet ./...` exited 0 with no output.
- Independent confirmation: `GOCACHE=/private/tmp/roundfix-0146-qa-gocache go test -count=1 -tags repocontract -run '^TestRepositoryGateRunsTheAnalyzer$' ./internal/baseline` exited 0 (`ok`, 1.148s).
- Outside evidence: `docs/specs/0123-runtime-readiness-and-model-capabilities/references/2026-09-08-acpx-runner-value-receivers-copy-its-mutex.md`, whose originating Secondbrain capture and fresh 2026-09-08 replay independently recorded 31 `copylocks` diagnostics before Spec 0145 repaired them. The current silent module run proves the prerequisite state has changed, while the failing fixture proves the analyzer remains capable of reporting a diagnostic.
- Status: pass.

### 3

```yaml
inputs:
  - kind: repository_path
    ref: Makefile
```

- Provenance: Requirement 3.
- Steps: inspect Make's public target graph and dry-run both tiers.
- Expected observable: `verify` and `verify-incremental` each depend on `vet`, whose command is `go vet ./...`.
- Independent confirmation: execute the focused composition contract.
- Persistence check: observe both tier expansions independently.
- Observed: `rg` found `verify: fmt-check vet $(VERIFY_TEST_TARGET) skills-sync-check skills-check build`, `verify-incremental: fmt-check vet test skills-sync-check skills-check build`, and `$(GO) vet $(PKGS)`. `make -n vet`, `make -n verify`, and `make -n verify-incremental` each exited 0; both tier expansions included `go vet ./...`.
- Independent confirmation: the focused repository-contract test in row 2 passed after checking both dependencies.
- Status: pass.

### 4

```yaml
inputs:
  - kind: repository_path
    ref: Makefile
  - kind: repository_path
    ref: .github/workflows/ci-verify.yml
```

- Provenance: Requirement 4.
- Steps: compare the audited tree with delivery-target commit `b9dc0072`, excluding the inserted `vet` target/dependencies, and compare the CI workflow byte-for-byte.
- Expected observable: every previous gate dependency and command remains ordered and unchanged; CI still invokes `make verify`; workflow diff is empty.
- Independent confirmation: Git diff plus current target inspection.
- Persistence check: compare both current and parent revisions.
- Observed: `git diff --unified=12 b9dc0072..HEAD -- Makefile .github/workflows/ci-verify.yml` showed only the `vet` phony declaration, insertion of `vet` after `fmt-check` in both tier dependency lists, and the new direct `vet` target. Existing dependency order and commands were unchanged.
- Independent confirmation: `git diff --exit-code b9dc0072..HEAD -- .github/workflows/ci-verify.yml` exited 0. The unchanged workflow still invokes `make verify` under its `Verify` step.
- Status: pass.

### 5

```yaml
inputs:
  - kind: repository_path
    ref: go.mod
  - kind: repository_path
    ref: go.sum
  - kind: repository_path
    ref: Makefile
  - kind: repository_path
    ref: internal/baseline/analyzer/testdata/printfdiagnostic/diagnostic.go
  - kind: repository_path
    ref: internal/baseline/repository_gate_repocontract_test.go
```

- Provenance: Requirement 5.
- Steps: inspect the complete changed-path set and dependency/configuration diffs from `b9dc0072` to the audited build; search changed content for suppressions and third-party analyzer use.
- Expected observable: no dependency manifest, analyzer configuration, suppression mechanism, or third-party tool was added.
- Independent confirmation: `go mod` files are byte-identical and the implementation invokes only `go vet`.
- Persistence check: inspect both Task commits and the aggregate diff.
- Observed: `git diff --exit-code b9dc0072..HEAD -- go.mod go.sum` and `git diff --check b9dc0072..HEAD` exited 0. The aggregate changed-path set contains only `Makefile`, Task 01 and Task 02, the fixture, and the repository-contract test.
- Independent confirmation: searching the implementation diff for `nolint`, suppression, allowlist, exemption, third-party analyzer, `golangci`, and `staticcheck` produced no matches. The only analyzer command added is the toolchain's `go vet`.
- Status: pass.

### 6

```yaml
inputs:
  - kind: repository_path
    ref: docs/specs/0146-a-gate-that-runs-the-analyzer/_prd.md
  - kind: repository_path
    ref: docs/specs/0146-a-gate-that-runs-the-analyzer/_techspec.md
  - kind: repository_path
    ref: docs/specs/0146-a-gate-that-runs-the-analyzer/_tasks.md
  - kind: repository_path
    ref: docs/specs/0146-a-gate-that-runs-the-analyzer/task_*.md
  - kind: repository_path
    ref: docs/specs/0146-a-gate-that-runs-the-analyzer/_authorization.md
```

- Provenance: Requirement 7.
- Steps: use the strict Spec checker for promise coverage; audit commit ancestry, actual commit paths, authorization timing, bounded tooling paths, and separation of the authorization and tooling commits.
- Expected observable: checker exits 0 with no findings; authorization predates implementation; task_02 changes only `Makefile` and its own Task file; task_01 changes only its ordinary source and own Task file.
- Independent confirmation: `git diff-tree` for each Task commit and `git merge-base --is-ancestor` for the authorization revision.
- Persistence check: aggregate changed-path audit from the approved revision through HEAD.
- Observed promise check: `roundfix spec check 0146-a-gate-that-runs-the-analyzer --strict` exited 0 with `No findings`; authored Verification commands were not executed by that checker.
- Observed chronology: the authorization file was committed at `88ceb936`, the target revision `b9dc0072` containing the operative record is an ancestor of Task 01 commit `16a32bbf`, and Task 01 is an ancestor of Task 02 commit `4917bed8`.
- Observed scope: `git diff-tree --no-commit-id --name-only -r 16a32bbf` listed Task 01 plus its ordinary contract test and fixture. The same command for `4917bed8` listed only authorized `Makefile` plus Task 02. The aggregate diff from `b9dc0072` contains no other paths. The incidental fsmonitor IPC warning did not alter either command's exit 0 or path output.
- Status: pass.

### 7

```yaml
inputs:
  - kind: live_service
    ref: GitHub Pull Request state for feat/0146-analyzer-gate
```

- Provenance: Pull Request row.
- Steps: use the prompt's supervised no-open-Pull-Request fact and record equivalent evidence for approval, checks/status, unresolved review threads, Merge-Ready acceptance, and review-artifact ancestry.
- Expected observable: environment block is explicit and every Pull Request control has equivalent observed or supervised evidence.
- Independent confirmation: target-branch identity and audited HEAD are recorded separately from the Run Worktree branch.
- Persistence check: unavailable until a Pull Request exists; this row is never carry-forward eligible.
- Environment cause: the Run prompt states that no Pull Request is open and Pull Request journeys are environment-blocked. The per-Run branch `roundfix/run-run_20260918T182723Z_652b1aff3a2db39d` is never pushed; the Spec target is `feat/0146-analyzer-gate` at `b9dc0072`, while the audited Run Worktree head is `4917bed8`.
- Equivalent approval evidence: the maintainer-approved `_authorization.md` grants implementation and Pull Request authority for this Spec's bounded change; no separate Pull Request acceptance exists yet.
- Equivalent checks and status evidence: the Daemon-supervised repository Verification exited 0, and rows 2-6 independently passed their focused controls.
- Equivalent unresolved-thread evidence: no Pull Request exists, so no review thread exists to remain unresolved.
- Equivalent Merge-Ready acceptance: none exists yet because there is no Pull Request or review handoff; this absence is explicitly recorded rather than treated as acceptance.
- Equivalent review-artifact ancestry: no review artifact exists yet. The report binds all local evidence to audited head `4917bed8`; it makes no Merge-Ready or review-ancestry claim for target head `b9dc0072`.
- Unblocking action: land the Run commits on `feat/0146-analyzer-gate`, open its Pull Request, and observe the live controls there. The supervised and local equivalents cover every enumerated control for this pre-Pull-Request gate.
- Status: blocked (environment: no open Pull Request; equivalent evidence recorded).

## Findings

None recorded.

## Blocked and skipped

Row 7 is environment-blocked because no Pull Request exists. Its cause, unblocking action, and equivalent observed or supervised evidence are recorded in the row. No row is finding-blocked, declared-blocked, or skipped.

## Coverage

All 7 rows settled: 6 pass and 1 environment-blocked with equivalent evidence. Coverage sources are Requirements 1-7, outside-evidence row, Pull Request row, and repository Verification; each appears in row provenance and no other source is named. Requirement 8 constrains acceptable evidence and is applied to every row; it is not a declared verification source. Counts: `rows_blocked_environment: 1`, `rows_blocked_finding: 0`, `rows_blocked_declared: 0`; skipped: 0.

## Final verdict

Pass. Every runnable row passed, the sole environment-blocked Pull Request row records its cause and equivalent evidence for every required control, and no row failed, remained pending, was finding-blocked, declared-blocked, or skipped.

## Mechanical skips

| Detector | Missing artifact |
| --- | --- |
| consequent-fix commit order | consequent-fix declaration |
| QA Report structure | QA Report |
| QA evidence paths | QA Report |
