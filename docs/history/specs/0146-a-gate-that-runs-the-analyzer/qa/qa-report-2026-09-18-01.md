---
spec: 0146-a-gate-that-runs-the-analyzer
date: 2026-09-18
build: e79d2880a43f7d6ed11752b8625eaec3b598698d
auditing_binary: "0.14.1 (cf1ba1b0, built 2026-09-18 16:13:14 -0300)"
auditor_staleness: "stale: commit ancestry: build commit predates audited tree"
status: closed
verdict: pass
rows_blocked_environment: 1
rows_blocked_finding: 0
rows_blocked_declared: 0
surfaces: [backend, infra]
---

# QA Report — A gate that runs the analyzer

## Scope and environment

Full authored terminal QA for commit `e79d2880a43f7d6ed11752b8625eaec3b598698d` in the Daemon Run Worktree. The actor is a repository maintainer using the Go toolchain and the repository's public Make targets. `roundfix spec check 0146-a-gate-that-runs-the-analyzer --strict` exited 0 with no findings before the matrix was built. The checker skipped four inapplicable or absent-artifact detectors: `SC-ROLLUP-MEMBER`, `SC-BACKLOG-UNMOVED`, the vocabulary documentation detector, and `SC-REF-UNRESOLVED`.

The prompt states that no Pull Request is open and Pull Request journeys are environment-blocked. The Daemon already ran the repository Verification outside the Agent sandbox; its supervised result is the required static-gate fact and was not rerun here. Commands that execute Go used `GOCACHE=/private/tmp/roundfix-0146-qa2-gocache` because the sandbox denied access to the default user cache.

## Static gate

- `make verify` — passed, exit 0. The Daemon ran this outside the Agent sandbox and removed its diagnostics artifact on success. The QA Agent did not rerun it. The current composition invokes `go vet ./...` in both Verification tiers.

## Performed repairs

None.

## Assigned repair failures

None.

## Authorization audit inputs

| Task | Outcome | Record | Revision | Detail |
| --- | --- | --- | --- | --- |
| task_04 | granted | docs/specs/0146-a-gate-that-runs-the-analyzer/_authorization.md | cf1ba1b0f16ad3d038468df5542360c48c6bbad9 | operative grant |

## Mechanical findings

None.

## Results

| # | Story / criterion / sweep | Actor and surface | Status | Evidence |
| - | --- | --- | --- | --- |
| 1 | Repository Verification includes the analyzer and passes | Maintainer; `make verify`; backend and infra | pass | This report, row 1 |
| 2 | Negative control fails with its named diagnostic while the module stays silent; prerequisite outside evidence is satisfied | Maintainer; Go analyzer and repository contract; backend | pass | This report, row 2 |
| 3 | Full and incremental Verification tiers compose the analyzer | Maintainer; Make targets; infra | pass | This report, row 3 |
| 4 | Existing gate steps and contracts retain their names, commands, and order; CI invocation is unchanged | Maintainer; Make and CI contracts; infra | pass | This report, row 4 |
| 5 | No dependency, analyzer configuration, suppression mechanism, or third-party analyzer was added | Maintainer; repository diff; backend and infra | pass | This report, row 5 |
| 6 | Spec promise coverage and every Task commit stay within the operative authority | Maintainer; Spec checker and Git evidence; infra | pass | This report, row 6 |
| 7 | Pull Request controls through required equivalent evidence | Maintainer; Pull Request boundary; infra | blocked (environment: no open Pull Request; equivalent evidence recorded) | This report, row 7 |

## Row evidence

### 1

```yaml
inputs:
  - kind: repository_path
    ref: Makefile
  - kind: repository_path
    ref: "**/*.go"
```

- Provenance: Requirement 1; repository Verification.
- Steps: record the Daemon-supervised `make verify` result without rerunning it.
- Expected observable: exit 0 with the analyzer target included in the gate.
- Independent confirmation: inspect and execute the direct analyzer and repository-contract paths in rows 2 and 3.
- Persistence check: both complete and incremental compositions are checked in row 3.
- Observed: the Daemon-supervised `make verify` exited 0. `make -n vet` printed `go vet ./...`; row 3 confirms that command belongs to both Verification tiers.
- Status: pass.

### 2

```yaml
inputs:
  - kind: repository_path
    ref: internal/baseline/analyzer/testdata/printfdiagnostic/diagnostic.go
  - kind: repository_path
    ref: internal/baseline/repository_gate_repocontract_test.go
  - kind: repository_path
    ref: docs/specs/0123-runtime-readiness-and-model-capabilities/references/2026-09-08-acpx-runner-value-receivers-copy-its-mutex.md
```

- Provenance: Requirement 2; Requirement 6; outside-evidence row.
- Steps: run the analyzer over the fixture and the full module, run the focused tagged and untagged controls, execute the public repository-contract gate, and compare the current module result with Spec 0123's owned finding.
- Expected observable: the fixture exits nonzero and names `fmt.Printf format %d`; the module exits 0 silently; the tagged control and `make repo-test` pass; the ordinary sweep does not select the control; the earlier 31 copied-lock diagnostics are absent.
- Independent confirmation: the public `make repo-test` path plus the finding's independently recorded prior state.
- Persistence check: repeat the analyzer behavior through the tagged repository contract and the gate that selects it.
- Observed fixture command: `GOCACHE=/private/tmp/roundfix-0146-qa2-gocache go vet ./internal/baseline/analyzer/testdata/printfdiagnostic` exited 1 and reported `fmt.Printf format %d has arg "not a number" of wrong type string` at `diagnostic.go:6:14`.
- Observed module command: `GOCACHE=/private/tmp/roundfix-0146-qa2-gocache go vet ./...` exited 0 with no output.
- Independent confirmation: the focused tagged command exited 0 with `ok roundfix/internal/baseline`; the untagged form exited 0 with `[no tests to run]`. `GOCACHE=/private/tmp/roundfix-0146-qa2-gocache make repo-test` exited 0 after selecting all four repository contracts, including `TestRepositoryGateRunsTheAnalyzer`; `internal/baseline` and `skills` both reported `ok`.
- Outside evidence: `docs/specs/0123-runtime-readiness-and-model-capabilities/references/2026-09-08-acpx-runner-value-receivers-copy-its-mutex.md` records a fresh 2026-09-08 replay with 31 `copylocks` diagnostics before Spec 0145 repaired them. The current silent module run proves the prerequisite state changed, while the failing fixture proves the analyzer still reports diagnostics.
- Status: pass.

### 3

```yaml
inputs:
  - kind: repository_path
    ref: Makefile
```

- Provenance: Requirement 3.
- Steps: inspect Make's public target graph and dry-run the analyzer target and both Verification tiers.
- Expected observable: `verify` and `verify-incremental` each depend on `vet`, whose command is `go vet ./...`.
- Independent confirmation: execute the focused tagged repository contract in row 2.
- Persistence check: observe both tier expansions independently.
- Observed: `verify: fmt-check vet $(VERIFY_TEST_TARGET) skills-sync-check skills-check build`, `verify-incremental: fmt-check vet test skills-sync-check skills-check build`, and the `vet` recipe runs `$(GO) vet $(PKGS)`. `make -n vet`, `make -n verify-incremental`, and `make -n verify` each exited 0; both tier expansions contained `go vet ./...`.
- Independent confirmation: the focused repository-contract test in row 2 passed after checking both dependencies.
- Status: pass.

### 4

```yaml
inputs:
  - kind: repository_path
    ref: Makefile
  - kind: repository_path
    ref: .github/workflows/ci-verify.yml
```

- Provenance: Requirement 4.
- Steps: compare the audited tree with the approved base and the corrective Task's parent; execute the public repository-contract gate; compare the CI workflow byte-for-byte.
- Expected observable: prior gate dependencies, commands, and three repository-contract names remain ordered and unchanged; the analyzer control is appended to the contract selection; CI still invokes `make verify`; the workflow diff is empty.
- Independent confirmation: Git diff plus the successful `make repo-test` execution in row 2.
- Persistence check: compare the current revision with both the approved base and corrective Task parent.
- Observed: the aggregate Makefile diff adds the direct `vet` target and inserts `vet` after `fmt-check` in both Verification tiers. The corrective `cf1ba1b0..e79d2880` diff only appends `TestRepositoryGateRunsTheAnalyzer` to `REPO_CONTRACT_TESTS`; the three prior names and `repo-test` recipe are byte-identical.
- Independent confirmation: `git diff --exit-code 88ceb936..e79d2880 -- .github/workflows/ci-verify.yml` exited 0. The unchanged workflow still invokes `make verify` under `Verify` and `make verify-docs` under `Verify repository contracts`.
- Status: pass.

### 5

```yaml
inputs:
  - kind: repository_path
    ref: go.mod
  - kind: repository_path
    ref: go.sum
  - kind: repository_path
    ref: Makefile
  - kind: repository_path
    ref: internal/baseline/analyzer/testdata/printfdiagnostic/diagnostic.go
  - kind: repository_path
    ref: internal/baseline/repository_gate_repocontract_test.go
```

- Provenance: Requirement 5.
- Steps: inspect the complete changed-path set from the approved base, compare dependency and CI files, run `git diff --check`, and search implementation changes for suppression or third-party analyzer vocabulary.
- Expected observable: no dependency manifest, analyzer configuration, suppression mechanism, or third-party tool is added.
- Independent confirmation: module files and CI are byte-identical, and the implementation invokes only `go vet`.
- Persistence check: inspect the aggregate diff and each implementation Task commit.
- Observed: `git diff --exit-code 88ceb936..e79d2880 -- go.mod go.sum .github/workflows/ci-verify.yml` and `git diff --check 88ceb936..e79d2880` exited 0. The changed-path set contains the Makefile, Spec/Task/QA artifacts, the analyzer fixture, and the repository-contract test; it adds no dependency or analyzer configuration path.
- Independent confirmation: a search of the implementation diff for `nolint`, suppression, allowlist, exemption, `golangci`, `staticcheck`, or third-party analyzer vocabulary returned no matches. The only analyzer command added is the Go toolchain's `go vet`.
- Status: pass.

### 6

```yaml
inputs:
  - kind: repository_path
    ref: docs/specs/0146-a-gate-that-runs-the-analyzer/_prd.md
  - kind: repository_path
    ref: docs/specs/0146-a-gate-that-runs-the-analyzer/_techspec.md
  - kind: repository_path
    ref: docs/specs/0146-a-gate-that-runs-the-analyzer/_tasks.md
  - kind: repository_path
    ref: docs/specs/0146-a-gate-that-runs-the-analyzer/task_*.md
  - kind: repository_path
    ref: docs/specs/0146-a-gate-that-runs-the-analyzer/_authorization.md
```

- Provenance: Requirement 7.
- Steps: use the strict Spec checker for promise coverage; audit commit ancestry, actual commit paths, authorization timing, bounded tooling paths, and separation of authorization and tooling commits.
- Expected observable: the checker exits 0 with no findings; authorization predates implementation; each tooling Task changes only `Makefile` and its own Task file; Task 01 changes only ordinary source and its own Task file.
- Independent confirmation: `git diff-tree` for every implementation Task and `git merge-base --is-ancestor` for the authorization revisions.
- Persistence check: aggregate changed-path audit from the approved base through audited HEAD.
- Observed promise check: `roundfix spec check 0146-a-gate-that-runs-the-analyzer --strict` exited 0 with `No findings`; authored Verification commands were not executed by that checker.
- Observed chronology and scope: the authorization was committed at `88ceb936`, which is an ancestor of Task 01 `16a32bbf`, Task 02 `4917bed8`, and Task 04 `e79d2880`. `git diff-tree --no-commit-id --name-only -r 16a32bbf` listed Task 01 plus its ordinary contract test and fixture. The same command for `4917bed8` listed only authorized `Makefile` plus Task 02; for `e79d2880` it listed only authorized `Makefile` plus Task 04. The seeded mechanical audit resolves Task 04's operative record at target revision `cf1ba1b0`.
- Status: pass.

### 7

```yaml
inputs:
  - kind: live_service
    ref: GitHub Pull Request state for feat/0146-analyzer-gate
```

- Provenance: Pull Request row.
- Steps: use the prompt's supervised no-open-Pull-Request fact and record equivalent evidence for approval, checks and status, unresolved review threads, Merge-Ready acceptance, and review-artifact ancestry.
- Expected observable: the environment block is explicit and every Pull Request control has equivalent observed or supervised evidence.
- Independent confirmation: target-branch identity and audited HEAD are recorded separately from the Run Worktree branch.
- Persistence check: unavailable until a Pull Request exists; this row is not carry-forward eligible.
- Environment cause: the Run prompt states that no Pull Request is open and Pull Request journeys are environment-blocked. The per-Run branch `roundfix/run-run_20260918T191334Z_45ce5cf6e67bdde8` is never pushed; the Spec target is `feat/0146-analyzer-gate` at `cf1ba1b0`, while the audited Run Worktree head is `e79d2880`.
- Equivalent approval evidence: `_authorization.md` records maintainer approval for the bounded implementation and Pull Request operations; no separate Pull Request acceptance exists yet.
- Equivalent checks and status evidence: the Daemon-supervised repository Verification exited 0, and rows 2-6 independently passed their focused controls.
- Equivalent unresolved-thread evidence: no Pull Request exists, so no Pull Request review thread exists to remain unresolved.
- Equivalent Merge-Ready acceptance: none exists yet because there is no Pull Request or review handoff; this absence is explicitly recorded and is not presented as acceptance.
- Equivalent review-artifact ancestry: no review artifact exists yet. This report binds local QA evidence to audited head `e79d2880` and makes no Merge-Ready claim for target head `cf1ba1b0`.
- Unblocking action: land the Run commit on `feat/0146-analyzer-gate`, open its Pull Request, and observe the live controls there. The supervised and local equivalents record every enumerated control for this pre-Pull-Request gate.
- Status: blocked (environment: no open Pull Request; equivalent evidence recorded).

## Findings

None recorded.

## Blocked and skipped

Row 7 is environment-blocked because no Pull Request exists. Its cause, unblocking action, and equivalent observed or supervised evidence are recorded in the row. No row is finding-blocked, declared-blocked, or skipped.

## Coverage

All 7 rows settled: 6 pass and 1 environment-blocked with equivalent evidence. Coverage sources are Requirements 1-7, the outside-evidence row, the Pull Request row, and repository Verification; each appears in row provenance and no other source is named. Requirement 8 constrains acceptable evidence and is applied to every row; it is not a declared verification source. Counts: `rows_blocked_environment: 1`, `rows_blocked_finding: 0`, `rows_blocked_declared: 0`; skipped: 0.

## Final verdict

Pass. Every runnable row passed, the sole environment-blocked Pull Request row records its cause and the required equivalent-control evidence, and no row failed, remained pending, was finding-blocked, declared-blocked, or skipped.

## Mechanical skips

| Detector | Missing artifact |
| --- | --- |
| consequent-fix commit order | consequent-fix declaration |
| QA evidence paths | docs/specs/0146-a-gate-that-runs-the-analyzer/qa/evidence |
