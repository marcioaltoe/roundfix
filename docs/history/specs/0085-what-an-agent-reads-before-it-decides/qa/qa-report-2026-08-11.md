---
spec: 0085-what-an-agent-reads-before-it-decides
date: 2026-08-11
build: 741875e8ef32f81610ce6c25721bb4e1fc9ced8f
status: closed
verdict: fail
rows_blocked_environment: 0
rows_blocked_finding: 0
rows_blocked_declared: 0
surfaces: [backend, infra, docs]
---

# QA Report

## Scope and environment

Full authored terminal gate for Task 08 at `HEAD`
`741875e8ef32f81610ce6c25721bb4e1fc9ced8f` on the per-Run branch
`roundfix/run-run_20260811T212222Z_d6a1c07a6106a060`. Actors are a Roundfix
operator using the built CLI and an Agent reading repository guidance. The
prompt confirms that no Pull Request is open; this Spec declares no Pull
Request surface or Pull Request-specific journey.

The installed `/Users/marcio/go/bin/roundfix` is an older build at commit
`5a47f385-dirty`, so QA built the current checkout with `-buildvcs=false` into
`/private/tmp/roundfix-spec0085-precheck/roundfix`. The installed binary's
strict Spec check failed on paths moved by this Spec; the current-checkout
binary's check exited 0 with no findings. `SC-VOCABULARY-UNDOCUMENTED` was
explicitly skipped because the TechSpec has no `## Vocabulary Contract`, so
QA-09 retains that manual obligation.

The Task Graph names `task_08` as the unique terminal `qa` node. Its direct
dependencies `task_05`, `task_06`, and `task_07`, and every transitive non-QA
dependency, are `completed`.

The local Secondbrain index and original external source were readable. The
required `qmd query` attempt failed with `SQLITE_CANTOPEN` while opening the
sqlite-vec store, but the precise source named by the Spec was independently
reachable and read directly; QA-08 therefore remained runnable.

## Static gate

- Authoring precondition: `rtk /private/tmp/roundfix-spec0085-precheck/roundfix spec check 0085-what-an-agent-reads-before-it-decides --strict` — passed with no findings; `SC-VOCABULARY-UNDOCUMENTED` skipped and retained as QA-09.
- Authoritative cold gate: `rtk env GOCACHE="$PWD/.gocache" go clean -testcache`
  exited 0, then the full-access `rtk make verify` exited 2 in `go test
  -parallel 16 ./...`. Nineteen top-level tests failed across `internal/cli`,
  `internal/spec`, `internal/specaudit`, `internal/speccheck`, and
  `internal/worktree`; see F-01 and QA-13. The first sandboxed attempt was
  environment-blocked at `api.github.com`, so QA reset the same Makefile-owned
  cache again before this authoritative rerun.

## Mechanical findings

None.

## Results

| # | Story / criterion / sweep | Actor and surface | Status | Evidence |
| - | --- | --- | --- | --- |
| QA-01 | PRD Goal 1 / Task 08: every retired family resolves under the single archive root through the built CLI | Operator / CLI + backend | fail | The real Archive Command moved a Spec to `_archived/specs/` and persisted, but the retired ADR family remains under active `docs/adr/` and `_archived/adr/` does not exist; F-02. |
| QA-02 | PRD Goal 2: retired material leaves active read paths and remains reachable through forward pointers | Agent / docs | fail | ADR-0106 remains in the default ADR read path; 18 active guidance/source files still name the retired per-tree paths; F-02 and F-03. |
| QA-03 | PRD Goal 3: review excludes one archive root with no per-tree filters and preserves non-archive filters | Maintainer / infra | pass | YAML parse found exactly `!**/_archived/**` and zero per-tree filters; the Task 07 commit adds only its root marker comment. |
| QA-04 | PRD Goal 4 / Task 08: real Baseline Command reaches both rendered guides with unconditional consultation | Maintainer / CLI + infra + docs | pass | Disposable clone update changed `skill-dispatch.md`; fresh reads found the authoring trigger there and the unconditional clause in `secondbrain.md`; a second update reported no changes and idempotence verified. |
| QA-05 | PRD Goal 5 / Task 08: every ADR has one lifecycle format; retired records have resolvable forward pointers | Agent / docs | pass | Whole-corpus audit: `adrs=116 standard=116 legacy_body_statuses=0 retired=1 retired_pointer_resolved_or_explicit_none=1`. |
| QA-06 | Task 06 protected-tooling audit: prior authorization, exact bounded paths, and sanctioned regeneration provenance | Maintainer / Git + infra | pass | Authorization `1a31c965` is an ancestor of `27c4eb45`; `diff-tree` shows only canonical clause/binding paths, the Task file, and Makefile-declared derived paths; scratch `make baseline-digests` returned `ok:true, changed:false`; no descendant fix commit touches the canonical paths. |
| QA-07 | Task 07 protected-tooling audit: prior authorization, exact bounded paths, and consequent-fix chronology | Maintainer / Git + infra | pass | Authorization `1a31c965` is an ancestor of `7512a28e`; `diff-tree` contains only `.coderabbit.yaml` and `task_07.md`; no descendant commit touches the config. |
| QA-08 | ADR-0104 outside evidence supports removing retired guidance from the default read path while retaining discoverability | Agent / external docs | pass | Read the original Secondbrain page at `/Users/marcio/dev/secondbrain/wiki/concepts/arquitetura-de-instrucoes-e-progressive-disclosure.md`; it independently identifies stale guidance as dangerous and requires discoverability rather than concealment. |
| QA-09 | Manual vocabulary/glossary check retained because `SC-VOCABULARY-UNDOCUMENTED` was skipped | Agent / docs | pass | `Archive Command` and the affected lifecycle/Secondbrain concepts already exist in `CONTEXT.md`; `ArchiveKind` and `ArchiveDir` are implementation identifiers. No domain term was coined, changed, or dropped. |
| QA-10 | Non-Goals: no archived artifact was deleted and ADR decision text was not re-litigated | Maintainer / Git + docs | pass | Old/new Git tree IDs are identical for Specs (`1781d09e…`) and findings (`d506a2f8…`); normalized exact comparison reported `decision_text_matches_parent=116/116`. |
| QA-11 | Non-Goal: the Secondbrain capture and inbox contract did not change | Agent / docs + infra | pass | Exact Task 06 diff changes only the consultation trigger clause and its module versions/digest; capture and inbox clauses are byte-unchanged. |
| QA-12 | Non-Goal: no Roundfix Run-time command behavior changed | Operator / CLI + backend | pass | Changed-code sweep found no `cmd`, daemon, rounds, agent, preflight, config, or runevent changes; the worktree edit only replaces the archived-QA path literal with the resolver. Built `--help`, `doctor --help`, and `implement --help` remained usable. |
| QA-13 | Task 08 Requirement 10: authoritative gate passes from the Makefile-owned cold cache | Maintainer / repository gate | fail | `rtk make verify` exited 2 with 19 top-level archive-path regression failures; F-01. |

## Detailed evidence

### QA-01

```yaml
inputs:
  - kind: repository_path
    ref: internal/spec/archive.go
  - kind: repository_path
    ref: internal/cli/archive.go
  - kind: repository_path
    ref: _archived/*
```

In a disposable Git clone, an archived passing Spec was moved back to the
active tree as setup. The built command then exited 0 and printed:

```text
archived 0004-watch-merge-readiness -> _archived/specs/0004-watch-merge-readiness
```

A fresh filesystem read found the complete Spec at that destination. A second
invocation exited 2 because the active source no longer existed, confirming
persistence and safe out-of-order behavior. The built `archive --help` also
names `_archived/specs/<slug>/`, and the built strict Spec check passes.
However, the whole-family inspection found 2,142 Spec files and 65 finding
files under the new root, no `_archived/adr/`, no `_archived/backlog/`, and the
one superseded ADR still under `docs/adr/`; see F-02.

### QA-02

```yaml
inputs:
  - kind: repository_path
    ref: docs/adr/*.md
  - kind: repository_path
    ref: _archived/**/*
```

`docs/adr/0106-opencode-reasoning-effort-is-managed-by-the-model.md` carries
`status: superseded` and `superseded_by: ADR-0108` but remains in the active
read path. A production-file sweep also found 18 active guide, skill, catalog,
fixture, and source-baseline files that still name `docs/specs/_archived` or
`docs/findings/_archived`; see F-02 and F-03.

### QA-03

```yaml
inputs:
  - kind: repository_path
    ref: .coderabbit.yaml
  - kind: repository_path
    ref: internal/spec/archive.go
```

`YAML.safe_load` parsed `.coderabbit.yaml`; its archive-filter set is exactly
`["!**/_archived/**"]` and its per-tree set is empty. The exact
`7512a28e^..7512a28e` diff is one added marker comment beside the unchanged
filter, so every non-archive filter is preserved.

### QA-04

```yaml
inputs:
  - kind: repository_path
    ref: internal/baseline/assets/modules/*.json
  - kind: repository_path
    ref: internal/baseline/assets/source-baselines/**
  - kind: repository_path
    ref: docs/agents/secondbrain.md
  - kind: repository_path
    ref: docs/agents/docs-layout.md
```

The built current-checkout binary ran:

```text
roundfix baseline update --repo <disposable-current-repository-clone> --format json --yes --no-skills
```

It exited 0 with `state:"verified"`, updated
`docs/agents/skill-dispatch.md`, and named the already-current Secondbrain
managed region. Fresh reads found
`trigger.context-workflow.knowledge-workspace-secondbrain-authoring` in the
dispatch guide and the unconditional design/authoring clause in the
Secondbrain guide. Repeating the same public command exited 0 with no file
changes and `idempotence:"verified"`.

### QA-05

```yaml
inputs:
  - kind: repository_path
    ref: docs/adr/*.md
```

The fresh 116-file audit required all five lifecycle keys, rejected body-only
status lines, and resolved every non-null retired pointer. It reported:

```text
adrs=116 standard=116 legacy_body_statuses=0 retired=1 retired_pointer_resolved_or_explicit_none=1
```

### QA-06

`git merge-base --is-ancestor 1a31c965 27c4eb45` exited 0. The Task commit's
18 changed paths consist of the assigned Task, authorized clause/binding
sources and guide, and deterministic artifacts under the Makefile's
`DERIVED_DIGEST_PATHS`. In a clean disposable clone, `make baseline-digests`
exited 0 and reported:

```json
{"schemaVersion":1,"type":"baseline-digests","ok":true,"changed":false}
```

No descendant commit through the QA build touches the canonical Task 06
paths, so no folded or late consequent-fix sequence exists.

### QA-07

`git merge-base --is-ancestor 1a31c965 7512a28e` exited 0. Fresh
`git diff-tree --no-commit-id --name-status -r 7512a28e` listed only
`.coderabbit.yaml` and the assigned Task file. No descendant commit through
the QA build touches `.coderabbit.yaml`.

### QA-08

```yaml
inputs:
  - kind: external_repository
    ref: /Users/marcio/dev/secondbrain/wiki/concepts/arquitetura-de-instrucoes-e-progressive-disclosure.md
  - kind: repository_path
    ref: docs/specs/0085-what-an-agent-reads-before-it-decides/references/_index.md
  - kind: repository_path
    ref: _archived/**/*
```

The original external page was read from the Secondbrain repository rather
than from this Spec's adopted copy. Its source list predates this Spec, and its
operative claims are that obsolete guidance can be mistaken for current
instruction and that progressive disclosure must retain discoverability. The
source supports the Spec's premise independently. The general `qmd query`
route was unavailable because its sqlite-vec database could not be opened;
that did not prevent direct observation of the named source.

### QA-09

```yaml
inputs:
  - kind: repository_path
    ref: docs/specs/0085-what-an-agent-reads-before-it-decides/_techspec.md
  - kind: repository_path
    ref: CONTEXT.md
```

Manual comparison against `CONTEXT.md` found the existing `Archive Command`
entry and existing lifecycle and Secondbrain concepts. The Spec introduces no
new ubiquitous-language term: `ArchiveKind` and `ArchiveDir` remain Go
identifiers, and "single archive root" is descriptive. No glossary update is
required, although the missing TechSpec Vocabulary Contract remains why the
automatic detector was skipped.

### QA-10

```yaml
inputs:
  - kind: repository_path
    ref: _archived/**/*
  - kind: repository_path
    ref: docs/adr/*.md
```

Fresh Git tree comparisons proved byte preservation across the move:

```text
Specs:    1781d09ecf5f525fb3d4231fe1d83435ea14c34b == 1781d09ecf5f525fb3d4231fe1d83435ea14c34b
Findings: d506a2f8d845fb3fbabcc0317100ef44b00be3ce == d506a2f8d845fb3fbabcc0317100ef44b00be3ce
```

An exact old/new ADR-body comparison stripped only lifecycle frontmatter and
the migrated legacy `Status:` line, then reported
`decision_text_matches_parent=116/116`.

### QA-11

```yaml
inputs:
  - kind: repository_path
    ref: internal/baseline/assets/modules/secondbrain.json
  - kind: repository_path
    ref: docs/agents/secondbrain.md
```

The exact `27c4eb45^..27c4eb45` diff for the Secondbrain guide and module
changes only `clause.secondbrain.01-consult-triggers` and the module versions;
all capture, inbox, safety, and ingestion clauses are unchanged.

### QA-12

```yaml
inputs:
  - kind: repository_path
    ref: internal/**/*.go
```

The complete code-path diff from `main` contains Baseline artifacts,
archive/spec/check/audit code, and one worktree archived-QA resolver
substitution. No operational Run package or `cmd/` entry point changed.
Built CLI help for the root, Doctor Command, and Implement Command exited 0
and retained their documented public contracts.

## Findings

### F-01 — The archive relocation leaves the authoritative suite red

- Impact: **Blocks-Completion** — the Spec's own required authoritative gate
  cannot pass, so the assembled change cannot reach a passing QA verdict.
- Expected: after clearing the Makefile-owned `.gocache`, `make verify` exits 0.
- Actual: `go test -parallel 16 ./...` failed 19 top-level tests. Failures
  include stale `docs/specs/_archived` expectations in CLI archive output and
  fixtures, archived QA corpus readers that now find no corpus, the resolver
  test itself, Spec audit and checker archive readers, and worktree QA-report
  classification cases.
- Reproduction: `rtk env GOCACHE="$PWD/.gocache" go clean -testcache`, then
  full-access `rtk make verify` at
  `741875e8ef32f81610ce6c25721bb4e1fc9ced8f`.
- Evidence: the gate output names failures from
  `TestRunArchiveMovesCompletedSpecAndStampsMetadata` through
  `TestInspectTerminalRunClassifiesSupersededQAReport`; the complete RTK log is
  `/Users/marcio/Library/Application Support/rtk/tee/1786484861_make_verify.log`.
- Affected row: QA-13. The remaining public and repository inspections do not
  depend on the suite being green and remain runnable.

### F-02 — The retired ADR remains in the active read path

- Impact: **Trust-Damage** — an Agent loading `docs/adr/` still reads the one
  record the repository identifies as superseded, which is the primary hazard
  this Spec promises to remove.
- Expected: retired ADRs live under `_archived/adr/` and the active successor
  remains reachable from the retired record's pointer.
- Actual: `_archived/adr/` is absent, while
  `docs/adr/0106-opencode-reasoning-effort-is-managed-by-the-model.md` remains
  active-path content with `status: superseded` and
  `superseded_by: ADR-0108`.
- Reproduction: enumerate `_archived/*`, then search `docs/adr/*.md` for
  `status: superseded` or `status: deprecated`.
- Affected rows: QA-01 and QA-02.

### F-03 — Active Agent contracts still teach the old archive layout

- Impact: **Trust-Damage** — the built Archive Command and active instructions
  contradict each other, so Agents can read or write history through paths the
  current product no longer owns.
- Expected: every active consumer and managed source names the single
  `_archived/<kind>/` root.
- Actual: a production-file sweep found 18 active files naming
  `docs/specs/_archived` or `docs/findings/_archived`, including
  `docs/agents/docs-layout.md`, both installed archive-spec skills, write
  workflow skills, the shipped Roundfix skill, catalog modules, formatter
  fixtures, and source-baseline corpus files. A real Baseline update leaves
  those docs-layout paths unchanged.
- Reproduction: search active `docs/agents`, `.agents/skills`, `skills`, and
  non-test `internal` files for the two old archive roots.
- Affected row: QA-02.

## Blocked and skipped

No QA row was blocked or skipped. The sandboxed first `make verify` attempt was
superseded by the full-access cold rerun; the `qmd` index query failure was
superseded by directly reading the precise external source.

## Coverage

Thirteen rows closed: 10 passed and 3 failed, with zero pending, blocked, or
skipped rows. All five PRD Goals, all four Non-Goals, the Task 08 Baseline,
archive, ADR, protected-tooling, outside-evidence, glossary, and cold-gate
requirements were exercised. Goal 1 failed on the retired ADR family; Goal 2
failed on the active retired ADR and stale Agent contracts; Goals 3–5 passed.
No frontend surface is declared.

The four pre-materialized mechanical skips were discharged manually:
authorization and bounded paths in QA-06/QA-07, consequent-fix chronology in
those same rows, and report structure/evidence paths in the closing validation.

## Mechanical skips

| Detector | Missing artifact |
| --- | --- |
| authorization bounded paths | Task commits |
| consequent-fix commit order | consequent-fix declaration |
| QA Report structure | QA Report |
| QA evidence paths | QA Report |

## Final verdict

**Fail.** Repair the archive-path fixtures and tests, move the retired ADR out
of the active read path, and update every active guide, skill, catalog source,
fixture, and source-baseline reference to the single root before rerunning the
gate. Counts: 10 pass, 3 fail, 0 environment-blocked, 0 finding-blocked,
0 declared-blocked, 0 skipped; findings: 1 Blocks-Completion and 2 Trust-Damage.
