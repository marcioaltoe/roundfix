---
spec: 0085-what-an-agent-reads-before-it-decides
date: 2026-08-11
build: 73798cbbd12f7939202d13bd156c999f15caf2fb
status: closed
verdict: pass
rows_blocked_environment: 0
rows_blocked_finding: 0
rows_blocked_declared: 0
surfaces: [backend, infra, docs]
---

# QA Report

## Scope and environment

Full rerun of the authored terminal gate for Task 08 at `HEAD`
`73798cbbd12f7939202d13bd156c999f15caf2fb` on the per-Run branch
`roundfix/run-run_20260812T004625Z_26e410781d1d4a99`. The prompt confirms that
no Pull Request is open; this Spec declares no Pull Request surface or
Pull Request-specific journey. Actors are a Roundfix operator using a
current-checkout binary built with `-buildvcs=false`, an Agent reading active
guidance, and a maintainer auditing Git provenance.

The machine-readable Task Graph names `task_08` as the unique terminal `qa`
node and makes it depend on completed Tasks 05, 06, 07, and 09. Every
transitive non-QA dependency is also `completed`. The PRD declares no
Unreachable Acceptance.

Disposable clones under `/private/tmp/roundfix-spec0085-{archive,baseline,regen}-qa02.*`
isolated the real Archive and Baseline Command flows and the regeneration
audit. Initial host-cache and network denials were environment constraints;
each authoritative check was rerun with a writable cache or authorized network
access before its row received a verdict. The Secondbrain index and named
outside source were readable; `qmd query` failed with `SQLITE_CANTOPEN`, but
direct observation of that precise source kept QA-08 runnable.

## Static gate

- Current-checkout build: the first host-cache attempt was environment-blocked
  by `~/Library/Caches/go-build: operation not permitted`; the same `go build
  -buildvcs=false` command with `GOCACHE` under `/private/tmp` exited 0 and
  produced `/private/tmp/roundfix-spec0085-qa02`.
- Authoring precondition: `rtk /private/tmp/roundfix-spec0085-qa02 spec check
  0085-what-an-agent-reads-before-it-decides --strict` exited 0 with no
  findings. It explicitly skipped `SC-VOCABULARY-UNDOCUMENTED` because the
  TechSpec has no `## Vocabulary Contract`, so QA-09 retains that manual row.
- Authoritative cold gate: after `rtk env GOCACHE="$PWD/.gocache" go clean
  -testcache`, the sandboxed `rtk make verify` reached the environment boundary
  at `api.github.com`. QA reset the same cache and reran the exact command with
  network access; it exited 0. Every Go package passed uncached, the focused
  Skill contract test passed, `roundfix skills check` passed, and the current
  binary built successfully.

## Mechanical findings

None.

## Results

| # | Story / criterion / sweep | Actor and surface | Status | Evidence |
| - | --- | --- | --- | --- |
| QA-01 | PRD Goal 1 / Task 08: every retired family resolves under the single archive root through the built CLI | Operator / CLI + backend | pass | In a disposable clone, the current binary archived active Spec `0004-watch-merge-readiness` to `_archived/specs/`; a fresh read found stamped content there and a second invocation safely refused with exit 2. The strict built checker passed, the four-kind resolver test passed, and the current root contains Spec, finding, and ADR families; backlog has no retired instance to materialize an empty directory. |
| QA-02 | PRD Goal 2: retired material leaves active read paths and remains reachable through forward pointers | Agent / docs | pass | No retired status remains under `docs/adr/`, the one retired ADR lives under `_archived/adr/` with `superseded_by: ADR-0108`, and its accepted target resolves under `docs/adr/`. A fresh active-source sweep found no old per-tree Spec or finding archive path. |
| QA-03 | PRD Goal 3: review excludes one archive root with no per-tree filters and preserves non-archive filters | Maintainer / infra | pass | YAML parsing found exactly `!**/_archived/**`, zero per-tree filters, and 106 total filters. Task 07 added only the resolver-linked root marker comment, and no descendant commit changes `.coderabbit.yaml`. |
| QA-04 | PRD Goal 4 / Task 08: real Baseline Command reaches both rendered guides with unconditional consultation | Maintainer / CLI + infra + docs | pass | A current built binary applied `baseline update` to a disposable clone, refreshed the setup manifest and authoring trigger, and verified the already-current Secondbrain/docs-layout managed regions. Fresh reads found the unconditional consultation clause and `_archived/{specs,findings}` paths; a second update returned no changes and `idempotence:"verified"`. |
| QA-05 | PRD Goal 5 / Task 08: every ADR has one lifecycle format; retired records have resolvable forward pointers | Agent / docs | pass | Whole-corpus audit reported `adrs=116 active=115 retired=1 legacy_body_statuses=0 violations=0 unresolved_pointers=0`; every active and retired ADR carries the five lifecycle fields. |
| QA-06 | Task 06 protected-tooling audit: prior authorization, exact bounded paths, and sanctioned regeneration provenance | Maintainer / Git + infra | pass | Authorization `1a31c965` is an ancestor of `27c4eb45`; fresh `diff-tree` lists the assigned Task, authorized consultation sources, rendered guide, and Makefile-declared deterministic fallout. Clean-clone `make baseline-digests` returned `changed:false`. |
| QA-07 | Task 07 protected-tooling audit: prior authorization, exact bounded paths, and consequent-fix chronology | Maintainer / Git + infra | pass | Authorization `1a31c965` is an ancestor of `7512a28e`; `diff-tree` contains only `.coderabbit.yaml` and `task_07.md`, and no descendant commit touches the configuration. |
| QA-08 | ADR-0104 outside evidence supports removing retired guidance from the default read path while retaining discoverability | Agent / external docs | pass | The current Secondbrain index points to the independently authored page `wiki/concepts/arquitetura-de-instrucoes-e-progressive-disclosure.md`; direct observation found its stale-guidance risk at line 40 and discoverability rule at line 72. The required `qmd query` route failed with `SQLITE_CANTOPEN`, but the precise source remained directly observable. |
| QA-09 | Manual vocabulary/glossary check retained when the strict checker does not supply an equivalent | Agent / docs | pass | `CONTEXT.md` already defines Archive Command, Baseline Command, lifecycle, Managed Region, and Secondbrain concepts. `ArchiveKind` and `ArchiveDir` are implementation identifiers and “single archive root” is a layout description; the Spec coins, changes, and drops no ubiquitous-language term. |
| QA-10 | Non-Goals: no archived artifact was deleted and ADR decision text was not re-litigated | Maintainer / Git + docs | pass | Exact Git object checks match pre/post tree IDs for archived Specs (`1781d09e...`) and findings (`d506a2f8...`); ADR-0106 remains a 100% rename with blob `05f3daa5...`. A normalized whole-corpus comparison reported `decision_text_matches_parent=116/116`. |
| QA-11 | Non-Goal: the Secondbrain capture and inbox contract did not change | Agent / docs + infra | pass | Fresh reads retain the capture, inbox, safety, and ingestion clauses. Task 06’s exact canonical diff changes only the consultation trigger plus version metadata, and no descendant commit changes the Secondbrain guide or module. |
| QA-12 | Non-Goal: no Roundfix Run-time command behavior changed | Operator / CLI + backend | pass | The complete `main..HEAD` diff contains no `cmd`, agent, daemon, rounds, runevent, preflight, or config change. Built root, Doctor, and Implement help exited 0, and the previously flaky public Implement integration journey passed 10/10 in 7.735s. |
| QA-13 | Task 08 Requirement 10: authoritative gate passes from the Makefile-owned cold cache | Maintainer / repository gate | pass | The full-access `rtk make verify` rerun after clearing `$PWD/.gocache` exited 0; `internal/cli` passed in 120.122s and all other packages, Skill checks, and the current binary build passed uncached. |
| QA-14 | Task 09 protected-tooling audit: authorization and Task boundary predate the effective change, paths are bounded, and regeneration is reproducible | Maintainer / Git + infra | pass | The three prematurely recorded edits were exactly unwound by `c3de2f55`; authorization clarification `ca998a1c` and Task-boundary commit `da6a47fd` are ancestors of replay `5296bc9b`, whose `diff-tree` contains only those three paths. The replay is byte-identical to the original intended postimages, has no later mutation, and clean-clone digest regeneration plus Skill sync checks pass without changes. |

## Detailed evidence

### QA-01

```yaml
inputs:
  - kind: repository_path
    ref: internal/spec/archive.go
  - kind: repository_path
    ref: internal/cli/archive.go
  - kind: repository_path
    ref: _archived/*
```

The built help names `_archived/specs/<slug>/`. In a disposable clone, the
current binary printed:

```text
archived 0004-watch-merge-readiness -> _archived/specs/0004-watch-merge-readiness
```

A fresh filesystem read found the complete, stamped Spec there. Repeating the
command returned exit 2 because the active source no longer existed and named
that no Run, Agent, commit, or push occurred. `TestArchiveDirAnswersEveryRetiredKind`
passed for Specs, findings, ADRs, and backlog entries. The repository contains
retired Spec, finding, and ADR instances; no retired backlog entry exists, so
Git has no empty `_archived/backlog/` directory to retain.

### QA-02

```yaml
inputs:
  - kind: repository_path
    ref: docs/adr/*.md
  - kind: repository_path
    ref: _archived/adr/*.md
  - kind: repository_path
    ref: docs/agents/**
  - kind: repository_path
    ref: .agents/skills/**
  - kind: repository_path
    ref: skills/**
```

The active ADR tree contains no `superseded`, `deprecated`, or `rejected`
record. `_archived/adr/0106-opencode-reasoning-effort-is-managed-by-the-model.md`
is `superseded`, names `ADR-0108`, and the accepted target resolves under
`docs/adr/`. A fresh active-source search for `docs/specs/_archived` or
`docs/findings/_archived` returned no matches.

### QA-03

```yaml
inputs:
  - kind: repository_path
    ref: .coderabbit.yaml
  - kind: repository_path
    ref: internal/spec/archive.go
```

`YAML.safe_load` reported:

```text
archive_filters=["!**/_archived/**"] per_tree=0 total_filters=106
```

The exact Task 07 diff is one added marker comment beside the unchanged root
filter. The descendant path log through this build is empty.

### QA-04

```yaml
inputs:
  - kind: repository_path
    ref: internal/baseline/assets/modules/*.json
  - kind: repository_path
    ref: internal/baseline/assets/source-baselines/**
  - kind: repository_path
    ref: docs/agents/secondbrain.md
  - kind: repository_path
    ref: docs/agents/docs-layout.md
```

The current binary ran `baseline update --format json --yes --no-skills`
against a disposable current-head clone. The first run returned `ok:true` and
`state:"verified"`, refreshed `setup-context.json` and `skill-dispatch.md`, and
verified the already-current Secondbrain and docs-layout managed regions.
Fresh reads found the unconditional consultation clause, its Idea/PRD/TechSpec
authoring trigger, and the `_archived/findings/` and `_archived/specs/` paths.
The second public update returned `fileChanges:[]` and
`idempotence:"verified"`.

### QA-05

```yaml
inputs:
  - kind: repository_path
    ref: docs/adr/*.md
  - kind: repository_path
    ref: _archived/adr/*.md
```

A fresh 116-file audit required `status`, `created_at`, `updated_at`,
`deprecated_at`, and `superseded_by`; rejected body-only status lines; rejected
retired records in the active tree; and resolved every non-null forward
pointer:

```text
adrs=116 active=115 retired=1 legacy_body_statuses=0 violations=0 unresolved_pointers=0
```

### QA-06

```yaml
inputs:
  - kind: repository_path
    ref: docs/workflow/authorizations/2026-08-09-what-an-agent-reads-before-it-decides.md
  - kind: repository_path
    ref: docs/specs/0085-what-an-agent-reads-before-it-decides/task_06.md
  - kind: repository_path
    ref: internal/baseline/assets/modules/secondbrain.json
  - kind: repository_path
    ref: internal/baseline/assets/modules/context-workflow.json
```

`git merge-base --is-ancestor 1a31c965 27c4eb45` exited 0. Fresh
`git diff-tree --no-commit-id --name-status -r 27c4eb45` lists the Task,
authorized canonical sources and rendered guide, plus files under the
Makefile's declared Baseline regeneration paths. A clean-clone
`make baseline-digests` returned `ok:true, changed:false`.

### QA-07

```yaml
inputs:
  - kind: repository_path
    ref: docs/workflow/authorizations/2026-08-09-what-an-agent-reads-before-it-decides.md
  - kind: repository_path
    ref: docs/specs/0085-what-an-agent-reads-before-it-decides/task_07.md
  - kind: repository_path
    ref: .coderabbit.yaml
```

`git merge-base --is-ancestor 1a31c965 7512a28e` exited 0. The Task commit's
`diff-tree` contains only `.coderabbit.yaml` and `task_07.md`; no descendant
commit changes `.coderabbit.yaml`, so there is no folded or late consequent
fix.

### QA-08

```yaml
inputs:
  - kind: external_repository
    ref: /Users/marcio/dev/secondbrain/wiki/concepts/arquitetura-de-instrucoes-e-progressive-disclosure.md
  - kind: repository_path
    ref: docs/specs/0085-what-an-agent-reads-before-it-decides/references/_index.md
```

The current Secondbrain index links the named page. The page predates this
Spec, identifies the risk that stale guidance can be interpreted as current
instruction at line 40, and separately requires retained discoverability at
line 72. The required `qmd query` attempt failed with `SQLITE_CANTOPEN`; direct
observation of the exact external source supplied the evidence instead of a
Spec-authored rehearsal.

### QA-09

```yaml
inputs:
  - kind: repository_path
    ref: docs/specs/0085-what-an-agent-reads-before-it-decides/_techspec.md
  - kind: repository_path
    ref: CONTEXT.md
```

`CONTEXT.md` already defines Archive Command, Baseline Command, the relevant
lifecycle concepts, Managed Region, and Secondbrain capture vocabulary.
`ArchiveKind` and `ArchiveDir` remain implementation identifiers; the archive
root is a layout. No glossary term was coined, changed, or dropped.

### QA-10

```yaml
inputs:
  - kind: repository_path
    ref: _archived/**/*
  - kind: repository_path
    ref: docs/adr/*.md
```

Exact Git object comparisons prove byte preservation across the moves:

```text
Specs:    1781d09ecf5f525fb3d4231fe1d83435ea14c34b == 1781d09ecf5f525fb3d4231fe1d83435ea14c34b
Findings: d506a2f8d845fb3fbabcc0317100ef44b00be3ce == d506a2f8d845fb3fbabcc0317100ef44b00be3ce
ADR-0106: 05f3daa511c7677c18fc05228cdf29e38b2788be == 05f3daa511c7677c18fc05228cdf29e38b2788be
```

Git identifies ADR-0106 as a 100% rename. A whole-corpus comparison stripped
only lifecycle frontmatter and the migrated legacy `Status:` line before an
exact byte comparison and reported `decision_text_matches_parent=116/116`.

### QA-11

```yaml
inputs:
  - kind: repository_path
    ref: internal/baseline/assets/modules/secondbrain.json
  - kind: repository_path
    ref: docs/agents/secondbrain.md
```

The exact Task 06 canonical diff changes only the consultation trigger and
version metadata. Current files retain every capture, inbox, safety, and
ingestion clause, and `git diff 27c4eb45..HEAD` is empty for both inputs.

### QA-12

```yaml
inputs:
  - kind: repository_path
    ref: cmd/**/*.go
  - kind: repository_path
    ref: internal/{agent,daemon,rounds,runevent,preflight,config}/**/*.go
  - kind: repository_path
    ref: internal/cli/implement_test.go
```

The complete changed-path query from `main` returned no file in the declared
Run-time packages or `cmd/`. Built root, Doctor, and Implement help each exited
0. After the sandboxed attempt reached `api.github.com`, the full-access focused
canary `TestRunImplementVerificationCapacityAndDaemonStatusIntegratedFlow`
passed 10/10 in 7.735s.

### QA-13

```yaml
inputs:
  - kind: repository_path
    ref: Makefile
  - kind: repository_path
    ref: internal/**/*.go
  - kind: repository_path
    ref: skills/**
```

The Makefile exports `GOCACHE ?= $(CURDIR)/.gocache`. QA cleared that exact
cache before both attempts. The sandboxed attempt reached `api.github.com`;
the full-access rerun exited 0 with every package uncached. `internal/cli`
passed in 120.122s, the Skill contract checks passed, `roundfix skills check`
passed, and the current binary built.

### QA-14

```yaml
inputs:
  - kind: repository_path
    ref: docs/workflow/authorizations/2026-08-09-what-an-agent-reads-before-it-decides.md
  - kind: repository_path
    ref: docs/specs/0085-what-an-agent-reads-before-it-decides/task_09.md
  - kind: repository_path
    ref: internal/baseline/assets/modules/context-workflow.json
  - kind: repository_path
    ref: skills/baseline_skill_contract_test.go
  - kind: repository_path
    ref: skills/owned_skill_edit_repocontract_test.go
```

The first QA rerun found these three edits preceding their required records.
Commit `c3de2f55` exactly restored all three paths to their pre-Task blobs.
Authorization clarification `ca998a1c` and Task-boundary commit `da6a47fd`
then landed before replay `5296bc9b`; both ancestry checks exited 0. The replay
changes only those three paths, recreates the intended postimages byte-for-byte,
and has no later descendant mutation. The original Task 09 paths remain bounded
by prior authorization `e93b103f` and Task record `5c1e5034`. In a clean clone,
`make baseline-digests` returned `changed:false`, `make skills-sync-check`
passed, and Git status remained clean.

## Findings

None.

## Blocked and skipped

No QA row was blocked or skipped. Initial sandbox denials were superseded by
successful equivalent authoritative reruns. The failed qmd index query did not
block direct observation of the precise named external source.

## Coverage

Fourteen rows closed and passed, with zero pending, failed, blocked, or skipped
rows. All five PRD Goals, all four Non-Goals, the real Archive and Baseline
Commands, the complete ADR corpus, all three protected-tooling histories,
outside evidence, glossary obligation, and the genuinely cold authoritative
gate were exercised. The prior full-gate timeout and Task 09 chronology
findings are repaired. No frontend surface is declared.

The two pre-materialized mechanical skips were discharged manually across
QA-06, QA-07, and QA-14: bounded paths were compared with complete Task commit
`diff-tree` output, and authorization, revert, Task-boundary, replay,
regeneration, and consequent-fix chronology were checked through the current
head.

## Mechanical skips

| Detector | Missing artifact |
| --- | --- |
| authorization bounded paths | Task commits |
| consequent-fix commit order | consequent-fix declaration |

## Final verdict

**Pass.** All 14 rows pass with fresh current-build evidence; there are 0
environment-blocked, 0 finding-blocked, 0 declared-blocked, 0 failed, and 0
skipped rows, and no findings by impact tier.
