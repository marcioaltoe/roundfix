---
spec: 0154-evidence-when-the-target-branch-is-gone
date: 2026-09-23
build: e9f603df910badf75539713e34eb947cf784c965
auditing_binary: "0.16.0 (386a90f7, built 2026-09-23 15:39:14 -0300)"
auditor_staleness: "stale: commit ancestry: build commit predates audited tree"
status: closed
verdict: pass
rows_blocked_environment: 1
rows_blocked_finding: 0
rows_blocked_declared: 0
surfaces: [backend, cli, docs]
---

# QA Report

## Scope and environment

- Scope: full authored terminal gate for Task 04 on build
  `e9f603df910badf75539713e34eb947cf784c965`.
- Actor: a local Roundfix operator inspecting retained terminal Runs.
- Entry points: a binary built from the audited worktree, the public
  `roundfix reconcile` CLI, local Git reads, the shipped Roundfix skill, and
  the daemon-owned repository Verification fact.
- Authoring precondition: `roundfix spec check
  0154-evidence-when-the-target-branch-is-gone --strict` exited 0 with no
  findings. Authored Verification commands were not executed by that check.
- Pull Request: none open; Pull Request journeys are environment-blocked.
- Repository Verification: daemon-owned; the supplied result is `make verify`
  exit 0 with diagnostics removed on success. This gate will not rerun it.
- Parity: CLI journeys used `/private/tmp/roundfix-qa-0154-02`, built from the
  audited worktree, against both a scratch local Git repository and real
  retained Runs in `/Users/marcio/dev/roundfix`. The scratch fixture used a
  temporary home and Run Database; no network or credential was involved.

## Performed repairs

None.

## Assigned repair failures

None.

## Authorization audit inputs

None.

## Mechanical findings

None.

## Static gate

- `make verify` — pass (exit 0), run once by the Daemon outside the Agent
  sandbox; diagnostics were removed on success. Per the QA contract, the Agent
  did not rerun it.
- `roundfix spec check 0154-evidence-when-the-target-branch-is-gone --strict`
  — pass (exit 0), no findings. The checker disclosed four skipped detectors:
  no findings rollup, no backlog, no TechSpec Vocabulary Contract, and no
  `references/_index.md`.

## Execution log

- `GOCACHE=/private/tmp/roundfix-qa-0154-02-gocache go build -o
  /private/tmp/roundfix-qa-0154-02 ./cmd/roundfix` — pass (exit 0).
- `/private/tmp/roundfix-qa-0154-02 --version` — pass (exit 0), reported
  `roundfix 0.16.0`.
- Two fresh CLI dry runs against scratch Run
  `run_20260923T193442Z_87da2c6b0955b268` — pass (exit 0); both emitted one
  Run Branch candidate backed by the archived report and said
  `would reclaim with --apply` after the target ref was deleted.
- Two CLI dry runs against real retained Runs
  `run_20260921T160122Z_07824e2f31e39776` and
  `run_20260921T110125Z_a0e8bf6b87cd6066` — pass (exit 0); the first remained
  unintegrated and the second dirty, and both were preserved.
- A CLI dry run against scratch Run
  `run_20260923T193900Z_0ad7f114857a29e7`, whose default branch carried only
  archived evidence for Spec `0099-another-spec` — pass (exit 0); it emitted
  no releasable Run Branch and preserved the Run under its own Spec slug.
- `GOCACHE=/private/tmp/roundfix-qa-0154-02-gocache go test -count=1 -v
  ./internal/worktree -run
  'Test(Reconcile(FallsBackToTheDefaultBranch|FallbackRequiresArchivedEvidence|PresentTargetAcceptsEitherSpecPath|PreservesWithoutDefaultBranchEvidence|ReasonNamesTheMissingProof|ReasonKeepsProofClauseForLongSlugs)|InspectTerminalRun(Safe|Unintegrated|ClassifiesSupersededQAReport|Dirty|RequiresArchivedEvidence)|PruneTerminal(ReportRequiresArchivedEvidence|ReconciliationReachableChangedBranch)|ClassifyRunBranchSet(FourFailedCycles|PreservesUnintegratedImplementationWork|PreservesBranchWithoutSupersededProof))$'`
  — pass (exit 0).
- `/private/tmp/roundfix-qa-0154-02 skills check` — pass (exit 0).
- `cmp -s .agents/skills/roundfix/SKILL.md
  skills/roundfix/SKILL.md` — pass (exit 0).
- Read-only `git diff-tree`, `merge-base`, `show-ref`, `ls-tree`, branch, and
  status reads produced the authority, outside-evidence, target-absence,
  dirty-state, and persistence observations recorded below.

## Results

| # | Status | Provenance |
| - | --- | --- |
| R1 | pass | Requirement 1; repository Verification |
| R2 | pass | Requirement 2 |
| R3 | pass | Requirement 3 |
| R4 | pass | Requirement 4 |
| R5 | pass | Requirement 5 |
| R6 | pass | Requirement 6 |
| R7 | pass | Requirement 7 |
| R8 | pass | Requirement 7 |
| R9 | pass | Requirement 8 |
| R10 | pass | outside-evidence row |
| R11 | blocked (environment: no open Pull Request) | Pull Request row |

## Row evidence

### R1

```yaml
inputs:
  - kind: repository_path
    ref: "**"
```

- Actor and surface: daemon; repository gate.
- Entry point and steps: run `make verify` once for the audited tree.
- Expected observable: exit 0 and no retained success diagnostics.
- Independent confirmation: daemon settlement reports the exit code and
  diagnostic disposition.
- Persistence check: not applicable to a static gate.
- Evidence path: daemon-owned gate fact in this report.
- Result: pass. The supplied daemon fact records the exact command, exit 0,
  and successful diagnostic disposition for this audited tree.

### R2

```yaml
inputs:
  - kind: repository_path
    ref: internal/worktree/worktree.go
  - kind: repository_path
    ref: internal/worktree/worktree_test.go
  - kind: repository_path
    ref: internal/cli/reconcile.go
```

- Actor and surface: local operator; CLI backed by the Worktree reconciler.
- Entry point and steps: exercise a squash-merged Run whose target was deleted
  and whose default branch carries archived-path content evidence.
- Expected observable: the Run is releasable and names the archived report.
- Independent confirmation: inspect the fixture Git graph and exact result.
- Persistence check: repeat from a fresh process.
- Evidence path: focused command output recorded in this report.
- Result: pass. Two fresh processes running the built public CLI against a
  scratch Run emitted one `runBranchCandidate` for
  `roundfix/run-run_20260923T193442Z_87da2c6b0955b268`, named
  `docs/history/specs/0066-run-teardown-reclaims-what-it-created/qa/qa-report-2026-07-29.md`
  as `supersedingReport`, and reported `would reclaim with --apply`. A fresh
  ref lookup proved `feat/deleted-target` absent. The focused suite also passed
  the archived-evidence release cases for both branch-set and direct-inspection
  paths.

### R3

```yaml
inputs:
  - kind: repository_path
    ref: internal/worktree/worktree.go
  - kind: repository_path
    ref: internal/worktree/worktree_test.go
  - kind: repository_path
    ref: internal/cli/reconcile.go
```

- Actor and surface: local operator; CLI backed by the Worktree reconciler.
- Entry point and steps: repeat the deleted-target journey with no archived
  evidence, active-path-only evidence, an unresolvable default branch, and
  evidence for another Spec; exercise direct inspection and prune paths too.
- Expected observable: every case stays preserved.
- Independent confirmation: inspect exact classifications and retained state.
- Persistence check: repeat from a fresh process or fixture.
- Evidence path: focused command output recorded in this report.
- Result: pass. The built CLI preserved real retained Run
  `run_20260921T160122Z_07824e2f31e39776` and named that its default branch had
  no superseding QA Report for Spec `0152-one-declared-acceptance-policy`. The
  focused suite passed missing evidence, active-path-only evidence,
  unresolvable default branch, evidence that cannot prove the Run's own Spec,
  direct-inspection preservation, and prune preservation. Fresh fixture and
  real-repository reads confirmed the candidate branches and Worktrees remain.
  A separate public-CLI fixture whose default branch carried only archived
  evidence for `0099-another-spec` also emitted no releasable candidate and
  preserved the Run with its own missing-report reason.

### R4

```yaml
inputs:
  - kind: repository_path
    ref: internal/worktree/worktree.go
  - kind: repository_path
    ref: internal/worktree/worktree_test.go
```

- Actor and surface: local operator; CLI and local Worktree state.
- Entry point and steps: make an evidenced deleted-target Run dirty and
  attempt the public apply flow.
- Expected observable: apply refuses release and both Run surfaces remain.
- Independent confirmation: read Git status, branch, and Worktree state.
- Persistence check: confirm retained state after the command.
- Evidence path: focused command output recorded in this report.
- Result: pass. The built CLI classified real retained Run
  `run_20260921T110125Z_a0e8bf6b87cd6066` as `dirty`, reported one preserved
  candidate, and emitted no releasable Run Branch. Fresh reads found the Run
  Branch at `1551b11f`, its Worktree still registered, and its tracked or
  untracked changes. The focused positive fixture also passed its apply-time
  dirty recheck and retained-branch and retained-Worktree assertions.

### R5

```yaml
inputs:
  - kind: repository_path
    ref: internal/worktree/worktree.go
  - kind: repository_path
    ref: internal/worktree/worktree_test.go
```

- Actor and surface: local operator; CLI backed by the Worktree reconciler.
- Entry point and steps: exercise safe, superseded, unintegrated, and dirty
  classifications on every cleanup path while the target exists.
- Expected observable: every established classification remains unchanged.
- Independent confirmation: exact compatibility assertions.
- Persistence check: repeat against fresh fixtures.
- Evidence path: focused command output recorded in this report.
- Result: pass. The focused suite passed the established safe, superseded,
  unintegrated, and dirty classifications; both active- and archived-path
  present-target cases; and the present-target direct-inspection and prune
  compatibility checks. Each ran against a fresh fixture.

### R6

```yaml
inputs:
  - kind: repository_path
    ref: internal/worktree/worktree.go
  - kind: repository_path
    ref: internal/worktree/worktree_test.go
```

- Actor and surface: local operator; CLI reason output.
- Entry point and steps: inspect every missing-proof reason, including a Spec
  slug long enough to hit the existing byte bound.
- Expected observable: each reason names the missing proof; the long-slug case
  keeps the proof clause and every reason remains within the bound.
- Independent confirmation: exact-fragment and byte-length assertions.
- Persistence check: rerun classification from fresh fixtures.
- Evidence path: focused command output recorded in this report.
- Result: pass. The public CLI emitted the Spec slug and missing superseding
  QA Report for the real retained Run. The focused suite passed the distinct
  missing-evidence and unresolved-default reasons, the active-only
  `Spec is not archived` reason, and the long-slug case that retains both
  `default branch` and `Spec is not archived` within 160 bytes.

### R7

```yaml
inputs:
  - kind: repository_path
    ref: .agents/skills/roundfix/SKILL.md
  - kind: repository_path
    ref: skills/roundfix/SKILL.md
```

- Actor and surface: Roundfix skill reader; docs.
- Entry point and steps: follow the shipped reconciliation guidance and
  compare its canonical and distributed copies.
- Expected observable: both describe the fallback, archived positive proof,
  preservation cases, and missing-proof reason; the copies match.
- Independent confirmation: public skill checker and byte comparison.
- Persistence check: reload both files after the check.
- Evidence path: focused command output recorded in this report.
- Result: pass. The built public skill checker accepted every shipped skill,
  the copies were byte-identical, and fresh reads at lines 818-824 of each copy
  found the default-branch fallback, positive archived evidence rule,
  unreachable-default, unarchived-Spec and other-Spec preservation cases, and
  the missing-proof reason contract.

### R8

```yaml
inputs:
  - kind: repository_path
    ref: docs/specs/0154-evidence-when-the-target-branch-is-gone/_authorization.md
  - kind: repository_path
    ref: .agents/skills/roundfix/SKILL.md
  - kind: repository_path
    ref: skills/roundfix/SKILL.md
```

- Actor and surface: maintainer; Git history and tooling authority.
- Entry point and steps: read each Task commit's actual paths with
  `git diff-tree` and compare governed paths and chronology with the grant.
- Expected observable: governed mutations stay within approved paths plus the
  assigned Task file, and authorization predates implementation.
- Independent confirmation: inspect authorization revision and ancestry.
- Persistence check: repeat the reads from audited HEAD.
- Evidence path: focused Git output recorded in this report.
- Result: pass. `git diff-tree --no-commit-id --name-only -r` showed Task 01
  (`325f6b21`), Task 02 (`32d7e849`), Task 05 (`f12fa7ad`), and Task 06
  (`e9f603df`) changed only their assigned Task file and the two ordinary
  Worktree Go files. Task 03 (`0d156154`) changed only its assigned Task file
  and the two approved skill paths. Authorization commit `891cd45f` changed
  only `_authorization.md`, is an ancestor of every Task commit, and grants the
  skill paths. `internal/speccheck/governed.go` identifies those paths as
  plugin declarations. No prerequisite- or consequent-fix commit exists.

### R9

```yaml
inputs:
  - kind: repository_path
    ref: docs/specs/0154-evidence-when-the-target-branch-is-gone/_prd.md
  - kind: repository_path
    ref: docs/specs/0154-evidence-when-the-target-branch-is-gone/_techspec.md
  - kind: repository_path
    ref: docs/specs/0154-evidence-when-the-target-branch-is-gone/_tasks.md
  - kind: repository_path
    ref: docs/specs/0154-evidence-when-the-target-branch-is-gone/task_04.md
```

- Actor and surface: Spec author; public `roundfix spec check` command.
- Entry point and steps: run the strict consistency check and inspect the
  Task 04 references that consume this Spec's promises.
- Expected observable: no promise-rule finding.
- Independent confirmation: inspect the explicit consuming references.
- Persistence check: rerun reads against audited HEAD.
- Evidence path: strict-check output recorded in this report.
- Result: pass. The strict checker exited 0 with no findings, and Task 04's
  `## References` explicitly names PRD Goals 1-3, Core Features 1-4, Success
  Metrics 1-3, Acceptance evidence, TechSpec Testing Approach 1-6, and API
  Contracts 1-3 as consuming coverage.

### R10

```yaml
inputs:
  - kind: external_repository
    ref: /Users/marcio/dev/roundfix
```

- Actor and surface: maintainer; external user checkout and its Git history.
- Entry point and steps: identify a real squash-merged Run Branch that is not
  an ancestor of the default branch while archived Spec evidence exists there.
- Expected observable: independent history demonstrates why content evidence
  is required after the target disappears.
- Independent confirmation: compare refs, ancestry, and archived content.
- Persistence check: repeat the read-only Git queries.
- Evidence path: focused Git output recorded in this report.
- Result: pass. Source repository `/Users/marcio/dev/roundfix`, which this Spec
  did not build: Run Branch
  `roundfix/run-run_20260921T160122Z_07824e2f31e39776` resolved to
  `eb60e6dbeeb42c2bb6021b08f5fc50ddfe82f047`; `main` resolved to
  `35fb7839338b6a9e3210332025a1cae0f9ebbced`; the target ref lookup exited 128
  because the ref is absent; and `git merge-base --is-ancestor <run-branch>
  main` exited 1. A fresh `git ls-tree` found three archived QA Reports for
  Spec 0152 on `main`, last changed by archive commit `f3931f68`. This external
  history independently demonstrates the squash/deleted-target condition in
  which ancestry cannot establish delivery and archived content evidence is
  required.

### R11

```yaml
inputs:
  - kind: live_service
    ref: Pull Request state for feat/0154-evidence-when-the-branch-is-gone
```

- Actor and surface: reviewer; Pull Request controls.
- Entry point and steps: observe approval, checks/status, unresolved threads,
  Merge-Ready acceptance, and review-artifact ancestry.
- Expected observable: all five controls are evidenced.
- Independent confirmation: compare each artifact with the claimed head.
- Persistence check: fresh Pull Request read when available.
- Evidence path: supplied environment fact and equivalent evidence recorded in
  this report.
- Result: blocked (environment: no open Pull Request), with equivalent evidence
  for every control:
  - approval: no acceptance was obtained because no Pull Request exists;
  - checks and status: daemon-owned `make verify` exited 0;
  - unresolved review threads: no Pull Request exists, so no thread set exists
    to leave unresolved;
  - Merge-Ready acceptance: no Review Source evidence exists yet;
  - review-artifact ancestry: no review artifact or Pull Request head exists,
    so no ancestry claim was made.
  The unblocking action is to open the Pull Request on
  `feat/0154-evidence-when-the-branch-is-gone` after this gate settles, then
  observe all five controls against the Pull Request's actual head.

## Findings

None.

## Blocked and skipped

- R11 is environment-blocked because the QA prompt proves that no Pull Request
  is open. All five Pull Request controls carry explicit equivalent evidence
  above. No row is finding-blocked, declared-blocked, or skipped.

## Coverage

Complete: 10 rows passed and 1 row is environment-blocked with equivalent
evidence. All eight declared verification Requirements and the mandatory
outside-evidence, Pull Request, and repository Verification sources appear in
provenance; no row covers another source. Requirement 9 constrains every row's
evidence and is not a declared verification source. The PRD declares no
Unreachable Acceptance and no frontend surface. Counts:
`rows_blocked_environment: 1`, `rows_blocked_finding: 0`,
`rows_blocked_declared: 0`; failed: 0; skipped: 0.

## Mechanical skips

| Detector | Missing artifact |
| --- | --- |
| authorization bounded paths | Task commits |
| consequent-fix commit order | consequent-fix declaration |
| QA evidence paths | docs/specs/0154-evidence-when-the-target-branch-is-gone/qa/evidence |

## Final verdict

Pass. Every runnable row passed; the sole environment-blocked Pull Request row
records its cause, unblocking action, and equivalent evidence for all five
controls. There are no findings, declared blocks, finding blocks, skipped
rows, or unresolved evidence paths.
