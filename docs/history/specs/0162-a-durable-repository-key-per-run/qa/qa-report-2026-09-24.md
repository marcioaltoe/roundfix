---
spec: 0162-a-durable-repository-key-per-run
date: 2026-09-24
build: d3adbd644b898da63d8b54980cf67a69e1d4b9b8
auditing_binary: "0.16.0 (e38c8561, built 2026-09-24 08:07:17 -0300)"
auditor_staleness: "current: commit ancestry: build commit does not predate audited tree"
status: closed
verdict: pass
rows_blocked_environment: 1
rows_blocked_finding: 0
rows_blocked_declared: 0
surfaces: [backend, cli]
---

# QA Report

## Scope and environment

Full terminal QA for commit `d3adbd644b898da63d8b54980cf67a69e1d4b9b8`
on the Run Worktree branch. The actor is a Roundfix operator using the built CLI
against isolated local Git repositories, linked worktrees, Run Databases and
Artifact Directories below `/private/tmp`. The Pull Request fact is supplied by
the QA assignment; no network, credential, commit, push or Pull Request mutation
is in scope.

The strict authoring precondition passed before this matrix opened:
`roundfix spec check 0162-a-durable-repository-key-per-run --strict` exited 0
with `No findings`. The installed checker identified itself as `roundfix
0.15.0` and did not execute authored Verification commands.

## Static gate

- Repository Verification: the QA assignment records the Daemon-owned
  `make verify-changed` run as passed with exit 0 and diagnostics removed on
  success. This gate records that fact and did not rerun the command.
- Current-tree build: `GOCACHE=/private/tmp/roundfix-0162-qa-gocache go build
  -o /private/tmp/roundfix-0162-qa ./cmd/roundfix` exited 0. The built CLI
  reports `roundfix 0.16.0`.
- Focused Run Database regression: `go test -count=1 ./internal/store` with the
  task-scoped cache exited 0 (`ok roundfix/internal/store 9.284s`).

## Performed repairs

None.

## Assigned repair failures

None.

## Authorization audit inputs

- Authorization commit `a940b6df63edde572f8dcdef26c8d31598a46ec5`
  is an ancestor of both Task commits and grants the bounded governed path
  `internal/cli/cli_test.go` for this Spec.
- `git diff-tree --no-commit-id --name-only -r d37c6e52...` returned only
  `task_01.md`, `internal/store/store.go` and `internal/store/store_test.go`.
- `git diff-tree --no-commit-id --name-only -r d3adbd64...` returned only
  `task_02.md`, `internal/cli/gc_test.go`, `internal/cli/reconcile.go`,
  `internal/cli/reconcile_test.go`, `internal/cli/window_test.go`,
  `internal/store/journal.go` and `internal/store/store.go`.
- Neither Task commit changed a governed path, so no tooling authorization row
  or consequent-fix chronology row was runnable.

## Mechanical findings

None.

## Results

| # | Status | Provenance |
| - | --- | --- |
| R1 | pass | Requirement 1; repository Verification |
| R2 | pass | Requirement 2 |
| R3 | pass | Requirement 3; outside-evidence row |
| R4 | pass | Requirement 4 |
| R5 | pass | Requirement 5 |
| R6 | pass | Requirement 5 |
| R7 | pass | Requirement 6 |
| R8 | blocked (environment: no open Pull Request) | Pull Request row |

## Row evidence

### R1

- Actor and surface: Daemon; repository gate.
- Steps: record the Daemon's command, exit status and diagnostics fact.
- Expected observable: `make verify-changed` exits 0 with no retained success
  diagnostics.
- Independent confirmation: bind the prompt-supplied fact to this report and
  audited build.
- Persistence: not applicable; this is one supervised static-gate result.
- Evidence: the QA assignment binds `command: make verify-changed`, `verdict:
  pass (exit 0)` and `diagnostics: removed on success` to this gate and audited
  build. This is supervised evidence from the Daemon that owns repository
  Verification, recorded as a fact rather than re-derived in the Agent sandbox.

### R2

```yaml
inputs:
  - kind: repository_path
    ref: internal/store/store.go
  - kind: repository_path
    ref: internal/store/store_test.go
```

- Actor and surface: operator; Run Database through the built CLI and an
  independent SQLite read.
- Steps: create a Run from a linked worktree, confirm its recorded key, migrate
  a pre-v17 database with resolvable and unresolvable rows, compare migrated and
  fresh schemas, and run the earlier-version migration sweep.
- Expected observable: new and resolvable migrated rows carry the main checkout
  key; an unresolvable row keeps an empty key; fresh and migrated schemas match.
- Independent confirmation: direct read of the Run Database after the public
  command reopens it.
- Persistence: close and reopen the database before the independent read.
- Evidence: fixture `/private/tmp/roundfix-0162-migration.0H4A72` created a v16
  Run Database with linked-worktree Run
  `run_20260924T213521Z_d72693206f91c6ce` and an unresolvable Run
  `run_20260924T213521Z_7ad5a39b695e3e0a`. From the main checkout, the built
  CLI's public `window show` command exited 0 and reopened the writer store,
  migrating the database. A separately compiled current-tree reader then
  observed `resolvableRoot` equal to the main checkout,
  `unresolvableRoot: ""`, and `schemaMatchesFresh: true` after creating and
  comparing a fresh database. `go test -count=1 ./internal/store` also exited
  0, exercising the package's historical migration fixtures and the named new
  key, backfill, fresh-schema and removed-worktree coverage.

### R3

```yaml
inputs:
  - kind: repository_path
    ref: internal/store/store.go
  - kind: repository_path
    ref: internal/cli/runs.go
  - kind: external_repository
    ref: scratch Git repository observed independently with git worktree list and git rev-parse --git-common-dir
```

- Actor and surface: operator; `roundfix runs list` plus native Git.
- Steps: record a linked-worktree Run, remove the worktree, then list from the
  main checkout twice.
- Expected observable: both listings retain the Run and preserve its removed
  checkout as `git_root`.
- Independent confirmation: native Git proves the main and linked checkouts
  shared one common directory before removal and proves the worktree is absent
  afterwards.
- Persistence: rerun the CLI from a fresh process after removal.
- Evidence: fixture `/private/tmp/roundfix-0162-live.10WY3b` created main and
  linked worktrees at the same commit `333e95802a85952bbd8503c7947ee91960dbb756`.
  Before removal, native Git listed both worktrees and the linked checkout's
  `git rev-parse --git-common-dir` returned the main checkout's `.git`
  directory. After `git worktree remove`, native Git listed only main. Two
  fresh `HOME=... /private/tmp/roundfix-0162-qa runs list --state all --limit
  0` processes from main each listed removed-checkout Run
  `run_20260924T213704Z_2f1a83b5afdd031f` with branch `feature/linked` and did
  not list unrelated Run `run_20260924T213704Z_a49651bf28a96958`. Both
  listings also emitted the existing retained-Run inspection warning for the
  absent checkout; the requested Run remained visible. The native Git
  observations are outside evidence from a scratch repository the Spec did
  not author.

### R4

```yaml
inputs:
  - kind: repository_path
    ref: internal/cli/reconcile.go
  - kind: repository_path
    ref: internal/store/store.go
```

- Actor and surface: operator; `roundfix reconcile <run-id> --format=json`.
- Steps: reconcile a linked-worktree Run from main, then request an unrelated
  repository's Run by ID.
- Expected observable: the first command accepts and reports the requested Run;
  the second refuses before output and names both repositories.
- Independent confirmation: repeat the accepted read and inspect the refused
  command's exit status and diagnostic.
- Persistence: use fresh CLI processes for both checks.
- Evidence: fixture `/private/tmp/roundfix-0162-reconcile.SPgp3L` recorded
  linked-worktree Run `run_20260924T213825Z_cb69bddbf99524f7` with repository
  key `/private/tmp/roundfix-0162-reconcile.SPgp3L/main`. Two fresh
  `reconcile <run-id> --format=json` processes from main exited 0, returned the
  requested Run and classified it `released`; each summary had zero operational
  failures. Requesting unrelated Run
  `run_20260924T213825Z_cb84c5f8d1dbfb12` exited 2 with no JSON and named its
  `/other` repository and the current `/main` repository. An additional
  out-of-scope probe against R3's already removed checkout reached repository
  selection but then exited 1 because terminal metadata inspection still needs
  the absent checkout; Requirement 4 does not promise reconciliation after
  removal, so the declared journey used the live linked worktree.

### R5

```yaml
inputs:
  - kind: repository_path
    ref: internal/cli/gc.go
  - kind: repository_path
    ref: internal/store/store.go
```

- Actor and surface: operator; `roundfix gc sanitize`.
- Steps: place a terminal linked-worktree Run under the shared Artifact Root,
  remove the worktree and preview sanitation from the main checkout.
- Expected observable: the shared root is classified `orphaned`, never
  `overridden`.
- Independent confirmation: rerun the preview and confirm the artifact remains
  present because no apply flag was supplied.
- Persistence: use a second fresh CLI process.
- Evidence: from the main checkout of removed-worktree fixture
  `/private/tmp/roundfix-0162-live.10WY3b`, two fresh `HOME=...
  /private/tmp/roundfix-0162-qa gc sanitize` previews exited 0. Each named
  shared root `.../artifacts/7bb8790931c0f6ed`, classified it `orphaned`,
  reported one terminal Run and never printed `Classification: overridden`.
  A separate file check after the second preview confirmed the Run's
  `marker.txt` still existed; neither command used `--apply`.

### R6

```yaml
inputs:
  - kind: repository_path
    ref: internal/cli/window.go
  - kind: repository_path
    ref: internal/store/store.go
```

- Actor and surface: operator; `roundfix window show` and `roundfix window
  clear`.
- Steps: seed a legacy window under the linked checkout's exact path, show it
  from that linked worktree, clear it, then show again.
- Expected observable: the stored cutoff is shown, clear reports success and a
  fresh show reports no Run Window.
- Independent confirmation: direct database read proves both key variants are
  absent after clear.
- Persistence: query from a fresh process after the mutation.
- Evidence: the fixture seeded one Run Window row under exact linked checkout
  `/private/tmp/roundfix-0162-reconcile.SPgp3L/linked`. From that worktree, a
  fresh `window show` exited 0 and printed cutoff `2026-09-24 21:39 -03`; a
  fresh `window clear` exited 0 and printed `Run Window cleared`; a third
  process printed `No Run Window is set`. An independent current-tree SQLite
  reader then returned `windowRows: 0` across the repository-key and exact
  checkout-key variants.

### R7

```yaml
inputs:
  - kind: repository_path
    ref: docs/specs/0162-a-durable-repository-key-per-run/**
  - kind: repository_path
    ref: internal/speccheck/**
```

- Actor and surface: Spec author; `roundfix spec check --strict`.
- Steps: run the strict checker from the audited tree.
- Expected observable: no findings, including no unmapped declared promise.
- Independent confirmation: use command output and exit status, never a file
  read alone.
- Persistence: rerun after the report closes.
- Evidence: current-tree binary `/private/tmp/roundfix-0162-qa` identifies as
  `roundfix 0.16.0`. Its `spec check
  0162-a-durable-repository-key-per-run --strict` command exited 0 and printed
  `No findings`, while explicitly reporting that authored Verification commands
  were not executed. Named skips were the absent rollup, backlog, Vocabulary
  Contract and references index; none is the promise-rule detector. The same
  current-tree command exited 0 with `No findings` again after the report body
  settled.

### R8

```yaml
inputs:
  - kind: live_service
    ref: prompt-supplied Pull Request fact and Daemon-supervised repository Verification
```

- Actor and surface: reviewer; Pull Request journey.
- Steps: account for approval, checks and status, unresolved review threads,
  Merge-Ready acceptance and review-artifact ancestry for the expected head.
- Expected observable: the no-open-Pull-Request cause is explicit and every
  control has equivalent observed or supervised evidence.
- Independent confirmation: bind each control to expected head
  `d3adbd644b898da63d8b54980cf67a69e1d4b9b8`.
- Persistence: not applicable while no Pull Request exists.
- Evidence: the QA assignment states `Pull Request: none open`, identifies
  `fix/0162-durable-repository-key` as the target branch and distinguishes it
  from the never-pushed Run Worktree branch. Equivalent controls: approval — no
  change acceptance has been obtained; `_authorization.md` grants bounded work
  and delivery authority but is not review approval; checks and status — the
  Daemon ran `make verify-changed` for this candidate and recorded exit 0;
  unresolved review threads — none can stand because no Pull Request or thread
  collection exists; Merge-Ready acceptance — none exists because configured
  pre-PR review follows this authored gate; review-artifact ancestry — no file
  exists under this Spec's `reviews/` path to misbind, while this report is bound
  to expected head `d3adbd644b898da63d8b54980cf67a69e1d4b9b8`. The target
  branch currently resolves to authorization head
  `a940b6df63edde572f8dcdef26c8d31598a46ec5`; integration is a later
  Daemon-owned step.

## Findings

None.

## Blocked and skipped

- R8 is `blocked (environment: no open Pull Request)`. The QA assignment proves
  the absence and R8 records equivalent evidence for approval, checks and
  status, unresolved review threads, Merge-Ready acceptance and review-artifact
  ancestry. Unblocking action: after the Daemon integrates this Run, perform
  configured pre-PR review and open the Pull Request from
  `fix/0162-durable-repository-key`; those later workflow steps are outside this
  gate.
- The mechanical-stage skips remain recorded below. None omitted a coverage
  source: Git evidence above resolves the Task commits and governed-path scope,
  no consequent-fix declaration exists, and this report plus its evidence now
  exist for the Daemon's later structural pass.

## Coverage

Eight of eight rows reached a terminal status: seven passed and one is blocked
only by the no-open-Pull-Request environment with equivalent evidence.
Requirements 1-6, the outside-evidence row, the Pull Request row and repository
Verification each appear in provenance; no row names another source. The PRD
declares no Unreachable Acceptance and no frontend surface. Counts:
`rows_blocked_environment: 1`, `rows_blocked_finding: 0`,
`rows_blocked_declared: 0`. No row failed, was skipped or remained pending.

## Final verdict

Pass — every runnable criterion passed, the one environment-blocked row carries
equivalent evidence for every Pull Request control, and no finding-blocked,
declared-blocked or skipped row remains. Results by status: seven pass, one
environment-blocked; findings by impact tier: none.

## Mechanical skips

| Detector | Missing artifact |
| --- | --- |
| authorization bounded paths | Task commits |
| consequent-fix commit order | consequent-fix declaration |
| QA Report structure | QA Report |
| QA evidence paths | QA Report |
