---
spec: 0170-authoring-that-fails-before-dispatch
date: 2026-09-25
build: 6404ba29429d8cccf031895f36bdc0b6b1517b01
auditing_binary: "0.17.0 (6404ba29-dirty, built 2026-09-25 16:31:53 -0300)"
auditor_staleness: "current: commit ancestry: build commit does not predate audited tree"
status: closed
verdict: pass
rows_blocked_environment: 1
rows_blocked_finding: 0
rows_blocked_declared: 0
surfaces: [backend, cli, docs]
---

# QA Report — authoring that fails before dispatch

## Scope and environment

- Scope: full authored terminal gate for Task 05 at `6404ba29429d8cccf031895f36bdc0b6b1517b01`.
- Entry point: freshly built `/tmp/roundfix-qa0170/roundfix` from this Run Worktree.
- Authoring precondition: `/tmp/roundfix-qa0170/roundfix spec check 0170-authoring-that-fails-before-dispatch --strict` exited 0 with no findings; `SC-ROLLUP-MEMBER` was skipped because no `docs/findings` rollup exists.
- Repository Verification is a Daemon-supervised fact supplied to this QA Agent: `make verify-changed` exited 0; it will not be rerun.
- Pull Request fact: no Pull Request is open for the Spec target branch, so the Pull Request journey is environment-blocked and must use equivalent evidence.

## Performed repairs

None.

## Assigned repair failures

None.

## Authorization audit inputs

| Task | Outcome | Record | Revision | Detail |
| --- | --- | --- | --- | --- |
| task_02 | granted | docs/specs/0170-authoring-that-fails-before-dispatch/_authorization.md | 4b1bbeca3c54c38dec32f7fb62dd9ea5de9ea1c9 | operative grant |

## Mechanical findings

None.

## Results

| # | Story / criterion / sweep | Actor and surface | Status | Provenance and evidence |
| - | --- | --- | --- | --- |
| R1 | Record the repository Verification without rerunning it | Daemon / repository gate | pass | Requirement 1; repository Verification — Daemon-supervised `make verify-changed` exited 0; success diagnostics were removed |
| R2 | Run the repository documentation contract gate | Maintainer / repository CLI | pass | Requirement 2 — `make verify-docs` exited 0 |
| R3 | Refuse and then accept a declared `Makefile` Governed Path | Spec author / built CLI | pass | Requirement 3 — negative exit 1 with one `SC-TOOLING-UNDECLARED`; declared rerun exit 0 with no findings |
| R4 | Replay archived Spec 0155's three undeclared Governed Paths | Spec author / built CLI | pass | Requirement 4; outside-evidence row — archived 0155 replay named all three historically undeclared paths |
| R5 | Report a CLI surface without a guide and clear it through local or dependency guidance | Spec author / built CLI | pass | Requirement 5 — non-strict gap, strict error, then no finding with local and dependency guides |
| R6 | Exercise the checker honesty cases through the built CLI | Spec author / built CLI | pass | Requirement 6 — all five public checker observations matched their stable codes or absence |
| R7 | Prove the templates author the accepted Tooling authority label | Spec author / templates and built CLI | pass | Requirement 7 — forbidden-label scan empty; template-derived PRD exited 0 with no findings |
| R8 | Check this Spec, every active Spec, and the two corpus counts | Maintainer / built CLI and corpus | pass | Requirement 8 — Spec 0170 strict clean; all-active scan exit 0; both new corpus counts 0; six unrelated target-identical gaps observed |
| R9 | Audit every governed path actually changed by Task commits | Maintainer / Git | pass | Requirement 9 — all four Task commit diffs are authorized or reproducible `make skills-sync` output |
| R10 | Prove this Spec's artifacts satisfy the promise rule | Spec author / built CLI | pass | Requirement 10 — strict JSON result contained an empty findings array |
| R11 | Observe Pull Request controls or equivalent evidence | Maintainer / Pull Request | blocked (environment: no open Pull Request) | Pull Request row — all five controls carry explicit equivalent evidence |

## Planned row evidence

### R1

- Actor / entry point / surface: Roundfix Daemon; repository Verification; repository gate.
- Steps: record the supplied `make verify-changed` command, exit status, and diagnostics disposition without rerunning it.
- Expected observable: exit 0 and no retained success diagnostics.
- Independent confirmation / persistence: the QA prompt is the supervised source; the row is intentionally not carriable.
- Evidence path: this report.
- Executed evidence: the Roundfix QA prompt records `make verify-changed`, verdict `pass` (exit 0), with diagnostics removed on success. Per the explicit QA contract, the command was not rerun.
- Status: pass.

### R2

```yaml
inputs:
  - kind: repository_path
    ref: Makefile
  - kind: repository_path
    ref: docs/**
  - kind: repository_path
    ref: .agents/skills/**
  - kind: repository_path
    ref: skills/**
```

- Actor / entry point / surface: maintainer; `make verify-docs`; repository CLI.
- Steps: run the full documentation contract gate and rerun it after the report closes.
- Expected observable: exit 0.
- Independent confirmation / persistence: the command includes the documentation and strict Spec contracts against the current tree; exact output and exit are recorded below.
- Evidence path: command transcript in this report.
- Executed evidence: `GOCACHE=/tmp/roundfix-qa0170-gocache make verify-docs` exited 0 before the functional rows and again after the report closed. Both runs passed `internal/docscontract`, the repository-contract tests in `internal/baseline` and `skills`, the corpus budget, and `bin/roundfix spec check`. The final scan reported no finding for Spec 0170; its only skip was `SC-ROLLUP-MEMBER` because no `docs/findings` rollup exists.
- Status: pass.

### R3

```yaml
inputs:
  - kind: repository_path
    ref: cmd/roundfix/**
  - kind: repository_path
    ref: internal/speccheck/**
```

- Actor / entry point / surface: Spec author; built `roundfix spec check`; CLI.
- Steps: create a scratch Spec with a pending Task declaring `interface: Makefile`; check while the grant omits it, then add it to the grant and both Tooling rows and check again.
- Expected observable: the negative case exits 1 with exactly one `SC-TOOLING-UNDECLARED` naming `Makefile`; the declared case reports none.
- Independent confirmation / persistence: rerun the public read-only command after the declaration repair in the same scratch repository.
- Evidence path: scratch path and command transcript in this report.
- Executed evidence: in `/tmp/roundfix-qa0170.RG9sRZ/repo`, `/tmp/roundfix-qa0170/roundfix spec check qa-undeclared` exited 1 and emitted exactly one `SC-TOOLING-UNDECLARED`, locating `task_01.md:12` and naming `Makefile` plus the grant, PRD, and TechSpec omissions. After adding `Makefile` to `_authorization.md` and both `bounded files:` rows, the same public command exited 0 and printed `No findings`.
- Status: pass.

### R4

```yaml
inputs:
  - kind: repository_path
    ref: docs/history/specs/0155-a-verify-that-runs-what-changed/**
  - kind: repository_path
    ref: cmd/roundfix/**
```

- Actor / entry point / surface: Spec author; built `roundfix spec check`; CLI against a scratch repository copy.
- Steps: restore archived Spec 0155 as active, make its Tasks pending, empty its authorization `paths:`, and run the checker.
- Expected observable: `SC-TOOLING-UNDECLARED` names `Makefile`, `.roundfixrc.yml`, and `.github/workflows/ci-verify.yml`.
- Independent confirmation / persistence: the source Tasks and grant predate and were not authored by Spec 0170; the scratch copy leaves this worktree unchanged.
- Evidence path: archived 0155 paths plus scratch command transcript in this report.
- Executed evidence: copied `docs/history/specs/0155-a-verify-that-runs-what-changed/` to the active Spec root inside `/tmp/roundfix-qa0170.RG9sRZ/repo`, changed all nine Task statuses to `pending`, restored the PRD to `active`, and emptied the copied grant's `paths:`. `/tmp/roundfix-qa0170/roundfix spec check 0155-a-verify-that-runs-what-changed` exited 1 and emitted `SC-TOOLING-UNDECLARED` findings naming `Makefile`, `.roundfixrc.yml`, and `.github/workflows/ci-verify.yml` at the archived 0155 Task declarations. It additionally exposed `.github/workflows/release.yml`, a read-only Verification operand; that does not replace or weaken the three required outside observations.
- Outside source: archived Spec 0155's Tasks and `_authorization.md`, authored and delivered before Spec 0170.
- Status: pass.

### R5

```yaml
inputs:
  - kind: repository_path
    ref: cmd/roundfix/**
  - kind: repository_path
    ref: internal/speccheck/surface.go
```

- Actor / entry point / surface: Spec author; built `roundfix spec check`; CLI.
- Steps: check a scratch pending Task naming `internal/cli/cli_test.go` without a guide, under `--strict`, then with a guide in that Task and in a dependency.
- Expected observable: one non-strict gap, strict exit 1, and no finding for either documented form.
- Independent confirmation / persistence: fresh public command invocations after each scratch-artifact state change.
- Evidence path: scratch path and command transcript in this report.
- Executed evidence: the authorized `qa-cli-guide` scratch Spec in `/tmp/roundfix-qa0170.RG9sRZ/repo` named `internal/cli/cli_test.go`. Without a guide, the built binary exited 0 in normal mode with one `SC-CLI-UNDOCUMENTED` gap and exited 1 under `--strict` with the same finding promoted to an error. After the same Task named `.agents/skills/roundfix/SKILL.md` and the grant and Tooling rows accounted for that Governed Path, the normal command exited 0 with `No findings`. A second fresh state moved the guide to `task_00` and made the CLI Task depend on it; the command again exited 0 with `No findings`.
- Status: pass.

### R6

```yaml
inputs:
  - kind: repository_path
    ref: cmd/roundfix/**
  - kind: repository_path
    ref: internal/speccheck/verification.go
  - kind: repository_path
    ref: internal/speccheck/citations.go
```

- Actor / entry point / surface: Spec author; built `roundfix spec check`; CLI.
- Steps: exercise captured and inline pipe-to-`grep` emptiness forms, a wrapped `None.` Success Metrics paragraph, an adopted Backlog Entry left in place, and a reason-only archived Finding.
- Expected observable: the two pipeline cases report `SC-VERIFY-INVERTED-EXIT`; wrapped `None.` has no `SC-METRIC-UNDECLARED`; the lifecycle cases report `SC-REF-UNRESOLVED` and `SC-ARCHIVE-LICENSE` respectively.
- Independent confirmation / persistence: separate scratch artifact states and fresh CLI reads for each case.
- Evidence path: scratch paths and command transcript in this report.
- Executed evidence in `/tmp/roundfix-qa0170.RG9sRZ/repo` with the freshly built binary:
  - `spec check qa-pipe-captured` exited 1 and reported `SC-VERIFY-INVERTED-EXIT` for `matches="$(go vet ./... 2>&1 | grep warning)"; test -z "$matches"`, naming the status-preserving replacement.
  - `spec check qa-pipe-inline` exited 1 and reported the same code for `[ -z "$(go vet ./... 2>&1 | grep warning)" ]`.
  - `spec check qa-wrapped-none` exited 0 with `No findings`; its wrapped two-line `None.` paragraph produced no `SC-METRIC-UNDECLARED`.
  - `spec check qa-adopted` exited 1 and reported `SC-REF-UNRESOLVED`, naming both `docs/backlog/2026-09-25-left-behind.md` and its indexed copy.
  - `spec check qa-archive-finding` exited 1 and reported `SC-ARCHIVE-LICENSE` for the archived Finding carrying `closure_reason` without `closure_evidence`.
- Status: pass.

### R7

```yaml
inputs:
  - kind: repository_path
    ref: .agents/skills/write-prd/references/prd-template.md
  - kind: repository_path
    ref: .agents/skills/write-techspec/references/techspec-template.md
  - kind: repository_path
    ref: cmd/roundfix/**
```

- Actor / entry point / surface: Spec author; authoring templates and built checker; docs and CLI.
- Steps: assert neither template contains `bounded paths:`; fill the PRD template's Tooling row in a scratch Spec and run the checker.
- Expected observable: template assertions pass and no `SC-TOOLING-UNBOUNDED` appears.
- Independent confirmation / persistence: the public checker freshly reads the materialized scratch PRD.
- Evidence path: canonical templates plus scratch command transcript in this report.
- Executed evidence: `rg -n "bounded paths:"` across the canonical and mirrored PRD and TechSpec templates exited 1 with empty output, proving the forbidden label is absent. The scratch `qa-template/_prd.md` materialized the canonical PRD template's Tooling authority row with `bounded files: Makefile` and a matching approved grant. `/tmp/roundfix-qa0170/roundfix spec check qa-template` then exited 0 with `No findings`; it emitted no `SC-TOOLING-UNBOUNDED`.
- Status: pass.

### R8

```yaml
inputs:
  - kind: repository_path
    ref: docs/specs/**
  - kind: repository_path
    ref: internal/docscontract/testdata/corpus-golden.json
  - kind: repository_path
    ref: cmd/roundfix/**
```

- Actor / entry point / surface: maintainer; built `roundfix spec check`; CLI and corpus data.
- Steps: check this Spec under `--strict`, check every active Spec under `--strict`, and inspect both new corpus counts.
- Expected observable: no finding and both counts equal 0.
- Independent confirmation / persistence: separate current-Spec and all-active invocations plus direct corpus read.
- Evidence path: command transcript and corpus path in this report.
- Executed evidence:
  - `/tmp/roundfix-qa0170/roundfix spec check 0170-authoring-that-fails-before-dispatch --strict` exited 0 and printed `No findings`.
  - `/tmp/roundfix-qa0170/roundfix spec check` checked all six active Specs and exited 0. Specs 0170, 0171, and 0172 reported no findings. Specs 0121, 0126, and 0127 each retain one `SC-METRIC-UNDECLARED` gap and one `SC-CONTRACT-UNDECLARED` gap.
  - The same current analyzer run against delivery-target revision `4b1bbeca3c54c38dec32f7fb62dd9ea5de9ea1c9` reproduced those six gaps at the same files and lines. Under the QA analyzer policy, these target-identical diagnostics are observed and remain owned by Specs 0121, 0126, and 0127; they do not fail Spec 0170.
  - `internal/docscontract/testdata/corpus-golden.json` records `"SC-CLI-UNDOCUMENTED": 0` and `"SC-TOOLING-UNDECLARED": 0`.
- Status: pass.

### R9

```yaml
inputs:
  - kind: repository_path
    ref: docs/specs/0170-authoring-that-fails-before-dispatch/_authorization.md
  - kind: repository_path
    ref: docs/specs/0170-authoring-that-fails-before-dispatch/task_0[1-4].md
  - kind: repository_path
    ref: .agents/skills/**
  - kind: repository_path
    ref: skills/**
```

- Actor / entry point / surface: maintainer; Git commit objects; repository history.
- Steps: resolve every Task commit with `git log`, enumerate actual paths using `git diff-tree`, prove the operative grant is earlier in target ancestry, and compare every governed path to `paths:` or `make skills-sync` outputs.
- Expected observable: every governed path is authorized or a reproducible mirror output; no authorization-shape or ordering defect exists.
- Independent confirmation / persistence: commit-object diffs and ancestry checks, not Task Result prose.
- Evidence path: Git SHAs and command transcript in this report.
- Executed evidence:
  - The authorization record was introduced at `256ad1568b34be256527e8f5e6999b2d8203f8ab` in a Spec-artifact commit with no governed implementation path. `git merge-base --is-ancestor` exited 0 from that revision to each Task commit, and `git diff --exit-code 256ad156... -- docs/specs/0170-authoring-that-fails-before-dispatch/_authorization.md` exited 0.
  - `git diff-tree --no-commit-id --name-only -r` enumerated Task 04 at `e247488b`, Task 01 at `ff714c9c`, Task 03 at `709ec908`, and Task 02 at `6404ba29`. The governed paths in those diffs are all named in `_authorization.md` except `skills/write-tasks/references/task-template.md`, which is the mirror output of the sanctioned `make skills-sync` command.
  - Task 04 changed the authorized canonical PRD, TechSpec, and write-tasks assets, their mirrors, and `skills/baseline_skill_contract_test.go`. Tasks 01 and 02 changed only their authorized checker/golden/characterization and Roundfix/qa-gate skill paths among governed files. Task 03 changed ordinary Go source and test files only.
  - `diff -r` exited 0 for all five canonical/mirror skill pairs: `qa-gate`, `roundfix`, `write-prd`, `write-techspec`, and `write-tasks`.
  - The seeded mechanical stage recorded no finding and named the operative authorization record; its consequent-fix-order detector was explicitly skipped because the Spec declares no consequent fix.
- Status: pass.

### R10

```yaml
inputs:
  - kind: repository_path
    ref: docs/specs/0170-authoring-that-fails-before-dispatch/**
  - kind: repository_path
    ref: cmd/roundfix/**
```

- Actor / entry point / surface: Spec author; built `roundfix spec check`; CLI.
- Steps: run the promise-rule detector over this Spec's own PRD, TechSpec, Task Graph, and Tasks.
- Expected observable: no promise mapping or consuming-Task finding.
- Independent confirmation / persistence: fresh strict checker output on the assembled Spec.
- Evidence path: current Spec artifacts and command transcript in this report.
- Executed evidence: `/tmp/roundfix-qa0170/roundfix spec check 0170-authoring-that-fails-before-dispatch --strict --format json` exited 0. Its `roundfix-speccheck/v1` payload carried `"findings":[]`; therefore neither `SC-COVERAGE-UNMAPPED` nor `SC-COVERAGE-UNTASKED` found an unconsumed PRD or TechSpec promise. The only skip was the unrelated absent `docs/findings` rollup.
- Status: pass.

### R11

```yaml
inputs:
  - kind: live_service
    ref: Pull Request fact supplied by the Roundfix QA prompt for the Spec target branch
```

- Actor / entry point / surface: maintainer; Open Pull Request; Pull Request controls.
- Steps: observe approval, checks/status, unresolved review threads, Merge-Ready acceptance, and review-artifact ancestry; when no Pull Request exists, record the environmental cause and equivalent evidence for every control.
- Expected observable: all five controls are answered with live or equivalent observed/supervised evidence.
- Independent confirmation / persistence: target-branch and Run-head Git ancestry plus supplied Daemon Verification fact; the row is never carriable.
- Evidence path: prompt fact, Git commands, and this report.
- Result: blocked (environment: no open Pull Request), with equivalent evidence for every control:
  - approval: no Pull Request acceptance was obtained because no Pull Request exists; the maintainer's 2026-09-25 approval covers the Spec's bounded implementation authority, not review acceptance;
  - checks and status: the Daemon-supervised `make verify-changed` exited 0, and QA's required `make verify-docs` exited 0;
  - unresolved review threads: no Pull Request exists, so no thread set exists to leave unresolved;
  - Merge-Ready acceptance: no Review Source evidence exists yet;
  - review-artifact ancestry: `rg --files docs/specs/0170-authoring-that-fails-before-dispatch | rg '/reviews/'` found no artifact, and no Pull Request head exists, so no ancestry claim was made. Git independently showed target tip `4b1bbeca3c54c38dec32f7fb62dd9ea5de9ea1c9` is an ancestor of Run head `6404ba29429d8cccf031895f36bdc0b6b1517b01`.
- Unblocking action: after this gate settles and the Run integrates, open the Pull Request on `roundfix/deliver-0170-authoring-that-fails-before-dispatch-a4dd1b82069a67bf`, then observe all five controls against its actual head.

## Static gate

- Authoring precondition: pass — current-tree binary, strict Spec check, no findings.
- Repository Verification: pass — Daemon-supervised `make verify-changed` exit 0; not rerun.
- Documentation contract gate: pass — `make verify-docs` exit 0.

## Findings

None.

## Blocked and skipped

- R11 is environment-blocked because the Roundfix QA prompt proves no Pull Request is open. Its five controls carry explicit equivalent evidence above.
- No row is finding-blocked, declared-blocked, or skipped.

## Coverage

- Complete: 10 rows passed and 1 row is environment-blocked with equivalent evidence. Requirements 1-10 and the mandatory outside-evidence, Pull Request, and repository Verification sources all appear in provenance; no row covers another source. Requirement 11 constrains every row's evidence and is not a declared verification source.
- The PRD declares no `## Unreachable Acceptance` entries and no frontend surface.
- Counts: `rows_blocked_environment: 1`, `rows_blocked_finding: 0`, `rows_blocked_declared: 0`; failed: 0; skipped: 0; pending: 0.

## Final verdict

Pass. Every runnable row passed; the sole environment-blocked Pull Request row records its cause, unblocking action, and equivalent evidence for all five controls. There are no findings, declared blocks, finding blocks, skipped rows, pending rows, or unresolved evidence paths.

## Mechanical skips

| Detector | Missing artifact |
| --- | --- |
| consequent-fix commit order | consequent-fix declaration |
| QA Report structure | QA Report |
| QA evidence paths | QA Report |
