# Non-Goals and historical evidence

The fresh commit-range inventory from the Spec-authoring parent through
`a51a94cb` contains no path under `docs/specs/_archived/`. No archived Spec
artifact or recorded historical QA evidence was rewritten.

The same range contains no change to ADR-0080, ADR-0091, a QA verdict
implementation, or Daemon Task-status ownership. The implementation changes
are confined to Spec parsing and consistency checks, the three order carriers,
the two authorized Skills and mirrors, tests/fixtures, Spec artifacts, and
sanctioned generated fallout.

The public CLI adds no command or flag, requires no bespoke harness for every
Task, does not weaken Daemon Verification ownership, and does not change
blocked-row verdict typing.
