---
spec: 0114-an-audit-that-reads-the-authorization-it-was-given
date: 2026-08-15
build: f3d8a5fc5c3849bb52b38707420ee71c32befce2
status: closed
verdict: fail
rows_blocked_environment: 0
rows_blocked_finding: 0
rows_blocked_declared: 0
surfaces: [backend, docs]
---

# QA report — An audit that reads the authorization it was given

## Scope and environment

Full same-day rerun on build `f3d8a5fc5c3849bb52b38707420ee71c32befce2`.
The prior report failed because both Spec artifacts retained the superseded
Tooling authority wording and `Governed Path` had not reached `CONTEXT.md`.
This rerun starts with those rows and then re-observes the complete matrix.

The intended actors are a Spec author, a Supervisor running the changed-path
audit, and a maintainer reading an authorization record. The exercised surfaces
are the built `roundfix spec check` CLI, the backend audit and ownership resolver
through their focused Go entry points, repository history through `git`, and the
Spec and glossary as a documentation reader.

The current source was built as `/tmp/roundfix-0114-qa-rerun` with
`GOCACHE=/tmp/roundfix-0114-qa-rerun-gocache`. No Pull Request is open, but this
Spec declares no Pull Request journey. No credentials, network service, or
external database are required. The QA node is terminal; its dependencies
`task_01` and `task_05` are completed.

`/tmp/roundfix-0114-qa-rerun spec check
0114-an-audit-that-reads-the-authorization-it-was-given --strict` exited 0 with
no findings. It skipped only `SC-REF-UNRESOLVED` because the Spec has no optional
`references/_index.md`; no acceptance row depends on that index.

## Static gate

- `GOCACHE=/tmp/roundfix-0114-qa-rerun-gocache make verify` exited 2. Two
  `internal/cli` force-stop integration tests were environment-caused: the
  sandbox denied process-table reads with `operation not permitted`. A separate
  `internal/daemon` failure is code-caused:
  `TestQAMechanicalRequestSelectsTheAuthorizedTaskCommit` expected the audit to
  block `outside.txt`, but the current audit correctly treats that ordinary path
  as ungoverned and returned no finding.
- The isolated daemon reproduction, `go test -count=1 ./internal/daemon -run
  '^TestQAMechanicalRequestSelectsTheAuthorizedTaskCommit$' -v`, failed with the
  same empty mechanical result. `git blame` traces the fixture and expectation
  to `a2a4c86b7`; the fixture changes `outside.txt`. The accepted ADR-0130
  behavior and `GovernedPath` state that ordinary files are unaudited. The
  current mechanical unit negative control uses an ungranted governed path,
  `.golangci.yml`, and passes. Finding: F-001.
- The feature-focused commands all exited 0: `TestToolingRowStatesApplicability`
  passed four subtests; `TestAuditJudgesTheGrant` passed six subtests;
  `TestOutputsForCommand` passed four subtests; `TestGovernedPath` passed fourteen
  subtests; and `TestEveryBoundedPathIsGoverned` passed its live-corpus and
  unmatched-path cases while its deliberate no-records case reported `SKIP`.
- `go test -count=1 ./internal/speccheck ./internal/suiteguard
  ./internal/suiteguardcontract ./internal/baseline` exited 0. The
  `suiteguardcontract` package has no direct tests.
- `go test -count=1 ./internal/speccheck -run
  '^(TestMechanicalReportShape|TestMechanicalEvidencePath)$' -v` exited 0.

## Mechanical findings

None.

## Results

| # | Status | Provenance |
| - | --- | --- |
| Q-01 | pass | Built CLI accepted the settled no-mutation wording; four checker cases preserved the three required refusals. |
| Q-02 | pass | Historical ordinary-Go and authorized-asset commits resolved from Git and passed one audit without a split. |
| Q-03 | pass | Both required pre-Spec refusal histories resolved from Git and passed the corrected audit. |
| Q-04 | pass | Resolver returned the tree-owned set, matched the 48-path enumeration, and exempted nothing for unknown or different commands. |
| Q-05 | pass | Ungranted governed, hand-edited derived, wrong-Spec record, and folded-authorization cases remained refused. |
| Q-06 | pass | Fourteen predicate cases and the live authorization-corpus contract passed. |
| Q-07 | pass | Both Spec artifacts now carry ADR-0131's applicable/no-mutation wording and strict checking accepts them. |
| Q-08 | pass | `CONTEXT.md` now defines `Governed Path` with the semantics the checker emits. |
| Q-09 | fail | F-001: required `make verify` exits 2 because a daemon integration fixture still expects an ordinary ungoverned path to block. |
| Q-10 | pass | Audit stage, PRD template, ownership schemas, and authorization frontmatter contract did not change. |
| Q-11 | pass | Docs contracts passed; the QA directory contains no gitlink, built binary, or scratch repository. |

## Mechanical skips

| Detector | Missing artifact |
| --- | --- |
| authorization bounded paths | tooling authorization |
| consequent-fix commit order | consequent-fix declaration |
| QA evidence paths | docs/specs/0114-an-audit-that-reads-the-authorization-it-was-given/qa/evidence |

## Detailed evidence

### Q-01

Actor and surface: Spec author; built `roundfix spec check` CLI and docs. Steps:
run the current Spec through both strict full checking and PRD-stage checking.
Expected: no `SC-TOOLING-UNBOUNDED` finding. Independent confirmation: run the
focused accepted-row case beside the three surviving refusal cases. Persistence:
repeat in fresh CLI and Go processes. Evidence: both CLI commands exited 0 with
no findings, and `TestToolingRowStatesApplicability` passed the template wording,
unbounded-mutation, wrong-Spec-record, and prose-only-record subtests.

```yaml
inputs:
  - kind: repository_path
    ref: internal/speccheck/constraints.go
  - kind: repository_path
    ref: internal/speccheck/report_test.go
  - kind: repository_path
    ref: .agents/skills/write-prd/references/prd-template.md
```

### Q-02

Actor and surface: Supervisor; backend changed-path audit over Git history.
Steps: replay the Spec 0095 Task commit and the historical authorized-asset and
ordinary-Go split pair. Expected: ordinary paths remain unaudited and authorized
assets pass without splitting the Task. Independent confirmation: resolve each
commit directly with `git cat-file`. Persistence: rerun in a fresh Go process.
Evidence: the historical subtest passed, and Git resolved
`28acf39cc193ad490646cb5a1d23500e0c08c273`,
`419a4661ac769ff7ee6ce5423bd795185c859d01`, and
`65c51ebf2e19220ff50d25fe03be809fcdf353f0` as commits.

```yaml
inputs:
  - kind: repository_path
    ref: internal/speccheck/mechanical.go
  - kind: repository_path
    ref: internal/speccheck/mechanical_test.go
  - kind: external_repository
    ref: this repository's pre-Spec Git history
```

### Q-03

Actor and surface: Supervisor; backend audit over evidence the Spec did not
author. Steps: replay the Spec 0095 refusal and the 2026-08-13 sanctioned-fallout
refusal. Expected: both commits resolve and pass under the corrected audit
without widening the grant. Independent confirmation: inspect the Git objects
separately. Persistence: rerun from the repository object database. Evidence:
the corresponding audit subtests passed, and Git resolved
`28acf39cc193ad490646cb5a1d23500e0c08c273` and
`c80e1266658929f68e8046af82f88e13392dc56d` as commits. The latter accepted nine
derived paths from a command-only grant. This is the required outside evidence.

```yaml
inputs:
  - kind: repository_path
    ref: internal/speccheck/mechanical.go
  - kind: repository_path
    ref: internal/speccheck/mechanical_test.go
  - kind: external_repository
    ref: this repository's pre-Spec Git history
```

### Q-04

Actor and surface: maintainer; authorization docs and ownership resolver. Steps:
read the grant's command and compare the resolver's complete output set with the
record's enumeration. Expected: the tree supplies all owned outputs, while an
unknown or different command exempts nothing. Independent confirmation: inspect
the authorization diff and ownership files separately. Persistence: rerun in a
fresh Go process. Evidence: `TestOutputsForCommand` passed all four cases; its
equality case matched all 48 paths. The authorization-record change adds only 20
missing `outputs:` entries and changes no frontmatter.

```yaml
inputs:
  - kind: repository_path
    ref: docs/workflow/authorizations/2026-08-06-proof-cost.md
  - kind: repository_path
    ref: internal/baseline/**/_ownership.yml
  - kind: repository_path
    ref: internal/baseline/derived_ownership.go
  - kind: repository_path
    ref: internal/baseline/derived_ownership_test.go
```

### Q-05

Actor and surface: Supervisor; backend changed-path audit. Steps: exercise an
ungranted governed path, a hand-edited derived value, a record omitting the Spec,
and a Task commit carrying its own authorization. Expected: all four are refused
with the offending path or record named. Independent confirmation: run them in
the same six-case suite as the two positive histories. Persistence: rerun in a
fresh Go process. Evidence: every named negative subtest passed after observing
its required refusal; the positive subtests passed in the same run.

```yaml
inputs:
  - kind: repository_path
    ref: internal/speccheck/mechanical.go
  - kind: repository_path
    ref: internal/speccheck/mechanical_test.go
```

### Q-06

Actor and surface: Supervisor; governed-path predicate and repository contract.
Steps: exercise every governed kind, three ordinary-path negatives, the live
authorization corpus, an unmatched synthetic path, and the no-records branch.
Expected: governed paths match, ordinary paths do not, unmatched paths name their
record, and no records skip. Independent confirmation: the repository-contract
test reads the live authorization directory. Persistence: rerun both suites in
fresh Go processes. Evidence: `TestGovernedPath` passed eleven governed-kind and
three ordinary-path cases. `TestEveryBoundedPathIsGoverned` passed its live and
synthetic cases; the deliberate empty directory reported `SKIP` as required.

```yaml
inputs:
  - kind: repository_path
    ref: internal/speccheck/governed.go
  - kind: repository_path
    ref: internal/speccheck/governed_test.go
  - kind: repository_path
    ref: internal/speccheck/governed_repocontract_test.go
  - kind: repository_path
    ref: docs/workflow/authorizations/*.md
```

### Q-07

Actor and surface: Spec author; PRD and TechSpec docs through the current built
CLI. Steps: reread both Tooling authority rows and run strict checking. Expected:
both say the constraint is applicable and no protected mutation is proposed or
authorized. Independent confirmation: exact text search plus the CLI. Persistence:
reread both files. Evidence: `_prd.md:48` and `_techspec.md:65` now begin
`Tooling authority: applicable`; both continue with the no-mutation declaration,
and strict checking exited 0. This closes prior F-001.

```yaml
inputs:
  - kind: repository_path
    ref: docs/specs/0114-an-audit-that-reads-the-authorization-it-was-given/_prd.md
  - kind: repository_path
    ref: docs/specs/0114-an-audit-that-reads-the-authorization-it-was-given/_techspec.md
```

### Q-08

Actor and surface: maintainer; domain glossary. Steps: read `Governed Path` in
`CONTEXT.md` and compare it with the checker and ADR-0130. Expected: the glossary
distinguishes governed tooling paths from ordinary paths and retains every path
historically bounded by an authorization. Independent confirmation: source search
in `GovernedPath`. Persistence: reread the glossary. Evidence: `CONTEXT.md:31`
defines all three semantics. This closes prior F-002.

```yaml
inputs:
  - kind: repository_path
    ref: CONTEXT.md
  - kind: repository_path
    ref: internal/speccheck/governed.go
  - kind: repository_path
    ref: docs/adr/0130-the-audit-judges-governed-paths-and-history-keeps-the-set-honest.md
```

### Q-09

Actor and surface: maintainer; complete repository Verification. Steps: run the
required `make verify`, isolate each non-environment failure, and compare the
failing fixture with the accepted governed-path contract. Expected: exit 0 with
no failed package. Independent confirmation: rerun the daemon test alone and
compare it with the passing mechanical negative control. Persistence: both runs
use the current head. Evidence: `make verify` exited 2 and the focused daemon
test reproduced the same failure. F-001 records the code-caused failure. The two
force-stop failures are separately environment-caused by the sandbox's explicit
`operation not permitted` process-table denial and do not invalidate the
feature-specific rows.

```yaml
inputs:
  - kind: repository_path
    ref: internal/daemon/task_engine_test.go
  - kind: repository_path
    ref: internal/speccheck/mechanical.go
  - kind: repository_path
    ref: internal/speccheck/governed.go
```

### Q-10

Actor and surface: maintainer; fixed-range Git audit and documentation. Steps:
inspect every Task commit with `git diff-tree`, inspect the full Spec range for
protected tooling or scope creep, and run neighboring package tests. Expected:
the audit remains in its mechanical stage, the PRD template and `_ownership.yml`
schemas stay unchanged, authorization frontmatter stays unchanged, and no
protected tooling mutation appears. Independent confirmation: compare exact
changed paths with the PRD Non-Goals. Persistence: rerun from fixed commit IDs.
Evidence: clean `git -c core.fsmonitor=false diff-tree` output recorded every
Task commit's paths. No `write-prd` skill or ownership record changed; the only
authorization edit adds 20 enumerated outputs. Neighboring packages exited 0.

```yaml
inputs:
  - kind: repository_path
    ref: internal/speccheck/**
  - kind: repository_path
    ref: internal/baseline/**/_ownership.yml
  - kind: repository_path
    ref: .agents/skills/write-prd/**
  - kind: repository_path
    ref: docs/workflow/authorizations/*.md
```

### Q-11

Actor and surface: QA auditor; report and Git index. Steps: close the report, run
the docs contract, inspect required frontmatter and terminal rows, and confirm the
QA directory contains no gitlink, built binary, or scratch repository. Expected:
the report contract passes and this Markdown report is the only new QA path.
Independent confirmation: Git mode and status inspection. Persistence: reread the
closed report. Evidence: `make verify-docs` exited 0 after the docs contract,
repository contract, corpus budget, and all active-Spec checks. The report carries
all required frontmatter, Q-01 through Q-11 are terminal, and Git/file inspection
found no mode `160000`, executable, binary, or nested repository under `qa/`.

```yaml
inputs:
  - kind: repository_path
    ref: docs/specs/0114-an-audit-that-reads-the-authorization-it-was-given/qa/qa-report-2026-08-15-01.md
```

## Findings

### F-001 — Daemon QA fixture still treats an ordinary path as governed

Impact: Blocks-Completion. Actor: maintainer running repository Verification.
Journey: Q-09, full gate. Expected: `make verify` exits 0 while the mechanical
audit refuses ungranted governed paths and ignores ordinary paths. Actual:
`internal/daemon.TestQAMechanicalRequestSelectsTheAuthorizedTaskCommit` adds
`outside.txt` and expects one `QA-AUTH-PATHS` finding, but the corrected audit
returns no finding because `outside.txt` is not governed. The assertion fails at
`internal/daemon/task_engine_test.go:938`, leaving `internal/daemon` and the
repository Verification red.

Reproduction from the repository test surface:

```text
GOCACHE=/tmp/roundfix-0114-qa-rerun-gocache go test -count=1 \
  ./internal/daemon \
  -run '^TestQAMechanicalRequestSelectsTheAuthorizedTaskCommit$' -v
```

The command consistently exits 1. Root cause: Task 05 changed the audit to judge
only governed paths, as ADR-0130 requires, but the older daemon integration
fixture from `a2a4c86b7` retained an ordinary `outside.txt` negative control and
its old expectation. The equivalent mechanical unit negative control already
uses `.golangci.yml`, an ungranted governed path, and passes. The corrective work
must update the daemon seam's negative-control input to a governed path while
retaining the assertion that the authorized Task commit is the one selected.
Affected row: Q-09.

## Blocked and skipped

No QA matrix row is blocked or skipped. The sandbox-denied force-stop tests are
environment-caused diagnostics inside the already failed full-gate row; F-001
independently makes that row fail. The materialized mechanical skips above remain
part of the Daemon's pre-agent evidence and do not represent omitted QA journeys.

## Coverage

Executed 11/11 rows: 10 passed and 1 failed. All 4 user stories, 3 goals, 4 core
features, 3 Non-Goals, and every Task acceptance criterion map to Q-01 through
Q-11. The required outside-evidence row passed using pre-Spec Git commits.
`rows_blocked_environment=0`, `rows_blocked_finding=0`, and
`rows_blocked_declared=0`. Findings by impact: 1 Blocks-Completion; 0 Data-Loss,
Trust-Damage, Friction, or Cosmetic.

## Final verdict

Fail: all feature-specific and documentation journeys pass, including both
outside-history replays and the prior gate's two corrective rows, but the
required repository Verification remains red on F-001. Replace the daemon seam's
ordinary `outside.txt` negative control with an ungranted governed path, retain
its authorized-Task-selection assertion, and rerun the full gate.
