---
spec: 0114-an-audit-that-reads-the-authorization-it-was-given
date: 2026-08-15
build: ebd2e4880b4352013414795847b8319608ca7b9b
status: closed
verdict: pass
rows_blocked_environment: 1
rows_blocked_finding: 0
rows_blocked_declared: 0
surfaces: [backend, docs]
---

# QA report — An audit that reads the authorization it was given

## Scope and environment

Full second same-day rerun on build `ebd2e4880b4352013414795847b8319608ca7b9b`.
The prior report failed because the Daemon QA fixture still treated an ordinary
path as governed. This rerun starts with that failed row, then re-observes the
complete matrix.

The actors are a Spec author, a Supervisor running the changed-path audit, and a
maintainer reading an authorization record. The public surfaces are the freshly
built `roundfix spec check` CLI, the backend audit and ownership resolver through
their Go entry points, repository history through `git`, and the Spec and glossary
as documentation.

The current CLI is `/tmp/roundfix-0114-qa02`, built with
`GOCACHE=/tmp/roundfix-0114-qa02-gocache`. The installed `roundfix` is stale and
still reports the pre-Spec `SC-TOOLING-UNBOUNDED` behavior; it is recorded as a
parity deviation and is not current-build evidence. No Pull Request is open, but
the Spec declares no Pull Request journey. No credentials, network service, or
external database are required. The authored QA node `task_06` is terminal and
all dependencies (`task_01`, `task_05`, and `task_07`) are completed.

`/tmp/roundfix-0114-qa02 spec check
0114-an-audit-that-reads-the-authorization-it-was-given --strict` exited 0 with
no findings. It skipped only `SC-REF-UNRESOLVED` because the Spec has no optional
`references/_index.md`.

## Static gate

- `GOCACHE=/tmp/roundfix-0114-qa02-gocache make verify` exited 2. The
  current-build Daemon package passed, closing the prior report's code-caused
  failure. Two `internal/cli` force-stop tests could not read the host process
  table because the sandbox returned `operation not permitted`; the gate does
  not retry-loop that explicit denial. One parallel `internal/agent` ACPX probe
  was killed, then passed in a fresh isolated run in 0.02 seconds, classifying
  that symptom as transient environment/resource pressure rather than a code
  defect.
- `go test -count=1 ./internal/daemon -run
  '^TestQAMechanicalRequestSelectsTheAuthorizedTaskCommit$' -v` exited 0 and
  reported both `ungranted_governed_path_blocks` and
  `ordinary_path_does_not_block` as passing subtests.
- `/tmp/roundfix-0114-qa02 spec check
  0114-an-audit-that-reads-the-authorization-it-was-given --strict` exited 0
  with no findings. `SC-REF-UNRESOLVED` skipped because the Spec has no optional
  `references/_index.md`.
- `GOCACHE=/tmp/roundfix-0114-qa02-gocache make verify-docs` exited 0 after
  docs contracts, repository contracts, the corpus budget, and every active-Spec
  consistency check passed.

## Mechanical findings

None.

## Results

| # | Status | Provenance |
| - | --- | --- |
| Q-01 | pass | Current CLI accepted the PRD-stage no-mutation wording; four focused checker cases preserved all required refusals. |
| Q-02 | pass | Historical ordinary-Go and authorized-asset commits resolved from Git and passed one audit without a split. |
| Q-03 | pass | Both required pre-Spec refusal histories resolved from Git and passed the corrected audit. |
| Q-04 | pass | Resolver matched the 48-path authorization enumeration and exempted nothing for unknown or different commands. |
| Q-05 | pass | Ungranted governed, hand-edited derived, wrong-Spec, and folded-authorization cases remained refused. |
| Q-06 | pass | Fourteen predicate cases and the live authorization-corpus contract passed. |
| Q-07 | pass | Both Spec artifacts carry the applicable/no-mutation wording and the current strict checker accepts them. |
| Q-08 | pass | `CONTEXT.md` defines `Governed Path` with the checker and ADR-0130 semantics. |
| Q-09 | blocked (environment: sandbox denies process-table reads) | Full Verification reached the corrected Daemon package successfully; the prior failing seam and transient ACPX probe pass in fresh isolated runs. |
| Q-10 | pass | Every Task commit was inspected; no protected tooling mutation or declared Non-Goal shipped. |
| Q-11 | pass | Docs contracts passed; QA contains only the seeded regular Markdown report as new evidence. |

## Mechanical skips

| Detector | Missing artifact |
| --- | --- |
| authorization bounded paths | tooling authorization |
| consequent-fix commit order | consequent-fix declaration |
| QA evidence paths | docs/specs/0114-an-audit-that-reads-the-authorization-it-was-given/qa/evidence |

## Detailed evidence

### Q-01

Actor and surface: Spec author; freshly built CLI and checker. Steps: run strict
full and PRD-stage checks, then the accepted wording beside surviving refusals.
Expected: the no-mutation wording passes while mutation without bounds,
wrong-Spec authorization, and prose-only authorization fail. Independent
confirmation and persistence: repeat in fresh CLI and Go processes. Evidence:
the current CLI's PRD-stage strict check exited 0 with no findings.
`TestToolingRowStatesApplicability` then exited 0 after the template wording,
unbounded mutation, wrong-Spec record, and prose-only record all ran as named
passing subtests.

```yaml
inputs:
  - kind: repository_path
    ref: internal/speccheck/constraints.go
  - kind: repository_path
    ref: internal/speccheck/report_test.go
  - kind: repository_path
    ref: .agents/skills/write-prd/references/prd-template.md
```

### Q-02

Actor and surface: Supervisor; changed-path audit over Git history. Steps: replay
the Spec 0095 Task commit and its historical split pair. Expected: ordinary paths
remain unaudited and the authorized asset passes without a split. Independent
confirmation: resolve each commit directly with `git cat-file`. Persistence:
rerun in a fresh Go process. Evidence: the historical split subtest exited 0,
and Git independently resolved `28acf39cc193ad490646cb5a1d23500e0c08c273`,
`419a4661ac769ff7ee6ce5423bd795185c859d01`, and
`65c51ebf2e19220ff50d25fe03be809fcdf353f0` as commit objects.

```yaml
inputs:
  - kind: repository_path
    ref: internal/speccheck/mechanical.go
  - kind: repository_path
    ref: internal/speccheck/mechanical_test.go
  - kind: external_repository
    ref: this repository's pre-Spec Git history
```

### Q-03

Actor and surface: Supervisor; audit over evidence the Spec did not author.
Steps: replay the Spec 0095 refusal and the 2026-08-13 sanctioned-fallout
refusal. Expected: both commits resolve and pass without widening the grant.
Independent confirmation: inspect both Git objects. Persistence: rerun from the
repository object database. Evidence: both historical audit subtests exited 0,
and Git independently resolved the Spec 0095 commit
`28acf39cc193ad490646cb5a1d23500e0c08c273` and the 2026-08-13 refusal commit
`c80e1266658929f68e8046af82f88e13392dc56d` as commit objects. The latter
accepted nine derived paths from a command-only grant. This is the required
outside evidence.

```yaml
inputs:
  - kind: repository_path
    ref: internal/speccheck/mechanical.go
  - kind: repository_path
    ref: internal/speccheck/mechanical_test.go
  - kind: external_repository
    ref: this repository's pre-Spec Git history
```

### Q-04

Actor and surface: maintainer; authorization docs and ownership resolver. Steps:
read the grant's command and compare the complete resolved set with the existing
enumeration. Expected: all 48 owned paths match and unknown or different commands
exempt nothing. Independent confirmation: inspect the record and ownership files.
Persistence: rerun in a fresh Go process. Evidence: `TestOutputsForCommand`
exited 0 after all four named subtests ran. Its equality case compared the
ownership-derived `make baseline-digests` set with the authorization's 48 lines;
the unknown-command and different-command cases also passed. Separate source
inspection confirmed that the record describes outputs as command effects, not
freely editable bounded files.

```yaml
inputs:
  - kind: repository_path
    ref: docs/workflow/authorizations/2026-08-06-proof-cost.md
  - kind: repository_path
    ref: internal/baseline/**/_ownership.yml
  - kind: repository_path
    ref: internal/baseline/derived_ownership.go
  - kind: repository_path
    ref: internal/baseline/derived_ownership_test.go
```

### Q-05

Actor and surface: Supervisor; changed-path audit. Steps: exercise an ungranted
governed path, hand-edited derived value, wrong-Spec record, and Task commit that
carries its authorization. Expected: all four are refused with the offending
path or record named. Independent confirmation and persistence: run beside both
positive histories in a fresh process. Evidence: `TestAuditJudgesTheGrant`
exited 0 after all six named cases ran. The four negative cases passed only after
observing the required refusal for an ungranted `.golangci.yml`, a hand-edited
derived asset, a record omitting the Spec, and a Task commit carrying its grant.

```yaml
inputs:
  - kind: repository_path
    ref: internal/speccheck/mechanical.go
  - kind: repository_path
    ref: internal/speccheck/mechanical_test.go
```

### Q-06

Actor and surface: Supervisor; governed-path predicate and repository contract.
Steps: exercise every governed kind, the ordinary source/test/Spec negatives,
the live corpus, an unmatched synthetic path, and no-records behavior. Expected:
all classifications and diagnostics match the contract. Independent confirmation:
the repository-contract test reads live authorization records. Persistence:
rerun both suites. Evidence: `TestGovernedPath` exited 0 after eleven governed
kinds and the ordinary Go source, ordinary test, and Spec-document negatives all
ran. `TestEveryBoundedPathIsGoverned` exited 0: the live corpus and synthetic
unmatched-path cases passed, while the deliberate empty-directory case reported
`SKIP`, which is its required observable.

```yaml
inputs:
  - kind: repository_path
    ref: internal/speccheck/governed.go
  - kind: repository_path
    ref: internal/speccheck/governed_test.go
  - kind: repository_path
    ref: internal/speccheck/governed_repocontract_test.go
  - kind: repository_path
    ref: docs/workflow/authorizations/*.md
```

### Q-07

Actor and surface: Spec author; PRD and TechSpec through the current CLI. Steps:
reread both Tooling authority rows and run strict checking. Expected: both use
the applicable/no-mutation wording and pass. Independent confirmation: exact
text search plus CLI. Persistence: reread both files. Evidence: multiline exact
search found the settled wording at `_prd.md:48` and `_techspec.md:65`, including
the declaration that no protected tooling mutation is proposed or authorized.
The current-build strict full and PRD-stage checks both exited 0 with no findings.

```yaml
inputs:
  - kind: repository_path
    ref: docs/specs/0114-an-audit-that-reads-the-authorization-it-was-given/_prd.md
  - kind: repository_path
    ref: docs/specs/0114-an-audit-that-reads-the-authorization-it-was-given/_techspec.md
```

### Q-08

Actor and surface: maintainer; domain glossary. Steps: compare `Governed Path`
in `CONTEXT.md` with the checker and ADR-0130. Expected: the glossary records the
emitted distinction and historical-union semantics. Independent confirmation:
source search in `GovernedPath`. Persistence: reread the glossary. Evidence:
`CONTEXT.md:31` defines the governed tooling kinds, distinguishes ordinary
source, test, and documentation paths, and records the historical-union rule.
The same search reached accepted ADR-0130, independently confirming the term's
meaning and ownership.

```yaml
inputs:
  - kind: repository_path
    ref: CONTEXT.md
  - kind: repository_path
    ref: internal/speccheck/governed.go
  - kind: repository_path
    ref: docs/adr/0130-the-audit-judges-governed-paths-and-history-keeps-the-set-honest.md
```

### Q-09

Actor and surface: maintainer; repository Verification and Daemon integration.
Steps: rerun the previously failing Daemon fixture, then `make verify`.
Expected: the fixture proves both governed refusal and ordinary-path permission,
and Verification exits 0. Independent confirmation: inspect both named subtests.
Persistence: fresh `-count=1` test and full gate. Evidence: the isolated Daemon
test exited 0 with both named directions passing, and the full gate reported
`ok roundfix/internal/daemon`. The isolated ACPX test also exited 0. Full
Verification remains unreachable in this sandbox because both force-stop tests
receive an explicit `operation not permitted` while reading the process table.
Equivalent evidence is the current-build pass for every other package, the
fresh corrected Daemon seam, and the prior supervised unsandboxed incremental
Verification recorded by completed Tasks for the unchanged force-stop surface.
Unblocking action: run `GOCACHE=<writable-cache> make verify` in a session with
host process-table access.

```yaml
inputs:
  - kind: repository_path
    ref: internal/daemon/task_engine_test.go
  - kind: repository_path
    ref: internal/speccheck/mechanical.go
  - kind: repository_path
    ref: internal/speccheck/governed.go
```

### Q-10

Actor and surface: maintainer; fixed-range Git audit and docs. Steps: inspect
every Task commit with `git diff-tree`, the full Spec range, and neighboring
packages. Expected: no protected tooling mutation, audit relocation, template or
ownership-schema change, authorization-frontmatter change, or wider grant.
Independent confirmation: compare exact changed paths with Non-Goals.
Persistence: fixed commit identifiers. Evidence: `git diff-tree` inspected all
Task and corrective commits from `6064cbc5` through `ebd2e488`. Their changes
are Task/Spec docs, production Go, Go tests, and the existing authorization
record; none creates or changes repository-tooling configuration, scripts,
ignore files, plugin declarations, or version pins, so no commit-dependent
tooling-authorization row applies. Fixed-range path checks found no change to
either `write-prd` skill tree or any `_ownership.yml`/`_ownership.yaml` file.
The authorization diff contains only 20 added `outputs:` entries and no
frontmatter change. `git diff --check` exited 0, and fresh neighboring-package
tests exited 0 for `internal/speccheck`, `internal/suiteguard`,
`internal/suiteguardcontract`, and `internal/baseline`.

```yaml
inputs:
  - kind: repository_path
    ref: internal/speccheck/**
  - kind: repository_path
    ref: internal/baseline/**/_ownership.yml
  - kind: repository_path
    ref: .agents/skills/write-prd/**
  - kind: repository_path
    ref: docs/workflow/authorizations/*.md
```

### Q-11

Actor and surface: QA auditor; report and Git index. Steps: close this report,
run docs contracts, inspect terminal rows and frontmatter, and confirm QA has no
gitlink, executable, binary, or scratch repository. Expected: report contract
passes and this Markdown file is the only new QA path. Independent confirmation:
Git mode and status inspection. Persistence: reread the closed report. Evidence:
`make verify-docs` exited 0 before closeout. Git index inspection found no mode
`160000` under QA; `stat` reports this report as a non-executable regular file,
and scoped status names it as the only new QA path. A final-byte
`make verify-docs` rerun exited 0 after closeout.

```yaml
inputs:
  - kind: repository_path
    ref: docs/specs/0114-an-audit-that-reads-the-authorization-it-was-given/qa/qa-report-2026-08-15-02.md
```

## Findings

None.

## Blocked and skipped

Q-09 is `blocked (environment: sandbox denies process-table reads)`. The exact
denial is `operation not permitted` from both force-stop tests while enumerating
the non-session owner's process tree. The sandbox must grant host process-table
access for `make verify` to exercise those tests. Equivalent observed and
supervised evidence is available: `internal/cli` is byte-unchanged across the
entire Spec range, completed Tasks record an unsandboxed incremental Verification
pass for that surface, every other current-build package passed, and both the
corrected Daemon seam and the transient ACPX probe passed in fresh isolated runs.

No row is finding-blocked, declared-blocked, or skipped. The materialized
mechanical skips remain Daemon pre-agent evidence and do not represent omitted
QA journeys.

## Coverage

Executed 11/11 rows: 10 passed and 1 is environment-blocked with equivalent
evidence. All 4 user stories, 3 goals, 4 core features, 3 Non-Goals, and every
Task acceptance criterion map to Q-01 through Q-11. The required outside-evidence
row passed using four pre-Spec Git commits. `rows_blocked_environment=1`,
`rows_blocked_finding=0`, and `rows_blocked_declared=0`. Findings by impact: 0
Blocks-Completion, Data-Loss, Trust-Damage, Friction, or Cosmetic.

## Final verdict

Pass: all runnable rows pass, the sole environment-blocked row carries an exact
cause plus equivalent observed and supervised evidence, no row is
finding-blocked, declared-blocked, or skipped, and the report's evidence paths
resolve.
