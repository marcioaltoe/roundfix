---
spec: 0114-an-audit-that-reads-the-authorization-it-was-given
date: 2026-08-15
build: 95abd5de0c234f51ccfd240433dcc3c22f9f6b3c
status: closed
verdict: fail
rows_blocked_environment: 0
rows_blocked_finding: 0
rows_blocked_declared: 0
surfaces: [backend, docs]
---

# QA report — An audit that reads the authorization it was given

## Scope and environment

Full gate for the current Run Worktree build at
`95abd5de0c234f51ccfd240433dcc3c22f9f6b3c`. The intended actors are a Spec
author, a Supervisor running the changed-path audit, and a maintainer reading an
authorization record. The exercised surfaces are the built `roundfix spec check`
CLI, the backend audit and ownership resolver through their current focused Go
entry points, repository history through `git`, and the Spec and glossary as a
documentation reader.

The first build attempt could not use Go's default cache because the sandbox
denied access to `~/Library/Caches/go-build`. The current source was then built
successfully as `/tmp/roundfix-0114-qa` with
`GOCACHE=/tmp/roundfix-0114-qa-gocache`. No Pull Request exists, but this Spec
declares no Pull Request journey. No credentials, network service, or external
database are required.

The QA Task is terminal and depends on completed `task_01` and `task_05`.
`/tmp/roundfix-0114-qa spec check 0114-an-audit-that-reads-the-authorization-it-was-given --strict`
exited 0 with no findings. It skipped only `SC-REF-UNRESOLVED` because the Spec
has no `references/_index.md`; no acceptance row depends on that optional index.

## Static gate

- `GOCACHE=/tmp/roundfix-0114-qa-gocache make verify` completed successfully on
  the current build. The gate ran `go test -parallel 16 ./...`, repository skill
  checks, and the build; RTK condensed the passing package output.
- `/tmp/roundfix-0114-qa spec check
  0114-an-audit-that-reads-the-authorization-it-was-given --strict` exited 0
  with no findings. `SC-REF-UNRESOLVED` skipped because this Spec has no optional
  `references/_index.md`.
- `go test -count=1 ./internal/speccheck -run
  '^TestAuditJudgesTheGrant$' -v` exited 0 after all six named subtests passed.
- `go test -count=1 -tags repocontract ./internal/speccheck -run
  '^TestEveryBoundedPathIsGoverned$' -v` exited 0. The live-corpus and unmatched
  cases passed; the deliberate no-records case reported `SKIP`, which is its
  expected observable.
- `go test -count=1 ./internal/baseline -run '^TestOutputsForCommand$' -v`
  exited 0 after all four resolver cases passed.
- `go test -count=1 ./internal/suiteguard ./internal/suiteguardcontract` exited
  0; `suiteguard` passed and `suiteguardcontract` has no direct tests.
- `go test -count=1 ./internal/speccheck -run
  '^(TestMechanicalReportShape|TestMechanicalEvidencePath)$'` exited 0.
- `GOCACHE=/tmp/roundfix-0114-qa-gocache make verify-docs` exited 0 after the
  docs contract, repository contract, corpus budget, and active-Spec checks.

## Mechanical findings

None.

## Results

| # | Status | Provenance |
| - | --- | --- |
| Q-01 | pass | Built CLI accepted the template wording; four focused checker cases passed. |
| Q-02 | pass | Historical ordinary-Go and authorized-asset replay passed without a split. |
| Q-03 | pass | Both outside-history refusals resolved from Git and passed the corrected audit. |
| Q-04 | pass | Resolver returned the tree-owned set and matched all 48 enumerated paths. |
| Q-05 | pass | Four unsafe cases remained refused by the six-case audit suite. |
| Q-06 | pass | Fourteen predicate cases and the live authorization-corpus contract passed. |
| Q-07 | fail | F-001: both Spec artifacts retain the superseded `not applicable` workaround. |
| Q-08 | fail | F-002: `Governed Path` is absent from `CONTEXT.md`. |
| Q-09 | pass | Full Verification and neighboring package regression checks passed. |
| Q-10 | pass | Audit stage, template, ownership schemas, and authorization frontmatter contract did not change. |
| Q-11 | pass | Closed report has terminal rows and QA contains no gitlink, binary, or scratch repository. |

## Mechanical skips

| Detector | Missing artifact |
| --- | --- |
| authorization bounded paths | tooling authorization |
| consequent-fix commit order | consequent-fix declaration |
| QA Report structure | QA Report |
| QA evidence paths | QA Report |

## Detailed evidence

### Q-01

Actor and surface: Spec author; built `roundfix spec check` CLI and docs.
Steps: check the template's exact no-mutation Tooling authority wording through
the PRD-stage checker. Expected: exit 0 with no `SC-TOOLING-UNBOUNDED` finding.
Independent confirmation: the focused checker suite exercises the accepted row
and the three surviving refusals. Persistence: rerun the CLI against the same
repository state. Evidence: in a disposable clone, the PRD row used the
template's exact `applicable — no protected tooling mutation proposed or
authorized` wording; `/tmp/roundfix-0114-qa spec check ... --stage prd --strict`
exited 0 with no findings. `TestToolingRowStatesApplicability` then passed its
accepted wording and three refusal subtests in a fresh process.

```yaml
inputs:
  - kind: repository_path
    ref: internal/speccheck/constraints.go
  - kind: repository_path
    ref: internal/speccheck/report_test.go
  - kind: repository_path
    ref: .agents/skills/write-prd/references/prd-template.md
```

### Q-02

Actor and surface: Supervisor; backend changed-path audit over Git history.
Steps: replay the historical authorized-asset and ordinary-Go-file case through
`TestAuditJudgesTheGrant`. Expected: the ordinary path is not audited and the
authorized asset passes without splitting the Task. Independent confirmation:
the replay resolves the historical commits from Git rather than a Spec-authored
fixture. Persistence: rerun against a fresh Go test process. Evidence:
`TestAuditJudgesTheGrant/historical_authorized_asset_and_ordinary_Go_split_now_share_one_audit`
passed. Git resolved `28acf39cc193ad490646cb5a1d23500e0c08c273` plus the
actual split pair `419a4661ac769ff7ee6ce5423bd795185c859d01` and
`65c51ebf2e19220ff50d25fe03be809fcdf353f0` as commit objects.

```yaml
inputs:
  - kind: repository_path
    ref: internal/speccheck/mechanical.go
  - kind: repository_path
    ref: internal/speccheck/mechanical_test.go
  - kind: external_repository
    ref: this repository's pre-Spec Git history
```

### Q-03

Actor and surface: Supervisor; backend changed-path audit over Git history.
Steps: replay both refusals that originated outside this Spec: the Spec 0095
Task history and the 2026-08-13 sanctioned-regeneration refusal. Expected: both
commits resolve and pass under the corrected audit without widening the grant.
Independent confirmation: inspect each Git object directly and run the focused
audit in a fresh process. Persistence: repeat from the current repository object
database. Evidence: `28acf39cc193ad490646cb5a1d23500e0c08c273` (Spec 0095)
and `c80e1266658929f68e8046af82f88e13392dc56d` (2026-08-13 refusal)
both resolved as commits. The two corresponding historical subtests passed; the
regeneration case took 36.09 seconds and accepted nine derived paths from a
command-only grant. This is the Spec's required outside evidence.

```yaml
inputs:
  - kind: repository_path
    ref: internal/speccheck/mechanical.go
  - kind: repository_path
    ref: internal/speccheck/mechanical_test.go
  - kind: external_repository
    ref: this repository's pre-Spec Git history
```

### Q-04

Actor and surface: maintainer; authorization docs and backend ownership resolver.
Steps: read the grant's command and compare the resolver's complete output set
with the record's enumeration. Expected: the record states the permitted cause,
the tree supplies 48 outputs, and unknown or different commands exempt nothing.
Independent confirmation: inspect the authorization record and ownership files
separately from the focused resolver suite. Persistence: rerun in a fresh Go test
process. Evidence: `TestOutputsForCommand` passed four cases. Its equality case
compared the `make baseline-digests` ownership-derived set with the 2026-08-06
record and passed with 48 paths; its unknown-command and different-command cases
also passed. The record states the command in its `Sanctioned regeneration`
block and explains that outputs are not freely editable bounded files.

```yaml
inputs:
  - kind: repository_path
    ref: docs/workflow/authorizations/2026-08-06-proof-cost.md
  - kind: repository_path
    ref: internal/baseline/**/_ownership.yml
  - kind: repository_path
    ref: internal/baseline/derived_ownership.go
  - kind: repository_path
    ref: internal/baseline/derived_ownership_test.go
```

### Q-05

Actor and surface: Supervisor; backend changed-path audit.
Steps: exercise an unauthorized governed path, a hand-edited derived value, a
record that omits the Spec, and a Task commit that carries its own authorization.
Expected: all four are refused with the offending path or authorization named.
Independent confirmation: six named audit subtests run, including the two
positive cases. Persistence: rerun in a fresh Go test process. Evidence:
`TestAuditJudgesTheGrant` exited 0. Its unsafe cases passed only after observing
the expected refusal for an ungranted `.golangci.yml`, a hand-edited derived
asset, a record omitting the Spec, and a Task commit carrying its own grant.

```yaml
inputs:
  - kind: repository_path
    ref: internal/speccheck/mechanical.go
  - kind: repository_path
    ref: internal/speccheck/mechanical_test.go
```

### Q-06

Actor and surface: Supervisor; backend governed-path predicate and repository
contract. Steps: exercise every governed kind, ordinary source/test/Spec paths,
the live authorization corpus, an unmatched synthetic path, and no-records
behavior. Expected: governed paths match, ordinary paths do not, unmatched paths
name their record, and no records skip. Independent confirmation: the
repository-contract test reads the live authorization directory. Persistence:
rerun both suites in fresh Go processes. Evidence: `TestGovernedPath` passed
eleven governed-kind cases and the three required ordinary-path negatives.
`TestEveryBoundedPathIsGoverned` passed against the live corpus and the synthetic
unmatched-path diagnostic; its deliberately empty authorization directory
reported `SKIP` as required.

```yaml
inputs:
  - kind: repository_path
    ref: internal/speccheck/governed.go
  - kind: repository_path
    ref: internal/speccheck/governed_test.go
  - kind: repository_path
    ref: internal/speccheck/governed_repocontract_test.go
  - kind: repository_path
    ref: docs/workflow/authorizations/*.md
```

### Q-07

Actor and surface: Spec author; PRD and TechSpec documentation through the
built strict checker. Steps: read both Tooling authority rows and run strict Spec
checking. Expected: both rows say the constraint is applicable and that no
protected tooling mutation is proposed or authorized. Independent confirmation:
exact text search plus the CLI. Persistence: reread both files. Evidence:
`rg -n 'Tooling authority:'` found `not applicable` at `_prd.md:48` and
`_techspec.md:65`. The strict CLI still exits 0, so it proves authoring
consistency but does not satisfy task_06's exact documentation acceptance.
Finding: F-001.

```yaml
inputs:
  - kind: repository_path
    ref: docs/specs/0114-an-audit-that-reads-the-authorization-it-was-given/_prd.md
  - kind: repository_path
    ref: docs/specs/0114-an-audit-that-reads-the-authorization-it-was-given/_techspec.md
```

### Q-08

Actor and surface: maintainer; domain glossary. Steps: locate `Governed Path`
in `CONTEXT.md` and compare its meaning with the checker emission and TechSpec
Vocabulary Contract. Expected: the glossary owns the term as a repository path
bound by tooling rules, distinct from an ordinary path. Independent
confirmation: source search in the predicate and mechanical audit. Persistence:
reread `CONTEXT.md`. Evidence: `rg -n 'Governed Path' CONTEXT.md` exited 1 with
no match, while the TechSpec Vocabulary Contract and accepted ADR-0130 define
the term. Finding: F-002.

```yaml
inputs:
  - kind: repository_path
    ref: CONTEXT.md
  - kind: repository_path
    ref: internal/speccheck/governed.go
  - kind: repository_path
    ref: internal/speccheck/mechanical.go
```

### Q-09

Actor and surface: maintainer; assembled backend and repository Verification.
Steps: run `make verify` and the focused neighboring package regression suite.
Expected: both exit 0 without failures. Independent confirmation: package-level
tests exercise the changed audit, resolver, suite guard, and shared contract.
Persistence: commands use uncached `-count=1` focused tests where specified.
Evidence: full `make verify` completed successfully. The fresh focused audit,
repository-contract, resolver, and neighboring package commands all exited 0.

```yaml
inputs:
  - kind: repository_path
    ref: '**/*.go'
  - kind: repository_path
    ref: go.mod
  - kind: repository_path
    ref: go.sum
```

### Q-10

Actor and surface: maintainer; Git diff and repository docs. Steps: inspect the
Spec range for movement of the audit, changes to authorization frontmatter or
ownership schemas, and edits to the PRD template. Expected: none of these
Non-Goals shipped. Independent confirmation: exact changed-path and diff
inspection plus the surviving compatibility suites. Persistence: reread the
range from its fixed base. Evidence: `git diff cec521b7..HEAD` found no change
under either `write-prd` skill tree and no `_ownership.yml` or `_ownership.yaml`
change. The authorization-record diff adds only 20 `outputs:` entries; it does
not change frontmatter. `mechanical.go` changes the existing audit in place and
does not move its stage. Clean `git diff-tree` reads of all five Task commits
matched the Daemon-authored Task file and implementation paths.

```yaml
inputs:
  - kind: repository_path
    ref: internal/speccheck/**
  - kind: repository_path
    ref: internal/baseline/**/_ownership.yml
  - kind: repository_path
    ref: .agents/skills/write-prd/**
  - kind: repository_path
    ref: docs/workflow/authorizations/*.md
```

### Q-11

Actor and surface: QA auditor; report and Git index. Steps: validate closed
frontmatter, terminal row statuses, evidence references, and confirm the QA
directory contains no gitlink, binary, or scratch repository. Expected: report
contract passes and only this Markdown report is authored as QA evidence.
Independent confirmation: mechanical report tests plus Git file-mode and status
inspection. Persistence: reread the closed report. Evidence: all Q-01–Q-11 rows
have terminal status, the three blocked counters are zero, and the only QA path
in the Run Worktree is this Markdown report. The disposable CLI clone and built
binary remain under `/tmp` and are not repository evidence.

```yaml
inputs:
  - kind: repository_path
    ref: docs/specs/0114-an-audit-that-reads-the-authorization-it-was-given/qa/qa-report-2026-08-15.md
```

## Findings

### F-001 — Spec artifacts retain the wording this Spec supersedes

Impact: Trust-Damage. Actor: Spec author and future maintainer. Journey: Q-07,
documentation closeout. Expected: both `_prd.md` and `_techspec.md` record
`Tooling authority: applicable` and state that no protected tooling mutation is
proposed or authorized, as accepted ADR-0131 and task_06 require. Actual: both
rows still begin `Tooling authority: not applicable`; the PRD additionally
retains the narrative workaround written because the old checker rejected the
template wording.

Reproduction from the repository docs surface:

```text
rg -n 'Tooling authority:' \
  docs/specs/0114-an-audit-that-reads-the-authorization-it-was-given/_prd.md \
  docs/specs/0114-an-audit-that-reads-the-authorization-it-was-given/_techspec.md
```

The command exits 0 and prints `_prd.md:48` and `_techspec.md:65`, both with
`not applicable`. A disposable-clone CLI run proves the settled replacement is
accepted, so the blocker is the shipped Spec text, not checker capability.
Affected row: Q-07.

### F-002 — Governed Path never reaches the domain glossary

Impact: Trust-Damage. Actor: maintainer reading a refusal or domain context.
Journey: Q-08, vocabulary closeout. Expected: `CONTEXT.md` defines Governed Path
with the semantics emitted by the checker: a repository path bound by tooling
rules, distinct from an ordinary path. Actual: the TechSpec coins and emits the
term, but `CONTEXT.md` contains no occurrence.

Reproduction from the docs surface:

```text
rg -n 'Governed Path' CONTEXT.md
```

The command exits 1 with no match. Affected row: Q-08.

## Blocked and skipped

No QA matrix row is blocked or skipped. The materialized mechanical skips above
remain part of the Daemon's pre-agent evidence and do not represent omitted QA
journeys.

## Coverage

Executed 11/11 rows: 9 passed and 2 failed. All 4 user stories, 3 goals, 4 core
features, 3 Non-Goals, and every Task acceptance criterion map to Q-01 through
Q-11. The required outside-evidence row passed using pre-Spec Git commits.
`rows_blocked_environment=0`, `rows_blocked_finding=0`, and
`rows_blocked_declared=0`. Findings by impact: 2 Trust-Damage; 0
Blocks-Completion, Data-Loss, Friction, or Cosmetic.

## Final verdict

Fail: the backend behavior and full Verification pass, but the Spec cannot close
until both Tooling authority rows use ADR-0131's applicable/no-mutation wording
and `CONTEXT.md` defines Governed Path; then rerun the strict check and this gate.
