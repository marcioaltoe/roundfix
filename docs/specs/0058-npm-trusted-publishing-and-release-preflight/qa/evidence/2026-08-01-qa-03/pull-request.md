# Pull Request #61

Read-only observations on 2026-08-01:

- State: open; draft: false.
- Head branch: `ma/npm-trusted-publishing-and-release-preflight`.
- Head SHA: `171f6a378c9e640a8a10c9382e28b501b21ff5a0`, exactly the QA build.
- Base: `main`.
- GitHub mergeability: `MERGEABLE`; merge state: `CLEAN`.
- Review decision: `APPROVED`.
- Checks: `Validate PR title` completed `SUCCESS`; `CodeRabbit` is `SUCCESS`.
- GraphQL review-thread refresh found two threads, both `isResolved: true` and
  outdated after the repair; unresolved count is zero.
- Commit `83fbf6b` contains only the workflow repair. Commit `171f6a3` follows
  it and contains only the eight round-001/round-002 review artifacts.
- Round 002 settles both actionable issues as `resolved`; two reply-only
  acknowledgements are `invalid` with reasons.

No Pull Request state, check, review, thread, comment, or branch was mutated by
QA.
