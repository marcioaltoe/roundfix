---
spec: 0155-a-verify-that-runs-what-changed
date: 2026-09-24
build: 8da12647fd2f0c007da26d489fa249631bb8b62f
auditing_binary: "0.16.0 (e38c8561, built 2026-09-24 08:07:17 -0300)"
auditor_staleness: "current: commit ancestry: build commit does not predate audited tree"
status: closed
verdict: pass
rows_blocked_environment: 1
rows_blocked_finding: 0
rows_blocked_declared: 0
surfaces: [backend, docs]
---

# QA Report

## Performed repairs

None.

## Assigned repair failures

None.

## Scope and environment

- Scope: full terminal QA gate for build
  `8da12647fd2f0c007da26d489fa249631bb8b62f` on Run Worktree branch
  `roundfix/run-run_20260924T171618Z_2a9a30e6c7a6a9e2`.
- Actor surfaces: repository maintainer through the public Go package, CLI,
  Make, Git, and workflow contracts; Pull Request reviewer through equivalent
  evidence because no Pull Request is open.
- Precondition: `roundfix spec check
  0155-a-verify-that-runs-what-changed --strict` exited 0 and reported no
  findings. It skipped only detectors whose optional artifacts are absent.
- Pull Request parity gap: the assignment confirms no Pull Request is open, so
  R8 uses the required environment-blocked equivalent-evidence path.

## Static gate

- `make verify-changed` — passed with exit 0 when the Roundfix Daemon ran it
  outside the Agent sandbox. Diagnostics were removed on success. This is the
  repository Verification fact supplied with the QA assignment; the Agent did
  not rerun it.

## Authorization audit inputs

None.

## Mechanical findings

None.

## Results

| # | Status | Provenance |
| - | --- | --- |
| R1 | pass | Requirement 1; repository Verification |
| R2 | pass | Requirement 2 |
| R3 | pass | Requirements 3 and 4 |
| R4 | pass | Requirement 5 |
| R5 | pass | Requirement 6 |
| R6 | pass | Requirement 7 |
| R7 | pass | outside-evidence row |
| R8 | blocked (environment: no open Pull Request) | Pull Request row |

## Planned row evidence

### R1

```yaml
actor: Roundfix Daemon
entry_point: make verify-changed
surface: backend
steps: Record the Daemon-run repository Verification supplied with this QA assignment.
expected_observable: The repository Verification exits 0.
independent_confirmation: The Daemon records the exact command and verdict outside the Agent sandbox.
persistence_check: The report binds the fact to the audited build.
evidence_path: This report, Static gate section.
status: pass
inputs:
  - kind: repository_path
    ref: .
```

Executed evidence: the Daemon supplied `command: make verify-changed`,
`verdict: pass (exit 0)`, and `diagnostics: removed on success`. This records
the authoritative static gate result without re-deriving it.

### R2

```yaml
actor: repository maintainer
entry_point: go test ./internal/verifyselect
surface: backend
steps: Run the public package test that inventories all packages and internal/cli tests, then executes both negative controls.
expected_observable: Every package and CLI test belongs to exactly one set; omission and overlap controls are detected and name the affected item.
independent_confirmation: Verbose test output names the passing top-level contract and its negative-control subtests.
persistence_check: Re-run the same uncached test against the audited build.
evidence_path: This report, R2 executed evidence.
status: pass
inputs:
  - kind: repository_path
    ref: internal/verifyselect/**
  - kind: repository_path
    ref: internal/cli/**
  - kind: repository_path
    ref: go.mod
```

Executed evidence: `GOCACHE=/tmp/roundfix-qa-0155-r2-gocache go test
-count=1 -v -run '^TestPartitionCoversEveryTestExactlyOnce$'
./internal/verifyselect` exited 0. The current-tree partition passed, and both
negative controls passed: an omitted package was detected and named, and a CLI
test selected by both invocations was detected and named.

### R3

```yaml
actor: repository maintainer
entry_point: go test ./internal/verifyselect
surface: backend
steps: Run the selector classification and fail-safe tests for core-only, Baseline-only, module-file, and unresolvable-base cases.
expected_observable: Each fixture selects only its expected set, while an unresolvable base selects both.
independent_confirmation: Run the built verify-select CLI with an unresolvable base and observe both set names.
persistence_check: Re-run the uncached tests and CLI probe against the audited build.
evidence_path: This report, R3 executed evidence.
status: pass
inputs:
  - kind: repository_path
    ref: internal/verifyselect/**
  - kind: repository_path
    ref: cmd/verify-select/**
```

Executed evidence: `GOCACHE=/tmp/roundfix-qa-0155-r3-gocache go test
-count=1 -v -run '^TestSelect' ./internal/verifyselect` exited 0. The output
named passing core-only, Baseline-only, both-path-class, module-file,
documentation-only, unresolvable-base, and failing-Git cases. Independently,
`go run ./cmd/verify-select -base refs/heads/does-not-exist` exited 0, printed
`core` and `baseline`, and reported the unresolved base on stderr.

### R4

```yaml
actor: repository maintainer
entry_point: make verify and make verify-changed
surface: backend
steps: Inspect the complete-gate recipe through Make's public dry-run surface, then execute verify-changed against a committed core-only comparison.
expected_observable: verify expands the complete suite and both skill checks; verify-changed completes while selecting core only.
independent_confirmation: Capture selector output for the same base and confirm it contains core but not baseline.
persistence_check: Repeat the dry run and the selective gate against the audited build.
evidence_path: This report, R4 executed evidence.
status: pass
inputs:
  - kind: repository_path
    ref: Makefile
  - kind: repository_path
    ref: cmd/verify-select/**
  - kind: repository_path
    ref: internal/verifyselect/**
```

Executed evidence: `make -n verify` exited 0 and expanded the complete
`go test -parallel 16 ./...` suite, both skill checks, and the Roundfix build.
In an isolated clone at the audited head, the untracked
`internal/verifyselect/qa_core_only_probe_test.go` fixture made
`go run ./cmd/verify-select -base HEAD` print only `core`. Then
`GOCACHE=/tmp/roundfix-qa-0155-r4-gocache make verify-changed
VERIFY_BASE=HEAD` exited 0: whole-tree format, vet, and build completed, and the
core set completed through `internal/cli` in 75.405 seconds. The first sandboxed
attempt reached the existing GitHub-dependent test and was denied network;
the permitted rerun completed the same command successfully.

### R5

```yaml
actor: repository maintainer
entry_point: Roundfix configuration and GitHub Actions workflow contracts
surface: docs
steps: Execute structural checks over the repository Verification setting and event-specific CI commands.
expected_observable: Runs and pull requests name make verify-changed; pushes to main and releases name make verify; pull requests retain full history and pass their base SHA.
independent_confirmation: Git diff evidence binds each governed path to the authorized Task commit and proves release.yml was not changed by the Spec.
persistence_check: Re-run the structural and Git-history checks against the audited build.
evidence_path: This report, R5 executed evidence.
status: pass
inputs:
  - kind: repository_path
    ref: .roundfixrc.yml
  - kind: repository_path
    ref: .github/workflows/ci-verify.yml
  - kind: repository_path
    ref: .github/workflows/release.yml
```

Executed evidence: an executable Ruby contract check exited 0 and printed
`daemon_selective: ok`, `pr_selective: ok`, `push_complete: ok`,
`full_history: ok`, and `release_complete: ok`. Git command checks also exited
0: authorization commit `493baddf` is an ancestor of all three tooling commits;
`f11ab44e` changed only `Makefile` and `task_03.md`; `d58c5968` changed only
`.github/workflows/ci-verify.yml` and `task_04.md`; `8da12647` changed only
`.roundfixrc.yml`; and `.github/workflows/release.yml` is byte-unchanged from
`v0.16.0` through the audited head. Every governed path is within the approved
bounded list, and the authorization predates each governed mutation.

### R6

```yaml
actor: Spec author
entry_point: roundfix spec check --strict
surface: docs
steps: Run the strict checker and independently trace every numbered Success Metric and API Contract to a consuming Task Reference.
expected_observable: The checker reports no findings and every declared promise names at least one consuming Task.
independent_confirmation: A separate repository query resolves the cited Task References for each numbered promise.
persistence_check: Re-run both checks against the audited Spec artifacts.
evidence_path: This report, R6 executed evidence.
status: pass
inputs:
  - kind: repository_path
    ref: docs/specs/0155-a-verify-that-runs-what-changed/**
```

Executed evidence: `roundfix spec check
0155-a-verify-that-runs-what-changed --strict` exited 0 and reported `No
findings`; it did not execute authored Verification commands. A separate
executable citation check exited 0 and printed `Success Metrics 1,2,3:
task_05.md` and `API Contracts 1,2,3: task_05.md`, proving that every numbered
promise declared by ADR-0156 names a consuming Task Reference.

### R7

```yaml
actor: repository maintainer
entry_point: verify-select package and pattern output
surface: backend
steps: Feed the selector the package and CLI-test inventory inherited from the pre-Spec v0.16.0 release tag.
expected_observable: The selector partitions the independently existing repository inventory without omission or overlap.
independent_confirmation: The source inventory is bound to the v0.16.0 tag object, which predates this Spec.
persistence_check: Re-run the comparison against the audited selector build.
evidence_path: This report, R7 executed evidence.
status: pass
inputs:
  - kind: repository_path
    ref: internal/verifyselect/**
  - kind: repository_path
    ref: cmd/verify-select/**
  - kind: external_repository
    ref: this repository at tag v0.16.0, before Spec 0155
```

Executed evidence: a separate local clone checked out release tag `v0.16.0`
at `35fb7839`, which predates Spec 0155. An executable comparison used that
tree's real `go list ./...` and `go test -list '^Test' ./internal/cli`
inventories, then applied the audited selector's public package lists and
derived Baseline pattern. It exited 0 and reported `packages_partitioned_once:
32/32` and `cli_tests_partitioned_once: 679/679 (core=600, baseline=79)`.
This evidence originates in repository content the Spec did not build.

### R8

```yaml
actor: pull request reviewer
entry_point: Open Pull Request for feat/0155-verify-what-changed
surface: docs
steps: Observe approval, checks and status, unresolved review threads, Merge-Ready acceptance, and review-artifact ancestry for the expected head.
expected_observable: Every Pull Request control is evidenced for the target branch head.
independent_confirmation: The QA assignment states that no Pull Request is open and supplies the environment classification.
persistence_check: Bind equivalent evidence to the audited head in this report.
evidence_path: This report, R8 executed evidence.
status: blocked (environment: no open Pull Request)
inputs:
  - kind: live_service
    ref: GitHub Pull Request for feat/0155-verify-what-changed
```

Executed evidence: the QA assignment confirms that no Pull Request is open for
`feat/0155-verify-what-changed`, so the live journey is unreachable. Equivalent
evidence covers every Pull Request control:

- approval — the maintainer's 2026-09-24 approval for the bounded tooling
  change is recorded as `status: approved` in `_authorization.md`; no separate
  Pull Request acceptance exists yet;
- checks and status — the Daemon-run `make verify-changed` exited 0, and R2-R7
  record the focused current-build checks;
- unresolved review threads — no Open Pull Request exists, so no Pull Request
  review thread stands;
- Merge-Ready acceptance — none exists yet because the Review Source has no
  Open Pull Request head to accept;
- review-artifact ancestry — no review artifact exists yet. The target branch
  and audited build both resolve to
  `8da12647fd2f0c007da26d489fa249631bb8b62f`, and all Task commits are ancestors
  of that head.

Unblocking action: open the Pull Request from
`feat/0155-verify-what-changed`, then observe approval, required checks, review
threads, Merge-Ready acceptance, and review-artifact ancestry on its current
head.

## Mechanical skips

| Detector | Missing artifact |
| --- | --- |
| authorization bounded paths | Task commits |
| consequent-fix commit order | consequent-fix declaration |
| QA Report structure | QA Report |
| QA evidence paths | QA Report |

## Findings

None.

## Blocked and skipped

- R8 is `blocked (environment: no open Pull Request)`. The assignment proves
  the environment cause, and R8 records equivalent evidence for approval,
  checks and status, unresolved review threads, Merge-Ready acceptance, and
  review-artifact ancestry. Opening the target branch's Pull Request makes the
  live journey runnable.
- No row is finding-blocked, declared-blocked, or skipped. The seeded
  mechanical skips remain recorded above; R5 supplied the missing commit audit
  through fresh Git evidence, and this closed report supplies the structure and
  evidence locators that did not exist during the mechanical stage.

## Coverage

- Coverage sources: 10/10 named and covered — Requirements 1-7, repository
  Verification, outside-evidence row, and Pull Request row.
- Results: 7 passed, 1 environment-blocked with equivalent evidence, 0 failed,
  0 finding-blocked, 0 declared-blocked, 0 skipped, and 0 pending.
- `rows_blocked_environment: 1`; `rows_blocked_finding: 0`;
  `rows_blocked_declared: 0`.
- No PRD Unreachable Acceptance declaration exists, and the Spec declares no
  frontend surface, so neither adds a coverage source.
- No finding or intentional break remained to probe beyond the declared
  Requirement matrix. The high-risk probes exercised were selector omission,
  selector overlap, unresolvable base, failing Git, a genuine core-only
  top-level selective gate, complete-gate preservation, governed-path history,
  and pre-Spec inventory coverage.

## Final verdict

Pass: every runnable row passed, the sole environment-blocked Pull Request row
records its cause and equivalent evidence for every control, and no row is
finding-blocked, declared-blocked, skipped, failed, or pending.
