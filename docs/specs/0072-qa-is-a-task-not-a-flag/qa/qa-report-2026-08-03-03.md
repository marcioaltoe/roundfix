---
spec: 0072-qa-is-a-task-not-a-flag
date: 2026-08-03
build: 8bd6bc77712a32a502362475649b1523fc38ae04
status: closed
verdict: fail
rows_blocked_environment: 0
rows_blocked_finding: 15
surfaces: [backend, cli, docs]
---

# QA report — QA is a Task, not a flag

## Scope and environment

Full rerun of the authored terminal QA Task against Run Worktree build
`8bd6bc77712a32a502362475649b1523fc38ae04` on macOS, branch
`roundfix/run-run_20260803T163802Z_8330e3722437fb14`. The real user entry
point is a freshly built `roundfix` CLI. Authoring and documentation journeys
use scratch repositories and shipped repository guidance. Pull Request #87 on
target branch `ma/qa-task-not-flag` is supplied as open and is observed
read-only. This gate writes only this report and evidence under `qa/`; it does
not commit, push, resolve threads, open a Pull Request, or edit Task status.

This rerun starts with F-001 and F-003 from
`qa/qa-report-2026-08-03-02.md`. F-003 is resolved: the exact current Pull
Request head is approved, all checks pass, and all twelve review threads are
resolved. The active artifacts now bound every protected path, and mirrors and
derived pins are current, but the immutable commit chronology still fails
F-001. The complete governance audit therefore stops public scratch-flow QA.

## Static gate

`rtk make verify` ran alone against the report build and exited `0`:

```text
rtk go test -parallel 16 ./...
Go test: 3103 passed in 24 packages
rtk go test -count=1 ./skills -run 'TestNoPythonBaselineRuntime|TestThinSetupSkill|TestCheckRejectsExecutableSetupEngineArtifacts|TestRecommendedSkillsMatchLock'
Go test: 4 passed in 1 packages
rtk go run -buildvcs=false ./cmd/roundfix skills check
Roundfix skill check passed: roundfix, write-idea, write-prd, write-techspec, write-tasks, setup-context-driven, implement-task, implement-spec, brainstorming, council, business-analyst, archive-spec, qa-gate, evidence-gate
rtk go build -buildvcs=false ... -o bin/roundfix ./cmd/roundfix
```

The build metadata correctly says `8bd6bc7-dirty` because this in-progress QA
Report existed while the binary was built; the implementation commit under
test is the frontmatter `build`.

## Results

| # | Story / criterion / sweep | Actor and public entry point | Expected observable | Independent confirmation / persistence / evidence | Status |
| - | --- | --- | --- | --- | --- |
| 1 | Authored-gate entry precondition | QA Agent; Spec Task Graph | `task_07` is the manifest-named terminal QA Task and all dependencies are completed | `_tasks.md` names `qa: task_07`; the terminal node depends on completed task_04, task_05, and task_06, which transitively cover every non-QA leaf. The Daemon-owned gate status remains `failed` from the prior attempt and is not edited here. | pass |
| 2 | Project Constraint: identifier strategy | Maintainer; active Spec artifacts and public graph contract | Task Type `qa` has one canonical owner; dependency/status ownership stays unchanged | Both active artifacts account for Task Type `qa`; `CONTEXT.md`, ADR-0091, and the manifest/task files retain manifest-owned dependencies and Task-file-owned status. | pass |
| 3 | Project Constraint: authentication and HTTP | Maintainer; active artifacts and Git ancestry | Both artifacts explain non-applicability and no credential/transport surface shipped | Both artifacts cite `docs/agents/agent-instructions.md`; commit-by-commit `git diff-tree` found no authentication, credential, HTTP, or transport path. | pass |
| 4 | Project Constraint: active ADR obligations | Maintainer; ADRs, graph, reports | ADR-0080 semantics and ADR-0088/0091 authored-node contract agree | ADR-0080, ADR-0088, and ADR-0091 are accepted. The manifest and all reports retain the authored-node and typed-count shapes; the full gate passes the verdict suites. | pass |
| 5 | Prior F-001 and complete tooling audit | Maintainer; authorization, Git ancestry, shipped skills | Exact prior authority, bounded paths in both artifacts, ordered prerequisite/edit/consequent commits, valid mirrors and derived pins | Active artifacts now bound every path; all three skill mirrors compare equal; `rtk make skills-sync-check` passes; isolated `rtk make baseline-digests` reports `changed:false`. Commit-by-commit `git diff-tree` still proves protected edits preceded or folded their exact authorization and repairs. | fail |
| 6 | Repository Verification | Maintainer; repository gate | `rtk make verify` exits 0 and builds the CLI | Fresh run exited 0 with 3,103 Go tests in 24 packages, four skill tests, the Roundfix skill check, and the CLI build. | pass |
| 7 | Author includes QA exactly once | Spec author; scratch graph through built CLI | One named terminal `type: qa` Task covers every non-QA leaf | Governance preflight F-001 prohibits public flow QA on this build; no scratch author journey ran. | blocked (finding: F-001) |
| 8 | Author declines QA once; invalid reasons fail | Spec author; valid, missing, and empty-reason scratch graphs | Valid decline loads; missing/empty reason names `qa_reason` | Governance preflight F-001 prohibits the public valid/missing/empty-reason probes. | blocked (finding: F-001) |
| 9 | Implement runs an unsettled authored gate without a flag | Operator; built CLI on scratch Spec | Gate executes last and settles from its report | Governance preflight F-001 prohibits the end-to-end Run; static and task evidence is not substituted for the user journey. | blocked (finding: F-001) |
| 10 | Removed `--qa` is a side-effect-free error | Operator; built CLI | Exit 2, empty stdout, actionable Task Graph remediation | Governance preflight F-001 prohibits the removed-flag and before/after Run probes. | blocked (finding: F-001) |
| 11 | Appended work invalidates a settled gate | Spec author; out-of-order scratch graph | Reload fails and names every inserted/unsettled dependency | Governance preflight F-001 prohibits the out-of-order public probe. | blocked (finding: F-001) |
| 12 | Non-terminal or incomplete gate coverage is rejected | Spec author; malformed scratch graphs | Preflight names the dependent or uncovered Task | Governance preflight F-001 prohibits the malformed-graph public probes. | blocked (finding: F-001) |
| 13 | Declined, legacy, and settled graphs stay gateless/no-op | Operator; built CLI on scratch graphs | No report/event; repeatable deterministic no-op where settled | Governance preflight F-001 prohibits repeated public invocation and independent Run reads. | blocked (finding: F-001) |
| 14 | Failed dependency withholds the gate | Operator; controlled failing scratch Run | Gate stays resumable; no QA report/event | Governance preflight F-001 prohibits the controlled failure journey; current review evidence is not substituted for user-seat proof. | blocked (finding: F-001) |
| 15 | QA node never enters an ordinary Agent Session | Operator; malformed graph through built CLI | Refusal before Agent work with node named | Governance preflight F-001 prohibits the malformed public routing probe. | blocked (finding: F-001) |
| 16 | ADR-0080 report, verdict, typed counts, and events stay compatible | QA Agent/operator; reports and scratch Runs | Stable schema and exact blocked-row counts across outcomes | This report proves schema and exact totals; public pass/fail/partial Run and event probes were stopped by F-001. | blocked (finding: F-001) |
| 17 | Gate executions are countable; Spec 0057 keeps three closings | Maintainer; Git/spec history and public history surface | Each closing has a stable graph/report identity | Git history retains the three prior reports and current authored gate, but F-001 stops assembled-build public history validation. | blocked (finding: F-001) |
| 18 | Legacy Specs load unchanged | Maintainer; archived manifests through public compatibility path | Legacy load succeeds without mutation | The green full gate includes the repaired characterization; F-001 stops the required public compatibility and persistence run. | blocked (finding: F-001) |
| 19 | Guides and shipped skills are followable | Supervisor/author; docs plus built CLI | Documented authored-gate flow works; no stale flag guidance | Canonical/mirror comparisons and `skills-sync-check` pass; F-001 stops the follow-the-docs scratch journey. | blocked (finding: F-001) |
| 20 | CLI help/streams/exits/non-interactive sweep | Operator/automation; built CLI | Help omits `--qa`; outputs and exit codes are deterministic | The current binary builds under the green gate; F-001 stops the public help/stream/exit/replay sweep. | blocked (finding: F-001) |
| 21 | Non-Goals: semantics/withholding unchanged; no runtime skip | Maintainer/operator; CLI plus bounded diff | Existing gate matrix and withholding hold; no skip path ships | Static scope and suites support the Non-Goals; F-001 stops live dependency and verdict paths. | blocked (finding: F-001) |
| 22 | Non-Goal: Spec 0071 cost boundary is not widened | Maintainer; changed paths and full gate | No check removed/bypassed; only sanctioned coverage reconciliation | No Makefile or workflow changed; the only 0071 path is the deliberately reconciled coverage record. The unchanged full gate passed. | pass |
| 23 | Gate-cycle attribution and trim | Maintainer; measurement artifact and focused behavior | Measured attribution, justified semantics-preserving trim, dominant cost disclosed | `gate-cost-2026-08-03.md` records 625.710s total, the before/after medians, and the 468.669s Agent-work share; current Verification passes the unchanged verdict suite. | pass |
| 24 | Pull Request #87 current-head readiness | Maintainer; read-only PR metadata/checks/reviews/threads | Exact target head, passing checks, accepted review, zero unresolved threads, Merge-Ready | Fresh reads show exact head `8bd6bc7`, OPEN, non-draft, `reviewDecision: APPROVED`, `mergeStateStatus: CLEAN`, and three successful current-head checks. All 12 threads are resolved with no next page. `8bd6bc7` is the exact separate review-artifact child of `d27edba` and changes only the five Spec-local review artifacts. | pass |

## Findings

### F-001 — Tooling Authority chronology remains invalid

Impact: `Blocks-Completion`. Repeated from all three prior reports; the active
artifact boundary and current derived state are repaired, but the commit
ordering is not.

The maintainer reaches the Project Constraint audit before public flow QA.
The current PRD and TechSpec now list the canonical `write-tasks`, `qa-gate`,
and `roundfix` skill trees, their mirrors, and the named corrective
golden/coverage paths. All three canonical trees equal their mirrors,
`rtk make skills-sync-check` exits 0, and `rtk make baseline-digests` against
an isolated archive of `HEAD` exits 0 with `changed:false`.

The complete chronological audit still proves three prohibited shapes:

1. Commit `160d417` changed the protected `write-tasks` and `qa-gate` skills,
   mirrors, and sanctioned digest fallout at 11:28. The Spec-specific exact
   authorization first appears later in `44b1fd7` at 12:17. The authorization
   record itself states that a Spec not listed had no authorization, so the
   maintainer's product direction is traceable but exact bounded authority was
   late.
2. Commit `44b1fd7` combines that late authorization with the coverage record,
   catalog diagnostic golden, and four plan-characterization goldens used to
   repair earlier red state. Protected-tooling choreography requires the
   authorization and each prerequisite repair to land as separate commits
   before the authorized edit.
3. Commit `a12c1a6` adds exact authorization for
   `.agents/skills/roundfix/**` and changes that protected skill and mirror in
   the same commit. The authorization is folded into the edit instead of
   preceding it. Later `ba78604` wording and `d27edba` review repairs do not
   rewrite those earlier commits.

`git diff-tree --root --name-status -r` ran for every commit from `origin/main`
through `8bd6bc7`, including all Task, QA, authorization/remediation, review
fix, and review-artifact commits. No additional authorization-shape problem
was found: `d27edba` is later than the recorded exact boundaries, its skill
mirror is synchronized, and its derived pins are sanctioned deterministic
fallout. `8bd6bc7` is a separate five-file Spec-local review-artifact commit.

Expected: exact bounded authorization and each prerequisite repair precede
the protected edit in separate commits. Actual: the first skill edit preceded
exact authorization, the remediation commit combined authorization and
repairs, and the later skill authorization was folded into its edit.

Affected rows: 5 and 7–21.

### Resolved prior finding — F-003

The Pull Request blocker from `qa-report-2026-08-03-02.md` is resolved. Fresh
read-only GitHub evidence shows PR #87 at exact head `8bd6bc7`, with all three
checks successful, `reviewDecision: APPROVED`, `mergeStateStatus: CLEAN`, and
zero unresolved threads across the complete twelve-thread set.

## Blocked and skipped

- Rows 7–21 are `blocked (finding: F-001)`. The QA contract stops public flow
  testing after a failed complete Tooling Authority audit. Unblock by rewriting
  the branch history so the exact authorization and prerequisite repairs each
  precede their protected edit as separate commits; then rerun every public
  journey against the rebuilt head.
- No row is environment-blocked or skipped. No row remains pending.

## Coverage

Results: 8 passed, 1 failed, 15 finding-blocked, 0 environment-blocked, 0
skipped, and 0 pending. `rows_blocked_environment: 0` and
`rows_blocked_finding: 15` match the table exactly.

The matrix maps every Core Feature, Success Metric, Non-Goal, Project
Constraint class, declared surface, prior finding, and the supplied Pull
Request. Explicit-empty input, out-of-order graph growth, replay after
settlement, repeated no-op invocation, controlled dependency failure, and
fresh event confirmation remain explicitly unexecuted because F-001 stops
flow QA. Pull Request metadata, checks, review decision, complete thread set,
and review-artifact ancestry were refreshed on the exact current head. No
frontend surface is declared.

## Final verdict

Fail: repository Verification and Pull Request readiness pass, but protected
tooling authorization and remediation remain chronologically invalid; counts
are 8 pass, 1 fail, 15 finding-blocked, 0 environment-blocked, and no skipped
or pending rows.
