---
spec: 0072-qa-is-a-task-not-a-flag
date: 2026-08-03
build: 37890c8a479aaeb68495f86bb16885cd5152f39a
status: closed
verdict: fail
rows_blocked_environment: 0
rows_blocked_finding: 15
surfaces: [backend, cli, docs]
---

# QA report — QA is a Task, not a flag

## Scope and environment

Full rerun of the authored terminal QA Task against Run Worktree build
`37890c8a479aaeb68495f86bb16885cd5152f39a` on macOS, branch
`roundfix/run-run_20260803T151944Z_d915fdab65c0d71c`. The real user entry
point is a freshly built `roundfix` CLI; authoring and documentation journeys
use scratch repositories and the shipped repository guidance. Pull Request
#87 on target branch `ma/qa-task-not-flag` is supplied as open and will be
observed read-only. This gate writes only this report and QA evidence; it does
not commit, push, resolve review threads, open a Pull Request, or edit Task
status.

This rerun answers both blocking findings from
`qa-report-2026-08-03.md` before public flow QA: F-001 (tooling authority) and
F-002 (repository Verification). The rerun closed without changing
implementation, Task status, the Pull Request, or any repository path outside
this report.

## Static gate

`rtk make verify` ran alone against the report build and exited `0`:

```text
rtk go test -parallel 16 ./...
Go test: 3100 passed in 24 packages
rtk go test -count=1 ./skills -run 'TestNoPythonBaselineRuntime|TestThinSetupSkill|TestCheckRejectsExecutableSetupEngineArtifacts|TestRecommendedSkillsMatchLock'
Go test: 4 passed in 1 packages
rtk go run -buildvcs=false ./cmd/roundfix skills check
Roundfix skill check passed: roundfix, write-idea, write-prd, write-techspec, write-tasks, setup-context-driven, implement-task, implement-spec, brainstorming, council, business-analyst, archive-spec, qa-gate, evidence-gate
rtk go build -buildvcs=false ... -o bin/roundfix ./cmd/roundfix
```

This resolves prior F-002: the Baseline characterization and coverage-record
drift from the first report no longer reproduces. The current build passes the
repository's complete formatting, test, skill, and build gate.

## Results

| # | Story / criterion / sweep | Actor and surface | Planned public check and confirmation | Status | Evidence |
| - | --- | --- | --- | --- | --- |
| 1 | Authored-gate entry precondition: `task_07` is the manifest-named terminal QA Task and every dependency is completed | QA Agent; Task Graph/docs | Load the real Spec graph; confirm the named node, type, leaf coverage, dependency states, and that its own state remains Daemon-owned | pass | `_tasks.md` names `qa: task_07`; `task_07.md` is `type: qa`, remains `pending`, and depends on completed `task_04`, `task_05`, and `task_06`. It is terminal and those dependencies collectively cover every non-QA leaf. |
| 2 | Project Constraint: identifier strategy accounts for Task Type `qa` without duplicating status or dependencies | Maintainer; backend/docs | Compare both active artifacts with `CONTEXT.md` and operative domain guidance; confirm through the real graph loader | pass | PRD and TechSpec account for the identifier; `CONTEXT.md` defines Task Type, Task Graph, and QA Report. The manifest owns dependencies and Task files own status. |
| 3 | Project Constraint: authentication and HTTP are inapplicable and no credential or transport policy shipped | Maintainer; backend/docs | Inspect the exact Task commits and active artifacts against the operative universal clause | pass | Both active artifacts give the inapplicability reason and cite `docs/agents/agent-instructions.md`; `git diff-tree` over the six Task commits shows no authentication, HTTP, credential, or transport path. |
| 4 | Project Constraint: active ADR obligations preserve ADR-0080 and implement ADR-0088/ADR-0091 | Maintainer; backend/docs | Read accepted ADRs; exercise report/count behavior and the authored graph through public paths | pass | ADR-0080, ADR-0088, and ADR-0091 are accepted. The current graph has the ADR-0091 shape, this report has ADR-0080's exact verdict/count frontmatter, and `rtk make verify` passes the graph and verdict suites. |
| 5 | Prior F-001 and tooling audit: Spec 0072 has traceable express authority for exact skill paths; commit scope, chronology, mirrors, and sanctioned digest fallout are valid | Maintainer; docs/tooling | Inspect the authorization record and every relevant commit with `git diff-tree`; confirm ancestry, exact bounded paths, mirror parity, and reproducible digest regeneration | fail | F-001 repeats in narrower form. The addendum now names Spec 0072 and exact paths, and the TechSpec matches it; the active PRD still says only “the owned skill pair,” contrary to the both-artifacts requirement. All Task 04 paths, mirror parity, and regenerated pins otherwise pass audit. |
| 6 | Prior F-002 and repository Verification: the full current-build gate passes | Maintainer; backend/cli/docs | Run `rtk make verify` alone and require exit 0 | pass | `rtk make verify` exited 0: 3,100 tests passed across 24 packages, four skill tests passed, `roundfix skills check` passed, and the binary built. Prior F-002 is resolved. |
| 7 | An author includes QA once: one manifest-named terminal `type: qa` Task covers every non-QA leaf | Spec author; docs/Task Graph | Create/load a scratch authored graph and confirm through a second loader invocation | blocked (finding: F-001) | The Project Constraint audit forbids flow QA after F-001. Static and Task evidence support the contract but cannot replace the planned author journey. |
| 8 | An author declines QA once with `qa: declined` and non-empty `qa_reason`; missing reason is rejected | Spec author; docs/Task Graph | Create valid and invalid scratch graphs; confirm deterministic public diagnostics on reload | blocked (finding: F-001) | F-001 stopped the scratch authoring journey. PR #87 also has an unresolved current review thread questioning explicit null/empty `qa_reason` handling. |
| 9 | `roundfix implement --spec <slug>` runs an unsettled authored gate after implementation Tasks complete without a QA parameter | Operator; CLI/backend | Run the built CLI on a scratch Spec and confirm terminal Run state/report through a fresh public read | blocked (finding: F-001) | No nested public Run was started after the governance preflight failed. |
| 10 | Removed `--qa` fails as unknown, names Task Graph remediation, writes no requested output, and creates no Run | Operator; CLI | Invoke the built command, capture streams/exit code, then compare `runs list` before and after | blocked (finding: F-001) | The public CLI and Run-list confirmation were not executed after F-001. |
| 11 | Appending a Task after a completed gate invalidates the result and names each unsettled insertion | Spec author; CLI/backend | Load a settled scratch graph, append an out-of-order dependency, reload, and inspect the public error | blocked (finding: F-001) | The out-of-order scratch probe was not executed after F-001. |
| 12 | A non-terminal gate or gate that misses a leaf is rejected with the offending Task named | Spec author; CLI/backend | Load two malformed scratch graphs and confirm independent repeat diagnostics | blocked (finding: F-001) | The malformed-graph public probes were not executed after F-001. |
| 13 | Declined and legacy graphs never start a gate; settled/no-gate graphs remain no-ops | Operator; CLI/backend | Invoke Implement repeatedly on scratch graphs and confirm via public Run listing/events | blocked (finding: F-001) | Repeated public invocation and independent Run reads were not executed after F-001. |
| 14 | A failed implementation dependency withholds the gate, leaves it pending/resumable, and emits no report or gate event | Operator; CLI/backend | Run a controlled failing scratch Task and confirm task/report/event state through public reads | blocked (finding: F-001) | The controlled failure journey was not executed after F-001. An unresolved PR thread separately questions the user-visible withheld-gate signal. |
| 15 | A hand-edited `type: qa` node cannot enter an ordinary Agent Session | Operator; CLI/backend | Present an invalid graph to the public command and confirm refusal occurs before Agent work | blocked (finding: F-001) | The malformed public graph journey was not executed after F-001. |
| 16 | Report path, verdict vocabulary, typed blocked counts, and `daemon.qa` events remain ADR-0080-compatible | QA Agent/operator; CLI/backend | Observe this report plus scratch pass/failure outcomes and fresh Run Event reads | blocked (finding: F-001) | This report proves the schema shape and static suites pass, but public pass/fail scratch Runs and fresh event reads were not executed. |
| 17 | Gate executions are countable as terminal Task history; Spec 0057's three closings remain legible | Maintainer; docs/backend | Inspect real history/artifacts and confirm the current authored node records one closing structurally | blocked (finding: F-001) | Current Git history shows the authored node and prior gate report, but the planned assembled-build history journey was not run after F-001. |
| 18 | Legacy Specs load unchanged and archived artifacts remain byte-identical | Maintainer; CLI/backend/docs | Run the public compatibility path and compare repository status/checksums after a second load | blocked (finding: F-001) | Static verification passes, but no public compatibility run was executed. PR #87 also has an unresolved current review thread stating the byte-identity test re-reads the source rather than the loaded copy. |
| 19 | Guides and shipped skills consistently teach the authored gate and remain followable without undocumented knowledge | Supervisor/Spec author; docs | Follow the documented commands/shape in a scratch Spec; scan for stale parameter guidance and compare mirrors | blocked (finding: F-001) | The stale-reference scan returned no `--qa` match and both skill mirrors compare equal, but the follow-the-docs scratch journey was not executed. |
| 20 | CLI surface sweep: help omits `--qa`; stdout/stderr and exit codes are deterministic and non-interactive | Operator/automation; CLI | Exercise help, invalid flag, valid no-op, repeated invocation, and stream separation on the built binary | blocked (finding: F-001) | The public CLI surface sweep was not executed after F-001. |
| 21 | Non-Goals: gate matrix/verdict semantics and Daemon withholding remain unchanged; no runtime skip path shipped | Maintainer/operator; backend/cli | Exercise dependency and verdict paths, then inspect the bounded diff for scope creep | blocked (finding: F-001) | Changed-path inspection and the green full gate support scope, but the required live dependency/verdict probes were stopped. |
| 22 | Non-Goal: Spec 0071's test-execution-cost boundary was not widened | Maintainer; backend/docs | Inspect changed paths and run the full gate without adding or bypassing checks | pass | Task commit paths do not change `Makefile`, CI workflow definitions, or Spec 0071 implementation; the only Spec 0071 path in remediation is its declared coverage record. The unchanged full gate passed. |
| 23 | Gate-cycle cost evidence is measured, its trim is semantics-preserving, and dominant untrimmable cost is explicit | Maintainer; backend/docs | Reproduce the focused gate behavior and inspect the committed attribution against current history | pass | `gate-cost-2026-08-03.md` records measured 625.710s end-to-end attribution, before/after medians, and the 468.669s untrimmable Agent-work share. The current full verdict suite passes without weakened checks. |
| 24 | Pull Request #87 is open on the target branch with current-head checks, approval/review state, unresolved-thread evidence, and Merge-Ready status observed read-only | Maintainer; Pull Request | Read PR metadata, checks, reviews, and threads; compare head SHA to this build and refresh once | fail | F-003. PR #87 is open, not draft, `mergeStateStatus: CLEAN`, and its head equals this build; all 3 checks pass. Two independent reads show `reviewDecision: CHANGES_REQUESTED` and 8 unresolved, current, non-outdated threads, so it is not Merge-Ready. |

## Findings

### F-001 — The active PRD still lacks the exact tooling boundary

Impact: `Blocks-Completion`. Repeated from the first report, narrowed after
partial remediation.

The 2026-08-03 authorization addendum now names Spec 0072 and the exact
canonical skill paths plus mirrors, quoting the maintainer's pre-implementation
direction. `_techspec.md` records the same exact boundary. `_prd.md` still
records only “the owned skill pair.” The QA contract requires express authority
and exact bounded repository-relative files in both active Spec artifacts; a
category label in one artifact is not the exact path boundary.

Reproduction:

1. Read the Tooling authority row in `_prd.md`: it names only “the owned skill
   pair.”
2. Read the matching row in `_techspec.md` and the Spec 0072 authorization
   addendum: both name `.agents/skills/write-tasks/**`,
   `.agents/skills/qa-gate/**`, and their `skills/` mirrors.
3. Compare with the `qa-gate` precondition requiring the exact bounded files in
   both active artifacts.

The rest of the tooling audit passes. `git diff-tree --no-commit-id
--name-status -r` over every Task commit finds protected tooling only in Task
04 commit `160d417`; its canonical skills, mirrors, and ADR-0081 derived paths
match the authorized shape. Both mirror directory comparisons exit 0. An
isolated `make baseline-digests` run reports `changed:false`. Authorization
source commit `2e560ce` precedes Task 04, and remediation commit `44b1fd7`
records the exact Spec-specific application after the first gate found the
omission; its quoted maintainer direction predates implementation, so the
authority is traceable rather than invented by the Task.

Affected rows: 5 and 7–21.

### F-003 — Pull Request #87 has unresolved review findings

Impact: `Blocks-Completion`.

PR #87 points to the exact report build and all three checks pass, but GitHub
reports `CHANGES_REQUESTED`. The complete first page contains eight unresolved,
non-outdated review threads and `hasNextPage: false`. They include current
functional/data-integrity concerns around explicit empty `qa_reason`, missing
QA-report commit paths, legacy-manifest proof, and derived fixture identities,
plus maintainability findings. The gate does not resolve, dismiss, or mutate
them.

Reproduction:

1. Run `rtk gh pr view 87 --repo marcioaltoe/roundfix --json ...` and observe
   head `37890c8a...`, `OPEN`, `mergeStateStatus: CLEAN`, three successful
   checks, and `reviewDecision: CHANGES_REQUESTED`.
2. Read all review threads through the GitHub GraphQL API; observe eight rows
   with `isResolved: false`, `isOutdated: false`, and no next page.
3. Refresh the same query; the head, decision, and eight-thread set remain
   unchanged.

Affected row: 24.

## Blocked and skipped

- Rows 7–21 are `blocked (finding: F-001)`. The gate contract requires the
  complete Project Constraint audit to pass before public flow QA. Unblock by
  recording the exact authorized repository-relative skill paths in the PRD,
  then rerun every public journey.
- No row is environment-blocked or skipped. No row remains pending.

## Coverage

Results: 7 passed, 2 failed, 15 finding-blocked, 0 environment-blocked, 0
skipped, and 0 pending. `rows_blocked_environment: 0` and
`rows_blocked_finding: 15` match the table exactly.

The matrix maps every Core Feature, Success Metric, Non-Goal, Project
Constraint class, declared surface, prior finding, and the supplied Pull
Request. Invalid-input, out-of-order append, replay, repeated invocation, and
live Run confirmation probes remain explicitly unexecuted because F-001 stops
flow QA. The Pull Request refresh ran twice. No frontend surface is declared.

## Final verdict

Fail: prior repository Verification finding F-002 is resolved, but the PRD must
record the exact authorized skill paths and PR #87 must clear its eight
unresolved review threads before a full public-flow rerun can pass; counts are
7 pass, 2 fail, 15 finding-blocked, 0 environment-blocked, and no skipped or
pending rows.
