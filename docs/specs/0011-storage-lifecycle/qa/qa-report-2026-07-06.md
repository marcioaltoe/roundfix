---
spec: 0011-storage-lifecycle
date: 2026-07-06
verdict: pass
surfaces: [cli, infra, docs]
---

# QA Report - Review Artifacts, Run Logs, and Spec Archiving

## Summary

QA validated the completed storage/lifecycle Spec against the built Roundfix CLI, the full repository verification gate, focused integration tests, skill drift checks, and user-facing documentation. All PRD user stories, core features, task acceptance criteria, and non-goals passed. No failures or blocked items were found.

## Results

| # | Criterion / user story | Verdict | Evidence |
| - | ---------------------- | ------- | -------- |
| 1 | PRD User Story 1: a PR tied to a Spec writes Round and Review Issue artifacts under that Spec folder. | pass | `rtk go test ./internal/config/ ./internal/agent/ ./internal/cli/ ./internal/reviewsource/... ./internal/spec/ ./internal/daemon/` passed (`517 passed in 7 packages`), including `TestRunFetchWritesReviewArtifactsUnderSpecSelector` and `TestRunFetchWritesReviewArtifactsUnderTrailerSpec`; built CLI help for `fetch`, `resolve`, and `watch` exposes `--spec <slug>`. |
| 2 | PRD User Story 2: a PR with no Spec writes artifacts to an in-repo review root. | pass | Focused integration gate passed, including `TestRunFetchWritesReviewArtifactsUnderSpeclessRoot` for `docs/specs/_reviews/pr-<n>/round-*`. |
| 3 | PRD User Story 3: an explicitly configured Artifact Directory still wins. | pass | Focused integration gate passed, including `TestResolveReviewRoot/explicit artifact directory keeps existing layout`; docs inspection found the legacy explicit layout documented in README and the Roundfix skill. |
| 4 | PRD User Story 4: production Runs write no per-Batch Agent log files unless opted in. | pass | Focused integration gate passed, including `TestRunResolveSkipsAgentLogFilesByDefaultAndStillJournals`; `rtk make verify` passed (`Go test: 765 passed in 17 packages`). |
| 5 | PRD User Story 5: a config key turns per-Batch Agent logs back on for debugging. | pass | Focused integration gate passed, including `TestLoadAppliesAgentLogConfigHierarchy`, `TestRunResolveWritesAgentLogFilesWhenEnabledAndStillJournals`, and `TestRunImplementUsesConfiguredArtifactDirectoryForAgentLogs`; docs inspection found `logs.agent` in README and both skill copies. |
| 6 | PRD User Story 6: a completed, QA-passed Spec can be archived out of the active set. | pass | Built CLI `rtk ./bin/roundfix archive --help` exited 0 and names the all-completed plus `verdict: pass` precondition; focused integration gate passed, including `TestRunArchiveMovesCompletedSpecAndStampsMetadata`. |
| 7 | PRD User Story 7: Review Issue titles are plain text without table markup or emoji. | pass | Focused integration gate passed, including `TestTitleFromBodyStripsCodeRabbitMarkup`. |
| 8 | PRD User Story 8: repeated watch status-poll lines are deduplicated. | pass | Focused integration gate passed, including `TestRunWatchTimeoutOffersManualReviewWithoutFetching`. |
| 9 | Core Feature 1: review artifact hierarchy is explicit Artifact Directory, then spec-associated folder, then `docs/specs/_reviews/pr-<n>/`. | pass | Focused integration gate passed for explicit, spec-associated, invalid trailer fallback, and spec-less cases; docs grep found ADR-0029 hierarchy in README and both Roundfix skill copies. |
| 10 | Core Feature 2: per-Batch Agent logs are default-off, opt-in by config, while the Run Event Journal and Detached Run console log remain durable. | pass | Focused integration gate passed for default-off, opt-in, journal payloads in both states, and Detached Run console logging; docs grep found ADR-0030 and `logs.agent`. |
| 11 | Core Feature 3: archive command verifies all Tasks completed and QA passed, stamps metadata, and moves to `_archived`. | pass | Built CLI `archive --help` verified command surface; focused integration gate passed archive happy path and refusal tests. |
| 12 | Core Feature 4: CodeRabbit table-fragment titles are normalized to plain text. | pass | Focused integration gate passed `TestTitleFromBodyStripsCodeRabbitMarkup`. |
| 13 | Core Feature 5: watch status-poll stderr prints on change only. | pass | Focused integration gate passed `TestRunWatchTimeoutOffersManualReviewWithoutFetching`. |
| 14 | Core Feature 6: missing merge-readiness output includes the docs expectation and next action. | pass | Focused integration gate passed `TestRunWatchMissingHeadCheckPrintsCleanNote`; Roundfix skill documents the current missing-check note. |
| 15 | Task 01 AC: a spec-associated PR via `--spec` or trailer writes under `docs/specs/<slug>/reviews/`. | pass | Focused integration gate passed `TestRunFetchWritesReviewArtifactsUnderSpecSelector` and `TestRunFetchWritesReviewArtifactsUnderTrailerSpec`; built `fetch`, `resolve`, and `watch` help show `--spec`. |
| 16 | Task 01 AC: a spec-less PR writes under `docs/specs/_reviews/pr-<n>/`. | pass | Focused integration gate passed `TestRunFetchWritesReviewArtifactsUnderSpeclessRoot`. |
| 17 | Task 01 AC: an explicitly configured `artifact_dir` wins and its layout is unchanged. | pass | Focused integration gate passed `TestResolveReviewRoot/explicit artifact directory keeps existing layout`. |
| 18 | Task 01 AC: an unknown or invalid trailer slug falls back to spec-less default. | pass | Focused integration gate passed `TestReviewSpecSlugAssociation/unknown trailer slug is no association` and `TestResolveReviewRoot/unknown spec falls back to spec-less root`. |
| 19 | Task 02 AC: default production Runs write zero per-Batch Agent log files. | pass | Focused integration gate passed `TestRunResolveSkipsAgentLogFilesByDefaultAndStillJournals` and related default-config CLI tests. |
| 20 | Task 02 AC: `logs.agent` enabled writes per-Batch Agent log files. | pass | Focused integration gate passed `TestRunResolveWritesAgentLogFilesWhenEnabledAndStillJournals` and `TestRunImplementUsesConfiguredArtifactDirectoryForAgentLogs`. |
| 21 | Task 02 AC: the Run Event Journal contains Agent payloads whether file logs are off or on. | pass | Focused integration gate passed default-off and opt-in journal assertions plus `TestACPXRunPromptAllowsEmptyLogPathAndStillJournals`. |
| 22 | Task 02 AC: Detached Run console log is written regardless of `logs.agent`. | pass | Focused integration gate passed `TestRunImplementDetachPrintsReportAndCompletesRun`; docs state Detached Run console logs are unconditional. |
| 23 | Task 03 AC: archiving a completed, QA-passed Spec moves its folder to `_archived` with metadata. | pass | Focused integration gate passed `TestRunArchiveMovesCompletedSpecAndStampsMetadata`. |
| 24 | Task 03 AC: archiving with any non-completed Task is refused with that condition named and folder left in place. | pass | Focused integration gate passed `TestRunArchiveRefusesIncompleteTask`. |
| 25 | Task 03 AC: archiving with no passing QA verdict is refused with that condition named. | pass | Focused integration gate passed `TestRunArchiveRefusesMissingOrFailingQA`. |
| 26 | Task 03 AC: archive creates no Run, never pushes, and returns a stable non-zero exit code on refusal. | pass | Focused integration gate passed archive tests asserting no Run Database creation, no engine collaborators, and exit code 2 refusal behavior. |
| 27 | Task 04 AC: CodeRabbit table-fragment issue yields a title with no pipes, heading markers, or emoji. | pass | Focused integration gate passed `TestTitleFromBodyStripsCodeRabbitMarkup`. |
| 28 | Task 04 AC: stable watch poll emits one status line across many intervals. | pass | Focused integration gate passed `TestRunWatchTimeoutOffersManualReviewWithoutFetching`. |
| 29 | Task 04 AC: merge-readiness missing output names the documentation expectation and next action. | pass | Focused integration gate passed `TestRunWatchMissingHeadCheckPrintsCleanNote`. |
| 30 | Task 04 AC: stdout remains unchanged for requested command output; changes are stderr/artifact-only. | pass | Focused integration gate passed stdout-stability assertions in the poll and missing-check tests. |
| 31 | Task 05 AC: docs describe the three-branch artifact hierarchy, default-off `logs.agent`, and `roundfix archive`. | pass | Docs grep found `ADR-0029`, `ADR-0030`, `logs.agent`, `roundfix archive`, and `docs/specs/_reviews` in README, agent docs, and both Roundfix skill copies. |
| 32 | Task 05 AC: SKILL.md matches shipped CLI surface and embedded copy is regenerated. | pass | `rtk make skills-sync-check` exited 0 with no drift output; built CLI help exposes `--spec` on review commands and archive help; `rtk ./bin/roundfix skills check` passed. |
| 33 | Task 05 AC: `roundfix skills check` passes and `skills-sync-check` reports no drift. | pass | `rtk make verify` passed the skills check; `rtk ./bin/roundfix skills check` printed `Roundfix skill check passed: roundfix`; `rtk make skills-sync-check` produced no drift output. |
| 34 | Non-goal: Roundfix does not auto-commit or gitignore review artifacts. | pass | Task 01 focused tests and docs confirm placement only; no generated `.gitignore` behavior is introduced by the tested paths. |
| 35 | Non-goal: existing Roundfix Home artifacts are not migrated. | pass | The implemented and tested resolver applies to new review roots only; no migration command or archive/root migration surface appears in CLI help. |
| 36 | Non-goal: Run Event Journal format/storage is unchanged. | pass | Focused integration tests assert journal payloads remain present; no report evidence showed schema or format changes, and full test suite passed. |
| 37 | Non-goal: no general log rotation or retention policy was added. | pass | CLI help and docs expose only `logs.agent`; no rotation/retention surface appeared in exercised CLI help or docs grep. |
| 38 | Non-goal: no unarchive or archive-browsing surface was added. | pass | Built CLI top-level/help checks show `archive` only; no unarchive or browse command is documented or exposed. |

## Failures and blocked items

None.

## Environment

- Worktree: `/Users/marcio/.roundfix/worktrees/roundfix-339f8dac/run_20260706T133314Z_84bb1496c25221c2`.
- Date: 2026-07-06.
- Surfaces exercised: CLI (`bin/roundfix` built by `rtk make verify`), infra/storage behavior through focused Go integration tests, and docs/skill text through grep and skill drift checks.
- Static gate: `rtk make verify` passed with `Go test: 765 passed in 17 packages`, `Roundfix skill check passed: roundfix`, and successful `go build`.
- Focused integration gate: `rtk go test ./internal/config/ ./internal/agent/ ./internal/cli/ ./internal/reviewsource/... ./internal/spec/ ./internal/daemon/` passed with `517 passed in 7 packages`.
- Real CLI help checks: `rtk ./bin/roundfix fetch --help`, `resolve --help`, `watch --help`, and `archive --help` all exited 0 and showed the expected public command surface.
- Skill drift checks: `rtk make skills-sync-check` exited 0 with no output, and `rtk ./bin/roundfix skills check` printed `Roundfix skill check passed: roundfix`.
- No frontend surface exists for this PRD, so no browser QA was required.
