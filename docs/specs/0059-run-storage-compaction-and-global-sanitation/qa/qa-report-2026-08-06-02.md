---
spec: 0059-run-storage-compaction-and-global-sanitation
date: 2026-08-06
build: 5d5d56cc64e9e2ae84ccf2987de17f6814f3a4b5
status: closed
verdict: fail
rows_blocked_environment: 0
rows_blocked_finding: 15
rows_blocked_declared: 0
surfaces: [backend, cli, data, docs]
---

# QA report — Run storage compaction and global sanitation

## Scope and environment

Full rerun of the authored terminal QA gate after corrective Task 07 invalidated
the closed failing report for build `bc44a3ac`. The intended actor is a local
Roundfix operator. The production-like entry point is a freshly built
`roundfix` CLI using isolated Roundfix Homes, scratch SQLite databases, and
scratch Artifact Roots. No Pull Request is open, and this Spec declares no Pull
Request journey. The PRD has no `Unreachable Acceptance` section.

The gate ran from commit `5d5d56cc64e9e2ae84ccf2987de17f6814f3a4b5`
on Run Worktree branch
`roundfix/run-run_20260806T053938Z_989cc852d4ba2c42`. The only worktree delta
before Verification was this QA report, so the binary's `-dirty` build label
does not include an application-code delta. The mandatory protected-tooling
audit failed before flow QA; the gate therefore did not create the planned
scratch storage fixture or run any mutating storage journey.

Evidence directory:
`docs/specs/0059-run-storage-compaction-and-global-sanitation/qa/evidence/2026-08-06-02/`.

High-risk probes: Active Run, concurrent writer, insufficient temporary
capacity, stale preview, and database reopen for compaction; dry-run/apply,
second apply, cross-repository discovery, Review Artifact preservation, and
ambiguous/outside-Home roots for sanitation; outside-Git execution, missing
root, unexpected input, repeated read, and byte-for-byte comparison for the
storage report.

## Project Constraint audit

Failed. The graph is valid: `task_06` is the authored terminal `type: qa` node,
depends on the only non-QA leaf (`task_07`), and every dependency is completed.
The PRD and TechSpec account for all four Project Constraint categories, and
the `9bdaedbe` authorization predates both Spec authoring and Task 07.

Exact `git diff-tree` inspection found that Task 07 changed four protected
plan-characterization golden files not bounded by either active Spec artifact.
The authorization record names the characterization regeneration commands and
focused tests prove the current values are generated, so the defect is not an
absent or late grant and not a hand-edited pin. The defect is exact path
accounting: the PRD and TechSpec say fallout lands in exactly five other files.
F-003 records the full audit; no other authorization-shape problem was found.

## Static gate

- `rtk make verify` — passed on build `5d5d56cc` (exit 0): 3,522 Go
  tests passed across 26 packages; the corpus-budget test, four Skill tests,
  `roundfix skills check`, build, and Spec Consistency Check also passed.
  Spec 0059 reported no consistency findings and only the existing optional
  Vocabulary Contract/reference-index skips.

## Results

| # | Story / criterion / sweep | Actor and surface | Entry point and steps | Expected observable | Independent confirmation / persistence | Evidence | Status |
| - | --- | --- | --- | --- | --- | --- | --- |
| R01 | Authored gate preconditions and Project Constraint/tooling-authority audit | Maintainer; docs/repository | Read manifest, PRD, TechSpec, every Task Result, authorization, ADRs, and exact Task commit paths | QA node is terminal; dependencies completed; constraints and protected-tooling choreography are valid | Graph and chronology passed; exact changed-path audit found four unbounded protected golden files | `evidence/2026-08-06-02/governance-and-focused.txt`; F-003 | fail |
| R02 | Full repository verification | Maintainer; backend/cli/data/docs | Run `rtk make verify` unpiped on this build | Formatter, tests, Skill checks, build, and Spec check exit 0 | Exit 0; 3,522 tests, Skill checks, build, and Spec check passed | `evidence/2026-08-06-02/static-gate.txt` | pass |
| R03 | US1: explicit compaction preview and apply | Operator; CLI/data | Run built `roundfix gc compact`, then `--apply`, on a scratch database with reclaimable pages | Preview and result report before/reclaimable/after; apply matches preview within tolerance | Flow QA cannot start until the complete tooling audit passes | `evidence/2026-08-06-02/governance-and-focused.txt`; F-003 | blocked (finding: F-003 — waits on exact protected-tooling path accounting) |
| R04 | US2: compaction safety refusals | Operator; CLI/data | Invoke apply with an Active Run, concurrent writer, insufficient capacity, and stale preview | Each refusal names its cause before mutation | Flow QA cannot start until the complete tooling audit passes | `evidence/2026-08-06-02/governance-and-focused.txt`; F-003 | blocked (finding: F-003 — waits on exact protected-tooling path accounting) |
| R05 | Compaction interruption/reopen and retention separation | Operator; CLI/data | Exercise cancellation/reopen evidence and ordinary retention sweep | Database remains usable; retention never compacts automatically | Flow QA cannot start until the complete tooling audit passes | `evidence/2026-08-06-02/governance-and-focused.txt`; F-003 | blocked (finding: F-003 — waits on exact protected-tooling path accounting) |
| R06 | US3: global sanitation discovers multiple repositories | Operator; CLI/data | Seed durable Runs for several repositories and invoke `gc sanitize` outside them | Every recorded Artifact Root appears from durable metadata | Flow QA cannot start until the complete tooling audit passes | `evidence/2026-08-06-02/governance-and-focused.txt`; F-003 | blocked (finding: F-003 — waits on exact protected-tooling path accounting) |
| R07 | US5: six root classifications and proof | Cautious operator; CLI/data | Exercise active, orphaned, missing, overridden, outside-Home, and unsafe roots | Each classification and its evidence is reported; ambiguity says preserved | Flow QA cannot start until the complete tooling audit passes | `evidence/2026-08-06-02/governance-and-focused.txt`; F-003 | blocked (finding: F-003 — waits on exact protected-tooling path accounting) |
| R08 | Sanitation deletion boundary, Review Artifacts, and idempotence | Cautious operator; CLI/data | Run dry-run, apply, then second apply with eligible, absent, ambiguous, and Review fixtures | Dry-run removes nothing; apply removes only proven directories; Review/ambiguous paths survive; second apply reclaims zero | Flow QA cannot start until the complete tooling audit passes | `evidence/2026-08-06-02/governance-and-focused.txt`; F-003 | blocked (finding: F-003 — waits on exact protected-tooling path accounting) |
| R09 | US4: read-only storage report grouped everywhere | Operator; CLI/data | Run `storage report` outside Git with repositories, states, tables, roots, and a missing root | One no-flag report groups measured rows/bytes and labels missing roots | Flow QA cannot start until the complete tooling audit passes | `evidence/2026-08-06-02/governance-and-focused.txt`; F-003 | blocked (finding: F-003 — waits on exact protected-tooling path accounting) |
| R10 | Storage report is read-only and input-safe | Operator; CLI/data | Hash database/artifacts; run report twice; try unexpected input and empty Home | Reads change no byte; invalid input refuses; empty Home stays empty | Flow QA cannot start until the complete tooling audit passes | `evidence/2026-08-06-02/governance-and-focused.txt`; F-003 | blocked (finding: F-003 — waits on exact protected-tooling path accounting) |
| R11 | Durable-table lifecycle coverage | Maintainer/operator; docs/data | Follow lifecycle policy and inspect fresh migrated schema | Every durable table has an owner/rule; unknown fixture table fails | Three focused live-schema, negative, and preservation cases passed | `docs/user-guide/run-database-lifecycle.md`; `evidence/2026-08-06-02/governance-and-focused.txt` | pass |
| R12 | Run index, Active Run locks, and Agent Selection preservation | Operator; data/CLI | Seed lifecycle records and run retention/sanitation | Runs, Active locks, and Agent Selections survive; only eligible journal rows prune | Flow QA cannot start until the complete tooling audit passes | `evidence/2026-08-06-02/governance-and-focused.txt`; F-003 | blocked (finding: F-003 — waits on exact protected-tooling path accounting) |
| R13 | Existing per-repository GC remains unchanged | Operator; CLI/data | Run `gc`, `--dry-run`, zero-retention/help cases | Existing Journal Retention and per-repository behavior remain unchanged | Flow QA cannot start until the complete tooling audit passes | `evidence/2026-08-06-02/governance-and-focused.txt`; F-003 | blocked (finding: F-003 — waits on exact protected-tooling path accounting) |
| R14 | Non-Goal: no automatic/threshold/scheduled compaction | Operator; CLI/data/docs | Run retention/startup paths and inspect public help/docs | No ordinary path compacts; compaction remains explicit | Flow QA cannot start until the complete tooling audit passes | `evidence/2026-08-06-02/governance-and-focused.txt`; F-003 | blocked (finding: F-003 — waits on exact protected-tooling path accounting) |
| R15 | Non-Goal: no extra Run-row or Active-lock deletion | Operator; data | Exercise retention and sanitation around Active/terminal Runs | No Run row or Active lock is deleted | Flow QA cannot start until the complete tooling audit passes | `evidence/2026-08-06-02/governance-and-focused.txt`; F-003 | blocked (finding: F-003 — waits on exact protected-tooling path accounting) |
| R16 | Non-Goal: no remote/cloud cleanup or filesystem guessing | Cautious operator; CLI/data/docs | Place unrecorded and outside-Home paths near recorded roots; apply sanitation | Only durable recorded metadata is trusted | Flow QA cannot start until the complete tooling audit passes | `evidence/2026-08-06-02/governance-and-focused.txt`; F-003 | blocked (finding: F-003 — waits on exact protected-tooling path accounting) |
| R17 | Non-Goal: terminal Run Worktree reconciliation unchanged | Operator; CLI/docs | Inspect Task diff and command contracts; credit focused regression from full gate | No Reconcile Command or worktree behavior changes | Exact changed-path audit found no reconciliation path; full gate passed | `evidence/2026-08-06-02/governance-and-focused.txt`; `static-gate.txt` | pass |
| R18 | CLI contract, failure recovery, and vocabulary sweep | Operator; CLI | Exercise help, no-flag commands, invalid/out-of-order input, streams, and exit codes | Canonical, deterministic, actionable output with no partial mutation | Flow QA cannot start until the complete tooling audit passes | `evidence/2026-08-06-02/governance-and-focused.txt`; F-003 | blocked (finding: F-003 — waits on exact protected-tooling path accounting) |
| R19 | Documentation and Roundfix Skill match shipped CLI | Operator/Agent; docs/CLI | Follow lifecycle guide and canonical Skill; compare every new command/flag/refusal | Docs teach shipped report, sanitation, and compaction; mirror is byte-identical | Static Skill identity passed, but its operator flow cannot start until the tooling audit passes | `evidence/2026-08-06-02/governance-and-focused.txt`; F-003 | blocked (finding: F-003 — waits on exact protected-tooling path accounting) |
| R20 | Measurement invariant: no acceptance asserts a recorded size | Maintainer; docs/tests | Search PRD, TechSpec, Tasks, tests, and report assertions | Criteria assert relations/tolerances, not recorded sizes | Every fixed-size match is historical/problem/Result context; tests derive measurements | `evidence/2026-08-06-02/governance-and-focused.txt` | pass |

## Findings

### F-003 — Active Spec artifacts omit four protected tooling paths

- Impact: `Trust-Damage`.
- Actor and journey: maintainer, R01 tooling-authority audit before flow QA.
- Expected: both active Spec artifacts bound every protected tooling path that
  Task 07 changes, while deterministic fallout remains reproducible from the
  sanctioned command.
- Actual: Task 07 commit `5d5d56cc` changes four files under
  `internal/baseline/testdata/plan-characterization/`, but the PRD and TechSpec
  each say deterministic Skill-digest fallout lands in exactly five other
  files. Only Task 07's post-implementation Result names the four golden paths.
- Reproduction: run
  `rtk git diff-tree --no-commit-id --name-only -r 5d5d56cc`, then compare the
  result with both `Project Constraints → Tooling authority` rows. The
  authorization record and Makefile prove the regeneration command exists;
  the focused characterization suite passes, so generated provenance is not
  the failure.
- Evidence: `evidence/2026-08-06-02/governance-and-focused.txt`.
- Affected rows: R01 fails; R03–R10, R12–R16, R18, and R19 are
  finding-blocked because the QA gate prohibits flow QA after any tooling audit
  problem.

## Blocked and skipped

- Fifteen rows are `blocked (finding: F-003 — waits on exact
  protected-tooling path accounting)`. Unblock them by adding the four exact
  plan-characterization golden paths to both active Spec Project Constraint
  rows, then rerun the complete gate from a new build/report.
- Environment-blocked: 0. Declared-blocked: 0. Skipped: 0.

## Coverage

Twenty planned rows closed: 4 pass, 1 fail, 15 finding-blocked, 0
environment-blocked, 0 declared-blocked, 0 skipped, and 0 pending.

- R01 covers the authored node and complete Project Constraint/tooling audit;
  R02 covers the full static gate; R11, R17, and R20 received safe focused or
  static evidence on the current build.
- All five user stories, four Core Features, CLI journeys, destructive data
  probes, and four flow-observed Non-Goals remain finding-blocked because flow
  QA cannot begin after the audit failure.
- No Pull Request journey exists in the Spec. No environment block or
  `Unreachable Acceptance` declaration applies.

## Final verdict

Fail. Account for the four plan-characterization golden paths exactly in both
active Spec Project Constraint rows, then rerun the full 20-row gate; the
repository Verification itself already passes on this build.
