---
spec: 0143-a-repository-says-who-reviews-before-the-pull-request
date: 2026-09-17
build: 655db6e6
auditing_binary: "0.13.1 (5ae17c21, built 2026-09-17 20:32:28 -0300)"
auditor_staleness: "stale: commit ancestry: build commit predates audited tree"
status: closed
verdict: pass
rows_blocked_environment: 1
rows_blocked_finding: 0
rows_blocked_declared: 0
surfaces: [backend, cli, docs]
---

# QA Report

## Scope and environment

This full gate audited commit `655db6e6` from the Run Worktree through the
configuration loader, the `doctor` command, repository history, and the public
guidance. The built user binary was `/private/tmp/roundfix-0143-qa-bin`, produced
from that commit with a sandbox-local Go cache. The actor was a repository
maintainer using Project Config, User Config, and `roundfix doctor`.

`roundfix spec check 0143-a-repository-says-who-reviews-before-the-pull-request
--strict` exited 0 with no findings. The checker skipped only detectors whose
named artifacts were absent. The seeded Auditing Binary predates the audited
tree, as recorded in frontmatter; the feature binary itself was built from the
audited tree.

No Pull Request is open for the Spec target branch. Pull Request journeys are
therefore environment-blocked and use the equivalent-evidence path in row 8.

## Performed repairs

None.

## Assigned repair failures

None.

## Authorization audit inputs

None. The three Task commits changed no Governed Path, so no tooling
authorization applies.

## Mechanical findings

None.

## Static gate

- `make verify`: passed with exit 0 in the Daemon outside the Agent sandbox.
  Per the QA prompt, this gate recorded that result and did not rerun it.
- `roundfix spec check 0143-a-repository-says-who-reviews-before-the-pull-request --strict`:
  passed with exit 0 and no findings.
- `GOCACHE=/private/tmp/roundfix-0143-go-cache go build -o
  /private/tmp/roundfix-0143-qa-bin ./cmd/roundfix`: passed with exit 0.
- `GOCACHE=/private/tmp/roundfix-0143-go-cache go test -count=1 -run
  '^TestPrePRReviewPolicyResolution$' ./internal/config`: passed 8 tests.
- `GOCACHE=/private/tmp/roundfix-0143-go-cache go test -count=1 -run
  '^(TestDoctorReportsPrePRReviewPolicy|TestRunDoctorProfileReadinessProvesEffectiveCategoriesAndReportsCounts|TestRunDoctorRealRepositoryCheckDoesNotMutateState)$'
  ./internal/cli`: passed 10 tests.
- `GOCACHE=/private/tmp/roundfix-0143-go-cache go test -count=1 -run
  '^(TestBuiltinReviewSourceExcludesNitpicks|TestLoadAppliesReviewSourceHierarchy|TestLoadAppliesReviewSourceIncludeNitpicksHierarchy)$'
  ./internal/config`: passed 5 tests.
- `git diff --check 5ae17c21..HEAD`: passed with exit 0.

## Results

| # | Status | Provenance |
| - | --- | --- |
| 1 | pass | Requirement 1; repository Verification |
| 2 | pass | Requirement 2 |
| 3 | pass | Requirement 3 |
| 4 | pass | Requirement 4 |
| 5 | pass | Requirement 5 |
| 6 | pass | Requirement 6; outside-evidence row |
| 7 | pass | Requirement 7 |
| 8 | blocked (environment: no open Pull Request) | Pull Request row |

## Row evidence

### 1

- Actor and surface: Daemon, repository Verification.
- Entry point and steps: the Daemon ran `make verify` against the assembled Run
  Worktree.
- Expected observable: exit 0 with no verification diagnostics.
- Independent confirmation and persistence: the Daemon recorded exit 0 after
  all non-QA Task commits were assembled at `655db6e6`.
- Evidence: the Repository Verification fact supplied in the QA prompt.

### 2

- Actor and surface: maintainer, configuration load boundary.
- Entry point and steps: `TestPrePRReviewPolicyResolution` loaded isolated User
  and Project Config fixtures for `codex`, `claude`, `coderabbit`, `none`, and
  `gemini` through the production loader.
- Expected observable: all four supported values load; `gemini` fails with
  `pre_pr_review.provider`, the offending value, and the complete supported set.
- Independent confirmation: the invalid User Config case remained invalid even
  when Project Config supplied a valid override, proving refusal occurs per
  loaded layer.
- Persistence: a fresh loader instance and fresh filesystem fixture served each
  case.
- Evidence: the focused configuration command passed all 8 cases.

### 3

- Actor and surface: maintainer, layered local configuration.
- Entry point and steps: the same production-loader suite exercised Project
  Config over User Config, User Config over the built-in value, and both layers
  silent.
- Expected observable: `coderabbit/project`, `claude/user`, and `codex/default`
  resolve in precedence order; silence does not resolve to `none`.
- Independent confirmation: row 6 ran the built binary against this repository's
  real silent Project Config and observed `codex/default`.
- Persistence: each resolution was reloaded from disk in its own fixture.
- Evidence: the focused configuration command passed all 8 cases, and the
  built-binary output in row 6 matched the silent-case result.

### 4

- Actor and surface: maintainer, `roundfix doctor` CLI.
- Entry point and steps: `TestDoctorReportsPrePRReviewPolicy` invoked the public
  command dispatcher for project, user, default, and explicit `none` policies.
- Expected observable: one `pre-pr-review` line names provider and source;
  explicit `none` is `skipped` with `review disabled by configuration`; no
  Agent probe is added.
- Independent confirmation: the built binary printed
  `pre-pr-review: ok (provider=codex; source=default)` against the real checkout.
  Its overall exit 1 came from unrelated profile and Codex signature readiness
  failures after the policy line was produced.
- Persistence: repeated command-seam cases constructed a fresh command
  environment, while the real invocation was confirmed against unchanged
  configuration bytes.
- Evidence: the focused CLI command passed 10 tests. The policy test asserted
  zero Agent requests and unchanged existing health-check call counts.

### 5

- Actor and surface: maintainer, existing configuration and Doctor diagnostics.
- Entry point and steps: focused regression cases exercised the built-in
  Review Source, its User/Project hierarchy and `include_nitpicks`; exact Doctor
  output cases exercised every pre-existing check around the new line.
- Expected observable: `review_source` behavior stays intact, existing Doctor
  checks retain their relative order and detail, and Doctor mutates no User
  Config, Project Config, Run state, skill state, or lockfile.
- Independent confirmation: the Daemon's full `make verify` passed after all
  changes were assembled.
- Persistence: `TestRunDoctorRealRepositoryCheckDoesNotMutateState` compared
  complete before/after snapshots of every relevant fixture root and file.
- Evidence: 5 focused Review Source tests and 10 focused Doctor tests passed.

### 6

- Actor and surface: maintainer, repository policy and built CLI.
- Entry point and steps: read the external-to-Spec obligation in
  `docs/agents/agent-instructions.md`, searched this repository's Project Config
  for `pre_pr_review`, built the command from audited commit `655db6e6`, and ran
  `roundfix doctor` from the repository root.
- Expected observable: the guide names `codex`, `claude`, `coderabbit`, and
  explicit `none`, with Project over User over default; the silent repository
  resolves to `codex/default`.
- Independent confirmation: the guide's operative clauses were found at lines
  24 and 68; `rg -n '^pre_pr_review:' .roundfixrc.yml` exited 1 with no match;
  the built command printed `provider=codex; source=default`.
- Persistence: the Project Config SHA-256 remained
  `b47978d9638f05af9518b2ab6094128890da59f0a85719b58a75aa10b9201837`
  before and after the command, and `git status --short` remained limited to the
  seeded untracked `qa/` directory.
- Evidence origin: the setup-owned agent guide and the repository's existing
  `.roundfixrc.yml`, neither authored by this Spec, plus observation through the
  built product.

### 7

- Actor and surface: QA auditor, Spec contract and Git history.
- Entry point and steps: ran strict Spec consistency checking, then enumerated
  every path in Task commits `7016b136`, `1b277997`, and `655db6e6` with
  `git diff-tree --no-commit-id --name-only -r <commit>`.
- Expected observable: the authored promise map is consistent and no Task
  commit changes a Governed Path.
- Independent confirmation: `git diff --name-only 5ae17c21..HEAD` returned only
  the three Task files, two user-guide files, and source/test files under
  `internal/config` and `internal/cli`; `git diff --check` passed.
- Persistence: commit-specific path enumeration binds the audit to immutable
  commit objects rather than Task prose.
- Evidence: strict Spec check exited 0 with no findings. No changed path was a
  repository-tooling configuration, script, ignore file, plugin declaration,
  version pin, `.roundfixrc.yml`, Baseline asset, or setup-owned agent guide.

### 8

- Actor and surface: maintainer, Pull Request controls.
- Entry point and steps: used the Roundfix QA prompt's supervised fact that no
  Pull Request is open and that Pull Request journeys are environment-blocked.
- Expected observable: approval, checks, unresolved threads, Merge-Ready
  acceptance, and review-artifact ancestry are each accounted for.
- Independent confirmation and equivalent evidence: no acceptance was obtained;
  the Daemon's `make verify` passed; no Pull Request exists to carry unresolved
  threads; no Merge-Ready Review Source evidence exists yet; no review artifact
  exists whose ancestry could bind it to expected head `655db6e6`.
- Persistence: the row is not eligible for carry-forward because it depends on
  live service state.
- Evidence: the supplied Pull Request fact and the Daemon's repository
  Verification result. Unblocking action: open the Pull Request later in the
  delivery sequence and observe those controls on the target-branch head.

## Findings

None.

## Blocked and skipped

- Row 8 is blocked by the declared environment fact that no Pull Request is
  open. All five Pull Request controls have equivalent observed or supervised
  evidence, so this environment block does not prevent a pass verdict.
- No row is finding-blocked, declared-blocked, or skipped.

## Coverage

All seven declared verification Requirements are covered. The repository
Verification, outside-evidence, and Pull Request rows are present; the PRD has
no Unreachable Acceptance declaration and does not declare a frontend surface.
Seven rows passed and one row is environment-blocked with equivalent evidence.

`rows_blocked_environment=1`, `rows_blocked_finding=0`, and
`rows_blocked_declared=0`. No risk-based cut or planned row remains.

## Final verdict

Pass: every runnable row passed, the one no-Pull-Request row records its cause
and equivalent control evidence, and no finding-blocked, declared-blocked, or
skipped row remains.

## Mechanical skips

| Detector | Missing artifact |
| --- | --- |
| authorization bounded paths | tooling authorization |
| consequent-fix commit order | consequent-fix declaration |
| QA Report structure | QA Report |
| QA evidence paths | QA Report |
