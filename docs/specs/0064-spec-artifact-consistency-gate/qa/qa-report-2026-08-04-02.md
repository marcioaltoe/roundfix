---
spec: 0064-spec-artifact-consistency-gate
date: 2026-08-04
build: 3c5ff1b4f61af7afa1184b72ed0e67ac72abe73a
status: closed
verdict: pass
rows_blocked_environment: 0
rows_blocked_finding: 0
surfaces: [backend, cli, docs]
---

# QA report — Spec artifact consistency gate

## Scope and environment

Full rerun of the authored terminal `task_09` gate after the prior report's
F-002. Actors are Spec authors, automation users, contributors, and repository
maintainers using the built `roundfix` CLI and documented Make targets on
macOS. Public entry points are `bin/roundfix spec check`, top-level and command
help, `make spec-budget`, and `make verify`.

The Run Worktree branch is
`roundfix/run-run_20260804T165953Z_b9febb7178c3a0b0`; the source commit is
`3c5ff1b4f61af7afa1184b72ed0e67ac72abe73a`. The prompt reports no Open Pull
Request and states that Pull Request journeys are environment-blocked. The PRD
declares no Pull Request surface or Pull Request-dependent criterion, so no
matrix row depends on one.

Evidence directory:
`qa/evidence/2026-08-04-02-spec-check-gate/`. Exact commands, exit codes, and
concise observations are recorded below as each row closes.

### Project Constraint and graph preflight

- The manifest names `task_09` as the only `type: qa` node. It is terminal and
  depends on completed `task_11`, which transitively covers every non-QA node;
  tasks 01–08, 10, and 11 are `completed`, while Daemon-owned task 09 remains
  `pending` during this gate.
- The PRD and TechSpec each account for identifier strategy, authentication and
  HTTP, active ADR obligations, and tooling authority, with applicability,
  reasons, and operative source paths under `docs/agents/`. ADR-0089 is present
  in both Active ADR snapshots after the prior correction.
- Authorization commit `2e560ce` names Spec 0064 and bounds the shipped command
  to `Makefile`; it predates task 08 commit `02c35e2`. Corrective authoring
  commit `c4e7613` predates task 10 `cab533f`, which predates tooling task 11
  `121fabd`. `git diff-tree --no-commit-id --name-only -r` was run for the
  authorization, authoring, implementation, corrective, and prior-finding
  repair commits. Task 08 and task 11 each changed exactly `Makefile` plus
  their own Task file. No skill, configuration, prerequisite fix, consequent
  tooling fix, or out-of-scope tooling path was folded into either tooling
  Task.
- Pre-gate `git -c core.fsmonitor=false status --short` listed only this report
  and its evidence directory. `git diff --check` exited 0.

## Static gate

`rtk make verify` ran without a pipe and exited 0. Its executed stages
reported 3,199 passing tests in 25 packages, one passing dedicated
`TestCheckCorpusBudget`, four passing skill tests, a passing Repository Skill
Set check, a successful `-buildvcs=false` build, and a final all-active
`bin/roundfix spec check` with no findings.

An extra verbose diagnostic initially inherited
`/Users/marcio/Library/Caches/go-build` and was denied by the sandbox. The
public Make target had already passed; one repository-local-cache invocation
then exposed the current numeric budget evidence without changing the build:
the full Spec corpus sweep completed in 315.226291 ms against the unchanged 1 s
budget. No row is blocked because the real public target and its equivalent
focused proof are both observed.

## Results

| # | Story / criterion / sweep | Actor and public entry point | Steps and probes | Expected observable and independent confirmation | Evidence | Status |
| --- | --- | --- | --- | --- | --- | --- |
| Q-01 | Authored-gate precondition; Project Constraints; tooling authorization and commit choreography | Maintainer; Spec artifacts and Git ancestry; docs | Validate graph and dependency status; audit both Constraint sections and operative sources; inspect each implementation/corrective commit with `git diff-tree`; prove authorization chronology and bounded paths; recheck final status. | QA is terminal and runnable; all four Constraint categories are reasoned and sourced in both artifacts; tooling Tasks change only authorized `Makefile` plus their Task files with valid chronology; final delta contains only QA evidence. | Graph/task frontmatter; both Constraint sections; accepted ADRs 0080, 0088, 0089, 0091, 0093, and 0094; authorization `2e560ce`; changed-path audit through `3c5ff1b`; pre-gate clean-scope status and `git diff --check`. | pass |
| Q-02 | Core Feature 1; clean named-Spec text journey; task 09 clean exit | Spec author; `bin/roundfix spec check 0064-spec-artifact-consistency-gate`; CLI/backend | Run twice, then after failure probes; compare output and Spec hashes. | Exit 0, clean named report, no stderr diagnostic; repeat and post-probe run agree; Spec artifacts remain byte-identical. | Two initial runs and the final post-probe canary exited 0 with identical `No findings` output and only expected skips. Final SHA-256 values for `_prd.md`, `_techspec.md`, `_tasks.md`, and all 11 Task files exactly matched the pre-flow set. | pass |
| Q-03 | Core Feature 1; all-active sweep; Goals 1 and 2 | Maintainer; `bin/roundfix spec check`; CLI/backend | Run with no slug twice; compare named Specs and results; measure elapsed time. | Every active Spec is checked once; no error exits nonzero; completion is in seconds; fresh repeat agrees. | The unpiped `make verify` run and a direct no-slug invocation both exited 0, named all nine active Specs once, reported no findings, and preserved presence-aware skips. | pass |
| Q-04 | Core Feature 1; task 04 JSON acceptance | Automation user; `--format json`; CLI/backend | Check one and multiple explicit Specs; parse every JSONL object; retry with supported flags before and after slugs. | One parseable `roundfix-speccheck/v1` object per Spec on stdout; requested slugs present; stderr diagnostic-only; rerun byte-stable. | Explicit 0064+0075 produced two JSONL objects; `jq -e` parsed both as `roundfix-speccheck/v1` with the requested slugs; `cmp` proved a second run byte-identical. Flags before/after the slug also produced byte-identical output. | pass |
| Q-05 | Core Features 2 and 7; task 09 error exit | Spec author; built CLI against scratch error fixture; CLI/backend | Run a located contradiction twice. | Exit 1; stable error code, severity, summary, at least two `path:line` locations, and concrete fix; fixture bytes and repeated output stay unchanged. | Two `tooling-unauthorized` runs exited 1 with byte-identical output: `SC-TOOLING-UNAUTHORIZED`, severity `error`, PRD and authorization `path:line` locations, and a concrete authorization fix. | pass |
| Q-06 | Core Feature 4; gap semantics; task 09 gap exit | Spec author; built CLI against scratch gap-only fixture; CLI/backend | Run default, strict, then default. | Gap-only default exits 0; strict promotes it and exits 1; final default returns to gap/0 with no persisted promotion. | Scratch `gap-only` default exited 0 with `[gap] SC-ADR-RELATED`; `--strict` exited 1 with `[error]`; the final default returned to exit 0 and `[gap]`. | pass |
| Q-07 | Core Feature 3; Project Constraint and tooling detector family | Spec author; built CLI against constraint/tooling fixtures; CLI/backend | Exercise missing row, unreasoned row, missing source, unauthorized, unbounded, clean, and absent-TechSpec cases; repeat key cases. | Dirty fixtures report their named `SC-*` errors with locations; clean exits 0; absent TechSpec records skips rather than errors; repeats are stable. | Missing row, unreasoned row, missing source, unauthorized record, and unbounded tooling each exited 1 with their named located `SC-*` error. `clean` and `no-techspec` exited 0; the latter recorded all TechSpec-dependent detector skips. | pass |
| Q-08 | Core Feature 5; ADR, coverage, and reference resolution | Spec author; built CLI against citation/coverage/reference fixtures; CLI/backend | Exercise direct unlisted ADR, related gap, unmapped range, untasked feature, unresolved reference, and clean collective ranges. | Located errors exit 1; relation candidate remains gap/0; valid ranges are clean; missing targets name declaration and target; reruns agree. | `citation-dirty` produced located `SC-ADR-UNLISTED` plus `SC-ADR-RELATED`; unmapped, untasked, Task Context, and reference-index fixtures produced the expected located errors; `coverage-range` exited 0 clean. | pass |
| Q-09 | Core Feature 6; emitted-vocabulary coverage | Spec author; built CLI against vocabulary fixtures; CLI/backend | Exercise five/four mismatch, satisfied contract, repeated token, invalid pattern, missing paths, and absent contract. | Undocumented fifth token reports once; satisfied and absent cases exit 0 with correct skip; invalid/unreadable inputs return findings without panic; fix teaches all declaration fields. | Five-emitted/four-documented reported only `publish:` once; satisfied and no-contract fixtures exited 0; invalid RE2 and unreadable emitting/documenting paths returned located findings without panic. Every fix showed `emits`, `pattern`, and `documented-in`. | pass |
| Q-10 | Success Metric 1; four motivating finding replays | Maintainer; built CLI against four replay fixtures; CLI/backend | Run every replay and inspect provenance READMEs. | Codes match the source reports: two coverage-unmapped, vocabulary-undocumented, and ADR-unlisted plus ADR-related; provenance states report-authored, not Git-recovered. | 0058 QA-001 → `SC-COVERAGE-UNMAPPED`; 0058 QA-004 → `SC-VOCABULARY-UNDOCUMENTED`; 0056 F-001 → `SC-ADR-UNLISTED` plus `SC-ADR-RELATED`; 0056 F-002 → `SC-COVERAGE-UNMAPPED`. All four READMEs name their source report and state report-authored, not Git-recovered. | pass |
| Q-11 | Success Metrics 2 and 3; controlled budget and observable archived counts | Maintainer; `make spec-budget`, dedicated budget test, active-corpus check, and golden; backend/infra | Run twice and compare measurement/counts. | Each dedicated sweep measures rather than skips and stays below 1 s; active counts are zero; archived counts match the checked-in characterization; repeats agree. | `make verify` and a separate `make spec-budget` each ran one passing dedicated proof. A verbose equivalent measured 315.226291 ms < 1 s. Golden and active-corpus tests passed; active counts are all 0 and archived counts remain 35 related ADR, 35 unlisted ADR, 320 missing Constraint, 10 missing source, 424 unmapped, 30 untasked, 183 unresolved reference, and 10 unbounded tooling findings. | pass |
| Q-12 | CLI and docs sweep; discoverability and canonical vocabulary | New Spec author; top-level help, command help, `make help`, and `CONTEXT.md`; CLI/docs | Follow only documented guidance; compare command, exit, mutation, and vocabulary statements. | Help exposes command, flags, formats, exits, and read-only boundary; Make targets are discoverable; glossary separates consistency findings from QA verdicts. | Top-level and command help expose the command, both formats, strict promotion, exits 0/1/2, and read-only behavior. `make help` lists `spec-check` and `spec-budget`; glossary entries distinguish consistency errors/gaps from QA verdicts. | pass |
| Q-13 | CLI failure and recovery sweep | Spec author; public CLI; CLI | Exercise unknown slug, invalid format, unknown flag, then a valid command; try supported flags before and after slugs. | Invalid input exits 2, names the error on stderr, leaves stdout empty, and suggests help; valid recovery exits 0; supported interspersed flags work. | Unknown slug, `--format yaml`, and `--wat` each exited 2, named the bad input on stderr, suggested command help, and left captured stdout at 0 bytes. Valid text/JSON recovery exited 0; flags worked before and after the slug. | pass |
| Q-14 | Four Non-Goals and scope-creep check | Maintainer; public CLI, help, hashes, and repository search; CLI/docs | Compare pre/post artifact hashes and inspect public output for mutation, verdict, style/correctness judgment, and `implement` precondition. | No artifact mutation, QA verdict emission, correctness/style judgment, or `implement` precondition; independent hashes persist after all probes. | Pre/post hashes for all 14 governed Spec artifacts are identical. Public output contains no QA verdict or auto-correction; help promises declaration/citation checking without style or correctness judgment; `implement --help` contains no consistency precondition. | pass |
| Q-15 | Mandatory repository gate and Makefile integration; regression of F-001 and F-002 | Contributor; `make verify`; CLI/backend/infra | Run full gate unpiped; inspect stage order; confirm owner-process regression and named-Spec canary. | Full gate exits 0; ordinary tests pass; dedicated budget proof measures serially; build precedes fail-closed Spec check; phony/help wiring is present; prior helper defect does not recur. | Full gate exited 0: 3,199 tests passed, including the repaired store process helper path; the dedicated serial budget proof passed; skill checks and build passed; final Spec check was clean. Make help exposes both phony targets, and named-Spec canaries exit 0. | pass |

## Findings

None.

## Blocked and skipped

No row is blocked or skipped. The prompt's no-Open-Pull-Request fact affects
no declared surface or acceptance criterion. One optional verbose Go command
was sandbox-denied because it inherited an external cache path; equivalent
public and repository-local commands passed, so no criterion is unevidenced.

## Coverage

Planned: 15 rows covering all seven Core Features, all three Success Metrics,
all four Non-Goals, task 09 acceptance, and the CLI/backend/docs surfaces.
There are no explicit PRD User Stories. Planned probes cover repeatability,
strict-state leakage, invalid input and recovery, interspersed flags, missing
artifacts, repeated vocabulary, unreadable declarations, read-only
persistence, JSON stability, and controlled budget measurement.

Terminal counts: 15 pass, 0 fail, 0 blocked by environment, 0 blocked by
finding, 0 skipped, and 0 pending. Every planned behavior probe ran. No finding
is open.

## Final verdict

Pass: all 15 rows passed on build
`3c5ff1b4f61af7afa1184b72ed0e67ac72abe73a`; the full gate and live CLI/docs
journeys are clean, and no blocked, skipped, or finding row remains.
