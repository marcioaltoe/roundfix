---
spec: 0091-a-proof-that-can-refuse
date: 2026-08-10
build: d5a8a8df6b56135648c62cf0ebe1e9f7180c98c2
status: closed
verdict: fail
rows_blocked_environment: 0
rows_blocked_finding: 0
rows_blocked_declared: 0
surfaces: [backend, cli]
---

# QA report — A proof that can refuse

## Scope and environment

Full same-day rerun of the authored terminal gate on Run Worktree branch
`roundfix/run-run_20260810T190536Z_aff08f6d1304291f`, build
`d5a8a8df6b56135648c62cf0ebe1e9f7180c98c2`, on macOS in
`America/Sao_Paulo`. The environment was macOS 26.5.2 arm64 with Go 1.26.5.
The actor is a Roundfix maintainer using the built
`roundfix profiles validate` CLI, live ACP runtimes, public Go package runners,
and Git's read-only commit path. The prompt confirms no Pull Request is open;
this Spec declares no Pull Request acceptance journey, and the per-Run branch
must not be pushed or used to resolve one.

`rtk roundfix spec check 0091-a-proof-that-can-refuse --strict` exited 0 with
`No findings.` before this matrix was created. `_tasks.md` names `task_05` as
the terminal `type: qa` node. Its dependencies `task_07` and `task_08` are
completed, and their ancestry reaches every non-QA leaf.

The exercised binary identified itself as
`roundfix 0.4.0 (d5a8a8df-dirty, built 2026-08-10 16:10:30 -0300)`; `dirty`
reflects this report-only QA delta after `HEAD`.

This rerun starts with prior F-007. Evidence root:
`docs/specs/0091-a-proof-that-can-refuse/qa/evidence/2026-08-10-gate-04/`.

## Static gate

`rtk proxy env GOCACHE="$PWD/.gocache" go clean -testcache` exited 0.
The authoritative `rtk make verify` then exited 0. Every tested Go package
reported an execution duration rather than `(cached)`; the focused skill
contracts, repository skill check, and binary build also passed. See
`evidence/2026-08-10-gate-04/make-verify-cold.txt`.

## Results

| # | Story / criterion / sweep | Actor, entry point, and surface | Steps and expected observable | Independent confirmation and persistence | Evidence path | Status |
| - | --- | --- | --- | --- | --- | --- |
| QA-14 | Project Constraint and bounded-scope audit: protected tooling authorization and Task commits respect exact paths and chronology | Maintainer; `git diff-tree`; repository commit surface | Resolve every relevant commit and current delta; expect exact bounded Task paths, authorized `.roundfixrc.yml` chronology, and no unauthorized tooling mutation. | Task commits remain bounded, and final restoration/glossary commits are separate; immutable ancestry still contains mixed commit `061a442e` and mixed revert `cddd1aac`, each changing protected `.roundfixrc.yml` with out-of-grant `CONTEXT.md`. | `evidence/2026-08-10-gate-04/tooling-scope-audit.txt` | fail (repeated F-007) |
| QA-01 | Goal 1, Goal 2, Task 05: live `codex` refusal | Maintainer; built CLI to live Codex ACP adapter; CLI/backend | Validate a scratch profile naming an unoffered Codex model; expect nonzero refusal and whichever adapter- or Roundfix-owned diagnosis fires first under ADR-0119. | Two fresh processes returned the same adapter-first refusal; both full repository validations then passed the configured Codex tuples. | `evidence/2026-08-10-gate-04/live-codex.txt` | pass |
| QA-02 | Goals 1–2, Task 03, Task 05: exact formerly passing `claude / opus-9-does-not-exist / high` tuple now refuses | Maintainer; built CLI to live Claude ACP adapter; CLI/backend | Validate the PRD's exact tuple; expect refusal, and an advertised set when Roundfix owns the verdict. | Two fresh processes replaced the PRD's `passed` state with the same adapter-first refusal; configured `opus/high` then passed twice. | `evidence/2026-08-10-gate-04/live-claude.txt` | pass |
| QA-03 | Goals 1–2, Task 05, outside evidence: live `opencode` refusal | Maintainer; built CLI to live OpenCode ACP adapter; CLI/backend | Validate a scratch profile naming an unoffered OpenCode model; expect refusal under ADR-0119. The outside source is the installed adapter's live behavior, not a Spec fixture. | Two fresh OpenCode 1.18.15 processes returned the same adapter-first `model not found`; the configured tuple then passed twice. | `evidence/2026-08-10-gate-04/live-opencode.txt` | pass |
| QA-04 | Goal 5, Task 03, Task 05, Task 07: repository's five distinct tuples retain outcomes and encodings | Maintainer; built CLI against `.roundfixrc.yml`; CLI/backend | Run `profiles validate`; expect all configured selections to pass, including `claude/opus/high`, with established encodings. | Two fresh processes exited 0 with the same five proofs and encodings; no effective mismatch remained. | `evidence/2026-08-10-gate-04/repository-profile.txt` | pass |
| QA-05 | Goal 4, Tasks 04 and 08: pre-session failure ends at the actionable diagnosis | Maintainer; built CLI with a pre-session failure; CLI/backend | Trigger installed ACPX's exit-1 `No named session` shape; expect no appended cleanup error while the observation remains recorded. | Sandbox reproduction emitted a separate cleanup warning; the returned diagnosis ended at the original Codex failure, and exact backend regressions passed. | `evidence/2026-08-10-gate-04/cleanup-diagnosis.txt` | pass |
| QA-06 | Tasks 01, 03, 06, 07, and 08 regression corpus | Maintainer; Go public package runners; backend/CLI | Run the renamed characterization, all sibling sequence cases, matching/mismatch cases, and both Task 08 fixtures; expect every named contract to pass. | All selected Agent and CLI cases passed, including both detach journeys; the cold full gate passed as the adjacent canary. | `evidence/2026-08-10-gate-04/regression-corpus.txt` | pass |
| QA-07 | Task 05 authoritative cold gate | Maintainer; repository verification; backend/CLI | Clear the Makefile-exported worktree cache and run exact `rtk make verify`; expect exit 0 and no Go package line marked `(cached)`. | Exit 0 after a fresh cache clear; every package carried a duration, and skill contracts plus binary build passed. | `evidence/2026-08-10-gate-04/make-verify-cold.txt` | pass |
| QA-08 | Task 05 vocabulary acceptance: `Runtime Catalogue` is coined and emitted vocabulary is honest | Maintainer; `CONTEXT.md`, TechSpec, production search; docs/CLI | Resolve the coined term and compare the Vocabulary Contract with emitted production tokens; expect documentation and no unsupported public encoding claim. | `CONTEXT.md` defines the term, production carries contamination as proof evidence without emitting a speculative token, and strict Spec check passed. | `evidence/2026-08-10-gate-04/glossary.txt` | pass |
| QA-09 | Goal 3, Task 02, Task 05: catalogue proof sends no prompt | Maintainer; built CLI plus observable ACP/session evidence; CLI/backend | Validate live selections; expect catalogue ensure/show with no prompt or work turn. | Six fresh closed Claude Sessions each reported `0/0` and `No history`; exact no-prompt package cases passed. | `evidence/2026-08-10-gate-04/token-free.txt` | pass |
| QA-10 | Task 02 seam: honest catalogue, canonical `opus` binding, contamination record, and unchanged intermediate verdicts | Maintainer; Go public package runner; backend | Exercise named catalogue cases; expect pre-request models, `opus` to `opus[1m]`, contamination recording, and no Task-02 verdict change. | Named catalogue cases passed; valid live Claude and two full repository validations supplied assembled canaries. | `evidence/2026-08-10-gate-04/backend-catalogue.txt` | pass |
| QA-11 | Task 03 seam: membership net, adapter fast path, valid encodings, and contamination advisory | Maintainer; Go public runner and built CLI; backend/CLI | Exercise membership, advertised-set, fast-path, encoding, contamination, and Claude characterization cases under ADR-0119. | Focused cases passed; all three live adapters refused twice, while five valid tuples retained their encodings twice. | `evidence/2026-08-10-gate-04/backend-membership.txt` | pass |
| QA-12 | Tasks 04 and 08 seam: both missing-session shapes are omitted, genuine close failure stays appended, and unrelated exit 1 is preserved | Maintainer; Go public package runner; backend | Run exact close/absence tests; expect exit 4 and installed exit 1 omission, recorded warnings, genuine cleanup chaining, and unchanged unrelated exit-1 classification. | Exact diagnostic/error-type cases passed for both absent shapes, genuine close failure, and unrelated exit 1. | `evidence/2026-08-10-gate-04/backend-cleanup.txt` | pass |
| QA-13 | PRD Non-Goals: no vendor-existence validation, reasoning-policy change, picker/recommendation, or automatic repair | Maintainer; built CLI, help, Git, and scratch-config read paths; CLI/backend | Refuse from runtime advertisement, inspect CLI help and relevant commits, prove established reasoning encodings, and confirm scratch profiles remain unchanged. | Encodings persisted, help stayed read-only, commits add no picker, and all three executed scratch profiles remained byte-identical. | `evidence/2026-08-10-gate-04/non-goals.txt` | pass |

Planned high-risk probes are fresh-process repeat after invalid selections,
valid-after-invalid recovery, scratch-profile immutability, an unrelated exit-1
canary, and a genuine effective-selection mismatch after alias normalization.

## Findings

### Repeated F-007 — Protected profile edits remain bundled with glossary edits in ancestry

- Impact: Trust-Damage.
- Actor and step: a maintainer audits the protected `.roundfixrc.yml`
  restoration after the attempted commit split.
- Expected: every protected tooling mutation occupies an exact bounded commit;
  the grant permits only `.roundfixrc.yml` entries naming
  `claude` / `opus` / `high`.
- Actual: `061a442e` and its later revert `cddd1aac` each change both
  `.roundfixrc.yml` and `CONTEXT.md`. The final reapplication is split across
  `af0967ec` and `8ec897c9`, but it does not remove the two mixed commits from
  ancestry.
- Reproduction: run the `diff-tree` commands recorded in
  `evidence/2026-08-10-gate-04/tooling-scope-audit.txt` and compare each result
  with the authorization's sole `paths` entry.
- Affected row: QA-14.

## Blocked and skipped

No row is blocked or skipped. The first Codex reproduction was intentionally
run inside the sandbox to reach the installed exit-1 missing-Session shape; a
single authorized full-access rerun then reached the live runtime, so no
acceptance scope remains environment-blocked. The prompt confirms no Pull
Request is open, and this Spec defines no Pull Request journey.

## Coverage

- Rows: 14 total — 13 pass, 1 fail, 0 environment-blocked, 0
  finding-blocked, 0 declared-blocked, 0 skipped, 0 pending.
- PRD: all five Goals and all four Non-Goals pass through the built CLI and
  backend public runners.
- Tasks: every acceptance cluster is mapped. Tasks 07 and 08 remain effective;
  live refusal, five-tuple, no-prompt, cleanup, vocabulary, regression, and
  cold-verification rows pass. The commit-dependent tooling audit repeats
  F-007.
- Surfaces: built CLI, backend package runners, live Codex/Claude/OpenCode ACP
  adapters, closed-session history, cold repository gate, and Git commit read
  path.
- Outside evidence: installed OpenCode 1.18.15 through ACPX 0.13.0 refused the
  invented model in two fresh processes; this source is outside the Spec's
  fixtures.
- Probes: fresh-process refusal repeats, valid-after-invalid recovery,
  scratch-profile immutability, unrelated exit-1 preservation, genuine
  effective-selection mismatch, and two complete five-tuple validations.
- Regression history: prior F-001 is accepted under ADR-0119; F-002 through
  F-006 remain fixed; F-007 repeats.

## Final verdict

Fail: 1 of 14 rows fails with one Trust-Damage finding. The assembled feature,
all five PRD Goals, all four Non-Goals, live-runtime flows, and authoritative
cold gate pass, but the protected-tooling chronology remains invalid: rebuild
the target-branch ancestry so every `.roundfixrc.yml` mutation is an exact
bounded commit with no `CONTEXT.md` sibling, then rerun the full gate.
