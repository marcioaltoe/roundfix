---
spec: 0091-a-proof-that-can-refuse
date: 2026-08-10
build: 918735265a0e7d6584c9d78c482210b9b86c243c
status: closed
verdict: fail
rows_blocked_environment: 0
rows_blocked_finding: 0
rows_blocked_declared: 0
surfaces: [backend, cli]
---

# QA report — A proof that can refuse

## Scope and environment

Full authored terminal gate on the Run Worktree branch
`roundfix/run-run_20260810T152444Z_e5f558a385a9311e`, build
`918735265a0e7d6584c9d78c482210b9b86c243c`, on macOS in
`America/Sao_Paulo`. The actor is a Roundfix maintainer validating Agent
Selection profiles through the built `roundfix profiles validate` CLI and the
live ACP runtimes. The exercised artifact identified itself as
`roundfix 0.4.0 (91873526-dirty, built 2026-08-10 12:56:14 -0300)`; `dirty`
is the report-only QA artifact added after `HEAD`. The prompt confirms that no Pull Request is open; this Spec
declares no Pull Request acceptance journey, and the per-Run branch must not be
pushed or used to resolve one.

`rtk roundfix spec check 0091-a-proof-that-can-refuse --strict` exited 0 with
`No findings.` before this matrix was created. `_tasks.md` names `task_05` as
the terminal `type: qa` node. Its dependencies `task_03` and `task_04` are
completed, and every non-QA leaf reaches it.

Evidence root:
`docs/specs/0091-a-proof-that-can-refuse/qa/evidence/2026-08-10-gate-01/`.

## Static gate

`GOCACHE="$PWD/.gocache" rtk go clean -testcache` exited 0 immediately before
the final authoritative run. `rtk make verify` exited 2 with no package marked
`(cached)`. Every listed package passed except `internal/cli`:

- `TestRunImplementDetachPrintsReportAndCompletesRun` failed because Task 06's
  fake catalogue advertised only `gpt-5.6-sol`; the built-in
  `codex/gpt-5.5/xhigh` fallback was refused before Run creation.
- `TestRunImplementDetachSurvivesCallerProcessGroupKill` timed out waiting for
  the first detach line on the same blocked preflight path.

The earlier full-suite-only
`TestRunImplementQueuedCancellationStartsNoChildAndKeepsResumableTasks`
timeout passed alone and did not repeat in the final cold run. The gate stopped
at the failing `test` prerequisite, so later `make verify` prerequisites did
not run in that command. See `evidence/2026-08-10-gate-01/make-verify-cold.txt`.

## Results

| # | Story / criterion / sweep | Actor and surface | Steps and expected observable | Independent confirmation and persistence | Evidence path | Status |
| - | --- | --- | --- | --- | --- | --- |
| QA-01 | Goal 1, Goal 2, Task 05: live `codex` refusal | Maintainer; built CLI → live Codex ACP adapter | Validate a scratch profile naming a model absent from Codex's advertised catalogue; expect nonzero exit, `model_not_advertised`, and the advertised set. | Exit 2 was `selection_rejected` from `session/set_config_option`; no advertised set. A valid Codex tuple then passed. | `evidence/2026-08-10-gate-01/live-codex.txt` | fail (F-001) |
| QA-02 | Goals 1–2, Task 03, Task 05: the exact formerly passing `claude / opus-9-does-not-exist / high` tuple now refuses | Maintainer; built CLI → live Claude ACP adapter | Validate the PRD's exact tuple; expect nonzero exit, `model_not_advertised`, and `default`, `opus[1m]`, `claude-fable-5[1m]`, `sonnet`, `haiku` in the advertised set. | Two fresh processes returned the same `selection_rejected` result without the advertised set. The PRD before-state was `passed`. | `evidence/2026-08-10-gate-01/live-claude.txt` | fail (F-001) |
| QA-03 | Goal 1, Goal 2, Task 05, outside evidence: live `opencode` refusal | Maintainer; built CLI → live OpenCode ACP adapter | Validate a scratch profile naming a model absent from OpenCode's advertised catalogue; expect nonzero exit, `model_not_advertised`, and the advertised set. The external source is the installed OpenCode ACP adapter's live catalogue, not a Spec fixture. | OpenCode 1.18.15 returned `selection_rejected` and `model not found`, but Roundfix omitted its promised classification and advertised set. | `evidence/2026-08-10-gate-01/live-opencode.txt` | fail (F-001) |
| QA-04 | Goal 5, Task 03, Task 05: this repository's five distinct configured tuples retain their outcomes and encodings | Maintainer; built CLI against `.roundfixrc.yml` | Run `profiles validate`; expect all configured selections to pass and the five distinct runtime/model/effort tuples to retain their established encodings, including `independent` and `runtime_deferred` where applicable. | Four tuples passed with established encodings when isolated; valid `claude/opus/high` failed `effective_selection_mismatch`, leaving two pending in the assembled command. | `evidence/2026-08-10-gate-01/repository-profile.txt` | fail (F-002) |
| QA-05 | Goal 3, Task 02, Task 05: catalogue proof sends no prompt | Maintainer; built CLI plus observable ACP invocation record | Validate live selections while capturing the adapter command/event trace; expect catalogue ensure/show and no prompt/work turn. | Codex/OpenCode left no session; both closed Claude preflight records had `0/0` history and `No history`. Three no-prompt public-runner cases passed. | `evidence/2026-08-10-gate-01/token-free.txt` | pass |
| QA-06 | Goal 4, Task 04: a pre-session proof failure ends at the actionable diagnosis | Maintainer; built CLI with an invalid selection | Trigger refusal before a disposable session opens; expect no trailing cleanup error, while the failure remains recorded. | A sandbox-caused pre-session Codex failure appended `No named session` cleanup noise. Fixture regressions passed but did not match the assembled path. | `evidence/2026-08-10-gate-01/cleanup-diagnosis.txt` | fail (F-003) |
| QA-07 | Task 02 seam: honest catalogue, canonical `opus` binding, contamination record, and unchanged intermediate verdicts | Maintainer; backend public runner | Exercise the named catalogue cases; expect the pre-request models, `opus` → `opus[1m]`, contamination recording, and no Task-02 verdict change. | Fresh command passed all three named catalogue cases; live adapters supplied the assembled canary. | `evidence/2026-08-10-gate-01/backend-catalogue.txt` | pass |
| QA-08 | Task 03 seam: membership owns refusal, adapter fast path remains, valid encodings remain, contamination stays advisory | Maintainer; backend public runner and built CLI | Exercise all five named membership/fast-path/encoding/contamination cases; expect their stated observable contracts. | Six focused cases passed, but all three live invalid models reached the adapter application step and returned the wrong classification without an advertised set. | `evidence/2026-08-10-gate-01/backend-membership.txt` | fail (F-001) |
| QA-09 | Task 04 seam: missing-session close is recorded but omitted; genuine close failure remains appended; leading diagnosis stays byte-identical | Maintainer; backend public runner | Run both named close cases; expect exact diagnosis equality in the absent-session case and an `AgentSessionCleanupError` in the genuine close-failure chain. | Two focused cases passed; the built CLI's missing-session path contradicted the omitted-suffix observable. | `evidence/2026-08-10-gate-01/backend-cleanup.txt` | fail (F-003) |
| QA-10 | Task 01 and Task 06 regression corpus: declared break transitioned in place and all sibling sequence contracts remain intact | Maintainer; backend/CLI public runners | Run the renamed characterization plus all sixteen sibling tests; expect all cases to execute and pass without deleted or weakened journey contracts. | The Agent corpus passed, but two named Task 06 CLI cases failed reproducibly because the corrective fake catalogue omitted the built-in `gpt-5.5` fallback. | `evidence/2026-08-10-gate-01/regression-corpus.txt` | fail (F-005) |
| QA-11 | Task 05 vocabulary acceptance: `Runtime Catalogue` is coined and public emitted terms are documented | Maintainer; docs/CLI contract | Search `CONTEXT.md` for `Runtime Catalogue`, `catalogue_contaminated`, and `catalogue_unavailable`; expect the coined concept and emitted vocabulary to resolve. | The glossary search exited 1 with no matches; producer search found `RuntimeCatalogue` but neither declared emitted term. | `evidence/2026-08-10-gate-01/glossary.txt` | fail (F-004) |
| QA-12 | PRD Non-Goals: no vendor-existence validation, reasoning-policy change, picker/recommendation, or automatic repair | Maintainer; built CLI and Git/config read path | Refuse based on runtime behavior, inspect CLI help, prove established reasoning encodings, and confirm scratch configs remain unchanged. | Existing recommendations remain advisory, validation did not rewrite scratch configs, no production picker/configure path changed, and passing tuples retained their established encodings. | `evidence/2026-08-10-gate-01/non-goals.txt` | pass |
| QA-13 | Task 05 authoritative cold gate | Maintainer; repository verification | Clear the Makefile-exported worktree cache and run exact `rtk make verify`; expect exit 0 and no Go package line marked `(cached)`. | Cache clear exited 0; the uncached gate exited 2 on the two deterministic Task 06 CLI failures. | `evidence/2026-08-10-gate-01/make-verify-cold.txt` | fail (F-005) |
| QA-14 | Project Constraint and bounded-scope audit: no protected tooling mutation; implementation and corrective commits respect declared paths | Maintainer; Git commit read path | Resolve every Spec Task commit with `git diff-tree`; expect no tooling configuration/script/version-pin mutation and no implementation path outside each Task's bounded scope. | All seven relevant commits stayed inside their Task/authoring bounds and changed no protected tooling path. | `evidence/2026-08-10-gate-01/tooling-scope-audit.txt` | pass |

Behavior probes attempted were fresh-process repeat of the exact Claude
refusal, recovery through valid isolated tuples after invalid selections,
fresh-process isolated validation of the two tuples the assembled command left
pending, and scratch-config immutability. They targeted the high-risk CLI
failure path without spending an Agent work turn.

## Findings

### F-001 — Live unoffered models bypass the catalogue-owned refusal

- Impact: Blocks-Completion.
- Actor and step: a maintainer runs the built `profiles validate` command for
  an unoffered model on Codex, Claude, or OpenCode.
- Expected: Roundfix refuses before applying the model with
  `model_not_advertised` and the live advertised set.
- Actual: all three adapters reach requested-model application and return
  `selection_rejected`; no advertised set is present. The exact Claude tuple
  changed from the PRD's 2026-08-09 `passed` state, but not through the promised
  Roundfix-owned verdict.
- Reproduction: run the command from each retained scratch directory under
  `evidence/2026-08-10-gate-01/scratch/` with the built binary and full adapter
  access.
- Affected rows: QA-01, QA-02, QA-03, QA-08.

### F-002 — A valid repository Claude selection is rejected as mismatched

- Impact: Blocks-Completion.
- Actor and step: a maintainer runs `bin/roundfix profiles validate --json`
  against this repository.
- Expected: all five distinct configured tuples prove with their established
  encodings.
- Actual: Codex Sol/high and OpenCode Flash/max pass, then valid
  `claude/opus/high` fails `effective_selection_mismatch` even though the
  diagnostic prints requested and observed as `opus/high`. Luna/max and
  Sonnet/xhigh pass when isolated.
- Evidence: `evidence/2026-08-10-gate-01/repository-profile.txt`.
- Affected row: QA-04.

### F-003 — A missing disposable session still adds cleanup noise

- Impact: Friction.
- Actor and step: a maintainer receives a proof failure before the Codex
  disposable session opens.
- Expected: the actionable selection diagnosis ends without a close error for
  a session that never existed; the close problem remains recorded.
- Actual: the built CLI appended `No named session` cleanup text after the
  pre-session state-initialization failure. Both fixture regressions pass, so
  they do not cover this assembled error shape.
- Evidence: `evidence/2026-08-10-gate-01/cleanup-diagnosis.txt`.
- Affected rows: QA-06, QA-09.

### F-004 — The coined Runtime Catalogue vocabulary is absent

- Impact: Trust-Damage.
- Actor and step: a maintainer looks up the public term and emitted encodings
  named by the TechSpec Vocabulary Contract.
- Expected: `CONTEXT.md` documents `Runtime Catalogue`,
  `catalogue_contaminated`, and `catalogue_unavailable`.
- Actual: the glossary contains none of them; the producer defines the Go type
  but contains neither declared emitted encoding.
- Evidence: `evidence/2026-08-10-gate-01/glossary.txt`.
- Affected row: QA-11.

### F-005 — Task 06's fake catalogue breaks two named Run journeys

- Impact: Blocks-Completion.
- Actor and step: a maintainer runs the repository's authoritative cold gate.
- Expected: all sixteen sibling tests Task 06 claims pass, and `make verify`
  exits 0.
- Actual: the fake ACPX default advertises only `gpt-5.6-sol`, so the built-in
  `codex/gpt-5.5/xhigh` fallback is refused before two detach journeys create a
  Run. Both tests fail alone and in the uncached full suite.
- Evidence: `evidence/2026-08-10-gate-01/regression-corpus.txt` and
  `evidence/2026-08-10-gate-01/make-verify-cold.txt`.
- Affected rows: QA-10, QA-13.

## Blocked and skipped

No row is blocked or skipped. The first sandboxed live validation could not
write Codex state under `~/.codex`, and the first gate attempt could not reach
`api.github.com`; each journey was rerun once with authorized full access, so
neither environmental boundary remains a blocked row. The prompt confirms no
Pull Request is open, but this Spec declares no Pull Request acceptance
journey.

## Coverage

- Rows: 14 total — 4 pass, 10 fail, 0 environment-blocked, 0 finding-blocked,
  0 declared-blocked, 0 skipped, 0 pending.
- PRD: five of five goals and four of four Non-Goals exercised; Goals 1, 2, 4,
  and 5 fail, while Goal 3 passes.
- Tasks: all acceptance clusters mapped; Task 06's claimed sibling result and
  Task 05's live-refusal, repository-profile, glossary, and cold-gate criteria
  fail.
- Surfaces: built CLI plus backend public runners; live Codex, Claude, and
  OpenCode adapters.
- Outside evidence: OpenCode 1.18.15 through ACPX 0.13.0, plus the installed
  Codex ACP 1.1.14 and Claude Agent ACP 0.66.0, all observed live rather than
  through Spec fixtures.
- Probes: fresh-process repeat, valid-after-invalid recovery, isolated tuple
  reopen, and config immutability.

## Final verdict

Fail: 10 of 14 rows fail across five findings (three Blocks-Completion, one
Trust-Damage, one Friction); fix the live membership/refusal path, valid
Claude `opus` binding, missing-session cleanup classification, glossary
contract, and Task 06 fake catalogue, then rerun the full gate from a cold
worktree cache.
