---
status: pending
type: docs
---

# Task: The authoring skills name the probing check

`--run-verification` appears zero times across every skill in this repository.
An author following the shipped guidance therefore runs the form of the Spec
Consistency Check that never executes the authored Verification commands, and a
command that cannot fail survives to a Run. This is the only part of this Spec
that reaches an author who reads no diagnostic.

## Tooling authorization

Express maintainer authorization granted 2026-08-26, recorded at
`docs/workflow/authorizations/2026-08-30-the-authoring-skills-name-the-probing-check.md`.

Bounded files, and nothing else:

- `.agents/skills/write-prd/SKILL.md`
- `.agents/skills/write-techspec/SKILL.md`
- `.agents/skills/write-tasks/SKILL.md`
- `.agents/skills/qa-gate/SKILL.md`
- `skills/write-prd/SKILL.md`
- `skills/write-techspec/SKILL.md`
- `skills/write-tasks/SKILL.md`
- `skills/qa-gate/SKILL.md`

The four copies under `skills/` are regenerated by the declared
`make skills-sync`; run it rather than hand-editing them. Changing any other
path fails this Task; stop before mutating one.

## Work

- Each skill's instruction to run the Spec Consistency Check names the probing
  form, so an author following it reaches the shared prober.
- The skill that owns the Verification contract states what a clean verdict
  from the non-probing form does not cover, and stops presenting that form's
  speed as a property to prefer — the speed is a consequence of the omission.
- Read every claim from the delivered command, not from this Spec's TechSpec.
  Where the two disagree, the command is the fact and the TechSpec is
  corrected.
- Stay inside the grant. It does not cover the Verification prober's
  classification, the Daemon's pre-work probe, the gate's verdict rules, or any
  other skill.

## References

- `_prd.md` → Goal 1, User Story 1, Core Feature 1; Project Constraints,
  Tooling authority
- `_techspec.md` → Build Order 5
- `references/2026-08-14-a-clean-checker-left-eight-vacuous-commands-standing.md`
  is the adopted measurement this Task closes

## Verification
- `for s in write-prd write-techspec write-tasks qa-gate; do grep -q -- "--run-verification" ".agents/skills/$s/SKILL.md" || exit 1; grep -q -- "--run-verification" "skills/$s/SKILL.md" || exit 1; done; go test -count=1 -tags docscontract ./internal/docscontract && go test -count=1 ./skills`
