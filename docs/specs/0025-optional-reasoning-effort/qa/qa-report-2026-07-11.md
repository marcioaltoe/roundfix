---
spec: 0025-optional-reasoning-effort
date: 2026-07-11
verdict: pass
surfaces: [cli, backend, docs]
---

# QA Report — Optional Reasoning Effort

## Summary

The QA gate validated the optional Default Reasoning Effort behavior across the
backend Agent selection path, non-interactive CLI commands, Interactive Input,
Project Config, and documentation surfaces. The full repository verification
gate passed, focused behavior tests passed, and real CLI Doctor smoke flows
confirmed model-managed preflight skips the reasoning set call while rejected
non-empty values fail loudly with the documented recovery text.

## Results

| # | Criterion / user story | Verdict | Evidence |
| - | ---------------------- | ------- | -------- |
| 1 | PRD goal: a runtime configured with empty Default Reasoning Effort selects the Agent Model without assigning reasoning, and the Run records model-managed reasoning. | pass | `rtk make verify` passed; focused tests `TestACPXProbeSkipsEmptyReasoningEffort`, `TestACPXRunSkipsEmptyReasoningEffort`, `TestRunResolveAcceptsExplicitEmptyReasoningEffort`, `TestRunWatchRendersModelManagedReasoningHeader`, and `TestRunImplementAcceptsExplicitEmptyReasoningEffort` passed in `rtk go test ./internal/agent ./internal/cli ./internal/config ./internal/tui -count=1 -run ...` (`20 passed in 4 packages`). |
| 2 | PRD goal: a configured non-empty Default Reasoning Effort keeps the existing rejection contract with no fallback. | pass | Scratch Doctor flow with `reasoning_effort: extreme` and fake acpx rejection exited failed on `set reasoning_effort`, printed recovery including `set runtimes.codex.reasoning_effort "" when the model manages reasoning`, and `/tmp/roundfix-qa-0025/reject-acpx.jsonl` recorded the set call before disposable session close. |
| 3 | PRD goal: this repository's Project Config drives codex with `gpt-5.6-sol` as the Default Agent Model. | pass | `.roundfixrc.yml` inspection shows `runtimes.codex.model: gpt-5.6-sol` with `reasoning_effort: ""`; Doctor smoke with signed disposable `CODEX_PATH` reported `agent: ok (codex)` and acpx ensure used `--model gpt-5.6-sol`. |
| 4 | PRD goal: Claude built-in Default Reasoning Effort ships empty so `--agent claude` can select only its Agent Model. | pass | `TestBuiltinRuntimeDefaults` and `TestResolveSelectionUsesBuiltInRuntimeDefaults` passed; docs inspection shows Claude built-in guidance as `model: opus`, `reasoning_effort: ""`. |
| 5 | PRD core feature: empty `runtimes.<runtime>.reasoning_effort` is valid in selection, disposable preflight, and live Agent Session setup. | pass | Doctor smoke output was `agent: ok (codex)`; `/tmp/roundfix-qa-0025/doctor-acpx.jsonl` recorded `--version`, `sessions ensure`, and `sessions close` only, with no `set reasoning_effort`; focused agent tests passed. |
| 6 | PRD core feature: rejected non-empty reasoning values fail Preflight Validation and name model-managed remediation. | pass | Scratch rejecting Doctor flow recorded `codex set reasoning_effort extreme`, exited failed, and printed recovery text naming `runtimes.codex.reasoning_effort ""`; `TestACPXProbeSelectionRejectionClosesDisposableSession` passed. |
| 7 | PRD core feature: Run headers, Run row, Attach, and Interactive Input represent model-managed reasoning honestly. | pass | Focused CLI/TUI tests passed for resolve/watch/implement headers and Run-row persistence; `TestCollectInputAcceptsModelManagedReasoningDefault` and `TestCollectInputOpenCodeAcceptsTypedModelWithModelManagedReasoning` passed; full `rtk make verify` passed existing Attach coverage. |
| 8 | PRD core feature: `.roundfixrc.yml`, README, CONTEXT.md, and both Roundfix Skill copies document the optional semantics with zero drift. | pass | Direct inspection confirmed the config and docs text; `rtk make skills-sync-check` exited 0; `rtk make verify` ran `roundfix skills check` successfully. |
| 9 | PRD non-goal: built-in Default Agent Model constants and codex built-in Default Reasoning Effort are not changed. | pass | `TestBuiltinRuntimeDefaults` passed with built-in Codex `gpt-5.5` / `xhigh` and Claude `opus` / empty reasoning. |
| 10 | PRD non-goal: no per-model effort metadata or static Model Catalog effort enforcement shipped. | pass | Code inspection shows Model Catalog entries remain label/value/description data; validation still delegates selected model/effort pairs to runtime preflight. |
| 11 | PRD non-goal: rejected non-empty reasoning values are not tolerated or retried. | pass | Scratch rejecting Doctor flow failed on the first rejected `set reasoning_effort`; acpx log showed no fallback selection and only the cleanup `sessions close` after rejection. |
| 12 | Task 01 AC: RuntimeSpec with model and empty effort passes validation; preflight assigns model and skips reasoning set. | pass | Doctor smoke with Project Config passed `agent: ok (codex)` and acpx log contained model ensure plus close, with no reasoning set; `TestACPXProbeSkipsEmptyReasoningEffort` passed. |
| 13 | Task 01 AC: live Agent Session for an empty-effort selection issues no reasoning set call. | pass | `TestACPXRunSkipsEmptyReasoningEffort` passed in the focused agent/CLI/config/TUI test run. |
| 14 | Task 01 AC: non-empty effort still issues the set call, and runtime rejection produces selection preflight failure with model-managed recovery copy. | pass | Scratch rejecting Doctor acpx log recorded `set reasoning_effort extreme`; output included the model-managed recovery text; `TestACPXProbeSelectionRejectionClosesDisposableSession` passed. |
| 15 | Task 01 AC: RuntimeSpec without a model still fails selection validation. | pass | `TestACPXRunRequiresModelSelection` passed in the focused behavior suite; explicit empty `--model ""` real CLI smoke exited 2 with `--model cannot be empty`. |
| 16 | Task 01 AC: Claude built-in defaults resolve to a model with empty reasoning, and codex keeps a non-empty effort. | pass | `rtk go test ./internal/cli -count=1 -run 'TestResolveSelection(AllowsEmptyReasoningEffort|RejectsExplicitEmptyModel|UsesBuiltInRuntimeDefaults)'` passed (`3 passed in 1 packages`); `TestBuiltinRuntimeDefaults` passed in the focused suite. |
| 17 | Task 02 AC: run command invoked with explicit empty reasoning flag passes argument validation and creates a Run with empty persisted reasoning effort. | pass | `TestRunResolveAcceptsExplicitEmptyReasoningEffort` and `TestRunImplementAcceptsExplicitEmptyReasoningEffort` passed; real `resolve --reasoning-effort ""` smoke advanced past argument validation and stopped only at GitHub network PR lookup. |
| 18 | Task 02 AC: implement, resolve, and watch headers show `Default Reasoning Effort: model-managed` for empty effective selection. | pass | Focused tests `TestRunResolveAcceptsExplicitEmptyReasoningEffort`, `TestRunWatchRendersModelManagedReasoningHeader`, and `TestRunImplementAcceptsExplicitEmptyReasoningEffort` passed. |
| 19 | Task 02 AC: Doctor reports `agent: ok` for a configured runtime with model and empty reasoning when the runtime probe succeeds. | pass | Real `./bin/roundfix doctor` with fake acpx and signed disposable `CODEX_PATH` exited 0 and printed `agent: ok (codex)`. |
| 20 | Task 02 AC: explicit empty model flag still fails argument validation. | pass | Real `./bin/roundfix resolve --pr 1 --agent codex --model "" --no-input` exited 2 and printed `--model cannot be empty; omit it to use runtimes.codex.model or pass a concrete value`. |
| 21 | Task 03 AC: Interactive Input completes with empty Default Reasoning Effort when configured default is empty and preserves the empty value. | pass | `TestCollectInputAcceptsModelManagedReasoningDefault` passed in the focused behavior suite. |
| 22 | Task 03 AC: reasoning field copy names the model-managed meaning of an empty value. | pass | `TestCollectInputAcceptsModelManagedReasoningDefault` passed and asserts the prompt copy; README and Skill inspections also show `model-managed` wording. |
| 23 | Task 03 AC: empty Agent Model still fails Interactive Input validation. | pass | `TestValidateCollectedSelectionsKeepsAgentModelRequired` passed in the focused behavior suite. |
| 24 | Task 04 AC: Project Config selects `gpt-5.6-sol` with explicit empty reasoning and passes config validation. | pass | `.roundfixrc.yml` inspection shows the required model, explanatory comment, and `reasoning_effort: ""`; Doctor smoke loaded that config and reported `agent: ok (codex)`; `rtk make verify` passed config tests. |
| 25 | Task 04 AC: CONTEXT.md defines empty Default Reasoning Effort as model-managed. | pass | Direct inspection of `CONTEXT.md` shows: an empty value is valid and means the Agent Model manages reasoning, so Roundfix assigns no reasoning option. |
| 26 | Task 04 AC: README documents optional-effort semantics and gpt-5.6 model-managed behavior. | pass | Direct README inspection confirmed empty config value, explicit empty flag, `model-managed` header, empty Claude built-in default, and rejected non-empty preflight guidance. |
| 27 | Task 04 AC: canonical and embedded Roundfix Skill copies document the optional-effort contract with zero drift. | pass | Direct canonical Skill inspection confirmed the same optional-effort contract; `rtk make skills-sync-check` exited 0 and `roundfix skills check` passed inside `rtk make verify`. |

## Failures and blocked items

None.

## Environment

- Static gate: `rtk make verify` exited 0 and reported `Go test: 1050 passed in 19 packages`, `Roundfix skill check passed`, and a successful `go build -buildvcs=false -o bin/roundfix ./cmd/roundfix`.
- Focused behavior gate: `rtk go test ./internal/agent ./internal/cli ./internal/config ./internal/tui -count=1 -run ...` exited 0 with `20 passed in 4 packages`; additional focused CLI selection and setup tests exited 0 with `3 passed in 1 packages` and `1 passed in 1 packages`.
- CLI smoke: ran the built binary at `./bin/roundfix` with a disposable fake acpx on `PATH=/tmp/roundfix-qa-0025:$PATH`; the fake acpx returned pinned version `0.12.0` and recorded each invocation to `/tmp/roundfix-qa-0025/*.jsonl`.
- Doctor smoke used `CODEX_PATH=/tmp/roundfix-qa-0025/fakecodex`, an ad-hoc signed disposable binary, to isolate Agent selection behavior from the host Codex installation. The host `/Users/marcio/.local/bin/codex` failed this repository's Codex hygiene check, but the isolated signed binary made the same Doctor flow pass.
- Network-dependent GitHub PR lookup was not required for the feature verdict. A real `resolve --reasoning-effort ""` smoke advanced past argument validation and then stopped at the expected restricted-network `gh pr view` lookup.
