---
spec: 0150-a-reopen-that-cannot-be-raced
date: 2026-09-19
build: f9d1fc984ecd95e63cae57db659a12f4da10d31f
auditing_binary: "0.15.0 (3effe767, built 2026-09-19 16:38:58 -0300)"
auditor_staleness: "stale: commit ancestry: build commit predates audited tree"
status: closed
verdict: pass
rows_blocked_environment: 1
rows_blocked_finding: 0
rows_blocked_declared: 0
surfaces: [backend, cli, docs]
---

# QA report — A reopen that cannot be raced

## Scope and environment

Full terminal QA gate for commit `f9d1fc984ecd95e63cae57db659a12f4da10d31f`
on `roundfix/run-run_20260919T200633Z_7ab55794ffe53366`. The actor was a local
Roundfix operator. CLI behavior was exercised through focused Go seam tests and
through the built `roundfix` binary against a scratch Git repository at
`/tmp/roundfix-qa0150-public-20260919`. The scratch journey used an isolated
home directory and the built-in `docs/specs` root.

`roundfix spec check 0150-a-reopen-that-cannot-be-raced --strict` exited 0 with
no findings before the matrix ran. It skipped only absent optional artifacts:
the findings rollup, backlog, TechSpec Vocabulary Contract, and references
index. No skipped detector displaced a row required by Task 04.

The Pull Request fact supplied to the gate says no Pull Request is open and
Pull Request journeys are environment-blocked. No network or credentialed
service was used. The repository Verification was supplied by the Daemon and
was not rerun.

## Static gate

- `make verify` — pass, exit 0, run by the Daemon outside the Agent sandbox;
  successful diagnostics were removed. This is the repository Verification
  fact required by the QA contract.
- `roundfix spec check 0150-a-reopen-that-cannot-be-raced --strict` — pass,
  exit 0, no findings.

## Performed repairs

None.

## Assigned repair failures

None.

## Authorization audit inputs

| Task | Outcome | Record | Revision | Detail |
| --- | --- | --- | --- | --- |
| task_03 | granted | docs/specs/0150-a-reopen-that-cannot-be-raced/_authorization.md | b1b0ec856f88f2a9c556226e683816a26d3a79b9 | operative grant |

## Mechanical findings

None.

## Results

| # | Status | Provenance |
| - | --- | --- |
| R1 | pass | Requirement 1; repository Verification |
| R2 | pass | Requirement 2 |
| R3 | pass | Requirement 3 |
| R4 | pass | Requirement 4 |
| R5 | pass | Requirement 5 |
| R6 | pass | Requirement 6 |
| R7 | pass | outside-evidence row |
| R8 | blocked (environment: no open Pull Request) | Pull Request row |

## Row evidence

### R1

```yaml
inputs:
  - kind: repository_path
    ref: Makefile
  - kind: repository_path
    ref: internal/**
```

- Actor, entry point, surface: Daemon; `make verify`; repository static gate.
- Steps and expected observable: run the repository Verification once against
  the assembled tree; expect exit 0.
- Observed: the Daemon reported `make verify` exited 0 and removed diagnostics
  on success.
- Independent confirmation and persistence: the Daemon supplied the terminal
  exit status for the audited Run Worktree; no narrower command was substituted
  and the Agent did not rerun the gate.
- Evidence path: this report's `Static gate` section and the QA prompt's
  Repository Verification fact.

### R2

```yaml
inputs:
  - kind: repository_path
    ref: internal/cli/reopen.go
  - kind: repository_path
    ref: internal/cli/reopen_test.go
```

- Actor, entry point, surface: local operator at the reopen command seam; Go
  CLI package test.
- Steps: run
  `GOCACHE=/tmp/roundfix-qa0150-go-cache go test -count=1 -v -run '^TestReopenRefusesWhenTheGateChangedBeforeTheWrite$' ./internal/cli`.
- Expected observable: after preflight, completing the stale dependency causes
  exit 2 with the named changed-gate refusal and no QA Task mutation.
- Observed: exit 0 from the test command and
  `--- PASS: TestReopenRefusesWhenTheGateChangedBeforeTheWrite`.
- Independent confirmation and persistence: the fixture compares the QA Task's
  bytes before and after the refusal; the passing comparison proves the file
  remained byte-identical after the command returned.
- Evidence path: this report and
  `internal/cli/reopen_test.go::TestReopenRefusesWhenTheGateChangedBeforeTheWrite`.

### R3

```yaml
inputs:
  - kind: repository_path
    ref: internal/spec/task.go
  - kind: repository_path
    ref: internal/spec/task_test.go
  - kind: repository_path
    ref: internal/cli/reopen.go
```

- Actor, entry point, surface: local operator; built `roundfix reopen --spec
  demo`; CLI and filesystem.
- Steps: build `./cmd/roundfix`, create a scratch Git repository with a
  completed QA Task symlinked to `docs/specs/targets/task_qa.md`, then run
  `HOME=/tmp/roundfix-qa0150-public-20260919/home
  /tmp/roundfix-qa0150-public-20260919/roundfix reopen --spec demo`.
- Expected observable: exit 0, target status `pending`, an invalidation naming
  `qa/qa-report-2026-09-18.md` and `task_01`, the Task path still a symlink, and
  the prior QA Report unchanged.
- Observed: stdout was `reopened task_qa pending — invalidated
  qa/qa-report-2026-09-18.md`; `stat` reported `Symbolic Link`; a fresh read of
  the target showed `status: pending` and both invalidation fields; a fresh read
  of the QA Report retained its original `verdict: pass` content. The command
  exited 0.
- Independent confirmation and persistence: `stat`, target-file `rg`, and
  report `cat` were separate reads after the CLI process exited. The focused
  `TestReopenGateWritesThroughASymlinkedTaskPath` command also passed.
- Evidence path: this report, the scratch repository named above, and
  `internal/spec/task_test.go::TestReopenGateWritesThroughASymlinkedTaskPath`.

### R4

```yaml
inputs:
  - kind: repository_path
    ref: internal/cli/reopen.go
  - kind: repository_path
    ref: internal/cli/reopen_test.go
```

- Actor, entry point, surface: local operator; public CLI dispatch exercised by
  the reopen test suite.
- Steps: run
  `GOCACHE=/tmp/roundfix-qa0150-go-cache go test -count=1 -v -run '^TestReopen' ./internal/cli`.
- Expected observable: every Spec 0149 success and refusal remains green,
  including stale reopen, healthy/pending refusal, active-Run refusal, resolved
  Spec-Root confinement, lexical Spec-directory confinement, and write failure.
- Observed: command exit 0; all `TestReopen*` cases passed. Named passes include
  `TestReopenStaleGatePreservesEvidence`,
  `TestReopenRefusesWhileARunIsActive`,
  `TestReopenRefusesAnActiveRunWithoutReclaimingIt`,
  `TestReopenRefusesQATaskResolvedOutsideSpecRoot`, and
  `TestReopenRejectsAManifestPathOutsideTheSpecDirectory`.
- Independent confirmation and persistence: each refusal test performs a fresh
  post-command read or directory digest and fails if the refused operation
  mutated the Spec; the stale success reloads the Spec after the write.
- Evidence path: this report and `internal/cli/reopen_test.go`.

### R5

```yaml
inputs:
  - kind: repository_path
    ref: .agents/skills/roundfix/SKILL.md
  - kind: repository_path
    ref: skills/roundfix/SKILL.md
  - kind: repository_path
    ref: docs/specs/0150-a-reopen-that-cannot-be-raced/_authorization.md
```

- Actor, entry point, surface: maintainer/QA auditor; shipped skill and Git
  commit history.
- Steps: run `go run -buildvcs=false ./cmd/roundfix skills check`; search both
  skill files for `gate changed since preflight`; compare the files; inspect
  each Task commit with `git diff-tree --no-commit-id --name-only -r <commit>`;
  prove authorization ancestry with `git merge-base --is-ancestor`.
- Expected observable: the canonical and mirrored skill name the refusal and
  match; task_03 changes only its Task file and the two granted skill paths;
  task_01 and task_02 change only their Task files and ordinary source/test
  paths; the authorization predates every Task commit.
- Observed: skill check exited 0; both anchors were at line 2001; `git diff
  --no-index` exited 0. Commits `9be26780`, `4974d51c`, and `f9d1fc98` contained
  only their assigned Task files and expected implementation/docs paths.
  Authorization commit `4a52f0eb` is an ancestor of all three Task commits.
- Independent confirmation and persistence: Git tree objects supplied the
  changed-path lists, rather than Task Result prose; the byte comparison
  independently confirmed the distributed mirror.
- Evidence path: this report, the authorization record, and the three named Git
  commits.

### R6

```yaml
inputs:
  - kind: repository_path
    ref: docs/specs/0150-a-reopen-that-cannot-be-raced/_prd.md
  - kind: repository_path
    ref: docs/specs/0150-a-reopen-that-cannot-be-raced/_techspec.md
  - kind: repository_path
    ref: docs/specs/0150-a-reopen-that-cannot-be-raced/_tasks.md
  - kind: repository_path
    ref: docs/specs/0150-a-reopen-that-cannot-be-raced/task_*.md
```

- Actor, entry point, surface: QA auditor; `roundfix spec check`; authored Spec
  contract.
- Steps: run `roundfix spec check 0150-a-reopen-that-cannot-be-raced --strict`
  and inspect its complete result.
- Expected observable: the Spec's promise mapping, terminal QA topology,
  Project Constraints, citations, and Task coverage produce no findings.
- Observed: exit 0 with `No findings. Authored Verification commands were not
  executed.` The checker reported only missing optional artifacts listed in
  `Scope and environment`.
- Independent confirmation and persistence: the checker loaded the committed
  PRD, TechSpec, graph, and Task files at the audited head; a second source read
  confirmed task_04 is terminal and depends on task_01, task_02, and task_03.
- Evidence path: this report and the Spec artifacts declared as inputs.

### R7

```yaml
inputs:
  - kind: repository_path
    ref: internal/spec/task.go
```

- Actor, entry point, surface: QA auditor; repository history predating this
  Spec. This is outside evidence because it comes from the shipped Spec 0149
  commit, not from Spec 0150's authored artifacts or tests.
- Steps: run `git log --oneline -S 'func replaceTaskFile' --
  internal/spec/task.go`; inspect `internal/spec/task.go` before and at commit
  `e5d4a8bc`.
- Expected observable: history shows Spec 0149 introduced the atomic helper,
  while the predecessor wrote through the Task path with `os.WriteFile`; the
  introduced helper renamed directly onto the possibly symlinked path.
- Observed: the `-S` search returned only `e5d4a8bc feat: a supported way to
  reopen a settled gate (#229)`. Its parent contains `os.WriteFile(taskPath,
  updated, info.Mode().Perm())`; `e5d4a8bc` contains `func replaceTaskFile` and
  `os.Rename(temporaryPath, path)`.
- Independent confirmation and persistence: immutable parent and commit tree
  objects provide separate before/after reads and remain addressable by commit
  id.
- Evidence path: Git commits `e5d4a8bc^` and `e5d4a8bc` in this repository.

### R8

```yaml
inputs:
  - kind: live_service
    ref: Pull Request for feat/0150-reopen-cannot-be-raced
```

- Actor, entry point, surface: reviewer; Pull Request controls.
- Steps and expected observable: inspect approval, checks/status, unresolved
  threads, Merge-Ready acceptance, and review-artifact ancestry for the Spec
  target head.
- Cause and proof: the QA prompt states `Pull Request: none open; Pull Request
  journeys are environment-blocked.` The per-Run branch is never pushed and
  has no Pull Request of its own.
- Equivalent observed or supervised evidence: no approval or acceptance has
  been obtained; the Daemon's repository Verification for the audited tree
  passed; without a Pull Request there are no review threads; no Merge-Ready
  Review Source evidence exists yet; no review artifact exists yet. The head
  claimed by this row is `f9d1fc984ecd95e63cae57db659a12f4da10d31f`, the
  audited Run Worktree head, and no review artifact claims another head.
- Unblocking action: after QA/archive integration, open a Pull Request from
  `feat/0150-reopen-cannot-be-raced` and observe all five controls against its
  current head.
- Independent confirmation and persistence: the prompt's supervised Pull
  Request fact is separate from local Git; `git rev-parse HEAD` confirmed the
  audited local head.
- Evidence path: this report, the QA prompt's Pull Request fact, and local Git
  head evidence.

## Mechanical skips

| Detector | Missing artifact |
| --- | --- |
| consequent-fix commit order | consequent-fix declaration |
| QA Report structure | QA Report |
| QA evidence paths | QA Report |

## Findings

None.

## Blocked and skipped

R8 is blocked only because no Pull Request exists. Its cause, unblocking
action, and equivalent evidence for all five Pull Request controls are recorded
above. No row is finding-blocked, declared-blocked, or skipped.

## Coverage

- Declared verification Requirements: 6 of 6 covered; all runnable rows passed.
- Non-waivable sources: repository Verification passed; outside evidence
  passed; Pull Request row environment-blocked with equivalent evidence. The
  PRD declares no Unreachable Acceptance section and no frontend surface.
- Result counts: 7 pass, 1 environment-blocked, 0 fail, 0 finding-blocked, 0
  declared-blocked, 0 skipped, 0 pending.
- High-risk behavior covered: the dependency completes after preflight, the
  stale dependency set changes, the QA Task path is a symlink, an Active Run
  owns the Spec, and both lexical and resolved confinement boundaries reject
  escapes.

## Final verdict

Pass: every runnable criterion passed, R8 records its no-open-Pull-Request cause
and equivalent supervised evidence, every evidence path resolves, and no row
is failed, finding-blocked, declared-blocked, skipped, or pending.
