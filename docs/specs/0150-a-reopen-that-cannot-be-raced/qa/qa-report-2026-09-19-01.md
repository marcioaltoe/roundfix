---
spec: 0150-a-reopen-that-cannot-be-raced
date: 2026-09-19
build: 2743fbdd21eee8989fc8ad8f86ad1f6a48024043
auditing_binary: "0.14.1 (b1c8a33d, built 2026-09-19 17:30:06 -0300)"
auditor_staleness: "stale: commit ancestry: build commit predates audited tree"
status: closed
verdict: pass
rows_blocked_environment: 1
rows_blocked_finding: 0
rows_blocked_declared: 0
surfaces: [backend, cli, docs]
---

# QA report — A reopen that cannot be raced

## Scope and environment

Full terminal QA gate for commit `2743fbdd21eee8989fc8ad8f86ad1f6a48024043`
on `roundfix/run-run_20260919T204646Z_47884859f50fe354`. The actor was a local
Roundfix operator. CLI behavior was exercised through the rebuilt
`/private/tmp/roundfix-qa0150-rerun` binary against the scratch Git repository
at `/tmp/roundfix-qa0150-public-20260919`, plus focused public-contract tests.
The scratch journey used an isolated home directory and the built-in
`docs/specs` root.

`roundfix spec check 0150-a-reopen-that-cannot-be-raced --strict` exited 0 with
no findings before the matrix ran. It skipped only absent optional artifacts:
the findings rollup, backlog, TechSpec Vocabulary Contract, and references
index. No skipped detector displaced a row required by Task 04.

The Pull Request fact supplied to the gate says no Pull Request is open and
Pull Request journeys are environment-blocked. No network or credentialed
service was used. The repository Verification was supplied by the Daemon and
was not rerun. The first skill-check attempt used the sandbox-denied default Go
cache; the identical command rerun with `GOCACHE` under `/private/tmp` passed.

## Static gate

- `make verify` — pass, exit 0, run by the Daemon outside the Agent sandbox;
  successful diagnostics were removed. This is the repository Verification
  fact required by the QA contract.
- `roundfix spec check 0150-a-reopen-that-cannot-be-raced --strict` — pass,
  exit 0, no findings.

## Performed repairs

None.

## Assigned repair failures

None.

## Authorization audit inputs

| Task | Outcome | Record | Revision | Detail |
| --- | --- | --- | --- | --- |
| task_03 | granted | docs/specs/0150-a-reopen-that-cannot-be-raced/_authorization.md | b1b0ec856f88f2a9c556226e683816a26d3a79b9 | operative grant |

## Mechanical findings

None.

## Results

| # | Status | Provenance |
| - | --- | --- |
| R1 | pass | Requirement 1; repository Verification |
| R2 | pass | Requirement 2 |
| R3 | pass | Requirement 3 |
| R4 | pass | Requirement 4 |
| R5 | pass | Requirement 5 |
| R6 | pass | Requirement 6 |
| R7 | pass | outside-evidence row |
| R8 | blocked (environment: no open Pull Request) | Pull Request row |

## Row evidence

### R1

```yaml
inputs:
  - kind: repository_path
    ref: Makefile
  - kind: repository_path
    ref: internal/**
```

- Actor, entry point, surface: Daemon; `make verify`; repository static gate.
- Steps and expected observable: run the repository Verification once against
  the assembled tree; expect exit 0.
- Observed: the Daemon reported `make verify` exited 0 and removed diagnostics
  on success.
- Independent confirmation and persistence: the Daemon supplied the terminal
  exit status for the audited Run Worktree; no narrower command was substituted
  and the Agent did not rerun the gate.
- Evidence path: this report's `Static gate` section and the QA prompt's
  Repository Verification fact.

### R2

```yaml
inputs:
  - kind: repository_path
    ref: internal/cli/reopen.go
  - kind: repository_path
    ref: internal/cli/reopen_test.go
```

- Actor, entry point, surface: local operator at the reopen command seam; Go
  CLI package test.
- Steps: run `GOCACHE=/private/tmp/roundfix-qa0150-rerun-gocache go test
  -count=1 -v -run '^TestReopen' ./internal/cli`.
- Expected observable: completing the stale dependency after preflight causes
  exit 2 with the named changed-gate refusal and no QA Task mutation; changing
  only the stale dependency set has the same refusal contract.
- Observed: exit 0; both
  `TestReopenRefusesWhenTheGateChangedBeforeTheWrite` and
  `TestReopenRefusesWhenTheStaleDependencySetChangedBeforeTheWrite` passed.
- Independent confirmation and persistence: both fixtures compare the QA Task
  bytes before and after the refused command; the passing comparisons prove
  the file remains byte-identical after the command returns.
- Evidence path: this report and `internal/cli/reopen_test.go`.

### R3

```yaml
inputs:
  - kind: repository_path
    ref: internal/spec/task.go
  - kind: repository_path
    ref: internal/spec/task_test.go
  - kind: repository_path
    ref: internal/cli/reopen.go
  - kind: repository_path
    ref: internal/cli/reopen_test.go
```

- Actor, entry point, surface: local operator; rebuilt `roundfix reopen --spec
  demo`; CLI and filesystem.
- Steps: build `./cmd/roundfix`, reset a scratch completed QA Task symlinked to
  `docs/specs/targets/task_qa.md`, hash its prior QA Report, then run
  `HOME=/tmp/roundfix-qa0150-public-20260919/home
  /private/tmp/roundfix-qa0150-rerun reopen --spec demo`.
- Expected observable: exit 0, validated target status `pending`, invalidation
  naming `qa/qa-report-2026-09-18.md` and `task_01`, Task path still a symlink,
  and the prior QA Report unchanged.
- Observed: stdout was `reopened task_qa pending — invalidated
  qa/qa-report-2026-09-18.md`; `stat` reported `Symbolic Link`; a fresh target
  read showed `status: pending` and both invalidation fields. The report hash
  remained `f40b2ddc2ff0e672e4dfeda4732af5629ed476b968a77e26d7d2eeeb9aa63526`.
- Independent confirmation and persistence: `stat`, `sed`, and `shasum` were
  separate reads after the CLI exited. The focused
  `TestReopenGateWritesToTheValidatedTarget` test also passed, proving the
  replacement writes to the one validated path even if the symlink changes.
- Evidence path: this report, the named scratch repository, and
  `internal/spec/task_test.go::TestReopenGateWritesToTheValidatedTarget`.

### R4

```yaml
inputs:
  - kind: repository_path
    ref: internal/cli/reopen.go
  - kind: repository_path
    ref: internal/cli/reopen_test.go
```

- Actor, entry point, surface: local operator; public CLI dispatch exercised by
  the reopen test suite.
- Steps: run `GOCACHE=/private/tmp/roundfix-qa0150-rerun-gocache go test
  -count=1 -v -run '^TestReopen' ./internal/cli`.
- Expected observable: every Spec 0149 success and refusal remains green,
  including stale reopen, healthy/pending refusal, active-Run refusal, resolved
  Spec-Root confinement, lexical Spec-directory confinement, and write failure.
- Observed: exit 0; every `TestReopen*` case passed. Named passes include
  `TestReopenStaleGatePreservesEvidence`,
  `TestReopenRefusesWhileARunIsActive`,
  `TestReopenRefusesAnActiveRunWithoutReclaimingIt`,
  `TestReopenRefusesQATaskResolvedOutsideSpecRoot`, and
  `TestReopenRejectsAManifestPathOutsideTheSpecDirectory`.
- Independent confirmation and persistence: refusal tests perform a fresh
  post-command read or directory digest and fail on mutation; stale successes
  reload the Spec after the write.
- Evidence path: this report and `internal/cli/reopen_test.go`.

### R5

```yaml
inputs:
  - kind: repository_path
    ref: .agents/skills/roundfix/SKILL.md
  - kind: repository_path
    ref: skills/roundfix/SKILL.md
  - kind: repository_path
    ref: docs/specs/0150-a-reopen-that-cannot-be-raced/_authorization.md
  - kind: repository_path
    ref: docs/specs/0150-a-reopen-that-cannot-be-raced/task_*.md
```

- Actor, entry point, surface: maintainer/QA auditor; shipped skill and Git
  commit history.
- Steps: run `GOCACHE=/private/tmp/roundfix-qa0150-rerun-gocache go run
  -buildvcs=false ./cmd/roundfix skills check`; search both skill files for
  `gate changed since preflight`; compare them; inspect commits `9be26780`,
  `4974d51c`, `f9d1fc98`, `b3d93e62`, and `2743fbdd` with `git diff-tree
  --no-commit-id --name-only -r`; prove authorization ancestry with
  `git merge-base --is-ancestor`.
- Expected observable: skill copies name the refusal and match; the skill Task
  changes only its Task file and two granted skill paths; other commits change
  only Spec artifacts or ordinary source/test paths; authorization predates all
  implementation commits.
- Observed: skill check exited 0; both anchors were at line 2001; `diff -u`
  exited 0. The commit trees matched those boundaries. Authorization commit
  `4a52f0eb` is an ancestor of the implementation and skill commits. Task 05's
  commit changes only its Task file and `internal/cli`/`internal/spec` source
  and tests; the corrective authoring commit changes only Spec artifacts.
- Independent confirmation and persistence: immutable Git tree objects supplied
  the path lists, rather than Task Result prose; byte comparison independently
  confirmed the mirror.
- Evidence path: this report, the authorization record, and the named commits.

### R6

```yaml
inputs:
  - kind: repository_path
    ref: docs/specs/0150-a-reopen-that-cannot-be-raced/_prd.md
  - kind: repository_path
    ref: docs/specs/0150-a-reopen-that-cannot-be-raced/_techspec.md
  - kind: repository_path
    ref: docs/specs/0150-a-reopen-that-cannot-be-raced/_tasks.md
  - kind: repository_path
    ref: docs/specs/0150-a-reopen-that-cannot-be-raced/task_*.md
```

- Actor, entry point, surface: QA auditor; `roundfix spec check`; authored Spec
  contract.
- Steps: run `roundfix spec check 0150-a-reopen-that-cannot-be-raced --strict`
  and inspect the graph and Task statuses.
- Expected observable: the Spec's promise mapping, terminal QA topology,
  Project Constraints, citations, and Task coverage produce no findings;
  task_04 depends on every non-QA leaf and every dependency is completed.
- Observed: exit 0 with `No findings. Authored Verification commands were not
  executed.` Task 04 is the unique terminal QA node and depends on completed
  task_01, task_02, task_03, and task_05.
- Independent confirmation and persistence: the checker loaded the authored
  artifacts at the audited head; direct graph and status reads independently
  confirmed its terminal topology and readiness.
- Evidence path: this report and the Spec artifacts declared as inputs.

### R7

```yaml
inputs:
  - kind: repository_path
    ref: internal/spec/task.go
```

- Actor, entry point, surface: QA auditor; repository history predating this
  Spec. This is outside evidence because it comes from the shipped Spec 0149
  commit, not from Spec 0150's authored artifacts or tests.
- Steps: run `git log --oneline -S 'func replaceTaskFile' --
  internal/spec/task.go`; inspect `internal/spec/task.go` before and at commit
  `e5d4a8bc`.
- Expected observable: Spec 0149 introduced the atomic helper, while the
  predecessor wrote through the Task path with `os.WriteFile`; the helper
  renamed directly onto the possibly symlinked path.
- Observed: the search returned `e5d4a8bc feat: a supported way to reopen a
  settled gate (#229)`. Its parent contains `os.WriteFile(taskPath, updated,
  info.Mode().Perm())`; that commit contains `replaceTaskFile` and
  `os.Rename(temporaryPath, path)`.
- Independent confirmation and persistence: immutable parent and commit tree
  objects provide separate before/after reads and remain addressable by id.
- Evidence path: Git commits `e5d4a8bc^` and `e5d4a8bc`.

### R8

```yaml
inputs:
  - kind: live_service
    ref: Pull Request for feat/0150-reopen-cannot-be-raced
```

- Actor, entry point, surface: reviewer; Pull Request controls.
- Steps and expected observable: inspect approval, checks/status, unresolved
  threads, Merge-Ready acceptance, and review-artifact ancestry for the Spec
  target head.
- Cause and proof: the QA prompt states `Pull Request: none open; Pull Request
  journeys are environment-blocked.` The per-Run branch is never pushed and
  has no Pull Request of its own.
- Equivalent observed or supervised evidence: no approval or Merge-Ready
  acceptance was obtained; the Daemon's repository Verification for the
  audited tree passed; without a Pull Request there are no review threads; no
  review artifact exists yet. The claimed head is
  `2743fbdd21eee8989fc8ad8f86ad1f6a48024043`, confirmed by `git rev-parse HEAD`,
  and no review artifact claims another head.
- Unblocking action: after QA/archive integration, open a Pull Request from
  `feat/0150-reopen-cannot-be-raced` and observe all five controls against its
  current head.
- Independent confirmation and persistence: the prompt's supervised Pull
  Request fact is separate from local Git head evidence.
- Evidence path: this report, the QA prompt's Pull Request fact, and local Git.

## Mechanical skips

| Detector | Missing artifact |
| --- | --- |
| authorization bounded paths | Task commits |
| consequent-fix commit order | consequent-fix declaration |
| QA evidence paths | docs/specs/0150-a-reopen-that-cannot-be-raced/qa/evidence |

## Findings

None.

## Blocked and skipped

R8 is blocked only because no Pull Request exists. Its cause, unblocking action,
and equivalent evidence for all five Pull Request controls are recorded above.
No row is finding-blocked, declared-blocked, or skipped.

## Coverage

- Declared verification Requirements: 6 of 6 covered; all runnable rows passed.
- Non-waivable sources: repository Verification passed; outside evidence
  passed; Pull Request row environment-blocked with equivalent evidence. The
  PRD declares no Unreachable Acceptance section and no frontend surface.
- Result counts: 7 pass, 1 environment-blocked, 0 fail, 0 finding-blocked, 0
  declared-blocked, 0 skipped, 0 pending.
- High-risk behavior covered: a dependency completes after preflight, the stale
  dependency set changes, the Task symlink changes after validation, the Task
  path remains a symlink on success, an active Run owns the Spec, and both
  lexical and resolved confinement boundaries reject escapes.

## Final verdict

Pass: every runnable criterion passed, R8 records its no-open-Pull-Request cause
and equivalent supervised evidence, every evidence locator resolves, and no
row is failed, finding-blocked, declared-blocked, skipped, or pending.
