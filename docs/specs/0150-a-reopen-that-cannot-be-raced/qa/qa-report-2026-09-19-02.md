---
spec: 0150-a-reopen-that-cannot-be-raced
date: 2026-09-19
build: 24faece3cc2ca8527355691e53545900d0a5d65b
auditing_binary: "0.15.0 (cb4a3511, built 2026-09-19 18:19:04 -0300)"
auditor_staleness: "stale: commit ancestry: build commit predates audited tree"
status: closed
verdict: pass
rows_blocked_environment: 1
rows_blocked_finding: 0
rows_blocked_declared: 0
surfaces: [backend, cli, docs]
---

# QA Report

## Scope and environment

Full terminal QA gate for commit `24faece3cc2ca8527355691e53545900d0a5d65b`
on `roundfix/run-run_20260919T220533Z_77b55050ab073094`. The actor is a local
Roundfix operator. The repository Verification was supplied by the Daemon and
will not be rerun. The Pull Request fact supplied to the gate says no Pull
Request is open and Pull Request journeys are environment-blocked.

`roundfix spec check 0150-a-reopen-that-cannot-be-raced --strict` exited 0 with
no findings before the matrix ran. It skipped only absent optional artifacts:
the findings rollup, backlog, TechSpec Vocabulary Contract, and references
index. No skipped detector displaced a row required by Task 04.

## Static gate

- `make verify` — pass, exit 0, run by the Daemon outside the Agent sandbox;
  successful diagnostics were removed. This is the repository Verification
  fact required by the QA contract.
- `roundfix spec check 0150-a-reopen-that-cannot-be-raced --strict` — pass,
  exit 0, no findings.

## Performed repairs

None.

## Assigned repair failures

None.

## Authorization audit inputs

| Task | Outcome | Record | Revision | Detail |
| --- | --- | --- | --- | --- |
| task_03 | granted | docs/specs/0150-a-reopen-that-cannot-be-raced/_authorization.md | 4a52f0eb1862a55bb3c8332515a171cdb9ae6b17 | operative grant predates Task commits |

## Mechanical findings

None.

## Results

| # | Status | Provenance |
| - | --- | --- |
| R1 | pass | Requirement 1; repository Verification |
| R2 | pass | Requirement 2 |
| R3 | pass | Requirement 3 |
| R4 | pass | Requirement 4 |
| R5 | pass | Requirement 5 |
| R6 | pass | Requirement 6 |
| R7 | pass | outside-evidence row |
| R8 | blocked (environment: no open Pull Request) | Pull Request row |

## Row evidence

### R1

```yaml
inputs:
  - kind: repository_path
    ref: Makefile
  - kind: repository_path
    ref: internal/**
```

- Actor, entry point, surface: Daemon; `make verify`; repository static gate.
- Steps: use the Daemon's supplied repository Verification result without
  rerunning it.
- Expected observable: the assembled tree exits 0 under `make verify`.
- Observed: the Daemon reported exit 0 and removed diagnostics on success.
- Independent confirmation and persistence: the Daemon's terminal exit status
  is bound to this Run Worktree.
- Evidence path: this report and the QA prompt's Repository Verification fact.

### R2

```yaml
inputs:
  - kind: repository_path
    ref: internal/cli/reopen.go
  - kind: repository_path
    ref: internal/cli/reopen_test.go
```

- Actor, entry point, surface: local operator; reopen command seam; CLI tests.
- Steps: run the focused changed-gate and changed-dependency-set journeys.
- Expected observable: exit 2 with the named changed-gate refusal and a
  byte-identical QA Task after both refused writes.
- Observed: `GOCACHE=/private/tmp/roundfix-qa0150-02-gocache go test
  -count=1 -v -run '^TestReopen' ./internal/cli` exited 0;
  `TestReopenRefusesWhenTheGateChangedBeforeTheWrite` and
  `TestReopenRefusesWhenTheStaleDependencySetChangedBeforeTheWrite` passed.
- Independent confirmation and persistence: the tests reread and compare the
  Task bytes after the command returns.
- Evidence path: this report and `internal/cli/reopen_test.go`.

### R3

```yaml
inputs:
  - kind: repository_path
    ref: internal/spec/task.go
  - kind: repository_path
    ref: internal/spec/task_test.go
  - kind: repository_path
    ref: internal/cli/reopen.go
  - kind: repository_path
    ref: internal/cli/reopen_test.go
  - kind: repository_path
    ref: cmd/roundfix/**
```

- Actor, entry point, surface: local operator; binary built from the audited
  tree; CLI and filesystem.
- Steps: run the built-binary reopen journey and the validated-target seam.
- Expected observable: the built command reopens a stale gate without changing
  its QA Report, refuses a healthy gate, and writes through a symlinked Task
  path while preserving the link; the replacement uses the validated target.
- Observed: the complete `TestReopenThroughTheBuiltBinary` test and its three
  named subtests passed. `GOCACHE=/private/tmp/roundfix-qa0150-02-gocache go
  test -count=1 -v -run '^TestReopenGateWritesToTheValidatedTarget$'
  ./internal/spec` also exited 0.
- Independent confirmation and persistence: the public test rereads the report,
  target, and link after each command; the seam test redirects the symlink
  after validation and reads both possible targets.
- Evidence path: this report, `internal/cli/reopen_test.go`, and
  `internal/spec/task_test.go`.

### R4

```yaml
inputs:
  - kind: repository_path
    ref: internal/cli/reopen.go
  - kind: repository_path
    ref: internal/cli/reopen_test.go
```

- Actor, entry point, surface: local operator; public CLI dispatch exercised by
  the complete reopen test suite.
- Steps: run every `TestReopen*` case.
- Expected observable: every Spec 0149 success and refusal remains green,
  including stale reopen, healthy/pending refusal, active-Run refusal, both
  confinement boundaries, and write failure.
- Observed: the complete `TestReopen*` suite exited 0. Its named passes include
  both active-Run cases, both confinement cases, stale and symlinked success,
  healthy and pending refusal, invalid-input refusal, and write failure.
- Independent confirmation and persistence: mutation-sensitive cases perform a
  fresh post-command read or directory digest; success cases reload the Spec.
- Evidence path: this report and `internal/cli/reopen_test.go`.

### R5

```yaml
inputs:
  - kind: repository_path
    ref: .agents/skills/roundfix/SKILL.md
  - kind: repository_path
    ref: skills/roundfix/SKILL.md
  - kind: repository_path
    ref: docs/specs/0150-a-reopen-that-cannot-be-raced/_authorization.md
  - kind: repository_path
    ref: docs/specs/0150-a-reopen-that-cannot-be-raced/task_*.md
  - kind: repository_path
    ref: internal/cli/**
  - kind: repository_path
    ref: internal/spec/**
```

- Actor, entry point, surface: maintainer and QA auditor; shipped skill plus
  immutable Git commit history.
- Steps: check skill truth and mirror equality; inspect each Task commit's paths
  with `git diff-tree`; prove authorization and commit chronology from ancestry.
- Expected observable: both skills name the refusal and match; governed changes
  stay inside the approved paths and their Task file; authorization predates
  implementation, and no unauthorized tooling path changed.
- Observed: `go run -buildvcs=false ./cmd/roundfix skills check` exited 0;
  both refusal anchors are at line 2001 and `diff -u` exited 0. `git diff-tree
  --no-commit-id --name-only -r` showed Task commits `9be26780`, `4974d51c`,
  `f9d1fc98`, `2743fbdd`, and `24faece3` stayed within their Task files and
  ordinary source/test paths, except Task 03's two exactly granted skill paths.
  Authorization commit `4a52f0eb` is an ancestor of all five Task commits.
- Independent confirmation and persistence: Git tree objects provide actual
  path lists independently of Task Result prose; a byte comparison confirms
  the skill mirror.
- Evidence path: this report, the authorization record, and named commits.

### R6

```yaml
inputs:
  - kind: repository_path
    ref: docs/specs/0150-a-reopen-that-cannot-be-raced/_prd.md
  - kind: repository_path
    ref: docs/specs/0150-a-reopen-that-cannot-be-raced/_techspec.md
  - kind: repository_path
    ref: docs/specs/0150-a-reopen-that-cannot-be-raced/_tasks.md
  - kind: repository_path
    ref: docs/specs/0150-a-reopen-that-cannot-be-raced/task_*.md
```

- Actor, entry point, surface: QA auditor; `roundfix spec check`; authored Spec
  contract.
- Steps: run the strict Spec check and inspect graph topology and Task statuses.
- Expected observable: no findings; task_04 is the unique terminal QA node,
  depends on every non-QA leaf, and every dependency is completed.
- Observed: the strict check exited 0 with `No findings. Authored Verification
  commands were not executed.` Direct reads showed `qa: task_04`, its complete
  dependency list (`task_01`, `task_02`, `task_03`, `task_05`, `task_06`), and
  `status: completed` in every dependency Task.
- Independent confirmation and persistence: direct graph and Task reads confirm
  the checker's result.
- Evidence path: this report and the declared Spec artifacts.

### R7

```yaml
inputs:
  - kind: repository_path
    ref: internal/spec/task.go
```

- Actor, entry point, surface: QA auditor; immutable repository history that
  predates this Spec.
- Steps: locate the commit that introduced `replaceTaskFile` and compare its
  predecessor with that commit.
- Expected observable: Spec 0149 changed a write-through `os.WriteFile` into a
  rename onto the possibly symlinked Task path, independently establishing the
  regression premise.
- Observed: `git log -S 'func replaceTaskFile'` returned only `e5d4a8bc feat: a
  supported way to reopen a settled gate (#229)`. Its parent contains
  `os.WriteFile(taskPath, updated, info.Mode().Perm())`; that commit contains
  `func replaceTaskFile` and `os.Rename(temporaryPath, path)`.
- Independent confirmation and persistence: parent and child Git tree objects
  provide separate before/after reads and remain addressable by commit id.
- Evidence path: the resulting immutable Git commits in this repository.

### R8

```yaml
inputs:
  - kind: live_service
    ref: Pull Request for feat/0150-reopen-cannot-be-raced
```

- Actor, entry point, surface: reviewer; Pull Request controls.
- Steps: inspect approval, checks/status, unresolved review threads,
  Merge-Ready acceptance, and review-artifact ancestry for the target head.
- Expected observable: all five controls are accounted for, or the no-open-Pull-
  Request cause is paired with equivalent observed or supervised evidence.
- Cause and proof: the QA prompt states `Pull Request: none open; Pull Request
  journeys are environment-blocked.` The per-Run branch is never pushed and
  has no Pull Request of its own.
- Equivalent observed or supervised evidence: no approval or Merge-Ready
  acceptance has been obtained; the Daemon's repository Verification passed;
  without a Pull Request there are no unresolved review threads; no Review
  Source evidence or review artifact exists yet. The head claimed by this row
  is `24faece3cc2ca8527355691e53545900d0a5d65b`, confirmed by `git rev-parse
  HEAD`, and no review artifact claims another head.
- Unblocking action: after QA/archive integration, open a Pull Request from
  `feat/0150-reopen-cannot-be-raced` and observe all five controls against its
  current head.
- Independent confirmation and persistence: compare the prompt's supervised
  Pull Request fact with local Git head evidence.
- Evidence path: this report, the QA prompt's Pull Request fact, and local Git.

## Mechanical skips

| Detector | Missing artifact |
| --- | --- |
| authorization bounded paths | Task commits |
| consequent-fix commit order | consequent-fix declaration |
| QA evidence paths | docs/specs/0150-a-reopen-that-cannot-be-raced/qa/evidence |

## Findings

None.

## Blocked and skipped

R8 is blocked only because no Pull Request exists. Its cause, unblocking
action, and equivalent evidence for all five Pull Request controls are recorded
above. No row is finding-blocked, declared-blocked, or skipped.

## Coverage

- Declared verification Requirements: 6 of 6 covered; all runnable rows passed.
- Non-waivable sources: repository Verification passed; outside evidence
  passed; Pull Request row environment-blocked with equivalent evidence. The
  PRD declares no Unreachable Acceptance section and no frontend surface.
- Result counts: 7 pass, 1 environment-blocked, 0 fail, 0 finding-blocked, 0
  declared-blocked, 0 skipped, 0 pending.
- High-risk behavior covered: both gate changes after preflight, a binary built
  from the audited tree, a symlinked Task path and post-validation symlink
  swap, an Active Run, and both lexical and resolved confinement boundaries.

## Final verdict

Pass: every runnable criterion passed, R8 records its no-open-Pull-Request cause
and equivalent supervised evidence, every evidence locator resolves, and no
row is failed, finding-blocked, declared-blocked, skipped, or pending.
