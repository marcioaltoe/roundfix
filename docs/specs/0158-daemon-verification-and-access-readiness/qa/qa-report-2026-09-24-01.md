---
spec: 0158-daemon-verification-and-access-readiness
date: 2026-09-24
build: 19fd85592b93241755b8a73ca20a22e5436e5573
auditing_binary: "0.16.0 (e38c8561, built 2026-09-24 08:07:17 -0300)"
auditor_staleness: "current: commit ancestry: build commit does not predate audited tree"
status: closed
verdict: pass
rows_blocked_environment: 1
rows_blocked_finding: 0
rows_blocked_declared: 0
surfaces: [backend, cli, docs]
---

# QA Report

## Scope and environment

- Scope: full authored terminal gate for Task 05 on build
  `19fd85592b93241755b8a73ca20a22e5436e5573` after corrective Tasks 06 and 07.
- Dependencies: Tasks 01, 02, 03, 04, 06 and 07 are completed; Task 05 is the
  terminal `qa` node and depends on every non-QA leaf.
- Actors: a local Roundfix operator, the Daemon, a Spec author and a reviewer.
- Entry points: focused public-contract Go runners, the public
  `roundfix spec check` command, local Git reads, documentation checks and the
  daemon-owned repository Verification fact.
- Authoring precondition: `roundfix spec check
  0158-daemon-verification-and-access-readiness --strict` exited 0 with no
  findings. The PATH binary reported `roundfix 0.15.0`; an independently built
  current-tree `roundfix 0.16.0` repeated the check with the same result.
  Authored Verification commands were not executed by either check.
- Pull Request: none open; Pull Request journeys are environment-blocked.
- Repository Verification: daemon-owned; the supplied result is `make verify`
  exit 0 with diagnostics removed on success. This gate did not rerun it.
- Parity: focused Go runners execute the Daemon and CLI boundaries used by an
  Implement Run. Access-readiness CLI cases need the repository freshness
  harness to reach `api.github.com`; the restricted attempt was blocked before
  the tests started, and the full-access rerun passed.
- The PRD declares no Unreachable Acceptance and no frontend surface.

## Performed repairs

None.

## Assigned repair failures

None.

## Authorization audit inputs

None.

## Mechanical findings

None.

## Static gate

- `make verify` — pass (exit 0), run by the Daemon outside the Agent sandbox;
  diagnostics were removed on success. Per the QA contract, the Agent did not
  rerun it.
- `roundfix spec check 0158-daemon-verification-and-access-readiness --strict`
  — pass (exit 0), no findings. The checker disclosed four skipped detectors:
  no findings rollup, no backlog, no TechSpec Vocabulary Contract and no
  `references/_index.md`.
- `/private/tmp/roundfix-0158-qa-rerun spec check
  0158-daemon-verification-and-access-readiness --strict` — pass (exit 0), no
  findings, through a binary built from the audited tree.

## Execution log

- `make verify` — pass (exit 0), supplied by the Daemon; not rerun by the
  Agent.
- `GOCACHE=/private/tmp/roundfix-0158-qa-rerun-gocache go test -count=1 -v
  -run '^(TestTaskParsesIndependentVerification|TestIndependentVerificationHandsEveryFailureToRepair|TestIndependentVerificationKeepsCollectedFailuresBesideATemporaryOne|TestIndependentVerificationKeepsCollectedFailuresBesideUnknownCause|TestUndeclaredVerificationStopsAtFirstFailure)$'
  ./internal/spec ./internal/daemon` — pass (exit 0). It covered two ordinary
  failures, an undeclared first-failure stop, a deterministic failure beside a
  temporary failure and a deterministic failure beside an unknown cause.
- `GOCACHE=/private/tmp/roundfix-0158-qa-rerun-gocache go test -count=1 -v
  -run '^(TestAuthorizationParsesPreconditionRepairs|TestNamedTaskRepairsKnownRedPrecondition|TestUnnamedTaskStaysBlockedByRedPrecondition|TestPreconditionRepairWithoutConfiguredCommandIsRefused|TestUnknownPreconditionRepairTaskIsRefused|TestRepairTaskCannotDropTheRepositoryCommand|TestRepairEntryNeedsAnObservedRedGate|TestPreconditionDiagnosticsAreNotOverwritten|TestPaddedRepositoryCommandStillChecksEntry|TestPreconditionRepairPlanningRequiresExactRepositoryCommand)$'
  ./internal/authorization ./internal/daemon ./internal/cli` — pass (exit 0).
- The first access-readiness command did not start because the sandbox blocked
  `api.github.com`. The same command in the authorized full-access environment
  passed (exit 0): `GOCACHE=/private/tmp/roundfix-0158-qa-rerun-gocache go test
  -count=1 -v -run
  '^(TestRuntimeReportsAccessPolicySupport|TestDisposableProofAppliesRequestedAccessMode|TestProofRecordsADegradedFullAccess|TestProfilePreflightRefusesUnhonouredAccessPolicy)$'
  ./internal/agent ./internal/cli`.
- `GOCACHE=/private/tmp/roundfix-0158-qa-rerun-gocache go test -count=1 -v
  -run
  '^TestImplementTaskContentChoosesVerificationByTaskType/supported_policy_is_proven_for_preferred_and_fallback_selections$'
  ./internal/cli` — pass (exit 0).
- `rg -q '^status: accepted' docs/adr/0159-verification-runs-past-a-failure-only-when-declared-independent.md
  && rg -q 'verification: independent'
  docs/adr/0159-verification-runs-past-a-failure-only-when-declared-independent.md
  && rg -q '^status: accepted'
  docs/adr/0160-only-the-frozen-authorization-opens-a-red-repository-gate.md
  && rg -q 'precondition_repairs'
  docs/adr/0160-only-the-frozen-authorization-opens-a-red-repository-gate.md
  && rg -qi 'agent_full_access|full access' docs/user-guide/configuration.md
  && rg -qi 'readiness' docs/user-guide/configuration.md && rg -qi 'refus'
  docs/user-guide/configuration.md && rg -qi 'predicate'
  docs/user-guide/configuration.md && rg -qi 'disable full access|supporting
  runtime|runtime that supports' docs/user-guide/configuration.md` — pass
  (exit 0).
- `GOCACHE=/private/tmp/roundfix-0158-qa-rerun-gocache go build -o
  /private/tmp/roundfix-0158-qa-rerun ./cmd/roundfix` — pass (exit 0). The
  binary reported `roundfix 0.16.0`, and its strict Spec check passed.
- `git blame -L 6,10 -- .roundfixrc.yml`, `git show -s --format=... c1ee13f9`,
  `git show -s --format=... 99c9741b`, and the two ancestry checks from the
  measurement commit to Spec authoring and from Spec authoring to `HEAD` — pass
  (exit 0). A broader `git diff --exit-code c1ee13f9..HEAD --
  .roundfixrc.yml` exited 1 because later profile configuration changed below
  the incident record; line-level blame confirmed that the five evidence lines
  still come from the pre-Spec measurement commit.
- `git log -1 --format=... --
  docs/specs/0158-daemon-verification-and-access-readiness/_authorization.md`,
  `git merge-base --is-ancestor 99c9741b HEAD`, authorization-field `rg` reads,
  and `test ! -d
  docs/specs/0158-daemon-verification-and-access-readiness/reviews` — pass (exit
  0), proving the authorization ancestry and absent Spec review-artifact tree.

## Results

| # | Status | Provenance |
| - | --- | --- |
| R1 | pass | Requirement 1; repository Verification |
| R2 | pass | Requirement 2 |
| R3 | pass | Requirement 3 |
| R4 | pass | Requirement 4 |
| R5 | pass | Requirement 5 |
| R6 | pass | Requirement 6 |
| R7 | pass | outside-evidence row |
| R8 | blocked (environment: no open Pull Request) | Pull Request row |

## Row evidence

### R1

```yaml
inputs:
  - kind: repository_path
    ref: "**"
```

- Actor and surface: Daemon; repository gate.
- Entry point and steps: run `make verify` once for the audited tree.
- Expected observable: exit 0 and no retained success diagnostics.
- Independent confirmation: Daemon settlement supplies the exit code and
  diagnostic disposition.
- Persistence check: not applicable to a static gate.
- Evidence path: daemon-owned gate fact in this report.
- Result: pass. The supplied Daemon fact records the exact command, exit 0 and
  successful diagnostic disposition for this audited tree.

### R2

```yaml
inputs:
  - kind: repository_path
    ref: internal/spec/task.go
  - kind: repository_path
    ref: internal/spec/task_test.go
  - kind: repository_path
    ref: internal/daemon/engine.go
  - kind: repository_path
    ref: internal/daemon/engine_test.go
  - kind: repository_path
    ref: internal/daemon/task_engine.go
  - kind: repository_path
    ref: internal/daemon/task_engine_test.go
```

- Actor and surface: Task author and Daemon; Task declaration and Verification
  Feedback boundary.
- Entry point and steps: execute the focused public-contract cases for declared
  independent Tasks and an undeclared Task.
- Expected observable: the ordinary declared Task runs all commands and hands
  both deterministic failures to its one repair turn; deterministic failures
  remain reported beside later temporary or unknown outcomes; the undeclared
  Task stops at the first failure.
- Independent confirmation: assert command order, attempt count, diagnostic
  identity, Verification Feedback content and published Verification events.
- Persistence check: a fresh Go test process reran all five focused cases.
- Evidence path: focused command output recorded in this report.
- Result: pass. All five named cases passed. The ordinary independent fixture
  ran all three commands on both attempts, published failures for commands one
  and three and handed both diagnostic paths to one repair turn. The temporary
  fixture retained its deterministic failure across the exclusive retry and
  delivered it to the repair turn. The unknown-cause fixture retained both the
  deterministic command and the unobserved command in the outcome reason. The
  undeclared fixture stopped on command one on both bounded attempts. Parser
  subcases recorded `verification: independent` and refused
  `sequential-groups` by name.

### R3

```yaml
inputs:
  - kind: repository_path
    ref: internal/authorization/authorization.go
  - kind: repository_path
    ref: internal/authorization/authorization_test.go
  - kind: repository_path
    ref: internal/daemon/task_engine.go
  - kind: repository_path
    ref: internal/daemon/task_engine_test.go
  - kind: repository_path
    ref: internal/cli/implement.go
  - kind: repository_path
    ref: internal/cli/implement_test.go
```

- Actor and surface: local operator; Implement planning, Daemon precondition
  and Task settlement boundaries.
- Entry point and steps: execute the focused authorization, named and unnamed
  entry, frozen-plan settlement, unknown-cause, diagnostic and exact-match
  cases around a red repository command.
- Expected observable: only a named Task with an observed red verdict enters;
  it cannot delete the frozen repository command before settlement; an unnamed
  Task remains blocked; planning requires the verbatim command; runtime entry
  detection still recognizes padding; precondition diagnostics survive later
  attempts.
- Independent confirmation: assert Agent-turn counts, command sequences,
  Daemon Verification events, final Task state, refusal messages, diagnostic
  paths and absent Run artifacts.
- Persistence check: a fresh Go test process reran all ten focused cases.
- Evidence path: focused command output recorded in this report.
- Result: pass. The named repair observed a red `make verify`, retained its
  Run-start authorization after the Worktree record changed and settled only
  after the gate passed. When the Agent removed `make verify`, both settlement
  attempts restored the frozen command and the Task failed while it stayed red.
  An unknown verdict and an unnamed padded command started no Agent turn. The
  red-entry diagnostic remained at a separate path. Planning rejected both a
  padded near-match in `precondition_repairs` and an unknown Task identifier
  before execution.

### R4

```yaml
inputs:
  - kind: repository_path
    ref: internal/agent/agent.go
  - kind: repository_path
    ref: internal/agent/agent_test.go
  - kind: repository_path
    ref: internal/agent/acpx_runner.go
  - kind: repository_path
    ref: internal/agent/acpx_runner_test.go
  - kind: repository_path
    ref: internal/agent/selection_assignment.go
  - kind: repository_path
    ref: internal/cli/profiles_validate.go
  - kind: repository_path
    ref: internal/cli/implement_test.go
```

- Actor and surface: local operator; profile readiness and Implement preflight.
- Entry point and steps: execute the focused unsupported-runtime,
  adapter-refusal, supported-mode and degraded-Codex cases for requested full
  access.
- Expected observable: unsupported or refused access fails readiness before a
  Run exists; supported access is applied and recorded for preferred and
  fallback selections; a missing Codex sandbox preset qualifies the effective
  policy as degraded and names that preset.
- Independent confirmation: assert the mode call, failed predicate, remedy,
  effective policy, disposable-session close and absent Run Database,
  Worktree, stdout and Agent work.
- Persistence check: the cases ran with fresh disposable sessions after the
  restricted environment prevented the first command from starting.
- Evidence path: focused command output recorded in this report.
- Result: pass. Codex and Claude reported full-access modes, OpenCode reported
  the requested mode unavailable and runtime-default access needed no mode.
  A disposable supported proof issued one `set-mode bypassPermissions`,
  recorded `full-access` and closed its session. Adapter rejection surfaced
  `adapter_accepts_access_mode`; the unsupported runtime surfaced
  `runtime_has_access_mode`; both preflight paths named the remedy and created
  no Run artifacts. When Codex accepted full access but did not advertise the
  `danger-full-access` sandbox preset, the proof returned
  `full-access (degraded: danger-full-access sandbox preset unavailable)`.
  The companion preflight case recorded `full-access` for every preferred and
  fallback proof.

### R5

```yaml
inputs:
  - kind: repository_path
    ref: docs/adr/0159-verification-runs-past-a-failure-only-when-declared-independent.md
  - kind: repository_path
    ref: docs/adr/0160-only-the-frozen-authorization-opens-a-red-repository-gate.md
  - kind: repository_path
    ref: docs/user-guide/configuration.md
```

- Actor and surface: maintainer; ADRs and configuration guide.
- Entry point and steps: run assertions over accepted decision status, the
  declared-independence and frozen-authorization contracts and the documented
  readiness refusal.
- Expected observable: both ADRs are accepted and the guide names
  `agent_full_access`, readiness, pre-Run refusal, the failed predicate and the
  remedy.
- Independent confirmation: the current-tree strict Spec check reports no
  unsupported citation or promise finding.
- Persistence check: repeat the assertions against the audited tree.
- Evidence path: assertion and strict-check output recorded in this report.
- Result: pass. Both ADRs carry accepted status, ADR-0159 names
  `verification: independent`, ADR-0160 names `precondition_repairs`, and the
  guide states that requested full access is applied during readiness and
  refused before Run creation with the failed predicate and a remedy to disable
  full access or select a supporting runtime.

### R6

```yaml
inputs:
  - kind: repository_path
    ref: docs/specs/0158-daemon-verification-and-access-readiness/_prd.md
  - kind: repository_path
    ref: docs/specs/0158-daemon-verification-and-access-readiness/_techspec.md
  - kind: repository_path
    ref: docs/specs/0158-daemon-verification-and-access-readiness/_tasks.md
  - kind: repository_path
    ref: docs/specs/0158-daemon-verification-and-access-readiness/task_05.md
```

- Actor and surface: Spec author; public `roundfix spec check` command.
- Entry point and steps: run the strict consistency check over this Spec's
  explicit promise mappings and consuming Task references.
- Expected observable: no promise-rule or coverage finding.
- Independent confirmation: repeat the check through a binary built from the
  audited tree and inspect the command's explicit no-findings output.
- Persistence check: two fresh public-command processes checked the same tree.
- Evidence path: strict-check outputs recorded in this report.
- Result: pass. Both the PATH command and the current-tree `roundfix 0.16.0`
  command exited 0 with `No findings` and stated that authored Verification
  commands were not executed. Task 05 names PRD Goals 1-3, Core Features 1-3,
  Success Metrics 1-3 and Acceptance evidence, plus TechSpec Testing Approach
  1-4 and API Contracts 1-3. The four disclosed missing-artifact skips were
  not promise or coverage findings.

### R7

```yaml
inputs:
  - kind: repository_path
    ref: .roundfixrc.yml
```

- Actor and surface: maintainer; pre-Spec access-policy measurement recorded in
  repository configuration and Git history.
- Entry point and steps: establish the measured codex-acp refusal, its date and
  its provenance before Spec 0158 was authored.
- Expected observable: independent evidence shows that requested full access
  reached `session/set_mode`, returned ACP `-32602` and failed Tasks before
  Agent work.
- Independent confirmation: compare the measurement commit with the Spec
  authoring commit and the fresh R4 refusal evidence.
- Persistence check: repeat line-level blame and ancestry checks.
- Evidence path: focused Git output recorded in this report.
- Result: pass. The source is `.roundfixrc.yml` lines 6-10, an incident record
  that Spec 0158 did not author: codex-acp 1.1.9 returned ACP `-32602` from
  `session/set_mode "full-access"` on 2026-08-04 and failed every Task before
  Agent work. `git blame` attributes all five lines to commit
  `c1ee13f9c74a40435314d33fcd7b4ba93067a409` dated 2026-08-05, and the
  ancestry check proves that commit predates Spec authoring commit
  `99c9741bf6b58da92adf91dcda6186680e98ca5f`, which is itself an ancestor of
  the audited head. R4 independently reproduced adapter refusal and the
  pre-Run no-artifact boundary on this build. Git emitted a non-blocking
  fsmonitor IPC warning while blame still completed with exit 0 and complete
  line provenance.

### R8

```yaml
inputs:
  - kind: live_service
    ref: Pull Request state for feat/0158-daemon-verification-and-access
```

- Actor and surface: reviewer; Pull Request controls.
- Entry point and steps: observe approval, checks and status, unresolved review
  threads, Merge-Ready acceptance and review-artifact ancestry.
- Expected observable: all five controls are explicitly accounted for.
- Independent confirmation: compare every available equivalent artifact with
  the claimed head.
- Persistence check: fresh Pull Request read when one exists.
- Evidence path: supplied environment fact and equivalent evidence recorded in
  this report.
- Result: blocked (environment: no open Pull Request), with equivalent evidence
  for every control:
  - approval: no Pull Request acceptance exists yet; authorization commit
    `99c9741b` records maintainer approval on 2026-09-24 for the Spec's
    implementation and delivery operations and is an ancestor of audited head
    `19fd8559`;
  - checks and status: daemon-owned `make verify` exited 0;
  - unresolved review threads: no Pull Request exists, so no thread set exists
    to leave unresolved;
  - Merge-Ready acceptance: no Review Source evidence exists yet;
  - review-artifact ancestry: the Spec has no `reviews/` artifact tree and no
    Pull Request head, so no ancestry claim was made.
  The unblocking action is to open the Pull Request on
  `feat/0158-daemon-verification-and-access` after this gate settles, then
  observe all five controls against the Pull Request's actual head.

## Findings

None.

## Blocked and skipped

- R8 is environment-blocked because the QA prompt proves that no Pull Request
  is open. All five Pull Request controls carry explicit equivalent evidence in
  the row. No row is finding-blocked, declared-blocked or skipped.

## Coverage

Complete: seven rows passed and one row is environment-blocked with equivalent
evidence. All six declared verification Requirements and the mandatory
outside-evidence, Pull Request and repository Verification sources appear in
provenance; no row covers another source. Requirement 7 constrains every row's
evidence and is not a declared verification source. The PRD declares no
Unreachable Acceptance and no frontend surface. Counts:
`rows_blocked_environment: 1`, `rows_blocked_finding: 0`,
`rows_blocked_declared: 0`; failed: 0; skipped: 0.

## Mechanical skips

| Detector | Missing artifact |
| --- | --- |
| authorization bounded paths | tooling authorization |
| consequent-fix commit order | consequent-fix declaration |
| QA evidence paths | docs/specs/0158-daemon-verification-and-access-readiness/qa/evidence |

These pre-Agent mechanical skips are preserved exactly as seeded; the Agent
did not reinterpret or remove them after materializing this report.

## Final verdict

Pass. Every runnable row passed; the sole environment-blocked Pull Request row
records its cause, unblocking action and equivalent evidence for all five
controls. There are no findings, declared blocks, finding blocks, skipped rows
or unresolved evidence paths.
