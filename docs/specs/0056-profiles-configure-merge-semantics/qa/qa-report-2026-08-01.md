---
spec: 0056-profiles-configure-merge-semantics
date: 2026-08-01
build: e1c3b1e
status: closed
verdict: fail
rows_blocked_environment: 3
rows_blocked_finding: 0
surfaces: [backend, cli, docs]
---

# QA report — Profiles configure merge semantics

## Scope and environment

Full gate over PRD user stories 1–6, Core Features 1–7, Success Metrics,
Non-Goals, all acceptance criteria in tasks 01–06, the Project Constraint
audit, the repository Verification, and the declared Pull Request environment.

- Build: `e1c3b1e` on Run Worktree branch
  `roundfix/run-run_20260801T231342Z_38cd1e8831b0ae1f`.
- Spec target branch: `ma/profiles-configure-merge-semantics`.
- Pull Request: none open; Pull Request journeys are environment-blocked.
- Intended actors: human operator, non-interactive Agent, maintainer, and docs
  reader.
- Production-like entry point used: freshly built `roundfix` binary in a
  scratch repository with isolated project configuration.
- Evidence:
  [command-evidence.md](evidence/2026-08-01-run-38cd1e8831b0ae1f/command-evidence.md).
  The `command-evidence.md` shorthand in the matrix refers to this file.

## Planned matrix

Every row starts `pending`. Status is updated immediately after execution.

| # | Story / criterion / sweep | Actor, entry point, surface, and steps | Expected observable | Independent confirmation and persistence | Evidence | Status |
| - | --- | --- | --- | --- | --- | --- |
| PC-01 | PRD/TechSpec Project Constraints; task completion; tooling authorization and actual changed-file ancestry | Maintainer; Spec artifacts and `git diff-tree`; docs/repository audit | Identifier, auth/HTTP, ADR, and tooling applicability are cited; every task is completed; protected-tooling scope and commit choreography are valid | Task status, authorization, and paths are clean, but accepted ADR obligations are incomplete (F-001) | `command-evidence.md` | fail |
| SG-01 | Repository static gate | Maintainer; `rtk make verify`; backend/CLI/docs | Full repository Verification exits 0 | Exit 0; 2,995 Go tests, 4 focused Skill tests, Skill check, and production binary build passed | `command-evidence.md` | pass |
| T01-01 | Task 01 ACs; Core Feature 7; corpus coverage, deterministic comparison, readable drift, explicit regeneration | Maintainer; focused current tests over the characterization corpus; backend | All required shapes exist, current writer outputs match, normal runs do not rewrite goldens, deliberate drift names the case, regeneration is flag-gated | Nine cases passed; two runs preserved every hash; current diagnostic/update seams and named Task reproduction were inspected | `command-evidence.md` | pass |
| US-01 | User Stories 1 and 3; Task 03 ACs; merge by category, atomic replacement, byte-preserved unrelated categories, minimal diff | Human operator; built `roundfix profiles configure --scope project --file ... --yes`; CLI/backend | Named category replaces atomically; four unnamed categories, comments, order, indentation, and Fallback Chains survive; only intended value lines change | Replacement was proof-environment-blocked with equivalent focused evidence; live removal independently contradicted minimal formatting preservation (F-003) | `command-evidence.md` | fail |
| US-02 | User Story 2; Task 04 summary ACs; add/replace/remove text and machine classifications | Human and Agent; built text and `--json` configure/dry-run flows; CLI | One ordered line/entry per affected category, correct kind, no untouched category, explicit absent removal | Live removal text/JSON agreed and preserved bytes; add/replace were blocked by invalid Codex signature with equivalent focused evidence | `command-evidence.md` | blocked (environment: effective Codex binary has invalid or missing code signature) |
| US-03 | User Story 4; Task 05 ACs; declined and unconfirmable writes | Agent; built non-interactive and declined confirmation flows; CLI | Refusal exits 1, writes nothing, diagnostic is on stderr, JSON marks `refused: true`; validation remains 2 | Captured exit 1, stdout-only JSON, stderr-only preview/refusal, unchanged hash; conflict exited 2 | `command-evidence.md` | pass |
| US-04 | User Story 5; Task 02/03 ACs; explicit removal, absent removal, conflict, deterministic ordering | Human and Agent; built repeatable `--remove`; CLI/backend | Present removal deletes only named category; absent removal is reported; omission never deletes; fragment/remove conflict names category and exits 2 | Functional removal, absent no-op, conflict, repeat, and public reload passed, but removal also deleted an unrelated blank separator (F-003) | `command-evidence.md` | fail |
| US-05 | User Story 6; successful legacy paths: apply, no-op, dry-run, empty file, and no-`profiles` file | Maintainer; built CLI across scratch configs; CLI/backend | Applied and no-op exit 0; dry-run exits 0 without write; empty/sectionless configs remain valid | Live removal apply/no-op/dry-run passed; proof-gated add/replace/empty/sectionless paths were environment-blocked with current focused equivalents | `command-evidence.md` | blocked (environment: effective Codex binary has invalid or missing code signature) |
| CF-06 | Core Feature 6 and Task 04 proof ACs; proof precedes writes and scopes to written categories | Agent; public configure path plus focused proof-order test; CLI/backend | PRD: every distinct tuple in the resulting effective map is proven before confirmation/write | Current tests/docs intentionally prove only written categories, contradicting the PRD (F-002) | `command-evidence.md` | fail |
| T03-02 | Task 03 alias/anchor AC and format corpus seam | Maintainer; built/config writer failure path plus focused corpus; backend/CLI | Dangling surviving alias is rejected before write; every corpus document reparses | Public removal exited 2 on unknown anchor and preserved hash; all nine corpus cases reparsed | `command-evidence.md` | pass |
| T06-01 | Task 06 ACs and docs surface sweep | Docs reader; follow `docs/user-guide/commands.md` and built `--help`; docs/CLI | Merge, omission, `--remove`, summary kinds, proof scope, and exit table are discoverable and accurate | Help, guide, focused synchronization test, and observed CLI behavior agree | `command-evidence.md` | pass |
| NG-01 | Non-Goals: no field-level merge; no resolution/fallback/proof semantic change; no YAML lint pass; no `profiles show`/`validate` change | Maintainer; public CLI canaries and changed-path review; CLI/backend/docs | Atomic replacement remains; unrelated commands work; untouched YAML is not generally reformatted; no scope creep | Atomic/show/validate source canaries passed; live writer removed an unrelated blank line (F-003) | `command-evidence.md` | fail |
| PR-01 | Pull Request approval/check/review/merge-ready journeys | Maintainer; read-only PR observation | If available, approval, checks, review threads, and Merge-Ready evidence are observable | No PR is open; local ancestry, full Verification, public CLI, and focused suites provide equivalent supervised evidence | `command-evidence.md` | blocked (environment: no open Pull Request) |

Behavior probes selected for the destructive-write journeys: repeat/no-op,
non-interactive EOF refusal, invalid fragment/removal conflict, absent removal,
dry-run/reopen, and a dangling alias. These directly probe accidental deletion,
false-success automation, determinism, and fail-closed persistence.

## Static gate

`rtk make verify` exited 0 on build `e1c3b1e` (the build metadata correctly
recorded the QA report worktree delta as `e1c3b1e-dirty`). It reported 2,995
passing Go tests across 24 packages, 4 passing focused Skill tests, a passing
Roundfix Skill check, and a successful `bin/roundfix` build.

## Results

| # | Story / criterion / sweep | Actor and surface | Status | Evidence |
| - | --- | --- | --- | --- |
| PC-01 | Project Constraints and changed-file audit | Maintainer; docs/repository | fail | F-001: ADR-0055 is absent from both Project Constraint audits; ADR-0086 is absent from the PRD audit. Task completion, authorization ancestry, and changed-file scope are otherwise valid. |
| SG-01 | Full repository Verification | Maintainer; backend/CLI/docs | pass | `rtk make verify`: exit 0; 2,995 Go tests, 4 Skill tests, Skill check, and binary build passed. |
| T01-01 | Characterization corpus | Maintainer; backend | pass | Nine named cases passed; consecutive normal runs left all golden hashes unchanged; current failure/update seams and Task drift reproduction are auditable. |
| US-01 | Merge and format preservation | Human operator; CLI/backend | fail | Focused current merge evidence passed; live write exposed F-003 unrelated blank-line churn. |
| US-02 | Per-category summary | Human and Agent; CLI | blocked (environment: invalid/missing Codex signature) | Live text/JSON removal summary agreed; focused current tests equivalently passed add and replace. |
| US-03 | Refusal and exit-code contract | Agent; CLI | pass | Live EOF refusal: exit 1, stdout JSON `refused:true`, diagnostics on stderr, byte-identical file; validation conflict: exit 2. |
| US-04 | Explicit removal | Human and Agent; CLI/backend | fail | Present/absent/conflict/repeat behavior passed; the persisted removal also produced F-003. |
| US-05 | Existing successful paths | Maintainer; CLI/backend | blocked (environment: invalid/missing Codex signature) | Live apply/no-op/dry-run safe paths passed; current focused tests equivalently passed proof-gated replacement/add/empty/sectionless paths. |
| CF-06 | Proof-before-write and proof scope | Agent; CLI/backend | fail | F-002: written-category-only implementation contradicts resulting-map proof promised by PRD Core Feature 6. |
| T03-02 | Alias failure and corpus parseability | Maintainer; backend/CLI | pass | Public dangling-alias removal exited 2 before write and preserved bytes; full corpus reload passed. |
| T06-01 | Documentation contract | Docs reader; docs/CLI | pass | Built help, user guide, synchronization test, and observed command agree. |
| NG-01 | Non-Goals and scope creep | Maintainer; CLI/backend/docs | fail | Show/validate and resolution sources are untouched; F-003 violates the no-general-YAML-formatting promise. |
| PR-01 | Pull Request journeys | Maintainer; Pull Request | blocked (environment: no open Pull Request) | Equivalent local evidence: ancestry audit, full Verification, built CLI flows, focused suites, and scope inspection. |

## Findings

### F-001 — Project Constraint ADR coverage is incomplete

- Impact: **Trust-Damage**.
- Actor: maintainer relying on the Spec as the durable governance contract.
- Expected: each active PRD and TechSpec accounts for every applicable active
  ADR obligation under the Project Constraint audit.
- Actual: ADR-0055 predates this Spec and explicitly governs exact capability
  proof before configuration mutation, but neither artifact names it. ADR-0086
  is accepted and defines this Spec's removal surface, but the PRD Project
  Constraints omit it.
- Reproduction: compare both `## Project Constraints` sections with
  `docs/adr/0055-agent-selection-encoding-follows-advertised-acp-capabilities.md`
  and `docs/adr/0086-category-removal-is-declared-by-flag-not-by-fragment-shape.md`.
- Affected rows: PC-01.

### F-002 — Implemented proof scope contradicts PRD Core Feature 6

- Impact: **Trust-Damage**.
- Actor: user relying on configure to prove the complete effective profile map.
- Expected: PRD Core Feature 6 says every distinct tuple in the resulting
  effective map is exact-proven before confirmation.
- Actual: TechSpec Decisions, task 04, `profilesConfigureProofScope`, help, and
  user docs intentionally prove only added/replaced categories and explicitly
  permit an unproven untouched category.
- Reproduction: compare `_prd.md` Core Feature 6 with `_techspec.md` Decisions,
  task 04 Requirement 5, and the passing `TestProfilesConfigureProofScope`.
- Affected rows: CF-06.

### F-003 — Explicit removal deletes unrelated YAML spacing

- Impact: **Friction**.
- Actor: maintainer reviewing the resulting Project Config diff.
- Expected: only the declared category is removed; unrelated formatting stays
  untouched, and the operation performs no general YAML formatting pass.
- Actual: deleting `profiles.frontend` also deleted the blank separator between
  the final `profiles` entry and `defaults`.
- Reproduction: in the five-category scratch repo, run
  `bin/roundfix profiles configure --scope project --remove frontend --yes --json`,
  then compare `.roundfixrc.yml` with the saved original using
  `git diff --no-index`.
- Evidence: the diff contains the expected nine-line `frontend` deletion plus
  an independent deleted blank line immediately before `defaults:`.
- Affected rows: US-01, US-04, NG-01.

## Blocked and skipped

### Environment-blocked rows

- US-02 and US-05: the effective Codex binary at
  `/Users/marcio/.local/bin/codex` has an invalid or missing code signature, so
  public add/replace journeys stop at Exact Agent Selection Proof before any
  write. Equivalent current-build evidence is recorded from focused public-runner
  tests and adjacent live removal/refusal/read paths. Unblock by reinstalling
  the officially signed Codex CLI, setting `CODEX_PATH`, proving it with
  `roundfix profiles validate`, and rerunning these rows.
- PR-01: no Pull Request is open on
  `ma/profiles-configure-merge-semantics`. Equivalent local supervised evidence
  covers ancestry, Verification, public flows, focused tests, and changed-file
  scope. Unblock by opening a Pull Request on the Spec target branch.

No row was skipped or finding-blocked.

## Coverage

- Matrix: 13/13 terminal — 5 pass, 5 fail, 3 environment-blocked, 0
  finding-blocked, 0 skipped, 0 pending.
- User stories: 5/6 behaviorally supported; Story 3 fails minimal-diff
  preservation. Stories 1, 2, and 6 include equivalent focused evidence where
  signed-runtime proof blocked the live add/replace seat.
- Core Features: 5/7 supported; Core Feature 4 fails F-003 and Core Feature 6
  fails F-002.
- Task acceptance: current focused suites cover all Task 01–06 criteria; the
  assembled PRD/governance contradictions remain gate failures.
- Non-Goals: 3/4 supported; the no-general-YAML-formatting exclusion fails
  F-003. Field-level merge, resolution/fallback changes, and show/validate
  changes were not observed.
- Probes completed: repeat/no-op, EOF and explicit decline, invalid
  fragment/removal conflict, absent removal, dry-run/reopen, and dangling
  alias. Frontend sweeps were not applicable because the declared surfaces are
  backend, CLI, and docs.
- Findings by impact: Trust-Damage 2, Friction 1, Blocks-Completion 0,
  Data-Loss 0, Cosmetic 0.

## Final verdict

**Fail.** Correct the Project Constraint ADR coverage, reconcile PRD Core
Feature 6 with the intended proof scope, and preserve unrelated YAML spacing;
then restore a signed Codex runtime and rerun the 3 environment-blocked rows.
Counts: 5 pass, 5 fail, 3 environment-blocked, 0 finding-blocked, 0 skipped,
0 pending.
