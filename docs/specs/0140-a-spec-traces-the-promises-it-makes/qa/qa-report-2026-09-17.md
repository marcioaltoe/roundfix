---
spec: 0140-a-spec-traces-the-promises-it-makes
date: 2026-09-17
build: d948c476
auditing_binary: "0.13.1 (aec92ead, built 2026-09-17 14:40:08 -0300)"
auditor_staleness: "stale: commit ancestry: build commit predates audited tree"
status: closed
verdict: pass
rows_blocked_environment: 1
rows_blocked_finding: 0
rows_blocked_declared: 0
surfaces: [backend, cli, docs]
---

# QA Report — A Spec traces the promises it makes

## Performed repairs

None.

## Assigned repair failures

None.

## Authorization audit inputs

| Task | Outcome | Record | Revision | Detail |
| --- | --- | --- | --- | --- |
| task_07 | granted | docs/specs/0140-a-spec-traces-the-promises-it-makes/_authorization.md | aec92eade5d0a248ac85141a4fecccc2b158a102 | operative grant |

## Mechanical findings

None.

## Scope and environment

- Scope: full authored QA gate on build `d948c476`.
- Actor: a Supervisor authoring and checking Specs through the public `roundfix spec check` CLI.
- Required precondition: `roundfix spec check 0140-a-spec-traces-the-promises-it-makes --strict` exited 0 with no findings. It skipped only absent rollup, backlog, and references-index artifacts.
- Repository Verification: the Daemon already ran `make verify` outside the Agent sandbox and reported exit 0; this gate will record that supervised result without rerunning it.
- Pull Request: none is open for `feat/0140-traced-promises`; Pull Request journeys are environment-blocked and use equivalent supervised evidence.
- Parity: the auditing binary identifies build `aec92ead`, which is an ancestor of the audited tree and therefore stale for newly implemented behavior. Public behavior rows use `go run ./cmd/roundfix` from audited source; mechanical metadata remains preserved above.

## Results

| # | Status | Provenance |
| - | --- | --- |
| R1 | pass | Requirement 1; repository Verification |
| R2 | pass | Requirement 2 |
| R3 | pass | Requirement 3 |
| R4 | pass | Requirement 4; outside-evidence row |
| R5 | pass | Requirement 5 |
| R6 | pass | Requirement 6 |
| R7 | pass | Requirement 7 |
| R8 | blocked (environment: no open Pull Request) | Pull Request row |

## Row evidence

### R1

- Actor and surface: repository maintainer; repository gate.
- Entry point and steps: supervised Daemon execution of `make verify`.
- Expected observable: exit 0 with no diagnostics.
- Independent confirmation and persistence: the Daemon gate ran against the assembled worktree after Task 07 and supplied the result directly to this QA run.
- Evidence path: this report's Static gate section.
- Observed: `make verify` exited 0 and produced no retained diagnostic.

### R2

```yaml
inputs:
  - kind: repository_path
    ref: internal/speccheck/testdata/repo/docs/specs/**
  - kind: repository_path
    ref: internal/speccheck/**
```

- Actor and surface: Supervisor; public CLI.
- Entry point and steps: run the source-built checker on fixtures with missing/prose declarations and on a fully declared fixture.
- Expected observable: undeclared sections produce their coined `gap` at the authoring stage; declared and consumed promises produce no promise finding.
- Independent confirmation and persistence: rerun as JSON and inspect code, severity, stage, and artifact fields after a fresh process start.
- Evidence path: `qa/evidence/2026-09-17-final/cli-declaration-behavior.txt`.
- Observed: Spec 0120 emitted the metric gap from `_prd.md` and the contract gap from `_techspec.md`; Spec 0140 emitted no finding.

### R3

```yaml
inputs:
  - kind: repository_path
    ref: internal/speccheck/**
```

- Actor and surface: Supervisor; public CLI and checker fixture seam.
- Entry point and steps: exercise a declared but unreferenced promise and an unmapped metric through the source-built checker, then run the named focused test as independent confirmation.
- Expected observable: `SC-COVERAGE-UNTASKED` and `SC-COVERAGE-UNMAPPED` remain `error` findings and name the declared unit.
- Independent confirmation and persistence: fresh CLI/test processes observe the same result.
- Evidence path: `qa/evidence/2026-09-17-final/cli-coverage-errors.txt`.
- Observed: the scratch CLI run emitted three unmapped metric errors and six untasked promise errors; the isolated five-case test passed.

### R4

```yaml
inputs:
  - kind: repository_path
    ref: docs/specs/012*/**
  - kind: repository_path
    ref: docs/specs/0140-a-spec-traces-the-promises-it-makes/qa/evidence/task-03-corpus-sweep.md
```

- Actor and surface: Supervisor; public CLI over the ten pre-existing authoring Specs.
- Entry point and steps: read the prediction recorded before the sweep, run the source-built checker over the active corpus as JSON, and aggregate findings for Specs 0120 through 0129.
- Expected observable: ten metric gaps, ten contract gaps, zero errors, no other new code, and no changed artifact in those Specs.
- Independent confirmation and persistence: compare the live aggregate with the committed corpus golden and recheck the ten Specs' worktree paths after the sweep.
- Evidence path: `qa/evidence/2026-09-17-final/corpus-replay.txt` and the pre-existing Task 03 prediction.
- Observed: the fresh replay matched 10 metric gaps, 10 contract gaps, zero errors, and no changed authoring Spec.

### R5

```yaml
inputs:
  - kind: repository_path
    ref: .agents/skills/write-*/**
  - kind: repository_path
    ref: skills/write-*/**
```

- Actor and surface: Spec author; distributed authoring documentation.
- Entry point and steps: follow each canonical skill/template clause and compare every mirror byte-for-byte.
- Expected observable: all bounded files carry the new declaration or tracing clause, the Task template names both promise kinds, and every mirror is identical.
- Independent confirmation and persistence: exact `cmp` checks plus searches from a fresh process.
- Evidence path: `qa/evidence/2026-09-17-final/authoring-distribution.txt`.
- Observed: all six canonical/mirror pairs were byte-identical, every clause was present, and every skill version remained `0.0.2`.

### R6

```yaml
inputs:
  - kind: repository_path
    ref: docs/specs/0140-a-spec-traces-the-promises-it-makes/_prd.md
  - kind: repository_path
    ref: docs/specs/0140-a-spec-traces-the-promises-it-makes/_techspec.md
  - kind: repository_path
    ref: docs/specs/0140-a-spec-traces-the-promises-it-makes/_tasks.md
  - kind: repository_path
    ref: docs/specs/0140-a-spec-traces-the-promises-it-makes/task_*.md
```

- Actor and surface: Supervisor; public CLI on this Spec.
- Entry point and steps: run the source-built strict checker on Spec 0140 and inspect its declarations, Coverage Map, and Task References.
- Expected observable: no promise declaration, mapping, or Task reference finding.
- Independent confirmation and persistence: a second JSON invocation confirms zero findings.
- Evidence path: `qa/evidence/2026-09-17-final/self-application.txt`.
- Observed: both strict text and JSON checks exited 0 with no finding.

### R7

- Actor and surface: maintainer; Git history and approved authority record.
- Entry point and steps: resolve each consuming Task commit, enumerate paths with `git diff-tree`, resolve the authorization commit and bounded files, and compare chronological ancestry.
- Expected observable: authorization precedes every consuming commit; each governed path lies within the exact grant plus its assigned Task file; prerequisite and consequent fix ordering is valid.
- Independent confirmation and persistence: repeat ancestry checks from commit identifiers and inspect the clean committed trees rather than Task prose.
- Evidence path: `qa/evidence/2026-09-17-final/authorization-audit.txt`.
- Observed: the final grant commit precedes all six Task commits, every governed path is authorized, and no authorization, prerequisite fix, consequent fix, or derived pin has invalid ordering.

### R8

```yaml
inputs:
  - kind: live_service
    ref: GitHub Pull Request state for feat/0140-traced-promises
```

- Actor and surface: maintainer; Pull Request controls.
- Entry point and steps: use the prompt's confirmed no-open-Pull-Request fact and record equivalent evidence for approval, checks/status, unresolved threads, Merge-Ready acceptance, and review-artifact ancestry.
- Expected observable: the environmental cause is explicit and every control has equivalent observed or supervised evidence.
- Independent confirmation and persistence: supervised prompt fact plus local branch/head and review-artifact inspection.
- Evidence path: this report's Blocked and skipped section.
- Observed: no Pull Request is open, and each Pull Request control is answered below with equivalent supervised or local evidence.

## Static gate

- `make verify` — pass, exit 0. The Daemon ran this repository Verification outside the Agent sandbox after Task 07 and removed diagnostics on success. Per the QA contract, this Agent did not rerun it.
- `roundfix spec check 0140-a-spec-traces-the-promises-it-makes --strict` — pass, exit 0, no findings. This was the required authoring precondition.
- Source-built CLI parity: the first `go run` attempt could not write the sandboxed default Go cache and never reached the checker. The audited source was then built once with `GOCACHE=/private/tmp/roundfix-qa0140-gocache`; all public behavior rows used that binary or the same source-built command.

## Findings

None.

## Blocked and skipped

- R8 is `blocked (environment: no open Pull Request)`. The prompt confirms that no Pull Request is open for `feat/0140-traced-promises`, and Pull Request journeys are environment-blocked.
  - Approval: no Pull Request acceptance was obtained. The maintainer's bounded tooling authorization is recorded separately in `_authorization.md`; it is not represented as Pull Request approval.
  - Checks and status: the supervised repository Verification `make verify` passed at exit 0.
  - Unresolved review threads: none can stand because no Pull Request exists.
  - Merge-Ready acceptance: no Review Source acceptance exists yet.
  - Review-artifact ancestry: no review artifact exists in the Spec directory. The audited Run head is `d948c47676729bd55736964feaa41cef76a94675`; the current target-branch head is `aec92eade5d0a248ac85141a4fecccc2b158a102`.
  - Unblocking action: after the Daemon lands the QA commit on `feat/0140-traced-promises`, open the Spec Pull Request and run its live approval, check, thread, Merge-Ready, and ancestry observations.
- No row was finding-blocked, declared-blocked, or skipped.

## Coverage

- Declared verification Requirements: 7/7 passed.
- Non-waivable sources: repository Verification passed; outside evidence passed; Pull Request row environment-blocked with all equivalent controls recorded.
- Rows: 7 passed, 1 environment-blocked, 0 failed, 0 finding-blocked, 0 declared-blocked, 0 skipped, 0 pending.
- Surfaces exercised: the public CLI, checker backend, distributed authoring documentation, local Git history, and supervised repository gate.
- Frontend sweep: not applicable; the PRD declares no frontend surface.
- Unreachable Acceptance: none declared.

## Final verdict

Pass. Every runnable row passed, the single environment-blocked Pull Request row records its cause and equivalent evidence for every control, all evidence paths resolve, and no row failed, remained pending, or was finding-blocked, declared-blocked, or skipped.

## Mechanical skips

| Detector | Missing artifact |
| --- | --- |
| consequent-fix commit order | consequent-fix declaration |
| QA Report structure | QA Report |
| QA evidence paths | QA Report |
