---
spec: 0006-acpx-run-robustness
date: 2026-07-05
verdict: pass
surfaces: [cli, infra]
---

# QA Report - ACPX Run Robustness

## Summary

The QA gate exercised the CLI and infra surfaces for the completed
ACPX Run Robustness spec. Static verification passed through `rtk make verify`;
focused runner, daemon, and settle regression groups passed; and the built
`roundfix` binary was exercised against a scratch failed Task recovery flow.
Every PRD user story, core feature, non-goal, and task acceptance criterion
has passing evidence, so the verdict is `pass`.

## Results

| # | Criterion / user story | Verdict | Evidence |
| - | ---------------------- | ------- | -------- |
| 1 | Preconditions: all Spec tasks are complete before QA. | pass | `rtk rg -n "^(status:|## Result|## Acceptance Criteria)" docs/specs/0006-acpx-run-robustness` showed `task_01.md`, `task_02.md`, `task_03.md`, and `task_04.md` with `status: completed` and `## Result` sections. |
| 2 | Static gate: repository verification passes before flow QA. | pass | `rtk make verify` exited 0: `rtk go test ./...` reported 605 passing tests in 16 packages, `roundfix skills check` passed, and `go build -o bin/roundfix ./cmd/roundfix` completed. |
| 3 | User story 1: a transport exit after a parsed prompt result proceeds to verification instead of failing the Batch. | pass | `rtk go test ./internal/agent/ -run TestACPXPromptExitClassificationMatrix -v` passed 9 tests; `rtk go test ./internal/daemon/ -run 'TestResolveCycleJournalsTransportAnomalyBeforeVerification|TestTaskCycleCommitsAfterTransportAnomalyAndPassingVerification' -v` passed the focused daemon checks. |
| 4 | User story 2: a failed-but-done Task can be recovered with one command after Verification passes. | pass | Scratch repo `/private/tmp/roundfix-qa-0006-settle-clean.7CDZxV`: `HOME=/private/tmp/roundfix-qa-0006-home-clean.Db8d1F rtk /Users/marcio/dev/roundfix/bin/roundfix settle --spec qa-smoke --task task_01` exited 0 with `verify test -f done.txt — ok` and `settled task_01 completed — 49e4861`; task status became `completed`. |
| 5 | User story 3: acpx buffer limit mitigation or upstream-ready evidence exists. | pass | `rtk npm list -g acpx --depth=0` reported `acpx@0.12.0`; `rtk acpx --help`, `rtk acpx config --help`, `rtk acpx config show`, `rtk acpx config show --help`, and `rtk acpx config init --help` showed no buffer-size option or key; source search found the hard constant and error path in `output-CjdF5rHk.js:1112`, `:1226`, and `:1228`. |
| 6 | User story 4: teardown anomalies are visible in the Run Event Journal with the stderr tail. | pass | `rtk go test ./internal/daemon/ -run 'TestResolveCycleJournalsTransportAnomalyBeforeVerification|TestTaskCycleTransportAnomalyStillLetsVerificationGateSettleFailure|TestTaskCycleCommitsAfterTransportAnomalyAndPassingVerification' -v` passed 3 daemon tests; `rtk rg -n 'TransportAnomaly|Message buffer exceeded' internal/daemon internal/agent` found the anomaly payload and stderr-tail assertions. |
| 7 | Core feature 1: result-over-exit classification changes only the parsed-result, nonzero-exit case; no-result and Stop cases remain unchanged. | pass | `TestACPXPromptExitClassificationMatrix` passed and covers result+exit0, result+exit1 anomaly success, no-result+exit1 failure, no-result+exit2/+exit4 infrastructure errors, result+exit130 Stop Request, partial-stream+exit1 failure, and the buffer-error incident shape. |
| 8 | Core feature 2: `roundfix settle` enforces Preflight Validation, runs Verification verbatim, commits on pass, writes nothing on failure, creates no Run, and never pushes. | pass | `rtk go test ./internal/cli/ -run TestRunSettle -v` passed 14 tests. The scratch run commit `49e486165394e9596c0e88c9f6763e33b83db0eb` had subject `feat: recover completed work`, trailers `Roundfix-Spec: qa-smoke` and `Roundfix-Task: task_01`, and included only `docs/specs/qa-smoke/task_01.md` plus `done.txt`. |
| 9 | Core feature 3: buffer investigation records the upstream-only outcome and shipped docs state the known constraint. | pass | `rtk rg -n 'Message buffer exceeded|MAX_MESSAGE_BUFFER_SIZE|10 MiB|large docs-task' README.md .agents/skills/roundfix/SKILL.md skills/roundfix/SKILL.md docs/_inbox/dogfood-findings-2.md` found the known constraint, trigger, hard-limit evidence, and dated findings outcome. |
| 10 | Core feature 4: no silent anomalies; Run path journals anomalies and settle reports deterministic stdout. | pass | Daemon anomaly tests passed; built CLI `rtk ./bin/roundfix settle --help` documented deterministic behavior; scratch settle stdout was exactly one Verification line followed by one settle line. |
| 11 | User experience: top-level help lists `settle`, and settle help documents flags, exit codes, failed-task recovery, no Run journal, no push, and the stage-everything contract. | pass | `rtk ./bin/roundfix --help` listed `roundfix settle --spec <slug> --task <task_id>` and described `settle`; `rtk ./bin/roundfix settle --help` exited 0 and documented `--spec`, `--task`, exit codes 0/1/2, all-current-worktree-changes commit content, no Run, no Run Event Journal entries, and no push. |
| 12 | Non-goal: no retry budgets or Agent escalation were added. | pass | The implemented and tested behavior is limited to classification, settle, docs, and acpx evidence. `rtk rg -n 'prompt-retries|retry budget|escalation|settle --spec' internal README.md .agents/skills/roundfix/SKILL.md` found no new Roundfix retry-budget or Agent escalation behavior tied to this spec. |
| 13 | Non-goal: verification semantics, commit contracts, and ADR-0014 ownership remain gated by Verification. | pass | `TestTaskCycleTransportAnomalyStillLetsVerificationGateSettleFailure` passed and proves an anomaly does not bypass failed Verification; `TestRunSettleVerificationFailureLeavesTaskAndTreeUntouched` passed through the `TestRunSettle` group. |
| 14 | Non-goal: no automatic settle; recovery remains explicit. | pass | The only recovery surface exercised was the explicit built command `roundfix settle --spec qa-smoke --task task_01`; `settle --help` describes one local recovery command, and focused tests assert preflight and no Run creation. |
| 15 | Non-goal: Roundfix did not attempt to fix acpx upstream itself. | pass | Task 03 Result records `Finding: upstream-only`; docs and findings describe known constraints and the upstream issue draft rather than local upstream changes or invented flags. |
| 16 | Task 01 AC: matrix tests cover all named acpx exit/result cases. | pass | `rtk go test ./internal/agent/ -run TestACPXPromptExitClassificationMatrix -v` exited 0 with 9 passing tests. |
| 17 | Task 01 AC: engine tests prove anomaly journaling with stderr tail and verification still gates settlement. | pass | Focused daemon command passed 3 tests, including `TestResolveCycleJournalsTransportAnomalyBeforeVerification` and `TestTaskCycleTransportAnomalyStillLetsVerificationGateSettleFailure`; source assertions include `Message buffer exceeded 10485760 bytes`. |
| 18 | Task 01 AC: the 0003/0005 incident shape now commits after passing Verification. | pass | `TestTaskCycleCommitsAfterTransportAnomalyAndPassingVerification` passed; the acpx matrix fixture includes the buffer-error JSON line followed by exit 1. |
| 19 | Task 01 AC: full suite passes. | pass | `rtk make verify` exited 0 and ran the full Go suite. |
| 20 | Task 02 AC: incident-shaped failed Task settles with Daemon-shaped commit and staged content. | pass | `rtk go test ./internal/cli/ -run TestRunSettle -v` passed; scratch CLI run created commit `49e4861`, set task status to completed, and committed `done.txt` plus the task file. |
| 21 | Task 02 AC: every Preflight refusal exits 2 and writes nothing. | pass | `TestRunSettlePreflightRefusalsWriteNothing` and `TestRunSettleRequiresSpecAndTask` passed in the focused `TestRunSettle` group. |
| 22 | Task 02 AC: Verification failure leaves task and tree untouched and exits 1. | pass | `TestRunSettleVerificationFailureLeavesTaskAndTreeUntouched` passed in the focused `TestRunSettle` group. |
| 23 | Task 02 AC: Active Run on the same working tree blocks settle. | pass | `TestRunSettleActiveRunOnSameWorkingTreeBlocks` passed in the focused `TestRunSettle` group. |
| 24 | Task 02 AC: `settle --help` documents flags, exit codes, and stage-everything contract. | pass | Built binary `rtk ./bin/roundfix settle --help` output matched the required command shape and contract; `TestRunSettleHelpDocumentsContract` passed. |
| 25 | Task 03 AC: Result names the exact buffer constant location and override finding. | pass | Local source search excluding source maps found `MAX_MESSAGE_BUFFER_SIZE = 10 * 1024 * 1024` and the `buffer.length > 10485760` failure path in the installed `acpx@0.12.0` bundle; config/help checks found no override. |
| 26 | Task 03 AC: upstream path has a complete issue draft and docs guidance. | pass | `task_03.md` Result includes the upstream issue title, body, reproduction, environment, evidence, hypothesis, and request; README and both skill copies carry the known acpx 10 MiB constraint and recovery guidance. |
| 27 | Task 03 AC: findings item 1 carries the dated outcome. | pass | `rtk rg -n '2026-07-05|upstream-only|run_20260705T152742Z_854a4b853b910140' docs/_inbox/dogfood-findings-2.md` found the dated upstream-only findings update. |
| 28 | Task 04 AC: canonical skill text matches shipped behavior and drift check passes. | pass | `rtk ./bin/roundfix skills check` exited 0 with `Roundfix skill check passed: roundfix`; `rtk diff .agents/skills/roundfix/SKILL.md skills/roundfix/SKILL.md` reported `[ok] Files are identical`. |
| 29 | Task 04 AC: documented settle stdout lines appear in CLI tests and skill copies. | pass | `rtk rg -n 'verify test -f done.txt — ok|task_01 stays failed — verification failed|settled task_01 completed —' internal/cli/settle_test.go .agents/skills/roundfix/SKILL.md skills/roundfix/SKILL.md` found the stdout examples in tests and both skill copies. |
| 30 | Task 04 AC: new terms are in the glossary or a gap is called out. | pass | `rtk rg -n 'Settle Command|Verification|Preflight Validation|Run Event Journal|Task Graph|Active Run|Batch|Daemon|Work Item' CONTEXT.md` found glossary entries for the terms used in the new docs. |

## Failures and blocked items

None.

## Environment

- Repository: `/Users/marcio/dev/roundfix`.
- Date: `2026-07-05`.
- Static gate: `rtk make verify`.
- Built CLI: `/Users/marcio/dev/roundfix/bin/roundfix`.
- Scratch settle repo: `/private/tmp/roundfix-qa-0006-settle-clean.7CDZxV`.
- Isolated settle home: `/private/tmp/roundfix-qa-0006-home-clean.Db8d1F`; only RTK's own history database appeared there, and no Roundfix Run Database or Run Event Journal files were created.
- Git status checks used `rtk git -c core.fsmonitor=false status --short` when the host fsmonitor daemon emitted `error: daemon terminated`; clean runs produced no status output.
- A first scratch fixture placed `HOME` inside the worktree and demonstrated that the stage-everything contract would include RTK history files under that worktree. That fixture was discarded as noisy evidence and replaced by the clean scratch repo listed above.
