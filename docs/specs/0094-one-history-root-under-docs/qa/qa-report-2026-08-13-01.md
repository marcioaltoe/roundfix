---
spec: 0094-one-history-root-under-docs
date: 2026-08-13
build: 9b51355151e728cdc833c455a1631837ab909650
status: closed
verdict: fail
rows_blocked_environment: 0
rows_blocked_finding: 0
rows_blocked_declared: 0
surfaces: [cli, docs]
---

# QA report — One history root under `docs/`

## Scope and environment

This same-day rerun targeted commit
`9b51355151e728cdc833c455a1631837ab909650` on macOS in the sandboxed Run
Worktree. The current source built successfully to
`/tmp/roundfix-0094-qa-rerun-20260813/roundfix` with a disposable Go cache
under `/tmp`.

The Task Graph names `task_09` as the unique terminal QA node. Every dependency
of `task_09` is `completed`; `task_09` remains Daemon-owned and pending. No Pull
Request is open, but Pull Request journeys were not reached because the mandatory
authoring precondition failed first.

The previous report at
`docs/specs/0094-one-history-root-under-docs/qa/qa-report-2026-08-13.md` tested
build `7ae7a2a3db1398486eac0da1526cb9b1610ab041` and returned `fail`. This rerun
started with its failed rows, but no result was carried forward: commit
`9b513551` changed the Baseline collision path after the previous report head
`82e20d65bc590995706e23bdb14bd0ffe246927c`.

## Static gate

The mandatory authoring precondition failed:

```text
$ /tmp/roundfix-0094-qa-rerun-20260813/roundfix spec check 0094-one-history-root-under-docs --strict
Spec 0094-one-history-root-under-docs
[error] SC-VOCABULARY-UNDOCUMENTED: internal/baseline/plan.go emits undocumented token "HistoryRelocation" absent from CONTEXT.md
  at internal/baseline/plan.go:625
  at CONTEXT.md:1
  fix: Document "HistoryRelocation" in CONTEXT.md or narrow the declared pattern.
exit 1
```

The JSON form reproduced the same single error and reported no skipped Spec
Consistency Check detector. The source build itself exited 0. `rtk make verify`
and `rtk make verify-docs` were not run: the QA contract requires stopping before
building the matrix when `roundfix spec check <slug> --strict` fails.

## Mechanical findings

None.

## Results

| # | Status | Provenance |
| - | --- | --- |

No QA matrix was built or executed. This is not a risk-based skip: the authored
gate stopped at its mandatory precondition, before any story row could be
materialized.

## Mechanical skips

| Detector | Missing artifact |
| --- | --- |
| authorization bounded paths | Task commits |
| consequent-fix commit order | consequent-fix declaration |

## Findings

### F-003 — Task 14 emits a domain token absent from the glossary

- Impact: Trust-Damage.
- Actor and journey: Agent forming a design recommendation, vocabulary close
  before the CLI and docs journeys.
- Expected: every token selected by the TechSpec Vocabulary Contract from
  `internal/baseline/plan.go` appears byte-for-byte in `CONTEXT.md`, allowing the
  strict Spec Consistency Check to pass before QA.
- Actual: Task 14 added `[]HistoryRelocation` and a `HistoryRelocation{...}`
  literal to `internal/baseline/plan.go`. `CONTEXT.md` documents the spaced domain
  heading `History Relocation` and the implementation tokens `HistoryMove` and
  `historyMoves`, but not the emitted token `HistoryRelocation`.
- Reproduction: build the current CLI, then run the strict command recorded in
  the Static gate section. The text and JSON forms both exit 1 with
  `SC-VOCABULARY-UNDOCUMENTED`.
- Root-cause evidence: `internal/speccheck/vocabulary.go` extracts each regex
  match from the declared emitter and requires `bytes.Contains` on the exact
  matched token in the documenting file. The diff from the previous report head
  changes `internal/baseline/plan.go` from iterating existing relocations to
  constructing `HistoryRelocation` collision candidates; `CONTEXT.md` and the
  Vocabulary Contract did not change in that range.
- Affected scope: the authoring precondition and the complete QA matrix. No
  product-flow result can be credited on this rerun.

## Blocked and skipped

No matrix row is environment-, finding-, or declaration-blocked, and no row is
skipped, because the matrix was not created. The strict authoring failure is a
gate-level finding rather than a typed blocked row. The next rerun must begin
after the vocabulary contract and its durable glossary owner agree on every
token emitted from `internal/baseline/plan.go`.

## Coverage

Executed: 0 user-story rows. The gate did not re-observe the previous report's
10 rows, its three failed flows, outside-fleet migration, Pull Request equivalent
evidence, Non-Goals, tooling chronology, or glossary close. Counts are 0 pass,
0 matrix fail, 0 environment-blocked, 0 finding-blocked, 0 declaration-blocked,
0 skipped, and 0 pending matrix rows. One gate-level Trust-Damage finding failed
the run before matrix construction.

The glossary lifecycle check therefore found a required correction; QA did not
edit `CONTEXT.md` because this daemon-assigned gate may write only its report and
evidence. No scratch repository, built binary, or unauthored evidence file was
added to the repository.

## Final verdict

Fail. The current build fails the mandatory strict authoring precondition with
`SC-VOCABULARY-UNDOCUMENTED`; make the TechSpec emitter pattern and `CONTEXT.md`
agree on `HistoryRelocation`, then rerun the complete gate from the previous
failed rows.
