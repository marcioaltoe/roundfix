---
spec: 0094-one-history-root-under-docs
date: 2026-08-13
build: 9e85a41e99dfb1f0ec7d1e3128ed69269e4f55fc
status: closed
verdict: fail
rows_blocked_environment: 1
rows_blocked_finding: 0
rows_blocked_declared: 0
surfaces: [cli, docs]
---

# QA Report — One history root under `docs/`

## Scope and environment

Full authored QA gate for the current Run Worktree at
`9e85a41e99dfb1f0ec7d1e3128ed69269e4f55fc`. The production CLI was rebuilt
from that commit with `go build -buildvcs=false -o bin/roundfix ./cmd/roundfix`.
The intended actors are a maintainer running the Archive and Baseline Commands,
an Agent reading active decisions, and a maintainer opening a Pull Request.
Scratch repositories are disposable local clones under `/tmp`; no source
checkout is a migration target. No Pull Request is open, so the live Review
Source journey is environment-blocked and must be backed by equivalent observed
repository evidence. The PRD contains no `Unreachable Acceptance` declaration.

The manifest names `task_09` as the unique `type: qa` Task. It is terminal and
depends on every non-QA leaf; every dependency is `completed`. The clean
authoring precondition passed:

- `rtk ./bin/roundfix spec check 0094-one-history-root-under-docs --strict` —
  exit 0, `No findings.`

Focused command evidence for every row and finding is recorded at
[`qa/evidence/2026-08-13-gate/current-command-evidence.md`](evidence/2026-08-13-gate/current-command-evidence.md).

## Static gate

- `rtk make verify` — exit 2. `TestCatalogCompatibility` and all four
  `TestBaselinePlanCharacterization` shapes found stale catalog digest
  `sha256:507ceef…`; the current embedded catalog produces
  `sha256:9773febe…`. Finding F-001.
- Focused reproduction — the catalog compatibility and clean-adoption
  characterization checks fail at current HEAD and pass in an exported tree at
  Task 16's parent. This classifies the failure as code-caused, not
  environment-caused.
- `rtk make verify-docs` — exit 2. The docs-contract package passed, then the
  repository-contract regeneration checks failed on the same stale catalog and
  characterization artifacts. Finding F-001.
- Focused lifecycle, liveness, history-move, collision, report, inventory-budget,
  ArchiveDir, Review Artifact root, CLI renderer, and review-configuration
  checks all exited 0. These narrower passes support their named rows only.

## Mechanical findings

None.

## Results

| # | Story / criterion / sweep | Actor and surface | Status | Evidence |
| - | --- | --- | --- | --- |
| QA-01 | Stories 1 and 3; Core Features 1–2: one five-family History Root, active decisions and intent kept apart from retired records | Maintainer and Agent · CLI/filesystem | pass | Public Baseline apply moved the superseded ADR and declined intent with identical hashes; proposed/open records remained active. ArchiveDir's five cases passed and a fresh read confirmed the paths. |
| QA-02 | Stories 2 and 6; Core Feature 6; outside evidence: migrate a pre-0085 fleet repository, preserve bytes, and rerun idempotently | Fleet maintainer · CLI | pass | Vortex `07826b2`, 413 files/13,600 KiB. Unconfirmed exit 3 left bytes unchanged; approved exit 0 moved all 413; before/after digest set `742aab…`; rerun exit 0/current/zero file changes. |
| QA-03 | Story 4; Core Features 3–5: finished orphan review retires, live/undecidable reviews stay live with reasons, Spec-owned reviews stay put, resolver never writes history | Maintainer · CLI/local Git | fail | File relocation and live/undecidable classification worked, but the finished review's empty live directories remained and the next run re-reported the residue as undecidable. F-002. |
| QA-04 | Core Feature 7 and collision UX: refuse an occupied destination, move its sibling, name both paths and reason, and remain not-current on rerun | Maintainer · CLI | pass | Public exit 3 named source, destination, `already exists`, and incomplete relocation; sibling moved with hash `bb02ca…`; both colliding hashes persisted; text/JSON reruns remained plan-ready with one move. |
| QA-05 | Core Feature 8: text and JSON planning report identity-only relocations; apply reports performed moves; current rerun reports none | Maintainer/automation · CLI | pass | Text plan/apply named moves and identities; JSON `historyMoves` contained only ordinal/from/to/contentIdentity; approved apply reported verified moves; the outside-fleet rerun had no move block. |
| QA-06 | Story 5; Core Feature 9: review excludes `docs/history/**` and active-Spec review directories and loads neither as project rules | Pull Request maintainer · Review Source/docs contract | blocked (environment: no open Pull Request) | Prompt proves no PR is open. Equivalent observed evidence: shipped-config positive checks and negative controls for both exclusions/rule-source trees all passed. Unblock by opening the Spec target-branch PR and observing its Review Source read-only. |
| QA-07 | Core Feature 10 and docs sweep: Archive help, active guides, canonical Skills, catalog modules, and embedded Source Baseline all name the resolved location | Maintainer/reader · CLI/docs | fail | Help and named canonical carriers say `docs/history/specs/`, and skill sync/contracts pass; derived catalog and plan-characterization carriers are stale after Task 16. F-001. |
| QA-08 | Non-Goals: Archive eligibility and Spec-owned review location are unchanged; migrations are byte-preserving; no provider, credential, or network dependency was added | Maintainer · CLI/docs | pass | A completed probe archived through the public command; Spec-owned review stayed put; hashes matched across migrations; liveness used only local `git` and no provider/HTTP call. |
| QA-09 | Current-repository success metrics: root `_archived/` is absent, `docs/history/` owns all retired families, and no retired ADR or declined intent remains active | Maintainer/Agent · filesystem/CLI | pass | Fresh reads found no root archive, no retiring ADR/declined intent in active trees, and no outstanding History Relocation. The Baseline still plans three managed-carrier changes due F-001, but no layout move. |
| QA-10 | Commit-dependent tooling authority: authorization precedes each bounded tooling change, consequent fixes follow their causes, and actual commit paths stay in scope | Maintainer · Git | fail | Earlier authorization/fix edges passed. Task 16 commit `65c51eb` predates its authorization/graph commit `e300c73`, created its own Task with protected edits, changed a manifest omitted from its Task scope, and left sanctioned derived artifacts stale. F-001, F-003. |

## Detailed evidence

### QA-01

```yaml
inputs:
  - kind: repository_path
    ref: internal/spec/**
  - kind: repository_path
    ref: internal/baseline/**
```

Entry point: built `roundfix baseline update` against a disposable repository.
Expected: retired ADR/backlog records move under `docs/history/`; proposed/open
records stay active; a fresh rerun confirms persistence.

### QA-02

```yaml
inputs:
  - kind: external_repository
    ref: /Users/marcio/dev/vortex
  - kind: repository_path
    ref: internal/baseline/**
```

Entry point: built `roundfix baseline update` against a `git clone --no-local`
copy. Expected: every nested archived file moves with identical content,
unconfirmed planning does not mutate, approved apply verifies, and a fresh rerun
reports current with zero History Relocations.

### QA-03

```yaml
inputs:
  - kind: repository_path
    ref: internal/spec/**
  - kind: repository_path
    ref: internal/config/**
  - kind: repository_path
    ref: internal/baseline/**
```

Entry point: built Baseline Command against disposable local Git histories.
Expected: finished moves; live and undecidable stay with distinct reasons;
Spec-owned reviews stay with their Spec; no result uses provider state.

### QA-04

```yaml
inputs:
  - kind: repository_path
    ref: internal/baseline/**
  - kind: repository_path
    ref: internal/cli/**
```

Entry point: public Baseline Command against a disposable dual-layout tree.
Expected: nonzero refusal, actionable paths/reason, sibling completion, preserved
colliding bytes, and a fresh rerun that remains not-current.

### QA-05

```yaml
inputs:
  - kind: repository_path
    ref: internal/baseline/**
  - kind: repository_path
    ref: internal/cli/**
```

Entry point: text and JSON forms of the built Baseline Command. Expected: stable
ordered `historyMoves` carry identities and no content; apply names verified
moves; fresh current output contains no relocation block.

### QA-06

```yaml
inputs:
  - kind: live_service
    ref: GitHub Pull Request Review Source
  - kind: repository_path
    ref: .coderabbit.yaml
  - kind: repository_path
    ref: internal/docscontract/publicdocs_test.go
```

Entry point: Open Pull Request review. Environment fact: no Pull Request is open.
Equivalent evidence must exercise the shipped review configuration through the
canonical docs-contract suite, including negative controls.

### QA-07

```yaml
inputs:
  - kind: repository_path
    ref: internal/cli/archive.go
  - kind: repository_path
    ref: docs/agents/**
  - kind: repository_path
    ref: .agents/skills/**
  - kind: repository_path
    ref: internal/baseline/assets/**
```

Entry point: `roundfix archive --help`, followed as a reader through every
authorized carrier. Expected: the built-in root always reads
`docs/history/specs/`; sanctioned derived-carrier checks pass.

### QA-08

```yaml
inputs:
  - kind: repository_path
    ref: internal/spec/**
  - kind: repository_path
    ref: internal/config/**
  - kind: repository_path
    ref: internal/baseline/**
```

Entry point: Archive and Baseline Commands plus adjacent public canaries.
Expected: eligibility and Spec-owned paths are unchanged, content identities
survive, and local-only liveness works without a provider integration.

### QA-09

```yaml
inputs:
  - kind: repository_path
    ref: docs/history/**
  - kind: repository_path
    ref: docs/adr/**
  - kind: repository_path
    ref: docs/backlog/**
```

Entry point: repository filesystem and unconfirmed Baseline update. Expected:
all five History Root families resolve under `docs/history/`, no root archive
exists, and the current tree needs no outstanding History Relocation.

### QA-10

```yaml
inputs:
  - kind: repository_path
    ref: docs/workflow/authorizations/2026-08-12-the-archive-root-under-docs.md
  - kind: repository_path
    ref: docs/specs/0094-one-history-root-under-docs/task_*.md
```

Entry point: repository Git history. Expected: exact `git diff-tree` path sets
match authorization, all grants precede their tooling changes, sanctioned
regeneration is reproducible, and consequent-fix commits follow the causal edit.

## Findings

### F-001 — Task 16 left Baseline-derived artifacts stale

- Impact: `Blocks-Completion`.
- Actor and step: maintainer runs the repository's required Verification after
  the Source Baseline routing-carrier correction.
- Expected: the sanctioned regeneration contract and all catalog/plan
  characterizations match the embedded catalog.
- Actual: both required gates exit 2. The embedded catalog resolves to
  `sha256:9773febe…`, while `catalog.digest` and four characterization goldens
  retain `sha256:507ceef…`.
- Reproduction: `rtk make verify`; `rtk make verify-docs`; focused
  `TestCatalogCompatibility|TestBaselinePlanCharacterization/clean-adoption`.
  The focused check passes at `65c51eb^` and fails after `65c51eb`.
- Root cause: Task 16 regenerated the Source Baseline-local artifacts but did
  not run the complete sanctioned `make baseline-digests` artifact set after
  changing an embedded catalog input.
- Evidence: current command evidence, “Preconditions and static gates.”
- Affected rows: QA-07, QA-10.

### F-002 — A retired Review Artifact leaves a live empty shell

- Impact: `Trust-Damage`.
- Actor and step: maintainer approves Baseline migration of a finished orphan
  Review Artifact, then reruns the Baseline.
- Expected: the finished review leaves `docs/specs/reviews/` and lives only
  under `docs/history/reviews/`; the next run does not reconsider it.
- Actual: `round.md` moves and verifies, but empty review/round directories stay
  live. The next run emits `baseline.history.review.undecidable` for that same
  review because its moved metadata is missing.
- Reproduction: create a locally merged review under
  `docs/specs/reviews/qa-finished/round-001/`, run approved Baseline update,
  inspect both roots, and rerun unconfirmed.
- Root cause: `relocateHistoryMove` renames files and syncs their parents but
  never removes emptied source directories; discovery later treats the residue
  as a Review Artifact.
- Evidence: current command evidence, “Lifecycle and Review Artifact flow.”
- Affected row: QA-03.

### F-003 — Task 16's protected tooling authorization landed after the change

- Impact: `Blocks-Completion`.
- Actor and step: maintainer audits the protected Source Baseline tooling commit
  before accepting the Spec.
- Expected: the authorization and bounded Task exist in ancestry before the
  tooling change, and the commit changes only the Task's bounded paths plus its
  Task file.
- Actual: at `65c51eb^`, Task 16 and its routing-carrier/derived-path grant do
  not exist. Commit `65c51eb` creates the Task and edits protected paths; later
  commit `e300c73` adds the authorization and Task Graph node. The tooling
  commit also changes the Source Baseline manifest omitted from Task 16's
  current bounded list.
- Reproduction: inspect both commits with `git diff-tree`; read the authorization
  at `65c51eb^`; run both `git merge-base --is-ancestor` directions.
- Evidence: current command evidence, “Tooling chronology and scope.”
- Affected row: QA-10.

## Blocked and skipped

QA-06 is `blocked (environment: no open Pull Request)`. The Run prompt proves
the absence and forbids resolving a PR from the unpushed Run Worktree branch.
Equivalent evidence is complete: the canonical docs-contract suite loaded the
shipped `.coderabbit.yaml`, passed both protected-path cases, and its negative
controls failed configurations with either exclusion removed or with rule
sources reaching history. Unblock the live journey by opening the Pull Request
on `ma/0094-one-history-root-under-docs` and observing the Review Source
read-only. No row is finding-blocked, declared-blocked, or skipped.

## Coverage

All 6/6 user stories and 10/10 Core Features have terminal coverage. Stories 1,
2, 3, and 6 passed; Story 4 failed on F-002; Story 5 is environment-blocked with
equivalent observed evidence. Of ten rows: 6 pass, 3 fail, 1 environment-blocked,
0 finding-blocked, 0 declared-blocked, 0 skipped, and 0 pending.

The run exercised unconfirmed-before-approved apply, rerun/deep reopen, live
versus undecidable local-Git state, a destination collision with an unaffected
sibling, text/JSON parity, the inventory-budget boundary, and removal/injection
negative controls for review configuration. Non-Goals were checked through the
Archive Command, Spec-owned Review Artifact path, content hashes, and local-only
Git liveness. The glossary check found `History Root`, `History Relocation`,
`HistoryRelocation`, `HistoryMove`, and `historyMoves` already current, so no
domain-context edit was needed.

## Final verdict

`fail` — repair F-001's complete sanctioned regeneration, F-002's empty live
Review Artifact residue, and F-003's tooling-authority chronology/scope before
rerunning the gate. Counts: 6 pass, 3 fail, 1 environment-blocked; impact tiers:
2 `Blocks-Completion`, 1 `Trust-Damage`.

## Mechanical skips

The seeded mechanical skips are preserved verbatim below. QA-10 supplies the
commit-dependent authorization and chronology audit that the seed did not
materialize; the final strict Spec check supplies the report/evidence checks.

| Detector | Missing artifact |
| --- | --- |
| authorization bounded paths | Task commits |
| consequent-fix commit order | consequent-fix declaration |
| QA Report structure | QA Report |
| QA evidence paths | QA Report |
