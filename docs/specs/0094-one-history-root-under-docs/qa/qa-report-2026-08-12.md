---
spec: 0094-one-history-root-under-docs
date: 2026-08-12
build: 89864eb7d86d228fde22269c1065462dab2ea2d9
status: closed
verdict: fail
rows_blocked_environment: 0
rows_blocked_finding: 0
rows_blocked_declared: 0
surfaces: [cli, docs]
---

# QA report — One history root under `docs/`

## Scope and environment

This was the full authored `task_09` gate for the CLI and documentation surfaces.
The graph names `task_09` as its terminal QA node, and all four direct dependencies
(`task_06`, `task_07`, `task_08`, and `task_10`) are `completed`. The Run Worktree
was on `roundfix/run-run_20260812T221443Z_322c8389b38029cc` at
`89864eb7d86d228fde22269c1065462dab2ea2d9`.

The installed `roundfix 0.4.0` binary was stale at commit `366e664`, so it was not
used as acceptance evidence. `rtk make build` produced `bin/roundfix` from the Run
Worktree head as `roundfix 0.4.0 (89864eb-dirty, built 2026-08-12 19:30:07 -0300)`;
the dirty marker reflects the seeded untracked QA report. The fresh binary is an
ignored build artifact and is not part of the report or commit scope.

No Pull Request is open. Pull Request journeys would have been environment-blocked,
but the authoring precondition stopped the gate before the QA matrix was built, so
no Pull Request row was materialized or counted.

## Static gate

- `rtk make build`: passed; built the current Run Worktree head into `bin/roundfix`.
- `rtk bin/roundfix version`: passed; reported commit `89864eb-dirty`.
- `rtk bin/roundfix spec check 0094-one-history-root-under-docs --strict`: failed
  with two `SC-VOCABULARY-UNDOCUMENTED` errors. `internal/baseline/plan.go` emits
  `HistoryMove` at line 144 and `historyMoves` at line 209, while `CONTEXT.md`
  documents neither token.
- `rtk make verify`: not run. The `qa-gate` authoring precondition requires the
  strict Spec check to pass before the matrix or repository verification gate is
  executed.

The exact unblocking action is to document the emitted `HistoryMove` and
`historyMoves` vocabulary in `CONTEXT.md` (or narrow the TechSpec's declared RE2
pattern without dropping a public emitted term), then rerun the gate from the
strict Spec check.

## Mechanical findings

None.

## Results

| # | Status | Provenance |
| - | --- | --- |

## Mechanical skips

| Detector | Missing artifact |
| --- | --- |
| consequent-fix commit order | consequent-fix declaration |
| QA Report structure | QA Report |
| QA evidence paths | QA Report |

## Findings

### F-001 — Emitted history-move vocabulary has no durable glossary definition

- Impact: Trust-Damage.
- Actor and journey: an Agent or maintainer reading the public Baseline Plan
  vocabulary before using `roundfix baseline plan`.
- Expected: every token declared by the TechSpec's Vocabulary Contract and emitted
  by `internal/baseline/plan.go` resolves to its durable owner in `CONTEXT.md`.
- Actual: the fresh strict Spec check reports `SC-VOCABULARY-UNDOCUMENTED` for
  `HistoryMove` and `historyMoves`.
- Reproduction: build the current binary with `rtk make build`, then run
  `rtk bin/roundfix spec check 0094-one-history-root-under-docs --strict`.
- Evidence: the Static gate section above records the command, build, locations,
  and diagnostics.
- Affected scope: the authoring precondition and therefore every planned user-story,
  acceptance, outside-evidence, Non-Goal, CLI, and documentation row.

## Blocked and skipped

No QA matrix row was marked blocked or skipped. The strict authoring check failed
before matrix construction, as required by the gate contract. Consequently, none
of the six user stories, the external pre-0085 repository migration, byte-identity
and idempotency checks, collision and rollback probes, review-exclusion journey,
release-readiness journey, or Non-Goal scope checks were executed.

The glossary closeout check did run and found that the Spec introduced the public
terms `HistoryMove` and `historyMoves` without documenting them. The QA gate did
not edit `CONTEXT.md`: gate execution writes only its report and evidence, and a
failed precondition must be repaired before a new run.

## Coverage

- User stories executed: 0/6.
- Acceptance rows executed: 0; the matrix was not built.
- Findings: 1 Trust-Damage; 0 Blocks-Completion, Data-Loss, Friction, or Cosmetic.
- `rows_blocked_environment`: 0.
- `rows_blocked_finding`: 0.
- `rows_blocked_declared`: 0.
- Frontend/TUI sweeps: not applicable; the PRD declares only CLI and docs surfaces.
- Behavior probes and Non-Goal checks: not run because the strict authoring
  precondition failed.

## Final verdict

Fail: document the emitted history-move vocabulary in its durable glossary owner,
obtain a clean strict Spec check, and rerun the complete QA gate before Pull
Request preparation.
