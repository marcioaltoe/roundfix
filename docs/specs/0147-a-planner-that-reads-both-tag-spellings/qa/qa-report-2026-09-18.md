---
spec: 0147-a-planner-that-reads-both-tag-spellings
date: 2026-09-18
build: 43efe008
auditing_binary: "0.14.1 (19cfd251, built 2026-09-18 19:24:29 -0300)"
auditor_staleness: "stale: commit ancestry: build commit predates audited tree"
status: closed
verdict: pass
rows_blocked_environment: 1
rows_blocked_finding: 0
rows_blocked_declared: 0
surfaces: [backend, cli, docs]
---

# QA Report — A planner that reads both tag spellings

## Scope and environment

- Full authored QA gate for commit `43efe008` in the Run Worktree.
- Actor: repository maintainer using the built `roundfix` CLI and the shipped
  Roundfix skill.
- Strict authoring precondition: `roundfix spec check
  0147-a-planner-that-reads-both-tag-spellings --strict` exited 0 with no
  findings; its declared missing-artifact detectors remained skipped.
- Pull Request fact: none open; Pull Request journeys are environment-blocked
  and use equivalent evidence where all required controls can be observed.
- Repository Verification is Daemon-owned and is recorded from the supplied
  gate fact; it is not rerun by this Agent.
- Built user binary: `/tmp/roundfix-0147-qa-bin` from commit `43efe008` with
  `GOCACHE=/tmp/roundfix-0147-go-cache go build -o
  /tmp/roundfix-0147-qa-bin ./cmd/roundfix` (exit 0).
- Scratch Git commits used local `commit.gpgsign=false` because the sandbox
  denied the global GPG keyring. Signing is unrelated to the read-only planner;
  tags, commits, repository cleanliness, and CLI entry points remained real.
- Reset-plan GitHub inventory used a deterministic read-only `gh api` fixture;
  the built public CLI, real local/remote Git refs, text/JSON rendering, and
  repository mutation boundary were exercised without network credentials.

## Static gate

- `make verify`: pass (exit 0), run by the Daemon outside the Agent sandbox;
  diagnostics were removed on success. Per the QA contract, the Agent did not
  rerun it.

## Performed repairs

None.

## Assigned repair failures

None.

## Authorization audit inputs

| Task | Outcome | Record | Revision | Detail |
| --- | --- | --- | --- | --- |
| task_04 | granted | docs/specs/0147-a-planner-that-reads-both-tag-spellings/_authorization.md | 19cfd251737f6b3d6853090aadee4a077e281fd6 | operative grant |

## Mechanical findings

None.

## Results

| # | Story / criterion / sweep | Actor and surface | Status | Evidence | Provenance |
| - | --- | --- | --- | --- | --- |
| R01 | Repository Verification | Daemon, repository gate | pass | Supplied `make verify` fact: exit 0 | Requirement 1; repository Verification |
| R02 | Both stable spellings parse; malformed, prerelease, and build metadata retain their rejections | Maintainer, CLI/backend | pass | Five built-CLI rejection probes plus 26 focused parser assertions passed | Requirement 2 |
| R03 | Selection spans spellings; an ambiguous highest version refuses with both refs and `--from`, without a proposal | Maintainer, CLI | pass | Mixed fixture selected `1.3.0`; ambiguous fixture exited 2 naming `2.0.0`, `v2.0.0`, and `--from`; exact selector restored a plan | Requirement 3 |
| R04 | Bare history yields a bare base/proposal; prefixed history retains the established output | Maintainer, CLI | pass | Bare `1.4.2` proposed `1.5.0`; prefixed `v1.4.2` proposed `v1.5.0`; fresh repeats were identical | Requirement 4 |
| R05 | Reset inventory and digest retain spelling, ref, and commit | Maintainer, CLI/backend | pass | Built text/JSON reset plans retained six ref identities and distinct exact-tag Release commits under one digest; 10 focused assertions passed | Requirement 5 |
| R06 | Canonical and mirrored shipped skills state both spellings, ambiguity refusal, selector, and spelling preservation, and remain identical | Skill reader, docs | pass | Release Plan guidance followed successfully; both copies share SHA-256 `81b58304…e149b0` | Requirement 6 |
| R07 | This repository's real prefixed tag history produces an unchanged plan | Maintainer, CLI/outside evidence | pass | Real tag inventory is wholly `v`-prefixed; old and new binaries both exited 3 with byte-identical plan output from base `v0.14.1` | Requirement 7; outside-evidence row |
| R08 | Spec promise mapping and Task-commit authority are valid | Maintainer, governance | pass | Strict check passed; Task commit path audit and authorization ancestry passed | Requirement 8 |
| R09 | Pull Request controls on the target branch | Maintainer, Pull Request | blocked (environment: no open Pull Request) | Prompt-established absence plus complete equivalent-control record below | Pull Request row |

## Detailed evidence

### R01

```yaml
inputs:
  - kind: repository_path
    ref: "**"
```

Entry point: Daemon repository Verification. Expected observable: `make verify`
exits 0. Independent confirmation and persistence: the Daemon's recorded gate
fact is tied to this assembled tree. Evidence path: this report's Static gate.

Observed: the supplied Daemon fact records `make verify` exit 0 and no retained
diagnostics. This is the required static-gate result and was not re-derived.

### R02

```yaml
inputs:
  - kind: repository_path
    ref: internal/releaseplan/version.go
  - kind: repository_path
    ref: internal/releaseplan/releaseplan_test.go
```

Entry point: built CLI plus focused parser contract. Expected observable: both
stable spellings are accepted while the named invalid shapes retain their
typed messages. Independent confirmation: repeat through the parser's public
package contract. Persistence: rebuild from the same commit. Evidence path:
this report's row result.

Observed:

- Built CLI reset-target probes rejected `1.2.3-rc.1`, `v1.2.3+build`, `1.2`,
  `01.2.3`, and `version-1.2.3`, each with exit 2 and the established
  prerelease or malformed-stable-version message.
- `GOCACHE=/tmp/roundfix-0147-go-cache go test -count=1 -run
  '^(TestParseStableVersion|TestStableVersionAcceptsBothSpellings|TestStableVersionPreservesRejectionMessages)$'
  ./internal/releaseplan` passed 26 assertions.

### R03

```yaml
inputs:
  - kind: repository_path
    ref: internal/cli/releaseplan_git_source.go
  - kind: repository_path
    ref: internal/releaseplan/version.go
  - kind: repository_path
    ref: internal/releaseplan/errors.go
```

Entry point: `roundfix release plan` in scratch Git repositories. Expected
observable: semantic selection crosses spellings and equal-highest aliases
refuse in preflight with both refs and `--from`, without a proposal.
Independent confirmation: rerun from a fresh process. Persistence: tags remain
after repository reopen. Evidence path: this report's row result.

Observed:

- In a repository with `v1.2.3` and the later `1.3.0`, the built command exited
  3 and selected base `1.3.0`, proposing `1.4.0`.
- In a repository with `2.0.0` and `v2.0.0` at the highest version, the built
  command exited 2, emitted no plan, and named both refs plus `--from`.
- Rerunning with `--from v2.0.0` exited 0 and proposed `v2.0.1`, independently
  confirming the named recovery path.
- `go test -count=1 -run '^TestSelectionSpansSpellings$'
  ./internal/releaseplan` passed all 4 focused assertions.

### R04

```yaml
inputs:
  - kind: repository_path
    ref: internal/cli/releaseplan_git_source.go
  - kind: repository_path
    ref: internal/releaseplan/version.go
```

Entry point: `roundfix release plan` in bare-only and prefixed-only scratch Git
repositories. Expected observable: base and proposal preserve the selected tag
spelling. Independent confirmation: a second process reads the same repository.
Persistence: repository tags survive reopen. Evidence path: this report's row
result.

Observed: the bare fixture exited 3 with base/version `1.4.2`, proposal
`1.5.0`, and question `Approve the minor increment to 1.5.0?`; the prefixed
fixture exited 3 with base/version `v1.4.2`, proposal `v1.5.0`, and the matching
prefixed question. Two fresh process reads of each repository were byte-identical,
and `git status --short` remained empty.

### R05

```yaml
inputs:
  - kind: repository_path
    ref: internal/cli/releaseplan_command_test.go
  - kind: repository_path
    ref: internal/releaseplan/releaseplan_test.go
```

Entry point: public reset-plan command seam and focused digest contract.
Expected observable: aliases remain separate, exact tag names select Releases,
and spelling/ref/commit mutations change the digest. Independent confirmation:
text and JSON inventories agree. Persistence: deterministic rerun. Evidence
path: this report's row result.

Observed:

- The built public CLI ran `release plan --reset-to 0.0.1` in both text and
  JSON modes against real local and remote refs plus a deterministic read-only
  GitHub Release response. Both exited 3, emitted digest
  `sha256:28f533ac2fb008d4e45cc3d5894db59bfcc1bafbbfd56c61ffdbee5384b4e166`,
  and left the repository clean.
- The inventory retained local and remote entries for `0.2.0`, `v0.1.0`, and
  `v0.2.0`, each with spelling, ref, immutable identity, and target commit.
  Releases `0.2.0` and `v0.2.0` matched different exact-tag commits.
- `go test -count=1 -run
  '^(TestBuildResetPlanSortsCompleteInventoryAndCalculatesDeterministicDigest|TestBuildResetPlanDigestChangesWithEveryBoundInput)$'
  ./internal/releaseplan` passed 9 assertions; the public command-boundary
  `TestReleasePlanResetTextAndJSONInventoryMatchThroughRunBoundary` passed.

### R06

```yaml
inputs:
  - kind: repository_path
    ref: .agents/skills/roundfix/SKILL.md
  - kind: repository_path
    ref: skills/roundfix/SKILL.md
```

Entry point: shipped Release Plan documentation. Expected observable: a reader
can identify both accepted spellings, the ambiguity refusal, `--from`, and
proposal spelling preservation; both copies are byte-identical. Independent
confirmation: digest comparison. Persistence: fresh reread. Evidence path:
this report's row result.

Observed: following the Release Plan section yielded both accepted forms,
`MAJOR.MINOR.PATCH` and `vMAJOR.MINOR.PATCH`, the ambiguous-highest refusal,
the `--from` recovery selector, and proposal spelling preservation. `cmp` exited
0, and `shasum -a 256` returned
`81b583042df8e061c0b2b1160bf99abecfea6427976f94811636aa2887e149b0` for
both files.

### R07

```yaml
inputs:
  - kind: external_repository
    ref: current Roundfix repository Git tag history
```

Entry point: built CLI in this repository. Expected observable: its real,
prefixed stable tags produce the established plan/state/exit behavior.
Independent confirmation: direct Git tag inventory. Persistence: repeat in a
fresh process. Evidence path: this report's row result.

Observed outside evidence: a clean local clone of the current Roundfix
repository exposed 17 reachable stable tags, from `v0.0.1` through `v0.14.1`,
all prefixed. The installed pre-change auditing binary and the newly built
binary each ran `release plan --format json` against commit `43efe008`, exited
3, selected `v0.14.1`, reported `manual_classification_required`, and produced
byte-identical JSON. This evidence originates in the repository's real Git tag
history and the prior binary, not in this Spec's fixtures or assertions.

### R08

```yaml
inputs:
  - kind: repository_path
    ref: docs/specs/0147-a-planner-that-reads-both-tag-spellings/**
  - kind: repository_path
    ref: .agents/skills/roundfix/SKILL.md
  - kind: repository_path
    ref: skills/roundfix/SKILL.md
```

Entry point: strict Spec check and Git commit/path audit. Expected observable:
the promise rules pass and Task 04 changes only its assigned Task file and the
two exactly authorized governed paths, with authorization in prior ancestry.
Independent confirmation: `git diff-tree` of each Task commit and authorization
revision ancestry. Persistence: commit-addressed evidence. Evidence path: this
report's authorization audit and row result.

Observed:

- `roundfix spec check 0147-a-planner-that-reads-both-tag-spellings --strict`
  exited 0 with `No findings`; authored Verification commands were not run.
- `git show --name-only` resolved Task commits `ec8c7475`, `ccf0c30d`,
  `87931f60`, and `43efe008`. Only Task 04 changed governed paths, and its
  commit contains exactly `.agents/skills/roundfix/SKILL.md`,
  `skills/roundfix/SKILL.md`, and its assigned `task_04.md`.
- `git merge-base --is-ancestor 19cfd251 43efe008` exited 0. The operative
  authorization at `19cfd251` names this Spec, grants `implement`, and bounds
  exactly the two skill paths. No derived pin changed and no consequent-fix
  declaration exists.

### R09

```yaml
inputs:
  - kind: live_service
    ref: Pull Request state for feat/0147-tag-spellings
```

Entry point: Pull Request controls. Expected observable: approval, checks and
status, unresolved review threads, Merge-Ready acceptance, and review-artifact
ancestry are each answered. Independent confirmation: the prompt's authoritative
Pull Request fact and local head evidence. Persistence: not applicable to live
service state. Evidence path: this report's blocked/equivalent-evidence record.

Observed equivalent controls:

- Approval: no acceptance of the implementation has been obtained; only the
  prior bounded tooling authorization exists.
- Checks and status: Daemon-run `make verify` passed with exit 0.
- Unresolved review threads: none stands because the prompt establishes that
  no Pull Request is open.
- Merge-Ready acceptance: none exists yet.
- Review-artifact ancestry: none exists yet; the audited Run Worktree head is
  `43efe008`, while target branch `feat/0147-tag-spellings` is at `19cfd251`.

## Findings

None.

## Blocked and skipped

- R09 is `blocked (environment: no open Pull Request)`, as established by the
  Roundfix QA prompt. Unblocking action: after the Daemon lands the Spec commits
  on `feat/0147-tag-spellings` and an authorized workflow opens a Pull Request,
  observe its approval, checks, threads, Merge-Ready evidence, and evidence
  ancestry read-only. All five controls have equivalent observations above, so
  this environment block does not cap the verdict.
- The first fixture-commit attempt was denied by the sandboxed global GPG
  keyring. Fresh scratch repositories with local signing disabled completed the
  same Git setup; this does not block a row or alter planner behavior.

## Coverage

- 9/9 rows reached terminal status: 8 pass, 1 environment-blocked with complete
  equivalent evidence, 0 fail, 0 finding-blocked, 0 declared-blocked, 0 skipped.
- Requirements 1–8: 8/8 covered. Non-waivable sources: outside evidence 1/1,
  Pull Request 1/1, repository Verification 1/1.
- No PRD Unreachable Acceptance declaration or frontend sweep applies.
- `rows_blocked_environment: 1`; `rows_blocked_finding: 0`;
  `rows_blocked_declared: 0`.

## Final verdict

Pass: every runnable criterion passed, the sole environment-blocked Pull
Request row records its cause and complete equivalent controls, and there are
no findings, declared blocks, finding blocks, or skips. Impact findings:
Blocks-Completion 0, Data-Loss 0, Trust-Damage 0, Friction 0, Cosmetic 0.

## Mechanical skips

| Detector | Missing artifact |
| --- | --- |
| consequent-fix commit order | consequent-fix declaration |
| QA Report structure | QA Report |
| QA evidence paths | QA Report |
