---
spec: 0171-a-deterministic-suite-under-load
date: 2026-09-25
build: 0b6ccab408a9c12a27f85f85d09b54f482783dd1
auditing_binary: "0.17.0 (256ad156, built 2026-09-25 15:19:40 -0300)"
auditor_staleness: "stale: commit ancestry: build commit predates audited tree"
status: closed
verdict: pass
rows_blocked_environment: 2
rows_blocked_finding: 0
rows_blocked_declared: 0
surfaces: [backend]
---

# QA report — A deterministic suite under load

## Scope and environment

This full rerun exercises the assembled backend test-suite behavior at commit
`0b6ccab408a9c12a27f85f85d09b54f482783dd1`. The actor is a Roundfix maintainer
running Go package tests and repository checks from the Run Worktree on macOS.
The Spec declares no frontend surface and no Unreachable Acceptance items.

The authored terminal node is `task_06`; it depends on completed Tasks 01-05,
corrective Task 07, and corrective Task 08. `roundfix spec check
0171-a-deterministic-suite-under-load --strict` passed with exit 0 before this
matrix opened. It reported no findings and did not execute authored
Verification commands. It skipped `SC-ROLLUP-MEMBER` because there is no
findings rollup and skipped the vocabulary detector because the TechSpec has
no Vocabulary Contract; Requirement 8 remains in row 9.

The mechanical audit passed without repairs or findings. Its `0.17.0` binary
is stale because its build commit predates the audited tree; that staleness is
preserved above instead of being presented as current evidence.

## Static gate

The Daemon already ran `make verify-changed` outside the Agent sandbox. It
reported exit 0 and removed diagnostics on success. Per the QA assignment, row
1 records that supervised fact and this Agent does not rerun repository
Verification.

## Performed repairs

None.

## Assigned repair failures

None.

## Authorization audit inputs

None.

## Mechanical findings

None.

## Results

| # | Story / criterion / sweep | Actor and surface | Status | Provenance |
| - | --- | --- | --- | --- |
| 1 | Repository Verification | Maintainer, backend static gate | pass | Requirement 1; repository Verification |
| 2 | Deadline-bound waits report ended work and prefer ready values | Maintainer, `go test` | pass | Requirement 2 |
| 3 | Wait and bootstrap sources contain no forbidden fixed durations | Maintainer, source-contract commands | pass | Requirement 3 |
| 4 | Four historical failures pass under stress beside the suite | Maintainer, concurrent `go test` | pass | Requirement 3 |
| 5 | Operational commands use the offline release lookup | Maintainer, `go test` | pass | Requirement 4 |
| 6 | Compiled adapter fixture runs through a relative binary path | Maintainer, compiled Go test binary | pass | Requirement 5 |
| 7 | Go deadline semantics and the four historical failures resolve from outside sources | Maintainer, Go documentation and prior Run reports | blocked (environment: fourth originating report unavailable) | Requirement 6; outside-evidence row |
| 8 | Git scope contains only the bounded corrective production files and no protected-file change | Maintainer, Git history | pass | Requirement 7 |
| 9 | Spec promises name consuming Tasks and no new vocabulary is emitted | Maintainer, strict Spec checker and focused commands | pass | Requirement 8 |
| 10 | Pull Request controls have equivalent evidence | Reviewer, Pull Request surface | blocked (environment: no open Pull Request) | Pull Request row |

## Row evidence

### 1

```yaml
inputs:
  - kind: repository_path
    ref: "**"
```

- Entry point: Daemon-owned repository Verification.
- Expected observable: exit 0 with no retained diagnostics.
- Independent confirmation: the Daemon supplies the verdict and diagnostic disposition.
- Persistence check: not applicable to a supervised static-gate fact.
- Evidence: supervised Daemon result: `make verify-changed` exited 0 and its
  diagnostics were removed on success. The command was not rerun in this Agent
  sandbox.

### 2

```yaml
inputs:
  - kind: repository_path
    ref: internal/testwait/testwait.go
  - kind: repository_path
    ref: internal/testwait/testwait_test.go
```

- Entry point: focused `go test` selection in `./internal/testwait`.
- Expected observable: the deadline case reaches its bound, ended work fails promptly and names its result, and ready wins over simultaneous end.
- Independent confirmation: each named test appears as passing.
- Persistence check: rerun through a fresh package test process.
- Evidence: `GOCACHE=/private/tmp/roundfix-0171-qa-03-gocache go test
  -count=1 -v -run
  '^(TestBoundFollowsTheTestDeadline|TestUntilPrefersTheReadyValueOverAnEndedWork|TestUntilFailsAtOnceWhenTheWorkEnds|TestUntilFailsAtTheDeadlineAndNotBefore)$'
  ./internal/testwait` exited 0. All four named tests passed; the deadline case
  took 0.30 s and the ended-work and simultaneous-ready cases completed
  immediately.

### 3

```yaml
inputs:
  - kind: repository_path
    ref: internal/cli/implement_test.go
  - kind: repository_path
    ref: internal/daemon/task_engine_test.go
  - kind: repository_path
    ref: internal/worktree/worktree_test.go
```

- Entry point: executable source-contract searches.
- Expected observable: forbidden searches have no match and required `testwait` searches match.
- Independent confirmation: row 4 exercises the migrated paths.
- Persistence check: search all three current files in one run.
- Evidence: contextual `rg` searches across all three files found no
  `implementWaitBudget`, `detachStartupBudget`, `time.After`, `time.NewTimer`,
  literal 90-second wait, removed Daemon wait helper, or unexamined one- or
  five-second bootstrap timeout. They found `testwait.Until`, `testwait.Poll`,
  or `testwait.Bound` on every migrated wait. The only remaining fixed
  durations are the Spec-0124-owned `attachDetachBudget`, the empty-command
  nanosecond sentinel, and Task 08's one-second behavior-under-test timeout,
  which begins only after `testwait.Poll` observes the bootstrap-created start
  marker. Row 4 exercises the migrated waits and that post-start timeout under
  load.

### 4

```yaml
inputs:
  - kind: repository_path
    ref: internal/cli/implement_test.go
  - kind: repository_path
    ref: internal/daemon/task_engine_test.go
  - kind: repository_path
    ref: internal/worktree/worktree_test.go
  - kind: repository_path
    ref: internal/testwait/testwait.go
  - kind: repository_path
    ref: internal/worktree/adminlock*.go
  - kind: elapsed_time
    ref: count-10 stress under concurrent package load
```

- Entry point: the four named regression tests through `go test -count=10 -cpu 1,4` while other suite packages run beside them.
- Expected observable: every focused iteration passes and the adjacent package load completes.
- Independent confirmation: require all four names in verbose pass output and the source constraints from row 3.
- Persistence check: 20 scheduler configurations per named test.
- Probe: concurrent packages reproduce the load shape that exposed the earlier Git race.
- Evidence: `GOCACHE=/private/tmp/roundfix-0171-qa-03-stress-gocache go test
  -count=10 -cpu 1,4 -v -run
  '^(TestRunImplementQueuedCancellationStartsNoChildAndKeepsResumableTasks|TestRunImplementBootstrapsEachConcurrentTaskWorktreeBeforeAgentWork|TestTaskCycleVerificationCapacityCancellationWhileQueuedStartsNoCommandOrSettlement|TestBootstrapSerializesAcrossSiblings|TestRunBootstrapReturnsBootstrapErrorOnTimeout)$'
  ./internal/cli ./internal/daemon ./internal/worktree` exited 0 while a fresh
  `go test -count=1 ./...` ran beside it. Each of the four historical tests
  passed 20/20 across CPU settings 1 and 4. Task 08's adjacent corrective
  timeout test also passed 20/20. Focused package durations were 81.726 s,
  33.995 s, and 44.213 s.

  The adjacent suite supplied load throughout the focused run. It later exited
  1 only because two unrelated force-stop integration tests could not read the
  host process table (`operation not permitted`); all other reported packages
  passed. This sandbox restriction neither interrupted the focused process nor
  changes row 1's supervised current-build Verification pass.

### 5

```yaml
inputs:
  - kind: repository_path
    ref: internal/cli/version_freshness_isolation_test.go
  - kind: repository_path
    ref: internal/cli/implement_test.go
  - kind: repository_path
    ref: internal/daemon/run_disposition_characterization_test.go
```

- Entry point: focused `go test` commands in `./internal/cli` and `./internal/daemon`.
- Expected observable: offline tags are recorded, seeded caches remain byte-identical, and the per-test override still prints the warning.
- Independent confirmation: tests reopen and compare the public cache file.
- Persistence check: fresh HOME per command and post-process cache reads.
- Evidence: `GOCACHE=/private/tmp/roundfix-0171-qa-03-gocache go test
  -count=1 -v -run
  '^(TestSuiteDefaultReleaseLookupStaysOffline|TestOperationalCommandsRecordTheOfflineLookup|TestHelperProcessRecordsTheOfflineLookup|TestBuiltBinaryLeavesTheSeededVersionCacheUntouched|TestPerTestReleaseLookupOverridesTheSuiteDefault|TestRunDispositionCharacterizationPreflightRefusesOnAnUnintegratedBranch)$'
  ./internal/cli ./internal/daemon` exited 0. The four operational-command
  subtests, helper process, both built-binary cache checks, and per-test
  override all passed through fresh public cache reads.

### 6

```yaml
inputs:
  - kind: repository_path
    ref: internal/agent/acpx_runner_test.go
```

- Entry point: `go test -c ./internal/agent`, then the compiled binary through a relative path from the package directory.
- Expected observable: relative and absolute fixture tests pass.
- Independent confirmation: both named tests appear in verbose pass output.
- Persistence check: the relative child executes the adapter from a distinct temporary directory.
- Evidence: `GOCACHE=/private/tmp/roundfix-0171-qa-03-gocache go test -c
  -o /private/tmp/roundfix-0171-agent-03.test ./internal/agent` exited 0 from
  the repository root. From `internal/agent`, the relative invocation
  `../../../../../../../../private/tmp/roundfix-0171-agent-03.test` selected
  the relative and absolute adapter tests; both passed and the suite guard
  confirmed no repository mutation. An initial compile attempted from the
  package directory duplicated the package path and failed during QA setup;
  the corrected compile changed no build input.

### 7

```yaml
inputs:
  - kind: live_service
    ref: https://pkg.go.dev/testing#T.Deadline
  - kind: external_repository
    ref: prior Roundfix Run QA reports named by the adopted Backlog Entry
```

- Entry point: Go API documentation and retained reports for the three named Runs.
- Expected observable: Go's deadline semantics and all four adopted failures resolve from outside sources.
- Independent confirmation: cite each source location rather than the Spec-authored summary.
- Persistence check: reopen each source during this rerun.
- Evidence: `go doc testing.T.Deadline` reported that the deadline is the time
  at which the test binary will have exceeded `-timeout`, with `ok=false` only
  for `-timeout=0`; this matches
  <https://pkg.go.dev/testing#T.Deadline>. Current searches reopened the three
  retained Run artifacts and found these originating failures:

  - `/Users/marcio/.roundfix/artifacts/339f8dac2b687a04/runs/run_20260925T153433Z_8603eb3e79157622/verification/batch-002-attempt-1.log:38-39`
    records the queued-cancellation failure after 91.57 s with one of two Agent
    starts.
  - `/Users/marcio/.roundfix/artifacts/339f8dac2b687a04/runs/run_20260925T155346Z_b801d0ee9b2a247a/verification/batch-001-attempt-1.log:36-37`
    records the concurrent-bootstrap Clean-exit failure and the underlying Git
    worktree-administration race.
  - `/Users/marcio/.roundfix/artifacts/95e45ae2977a5c2d/runs/run_20260924T223414Z_54e6f85ae98b6550/verification/batch-006-attempt-1.log:12-13`
    records the queued Verification-capacity failure.

  The named Run artifacts still contain no originating report for the
  documentation-only `TestBootstrapSerializesAcrossSiblings` failure, so the
  source-specific row remains environment-blocked. Equivalent observed
  evidence is complete: row 4 ran that test 20/20 beside a full suite load on
  the current build, and the other three historical failures also passed
  20/20. Unblock the source path by supplying the original documentation-only
  report or correcting Requirement 6 to cite its retained artifact.

### 8

```yaml
inputs:
  - kind: repository_path
    ref: internal/**
  - kind: repository_path
    ref: Makefile
  - kind: repository_path
    ref: docs/references/coverage-record.json
```

- Entry point: Git diff from the Spec target branch to the audited head.
- Expected observable: production changes are limited to Task 07's bounded corrective paths, and the governed CLI test, coverage record, and Makefile are unchanged.
- Independent confirmation: compare range-level and commit-level paths.
- Persistence check: resolve target branch and head at execution time.
- Evidence: the current delivery target resolves to
  `322e1e28e24c555ea352ead59966e88649289484`, and its diff to audited head
  contains only `task_08.md` and `internal/worktree/worktree_test.go`. The full
  Spec range from pre-Spec base `256ad1568b34be256527e8f5e6999b2d8203f8ab`
  contains exactly four product-runtime paths:
  `internal/worktree/worktree.go`, `adminlock.go`, `adminlock_unix.go`, and
  `adminlock_windows.go`; the remaining Go changes are tests or the
  test-only `internal/testwait` package. `git diff-tree --no-commit-id
  --name-status -r 8eadbadebc72a81caf894312662ac5261877a092`
  binds those paths and `adminlock_test.go` to corrective Task 07, while the
  same command for `0b6ccab408a9c12a27f85f85d09b54f482783dd1`
  lists only Task 08's file and `worktree_test.go`. `git diff --quiet` confirms
  `Makefile`, `internal/cli/cli_test.go`, and
  `docs/references/coverage-record.json` are unchanged across the full range.

### 9

```yaml
inputs:
  - kind: repository_path
    ref: docs/specs/0171-a-deterministic-suite-under-load/_prd.md
  - kind: repository_path
    ref: docs/specs/0171-a-deterministic-suite-under-load/_techspec.md
  - kind: repository_path
    ref: docs/specs/0171-a-deterministic-suite-under-load/_tasks.md
  - kind: repository_path
    ref: docs/specs/0171-a-deterministic-suite-under-load/task_*.md
```

- Entry point: strict Spec checker and focused promise/vocabulary commands.
- Expected observable: every promise names a consuming Task and no changed production surface emits undocumented vocabulary.
- Independent confirmation: combine checker, references, and Git path results.
- Persistence check: rerun commands against the audited head.
- Evidence: the strict Spec check exited 0 with no findings. Focused Task
  reference searches show Success Metrics 1-2 consumed by Tasks 02 and 03,
  Metric 3 and API Contracts 1-2 by Task 01, Metric 4 and API Contract 3 by
  Task 04, Metric 5 by Task 05, and the complete promise sets by Task 06. The
  skipped vocabulary detector was checked manually against Task 07's complete
  production diff: it adds no command token, status, field, prefix, or
  competing domain term. Its diagnostics use the existing Git, Worktree,
  context, and lock vocabulary recorded in `CONTEXT.md`; no Vocabulary
  Contract or glossary change is required.

### 10

```yaml
inputs:
  - kind: live_service
    ref: Pull Request for roundfix/deliver-0171-a-deterministic-suite-under-load-eb5d1ed509eacdb3
  - kind: repository_path
    ref: "**"
```

- Entry point: Pull Request controls on the Spec target branch.
- Expected observable: approval, checks/status, unresolved threads, Merge-Ready acceptance, and review-artifact ancestry each have an observed or supervised answer.
- Independent confirmation: tie repository-local evidence to the current audited head.
- Persistence check: compare evidence ancestry to the current head.
- Evidence: the assignment states that no Pull Request is open for the Spec
  target branch, so the live journey is unreachable. Equivalent controls are
  fully answered: approval — the maintainer authorized the implementation
  scope on 2026-09-25, but no PR acceptance has been obtained; checks and
  status — the Daemon's current-build `make verify-changed` passed; unresolved
  review threads — none can stand because no Pull Request exists; Merge-Ready
  acceptance — no Review Source evidence or acceptance exists yet; and
  review-artifact ancestry — `rg --files docs/specs | rg '(^|/)reviews/'`
  found no review artifact, while `git merge-base --is-ancestor` proved target
  head `322e1e28e24c555ea352ead59966e88649289484` is an ancestor of audited head
  `0b6ccab408a9c12a27f85f85d09b54f482783dd1`. Complete the configured pre-PR
  review and observe the opened Pull Request controls during delivery.

## Findings

None.

## Blocked and skipped

- Row 7 — `blocked (environment: fourth originating report unavailable)`.
  Go documentation and three originating failures are reachable, but the
  retained artifacts for the named Runs do not contain the documentation-only
  `TestBootstrapSerializesAcrossSiblings` failure. Equivalent observed
  evidence is row 4's 20/20 loaded regression for that test and the other
  three historical failures. Unblock the source path by supplying its original
  report or correcting Requirement 6 to cite a retained outside artifact.
- Row 10 — `blocked (environment: no open Pull Request)`. Equivalent evidence
  above answers approval, checks and status, unresolved review threads,
  Merge-Ready acceptance, and review-artifact ancestry. Unblock at delivery by
  completing the configured pre-PR review, opening the PR from the Spec target
  branch, and observing its controls read-only.
- No row was skipped.

## Coverage

Ten rows cover all eight declared verification Requirements plus the
outside-evidence, Pull Request, and repository Verification sources. Eight
rows passed, two were environment-blocked with equivalent evidence, and none
failed or were finding-blocked, declared-blocked, or skipped. No Unreachable
Acceptance or frontend source applies.

## Mechanical skips

| Detector | Missing artifact |
| --- | --- |
| authorization bounded paths | tooling authorization |
| consequent-fix commit order | consequent-fix declaration |
| QA evidence paths | docs/specs/0171-a-deterministic-suite-under-load/qa/evidence |

## Final verdict

Pass: every runnable criterion passed, both environment-blocked rows record
their cause and equivalent observed or supervised evidence, and no finding,
declared block, or skip remains. Final counts: 8 pass, 0 fail, 2
environment-blocked, 0 finding-blocked, 0 declared-blocked, 0 skipped;
findings: 0 Blocks-Completion, 0 Data-Loss, 0 Trust-Damage, 0 Friction, 0
Cosmetic.
