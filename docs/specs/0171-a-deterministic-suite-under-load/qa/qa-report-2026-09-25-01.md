---
spec: 0171-a-deterministic-suite-under-load
date: 2026-09-25
build: 8eadbadebc72a81caf894312662ac5261877a092
auditing_binary: "0.17.0 (256ad156, built 2026-09-25 15:19:40 -0300)"
auditor_staleness: "stale: commit ancestry: build commit predates audited tree"
status: closed
verdict: fail
rows_blocked_environment: 2
rows_blocked_finding: 0
rows_blocked_declared: 0
surfaces: [backend]
---

# QA report — A deterministic suite under load

## Scope and environment

This full rerun exercises the assembled backend test-suite behavior at commit
`8eadbadebc72a81caf894312662ac5261877a092`. The actor is a Roundfix maintainer
running Go package tests and repository checks from the Run Worktree on macOS.
The Spec declares no frontend surface and no Unreachable Acceptance items.

The authored terminal node is `task_06`; it depends on completed Tasks 01-05
and corrective Task 07. `roundfix spec check
0171-a-deterministic-suite-under-load --strict` passed with exit 0 before this
matrix opened. It reported no findings and did not execute authored
Verification commands. It skipped `SC-ROLLUP-MEMBER` because there is no
findings rollup and skipped the vocabulary detector because the TechSpec has
no Vocabulary Contract; Requirement 8 remains in row 9.

The mechanical audit passed without repairs or findings. Its `0.17.0` binary
is stale because its build commit predates the audited tree; that staleness is
preserved above instead of being presented as current evidence.

## Static gate

The Daemon already ran `make verify-changed` outside the Agent sandbox. It
reported exit 0 and removed diagnostics on success. Per the QA assignment, row
1 records that supervised fact and this Agent does not rerun repository
Verification.

## Performed repairs

None.

## Assigned repair failures

None.

## Authorization audit inputs

None.

## Mechanical findings

None.

## Results

| # | Story / criterion / sweep | Actor and surface | Status | Provenance |
| - | --- | --- | --- | --- |
| 1 | Repository Verification | Maintainer, backend static gate | pass | Requirement 1; repository Verification |
| 2 | Deadline-bound waits report ended work and prefer ready values | Maintainer, `go test` | pass | Requirement 2 |
| 3 | Wait and bootstrap sources contain no forbidden fixed durations | Maintainer, source-contract commands | pass | Requirement 3 |
| 4 | Four historical failures pass under stress beside the suite | Maintainer, concurrent `go test` | pass | Requirement 3 |
| 5 | Operational commands use the offline release lookup | Maintainer, `go test` | pass | Requirement 4 |
| 6 | Compiled adapter fixture runs through a relative binary path | Maintainer, compiled Go test binary | pass | Requirement 5 |
| 7 | Go deadline semantics and the four historical failures resolve from outside sources | Maintainer, published Go docs and prior Run reports | blocked (environment: fourth originating report unavailable) | Requirement 6; outside-evidence row |
| 8 | Git scope contains no production or protected-file change | Maintainer, Git history | fail | Requirement 7 |
| 9 | Spec promises name consuming Tasks and no new vocabulary is emitted | Maintainer, strict Spec checker and focused commands | pass | Requirement 8 |
| 10 | Pull Request controls have equivalent evidence | Reviewer, Pull Request surface | blocked (environment: no open Pull Request) | Pull Request row |

## Row evidence

### 1

```yaml
inputs:
  - kind: repository_path
    ref: "**"
```

- Entry point: Daemon-owned repository Verification.
- Expected observable: exit 0 with no retained diagnostics.
- Independent confirmation: the Daemon supplies the verdict and diagnostic
  disposition.
- Persistence check: not applicable to a supervised static-gate fact.
- Evidence: supervised Daemon result: `make verify-changed` exited 0 and its
  diagnostics were removed on success. The command was not rerun in this Agent
  sandbox.

### 2

```yaml
inputs:
  - kind: repository_path
    ref: internal/testwait/testwait.go
  - kind: repository_path
    ref: internal/testwait/testwait_test.go
```

- Entry point: focused `go test` selection in `./internal/testwait`.
- Expected observable: the deadline case reaches its bound, ended work fails
  promptly and names its result, and ready wins over simultaneous end.
- Independent confirmation: each named test appears as passing.
- Persistence check: rerun through a fresh package test process.
- Evidence: `GOCACHE=/private/tmp/roundfix-0171-qa-rerun-gocache go test
  -count=1 -v -run
  '^(TestBoundFollowsTheTestDeadline|TestUntilPrefersTheReadyValueOverAnEndedWork|TestUntilFailsAtOnceWhenTheWorkEnds|TestUntilFailsAtTheDeadlineAndNotBefore)$'
  ./internal/testwait` exited 0. All four tests passed; the deadline case took
  0.30 s and both ended-work cases completed immediately.

### 3

```yaml
inputs:
  - kind: repository_path
    ref: internal/cli/implement_test.go
  - kind: repository_path
    ref: internal/daemon/task_engine_test.go
  - kind: repository_path
    ref: internal/worktree/worktree_test.go
```

- Entry point: executable source-contract searches.
- Expected observable: forbidden searches have no match and required
  `testwait` searches match.
- Independent confirmation: row 4 exercises the migrated paths.
- Persistence check: search all three current files in one run.
- Evidence: focused `rg` commands rejected
  `implementWaitBudget|detachStartupBudget|time\.After\(|time\.NewTimer\(|bootstrap_timeout:[[:space:]]*1s|90[[:space:]]*\*[[:space:]]*time\.Second|command,
  "1s"\)` in `implement_test.go`, the removed Daemon wait helpers and
  `time.After` in `task_engine_test.go`, and one- or five-second timeout
  literals in `worktree_test.go`. Each exited 1 with no output. The positive
  `testwait\.(Until|Poll|Bound)|attachDetachBudget` search exited 0 and found
  the helper throughout all three files, including every deadline-derived
  non-subject bootstrap timeout. `attachDetachBudget` remains only on the
  attach-owned path.

### 4

```yaml
inputs:
  - kind: repository_path
    ref: internal/cli/implement_test.go
  - kind: repository_path
    ref: internal/daemon/task_engine_test.go
  - kind: repository_path
    ref: internal/worktree/worktree_test.go
  - kind: repository_path
    ref: internal/testwait/testwait.go
  - kind: repository_path
    ref: internal/worktree/adminlock*.go
  - kind: elapsed_time
    ref: count-10 stress under concurrent package load
```

- Entry point: the four named regression tests through `go test -count=10
  -cpu 1,4` while other suite packages run beside them.
- Expected observable: every focused iteration passes and adjacent package
  load completes or has any environmental failure classified separately.
- Independent confirmation: require all four names in verbose pass output and
  the source constraints from row 3.
- Persistence check: 20 scheduler configurations per named test.
- Probe: concurrent packages reproduce the load shape that exposed F-001.
- Evidence: `GOCACHE=/private/tmp/roundfix-0171-qa-rerun-gocache go test
  -count=10 -cpu 1,4 -v -run
  '^(TestRunImplementQueuedCancellationStartsNoChildAndKeepsResumableTasks|TestRunImplementBootstrapsEachConcurrentTaskWorktreeBeforeAgentWork|TestTaskCycleVerificationCapacityCancellationWhileQueuedStartsNoCommandOrSettlement|TestBootstrapSerializesAcrossSiblings)$'
  ./internal/cli ./internal/daemon ./internal/worktree` exited 0. Each named
  test passed 20 times: ten iterations at CPU settings 1 and 4. Package
  durations were 93.671 s, 42.862 s, and 24.656 s respectively.

  Concurrent `GOCACHE=/private/tmp/roundfix-0171-qa-rerun-gocache go test
  -count=1 ./...` supplied adjacent package load for the complete 93.671-second
  focused run. It completed `internal/agent`, `internal/app`,
  `internal/authorization`, `internal/baseline`, and `internal/baselineacp`
  successfully before the sandbox terminated its remaining network access to
  `cafe.github.com`. That environment denial did not interrupt or invalidate
  the independently captured focused command. F-001 from the previous report
  did not reproduce and is closed by this row's 20/20 regression result.

### 5

```yaml
inputs:
  - kind: repository_path
    ref: internal/cli/version_freshness_isolation_test.go
  - kind: repository_path
    ref: internal/cli/implement_test.go
  - kind: repository_path
    ref: internal/daemon/run_disposition_characterization_test.go
```

- Entry point: focused `go test` commands in `./internal/cli` and
  `./internal/daemon`.
- Expected observable: offline tags are recorded, seeded caches remain
  byte-identical, and the per-test override still prints the warning.
- Independent confirmation: tests reopen and compare the public cache file.
- Persistence check: fresh HOME per command and post-process cache reads.
- Evidence: `GOCACHE=/private/tmp/roundfix-0171-qa-rerun-gocache go test
  -count=1 -v -run
  '^(TestSuiteDefaultReleaseLookupStaysOffline|TestOperationalCommandsRecordTheOfflineLookup|TestHelperProcessRecordsTheOfflineLookup|TestBuiltBinaryLeavesTheSeededVersionCacheUntouched|TestPerTestReleaseLookupOverridesTheSuiteDefault|TestRunDispositionCharacterizationPreflightRefusesOnAnUnintegratedBranch)$'
  ./internal/cli ./internal/daemon` exited 0. The four operational-command
  subtests, helper process, both built-binary cache checks, and per-test
  override all passed.

### 6

```yaml
inputs:
  - kind: repository_path
    ref: internal/agent/acpx_runner_test.go
```

- Entry point: `go test -c ./internal/agent`, then the compiled binary through
  a relative path from the package directory.
- Expected observable: relative and absolute fixture tests pass.
- Independent confirmation: both named tests appear in verbose pass output.
- Persistence check: the relative child executes the adapter from a distinct
  temporary directory.
- Evidence: `go test -c -o
  /private/tmp/roundfix-0171-agent-rerun.test ./internal/agent` exited 0. From
  `internal/agent`, the relative invocation
  `../../../../../../../../private/tmp/roundfix-0171-agent-rerun.test` selected
  the relative and absolute adapter tests; both passed and the suite guard
  confirmed no repository mutation. An initial compile attempted from the
  package directory used the repository-relative package path twice and failed
  during QA setup; the corrected compile from the repository root changed no
  build input.

### 7

```yaml
inputs:
  - kind: live_service
    ref: https://pkg.go.dev/testing#T.Deadline
  - kind: external_repository
    ref: prior Roundfix Run QA reports named by the adopted Backlog Entry
```

- Entry point: Go API documentation and retained reports for the three named
  Runs.
- Expected observable: Go's deadline semantics and all four adopted failures
  resolve from outside sources.
- Independent confirmation: cite each source location rather than the
  Spec-authored summary.
- Persistence check: reopen each source during this rerun.
- Evidence: `go doc testing.T.Deadline` reported that the deadline is the time
  at which the test binary will have exceeded `-timeout`, with `ok=false` only
  for `-timeout=0`; this matches
  <https://pkg.go.dev/testing#T.Deadline>. Current searches reopened the three
  retained Run artifacts and found these originating failures:

  - `/Users/marcio/.roundfix/artifacts/339f8dac2b687a04/runs/run_20260925T153433Z_8603eb3e79157622/verification/batch-002-attempt-1.log:38-39`
    records the queued-cancellation failure after 91.57 s with one of two Agent
    starts.
  - `/Users/marcio/.roundfix/artifacts/339f8dac2b687a04/runs/run_20260925T155346Z_b801d0ee9b2a247a/verification/batch-001-attempt-1.log:36-37`
    records the concurrent-bootstrap Clean-exit failure.
  - `/Users/marcio/.roundfix/artifacts/95e45ae2977a5c2d/runs/run_20260924T223414Z_54e6f85ae98b6550/verification/batch-006-attempt-1.log:12-13`
    records the queued Verification-capacity failure.

  The same search found no originating report for the documentation-only
  `TestBootstrapSerializesAcrossSiblings` failure. The Spec-authored adopted
  Backlog Entry cannot substitute for its claimed outside source, so the row
  remains environment-blocked.

### 8

```yaml
inputs:
  - kind: repository_path
    ref: internal/**
  - kind: repository_path
    ref: Makefile
  - kind: repository_path
    ref: docs/references/coverage-record.json
```

- Entry point: Git diff from the Spec target branch to the audited head.
- Expected observable: no production file, governed CLI test, coverage record,
  or Makefile change.
- Independent confirmation: compare range-level and commit-level paths.
- Persistence check: resolve target branch and head at execution time.
- Evidence: the target branch and merge base both resolve to
  `fc29a824292071ac81e57292b6a90187f0c911eb`; the audited head is
  `8eadbadebc72a81caf894312662ac5261877a092`. Both the range diff and
  `git diff-tree --no-commit-id --name-only -r 8eadbade...` list
  `internal/worktree/adminlock.go`, `adminlock_unix.go`,
  `adminlock_windows.go`, and `worktree.go`, all production files. The same
  focused Git check confirms `Makefile`, `internal/cli/cli_test.go`, and
  `docs/references/coverage-record.json` are unchanged. Requirement 7 forbids
  any production-file change, so the bounded corrective implementation still
  contradicts this row. See F-002.

### 9

```yaml
inputs:
  - kind: repository_path
    ref: docs/specs/0171-a-deterministic-suite-under-load/_prd.md
  - kind: repository_path
    ref: docs/specs/0171-a-deterministic-suite-under-load/_techspec.md
  - kind: repository_path
    ref: docs/specs/0171-a-deterministic-suite-under-load/_tasks.md
  - kind: repository_path
    ref: docs/specs/0171-a-deterministic-suite-under-load/task_*.md
```

- Entry point: strict Spec checker and focused promise/vocabulary commands.
- Expected observable: every promise names a consuming Task and no changed
  production surface emits undocumented vocabulary.
- Independent confirmation: combine checker, references, and Git path results.
- Persistence check: rerun commands against the audited head.
- Evidence: the strict Spec check exited 0 with no findings. Focused Task
  reference searches show Success Metrics 1-2 consumed by Tasks 02 and 03,
  Metric 3 and API Contracts 1-2 by Task 01, Metric 4 and API Contract 3 by
  Task 04, Metric 5 by Task 05, and the complete sets by Task 06. The skipped
  vocabulary detector was checked manually against the corrective production
  diff: it adds no stable command token, status, field, prefix, or canonical
  domain term; its error prose uses the existing Git, Worktree, context, and
  lock vocabulary. No Vocabulary Contract or `CONTEXT.md` change is required.

### 10

```yaml
inputs:
  - kind: live_service
    ref: Pull Request for roundfix/deliver-0171-a-deterministic-suite-under-load-eb5d1ed509eacdb3
  - kind: repository_path
    ref: "**"
```

- Entry point: Pull Request controls on the Spec target branch.
- Expected observable: approval, checks/status, unresolved threads,
  Merge-Ready acceptance, and review-artifact ancestry each have an observed
  or supervised answer.
- Independent confirmation: tie repository-local evidence to the current
  audited head.
- Persistence check: compare evidence ancestry to the current head.
- Evidence: the assignment states that no Pull Request is open for the Spec
  target branch, so the live journey is unreachable. Equivalent controls are
  fully answered: the maintainer authorized the implementation scope on
  2026-09-25, but no PR acceptance has been obtained; the Daemon's
  current-head `make verify-changed` passed; no PR exists to hold unresolved
  threads; no Merge-Ready acceptance exists yet; and a repository search found
  no review artifact tied to `8eadbadebc72a81caf894312662ac5261877a092`.
  The expected pre-PR review and Pull Request controls remain delivery-stage
  work.

## Findings

### F-002 — The QA scope still forbids the corrective production change

- Impact: Trust-Damage.
- Actor and journey step: maintainer, row 8's Git-scope audit against the Spec
  target branch.
- Expected: Requirement 7 observes no production-file change.
- Actual: corrective Task 07 adds three production lock files and edits
  `internal/worktree/worktree.go` to serialize Git worktree administration.
- Reproduction: run `git diff --name-status
  roundfix/deliver-0171-a-deterministic-suite-under-load-eb5d1ed509eacdb3...HEAD`
  and `git diff-tree --no-commit-id --name-only -r 8eadbade...`.
- Evidence: both commands list the four production files; affected row 8 only.
- Required correction: update the PRD/TechSpec production-scope statements and
  Task 06 Requirement 7 to name and audit the bounded corrective production
  paths, then rerun the gate. Do not remove the correction merely to satisfy a
  stale no-production-change promise.

## Blocked and skipped

- Row 7 — `blocked (environment: fourth originating report unavailable)`.
  Go documentation and three originating failures are reachable, but the
  retained artifacts for the named Runs do not contain the documentation-only
  `TestBootstrapSerializesAcrossSiblings` failure. Unblock by supplying its
  original report or correcting Requirement 6 to cite an existing outside
  artifact.
- Row 10 — `blocked (environment: no open Pull Request)`. Equivalent evidence
  above answers every PR control. Unblock at delivery by completing the
  configured pre-PR review, opening the PR from the Spec target branch, and
  observing approval, checks, threads, Merge-Ready acceptance, and ancestry.

## Coverage

Ten rows cover all eight declared verification Requirements plus the
outside-evidence, Pull Request, and repository Verification sources. Seven
rows passed, one failed, two were environment-blocked, and none were
finding-blocked, declared-blocked, or skipped. No Unreachable Acceptance or
frontend source applies.

## Mechanical skips

| Detector | Missing artifact |
| --- | --- |
| authorization bounded paths | tooling authorization |
| consequent-fix commit order | consequent-fix declaration |
| QA evidence paths | docs/specs/0171-a-deterministic-suite-under-load/qa/evidence |

## Final verdict

Fail: the corrective concurrency behavior passes the required load stress, but
Requirement 7 still forbids Task 07's production files. Update the Spec's
production-scope contract and supply or correct the unavailable fourth outside
source before rerunning. Final counts: 7 pass, 1 fail, 2 environment-blocked,
0 finding-blocked, 0 declared-blocked, 0 skipped; findings: 0
Blocks-Completion, 0 Data-Loss, 1 Trust-Damage, 0 Friction, 0 Cosmetic.
