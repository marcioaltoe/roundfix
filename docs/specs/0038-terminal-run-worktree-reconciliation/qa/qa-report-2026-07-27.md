---
spec: 0038-terminal-run-worktree-reconciliation
date: 2026-07-27
build: 01a16c8d9fe430a4d70d0f8db5c5a135eb1d803b
status: closed
verdict: fail
surfaces: [backend, cli, data, docs]
---

# QA report — Terminal Run Worktree reconciliation

## Scope and environment

This is the full Spec gate. It covers every PRD user story, Core Feature,
Success Metric, Task acceptance criterion, Project Constraint, and
scope-exclusion that needs an observable check. The intended actor is a
Roundfix user or an Agent operating the public CLI. Destructive flows will run
only in an isolated temporary Git repository with an isolated Roundfix Home
and Run Database.

Build under test: `01a16c8d9fe430a4d70d0f8db5c5a135eb1d803b` on
`roundfix/run-run_20260727T194155Z_ca63a837c414679b`.

Evidence directory:
`qa/evidence/2026-07-27-terminal-reconciliation/`.

Parity deviations: pending environment setup. No network, GitHub, or HTTP
service is required by the Spec.

## Project Constraint audit

| Constraint | PRD | TechSpec | Operative source | Status | Evidence |
| --- | --- | --- | --- | --- | --- |
| Identifier strategy | Not applicable: existing Run, branch, path, and Git object identities only | Same applicability and reason | `docs/agents/domain.md` | pass | Matching, reasoned entries cite the operative source; source/runtime audit found only existing identities. |
| Authentication and HTTP | Not applicable: local Run Database and Git only | Same applicability and reason | `docs/agents/cli.md` | pass | Matching, reasoned entries cite the operative source; built flows used no network, authentication, or HTTP contract. |
| Active ADR obligations | ADR-0023, ADR-0024, ADR-0052, ADR-0053 apply | Same four obligations | `docs/agents/domain.md` | pass | Live Git, apply, and persistence evidence matched all four active decisions. |
| Tooling authority | Exact Skill pair plus five derived digest paths expressly authorized | Same bounded authorization | `docs/agents/agent-instructions.md` | pass | Task 07 `diff-tree` exactly matched the two Skill paths, five digest pins, and Task file; byte identity and digest/Skill validation passed. |

## Static gate

- `rtk make verify` — the sandboxed attempt could not read
  `/Users/marcio/Library/Caches/go-build/...` and stopped before compilation.
  The same exact command rerun with normal host-cache access passed:
  2,563 tests in 23 packages, four focused Skill tests, all fourteen shipped
  Skills, and the production build.
- `rtk git -c core.fsmonitor=false diff --check` — passed after Verification.
- `rtk git -c core.fsmonitor=false status --short` — only this untracked
  Spec-local `qa/` directory; Verification introduced no tracked side effect.

## Results

| # | Story / criterion / sweep | Actor and surface | Entry point and steps | Expected observable and independent confirmation | Persistence / probe | Status | Evidence |
| --- | --- | --- | --- | --- | --- | --- | --- |
| QA-01 | Project Constraints: identifier and authentication/HTTP applicability | Maintainer; docs/backend | Compare PRD and TechSpec entries to operative guides and runtime dependencies. | Both artifacts account for each constraint with reasons and operative paths; runtime stays local and creates no new identity scheme or HTTP/auth contract. | Inspect the built command and isolated flow for network/auth dependencies. | pass | PRD and TechSpec carry matching reasoned entries with operative sources. Reconcile source has no HTTP/auth or new identifier dependency; all built-CLI flows used only local Git and the isolated Run Database. |
| QA-02 | Project Constraints: active ADR obligations | Maintainer; docs/backend | Read ADR-0023, ADR-0024, ADR-0052, and ADR-0053, then compare live outcomes. | Per-Run worktree ownership, porcelain Git, guarded Integration Pending transition, and positive-proof cleanup all remain true. | Confirm after apply and persistence reload. | pass | Real worktrees/branches followed ADR-0023/0024; mixed apply required clean ancestry proof; fresh Store reload showed the sole IntegrationPending-to-Clean transition and full evidence payload. |
| QA-03 | Project Constraints: tooling authorization and exact changed-file scope; Task 07 AC3-AC6 | Maintainer; docs/tooling | Resolve Task 07's Daemon commit, run `git diff-tree`, inspect current delta, compare the Skill pair, and run shipped Skill validation. | Only the two authorized Skill files, five authorized digest pins, and Task file changed in the tooling Task; Skill copies are byte-identical; no other protected Skill changed; shipped Skill and full gate pass. | Fresh current-worktree status and digest checks. | pass | Commit `16da675` contains exactly the authorized Skill pair, five authorized digest pins, and Task file. `cmp`, authorial sync, shipped Skill check, and full gate passed; Task 06 touched no protected path. |
| QA-04 | Static repository gate; Task 07 AC6 | Maintainer; all | Run `rtk make verify` from the repository root. | Formatting, complete tests, Skill sync/check, and build all exit zero. | Reinspect status and `git diff --check` for verification side effects. | pass | Exact host-access rerun exited 0: 2,563 tests/23 packages, 4 Skill-sync tests, 14 shipped Skill checks, and `bin/roundfix` build. Postflight found only `qa/` and no whitespace error. |
| QA-05 | US1; Core Features 1-3; Success Metric 1; Task 01 AC1-AC7 | User; CLI/backend | In one isolated real Git repository, create terminal Run fixtures for `safe`, `unintegrated`, tracked and untracked `dirty`, missing-metadata/ambiguous `unknown`, and `released`; run `roundfix reconcile --format json`. | Every fixture receives exactly one documented state. Recorded paths, known heads, and bounded deterministic evidence are present. Missing or ambiguous proof never becomes `safe`. | Repeat the read and compare JSON classification fields; include unsafe-path fixture coverage from current static evidence. | pass | Built text/JSON CLI returned exactly one of all five states with full evidence; missing target stayed unknown. Fresh worktree focused family covered ambiguous refs and unsafe paths. |
| QA-06 | US2; Core Features 4-5; Task 04 AC1-AC3 | Agent; CLI/data | Snapshot Git refs/worktrees and Run Database, run one-Run and repository-wide dry-runs in text and JSON. | Default mode is read-only; text and versioned JSON expose consistent ordered classification, branches, heads, worktree, evidence/action/refusal, and summary fields; results are newest-first and repository-scoped. | Byte/hash comparison of database plus fresh Git reads after both dry-runs; repeat JSON for determinism. | pass | Text/JSON matched and were newest-first; flags-before-ID selected one Run. DB hash `9a7646…`, refs, and worktree list were unchanged. |
| QA-07 | US3; Core Feature 8; Success Metric 4; Task 05 AC1-AC6 | User; CLI | Run `runs list` in active, terminal, all-state, zero-residue, and mixed-repository views. | stdout row shape remains unchanged; stderr reports the exact relevant retained count and points to `roundfix reconcile` without labeling safety; no retained-worktree note appears for released-only history. | Capture stdout and stderr separately, then rerun after cleanup to confirm the retained note disappears. | pass | Active/terminal/all views reported exactly 3 retained in repository scope; `--all` reported 6. Released-only repository emitted no retained-worktree note. Full-gate stream assertions preserved stdout rows. |
| QA-08 | US4; Core Features 2-4 and 6; Success Metric 2; Task 02 AC1, AC3-AC5; Task 04 AC4-AC5 | User; CLI/backend/data | Run repository-wide `reconcile --apply` against mixed `safe`, `dirty`, `unintegrated`, `unknown`, and `released` fixtures. | All and only freshly safe worktrees/branches are removed. Preserved states remain intact and are reported without making the scan fail. Worktree removal precedes branch deletion and has no force bypass. | Fresh `git worktree list`, ref reads, dirty-file checks, and database reload after apply. | pass | Apply summary was `applied=1 preserved=3`; fresh Git reads found only safe surfaces gone and preserved dirty/unintegrated/unknown surfaces intact. |
| QA-09 | US5; Core Feature 6; Success Metric 5; Task 03 AC1-AC3 | User/Supervisor; CLI/data | Apply one safe Integration Pending fixture and inspect it through `runs list`, `reconcile`, and persisted Run events/state. | The Run becomes Clean before cleanup and one durable evidence event contains prior/current outcomes, classification, branches, heads, worktree, and action. | Close/reopen via a second CLI process and re-read state/event evidence. | pass | Fresh `runs list` showed Clean. Reopened Store showed one daemon outcome event with IntegrationPending/Clean, both branches/heads, worktree, safe classification, and cleanup action. |
| QA-10 | Core Feature 6; Non-Goal: no other terminal outcome rewrite; Task 03 AC2-AC3 | User; CLI/data | Apply safe Unresolved, Failed, Stopped, and other terminal fixtures. | Cleanup evidence is durable but each non-Integration-Pending terminal outcome stays unchanged. | Re-read through a fresh CLI process and compare outcomes. | pass | Fresh `runs list --state all` retained Unresolved, Failed, Stopped, and TimedOut exactly while their safe Git surfaces were released. |
| QA-11 | Core Feature 7; Success Metric 3; Task 02 AC6; Task 03 AC6; Task 04 AC7 | User/Agent; CLI/data | Run `reconcile --apply` again after successful cleanup. | Result is `released`, `applied=0`, and no Git or Run Database mutation occurs. | Hash database and list refs/worktrees before and after the second apply. | pass | Second apply returned 5 released and `applied=0`; DB hash `bf3b8a…`, worktrees, and refs were identical before/after. |
| QA-12 | Apply revalidation race boundary; Task 02 AC1-AC3; Task 03 AC4 | User; CLI/backend/data | Use current static stale-head, changed-metadata, and newly-dirty evidence, plus a public-process interruption probe where feasible. | A changed head, worktree state, or recorded metadata blocks mutation; no path/ref/state is deleted or rewritten. | Confirm every retained Git surface and unchanged database state. | pass | Fresh worktree family and race runs passed stale-head, stale-metadata, newly-dirty, and revalidation cases. The public locked-worktree probe also preserved both surfaces. |
| QA-13 | Operational failure behavior; Task 02 AC5; Task 03 AC5; Task 04 AC6 | User; CLI/backend/data | Make a real safe fixture's clean worktree non-removable, then apply. Use current injected persistence-failure evidence for the store-before-Git boundary. | CLI exits non-zero, names the next safe action and remaining recoverable surface; persistence failure starts no Git cleanup. | Re-read path/ref/state after failure. | pass | Locked real worktree exited 1 with next action and remaining path/ref; fresh Git reads found both. Focused StoreFailure test passed with zero cleanup. |
| QA-14 | Invalid selectors and repository scope; Task 04 requirements 2-3 and AC2 | Agent; CLI | Select missing, Active, review, and other-repository Runs; invoke the no-ID form from the isolated repository. | Each invalid selector exits through Preflight Validation before Git mutation; no-ID includes only the current repository's terminal spec Runs. | Snapshot refs/worktrees/database and confirm no change. | pass | Four selectors exited 2 with precise reasons; DB hash `bf3b8a…`, refs, and worktrees were unchanged. No-ID JSON returned only the current repository's empty terminal-spec set. |
| QA-15 | Automatic reaper shares proof; Core Feature 9; Task 02 AC7 | User; CLI/backend | Exercise Implement Preflight against a changed Run Branch already reachable from target and a unique divergent Run Branch. | Reachable safe residue is reaped; unique work is preserved. The old creation-base shortcut is insufficient. | Fresh Git ref/worktree reads after Preflight. | pass | Fresh CLI focused family passed `TestRunImplementPreflight.*Terminal.*(Safe|Unique|Reachable)` against real Git fixtures; mixed public apply independently preserved the unique divergent tip. |
| QA-16 | Safety boundary and path containment; Task 01 AC7 | User; backend | Exercise recorded path, symlink ancestor/final-component, ambiguous-ref, and cross-root fixtures through current real-Git verification evidence. | Inspection never reads outside the recorded Git root and uncertainty remains `unknown`. | Confirm outside sentinel paths remain unchanged. | pass | Fresh `TestInspectTerminalRun` family passed registered-path, ancestor/final symlink, ambiguous-ref, and unsafe-path cases; missing target also stayed unknown through built CLI. |
| QA-17 | Task 02 AC2-AC4 implementation contract | Maintainer; backend | Inspect fresh focused evidence for immediate revalidation, non-force worktree removal, and worktree-before-branch ordering after the full gate. | Only sealed fresh proof reaches mutation; command ordering and arguments match the contract. | Correlate with QA-08 public Git state. | pass | Fresh ApplyTerminalRun family and race run passed ordering/argument assertions; public mixed apply removed only fresh safe state. |
| QA-18 | Task 03 AC4-AC6 transaction and replay contract | Maintainer; data/backend | Inspect fresh focused persistence-failure, stale-evidence, other-outcome, and idempotency evidence after the full gate. | No state/Git mutation on invalid or failed persistence; exact replay emits no duplicate transition/event. | Correlate with QA-09 to QA-11 fresh public reads. | pass | Fresh Store/worktree families passed invalid, stale, failure, outcome, and replay cases; public repeat kept identical DB/Git hashes and one event. |
| QA-19 | Docs surface; Task 06 AC1-AC7 | User/Agent; docs/CLI | Follow `CONTEXT.md`, command guide, usage guide, help, dry-run, JSON, apply, and Runs List instructions without source-only knowledge. Resolve cited links and terminology. | Reader can predict all five states; stdout/stderr and mutation boundaries are explicit; no age/outcome/missing-path/force deletion claim appears; Integration Pending, other outcomes, and Runs List routing match the CLI; no protected tooling changed in Task 06. | Execute documented commands against the isolated repository and compare help/copy to output. | pass | Built help and every documented flow matched. Glossary/state table, stdout/stderr, no-force, outcomes, and links resolved; Task 06 diff contains no protected tooling. |
| QA-20 | Task 07 AC1-AC2 and Agent workflow | Agent; docs/CLI | Read the shipped Skill, perform its prescribed dry-run-first flow, and compare canonical/generated copies. | Guidance requires inspection before apply, preserves dirty/unintegrated/unknown work, rejects manual Git deletion, and both copies are byte-identical. | `cmp`, Skill checks, and live CLI flow. | pass | Shipped Skill carries the dry-run/state/preservation/manual-deletion rules; `cmp`, Skill checks, and live dry-run-before-apply all passed. |
| QA-21 | Non-Goals: no integration or dirty repair; no failed Task Worktree deletion | User; CLI/backend | Apply mixed divergent, dirty, and Task Worktree-adjacent fixtures. | Unique commits are not integrated, dirty content is not repaired/selected, and failed Task Worktrees are outside reconciliation scope. | Compare commits, files, worktrees, and task refs before/after. | pass | Unique commit and both dirty files persisted. A sibling `-task_01` worktree/branch remained after its safe parent Run Branch was released. |
| QA-22 | Non-Goals: GC remains storage owner; no age/outcome/missing-path cleanup | User; CLI/backend | Inspect help/docs and reconcile fixtures that are old, terminal, or path-only without complete proof. | Reconcile does not prune Run Event Journal/artifact storage and never deletes from age, outcome, or one missing path alone. | Confirm retained artifacts/events and Git ref/path state. | pass | Reconciliation event remained readable after cleanup; missing-target and path-only states stayed preserved. Help/docs expose no retention/age deletion path and keep GC separate. |
| QA-23 | Non-Goal: no cross-repository bulk invocation | Agent; CLI | Seed retained Runs for two repositories and run no-ID reconcile in one. | Only current-repository Runs appear or mutate; cross-repository operation requires a separate invocation. | Re-read the other repository's Git and database state. | pass | Repository-scoped runs excluded all other fixture repositories; selected cross-repository Run exited 2 without DB/Git change. |
| QA-24 | High-risk behavior probes: repeated apply, out-of-order flags, invalid format/extra argument, concurrent stale proof | Agent; CLI | Run repeat, flags-before/after-ID, malformed format/arguments, and stale-evidence probes. | Replay is harmless; supported orderings agree; invalid input exits 2 without mutation; concurrency uncertainty preserves work. | Snapshot and independently re-read state after each probe. | pass | Repeat and ordering passed. Invalid format/extra arg/`--force`/missing value exited 2 with identical DB hash. Fresh race/stale proof checks passed. |
| QA-25 | Repository scope integrity / changed artifacts | Maintainer; repository | Compare all Spec Task commits and the full Spec diff to declared Task slices, authorized tooling scope, and repository artifact conventions. | No unexplained generated artifact or out-of-slice path ships with the Spec. | `git diff-tree`, file-type inspection, and clean current status. | fail | Task 04 commit `0d56f42` added root `roundfix`, a 20.1 MiB Mach-O arm64 executable. Base has no path; repository build contract writes ignored `bin/roundfix`. See RFQA-0038-01. |

## Acceptance-criterion traceability

- Task 01 AC1-AC7 → QA-05, QA-16.
- Task 02 AC1-AC7 → QA-08, QA-11-QA-13, QA-15, QA-17.
- Task 03 AC1-AC6 → QA-09-QA-10, QA-13, QA-18.
- Task 04 AC1-AC7 → QA-06, QA-08, QA-11, QA-13-QA-14.
- Task 05 AC1-AC6 → QA-07.
- Task 06 AC1-AC7 → QA-19.
- Task 07 AC1-AC6 → QA-03-QA-04, QA-20.

## Behavior probes

- Repeated apply targets destructive-operation idempotency (QA-11, QA-24).
- Out-of-order flags and invalid arguments target deterministic Agent use
  without Interactive Input (QA-24).
- Dirtying or advancing a fixture between inspection and apply targets stale
  proof at the deletion boundary (QA-12, QA-24).
- A locked worktree targets actionable partial-failure recovery without a
  force workaround (QA-13).
- Mixed safe and preserved states target repository-wide blast radius (QA-08).

## Findings

### RFQA-0038-01 — Task 04 committed a platform-specific root binary

- Impact: Friction.
- Actor and journey: maintainer or contributor reviewing, cloning, or
  integrating Spec 0038; QA-25 scope-integrity step.
- Expected: the Spec changes source, tests, docs, authorized Skill artifacts,
  and declared derived pins. Builds write the ignored application binary to
  `bin/roundfix`.
- Actual: Task 04 commit
  `0d56f4281b06602b6108a8d304885237a897f4d6` added repository-root
  `roundfix`, a 20.1 MiB Mach-O arm64 executable. Base `8ec92ad` has no such
  path; HEAD stores executable blob
  `01aaed49a73f1e33f4d2dc8bb9ca51208aaa4db9`.
- Reproduction:
  1. `rtk proxy git -c core.fsmonitor=false diff-tree --no-commit-id --name-only -r 0d56f4281b06602b6108a8d304885237a897f4d6`
  2. `rtk file roundfix`
  3. `rtk ls -lh roundfix`
  4. `rtk proxy git -c core.fsmonitor=false ls-tree 8ec92ad -- roundfix`
  5. `rtk proxy git -c core.fsmonitor=false ls-tree HEAD -- roundfix`
  6. `rtk rg -n 'bin/roundfix' Makefile README.md`
- Evidence:
  `qa/evidence/2026-07-27-terminal-reconciliation/run-summary.md` →
  “Tooling and scope audit.”
- Affected rows: QA-25.
- Required before rerun: remove the generated root binary from the delivered
  branch, keep the build output at ignored `bin/roundfix`, then rerun the full
  repository gate and QA-25 scope audit.

## Blocked and skipped

None.

## Coverage

Executed: 5/5 user stories, 9/9 Core Features, 5/5 Success Metrics, all 46 Task
acceptance criteria, 6/6 Non-Goals, four Project Constraint categories, four
declared surfaces, and five high-risk behavior probes.

Results: 24 pass, 1 fail, 0 blocked, 0 skipped, 0 pending.

Findings by impact: 0 Blocks-Completion, 0 Data-Loss, 0 Trust-Damage,
1 Friction, 0 Cosmetic.

## Final verdict

Fail. The assembled reconciliation behavior and every declared acceptance
criterion passed, but the delivered Spec contains one unexplained 20.1 MiB
platform-specific root binary; remove it and rerun the full gate plus QA-25.
