---
spec: 0082-the-manifest-already-answered-that
date: 2026-08-07
build: cda415f177144ebb6be60c95410ae144343392ce
status: closed
verdict: fail
rows_blocked_environment: 1
rows_blocked_finding: 1
rows_blocked_declared: 0
surfaces: [cli, docs]
---

# QA report — Baseline update

## Scope and environment

Full authored terminal QA gate for `task_08`. The build under test is the Run
Worktree commit named in frontmatter on branch
`roundfix/run-run_20260807T153133Z_cd76cdbfc21eed56`. The Spec target branch is
`ma/baseline-update-command`; no Pull Request is open, so any planned Pull
Request observation is environment-blocked and must be supported by equivalent
local evidence. All mutable rehearsal repositories were gate-created copies
under the operating-system temporary directory with the fixed
`roundfix-qa-0082-` prefix. No source repository outside this checkout may be
mutated.

Actors: fleet maintainer, one-off repository maintainer, first-adoption
maintainer, documentation reader, and QA operator. Public entry point:
`go run -buildvcs=false ./cmd/roundfix` or a freshly built equivalent binary.

The QA binary was built with
`rtk go build -buildvcs=false -o /private/tmp/roundfix-qa-0082-roundfix ./cmd/roundfix`
(exit 0). The nine adopted source repositories were read-only inputs; only the
listed temporary clones were mutated.

### Fleet reality

The nine adopted copies reproduce three Profile identities and six distinct
prior catalog digests, exactly as required by `task_08`.

| Copy | Profile | Prior catalog suffix | First `--yes --no-skills` outcome |
| --- | --- | --- | --- |
| `conexus` | `standard-typescript-monorepo` | `edd99b3d…78a1` | exit 3, 14 unaccounted clauses |
| `fiscus` | `standard-typescript-monorepo` | `3e67a4f1…8dbb` | exit 3, 14 unaccounted clauses |
| `fluxus` | `standard-typescript-monorepo` | `f082de03…8c1e` | exit 3, two modified managed markers |
| `tax_poc` | `standard-typescript-monorepo` | `daa5be17…b69` | exit 3, 14 unaccounted clauses |
| `vortex` | `standard-typescript-monorepo` | `11d71526…4bb` | exit 3, 14 unaccounted clauses |
| `gss` | `gss-apps-monorepo` | `c0435d25…a014` | exit 0, five file changes |
| `oraculum` | `oraculum-backend` | `11d71526…4bb` | exit 0, two file changes |
| `oraculum_wt` | `oraculum-backend` | `11d71526…4bb` | exit 0, two file changes |
| `oraculum_run` | `oraculum-backend` | `11d71526…4bb` | exit 0, two file changes |

The never-adopted copy came from OnionCry and had no Setup Manifest.

### Temporary-copy ledger

All entries were created below exact top-level roots
`/private/tmp/roundfix-qa-0082-audit.kqRikZ` and
`/private/tmp/roundfix-qa-0082-fleet.Aszdyf`. Both exact roots and the QA
binary were removed before close; each nonexistence check exited 0 and the
prefix sweep returned no path.

- `/private/tmp/roundfix-qa-0082-audit.kqRikZ/repo`
- `/private/tmp/roundfix-qa-0082-fleet.Aszdyf/conexus`
- `/private/tmp/roundfix-qa-0082-fleet.Aszdyf/fiscus`
- `/private/tmp/roundfix-qa-0082-fleet.Aszdyf/fluxus`
- `/private/tmp/roundfix-qa-0082-fleet.Aszdyf/gss`
- `/private/tmp/roundfix-qa-0082-fleet.Aszdyf/oraculum`
- `/private/tmp/roundfix-qa-0082-fleet.Aszdyf/oraculum_wt`
- `/private/tmp/roundfix-qa-0082-fleet.Aszdyf/tax_poc`
- `/private/tmp/roundfix-qa-0082-fleet.Aszdyf/vortex`
- `/private/tmp/roundfix-qa-0082-fleet.Aszdyf/oraculum_run`
- `/private/tmp/roundfix-qa-0082-fleet.Aszdyf/never_adopted`
- `/private/tmp/roundfix-qa-0082-fleet.Aszdyf/oraculum_confirmation`
- `/private/tmp/roundfix-qa-0082-fleet.Aszdyf/oraculum_profile_drift`
- `/private/tmp/roundfix-qa-0082-fleet.Aszdyf/oraculum_marker_edit`
- `/private/tmp/roundfix-qa-0082-fleet.Aszdyf/oraculum_missing_decision`
- `/private/tmp/roundfix-qa-0082-fleet.Aszdyf/fluxus_missing_decision`
- `/private/tmp/roundfix-qa-0082-fleet.Aszdyf/oraculum_skills`
- `/private/tmp/roundfix-qa-0082-fleet.Aszdyf/oraculum_owned_skills`
- `/private/tmp/roundfix-qa-0082-fleet.Aszdyf/oraculum_unreachable_skill`

Auxiliary moved-skill paths under the fleet root were `context7_saved`,
`roundfix_skill_saved`, and `humanizer_saved`; they are covered by the same
exact-root cleanup.

## Static gate

`rtk make verify` was run un-piped from the repository root. The first
sandboxed attempt was denied before the gate by the environment's blocked
access to `api.github.com`; the required full-access rerun reached the suite and
exited `2` after `rtk go test -parallel 16 ./...` reported 3,647 passed, one
failed, and three skipped tests. The failing check was
`TestCoverageEquivalence` in `internal/spec/coverage_test.go`.

Minimal reproduction:

```text
$ rtk proxy go test -buildvcs=false ./internal/spec -run '^TestCoverageEquivalence$' -count=1 -v
exit 1
coverage regression: package "roundfix/internal/cli" no longer executes "TestHumanBaselineIncompatibleManifestKeepsValidDefaults"
coverage regression: package "roundfix/internal/cli" no longer executes "TestHumanBaselineUpdate"
```

Root-cause evidence: commit `693cbccc` (Task 06) renamed/replaced both recorded
top-level tests, while the archived Spec 0071 coverage record did not change.
The additions logged by the check are informational; these two regressions are
the actual failure signal. This is code-caused, not the earlier network
boundary, and is Finding F-001.

## Results

Every row started pending before execution. For CLI rows, the persistence check
is a second public CLI read or a complete tree/region digest comparison after
the command. Evidence is recorded inline in this report. Evidence labels
`this report`, `Static gate`, and `temporary-copy ledger` resolve to this file
and its named sections; Spec artifact basenames resolve under
`docs/specs/0082-the-manifest-already-answered-that/`.

| # | Story / criterion / sweep | Actor and surface | Steps and expected observable | Independent confirmation / persistence | Evidence path | Status |
| - | --- | --- | --- | --- | --- | --- |
| 1 | Authored gate, dependency completion, and Project Constraints | QA operator; docs/Git | Validate `qa: task_08`, terminality, completed non-QA dependencies, PRD/TechSpec applicability rows, operative sources, and active ADR accounting. | Re-read Task frontmatter and cited repository guidance/ADRs. | `_tasks.md`, `_prd.md`, `_techspec.md`, `task_01.md` through `task_08.md` | pass — `task_08` is the unique terminal `qa` node, its only dependency and every non-QA Task are completed; both active Spec artifacts account for identifier strategy, authentication/HTTP, active ADRs, and tooling authority with operative sources. |
| 2 | Protected-tooling authorization, chronological commit shape, exact changed paths, and regenerated-pin provenance | QA operator; Git/tooling | Resolve authorization, prerequisite, Task, and any consequent commits; enumerate every committed path with `git diff-tree`; reproduce sanctioned regeneration. | Fresh regeneration in an isolated copy must produce no delta; canonical/mirror skills must be byte-identical. | `docs/workflow/authorizations/2026-08-07-baseline-update-command.md`; this report | pass — authorization `5c0e57f3` and Task-scope prerequisite `9340a52f` are ancestors of Task commit `cda415f1`; exact `diff-tree` paths match the Task boundary plus sanctioned derived ownership; isolated `make baseline-digests` returned `changed:false`; both skill pairs are byte-identical; no consequent commit exists. |
| 3 | Repository Verification | QA operator; CLI | Run `rtk make verify`; expect exit 0 with formatter, tests, Repository Skill Set, and build passing. | Read the un-piped command exit status and final summary. | This report, Static gate | fail — Finding F-001; full-access gate exit 2, 3,647 passed / 1 failed / 3 skipped; `TestCoverageEquivalence` reports two removed recorded tests. |
| 4 | Public update help and structured contract | Fleet maintainer; CLI | Run `baseline update --help`; expect every documented flag, result schema, and exits `0/1/2/3/130`. Probe mutually exclusive confirmation flags. | Follow the user guide independently and compare stderr/stdout and exit categories. | `docs/user-guide/context-driven-development.md`; this report | pass — built binary help exits 0 and names all seven flags, schema, and five exits; `--yes` plus `--confirm-plan` exits 2 with category `invalid`. |
| 5 | Story 1 — non-interactive stale adopted repository refresh | Fleet maintainer; CLI | On a real adopted repository copy, run `baseline update --repo <copy> --yes --format json`; expect no questions, managed refresh, structured per-repository result, exit 0. | Fresh JSON read plus current Setup Manifest/catalog identity after apply. | This report; temporary-copy ledger | pass — real GSS and Oraculum copies applied with exits 0 and 5/2 changed files respectively; result schema and approved Plan Digests were present and the copied manifests reloaded at the current catalog. Fleet-wide completion is evaluated separately in row 17. |
| 6 | Idempotence success metric | Fleet maintainer; CLI | Run the same update immediately again; expect exit 0 and zero file changes. | Compare complete repository tree digest before and after the second invocation. | This report; temporary-copy ledger | pass — second GSS update exits 0 with `fileChanges: []` and `idempotence: verified`; the complete binary diff digest remained `1bfb3441…169c1` before and after. |
| 7 | Story 2 — interactive adopted update asks only what is new | One-off maintainer; CLI | Run the interactive Baseline command on an adopted copy; expect direct Plan confirmation, zero settled-decision prompts, and no semantic analyzer/ACP process. Probe plan rejection. | Observe child-process creation and prove the declined Plan leaves the tree byte-identical. | This report; temporary-copy ledger | pass — adopted Oraculum announces `update` and reaches `Prompt 1: Final confirmation` directly; live PID 31158 had no child process; decline exits 3 and preserves binary diff digest `6348ab5c…a0c4`. |
| 8 | Story 3 — repository-authored bytes remain byte-identical | Repository maintainer; CLI | Refresh a real adopted copy containing authored prose and Repository-Specific Normative Rules. | Exact pre/post digest comparison of every non-managed region and exact block diff after reload. | This report; temporary-copy ledger | pass — every touched GSS/Oraculum guide's ordered non-managed byte regions compare equal; GSS `AGENTS.md` blob remained `94ca200d…c0b7` and `specific-repository.md` remained `13a971f5…38c`, confirmed by exact `HEAD`/working-tree blob identity. |
| 9 | Story 4 — guidance and Repository Skill Set refresh together | Repository maintainer; CLI | Drift/remove owned and external skills in a copy; apply update; expect installed/restored names and independent skills status. | Re-run Repository Skill Set check and read installed files from the public repository surface. | This report; temporary-copy ledger | fail — owned-skill reinstall passes (`installedCount: 14`, `skills check` exit 0), but real external restoration fails in two seams: Finding F-002 blocks a missing required skill before the stage, and Finding F-003 makes an optional missing skill on a repository-owned Profile exit 1 after guidance apply. |
| 10 | External skill upstream unreachable degrades to warning | Repository maintainer; CLI | Refresh with an unreachable immutable source; expect guidance apply exit 0, named drifted skill/reason, apply verified, skills warning. | Re-read Setup Manifest and skill status after command. | This report; temporary-copy ledger | blocked (finding: F-003 — waits on repository-owned Profile restoration) — the real optional-skill case exits 1 at Profile resolution inside restore before the immutable source can be exercised. |
| 11 | Story 5 — newly required decision stops without writing; explicit suggestion adoption applies | Fleet maintainer; CLI | Use a `standard-typescript-monorepo` copy missing a required decision; first expect exit 3 with exact ID and no writes, then run `--adopt-suggested --yes` and expect the adopted value listed. | Whole-tree digest for refusal, then Setup Manifest read after explicit adoption. | This report; temporary-copy ledger | pass — both real Fluxus and Oraculum copies missing only `secondbrain.enabled` exit 3 naming exactly that ID with unchanged diff digests; Oraculum `--adopt-suggested --yes` exits 0 and lists the adopted value. Fluxus independently proves the standard-profile decision path before its pre-existing marker conflict. |
| 12 | Story 6 — unaccounted managed clause fails closed | Repository maintainer; CLI | Use an adopted copy/catalog transition with an unaccounted prior managed clause; expect preview/apply blocked, clause named, and no writes. | Whole-tree digest and structured Clause Delta from a second read. | This report; temporary-copy ledger | pass — Conexus, Fiscus, Tax PoC, and Vortex each exit 3/category `classification`, name all 14 unaccounted clauses, emit `ClauseDelta`, and remain clean after the attempt. |
| 13 | No manifest remains first adoption | First-adoption maintainer; CLI | Run update on a real never-adopted repository copy; expect exit 3, first-adoption direction, and no writes. Then enter interactive Baseline and observe the full interview start. | Whole-tree digest after refusal; prompt sequence from the public CLI. | This report; temporary-copy ledger | pass — OnionCry copy exits 3/category `adoption`, directs `roundfix baseline`, remains clean, and interactive entry announces `adoption` then presents `Prompt 1: Instruction preservation`. |
| 14 | Hand-edited managed marker blocks safely | Repository maintainer; CLI | Edit one copied managed marker and invoke update; expect invalid/unsafe refusal naming the path. | Whole-tree digest proves the command added no writes after the deliberate precondition edit. | This report; temporary-copy ledger | pass — copied `skill-dispatch.md` marker edit yields exit 3 and `baseline.preservation.managed-marker.modified` naming the path; deliberate-diff digest `ae96e56d…c6e1` is identical before/after. |
| 15 | Confirmation-gated apply and invalid/out-of-order input probes | One-off maintainer; CLI | Preview without confirmation, confirm the exact prior digest, reject stale digest, and pass `--yes` plus `--confirm-plan`; expect no-write preview/action-required, exact-digest apply, stale-digest action-required, mutual-exclusion invalid input. | Tree digests around each step and fresh JSON result reads. | This report; temporary-copy ledger | pass — preview exits 3/category `approval` with digest `90940844…a383` and a clean tree; exact digest applies at exit 0; reusing it against current Plan `7e9920e5…e05a` exits 3 without changing diff digest; mutually exclusive forms exit 2/category `invalid`. |
| 16 | Moved profile digest remains an update | Repository maintainer; CLI | Alter only the copied manifest's stored profile digest while keeping profile/decisions valid; expect update rather than adoption and no re-interview. | Structured result plus Setup Manifest identity after apply/reload. | This report; temporary-copy ledger | pass — zeroed stored digest on a real Oraculum copy still applies as `state: verified`, then reload shows resolved Profile digest `dcc06a50…0cd8`; no adoption interview occurs. |
| 17 | Fleet-shape sweep across several real repository copies and three profile identities | Fleet maintainer; CLI | Execute a shell loop over adopted and never-adopted copies; expect per-repository JSON/exit categories branchable without prose parsing and stop only on genuine action-required cases. | Parse each result schema/category and independently inspect each copy's post-state. | This report; temporary-copy ledger | fail — Finding F-004. The loop itself branches correctly (`gss=0`, `oraculum=0`, `conexus=3`, `never_adopted=3`), but four of five real `standard-typescript-monorepo` fleet copies stop on the same 14 unaccounted clauses and the fifth on marker drift; the promised nine-repository refresh cannot complete. |
| 18 | Docs journey and published examples | Documentation reader; docs/CLI | Follow the update instructions without undocumented knowledge; execute every published update example through its own parser. | CLI help and example-contract behavior agree with the guide. | `docs/user-guide/context-driven-development.md`; this report | pass — focused documentation/example filter reports 4 passed; built help and all flags/exits match the guide. Findings F-002/F-003 identify product behavior behind two documented skill claims, not parser or guide-shape defects. |
| 19 | Non-Goals and scope-creep sweep | QA operator; CLI/docs | Check first adoption unchanged; no implicit Profile change; no profile remediation choice; authored rules not re-filed; no fleet driver; immutable Git acquisition unchanged; edited markers not repaired. | Inspect public command/help/results and affected copies after fresh reads. | `_prd.md`; this report | pass — first adoption starts the full interview; updates retain their Profile; marker conflicts and unaccounted retention refuse without repair; authored rules stay byte-identical; help exposes one-repository operation only and the existing immutable-source/offline-source contract. |
| 20 | Pull Request observation | Maintainer; Pull Request/Git | No Open Pull Request exists, so approval/check/thread/Merge-Ready observation is unreachable in this Run Worktree. | Equivalent evidence: authoritative local Verification, complete Git ancestry/scope audit, and local CLI journeys; no PR state will be invented from the Run Branch. | This report | blocked (environment: no open Pull Request) — local commit ancestry/scope and CLI evidence were observed, but `make verify` is red under F-001, so the absent PR has no equivalent green-check evidence. |
| 21 | Temporary-copy containment and cleanup | QA operator; filesystem/Git | Track every created `roundfix-qa-0082-*` path; remove only those exact paths before close. | Exact-path nonexistence checks, prefix sweep, and repository status prove no copy remains. | This report, temporary-copy ledger | pass — both exact temporary roots and the QA binary were removed; three exact nonexistence checks exit 0, `/private/tmp` prefix sweep is empty, and worktree status contains only this QA Report. |

## Findings

### F-001 — Task 06 removed recorded test identities without updating the coverage contract

- Impact: **Trust-Damage**. The feature's focused tests may pass, but the only
  repository gate is red, so a maintainer cannot trust or deliver this build as
  verified.
- Actor and step: QA operator runs the public repository Verification before
  exercising the assembled CLI.
- Expected: `rtk make verify` exits 0.
- Actual: exit 2; `TestCoverageEquivalence` reports that
  `TestHumanBaselineIncompatibleManifestKeepsValidDefaults` and
  `TestHumanBaselineUpdate` no longer execute.
- Reproduction: `rtk proxy go test -buildvcs=false ./internal/spec -run
  '^TestCoverageEquivalence$' -count=1 -v` exits 1 with those two regressions.
- Root cause: Task commit `693cbccc` replaces those test functions with new
  names/coverage, but
  `docs/specs/_archived/0071-verification-cost/coverage-record.json` is unchanged
  across `003b6d97..HEAD`.
- Affected rows: 3 directly. Other CLI journeys remain runnable because the
  binary compiles and the failure is the coverage-record invariant, not the
  command entry point; their results cannot erase this failed authoritative
  gate.

### F-002 — A missing required external skill blocks before update can restore it

- Impact: **Blocks-Completion**. A maintainer cannot use the promised one-act
  refresh to repair a repository whose required Repository Skill Set member is
  missing.
- Actor and step: repository maintainer moves the real external `context7`
  skill out of an adopted Oraculum copy, then runs `baseline update --yes
  --skills-source-dir /Users/marcio/dev/skills --format json`.
- Expected: Profile input resolves, guidance applies, and the post-apply skill
  stage restores `context7` through preview and exact Plan confirmation.
- Actual: exit 3 before Plan/apply, category `decision`, message `required
  profile alignment is unresolved: capability.context7`; `skills.status` is
  `not_run`.
- Reproduction path: temporary `oraculum_skills` copy from the ledger.
- Affected rows: 9. Row 10 uses an optional skill to get past this earlier
  boundary.

### F-003 — External restore rejects repository-owned Profiles after guidance apply

- Impact: **Blocks-Completion**. The guidance half applies, but a missing
  external skill remains missing and the documented warning/continuation
  contract is unreachable on a repository-owned Profile.
- Actor and step: repository maintainer moves optional real external skill
  `humanizer` out of an adopted Oraculum copy, then runs `baseline update
  --yes --format json`.
- Expected: an unreachable immutable source becomes a named drift warning with
  exit 0; a reachable source restores the skill.
- Actual: exit 1 after guidance apply; top-level `state` is `verified`, category
  is `skills`, skills status is `failed`, and the error is `restore external
  skill "humanizer": Unknown built-in Baseline Profile "oraculum-backend"`.
- Reproduction path: temporary `oraculum_unreachable_skill` copy from the
  ledger.
- Affected rows: 9 and 10. Row 10 is finding-blocked because execution never
  reaches immutable-source acquisition.

### F-004 — The standard TypeScript fleet cannot reach the current catalog through update

- Impact: **Blocks-Completion**. The fleet sweep promised by the PRD cannot
  bring any of the five real standard-TypeScript copies current.
- Actor and step: fleet maintainer runs the built update command with `--yes
  --no-skills --format json` over all nine adopted copies.
- Expected: each compatible adopted repository applies, or stops for a newly
  required decision with that decision named.
- Actual: Conexus, Fiscus, Tax PoC, and Vortex all stop with the same 14
  unaccounted managed clauses; Fluxus stops on two modified managed markers.
  None of the five standard-profile copies reaches the current catalog.
- Reproduction: the Fleet reality table and the four-repository scripted loop
  (`gss=0`, `oraculum=0`, `conexus=3`, `never_adopted=3`). Conexus remains
  clean after refusal and exposes all 14 entries in structured `ClauseDelta`.
- Affected rows: 17. Row 12 passes because fail-closed retention itself is
  correct; the defect is that the authored fleet-success promise is not met.

## Blocked and skipped

- Row 10 is `blocked (finding: F-003 — waits on repository-owned Profile
  restoration)`. Unblock by resolving the repository-owned Profile through the
  restoration boundary before source acquisition, then rerun both reachable
  and unreachable immutable-source cases.
- Row 20 is `blocked (environment: no open Pull Request)`. Unblock by opening a
  Pull Request on `ma/baseline-update-command` after the product findings and
  red Verification are repaired. Commit ancestry and local journeys are
  equivalent evidence for scope and behavior only; there is no equivalent
  green-check evidence while F-001 remains.
- No rows are declared-blocked or skipped.

## Coverage

All 21 planned rows are terminal: 16 pass, 3 fail, 1 environment-blocked, and 1
finding-blocked; zero are pending, declared-blocked, or skipped. Strict user
story coverage is 4/6 passed: Stories 2, 3, 5, and 6 pass; Story 1 fails its
fleet outcome under F-004; Story 4 fails external-skill restoration under
F-002/F-003. Core Features are 8/9 passed, with Core Feature 5 failing.

The ten authored rehearsal cases close as 7 pass, 2 fail, and 1
finding-blocked. The CLI and docs surfaces, three Profile identities, six prior
catalog digests, all nine adopted copies, one never-adopted copy, relevant
Non-Goals, static Verification, Project Constraints, tooling authority, and
cleanup were exercised. Probes covered idempotent resubmission, Plan preview
and reopen, stale and mutually exclusive confirmation, missing decisions,
edited markers, missing/unreachable skills, Profile-digest drift, process-child
observation, and first-adoption fallback.

Blocked counts: `rows_blocked_environment: 1`, `rows_blocked_finding: 1`,
`rows_blocked_declared: 0`. Finding impact counts: 3 Blocks-Completion, 1
Trust-Damage, 0 Data-Loss, 0 Friction, and 0 Cosmetic.

## Final verdict

**Fail.** Repair the coverage-record regression, make required and optional
external skill restoration work for repository-owned Profiles (including the
unreachable-source warning path), and supply a retention path that lets the
real standard-TypeScript fleet meet the authored update promise; then rerun the
full gate. Counts: 16 pass, 3 fail, 1 environment-blocked, 1 finding-blocked;
findings are 3 Blocks-Completion and 1 Trust-Damage.
