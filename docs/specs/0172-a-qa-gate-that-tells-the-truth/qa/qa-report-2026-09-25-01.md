---
spec: 0172-a-qa-gate-that-tells-the-truth
date: 2026-09-25
build: ebac7cbeda673669e384ace6708f998844b426ec
auditing_binary: "0.17.0 (256ad156, built 2026-09-25 15:19:40 -0300)"
auditor_staleness: "stale: commit ancestry: build commit predates audited tree"
status: closed
verdict: pass
rows_blocked_environment: 1
rows_blocked_finding: 0
rows_blocked_declared: 0
surfaces: [backend, cli, docs]
---

# QA Report

## Scope and environment

- Full terminal gate for build `ebac7cbeda673669e384ace6708f998844b426ec` on the Run Worktree branch.
- Strict authoring precondition: `roundfix spec check 0172-a-qa-gate-that-tells-the-truth --strict` exited `0` with no findings. It skipped only the absent findings-rollup detector and the inapplicable Vocabulary Contract detector.
- User-flow binary: `bin/roundfix`, built from the audited tree as `roundfix 0.17.0 (ebac7cbe-dirty, built 2026-09-25 19:22:22 -0300)`; the dirty marker is the untracked seeded QA Report.
- The Daemon already ran repository Verification outside this Agent sandbox. This gate records that fact and does not rerun it.
- Pull Request fact: none open; Pull Request journeys are environment-blocked.

## Static gate

- `make verify-changed` — pass, exit `0`. The Daemon ran this outside the Agent sandbox and removed diagnostics on success. The Agent recorded this supplied gate fact and did not rerun the command.

## Performed repairs

None.

## Assigned repair failures

None.

## Authorization audit inputs

None.

## Mechanical findings

None.

## Results

| # | Story / criterion / sweep | Actor and surface | Status | Provenance | Evidence |
| - | --- | --- | --- | --- | --- |
| 1 | Repository Verification | Maintainer; repository gate | pass | Requirement 1; repository Verification | Daemon-supervised `make verify-changed`: exit `0`; diagnostics removed on success. |
| 2 | QA Report acceptance and archive refusal through the built CLI | Maintainer; CLI | pass | Requirement 2 | Built binary produced the five expected accept/refuse results and refused archive without moving the disposable Spec. |
| 3 | QA Task settlement for untouched and hollow reports | Daemon operator; backend | pass | Requirement 3 | Fresh settlement tests passed for the untouched pending seed and the hollow-pass cause. |
| 4 | Collision-safe QA Report allocation and newest selection | Daemon operator; backend | pass | Requirement 4 | Four fresh allocator tests passed, including gap, sequence-one, fresh-date and later-date cases. |
| 5 | Independent Verification retains the deterministic first-run failure | Daemon operator; backend | pass | Requirement 5 | Fresh merge and Task-cycle tests retained the first diagnostic and used no repair turn. |
| 6 | Unobserved Verification projects as unknown | Supervisor; Run Event Stream | pass | Requirement 6 | Four projection tests passed; deterministic failures remained unclassified. |
| 7 | Event replay survives vacuous and malformed journal records | Supervisor; CLI | pass | Requirement 7 | The built binary replayed disposable current-schema Run Databases with exit `0`, the expected JSONL and one malformed-record warning. |
| 8 | Archived Roundfix and external Fiscus reports remain eligible | Maintainer; CLI and external repository | pass | Requirement 8; outside-evidence row | 122 Roundfix archived pass Specs and all 41 newest Fiscus pass reports were accepted. |
| 9 | User guidance and shipped skills describe the current contracts | Maintainer; docs | pass | Requirement 9 | Skill synchronization passed and each required phrase matched behavior observed through the built CLI and event projection. |
| 10 | Spec promises name consuming Tasks | Spec author; docs | pass | Requirement 10 | Current built binary's strict Spec check exited `0` with no findings. |
| 11 | Pull Request controls have equivalent evidence | Reviewer; Pull Request | blocked (environment: no open Pull Request) | Pull Request row | No PR exists; approval, checks, threads, Merge-Ready and ancestry states are recorded below. |

## Row evidence

### 1

- Entry and steps: recorded the Daemon's supplied repository-gate fact for `make verify-changed`.
- Expected and observed: exit `0`; the Daemon reported pass and removed diagnostics on success.
- Independent confirmation and persistence: the command result is Daemon-supervised evidence for this exact Run Worktree head. Per the QA contract, the Agent did not rerun it.
- Status: pass.

### 2

- Entry and steps: invoked `bin/roundfix qa-report accept <path>` as a maintainer against five reports in disposable Spec directories, then invoked `bin/roundfix archive archive-demo` in a disposable Git repository whose newest report was a hollow `pass`.
- Expected and observed: `pending` exited `1` with `expected "pass"`; hollow `pass` exited `1` with `records no QA row`; a Results row, an external `Status` table, and no `## Results` section each exited `0`; archive exited `2` with `records no QA row`.
- Independent confirmation and persistence: every shape used a separate binary process. After archive refusal, the active Spec remained in place and no archive destination was created.
- Status: pass.

### 3

- Entry and steps: ran `go test -count=1 -v` for `TestQASettlementRefusesAnUntouchedSeed` and `TestQASettlementRefusesAHollowPass` in `./internal/daemon`.
- Expected and observed: both tests passed. The first observed a failed QA Task with verdict `pending`; the second observed `QA verdict pass not accepted` with the hollow-row cause.
- Independent confirmation and persistence: `-count=1` forced a fresh execution; each test reloads the settled Task artifact.
- Status: pass.

### 4

- Entry and steps: ran the four `TestMechanicalQAReport*` allocation cases in `./internal/daemon` with `-count=1`.
- Expected and observed: fresh date used the unsuffixed name, the next report used `-01`, a gap below `-02` produced `-03` and `NewestQAReport` returned it, and a later-dated report refused allocation. All passed.
- Independent confirmation and persistence: each successful test reread the QA directory through `NewestQAReport`; the refusal test confirmed the directory stayed unchanged.
- Status: pass.

### 5

- Entry and steps: ran `TestTemporaryOnRetryKeepsTheFirstRunDeterministicFailure`, `TestPassOnRetryReplacesTheFirstRunFailure` and `TestIndependentVerificationTemporaryRetryNamesTheDeterministicFailure` with `-count=1`.
- Expected and observed: all passed. The Task-cycle case retained the initial diagnostic path, settled failed and opened no Verification Feedback repair turn; the pass case removed the superseded first-run failure.
- Independent confirmation and persistence: the Task-cycle test reread the durable Task reason after settlement.
- Status: pass.

### 6

- Entry and steps: ran the four `Test*Verification*Projects*` cases in `./internal/daemon` with `-count=1`.
- Expected and observed: both unknown events projected `verification_unknown`, command, reason and diagnostic path; missing evidence projected `unavailable`; precondition metadata was overridden; a deterministic command verdict had no classification. All passed.
- Independent confirmation and persistence: each case passed the published payload through `runevent.ProjectStreamEvent` and compared the projected public fields value by value.
- Status: pass.

### 7

- Entry and steps: a QA-only overlay seeded two disposable current-schema Run Databases, then invoked the built `bin/roundfix events <run-id>` process as a Supervisor for each. The tracked tree was not changed by the overlay.
- Expected and observed: the vacuous Run exited `0`, wrote no stderr and emitted `verification_vacuous` with both commands. The malformed Run exited `0`, warned once with cursor `1`, kind `daemon.batch` and `missing payload field "phase"`, then emitted the cursor-`2` outcome JSONL record.
- Independent confirmation and persistence: each invocation reopened the Run Database through the binary's public read-only command; `TestQABuiltBinaryReplaysVacuousAndMalformedRecords` passed with `-count=1`.
- Status: pass.

### 8

- Entry and steps: ran `TestArchivedPassCorpusRemainsArchiveEligible` with `-count=1`, then enumerated the read-only Fiscus mirror at `/Users/marcio/dev/secondbrain/projects/fiscus/mirror/docs/history/specs/`, selected the highest date and numeric suffix per Spec, and invoked the built `roundfix qa-report accept` for every newest report whose verdict was `pass`.
- Expected and observed: the Roundfix corpus test accepted 122 archived pass Specs. The external mirror held 136 reports across 45 Specs; 41 newest reports were `pass`, and all 41 built-binary invocations exited `0` with no refusal.
- Independent confirmation and persistence: Roundfix's corpus and the Fiscus mirror are separate repositories this Spec did not author. Every external report was read by a fresh built-binary process from the mirror's current bytes.
- Status: pass.

### 9

- Entry and steps: ran `make skills-sync-check`; searched both canonical and shipped skill copies, `docs/user-guide/commands.md` and `CONTEXT.md` for the pending seed, hollow-row refusal, highest-suffix allocation, `verification_unknown`, `verification_vacuous` and malformed-record warning contracts.
- Expected and observed: `make skills-sync-check` exited `0`; every required phrase was present in the owning document and both skill copies. The built CLI and event commands in this run independently produced each documented behavior.
- Independent confirmation and persistence: the synchronization test compared generated ownership, while fresh public-command invocations exercised the described CLI outcomes.
- Status: pass.

### 10

- Entry and steps: invoked `bin/roundfix spec check 0172-a-qa-gate-that-tells-the-truth --strict` from the audited worktree.
- Expected and observed: exit `0`, `No findings`, and no authored Verification command executed. The only skips were the absent findings-rollup detector and the inapplicable Vocabulary Contract detector.
- Independent confirmation and persistence: the command reparsed the current PRD, TechSpec, Task Graph, Tasks and citations through the built CLI.
- Status: pass.

### 11

- Entry and steps: applied the supplied Run fact that no Pull Request is open for the Spec target branch and recorded each PR control instead of probing the never-pushed Run Worktree branch.
- Expected and observed: approval — no Pull Request acceptance has been obtained yet; checks and status — Daemon-supervised `make verify-changed` passed at build `ebac7cbeda673669e384ace6708f998844b426ec`; unresolved review threads — none can stand because no Pull Request exists; Merge-Ready acceptance — no Review Source evidence exists yet; review-artifact ancestry — no review artifact exists, and this row claims only head `ebac7cbeda673669e384ace6708f998844b426ec`.
- Independent confirmation and persistence: the prompt separately identifies the target branch and states `Pull Request: none open`; local Git resolved the claimed head. Opening or mutating a Pull Request is outside this gate's authority.
- Status: blocked (environment: no open Pull Request). Equivalent evidence answers every control, so this environment block does not cap the verdict.

## Findings

None.

## Blocked and skipped

- Row 11 is blocked by the declared environment fact `no open Pull Request`. Unblocking action: open a Pull Request on `roundfix/deliver-0172-a-qa-gate-that-tells-the-truth-1780e60e51f9a229`; do not open one on the never-pushed Run Worktree branch. Equivalent evidence for each Pull Request control is recorded in row 11.
- No QA row was skipped, finding-blocked or declared-blocked. The preserved mechanical skips below name absent detector inputs, not skipped matrix coverage.

## Coverage

- Coverage sources: 12/12 covered — Requirements 1-10, the repository Verification, the outside-evidence row and the Pull Request row. Requirement 1 shares the repository Verification row; Requirement 8 shares the outside-evidence row.
- Matrix rows: 11/11 terminal — 10 pass, 1 environment-blocked, 0 fail, 0 finding-blocked, 0 declared-blocked, 0 skipped and 0 pending.
- `rows_blocked_environment: 1`; `rows_blocked_finding: 0`; `rows_blocked_declared: 0`.
- The PRD declares no `## Unreachable Acceptance` entries and no frontend surface, so neither adds a matrix row.

## Final verdict

Pass: every runnable row passed, the one environment-blocked Pull Request row records its cause and equivalent evidence for every control, and no row failed, remained pending, or was finding-blocked, declared-blocked or skipped.

## Mechanical skips

| Detector | Missing artifact |
| --- | --- |
| authorization bounded paths | Task commits |
| consequent-fix commit order | consequent-fix declaration |
| QA evidence paths | docs/specs/0172-a-qa-gate-that-tells-the-truth/qa/evidence |
