---
spec: 0002-acpx-migration
date: 2026-07-05
verdict: pass
surfaces: [cli, infra]
---

# QA Report - ACPX Migration

## Summary

The ACPX migration passed this QA gate across the CLI and infra surfaces. The required static gate, public CLI checks, focused session/cancel/journal tests, SDK-removal checks, docs/skill drift checks, completed-spec flow, and real-acpx integration all passed with fresh evidence from this run. Sandbox denials for `~/.roundfix` and `~/.acpx` were resolved with approved full-access reruns and did not leave any blocked criteria.

## Results

| # | Criterion / user story | Verdict | Evidence |
| - | ---------------------- | ------- | -------- |
| 1 | Preconditions: all task files for this Spec are completed before QA runs. | pass | `rtk rg -n "^(status:\|# \|## \|### \|- \[\|surfaces:\|Surfaces\|User story\|Acceptance\|Non-Goal\|Non-goal\|Result\|Verdict)" docs/specs/0002-acpx-migration` showed `task_01.md` through `task_06.md` with `status: completed`. |
| 2 | Static gate: repository verification passes before flow-level QA. | pass | `rtk make verify` exited 0: `rtk go test ./...` reported 479 passed in 16 packages, `roundfix skills check` passed, and `go build -buildvcs=false -o bin/roundfix ./cmd/roundfix` passed. |
| 3 | CLI surface remains stable: no new commands or flags, expected command help remains available. | pass | `rtk ./bin/roundfix --help`, `rtk ./bin/roundfix resolve --help`, `rtk ./bin/roundfix implement --help`, and `rtk ./bin/roundfix --version` exited 0. Help lists fetch, resolve, watch, implement, stop, attach, and skills; version output was `roundfix 0.0.0-dev`. |
| 4 | User Story 1: consecutive Batches and Tasks in one Run reuse one warm Agent Session. | pass | `rtk go test ./internal/cli/ -run 'TestRunResolve(UsesOneAgentSessionPerRunAndCloses\|ClosesAgentSessionForTerminalOutcomes)\|TestRunWatchReusesOneAgentSessionAcrossRoundsAndCloses\|TestRunResolveACPXProbeFailureReportsActionablePreflight\|TestResolveJournalsAgentRunEventsDurably\|TestAttachReplaysCompletedRunReadOnly' -count=1` passed 12 tests. `rtk go test ./internal/cli/ -run 'TestRunImplement(UsesOneAgentSessionPerRunAndCloses\|ClosesAgentSessionForTerminalOutcomes\|PreflightProbeFailureCreatesNoRun\|StopRequestEndsStoppedWithInterruptMapping)\|TestAttachReplaysCompletedSpecRunReadOnly' -count=1` passed 9 tests. |
| 5 | User Story 2: a runtime crash mid-Run resumes on the next Work Item instead of failing the Run. | pass | `rtk env ROUNDFIX_REAL_ACPX=1 go test ./internal/agent/ -run TestRealACPXCommandOverrideRoundTripCancelCrashResume -count=1 -v` passed with approved access to `~/.acpx`. The test covers pinned-acpx round-trip, cooperative cancel, induced crash-resume, and teardown. |
| 6 | User Story 3: Stop Request semantics stay cooperative and immediate, with stop behavior preserved. | pass | The 28-test agent slice passed `TestACPXRunCancelsPromptCooperatively` and `TestACPXRunKillsPromptAfterCancelGracePeriod`; the 9-test implement slice passed `TestRunImplementStopRequestEndsStoppedWithInterruptMapping`. |
| 7 | User Story 4: full-access opt-in applies runtime modes as before. | pass | `rtk go test ./internal/agent/ -run 'TestACPX(PromptArgsPlaceGlobalsBeforeAgentAndSubcommand\|RunPromptSendsPromptOnStdin\|RunPromptPublishesUpdateLinesAndCapturesStopReason\|ExitCodeMapping\|RunEnsuresSessionOncePerRunnerAndSessionName\|RunAppliesFullAccessSessionSetup\|RunFailsSetupWhenFullAccessModeFails\|RunWarnsWhenCodexSandboxPresetUnavailable\|RunCancelsPromptCooperatively\|RunKillsPromptAfterCancelGracePeriod\|EndSessionClosesBestEffort\|Probe)' -count=1` passed 28 tests. |
| 8 | User Story 5: custom stdio runtime command override remains the escape hatch. | pass | The 28-test agent slice passed command construction and command-override probe coverage. The real-acpx integration passed through the command-override echo agent. |
| 9 | User Story 6: Attach and replay render from unchanged raw Run Event Journal payloads. | pass | The 12-test resolve/watch slice passed `TestResolveJournalsAgentRunEventsDurably` and `TestAttachReplaysCompletedRunReadOnly`; the 9-test implement slice passed `TestAttachReplaysCompletedSpecRunReadOnly`. |
| 10 | User Story 7: missing or wrong acpx version produces one actionable Preflight Validation message before any Run is created. | pass | `rtk acpx --version` returned `0.12.0`. The 28-test agent slice passed matching, missing, mismatched, and command-override probe cases. The 12-test CLI slice passed `TestRunResolveACPXProbeFailureReportsActionablePreflight`; the 9-test implement slice passed `TestRunImplementPreflightProbeFailureCreatesNoRun`. |
| 11 | Core Feature 1: acpx is the only path to ACP Runtimes and the version is pinned and verified. | pass | `rtk rg -n "PinnedACPXVersion\|func acpxInstallCommand\|version mismatch\|was not found" internal/agent/acpx_runner.go internal/cli/cli_test.go` found `PinnedACPXVersion = "0.12.0"` and the install command builder. `rtk acpx --version` returned `0.12.0`. |
| 12 | Core Feature 2: a Run creates one named Agent Session and closes it at terminal outcome; sessions do not cross Runs. | pass | The 12-test resolve/watch slice, 9-test implement slice, and `rtk go test ./internal/daemon/ -run 'TestTaskCycle(ExecutesAgentVerifySettleCommitContract\|StopDuringAgentPreservesTaskAndHalts\|QAVerdictMatrixSettlesRunAndCommitsReport\|QAOnlyRunWhenEveryTaskAlreadyCompleted)' -count=1` all passed. |
| 13 | Core Feature 3: runtime selection parity is preserved for codex, claude, opencode, model override, and command override. | pass | Public help still lists `--agent`, `--model`, and `--agent-command`. The 28-test agent slice passed argument-order and command-override coverage. |
| 14 | Core Feature 4: permission parity is preserved for blanket approval and full-access modes. | pass | The 28-test agent slice passed `TestACPXRunAppliesFullAccessSessionSetup`, `TestACPXRunFailsSetupWhenFullAccessModeFails`, and `TestACPXRunWarnsWhenCodexSandboxPresetUnavailable`. |
| 15 | Core Feature 5: raw ACP protocol stream is captured into the Run Event Journal without schema changes. | pass | The 28-test agent slice passed raw update journaling with stop reason; the 12-test CLI slice passed durable resolve journaling and attach replay. |
| 16 | Core Feature 6: acpx exit codes map to existing outcomes. | pass | The 28-test agent slice passed `TestACPXExitCodeMapping`, covering success, agent/protocol errors, timeout, permission denial, usage error, missing session, and Stop Request mapping. |
| 17 | Core Feature 7: Stop Request sends cooperative cancel and preserves Run state/history. | pass | Agent cancellation tests, implement Stop Request test, and daemon stop-preservation test all passed in the focused slices. |
| 18 | Core Feature 8: every terminal outcome closes the Agent Session and no owner process outlives the integration test. | pass | CLI terminal-path slices passed for resolve and implement; `rtk go test -race ./internal/daemon/ -count=1` passed 46 tests; the real-acpx integration passed teardown/orphan checks. |
| 19 | Task 01: acpx prompt invocation, `--json-strict` NDJSON parsing, agent log handling, and exit-code mapping work as specified. | pass | The 28-test agent slice passed command construction, stdin prompt delivery, raw update journaling with stop reason, and exit-code mapping cases. |
| 20 | Task 02: Agent Session lifecycle works: ensure once, full-access setup, cooperative cancel, kill fallback, and best-effort close. | pass | The 28-test agent slice passed ensure-once, full-access setup, setup failure, unsupported Codex sandbox warning, cooperative cancel, kill fallback, and `EndSession` best-effort checks. |
| 21 | Task 03: pinned acpx version and Preflight Validation behavior match the install command. | pass | `rtk acpx --version` returned `0.12.0`; code defines the same pin and install command; probe tests passed in the 28-test agent slice. |
| 22 | Task 04: resolve, watch, and implement Runs use one named Agent Session and close it on terminal paths. | pass | The 12-test resolve/watch slice passed; the 9-test implement slice passed; the 9-test daemon task slice passed; `rtk go test -race ./internal/daemon/ -count=1` passed. |
| 23 | Task 05: SDK layer is removed, module graph shrank, and real-acpx integration covers round-trip, cancel, crash-resume, and teardown. | pass | `rtk rg "github.com/coder/acp-go-sdk" -n go.mod go.sum internal cmd` exited 1 with no matches. `rtk rg "flow run\|acpx flow\|SDK runner\|ACPRunner\|acp_runner\|NewSDK\|coder/acp-go-sdk" -n internal cmd go.mod go.sum README.md .agents/skills/roundfix/SKILL.md skills/roundfix/SKILL.md` exited 1 with no matches. The real-acpx integration passed. |
| 24 | Task 06: canonical skill, embedded skill, README, handoff, and glossary wording match the shipped dependency and behavior. | pass | `rtk make skills-sync-check` exited 0. `rtk ./bin/roundfix skills check` exited 0. A quoted `rtk rg -n` search found the expected Node.js, acpx `0.12.0`, install-command, Agent Session, adapter-binaries, and Preflight Validation entries in README, both skill copies, and `CONTEXT.md`. `rtk rg -n "2026-07-05\|0002-acpx-migration\|ADR-0017\|work-plan item 2\|Item 2\|acpx" docs/handoffs/2026-07-04-implementation-daemon-acpx.md` found the dated closure note. |
| 25 | Non-goal: acpx flows are not implemented and orchestration stays in Roundfix. | pass | The no-fallback grep in result 23 found no `flow run` or `acpx flow` references in shipped code/docs checked for runtime behavior. |
| 26 | Non-goal: no dual backend or SDK fallback remains. | pass | The no-fallback grep in result 23 found no SDK runner, ACP runner, or `coder/acp-go-sdk` references in `internal`, `cmd`, module files, README, or skill files. |
| 27 | Non-goal: cross-Run session reuse is not introduced. | pass | Session tests in the 28-test agent slice and 12/9-test CLI slices assert one session per Run and re-ensure behavior for new session names. |
| 28 | Non-goal: completed spec implement path must not create a new Run. | pass | After the sandboxed write-check failed on `~/.roundfix/artifacts`, the approved rerun `rtk env PATH=/usr/bin:/bin ./bin/roundfix implement --spec 0002-acpx-migration --agent codex --no-input` exited 0, printed all six tasks as completed, and ended with `All 6 Task(s) already completed; no Run was created.` |

## Failures and blocked items

None.

## Environment

- Repo: `/Users/marcio/dev/roundfix`
- Date: 2026-07-05
- Built binary: `bin/roundfix`
- Roundfix version output: `roundfix 0.0.0-dev`
- Node: `v25.6.1`
- acpx: `/Users/marcio/.asdf/installs/nodejs/25.6.1/bin/acpx`, version `0.12.0`
- Static gate: `rtk make verify`
- Focused tests: agent acpx slice, CLI resolve/watch slice, CLI implement slice, daemon task slice, daemon race test, and real-acpx integration.
- Sandbox notes: the first completed-spec CLI flow failed its default Artifact Directory write-check under `~/.roundfix/artifacts`; the approved rerun passed. The first real-acpx integration failed with `EPERM` writing `~/.acpx/sessions`; the approved rerun passed.
- No commits, pushes, or pull requests were made.
