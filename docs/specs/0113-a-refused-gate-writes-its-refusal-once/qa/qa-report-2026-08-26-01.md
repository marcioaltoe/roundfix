---
spec: 0113-a-refused-gate-writes-its-refusal-once
date: 2026-08-26
build: 342a8a5db39ef39450833120ea7633a8e3d25bdb
status: closed
verdict: pass
rows_blocked_environment: 2
rows_blocked_finding: 0
rows_blocked_declared: 0
surfaces: [backend, docs]
---

# QA Report

## Performed repairs

None.

## Assigned repair failures

None.

## Mechanical findings

None.

## Scope and environment

Full terminal gate for Spec 0113 — three user stories, four Core Features, the
six TechSpec acceptance criteria, the three Success Metrics, the three
Non-Goals, the two Project Constraints the checker could not decide when the
Spec opened, the commit-dependent tooling audit, and behaviour probes against
the delivered detector. This is a rerun of the 2026-08-26 gate that closed
`fail` at `a7628d5b` on findings F-1 and F-2; both are re-measured here first,
then the whole matrix is run against the current build.

Build under test: HEAD `342a8a5d` on the Run Worktree branch
`roundfix/run-run_20260826T171301Z_eb768db2b9523448`, Spec target branch
`ma/a-gate-that-cannot-certify-its-own-cache`. Binary built by `make build`
into `bin/roundfix` (`roundfix 0.6.0 (342a8a5d-dirty, built 2026-08-26
14:28:38 -0300)`).

Actors and seats:

- **Supervisor at the CLI**, running the whole product: `roundfix implement
  --spec <slug>` in real Git repositories under `/tmp`, driving the real
  Daemon, the real QA gate, the real report writer and the real report commit.
  Three complete Runs were executed this way; this is the seat the previous
  gate could not reach.
- **Maintainer at the CLI**: `bin/roundfix spec check … --strict`, the check
  `speccheck.GatePreconditionCheck` names as the gate's own precondition.
- **Supervisor at the mechanical stage**: `speccheck.RunMechanicalStage`,
  invoked through a compiled program that reproduces the single call
  `internal/daemon/task_engine.go:1920` makes, against real repository
  artifacts and real Git repositories. Used only for ordering and shape probes
  a full Run cannot isolate. Its source is kept at
  `evidence/2026-08-26-run-20260826T171301Z/00-harness-main.go.txt`; it was
  removed from the tree after building, and `git status --porcelain
  --untracked-files=all` lists only this report and its evidence directory.
- **Reader of the documentation**: `docs/user-guide/`, `CONTEXT.md`, and the
  installed `skills/qa-gate/SKILL.md`, followed without repository knowledge.

Parity gaps. None for the Supervisor journey: unlike the previous run, the
selected ACP Runtime (`codex`) answered, so `roundfix implement` completed end
to end three times, including one Run that spent a real Agent turn. Every
scratch repository was created under `/tmp`, initialised with `git init`, and
this repository's tracked files were never modified. One environment block
remains and is recorded as R01.

Evidence lives under `evidence/2026-08-26-run-20260826T171301Z/`, one file per
measurement, each carrying the exact command context and its full output.

## Static gate

`make verify` — **ran and failed in one package**, `internal/agent`, and passed
everywhere else. `fmt-check` clean; 24 of 25 test-bearing packages `ok`,
including `internal/spec` (52.6s), `internal/speccheck` (66.4s),
`internal/daemon` (17.8s) and `internal/cli` (114.0s). The `test` target's
failure stopped the later stages, so they were run individually and all passed:
`skills-sync-check`, `skills-check` (all 14 Roundfix-owned skills), and `build`.
`docs-test`, `repo-test`, `spec-budget`, and `spec-check` (18 active Specs, no
findings) also pass.

The `internal/agent` failure is environment-caused and is recorded as R01. Its
proof is in **Blocked and skipped**: the same package fails the same way at the
unchanged pre-Spec base `854039b8`, which contains none of this Spec's Task
commits.

`bin/roundfix spec check 0113-a-refused-gate-writes-its-refusal-once --strict`
— exit 0, no findings. One detector remains in the checker's skipped list, so
its row stays in this matrix rather than being credited to the checker:
`SC-REF-UNRESOLVED` (no `references/_index.md`), carried as R19. The vocabulary
documentation detector, skipped at the previous gate, now runs and passes — that
change is R18.

After the sweep, `git status --porcelain --untracked-files=all` lists only this
report and its evidence directory.

## Results

| # | Story / criterion / sweep | Actor and surface | Status | Evidence |
| - | --- | --- | --- | --- |
| R01 | Static gate: the repository's full verification pipeline | Maintainer · backend | blocked (environment: `internal/agent`'s fake-ACPX tests fail nondeterministically under full-suite parallelism) | Reproduced at the unchanged pre-Spec base `854039b8`; every other package `ok`; `internal/agent` alone passes 3/3 at HEAD (`evidence/2026-08-26-run-20260826T171301Z/01-make-verify.txt`, `…/04-static-gate-agent-unchanged-base.txt`, `…/05-verify-stages-and-touched-packages.txt`) |
| R02 | Gate precondition: the Spec passes its own strict authoring check | Maintainer · cli | pass | `bin/roundfix spec check 0113… --strict` exit 0, no findings; one skipped detector carried to R19 (`evidence/2026-08-26-run-20260826T171301Z/06-gate-precondition.txt`) |
| R03 | Story 1: a gate that stops at a precondition writes a report a later run does not inherit an error from | Supervisor · cli, backend | pass | Two complete `roundfix implement` Runs. The first refused and wrote the refusal; the second, on the fixed Spec with that refusal as the only report in `qa/`, reported `QA mechanical stage seeded …` — not blocked (`evidence/2026-08-26-run-20260826T171301Z/07-production-refusal-implement-run.txt`, `…/09-refusal-does-not-block-its-successor.txt` A) |
| R04 | Story 2: the refusal is recorded so its reason is auditable and does not block future runs | Supervisor · cli, backend | pass | The report the production gate wrote carries `precondition_check` and `precondition_reason` naming all four refusing `SC-CONSTRAINT-MISSING` codes with their sentences, and round-trips verbatim through `spec.ReadQAReport` (`evidence/2026-08-26-run-20260826T171301Z/08-refusal-report-written-by-the-real-gate.md`, `…/20-qa-task-verification-and-report-accepted.txt`) |
| R05 | Story 3: a maintainer reading a gate refusal learns which check caused it | Maintainer · backend, docs | pass | The refusal names the check in frontmatter and again under `## Precondition refusal`; the user guide, the `CONTEXT.md` glossary and the installed skill each answer the reader's question (`evidence/2026-08-26-run-20260826T171301Z/08-refusal-report-written-by-the-real-gate.md`, `…/17-docs-surface-read-as-a-reader.txt`) |
| R06 | Core Feature 1: a precondition-refused gate writes exactly one terminal row | Supervisor · cli, backend | pass | The production report's `## Results` table is header, separator and one row `\| 0 \| blocked \| precondition \|`; the `## Mechanical rows` table below it contributes no result rows (`evidence/2026-08-26-run-20260826T171301Z/08-refusal-report-written-by-the-real-gate.md`, `…/20-qa-task-verification-and-report-accepted.txt`) |
| R07 | Core Feature 2: the mechanical stage reads the newest report, so a superseded refusal cannot block the run that supersedes it | Supervisor · backend | pass | Asked for the oldest of three same-date reports, the stage validates the newest and returns 0 findings; measured live on this Spec's own directory, where the previous run's report is superseded by the seeded one (`evidence/2026-08-26-run-20260826T171301Z/10-ordering-and-missing-matrix-probes.txt` P1, `…/18-previous-report-does-not-block-this-run.txt`) |
| R08 | Core Feature 3: the refusal row records the check name and the reason it failed | Supervisor · backend | pass | `speccheck.GatePrecondition` → `PreconditionRefusal` → `spec.WritePreconditionRefusalReport` carries both through the production path; the reason names every refusing code, code first and sentence after, joined with `; ` (`evidence/2026-08-26-run-20260826T171301Z/08-refusal-report-written-by-the-real-gate.md`) |
| R09 | Core Feature 4: only the Results table is read as results | Supervisor · backend | pass | Spec 0098's real report, isolated so newest-only cannot mask it: 7 findings at `4cc3b5fb`, 3 at HEAD — the four prose-comparison cells the PRD names (`Case`, `82-line function vs 80`, `2462-line generated file vs 500`, `` `sort()` vs `toSorted()` ``) are gone, the three genuine Results rows remain (`evidence/2026-08-26-run-20260826T171301Z/11-results-table-only-0098-before-and-after.txt`) |
| R10 | TechSpec Acceptance 1: gate writes a valid report on precondition refusal | Supervisor · cli, backend | pass | Every sub-clause holds on the artifact a real Run left behind: one terminal row, status `blocked`, provenance `precondition`, `rows_blocked_precondition: 1`, check and reason recorded (`evidence/2026-08-26-run-20260826T171301Z/07-production-refusal-implement-run.txt`, `…/08-refusal-report-written-by-the-real-gate.md`) |
| R11 | TechSpec Acceptance 2: newest report only, with deterministic ordering | Supervisor · backend | pass | `-10` supersedes `-02` supersedes the unsuffixed report on the same date, and a later date supersedes all three; raw byte order would invert the first pair (`evidence/2026-08-26-run-20260826T171301Z/10-ordering-and-missing-matrix-probes.txt` P1) |
| R12 | TechSpec Acceptance 3 and Success Metric 1: a shape refusal never blocks the run that supersedes its cause, with no manual intervention | Supervisor · cli, backend | pass | Before-and-after in one repository: with the refusal as newest report the next Run is seeded; with the pre-Spec empty-matrix shape in its place the same Run is blocked by `QA-REPORT-SHAPE: Results table has no report rows`. No report was deleted in either case (`evidence/2026-08-26-run-20260826T171301Z/09-refusal-does-not-block-its-successor.txt` A and B) |
| R13 | TechSpec Acceptance 4: precondition check information is recoverable from the report | Supervisor · backend | pass | `spec.ReadQAReport` recovers `Verdict: fail`, `RowsBlockedPrecondition: 1`, `CheckName` and the full `Reason` verbatim from the production artifact (`evidence/2026-08-26-run-20260826T171301Z/20-qa-task-verification-and-report-accepted.txt`) |
| R14 | TechSpec Acceptance 5, carrying this Spec's outside-evidence obligation: three earlier Specs show the same shape | Maintainer · backend | pass | Reports authored by earlier gates and not by this Spec — `docs/history/specs/0078-…/qa/qa-report-2026-08-05.md`, `…/0094-…/qa/qa-report-2026-08-13.md`, `…/0103-…/qa/qa-report-2026-08-14.md`, each with 0 commits since `854039b8` — read in place: 0078 clean, 0094 still carrying `Results table has no report rows`, 0103 clean because its gate wrote its refusal as one terminal row (`evidence/2026-08-26-run-20260826T171301Z/13-outside-evidence-0078-0094-0103.txt`) |
| R15 | Non-Goal: what checks the gate runs, and their order, are unchanged | Maintainer · cli | pass | Two binaries, one fixed tree: the pre-Spec binary and the HEAD binary sweep every Spec at `854039b8` with `--strict`. Byte-identical output and identical exit codes on all 16, including every skipped-detector record (`evidence/2026-08-26-run-20260826T171301Z/14-non-goals-two-binaries-one-tree.txt`) |
| R16 | Non-Goal: the strict precondition itself is unchanged | Maintainer · cli | pass | Same byte-identical comparison; the `promoteSpecCheckGaps` → `speccheck.PromoteGaps` export changes no verdict the command reports (`evidence/2026-08-26-run-20260826T171301Z/14-non-goals-two-binaries-one-tree.txt`) |
| R17 | Non-Goal: Review Source behaviour and other external contracts are unchanged | Maintainer · backend | pass | `git diff --name-only 854039b8..HEAD` outside `docs/specs/` touches `internal/spec`, `internal/speccheck`, `internal/daemon`, `internal/cli`, one testdata fixture, the qa-gate skill pair, `CONTEXT.md` and one user-guide page; `internal/reviewsource` 0 files (`evidence/2026-08-26-run-20260826T171301Z/15-tooling-audit.txt`) |
| R18 | Project Constraint, identifier strategy: the emitted vocabulary is documented, and the detector that decides it now runs | Maintainer · docs | pass | At `854039b8` the checker skipped `vocabulary documentation detector: missing … Vocabulary Contract`; at HEAD it runs and finds nothing. Spec 0098, which has no contract, still shows the skip — so the pass is a change, not an absence (`evidence/2026-08-26-run-20260826T171301Z/16-skipped-detector-rows.txt`) |
| R19 | Retained for the skipped `SC-REF-UNRESOLVED` detector: repository paths the Spec cites resolve | Maintainer · docs | pass | 66 distinct repository-relative citations swept; the nine a bare existence check flagged resolve individually — five are bare filenames whose full path is named in the same sentence, two are elided prose, one is inside task_08's rendered example artifact. Six cited line anchors still point at the construct they name (`evidence/2026-08-26-run-20260826T171301Z/16-skipped-detector-rows.txt`) |
| R20 | Declared `docs` surface: a reader can follow the documentation to the refusal contract | Maintainer · docs | pass | All three reader questions answered without repository knowledge: the shape of a refusal and the newest-report rule in `docs/user-guide/context-driven-development.md`, the three keys and a new **Precondition Refusal** glossary entry in `CONTEXT.md`, the gate's own obligation in `skills/qa-gate/SKILL.md` §1 and §6 (`evidence/2026-08-26-run-20260826T171301Z/17-docs-surface-read-as-a-reader.txt`, `…/03-docs-surface.txt`) |
| R21 | Commit-dependent tooling audit over the Daemon-owned Task commits and the worktree delta | Maintainer · backend | pass | `git diff-tree` over all eleven commits in `854039b8..HEAD`: Go sources, Go tests, one `testdata` fixture, two documentation files, the qa-gate skill pair, and Spec artifacts. No linter, formatter, test-runner, build, ignore, plugin, or version-pin file. The PRD's bounded list is `.agents/skills/qa-gate/SKILL.md`; exactly that file plus its declared `make skills-sync` mirror changed, the mirror is byte-identical, and the authorization landed in `cfa3d9b5`, its own commit and an ancestor of the Task commit `34b09533` that used it (`evidence/2026-08-26-run-20260826T171301Z/15-tooling-audit.txt`) |
| R22 | Probe: same-date recency, where raw filename order would pick the wrong report | Supervisor · backend | pass | Two defective same-date reports beside a valid `-10` yield 0 findings; `sort -r` would have picked the unsuffixed one (`evidence/2026-08-26-run-20260826T171301Z/10-ordering-and-missing-matrix-probes.txt` P1) |
| R23 | Probe: a report with no `## Results` heading still reports the missing matrix instead of passing on silence | Supervisor · backend | pass | A report whose only table sits under `## Mechanical rows` yields exactly one finding, `Results table has no report rows` (`evidence/2026-08-26-run-20260826T171301Z/10-ordering-and-missing-matrix-probes.txt` P2) |
| R24 | Probe: invalid and out-of-order refusal-row input | Supervisor · backend | pass | Four cases each refuse by name — `precondition` provenance with status `pending`, a bare `blocked` outside the typed causes, a declared count with no matching row, and a refusal row whose count was never declared — while the shape a real gate writes passes with 0 findings (`evidence/2026-08-26-run-20260826T171301Z/12-refusal-row-probes.txt`) |
| R25 | Success Metric 2: a gate refusal report is structurally valid per the QA Report contract | Supervisor · backend | pass | The bytes a production Run wrote, fed back to `speccheck.RunMechanicalStage`: `Blocking false`, 0 findings, 0 blocked rows (`evidence/2026-08-26-run-20260826T171301Z/20-qa-task-verification-and-report-accepted.txt`) |
| R26 | Success Metric 3: newest-report-only is proven by measurement, not by assertion | Supervisor · backend | pass | Measured on this Spec's live directory: asked for the previous run's report, the stage validated the newer seeded one instead, and the previous report's ten `fail` rows and 30 in-window table lines produced no shape finding at HEAD or at `4cc3b5fb` (`evidence/2026-08-26-run-20260826T171301Z/18-previous-report-does-not-block-this-run.txt`) |
| R27 | Pull Request journeys for this Spec | Supervisor · backend | blocked (environment: no open Pull Request) | The Roundfix QA prompt states no Pull Request is open and that Pull Request journeys are environment-blocked; the Run Worktree branch is never pushed and has no Pull Request of its own |
| R28 | The complete Supervisor journey: `roundfix implement --spec <slug>` driving the QA gate through the Daemon | Supervisor · cli | pass | Three complete Runs in scratch repositories — refuse, supersede, and the empty-matrix control — each reaching `QA verdict`, task settlement and `QA Report commit created`; and this Run's own Daemon executing this gate on Spec 0113 (`evidence/2026-08-26-run-20260826T171301Z/07-production-refusal-implement-run.txt`, `…/09-refusal-does-not-block-its-successor.txt`, `…/19-seeded-report-as-written-by-this-runs-daemon.md`) |
| R29 | The authored QA Task's own Verification command | Maintainer · cli | pass | `roundfix spec check 0113… --strict && go test -count=1 ./internal/spec ./internal/speccheck \| grep -q '^ok'` → exit 0 (`evidence/2026-08-26-run-20260826T171301Z/20-qa-task-verification-and-report-accepted.txt`) |
| R30 | This report, read by the mechanical stage it must itself satisfy | Supervisor · backend | pass | `Blocking false`, 0 findings, so this report does not become the next run's blocker. The first attempt was refused — this gate had written `rows_blocked_environment: 1` against two environment-blocked rows, the detector named the mismatch, and the count was corrected to 2 before the row closed (`evidence/2026-08-26-run-20260826T171301Z/21-this-report-read-by-the-stage.txt`) |

## Findings

None. Both findings from the 2026-08-26 gate at `a7628d5b` are closed and were
re-measured from the seat that exposed them:

- **F-1 — the refusal report had no writer in production.** Closed by task_08.
  The previous gate's reproduction — seed a report, run the stage, watch the
  deadlock — was repeated as a whole `roundfix implement` Run this time. A gate
  that refuses now writes the refusal shape, and the run that supersedes it is
  seeded rather than blocked (R03, R04, R06, R08, R10, R12, R13, R28).
- **F-2 — the declared `docs` surface received nothing.** Closed by task_09.
  `CONTEXT.md` names all three tokens and gains a **Precondition Refusal**
  entry, the user guide states both the refusal shape and the newest-report
  rule, and the TechSpec's `## Vocabulary Contract` turned a skipped checker
  detector into a running one (R05, R18, R20).

Two observations that are not findings, recorded so a later reader does not
have to rediscover them:

- The `precondition_check` field carries `spec check --strict` when the Daemon
  writes the refusal and `roundfix spec check <slug> --strict` in the skill's
  template for an Agent-written one. Both name the same check, nothing keys on
  the exact string, and `detectPreconditionCount` reconciles only the count, so
  no journey breaks.
- Spec 0094's archived report still carries the empty-matrix shape and still
  refuses when read (R14). It is evidence of the pattern, not a live deadlock:
  archived Specs live under `docs/history/specs/` and no gate runs on them
  again. Rewriting it would be the evidence removal this Spec exists to avoid.

## Blocked and skipped

**R01 — the static gate.** `blocked (environment: internal/agent's fake-ACPX
tests fail nondeterministically under full-suite parallelism)`.

Proof, and why it is not this Spec's: `make verify` was run twice. The first
attempt was discarded because this gate's own evidence writes tripped
`suiteguard`'s repository-boundary check mid-run; the second was run with no
concurrent repository writes. It failed in `internal/agent` on
`TestACPXRunReappliesSelectionForFreshRunnerSessionResume`,
`TestACPXRunWarmSessionIsIdempotent`, and
`TestACPXExitCodeMapping/all_permissions_denied_failure` — the last reporting
`open …/args.json: no such file or directory`, a fake harness process that
never wrote its arguments file. The discarded first attempt failed in the same
package on a *different* test. Run alone at HEAD, `go test -count=1
./internal/agent` passes 3 times out of 3. The same package fails the same way
under `go test -parallel 16 ./...` at the unchanged base `854039b8` — the
commit before this Spec's first Task — on yet another test,
`TestACPXProbeSelectionRejectionClosesDisposableSession`. A different test each
run, in a package this Spec never touched, passing in isolation, failing at a
base that contains none of this Spec's code.

Equivalent observed evidence: every other package is `ok` at HEAD in the same
invocation, including all four this Spec changes (`internal/spec`,
`internal/speccheck`, `internal/daemon`, `internal/cli`, also `ok` when run on
their own). The stages the failed `test` target prevented were run
individually and all pass: `fmt-check`, `skills-sync-check`, `skills-check`,
`build`. So do `docs-test`, `repo-test`, `spec-budget` and `spec-check`. No row
in this matrix has an entry point, observable, or evidence path that depends on
`internal/agent`.

Unblocking action: rerun `make verify` on a host that is not spawning the
sandboxed suite under contention, or harden the fake `acpx` fixture against a
failed process spawn so the package is deterministic under `-parallel 16`. That
work belongs to whichever Spec owns `internal/agent`, not to this one.

**R27 — Pull Request journeys.** `blocked (environment: no open Pull Request)`.
The Roundfix QA prompt states that no Pull Request is open for this Spec and
that Pull Request journeys are environment-blocked. The Run Worktree branch
`roundfix/run-run_20260826T171301Z_eb768db2b9523448` is never pushed and has no
Pull Request of its own, so it was not searched for one. Unblocking action: open
a Pull Request on `ma/a-gate-that-cannot-certify-its-own-cache` and rerun the
gate; approval, checks, unresolved threads, Merge-Ready acceptance, and
review-artifact ancestry then become observable read-only through `gh`. This is
the one row whose cause the previous gate also recorded, unchanged.

Both blocked rows are environment-caused and both count in
`rows_blocked_environment`, which is therefore 2. No row was skipped, no row is
finding-blocked, and no row is declared-blocked; the PRD declares no
`## Unreachable Acceptance` section, so no declaration could be matched or found
wrong.

## Coverage

Stories: 3 of 3 passed (R03, R04, R05). Core Features: 4 of 4 passed (R06, R07,
R08, R09). TechSpec acceptance criteria: 5 of 5 runnable passed (R10, R11, R12,
R13, R14); Acceptance 6's "no external acceptance row required" is answered by
carrying the outside-evidence obligation in R14 rather than dropping it. Success
Metrics: 3 of 3 passed (Metric 1 with R12, Metric 2 with R25, Metric 3 with
R26). Non-Goals: 3 of 3 checked, none shipped (R15, R16, R17). Project
Constraints the checker could not decide: 2 of 2 passed (R18, R19). Declared
`docs` surface swept as R20. Commit-dependent tooling audit passed (R21).

Rows: 30 planned, 30 terminal, 0 pending. 28 pass, 0 fail, 2 blocked.
`rows_blocked_environment` 2, `rows_blocked_finding` 0, `rows_blocked_declared`
0. R27's Pull Request block is a standing fact of the prompt rather than a
condition this build produced; R01 is the suite failure proved at an unchanged
base.

Behaviour probes attempted: 7 — same-date recency inversion (R22), missing
`## Results` heading (R23), and four invalid or out-of-order refusal-row inputs
(R24). Three non-vacuity controls were run rather than asserted: R09 and R26
were replayed against the pre-change tree `4cc3b5fb`, and R12's empty-matrix
control was a whole `roundfix implement` Run in a repository identical to the
passing one except for the newest report's shape. R15 and R16 were measured with
two binaries over one fixed tree, so a difference could only come from the
binary. No `frontend` surface is declared, so no frontend or TUI sweep applies.

## Final verdict

**pass** — a gate that refuses at a precondition now writes a report its own
contract accepts, on the path a Supervisor actually reaches: three complete
`roundfix implement` Runs show the refusal written, the next Run seeded rather
than blocked, and the pre-Spec empty-matrix shape still deadlocking in the same
repository as the control. The reading half — newest-report-only, Results-table
only, and the detector's acceptance of the refusal row — holds under measurement
and under its pre-change controls, and the declared `docs` surface now answers
every question a refused maintainer would ask.

Counts by status: 28 pass, 0 fail, 2 blocked (both environment, each with its
cause and equivalent evidence recorded), 0 skipped, 0 pending. Findings by
impact tier: none — no `Blocks-Completion`, `Data-Loss`, `Trust-Damage`,
`Friction`, or `Cosmetic` finding. The two findings the previous gate raised are
closed and re-measured from the seat that exposed them.

Pull request preparation is permitted on this verdict. Opening the Pull Request
on `ma/a-gate-that-cannot-certify-its-own-cache` would also unblock R27, the
only row this build left unobserved for a reason of its own.

## Mechanical skips

| Detector | Missing artifact |
| --- | --- |
| authorization bounded paths | tooling authorization |
| consequent-fix commit order | consequent-fix declaration |
