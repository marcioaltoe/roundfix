---
spec: 0153-a-reviewer-the-workflow-runs
date: 2026-09-22
build: eb02c4f0ac3a675548d13560218b93acd48e9695
auditing_binary: "0.15.0 (c60048c0, built 2026-09-22 17:19:39 -0300)"
auditor_staleness: "stale: commit ancestry: build commit predates audited tree"
status: closed
verdict: pass
rows_blocked_environment: 1
rows_blocked_finding: 0
rows_blocked_declared: 0
surfaces: [backend, cli, docs]
---

# QA Report

## Scope and environment

Full terminal QA gate for commit `eb02c4f0ac3a675548d13560218b93acd48e9695` in the Run Worktree. The actors are an operator invoking `roundfix review`, a Spec author checking promise coverage, and a maintainer auditing the candidate for delivery. No Pull Request is open, so that row uses the required environment-blocked equivalent-evidence path. No real reviewer session, paid API call, commit, push, or Pull Request mutation is permitted.

The strict authoring precondition passed with the stale auditing binary recorded above:

```text
roundfix spec check 0153-a-reviewer-the-workflow-runs --strict
Spec 0153-a-reviewer-the-workflow-runs
No findings. Authored Verification commands were not executed.
```

The checker skipped only detectors whose optional or project-family inputs are absent: a Findings Rollup, active Backlog Entry, TechSpec Vocabulary Contract, and references index. None replaces a declared QA row.

## Static gate

The Daemon ran `make verify` outside the Agent sandbox for this Run Worktree and reported exit 0. Diagnostics were removed on success. Per the QA contract, the Agent recorded that supervised result and did not rerun repository Verification.

## Performed repairs

None.

## Assigned repair failures

None.

## Authorization audit inputs

None.

## Mechanical findings

None.

## Results

| # | Story / criterion / sweep | Actor and surface | Status | Evidence |
| - | --- | --- | --- | --- |
| 1 | Daemon repository Verification | Maintainer; repository gate | pass | Daemon-supervised `make verify`: exit 0; diagnostics removed on success |
| 2 | Codex receives the candidate diff, runs read-only in an invocation-isolated session, and records the selected reviewer with repository/base/head | Operator; CLI and backend | pass | Focused public-runner and ACPX-boundary checks passed |
| 3 | Explicit `none` omits without reviewer activity and exits zero | Operator; CLI and backend | pass | Activity-counting public-runner check passed |
| 4 | Every selected-mode failure remains blocked; anomaly beats clean text; timeout has no fallback | Operator; CLI and backend | pass | Focused failure and ACPX classification checks passed |
| 5 | `claude` and `coderabbit` refuse by provider name | Operator; CLI | pass | Both public-runner provider cases passed |
| 6 | Verification spends no paid API use and starts no real reviewer | Maintainer; test and Task contracts | pass | Authored-command audit and stubbed review suite passed |
| 7 | Shipped skill, mirror, and user guide describe the command truthfully | Maintainer; docs and CLI contract | pass | Skill, mirror, help, and content checks passed |
| 8 | Every Task commit stayed inside the approved tooling authority | Maintainer; Git history | pass | Commit paths, governed set, grant ancestry, and regeneration output passed |
| 9 | This Spec's artifacts satisfy the promise rule | Spec author; checker CLI | pass | Strict current-tree Spec check reported no findings |
| 10 | Existing pre-PR policy contract remains the command's external foundation | Operator; config and CLI | pass | Pre-Spec policy provenance and focused integration checks passed |
| 11 | Pull Request controls through equivalent evidence | Maintainer; Pull Request journey | blocked (environment: no open Pull Request) | Prompt fact plus equivalent evidence for all five Pull Request controls |

## Row evidence

### 1

Provenance: Requirement 1; repository Verification.

```yaml
inputs:
  - kind: repository_path
    ref: "**"
```

- Steps: record the Daemon-supervised repository Verification for the audited head; do not rerun it.
- Expected observable: `make verify` exits 0.
- Independent confirmation: the Daemon supplies the exact command, verdict, and absence of retained success diagnostics.
- Persistence check: bind the result to the audited Run Worktree head.
- Evidence path: this report's Static gate section.

Observed: pass. The supervised command was `make verify`; verdict exit 0; no success diagnostics remained. The result is bound to commit `eb02c4f0ac3a675548d13560218b93acd48e9695` and was not re-derived.

### 2

Provenance: Requirement 2.

```yaml
inputs:
  - kind: repository_path
    ref: internal/cli/review.go
  - kind: repository_path
    ref: internal/cli/review_test.go
  - kind: repository_path
    ref: internal/agent/agent.go
  - kind: repository_path
    ref: internal/agent/acpx_runner.go
  - kind: repository_path
    ref: internal/agent/acpx_runner_test.go
```

- Steps: execute the focused CLI and runtime-boundary checks for diff prompt content, read-only permissions, selected-provider/profile agreement, invocation-isolated sessions, and the persisted clean record.
- Expected observable: the prompt contains the actual diff, the request permits reads but not writes, mismatching selections are refused before readiness, concurrent invocations cannot share a session reference, and the record names repository, base, and head.
- Independent confirmation: fixture repository, captured runner request, ACPX argument boundary, decoded record, activity counters, and independently generated session references must agree.
- Persistence check: each check creates fresh repositories, session references, and artifacts.
- Evidence path: this report and the named implementation/test inputs.

Observed: pass. Fresh focused checks passed the candidate-diff prompt, read-only request, explicit-clean/default-base record, four-outcome round trip, missing-head and invalid-detail refusals, provider/profile mismatch, and unique-session-reference cases. The mismatch cases named `codex` against `claude` or `opencode` and observed zero readiness or reviewer activity. Two references generated for the same head differed. `TestACPXPromptArgsPlaceGlobalsBeforeAgentAndSubcommand/read-only_session` independently passed at the runtime argument boundary.

### 3

Provenance: Requirement 3.

```yaml
inputs:
  - kind: repository_path
    ref: internal/cli/review.go
  - kind: repository_path
    ref: internal/cli/review_test.go
```

- Steps: execute the `none` policy public-runner check with a stub counting probe, prepare, and prompt activity; decode the persisted record.
- Expected observable: exit 0, outcome `omitted`, and zero reviewer calls or readiness probes.
- Independent confirmation: process result and stub activity counters must agree.
- Persistence check: the check creates a fresh artifact directory and record.
- Evidence path: this report and `internal/cli/review_test.go`.

Observed: pass. `TestReviewCommandNoneRecordsOmissionWithoutAgentActivity` passed against a fresh fixture. It decoded `omitted`, asserted exit 0, and observed zero probe, prepare, and prompt calls on the injected runner.

### 4

Provenance: Requirement 4.

```yaml
inputs:
  - kind: repository_path
    ref: internal/cli/review.go
  - kind: repository_path
    ref: internal/cli/review_test.go
  - kind: repository_path
    ref: internal/agent/acpx_runner.go
  - kind: repository_path
    ref: internal/agent/acpx_runner_test.go
```

- Steps: execute the focused runtime-failure, timeout, transport-anomaly, empty-output, unclassifiable-output, protocol-stream, and fallback checks.
- Expected observable: every named failure exits 2, records `blocked`, and names its reason; clean-looking output cannot override an anomaly; timeout causes no fallback.
- Independent confirmation: decoded records, stderr diagnostics, exit codes, and runner activity counts must agree.
- Persistence check: every case creates fresh fixtures and artifacts.
- Evidence path: this report and the named test inputs.

Observed: pass. Fresh checks passed runtime failure, post-prompt failure without fallback, timeout, transport anomaly, empty Agent message, unclassifiable Agent message, and selection-only fallback. The transport-anomaly case supplied clean-looking `No findings` output and still blocked. The timeout case supplied the same clean-looking output and observed one prepare and one prompt with no fallback. The ACPX exit-classification matrix passed all nine terminal-signal cases.

### 5

Provenance: Requirement 5.

```yaml
inputs:
  - kind: repository_path
    ref: internal/cli/review.go
  - kind: repository_path
    ref: internal/cli/review_test.go
```

- Steps: execute the public-runner cases for `claude` and `coderabbit`.
- Expected observable: each exits 2, names the selected provider, records `blocked`, and never records `omitted`.
- Independent confirmation: process result, persisted record, and zero Agent Runtime activity agree.
- Persistence check: each provider case uses a fresh artifact directory.
- Evidence path: this report and `internal/cli/review_test.go`.

Observed: pass. `TestReviewCommandRefusesUnimplementedProvider` passed for both `claude` and `coderabbit`; each case asserted exit 2, named the selected provider in the diagnostic, persisted `blocked`, and made no Agent Runtime call.

### 6

Provenance: Requirement 6.

```yaml
inputs:
  - kind: repository_path
    ref: docs/specs/0153-a-reviewer-the-workflow-runs/task_*.md
  - kind: repository_path
    ref: internal/cli/review_test.go
  - kind: repository_path
    ref: internal/agent/acpx_runner_test.go
```

- Steps: inspect every authored Verification command for external reviewer invocation, execute only stubbed focused checks, and confirm they inject a fake `agent.Runner` rather than constructing the production reviewer.
- Expected observable: no Verification command invokes `roundfix review`, Codex, or another live reviewer; exercised review paths terminate at test stubs.
- Independent confirmation: command-text search and captured fake-runner activity establish separate boundaries.
- Persistence check: bind the command audit to the current Task artifacts and test tree.
- Evidence path: this report, Task Verification sections, and named test inputs.

Observed: pass. Every authored Verification section was extracted and inspected. Tasks 01-03 and 06 invoke focused Go tests or public help, Task 04 invokes local content checks and `roundfix skills check`, and Task 05 reads the newest report verdict; none invokes `roundfix review`, Codex, ACPX, or another live reviewer. The current test tree exposes `recordingReviewRunner` and `reviewCommandRunner`, and `newReviewCommandFixture` accepts the injected `agent.Runner`. A fresh `go test -count=1 -run '^TestReview' ./internal/cli` completed against those fixtures, so no production reviewer or paid API was started.

### 7

Provenance: Requirement 7.

```yaml
inputs:
  - kind: repository_path
    ref: .agents/skills/roundfix/SKILL.md
  - kind: repository_path
    ref: skills/roundfix/SKILL.md
  - kind: repository_path
    ref: docs/user-guide/commands.md
  - kind: repository_path
    ref: docs/user-guide/configuration.md
  - kind: repository_path
    ref: docs/user-guide/usage.md
```

- Steps: run the shipped skill check, compare canonical and distributed skill bytes, exercise public help, and assert the command, option, exits, omission path, blocking cases, and unsupported providers appear in the documented surfaces.
- Expected observable: skill validation passes, mirrors match, help exposes `review`, and no stale unenforced-policy wording remains.
- Independent confirmation: CLI validation, byte comparison, help output, and content searches are distinct observations.
- Persistence check: bind results to current repository paths.
- Evidence path: this report and the named docs/skill inputs.

Observed: pass. `roundfix skills check` passed the Roundfix skill and its related workflow skills. `cmp .agents/skills/roundfix/SKILL.md skills/roundfix/SKILL.md` exited 0. The built public entry point printed `roundfix review [--base <ref>]` and its default-base option. Both skill copies and the user guide document the candidate diff, read-only session, repository/base/head record, exits 0/1/2, explicit `none`, all five blocking classes, and `claude`/`coderabbit` refusal. The stale unenforced-policy search returned no matches.

### 8

Provenance: Requirement 7.

```yaml
inputs:
  - kind: repository_path
    ref: docs/specs/0153-a-reviewer-the-workflow-runs/_authorization.md
  - kind: repository_path
    ref: internal/speccheck/governed.go
  - kind: repository_path
    ref: .git
```

- Steps: enumerate every non-QA Task commit with `git diff-tree`, intersect paths with the governed set, prove authorization ancestry for governed changes, and confirm sanctioned regeneration output.
- Expected observable: each governed path is exactly bounded by a prior operative grant; authorization and prerequisite/consequent changes are not folded or misordered; no other Task path is governed.
- Independent confirmation: immutable Git objects, the literal governed-path set, grant record, and skill byte comparison decide the result rather than Task prose.
- Persistence check: bind observations to exact commit IDs.
- Evidence path: this report, authorization record, governed-path source, and Git objects.

Observed: pass. `git diff-tree --no-commit-id --name-status -r` enumerated the Task commits `1544a94d`, `5097b24d`, `4beb55f1`, `ba06fb3d`, and `eb02c4f0`, plus the QA-invalidation authoring commit `f25744dd`. Tasks 01, 02, and 06 touched no governed path. Task 03 touched governed `internal/cli/cli_test.go`; Task 04 touched governed `.agents/skills/roundfix/SKILL.md` and `skills/roundfix/SKILL.md`. Those are exactly the three paths bounded by `_authorization.md`. The grant commit `9290fab0e1e9897f3bad7c2cd01ceb54f790af89` is a strict ancestor of both governed Task commits, the authorization record is unchanged since that commit, and the grant was not folded into either Task. The canonical and distributed skills are byte-identical, no derived pin changed, and the governed paths have not changed since Task 04. No Task touched another path in the literal governed set.

### 9

Provenance: Requirement 8.

```yaml
inputs:
  - kind: repository_path
    ref: docs/specs/0153-a-reviewer-the-workflow-runs/**
  - kind: repository_path
    ref: internal/speccheck/**
```

- Steps: rerun `roundfix spec check 0153-a-reviewer-the-workflow-runs --strict` after the matrix exists.
- Expected observable: no promise-rule finding; authored Verification commands remain unexecuted by the checker.
- Independent confirmation: the checker parses declared promises and consuming Tasks from committed artifacts.
- Persistence check: rerun against the same audited head.
- Evidence path: this report and Spec artifacts.

Observed: pass. After the matrix existed, `roundfix spec check 0153-a-reviewer-the-workflow-runs --strict` again reported `No findings. Authored Verification commands were not executed.` The four skipped detectors concern absent optional or project-family artifacts and do not hide the promise-to-Task rule or replace a matrix row.

### 10

Provenance: outside-evidence row.

```yaml
inputs:
  - kind: repository_path
    ref: docs/history/specs/0143-a-repository-says-who-reviews-before-the-pull-request/**
  - kind: repository_path
    ref: internal/config/config.go
  - kind: repository_path
    ref: internal/config/config_test.go
  - kind: repository_path
    ref: internal/cli/review_test.go
  - kind: repository_path
    ref: .git
```

- Steps: identify the pre-existing policy contract and its pre-Spec Git provenance, run focused precedence/default checks, then run the current command integration that consumes the resolved policy.
- Expected observable: the existing `codex, claude, coderabbit, none` policy and precedence remain unchanged while `review` consumes the resolved value.
- Independent confirmation: archived Spec 0143 artifacts and commits predate this Spec and were not authored by it.
- Persistence check: bind outside evidence to immutable pre-Spec commits and fresh fixtures.
- Evidence path: this report, archived Spec 0143, config tests, and Git objects.

Observed: pass. The external source is archived Spec 0143 and the policy implementation it delivered in commit `e36746876a257f0af1e135a3495b56dd5162fdf8` on 2026-09-18, before this Spec's target commit `7f13b7aebf7884471ed8cec9ddb1b6c5ffd6f616` on 2026-09-22. Git ancestry confirmed that ordering, and `git diff --exit-code 7f13b7ae..HEAD -- internal/config/config.go internal/config/config_test.go` exited 0, proving this Spec did not alter that policy. Fresh `TestPrePRReviewPolicyResolution` cases passed the built-in Codex default, user/project precedence, explicit `none`, explicit `codex`, and invalid-value refusals. Fresh command checks then proved `review` consumes the resolved policy, refuses provider/profile disagreement before readiness, and preserves the explicit omission and unsupported-provider contracts.

### 11

Provenance: Pull Request row.

```yaml
inputs:
  - kind: live_service
    ref: Pull Request for feat/0153-codex-reviews-the-candidate
  - kind: repository_path
    ref: "**"
```

- Steps: because the prompt confirms no Pull Request is open, record the cause and inspect equivalent evidence for approval, checks/status, unresolved threads, Merge-Ready acceptance, and review-artifact ancestry.
- Expected observable: the row records every control and identifies absent evidence without inferring unavailable Pull Request state.
- Independent confirmation: the prompt supplies the no-PR fact; repository and Run facts supply equivalent evidence.
- Persistence check: environment-blocked live-service rows are not carry-forward eligible.
- Evidence path: this report's Blocked and skipped section.

Observed: blocked by the environment fact in the QA prompt. Equivalent evidence for approval, checks/status, unresolved review threads, Merge-Ready acceptance, and review-artifact ancestry is recorded below; no unavailable Pull Request state is inferred.

## Findings

None.

## Blocked and skipped

Row 11 is `blocked (environment: no open Pull Request)`. The prompt confirms that no Pull Request is open and that Pull Request journeys are environment-blocked. Equivalent evidence:

- Approval: no Pull Request approval was obtained.
- Checks and status: the Daemon ran `make verify` for audited head `eb02c4f0ac3a675548d13560218b93acd48e9695` and reported exit 0.
- Unresolved review threads: no Pull Request exists, so no Pull Request review thread stands.
- Merge-Ready acceptance: no Review Source evidence exists yet; no Merge-Ready claim is made.
- Review-artifact ancestry: no review artifact exists yet; no evidence from another head is credited.

Unblocking action: open a Pull Request from `feat/0153-codex-reviews-the-candidate`, then observe its approval, checks, review threads, Merge-Ready evidence, and review-artifact ancestry read-only. No rows are finding-blocked or declared-blocked.

## Coverage

All 11 coverage sources appear in the matrix: Requirements 1-8, repository Verification, the outside-evidence row, and the Pull Request row. Ten rows passed. One row is environment-blocked with its cause, unblocking action, and equivalent evidence recorded. `rows_blocked_environment: 1`, `rows_blocked_finding: 0`, and `rows_blocked_declared: 0`. Requirement 7 is split across documentation truth and the commit-dependent tooling audit. No frontend sweep or PRD Unreachable Acceptance declaration applies, and no row covers a source outside this set.

## Final verdict

Pass: all 10 runnable rows passed, the one environment-blocked Pull Request row records its cause and equivalent evidence for every control, and no row failed, was skipped, or was finding- or declared-blocked. Findings by impact: Blocks-Completion 0, Data-Loss 0, Trust-Damage 0, Friction 0, Cosmetic 0.

## Mechanical skips

| Detector | Missing artifact |
| --- | --- |
| authorization bounded paths | Task commits |
| consequent-fix commit order | consequent-fix declaration |
| QA evidence paths | docs/specs/0153-a-reviewer-the-workflow-runs/qa/evidence |
