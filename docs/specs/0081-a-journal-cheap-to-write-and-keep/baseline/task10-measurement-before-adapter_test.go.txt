package store

import (
	"context"

	"roundfix/internal/runevent"
)

func task10Publish(ctx context.Context, store *Store, event runevent.RunEvent) error {
	return (JournalSink{Store: store}).Publish(ctx, event)
}

func task10Flush(context.Context, *Store) error { return nil }
