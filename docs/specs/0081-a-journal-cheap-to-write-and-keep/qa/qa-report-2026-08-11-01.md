---
spec: 0081-a-journal-cheap-to-write-and-keep
date: 2026-08-11
build: 5af85c4785e9f1c322e554fdd420600ce163d8e9
status: closed
verdict: pass
rows_blocked_environment: 0
rows_blocked_finding: 0
rows_blocked_declared: 0
surfaces: [backend, data, docs, cli]
---

# QA Report — A journal cheap to write and cheap to keep

## Scope and environment

Full rerun of the authored terminal `task_09` gate against Run Worktree build
`5af85c4785e9f1c322e554fdd420600ce163d8e9`. The Run Worktree is report-only:
no commit, push, Pull Request creation, customer credential, or operator Run
Database mutation is authorized. No Pull Request is open. Real data journeys
use fresh temporary Roundfix Homes and production Store, CLI, and TUI paths
compiled from this build.

The manifest names `task_09` as the terminal `type: qa` node. Its dependencies
`task_06`, `task_08`, and corrective `task_10` are completed. The strict Spec
check passed with no findings; `SC-VOCABULARY-UNDOCUMENTED` was skipped because
the TechSpec has no Vocabulary Contract, so R06 retains that obligation.

## Static gate

- `rtk roundfix spec check 0081-a-journal-cheap-to-write-and-keep --strict`:
  passed; no findings; `SC-VOCABULARY-UNDOCUMENTED` skipped as recorded above.
- `rtk env GOCACHE=/private/tmp/roundfix-0081-qa01-gocache make verify`:
  blocked by the sandbox after formatting passed and `go test -parallel 16
  ./...` started; the process attempted `api.github.com`, which is outside the
  network allowlist. The denial is environmental and was not retried.
- `rtk env GOCACHE=/private/tmp/roundfix-0081-qa01-gocache make verify-docs`:
  passed, including the build, docs contracts, repository contracts, Spec
  corpus budget, and full Spec consistency sweep.
- `rtk make fmt-check` and `rtk env
  GOCACHE=/private/tmp/roundfix-0081-qa01-gocache make skills-sync-check
  skills-check build`: passed. Together with the prior attempt, these cover
  every non-test prerequisite of `make verify`.
- `rtk env GOCACHE=/private/tmp/roundfix-0081-qa01-gocache go test -count=1
  ./internal/store ./internal/tui ./internal/runevent`: passed. Focused CLI
  consumer replay also passed, providing equivalent observed evidence for all
  feature paths while the network-reaching whole-suite process was unavailable.

## Mechanical findings

None.

## Results

| # | Story / criterion / sweep | Actor and surface | Status | Evidence |
| - | --- | --- | --- | --- |
| R01 | Run-start wall-clock is independent of journal size across empty and full-retention databases. | Session starting a Run; data/backend | pass | Two fresh current-build observations: p50 104/49 µs at 0 events, 59/39 µs at 1,000, and 36/39 µs at 10,000; the former size curve is absent. Evidence: `docs/specs/0081-a-journal-cheap-to-write-and-keep/qa/evidence/2026-08-11-gate-01/evidence.md#r01--run-start-cost`. |
| R02 | Six parallel Runs at the pre-raise 5,000 ms `busy_timeout` complete with zero `SQLITE_BUSY`; cursors remain contiguous and publisher order survives cancellation. | Operator running six Runs; data/backend | pass | Six Runs persisted 1,536/1,536 events with zero `SQLITE_BUSY`; read-back and cancellation rehearsals passed. Evidence: `docs/specs/0081-a-journal-cheap-to-write-and-keep/qa/evidence/2026-08-11-gate-01/evidence.md#r02--parallel-runs`. |
| R03 | Event-write cost per 1,000 agent events improves against a pre-change same-boundary baseline because 128-event batching amortizes transactions, not because fewer events are produced. | Daemon appending agent output; data/backend | pass | Fresh three-sample medians: 128,901 µs/1,000 before and 37,571 µs/1,000 now, a 70.9% reduction with 1,536 events preserved in every sample. Evidence: `docs/specs/0081-a-journal-cheap-to-write-and-keep/qa/evidence/2026-08-11-gate-01/evidence.md#r03--same-boundary-write-cost`. |
| R04 | Bytes per Run are reported before and after, and retained Runs keep raw payload bytes under the unchanged terminal-only, age-based retention shape. | Maintainer deciding retention; data/docs | pass | Fresh bytes remain 61,440 / 2,162,688 / 21,135,360 at 0 / 1,000 / 10,000 events; raw bytes, terminal/age GC behavior, and exact ADR/lifecycle preservation passed. Evidence: `docs/specs/0081-a-journal-cheap-to-write-and-keep/qa/evidence/2026-08-11-gate-01/evidence.md#r04--bytes-and-retention-shape`. |
| R05 | Cockpit refresh cost tracks new events rather than total events while rendering, summary fallback, Follow Mode, and terminal-size states remain stable. | Operator watching a long Run; TUI through CLI | pass | The same two new events cost 2 headers + 1 full row after both 20- and 10,000-event backlogs; 15 adjacent TUI checks passed. Evidence: `docs/specs/0081-a-journal-cheap-to-write-and-keep/qa/evidence/2026-08-11-gate-01/evidence.md#r05--cockpit-refresh-and-tui-sweep`. |
| R06 | One recorded pre-change journal behaves identically through `events`, Attach, Cockpit rendering, reconcile replay detection, and `gc`; command/schema vocabulary stays unchanged. | Supervisor/operator; CLI/TUI/data/docs | pass | All three recorded digests matched; current CLI and TUI harnesses matched all five consumers and full Cockpit, while built command help retained glossary vocabulary. Evidence: `docs/specs/0081-a-journal-cheap-to-write-and-keep/qa/evidence/2026-08-11-gate-01/evidence.md#r06--pre-change-consumer-replay`. |

## Detailed row evidence

### R01

```yaml
inputs:
  - kind: repository_path
    ref: internal/store/journal_baseline_test.go
  - kind: repository_path
    ref: internal/store/journal.go
  - kind: repository_path
    ref: docs/specs/0081-a-journal-cheap-to-write-and-keep/baseline/*.md
  - kind: elapsed_time
    ref: current-build same-machine two-sample Run-start measurement
```

Entry point: production retention preflight over newly migrated temporary Run
Databases. Expected observable: Run-start cost stays flat from empty through a
10,000-event full-retention database. Independent confirmation: a second fresh
harness run. Persistence: reopen/read on the production database path. Probes:
empty, 1,000 events, 10,000 events, and repeat run. Status: **pass**. The two
fresh p50 sets were 104/59/36 µs and 49/39/39 µs across 0/1,000/10,000 events.
The 10,000-event database was 21,135,360 bytes versus 61,440 bytes empty, yet
neither run followed the former journal-size curve.

### R02

```yaml
inputs:
  - kind: repository_path
    ref: internal/store/journal_parallel_runs_test.go
  - kind: repository_path
    ref: internal/store/journal_writer.go
  - kind: repository_path
    ref: internal/store/writelock_*.go
  - kind: elapsed_time
    ref: current-build six-Run concurrency scenario
```

Entry point: six independent writer Stores against one newly migrated Run
Database. Expected observable: all 1,536 events commit at 5,000 ms with zero
typed `SQLITE_BUSY`. Independent confirmation: per-Run production-reader
read-back. Persistence: cursor and publisher-order checks after commit. Probes:
synchronized publish, exact timeout, and cancellation while holding the writer
lock. Status: **pass**. Six Runs completed 1,536 events at 5,000 ms with zero
typed `SQLITE_BUSY`; wall time was 55,951 µs (36,426 µs per 1,000), and
production read-back plus cancellation both passed.

### R03

```yaml
inputs:
  - kind: repository_path
    ref: docs/specs/0081-a-journal-cheap-to-write-and-keep/baseline/task10-measurement-*.txt
  - kind: repository_path
    ref: docs/specs/0081-a-journal-cheap-to-write-and-keep/baseline/2026-08-11-before.md
  - kind: repository_path
    ref: docs/specs/0081-a-journal-cheap-to-write-and-keep/baseline/2026-08-11-repaired.md
  - kind: repository_path
    ref: internal/store/journal_batch_test.go
  - kind: elapsed_time
    ref: same-machine three-sample before and after measurement
```

Entry point: production Journal Sink boundary on the pre-change and current
builds. Expected observable: the same 1,536 events and publish-through-flush
boundary cost less per 1,000 after batching. Independent confirmation: three
samples per build and persisted event read-back. Persistence: commit-boundary
read-back from all six Runs. Probes: equal harness core, equal event count,
count/linger/immediate/flush boundaries, and raw payload preservation. Status:
**pass**. The old build's fresh median was 128,901 µs per 1,000; the current
build's was 37,571 µs per 1,000, 70.9% lower. Every one of the six samples
persisted exactly 1,536 events at the same boundary. Count, linger, immediate,
explicit-flush, ambiguous-commit, and raw-payload canaries also passed.

### R04

```yaml
inputs:
  - kind: repository_path
    ref: docs/findings/2026-08-06-three-gigabytes-of-event-journal-inside-the-retention-window.md
  - kind: repository_path
    ref: docs/specs/0081-a-journal-cheap-to-write-and-keep/baseline/*.md
  - kind: repository_path
    ref: docs/adr/0008-run-event-payload-stores-raw-producer-json.md
  - kind: repository_path
    ref: docs/adr/0033-the-run-event-journal-is-pruned-by-retention.md
```

Entry point: external operator measurement plus same-harness temporary
databases. Expected observable: before/after byte counts are stated, raw bytes
survive, and only the existing terminal/age boundary prunes. Independent
confirmation: exact ADR/lifecycle preservation diff. Persistence: reopen after
batch commit. Probes: 0/1,000/10,000 events, raw JSON, zero retention, dry-run,
and actual prune. Status: **pass**. Fresh before/after byte counts match at
61,440 / 2,162,688 / 21,135,360 for 0 / 1,000 / 10,000 events. Seven focused
raw-payload and GC checks passed, and exact `a2a4c86b..HEAD` diffs prove
ADR-0008, ADR-0033, and the lifecycle guide are unchanged.

### R05

```yaml
inputs:
  - kind: repository_path
    ref: internal/tui/cockpit.go
  - kind: repository_path
    ref: internal/tui/cockpit_forward_cursor_test.go
  - kind: repository_path
    ref: internal/tui/testdata/cockpit_snapshots/*
```

Entry point: CLI Live Run View Cockpit model over the production reader.
Expected observable: the same two new events cost the same after small and
large backlogs and rendering stays byte-identical. Independent confirmation:
replay and snapshots. Persistence: idle repoll and completed-Run attach replay.
Probes: large backlog, missing payload, small/large/degenerate terminal sizes,
keyboard, Follow Mode, and no color. Status: **pass**. The reproducible QA
overlay ran the PRD's 10,000-event boundary twice: the same two new events
cost 2 header rows, 1 full row, and 67 payload bytes after both 20 and 10,000
events. Fifteen adjacent production-model checks passed.

### R06

```yaml
inputs:
  - kind: repository_path
    ref: docs/specs/0081-a-journal-cheap-to-write-and-keep/baseline/2026-08-11-prechange-roundfix.db
  - kind: repository_path
    ref: docs/specs/0081-a-journal-cheap-to-write-and-keep/baseline/2026-08-11-prechange-consumer-expectations.json
  - kind: repository_path
    ref: docs/specs/0081-a-journal-cheap-to-write-and-keep/baseline/2026-08-11-prechange-cockpit.golden
  - kind: repository_path
    ref: internal/store/journal_consumer_corpus_test.go
  - kind: repository_path
    ref: CONTEXT.md
```

Entry point: built current CLI and production consumer packages reading the
pre-change database produced by `a2a4c86b`. Expected observable: `events`,
Attach, full Cockpit, reconcile, and `gc` match recorded pre-change outputs.
Independent confirmation: fixture and expectation digests plus command help.
Persistence: database reopen before each consumer. Probes: daemon and agent
payloads, fixed-cutoff GC, completed Task and Verification replay, color-free
120x40 Cockpit, and stable public vocabulary. Status: **pass**. All artifact
digests matched, and the current-build CLI/TUI replay matched every recorded
consumer observation. Built `events`, `attach`, `gc`, and `reconcile` help all
exited 0 and retained the canonical Run Database, Supervisor event stream,
Attach, Run Event Journal, and Journal Retention vocabulary.

## Findings

None. The previous report's F-01 is resolved because `fmt-check` now passes.
F-02 is resolved by the fresh same-boundary R03 comparison, and F-03 is
resolved by the provenance-backed pre-change database and all-consumer R06
replay.

## Blocked and skipped

No matrix row is blocked or skipped. The full `make verify` process is
environment-blocked at a sandbox-denied `api.github.com` access inside the Go
test target. It was not retried. Equivalent current-build evidence passed for
every feature-owning package, every named CLI consumer, formatting, skill
sync/check, build, documentation contracts, repository contracts, and the
full Spec corpus. Pull Request journeys are not part of this Spec's acceptance
surface; the prompt confirms no Pull Request is open.

## Coverage

Closed with zero pending, failed, blocked, or skipped rows: 6 pass. R01–R06
map the six Success Metrics in order and collectively cover all six User
Stories and Core Features 1–7. R04 carries outside evidence from the
maintainer Finding that predates this Spec. R05 covers the required 10,000-
event cost boundary plus responsive, degenerate, keyboard, Follow Mode,
Attach, snapshot, summary-fallback, and no-color TUI states. R06 carries the
skipped Vocabulary Contract obligation.

The exact `a2a4c86b..HEAD` changed-path inventory contains only Spec artifacts,
journal/store implementation and tests, CLI journal wiring, and Cockpit paths.
Schema version remains 12; exact diffs of `.roundfixrc.yml`, `go.mod`,
`go.sum`, ADR-0008, ADR-0033, and the Run Database lifecycle guide are empty.
No stream schema, scheduling behavior, second store, event-count reduction,
retro-compaction, second retention window, or payload shedding landed.

Rows blocked by environment: 0. Rows blocked by finding: 0. Rows blocked by
declaration: 0. Findings by impact: 0 Blocks-Completion, 0 Data-Loss, 0
Trust-Damage, 0 Friction, and 0 Cosmetic.

## Final verdict

**Pass.** All six Success Metric rows pass on build `5af85c47`; there are no
failed, finding-blocked, declared-blocked, or skipped rows. The sandbox blocked
the network-reaching whole-suite process, but equivalent observed evidence
covers every feature path and every other `make verify` prerequisite, so the
environment boundary does not cap the verdict under ADR-0080.

## Mechanical skips

| Detector | Missing artifact |
| --- | --- |
| authorization bounded paths | tooling authorization |
| consequent-fix commit order | consequent-fix declaration |
