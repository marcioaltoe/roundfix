---
spec: 0068-spec-close-audit
date: 2026-08-04
build: 30ec663cf7ae65b3f03fcd696a576dc8fa578359
status: closed
verdict: pass
rows_blocked_environment: 1
rows_blocked_finding: 0
rows_blocked_declared: 0
surfaces: [backend, cli, docs]
---

# QA report — Spec close audit rerun

## Scope and environment

Full rerun of the authored terminal QA Task after task_08 corrected F-001 from
the first 2026-08-04 gate. The Run Worktree is on
`roundfix/run-run_20260805T013130Z_31398370e8ba8670` at build
`30ec663cf7ae65b3f03fcd696a576dc8fa578359`. The actor is a maintainer closing
a Spec through the built `roundfix spec audit <slug>` CLI, this repository, and
disposable real-Git repositories.

The prompt states that no Pull Request is open and Pull Request journeys are
environment-blocked. QA-05 must therefore record that typed cause and fresh
equivalent fixture evidence. The PRD has no `## Unreachable Acceptance`
section, so no row can be declaration-blocked.

Evidence root:
`docs/specs/0068-spec-close-audit/qa/evidence/2026-08-04-spec-0068-01/`.

The complete matrix is pending before execution. Rerun order starts with the
previously failed QA-18 and environment-blocked QA-05, then covers the rest of
the assembled feature.

## Static gate

Pass. The required unpiped `rtk make verify` exited 0: 3,318 Go tests passed
across 26 packages; the isolated corpus-budget check and four Skill tests
passed; the Repository Skill Set passed; the binary built; and the Spec
Consistency Check reported no finding for Spec 0068. Evidence:
`evidence/2026-08-04-spec-0068-01/qa-02-make-verify.md`.

## Results

| # | Story / criterion / sweep | Actor, entry point, and steps | Expected observable | Independent confirmation / persistence | Evidence | Status |
| - | --- | --- | --- | --- | --- | --- |
| QA-18 | A pushed-and-merged Supervisor scratch worktree becomes reported residue while its branch remains | Maintainer; build the binary, create a disposable pushed-and-merged scratch worktree without a matching Run, then run text and JSON audits | Worktree and checked-out branch classify `residue`; one shell-safe command removes the worktree before deleting the branch; the audit does not execute it | Rerun the audit, confirm refs and worktree registration persist, then execute the emitted command only in a disposable copy and confirm it succeeds | `evidence/2026-08-04-spec-0068-01/qa-18-scratch-worktree.md` | pass |
| QA-05 | A branch backing an Open Pull Request classifies `pull-request`, names the Pull Request, and is never reclaimable | Maintainer; observe the prompt's Pull Request fact and run the public CLI over an equivalent real-Git fixture | Live journey is environment-blocked because no Pull Request is open; equivalent output names the Pull Request and emits no reclaim command | Fresh JSON invocation plus focused assembled verification; human unblocking action is to open a Pull Request on `ma/spec-0068-implementation` and rerun read-only | `evidence/2026-08-04-spec-0068-01/qa-05-pull-request.md` | blocked (environment: no open Pull Request) |
| QA-01 | Authored QA node, completed dependency closure, Project Constraints, and protected-tooling provenance | QA operator; inspect Spec artifacts, Task status, authorization, commit ancestry, and exact changed paths | `task_07` is the unique terminal `qa` node; every dependency is completed; both active Spec artifacts account for all four constraints; tooling authorization and commit choreography are valid | Resolve every Task and authorization commit with `git diff-tree`; compare Skill mirrors and derived outputs on the current build | `evidence/2026-08-04-spec-0068-01/qa-01-governance.md` | pass |
| QA-02 | Repository Verification | QA operator; run unpiped `rtk make verify` | Full gate exits 0 without format, test, Skill, build, or Spec consistency failure | Record the exact exit code and final summary from the command itself | `evidence/2026-08-04-spec-0068-01/qa-02-make-verify.md` | pass |
| QA-03 | A maintainer can discover and run the close audit for a real Spec without knowing its Runs or branches | Maintainer; invoke built top-level help, command help, and text audit using only `0068-spec-close-audit` | Help lists the command and the slug alone yields kinds plus evidence | Invoke JSON from a fresh process and compare its current-repository state with text output | `evidence/2026-08-04-spec-0068-01/qa-03-real-spec.md` | pass |
| QA-04 | A clean Spec audit reports nothing needing attention | Maintainer; run the built CLI against a disposable committed clean Spec repository | Text exits 0 and reports clean state | Fresh JSON process returns one empty `roundfix-specaudit/v1` object; repeated audit leaves state unchanged | `evidence/2026-08-04-spec-0068-01/qa-04-clean.md` | pass |
| QA-06 | A branch with unintegrated work classifies `pending` with deciding evidence | Maintainer; run the built CLI against a disposable branch-only real-Git fixture | Survivor is `pending`, evidence names unintegrated content, and reclaim is empty | Fresh JSON process plus unchanged branch tip, worktree, and database state | `evidence/2026-08-04-spec-0068-01/qa-06-pending.md` | pass |
| QA-07 | Merged local and stale remote branches classify `residue` with exact operator reclaim commands, but the audit executes neither | Maintainer; run the built CLI over disposable local and bare-remote fixtures | Exit 1; local and remote residue carry evidence and correct reclaim commands | Snapshot refs and worktrees before and after repeated audits; confirm byte-equivalent state | `evidence/2026-08-04-spec-0068-01/qa-07-residue.md` | pass |
| QA-08 | An unmatched worktree that lacks push-and-merge proof stays `preserved` | Maintainer; run the built CLI against unpushed, unmerged, and indeterminate registered worktrees without matching Runs | Each survivor is `preserved`, names the missing proof, and has no reclaim command | Fresh `git worktree list --porcelain` after repeated audits confirms persistence | `evidence/2026-08-04-spec-0068-01/qa-08-preserved.md` | pass |
| QA-09 | An Active Run's branch and worktree are never classified `residue` or mutated | Maintainer; audit the real active Run and an injected pushed-and-merged Active Run fixture | Both survivors remain protected with Active Run evidence and no reclaim command | Compare Run state, refs, worktree registration, and filesystem state before and after audit | `evidence/2026-08-04-spec-0068-01/qa-09-active-run.md` | pass |
| QA-10 | Delivery verification trusts the synced default tree, names a holding branch, and never guesses a holder | Maintainer; run delivered, branch-held, working-copy-only, and nowhere fixtures | Delivered claims are absent; branch-only claim names its holder; uncommitted and nowhere claims are undelivered without a guessed holder | Fresh Git-tree reads after each CLI run confirm the report against committed state | `evidence/2026-08-04-spec-0068-01/qa-10-delivery.md` | pass |
| QA-11 | A terminal Run whose target branch was deleted resolves by content, never optimistic ancestry | Maintainer; run built `reconcile` and `spec audit` over full-match, Run-only, and differing-content fixtures | Full representation resolves `safe`; unique or differing content resolves `unintegrated`; reason names the evidence | Repeat read-only inspection and confirm refs and worktrees persist; check other refusals remain | `evidence/2026-08-04-spec-0068-01/qa-11-deleted-target.md` | pass |
| QA-12 | The motivating session replay reports two scratch worktrees, the orphaned Run Worktree, the remote backup branch, and two Pull Request branches | Maintainer; exercise the real-Git replay fixture tied to the source finding through the assembled audit | All four survivor categories appear with evidence; Pull Request branches are not reclaimable; the orphan resolves by content | Compare fixture provenance and Git state after a second audit | `evidence/2026-08-04-spec-0068-01/qa-12-replay.md` | pass |
| QA-13 | Text, JSON, usage, stderr, invalid order, and exit-code contracts are deterministic | Maintainer and automation; run the built CLI with valid, invalid, missing, unknown, and out-of-order input | Text and JSON agree; JSON is exactly one object; clean exits 0, attention exits 1, and usage errors exit 2 on stderr | Decode JSON to EOF, rerun inputs from fresh processes, and confirm no partial JSON or mutation | `evidence/2026-08-04-spec-0068-01/qa-13-contract.md` | pass |
| QA-14 | Existing `spec check` and `reconcile` refusals retain their public behavior | Maintainer; run built regression journeys through the existing commands | `spec check` keeps its output and exit contract; reconcile still refuses dirty, ambiguous, and unintegrated state | Fresh invocations over disposable fixtures confirm state persistence | `evidence/2026-08-04-spec-0068-01/qa-14-regression.md` | pass |
| QA-15 | The shipped Roundfix Skill accurately teaches the command | Maintainer; follow canonical and mirrored Skill instructions against built help and fixtures | Slug, both formats, four kinds, evidence, operator-only reclaim, and exit codes match the binary | Recursive mirror comparison plus fresh command invocations from the Skill | `evidence/2026-08-04-spec-0068-01/qa-15-docs.md` | pass |
| QA-16 | The close audit is read-only across Git, Run Database, Spec artifacts, transport, and filesystem | Maintainer; repeat built audits while snapshotting public and persisted state | No ref, worktree, database, Spec, or filesystem mutation; no network transport; ambiguity remains preserved | Byte/hash snapshots before and after plus production import/call sweep | `evidence/2026-08-04-spec-0068-01/qa-16-read-only.md` | pass |
| QA-17 | Non-Goals remain absent: no automatic branch deletion, merge/review/close, process termination, compaction, or sanitation | QA operator; inspect exact changed paths and exercise attention reports | Feature reports only; unrelated command behavior and owned subsystems remain unchanged | Compare public state before and after and map every implementation commit path to the declared scope | `evidence/2026-08-04-spec-0068-01/qa-17-non-goals.md` | pass |

Selected behavior probes target the destructive-safety boundary: repeat audit,
invalid and out-of-order input, working-copy-only delivery, Run-only deleted
target content, and pushed-but-unmerged or unpushed scratch worktrees. Frontend,
viewport, browser session, locale, accessibility, and offline sweeps do not
apply to this backend, CLI, and docs-only Spec; transport absence is checked
directly.

## Findings

None.

## Blocked and skipped

QA-05 is `blocked (environment: no open Pull Request)` because the prompt
proves none is open. Fresh equivalent real-Git fixtures classified Pull
Request #42, #58, and #68 branches `pull-request`, named each Pull Request, and
emitted no reclaim command. Opening a Pull Request on
`ma/spec-0068-implementation` would make the live journey runnable. This one
environment block carries its cause, unblocking action, and equivalent evidence
and therefore does not cap the verdict under ADR-0080.

No row is finding-blocked, declaration-blocked, or skipped.

## Coverage

All 18 planned rows reached terminal status: 17 pass, 1 blocked by environment,
0 fail, 0 blocked by finding, 0 blocked by declaration, 0 skipped, and 0
pending. Typed blocked counts: environment 1, finding 0, declared 0. Findings
by impact: Blocks-Completion 0; Data-Loss 0; Trust-Damage 0; Friction 0;
Cosmetic 0.

PRD traceability:

- Goals 1 and 4 and Core Features 1, 4, and 5 map to QA-03 and QA-05 through
  QA-09, QA-12, QA-16, and QA-18.
- Goal 2 and Core Feature 2 map to QA-10.
- Goal 3 and Core Feature 3 map to QA-11 and QA-14.
- Core Feature 6 maps to QA-03 and QA-13.
- Success Metrics 1 through 4 map to QA-12, QA-11, QA-10, and QA-09.
- Every Non-Goal maps to QA-16 and QA-17.

Task acceptance traceability: task_01 maps to QA-09, QA-11, and QA-14;
task_02 to QA-05 through QA-09, QA-16, and QA-18; task_03 to QA-10 and QA-16;
task_04 to QA-03, QA-04, QA-07, QA-10, QA-13, and QA-14; task_05 to QA-12
and QA-17; task_06 to QA-01, QA-02, and QA-15; task_08 to QA-08, QA-09,
QA-16, and QA-18; task_07 to the complete matrix and this report.

The behavior probes covered repeat execution, invalid and out-of-order input,
working-copy-only delivery, Run-only deleted-target content, and unpushed,
unmerged, or indeterminate scratch worktrees. Frontend, viewport, browser
session, locale, accessibility, and offline sweeps were not applicable to the
declared backend, CLI, and docs surfaces; transport absence was checked
directly.

## Final verdict

Pass: all 17 runnable rows passed, the one environment-blocked Pull Request row
has fresh equivalent evidence, and no finding-blocked, declaration-blocked,
failed, skipped, or pending row remains.
