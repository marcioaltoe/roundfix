---
spec: 0060-spec-owned-reference-lifecycle
date: 2026-07-31
build: 00ca18ee7c0fa2bbc31f00b98c41c4208170cf5f
status: closed
verdict: pass
rows_blocked_environment: 1
rows_blocked_finding: 0
surfaces: [docs]
---

# QA report — Spec-owned reference lifecycle

## Scope and environment

Full QA gate against build `00ca18ee7c0fa2bbc31f00b98c41c4208170cf5f`
on the per-Run branch
`roundfix/run-run_20260731T125720Z_ce9ff882e5223678`. The intended actors are
a Spec author, an archiving maintainer, a reader of an archived Spec, and a
future investigator. The public surface is the repository documentation and
the authorial Skill procedures, exercised through their documented Git and
shell entry points in disposable clones.

The prompt states that no Pull Request is open for the Spec target branch.
The PRD declares no Pull Request surface, story, criterion, or review-dependent
observable, so no Pull Request journey is in the planned scope.

Project Constraint applicability is planned explicitly in QA-01. No browser,
HTTP, authentication, database, or frontend surface is declared.

## Planned matrix

| # | Story / criterion / sweep | Actor and entry point | Steps | Expected observable | Independent confirmation / persistence | Evidence path | Status |
| - | --- | --- | --- | --- | --- | --- | --- |
| QA-01 | Project Constraints and protected-tooling authorization audit | Maintainer; PRD, TechSpec, `docs/agents/`, ADR, and Git ancestry | Check identifier, auth/HTTP, ADR, and tooling applicability; resolve changed paths from each Task commit; verify bounded authorization and commit order; reproduce sanctioned digest generation | All four constraints are accounted for; only authorized paths plus assigned Task files changed; authorization predates the tooling Task; no prerequisite or consequent fix is folded or misordered; derived pins reproduce | `git diff-tree` for every Task commit, ancestry inspection, and clean deterministic regeneration in a disposable clone | Results QA-01 | pass |
| QA-02 | Authoritative static gate | Maintainer; repository root | Run `rtk make verify` unchanged | Exit 0 with formatting, tests, Skill checks, and build green | Exit status plus repository status before and after | Static gate section | pass |
| QA-03 | Story 1 / Features 1, 2, 3, and 7: adopt one finding and one inbox source | Spec author; `write-prd` adoption procedure in a disposable Git clone | Seed both source types and repository links; inventory, classify, claim ownership, flip/link the finding, `git mv` both sources, write the fixed index, rewrite all links, and run the authoring gate | Exactly one move per source; no copy or stub; finding is `done` and linked; index has all five fields; old links are gone | Fresh-shell reads; resolve both indexed paths and count old-path occurrences repository-wide | [Lifecycle rehearsal](evidence/2026-07-31-spec-0060/lifecycle-rehearsal.md) | pass |
| QA-04 | Authoring-gate negative and recovery probes | Spec author; `write-prd` step 8 | Probe an adopted source left at its original path and a rewritten link with a missing target; then repair each | Each defect prevents completion and names the offending source/link plus skipped step; repair makes the same gate pass | Rerun the gate from a fresh shell after each repair | [Lifecycle rehearsal](evidence/2026-07-31-spec-0060/lifecycle-rehearsal.md) | pass |
| QA-05 | Story 2 / Feature 4: archive an indexed, self-contained Spec | Archiving maintainer; `archive-spec` preconditions | Run the documented Task-status, latest-QA, link-destination, index-path, and source-absence checks | All three preconditions pass for a complete self-contained Spec | Move the whole Spec to `_archived/` in the disposable clone and rerun reference checks | [Lifecycle rehearsal](evidence/2026-07-31-spec-0060/lifecycle-rehearsal.md) | pass |
| QA-06 | Archive stale-link failure and recovery | Archiving maintainer; `archive-spec` self-containment command | Inject inline and reference-style links into findings/inbox trees, while keeping prose mentions; run the documented command and repair | Link cases fail and print each offending link with adoption-step-7 guidance; prose alone does not match; repair passes | Fresh rerun after repair and explicit exit codes | [Lifecycle rehearsal](evidence/2026-07-31-spec-0060/lifecycle-rehearsal.md) | pass |
| QA-07 | Archive missing-current-path and surviving-source failures | Archiving maintainer; indexed-row checks | Remove an indexed current file, then restore it and recreate the original source path | Missing `path` and surviving `source` each fail with the offending value and step-5 guidance | Fresh rerun after each repair | [Lifecycle rehearsal](evidence/2026-07-31-spec-0060/lifecycle-rehearsal.md) | pass |
| QA-08 | Feature 6: migration boundary and `qa_override` boundary | Archiving maintainer; `archive-spec` | Check a legacy Spec with no index; inspect and exercise the override boundary against a self-containment defect | No-index Spec passes trivially; `qa_override: true` cannot bypass an indexed self-containment failure | Compare no-index and indexed cases in the same disposable build | [Lifecycle rehearsal](evidence/2026-07-31-spec-0060/lifecycle-rehearsal.md) | pass |
| QA-09 | Story 3: provenance remains readable after archive | Reader; `references/_index.md` in active then archived Spec | Read original path, type, owner, adopted date, and relative path; archive the Spec directory | Every field is legible and the relative path resolves unchanged after the directory move | Resolve the same paths from a fresh shell after the archive move | [Lifecycle rehearsal](evidence/2026-07-31-spec-0060/lifecycle-rehearsal.md) | pass |
| QA-10 | Story 4 / Feature 5: shared source has one primary owner | Secondary Spec author; `write-prd`, `write-idea`, `write-techspec`, and `write-tasks` | Search active and archived Specs for ownership; model a later Spec referencing the primary owner's copy | The first implementation-committing Spec is the sole owner; the secondary links the owned copy and creates no duplicate/index adoption | Repository-wide basename and index-row counts after a fresh load | [Lifecycle rehearsal](evidence/2026-07-31-spec-0060/lifecycle-rehearsal.md) | pass |
| QA-11 | Story 5: moved finding remains discoverable | Future investigator; Git history | Follow the moved finding across the committed rename and inspect the adoption commit | `git log --follow` reaches pre-adoption commits; the moved content shows `status: done` and the owning-Spec link without rewritten observations | Staged rename, committed-source history, and existing committed archive-move history provide equivalent evidence because this QA contract prohibits a rehearsal commit | [Lifecycle rehearsal](evidence/2026-07-31-spec-0060/lifecycle-rehearsal.md) | blocked (environment: QA contract prohibits the commit required for direct moved-destination history proof) |
| QA-12 | Downstream referencing convention | Spec author; three downstream canonical Skills and embedded copies | Follow the source-link rule in `write-idea`, `write-techspec`, and `write-tasks` | Each stage points to the primary owner's post-adoption path and never secondary-adopts | Canonical/embedded byte comparison, focused source inspection, and secondary-Spec rehearsal | [Lifecycle rehearsal](evidence/2026-07-31-spec-0060/lifecycle-rehearsal.md) | pass |
| QA-13 | Layout, routing, and glossary contract | Spec author/reader; `docs/agents/docs-layout.md`, `docs/agents/spec-routing.md`, and `CONTEXT.md` | Follow the documented layout and ownership transfer | Layout names `references/` and `_index.md`; inbox/findings departure and Git trail are clear; routing defines transfer; glossary lists the artifacts | Cross-check all three public documents and setup-owned marker bytes against the base | [Protected-tooling audit](evidence/2026-07-31-spec-0060/protected-tooling-audit.md) | pass |
| QA-14 | Non-Goals: preserve evidence, do not migrate history, do not generate a cross-repository index, keep one-purpose folders | Maintainer; base-to-build diff plus disposable adoption | Compare source bytes around the allowed lifecycle metadata, archived/historical trees, generated files, and docs layout | Observations remain byte-preserved; historical archives/findings are untouched; no cross-repository index ships; `references/` stays inside one Spec folder | Base-to-build path audit and fresh repository search | [Lifecycle rehearsal](evidence/2026-07-31-spec-0060/lifecycle-rehearsal.md) and [protected-tooling audit](evidence/2026-07-31-spec-0060/protected-tooling-audit.md) | pass |
| QA-15 | Skill synchronization and deterministic derived pins | Maintainer; canonical/embedded Skills and sanctioned regeneration | Compare all five Skill pairs; run `rtk make baseline-digests` in a disposable clone; inspect resulting status | All five pairs are byte-identical and sanctioned regeneration produces no drift from committed pins | Fresh clone status after regeneration plus current `roundfix skills check` within QA-02 | Results QA-15 | pass |

Behavior probes are selected where the lifecycle is highest-risk: a surviving
source, an unresolved rewritten link, inline versus reference-style stale
links, a prose false positive, a missing current path, a no-index legacy Spec,
and a secondary owner attempting duplicate adoption. Refresh/reopen persistence
is represented by fresh clones and post-archive path resolution because the
surface is Git-backed documentation, not an interactive frontend.

## Static gate

`rtk make verify` exited 0 on build `00ca18e`. It reported 2,935 passing Go
tests across 24 packages, 4 passing focused Skill-contract tests, a passing
public `roundfix skills check` for all required Skills, and a successful
`go build`. `rtk git status --short` before and after named only the untracked
Spec-local `qa/` directory. Both status reads emitted the existing fsmonitor
IPC warning for the user checkout's worktree metadata; the warning did not
change either command's exit or the worktree.

After closing the report and writing its evidence, a final unchanged
`rtk make verify` rerun also exited 0 with the same 2,935 + 4 passing tests,
passing public Skill check, and successful build.

## Results

| # | Story / criterion / sweep | Actor and surface | Status | Evidence |
| - | --- | --- | --- | --- |
| QA-01 | Project Constraints and protected-tooling authorization audit | Maintainer / docs + Git | pass | Identifier and auth/HTTP are inapplicable with operative guide citations; ADR-0083 is accepted and satisfied. Authorization commits `397227f` and `f6b284f` precede Task 01. Exact `git diff-tree` paths match the authorized Skill/pin set plus assigned Task files; Tasks 02–03 contain only their docs/Task files. No prerequisite or consequent fix commit exists. Disposable regeneration returned `ok:true, changed:false` and clean status. |
| QA-02 | Authoritative static gate | Maintainer / repository | pass | `rtk make verify`: exit 0; 2,935 + 4 tests passed; Skill check and build passed; before/after status differed only by this QA directory. |
| QA-03 | Adopt one finding and one inbox source | Spec author / docs + Git | pass | Two moves, finding lifecycle update, fixed index, resolved current paths, and zero old-path occurrences outside provenance. See [rehearsal](evidence/2026-07-31-spec-0060/lifecycle-rehearsal.md). |
| QA-04 | Authoring-gate negative and recovery probes | Spec author / docs + Git | pass | Surviving-source and unresolved-link probes exited 1 with the offending path and steps 5/7; repaired reruns exited 0. |
| QA-05 | Archive an indexed self-contained Spec | Archiving maintainer / docs + Git | pass | Task, QA, link, current-path, and source-absence preconditions passed; both relative paths survived the directory archive move. |
| QA-06 | Archive stale-link failure and recovery | Archiving maintainer / docs + shell | pass | Inline and reference-style links were both printed on exit 1; prose did not match; repaired rerun exited 0. |
| QA-07 | Missing-current-path and surviving-source failures | Archiving maintainer / docs + shell | pass | Each injected defect exited 1 with its exact index value and step-5 guidance; both repaired checks exited 0. |
| QA-08 | Migration and QA-override boundaries | Archiving maintainer / docs + shell | pass | Spec 0060's no-index case passed trivially; an indexed stale link still failed with `qa_override: true`. |
| QA-09 | Provenance remains readable after archive | Reader / docs + Git | pass | Both five-field index rows remained legible and their relative `path` values resolved after moving the whole Spec to `_archived/`. |
| QA-10 | Shared source has one primary owner | Secondary Spec author / docs + Git | pass | Secondary Spec linked the archived owner, had no index, and repository search found one physical finding file owned by `9999`. |
| QA-11 | Moved finding remains discoverable | Investigator / Git | blocked (environment: QA contract prohibits the commit required for direct moved-destination history proof) | Equivalent evidence: staged 98% rename; source history reached `fe345ab`; existing committed archive move followed from `d930d3e` to `397227f`; moved observations unchanged. Direct unblocking action: authorize one throwaway commit in a disposable clone. |
| QA-12 | Downstream referencing convention | Spec author / docs | pass | All three canonical Skills require the primary owner's post-adoption path; embedded pairs are byte-identical; the secondary rehearsal adopted nothing. |
| QA-13 | Layout, routing, and glossary contract | Spec author/reader / docs | pass | All required public wording is present; byte comparisons prove the setup-owned blocks match base `37d57b2`. |
| QA-14 | Non-Goals | Maintainer / docs + Git | pass | Observations were unchanged; base-to-build audit found no inbox, findings, or archived-Spec migration and no generated cross-repository index; `references/` remains Spec-local. |
| QA-15 | Skill sync and deterministic pins | Maintainer / docs + build tooling | pass | All five `rtk cmp -s` canonical/embedded comparisons exited 0; QA-02's public `roundfix skills check` passed; disposable `rtk make baseline-digests` returned `ok:true, changed:false` and left no status output. |

## Findings

No product finding was recorded. Impact counts: Blocks-Completion 0,
Data-Loss 0, Trust-Damage 0, Friction 0, Cosmetic 0.

## Blocked and skipped

QA-11 is `blocked (environment: QA contract prohibits the commit required for
direct moved-destination history proof)`. The staged move is a 98% Git rename;
the source path reaches commit `fe345ab`; and a committed active-to-archive
directory move follows from `d930d3e` to pre-archive commit `397227f`. This is
equivalent observed evidence for Git rename/history behavior, while the moved
finding's diff proves observations were preserved. Direct proof requires one
authorized throwaway commit in a disposable clone. No row is finding-blocked or
skipped.

## Coverage

Covered: 5/5 user stories, 7/7 core features, 4/4 Non-Goals, all Task
acceptance seams not safely credited from recorded evidence, the authoritative
static gate, and the protected-tooling audit. Fourteen rows passed directly;
one row is environment-blocked with equivalent observed evidence;
`rows_blocked_environment: 1`; `rows_blocked_finding: 0`; zero failed, skipped,
or pending rows. The docs surface used fresh-shell rereads, a disposable clone,
an archive/reopen move, stale-source and broken-link recovery probes, inline and
reference-link probes, a prose false-positive probe, missing-path and
QA-override probes, and a secondary-owner duplication probe.

## Final verdict

Pass: all 14 runnable rows passed, QA-11 records its commit-policy cause and
equivalent observed evidence, and there are zero findings, finding-blocked
rows, skipped rows, or pending rows.
