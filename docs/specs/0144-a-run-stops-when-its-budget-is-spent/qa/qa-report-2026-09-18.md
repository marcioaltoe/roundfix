---
spec: 0144-a-run-stops-when-its-budget-is-spent
date: 2026-09-18
build: 0d30b9b8
auditing_binary: "0.13.1 (d459a00e, built 2026-09-18 07:53:34 -0300)"
auditor_staleness: "stale: commit ancestry: build commit predates audited tree"
status: closed
verdict: pass
rows_blocked_environment: 1
rows_blocked_finding: 0
rows_blocked_declared: 0
surfaces: [backend, cli, docs]
---

# QA Report — A Run stops when its budget is spent

## Scope and environment

Full terminal gate for build `0d30b9b8` on the Run Worktree branch. The actor is
a repository maintainer using the Roundfix CLI. The strict authoring
precondition passed with `roundfix spec check
0144-a-run-stops-when-its-budget-is-spent --strict`; it reported no findings
and did not execute authored Verification commands. The auditing binary is
stale by commit ancestry, so the report records its identity separately from
the audited build.

The prompt proves that no Pull Request is open and declares Pull Request
journeys environment-blocked. Repository Verification was already run by the
Daemon outside the Agent sandbox and must not be rerun here.

## Static gate

- `make verify`: passed with exit 0. The Daemon ran this outside the Agent
  sandbox before this gate and removed diagnostics on success. Per the QA
  contract, this Agent recorded the fact and did not rerun the command.

## Performed repairs

None.

## Assigned repair failures

None.

## Authorization audit inputs

| Task | Outcome | Record | Revision | Detail |
| --- | --- | --- | --- | --- |
| task_04 | granted | docs/specs/0144-a-run-stops-when-its-budget-is-spent/_authorization.md | d459a00e877fdbecf1a9a426af89f69dae5b64a2 | operative grant |

## Mechanical findings

None.

## Results

| # | Story / criterion / sweep | Actor and surface | Status | Provenance |
| - | --- | --- | --- | --- |
| R1 | Repository Verification | Daemon, repository gate | pass | Requirement 1; repository Verification |
| R2 | Enabled budget ends owned work and settles with the existing outcome and reason | Maintainer, backend and CLI | pass | Requirement 2 |
| R3 | Disabled budget preserves current settlement | Maintainer, backend and CLI | pass | Requirement 3 |
| R4 | Carry-forward accepts proved budget-expired work and refuses unproved or unsupported work | Maintainer, CLI | pass | Requirement 4 |
| R5 | Canonical and mirrored Roundfix skills state the bounded Run contract and match | Maintainer, docs | pass | Requirement 5 |
| R6 | Existing watch-loop rule and Run Database outcome are independently exercised | Maintainer, backend | pass | Requirement 6; outside-evidence row |
| R7 | Spec promises and Task commits satisfy the authored contract and authority boundary | Maintainer, repository governance | pass | Requirement 7 |
| R8 | Pull Request controls have equivalent evidence while no Pull Request exists | Maintainer, Pull Request journey | blocked (environment: no open Pull Request) | Pull Request row |

## Row evidence

### R1

```yaml
inputs:
  - kind: repository_path
    ref: Makefile
  - kind: repository_path
    ref: internal/**
  - kind: repository_path
    ref: docs/**
```

Expected: the repository Verification exits 0. Independent confirmation is
the Daemon's recorded exit status. Persistence is not applicable to a static
gate.

Evidence: the Daemon recorded `make verify` exit 0 and removed its diagnostic
artifact on success. This is the supervised static-gate result supplied in the
QA assignment.

### R2

```yaml
inputs:
  - kind: repository_path
    ref: internal/daemon/**
  - kind: repository_path
    ref: internal/cli/**
```

Steps: exercise an expiring enabled Run through the task-cycle and Implement
command seams. Expected: the active Agent Session is cancelled, no later Task
starts, completed Task state remains, the Run settles `BudgetExceeded`, the
reason names the configured maximum and elapsed time, and the Run Worktree and
Run Branch remain. Independent confirmation: CLI-level preservation evidence
beside daemon lifecycle evidence. Persistence: reopen the stored Run and Task
state through the fixture's public store reads.

Evidence: `go test -count=1 -v -run
'^(TestTaskCycleEndsRunAtBudgetDeadline|TestBudgetExceededRunRecordsItsReason|TestRunImplementBudgetExceededPreservesRunWorktreeAndBranch)$'
./internal/daemon ./internal/cli` exited 0. Both termination-proof branches
passed; the daemon outcome and reason test passed; and the Implement command
harness confirmed the stored outcome, completed Task state, and retained Run
Worktree and Run Branch.

### R3

```yaml
inputs:
  - kind: repository_path
    ref: internal/daemon/**
  - kind: repository_path
    ref: internal/cli/**
```

Steps: exercise an already-old Run with the budget disabled and a Run that
finishes before an enabled deadline. Expected: no deadline changes settlement
or completion. Independent confirmation: both disabled and finishes-first
paths. Persistence: fixture store state after the lifecycle returns.

Evidence: `go test -count=1 -v -run
'^(TestTaskCycleDisabledBudgetDerivesNoDeadline|TestTaskCycleFinishesBeforeBudgetDeadline)$'
./internal/daemon` exited 0 and both lifecycle paths passed.

### R4

```yaml
inputs:
  - kind: repository_path
    ref: internal/cli/**
  - kind: repository_path
    ref: internal/worktree/**
```

Steps: invoke the Reconcile Command harness for a proved `BudgetExceeded` Run,
an input-moved Run, a mixed provability set, and an unsupported `Clean` Run.
Expected: only the fully proved accepted set carries forward. Independent
confirmation: checkout and Task files read after each command. Persistence:
the accepted checkout remains fast-forwarded and refused checkouts remain
unchanged.

Evidence: the focused `go test` run over `./internal/cli` and
`./internal/worktree` exited 0. It passed the public Reconcile Command harness
for a proved `BudgetExceeded` Run; moved-input and mixed-set refusal for
`Stopped`, `Unresolved`, and `BudgetExceeded`; unsupported-outcome refusal; and
the unchanged terminal reconciliation classifications.

### R5

```yaml
inputs:
  - kind: repository_path
    ref: .agents/skills/roundfix/SKILL.md
  - kind: repository_path
    ref: skills/roundfix/SKILL.md
```

Steps: inspect the operator documentation for the budget outcome, reason,
preserved Run surfaces, and accepted carry-forward outcomes, then compare both
copies byte for byte. Expected: all facts are present and both copies match.
Independent confirmation: exact byte comparison. Persistence is not applicable
to immutable documentation in this build.

Evidence: commit `0d30b9b8` shows both copies state that the configured maximum
settles `BudgetExceeded`, names maximum and elapsed time, preserves the Run
Worktree and Run Branch, and accepts `BudgetExceeded`, `Stopped`, and
`Unresolved` for carry-forward. `cmp .agents/skills/roundfix/SKILL.md
skills/roundfix/SKILL.md` exited 0.

### R6

```yaml
inputs:
  - kind: repository_path
    ref: internal/watch/**
  - kind: repository_path
    ref: internal/store/**
```

Steps: locate the pre-existing watch-loop deadline behavior and Run Database
outcome vocabulary, then execute their existing focused tests. Expected: the
watch loop derives and enforces the same budget rule and the database round
trips `BudgetExceeded`. Independent confirmation: executable tests plus the
named source origins. Persistence: Run Database round-trip evidence.

Evidence origin outside the Spec artifacts:
`internal/watch/watch.go:405-406` derives the watch-loop deadline from the Run
start and configured maximum, while `internal/watch/watch.go:411` settles the
existing `BudgetExceeded` outcome; `internal/store/store.go:46,70` defines and
registers that terminal outcome. The focused watch tests
`TestRunTransientReviewEvidenceExhaustsRunBudgetBeforeTimeout` and
`TestRunWithoutStopRequestKeepsRunBudgetBehavior` exited 0. The store tests
`TestListRunsStateFilterAndLimit` and
`TestTerminalOutcomeEveryStoredTerminalStateIsImmutable`, including its
`BudgetExceeded` subtest, also exited 0 and confirmed persistence and terminal
immutability.

### R7

```yaml
inputs:
  - kind: repository_path
    ref: docs/specs/0144-a-run-stops-when-its-budget-is-spent/**
  - kind: repository_path
    ref: .agents/skills/roundfix/SKILL.md
  - kind: repository_path
    ref: skills/roundfix/SKILL.md
```

Steps: run the strict Spec check and audit each Task commit with
`git diff-tree`, comparing governed paths and chronological ancestry against
the approved authorization. Expected: promises map cleanly and Task 04 changes
only its assigned Task file and the two authorized skill paths. Independent
confirmation: commit ancestry and exact changed paths. Persistence: committed
history at the audited head.

Evidence: `roundfix spec check
0144-a-run-stops-when-its-budget-is-spent --strict` exited 0 with no findings and
without running authored Verification. `git diff-tree --no-commit-id
--name-only -r` over Task commits `51a734d4`, `3321bc35`, `bbebd95e`, and
`0d30b9b8` resolved their exact paths. Tasks 01-03 changed ordinary source,
tests, and their assigned Task files. Task 04 changed only its assigned Task
file plus `.agents/skills/roundfix/SKILL.md` and
`skills/roundfix/SKILL.md`, exactly the authorized paths. Ancestry checks
passed from authorization commit `9ef244b6` through Spec commit `d459a00e` and
all four Task commits; no prerequisite fix, consequent fix, or derived pin was
declared or folded into the governed change.

### R8

```yaml
inputs:
  - kind: live_service
    ref: GitHub Pull Request state for feat/0144-run-budget
  - kind: repository_path
    ref: docs/specs/0144-a-run-stops-when-its-budget-is-spent/_authorization.md
```

Steps: use the prompt's no-open-Pull-Request fact and record equivalent evidence
for approval, checks and status, unresolved review threads, Merge-Ready
acceptance, and review-artifact ancestry. Expected: the row remains
environment-blocked while every unavailable control is answered explicitly.
Independent confirmation: Daemon-supervised Verification and local commit
ancestry. Persistence: not applicable to absent live Pull Request state.

Evidence and equivalent controls:

- Cause: the QA assignment states that no Pull Request is open and that Pull
  Request journeys are environment-blocked. Unblocking requires opening a Pull
  Request from `feat/0144-run-budget`; this gate has no authority to do so.
- Approval: maintainer authorization for the bounded skill mutation exists at
  commit `9ef244b6`; no Pull Request acceptance has been obtained.
- Checks and status: the Daemon-supervised repository Verification passed with
  exit 0.
- Unresolved review threads: none can stand because no Pull Request exists.
- Merge-Ready acceptance: no Review Source acceptance exists yet because no
  Pull Request exists.
- Review-artifact ancestry: no review artifact exists. The audited implementation
  head is `0d30b9b878ba86d5b3aa5b5a03cffec33bef65e4`; the target branch remains at
  `d459a00e877fdbecf1a9a426af89f69dae5b64a2` until the Daemon settles this Run.

## Findings

None.

## Blocked and skipped

R8 is `blocked (environment: no open Pull Request)`. The prompt proves the
cause, and the row records equivalent observed or supervised evidence for all
five Pull Request controls. Opening a Pull Request from the Spec target branch
after Run settlement makes the live journey reachable.

## Coverage

Eight of eight rows reached a terminal status: seven passed and one was blocked
by the declared environment fact with complete equivalent evidence. All seven
declared Requirements appear in provenance, alongside the mandatory repository
Verification, outside-evidence, and Pull Request sources. No row names another
source. `rows_blocked_environment: 1`, `rows_blocked_finding: 0`, and
`rows_blocked_declared: 0`. The PRD declares no Unreachable Acceptance and no
frontend surface. No row failed or was skipped.

## Mechanical skips

| Detector | Missing artifact |
| --- | --- |
| consequent-fix commit order | consequent-fix declaration |
| QA Report structure | QA Report |
| QA evidence paths | QA Report |

## Final verdict

Pass. Every runnable criterion passed on build `0d30b9b8`; the sole
environment-blocked Pull Request row records its cause and complete equivalent
evidence, and there are no findings, declared blocks, or skipped rows.
