---
spec: 0052-claude-adapter-standardization
date: 2026-07-28
build: aa12daaeb731242dcdbebdf861d35b8996d50859
status: closed
verdict: pass
surfaces: [backend, cli, docs]
---

# QA report — Official Claude adapter and opaque model identifiers

## Scope and environment

Full Spec gate against commit `aa12daaeb731242dcdbebdf861d35b8996d50859`
on the Run Worktree branch
`roundfix/run-run_20260729T015103Z_a46dd05744d49f88`. The Spec target branch
`ma/claude-adapter-standardization` resolves to the same commit. The intended
actors are a Roundfix operator diagnosing adapters, a user migrating acpx
configuration, a profile author selecting an advertised Claude model, a new
user accepting built-in profiles, and an operator diagnosing a refused
Preflight Validation.

The public seat is the built `roundfix` CLI. Real flows use disposable
directories and fake local adapter executables to exercise the production
command boundary without reading or writing the tester's real configuration.
Backend behavior is confirmed through the CLI and through fresh focused Go
tests at the existing public package seams. Documentation is followed from
the operator guides and shipped Roundfix Skill. Evidence belongs under
`qa/evidence/2026-07-28-aa12daa/`.

High-risk probes:

- Doctor: official, legacy, below-pin, unproven same-named executable, and
  missing runtime evidence; these probe misleading optimistic readiness.
- Setup: accept, decline, `--no-input`, dual-runtime migration, and a second
  run; these probe silent writes, partial migration, and idempotency.
- Selection: canonical `opus`, copied `opus[1m]`, invalid `1m` effort,
  adapter-echoed aliases, and the legacy no-independent-control variant; these
  probe the ambiguity and context-window regressions named by the Spec.
- Preflight: a broken Preferred Selection with a configured Fallback Chain;
  this probes the misleading expectation that Preflight substitutes a
  fallback before Run creation.

## Static gate

Pass. `rtk make verify` first encountered the sandbox's unwritable default Go
cache. The exact gate with a Run-local `GOCACHE` reached five owner-process
tests, all blocked by `fork/exec /bin/ps: operation not permitted`; a direct
`/bin/ps` reproduction confirmed the environment boundary. The same
cache-adjusted gate rerun with full OS access exited 0: 2,838 Go tests passed
across 24 packages, four focused Skill tests passed, `roundfix skills check`
passed, and the production binary built. Evidence:
`qa/evidence/2026-07-28-aa12daa/static-gate.txt`.

## Results

| # | Story / criterion / sweep | Actor and surface | Status | Evidence |
| - | --- | --- | --- | --- |
| PC-01 | PRD and TechSpec account for identifier strategy, authentication and HTTP, active ADR obligations, and tooling authority with operative `docs/agents/` sources. | Maintainer; docs | pass | `qa/evidence/2026-07-28-aa12daa/project-constraint-audit.txt` records both artifacts, sources, and reconciled ADRs. |
| PC-02 | Every protected-tooling mutation is expressly authorized and its Task commit changes only the bounded files plus its own Task file. | Maintainer; repository governance | pass | `qa/evidence/2026-07-28-aa12daa/tooling-scope-audit.txt` records Task commit `5512c70`, its exact eight paths, and the current-delta audit. The later unauthorized `.roundfixrc.yml` edit was explicitly reverted by `aa12daa`; the current build matches the base for that file. |
| US-01 | Doctor rejects either deprecated Claude lineage, names the effective command and classification, and gives the official install action; an official adapter reports package/version evidence. | Operator; CLI/backend | pass | Built Doctor runs covered installed official Claude `0.63.0` plus both legacy package paths. Each legacy run reported `adapter_lineage_unknown`, retained Codex evidence, and named the official install action. `qa/evidence/2026-07-28-aa12daa/cli-live-flows.txt`. |
| US-02 | Setup diagnoses a stale Claude override and proposes the official adapter without silently editing it. | Configuration user; CLI/backend | pass | Built Setup diagnosed the wrong-scope legacy fixture, proved the official proposal, named `claude` in the prompt, and preserved the real acpx config hash after `--no-input` and interactive decline. `qa/evidence/2026-07-28-aa12daa/cli-live-flows.txt`. |
| US-03 | Both `opus` and the copied advertised identifier `opus[1m]` prove with explicit `xhigh`. | Profile author; backend/CLI | pass | Real full-access `profiles validate --category frontend --json` passed both tuples through official Claude `0.63.0` with `encoding: independent`; each used a fresh disposable Agent Session. `qa/evidence/2026-07-28-aa12daa/cli-live-flows.txt`. |
| US-04 | `opus / 1m` is rejected as an invalid reasoning effort and names the advertised efforts. | Profile author; backend/CLI | pass | Real profile validation exited 2 with `reasoning_control_not_advertised` and named `default, low, medium, high, xhigh, max`; the adjacent `xhigh` selections passed. `qa/evidence/2026-07-28-aa12daa/cli-live-flows.txt`. |
| US-05 | A fresh built-in `frontend` profile resolves to and proves `claude / opus / xhigh` against the official adapter with no config edit; fallback stays unchanged. | New user; CLI/backend | pass | In a new Git root, built `init --scope project` generated `claude / opus / xhigh` with the unchanged Codex fallback; a separate Show process confirmed it and Setup passed readiness for three distinct selections without a manual edit. `qa/evidence/2026-07-28-aa12daa/cli-live-flows.txt`. |
| US-06 | An operational Preflight refusal names the failed tuple, states the ADR-0050 post-Run-creation Fallback Chain boundary, and gives the next action. | Operator; CLI | pass | Real invalid profile validation printed the tuple, classification, advertised controls, remediation, and exact ADR-0050 boundary while leaving the fallback pending. Fresh Implement/Resolve/Watch tests prove the shared operational Preflight path creates no Run and substitutes none. `qa/evidence/2026-07-28-aa12daa/cli-live-flows.txt`; `qa/evidence/2026-07-28-aa12daa/focused-regressions.txt`. |
| AC-01 | Claude lineage contract accepts official `0.63.0+`, rejects below-pin, legacy, malformed, and unproven paths; path-proven official installs pass. Codex `1.1.4` fails under the `1.1.5` pin; OpenCode remains command-only. | Operator; backend | pass | Real official/legacy Doctor evidence plus fresh Agent lineage tables. `qa/evidence/2026-07-28-aa12daa/cli-live-flows.txt`; `qa/evidence/2026-07-28-aa12daa/focused-regressions.txt`. |
| AC-02 | Opaque parsing applies to config-options and session-snapshot evidence; copied diagnostics remain selectable; adapters without independent reasoning retain `canonical[effort]`. | Profile author; backend | pass | Real canonical/raw selections passed and `1m` failed; fresh capability/assignment tests cover both evidence entry points and the retained variant encoding. `qa/evidence/2026-07-28-aa12daa/cli-live-flows.txt`; `qa/evidence/2026-07-28-aa12daa/focused-regressions.txt`. |
| AC-03 | Setup supports fresh-machine seeding, independent dual-runtime migration, byte-preserving decline/no-input/failure behavior, unrelated agents, and a second-run no-op. | Configuration user; CLI/backend | pass | Live proposal/decline/no-input hashes plus fresh Setup tables for accepted persistence, dual-runtime migration, fresh seed, failure preservation, unrelated bytes, and idempotency. `qa/evidence/2026-07-28-aa12daa/cli-live-flows.txt`; `qa/evidence/2026-07-28-aa12daa/focused-regressions.txt`. |
| AC-04 | Doctor collects every runtime in required Preferred Selections and Fallback Chains, deduplicates and sorts them, keeps one `adapter:` line, and preserves the six-check sequence. | Operator; CLI/backend | pass | Three built Doctor flows kept the ordered six-line report and one sorted multi-runtime adapter line; fresh Doctor tables cover fallback-only runtimes and deduplication. `qa/evidence/2026-07-28-aa12daa/cli-live-flows.txt`; `qa/evidence/2026-07-28-aa12daa/focused-regressions.txt`. |
| AC-05 | User guide and shipped Skill describe official Claude `0.63.0`, Codex `1.1.5`, multi-runtime Doctor, Setup migration, `claude / opus / xhigh`, and opaque identifiers; supported paths exclude the deprecated fully scoped package. | Operator/reader; docs | pass | Manual guide/help walk, no-match searches, byte-identical Skill check, Baseline validation, and corrected documentation tests passed. `qa/evidence/2026-07-28-aa12daa/docs-and-nongoals.txt`; `qa/evidence/2026-07-28-aa12daa/focused-regressions.txt`. |
| NG-01 | Fallback Chain activation remains post-Run-creation and notification-first; only refusal diagnostics change. | Operator; CLI/backend | pass | Focused notification ordering and no-dynamic-Preflight-substitution tests passed; changed production paths add no activation mechanism. `qa/evidence/2026-07-28-aa12daa/docs-and-nongoals.txt`. |
| NG-02 | The Model Catalog allowlist is unchanged and explicit custom model strings still travel through Exact Agent Selection Proof. | Profile author; backend | pass | `internal/agent/catalog.go` has no branch diff; catalog and custom-string regressions passed. `qa/evidence/2026-07-28-aa12daa/docs-and-nongoals.txt`; `qa/evidence/2026-07-28-aa12daa/focused-regressions.txt`. |
| NG-03 | Codex adapter behavior changes only at the minimum version pin. | Operator; backend | pass | Fresh Codex official/unready tables passed at the `1.1.5` boundary, and the full gate found no adjacent regression. `qa/evidence/2026-07-28-aa12daa/focused-regressions.txt`. |
| NG-04 | User-owned acpx configuration is never edited without diagnosis and confirmation; neither legacy Claude lineage remains supported. | Configuration user; CLI/backend | pass | Real `--no-input` and decline checks preserved the acpx SHA-256; both legacy lineages failed built Doctor readiness. `qa/evidence/2026-07-28-aa12daa/cli-live-flows.txt`. |
| NG-05 | OpenCode receives no official pinned lineage contract. | Operator; backend | pass | Fresh `TestCheckAdapterPreservesOpenCodeResolutionWithoutVersionExecution` and OpenCode catalog regression passed. `qa/evidence/2026-07-28-aa12daa/focused-regressions.txt`. |
| SW-CLI | Public command names, help, stdout/stderr placement, exit codes, deterministic ordering, invalid-input recovery, and fresh-process behavior remain usable. | Operator; CLI | pass | Built help/version/Doctor/Setup/profile commands and an invalid flag were exercised with recorded exit codes; independent reruns confirmed persistent state. `qa/evidence/2026-07-28-aa12daa/cli-live-flows.txt`. |
| SW-DOC | An operator can follow the documented Doctor, Setup, adapter-install, profile, and opaque-identifier guidance without undocumented knowledge. | Operator/reader; docs | pass | The documented commands were followed against the built binary; official packages, versions, output ordering, migration behavior, and ADR link matched. `qa/evidence/2026-07-28-aa12daa/docs-and-nongoals.txt`. |

## Findings

No in-scope product findings.

Evidence-quality note: Task 07's declared
`go test -run 'TestDocumentation|TestHelp'` command exited 0 with
`[no tests to run]`, so this gate gave it no credit. The corrected exact-name
documentation/help test set passed and is recorded in
`qa/evidence/2026-07-28-aa12daa/focused-regressions.txt`.

Scope note: `.roundfixrc.yml` retains an older comment claiming Roundfix
rejects `opus[1m]` verbatim. The public CLI, user guide, and shipped Skill are
correct. Commit `aa12daa` restored the Project Config because this active
Spec's exact Tooling Authority does not authorize that Verification
configuration path; changing the comment requires separate express
authorization. It does not affect an in-scope row.

## Blocked and skipped

None.

## Coverage

Passed: 6/6 user stories, every grouped acceptance criterion from Tasks 01–08,
6/6 Non-Goals, all declared surfaces, the Project Constraint audit, and the
high-risk behavior probes listed above. Final row counts: 20 pass, 0 fail,
0 blocked, 0 skipped, 0 pending. Impact totals: 0 Blocks-Completion,
0 Data-Loss, 0 Trust-Damage, 0 Friction, 0 Cosmetic.

## Final verdict

Pass — every planned row passed against build
`aa12daaeb731242dcdbebdf861d35b8996d50859`; the Spec can proceed to
PR preparation subject to its separate delivery authority.
