---
spec: 0055-owner-identity-without-fork
date: 2026-07-31
build: 62c4ff10c4f22f4d29de5146b4ddf3f688bf20e4
status: closed
verdict: pass
rows_blocked_environment: 2
rows_blocked_finding: 0
surfaces: [backend, cli, docs]
---

# QA report — Owner identity without fork

## Scope and environment

Full QA gate for the five PRD user stories, every Task acceptance criterion,
the declared Non-Goals, the CLI/docs surface, and the required Project
Constraint audit. The build under test is the Run Worktree head on macOS
Darwin; production-like CLI checks use a binary built from that head and an
isolated Roundfix Home. The prompt confirms that no Pull Request is open for
the Spec target branch, so Pull Request observation is planned as an
environment-blocked row with equivalent local ancestry and verification
evidence.

Evidence root:
`qa/evidence/2026-07-31-run-01/`.
Every evidence filename in the Results table is relative to this root.

## Project Constraint audit

Pass. Both active Spec artifacts account for identifier strategy,
authentication and HTTP, ADR-0044/ADR-0052 obligations, and tooling authority
with operative `docs/agents/` sources. The PRD authorization commit
`397227ff` and TechSpec authorization commit `f6b284f` predate every Task
commit. `git diff-tree --no-commit-id --name-only -r` over all five Task
commits found no unbounded protected path. Task 05 contains only its Task file,
the authorized Skill pair, product documentation/context, and the five exact
derived digest paths. A fresh sanctioned regeneration returned
`ok:true, changed:false`. See `project-constraint-audit.txt` and
`baseline-digests.txt`.

## Static gate

`rtk make verify` exited `0` against the final non-executable QA artifact set:
2,935 Go tests passed in 24 packages, four focused Skill tests passed, the
Repository Skill Set check passed, and the production binary built. See
`make-verify.txt`.

## Results

| # | Story / criterion / sweep | Actor and surface | Entry point and planned steps | Expected observable | Independent confirmation / persistence | Evidence | Status |
| - | --- | --- | --- | --- | --- | --- | --- |
| 1 | Project Constraints and protected-tooling ancestry | Maintainer; repository audit | Read PRD/TechSpec constraint rows; inspect each Spec/Task commit with `git diff-tree`; reproduce the sanctioned digest generation | All four required constraint categories are reasoned and sourced; authorization predates Task 05; changed paths and derived pins are bounded | Repeat digest generation returned `changed:false`; only qa/ is uncommitted | `project-constraint-audit.txt`, `baseline-digests.txt` | pass |
| 2 | Authoritative repository Verification | Maintainer; backend/CLI | Run `rtk make verify` unchanged | Exit 0 with format, test, Skill, and build gates green | Final rerun passed after evidence was reduced to non-executable text; status lists only qa/ | `make-verify.txt`, `worktree-state.txt` | pass |
| 3 | Story 1; Task 01 AC 1–2: kernel identity without a subprocess and stable token | Operator; CLI/backend | Seed a controlled Active Run for a live helper process; shadow `ps`; invoke built `roundfix stop --force`; read identity twice through the shipped package boundary | Force Stop reaches the owner proof without invoking `ps`; consecutive live-process tokens match and carry the platform prefix | The `ps` marker stayed absent; the live CLI produced the unreadable proof; 26 fresh ownership tests passed | `public-cli-force-stop.txt`, `focused-backend.txt`, `harness-source.md` | pass |
| 4 | Task 01 AC 3–5: absent PID, correct proc parsing, supported target builds | Operator; backend | Exercise absent-process identity, Linux synthetic stat parsing, and linux/darwin/windows build targets | Gone process stays distinct from read failure; field 22 survives spaces/`)`; all targets compile | 2 Linux parser cases and linux/windows/freebsd cross-target compile probes passed; Darwin passed the full gate | `focused-backend.txt` | pass |
| 5 | Story 2; Task 02 AC 1–3: unreadable and mismatch diagnostics are distinct | Operator; CLI | Force Stop controlled Runs with a legacy/non-comparable token and a comparable wrong token | Unreadable diagnostic names its condition and action; mismatch names PID reuse proof; neither signals without authorization | Both controlled helpers stayed live and Runs stayed Active after refusal | `public-cli-force-stop.txt` | pass |
| 6 | Story 3; Task 03 AC 1–5: failed capture warns once, marks durably, and still creates the Run | Operator; CLI | Attempt a safe capture-failure startup; query the Run repeatedly with `runs list`; run the focused creation/warning contract | One startup warning; `owner_identity_unproven=true` persists; creation succeeds; successful and legacy cases remain unmarked | Healthy Darwin could not safely induce a real sysctl failure; the fresh CLI contract test passed and public list reads proved marker persistence | `capture-failure.txt`, `focused-backend.txt`, `public-cli-force-stop.txt` | blocked (environment: no safe public kernel-read failure injection) |
| 7 | Story 4; Task 04 AC 2–5: supervised unreadable exit and refusal guards | Operator; CLI | Retry unreadable Force Stop with `--owner-identity-unreadable`; pass the flag for matching and mismatched identities; retry unreadable without it | Only unreadable proof permits termination; matching/mismatch exit 2 without signal; default unreadable path fails closed | Unreadable helper exited only after the supervised command; matching/mismatch helpers remained live after refusal | `public-cli-force-stop.txt` | pass |
| 8 | Story 5; Task 04 AC 1: Stop flags work before or after Run ID | Operator; CLI | Force-stop two equivalent controlled Runs with `stop --force <id>` and `stop <id> --force` | Both forms have the same exit, report shape, and Stopped state | Fresh `runs list --state all` confirmed both terminal records | `public-cli-force-stop.txt` | pass |
| 9 | Task 02 AC 4–5 and Feature 6: exited-between-checks, matching identity, initial absence, and legacy empty-token behavior remain intact | Operator; backend/CLI | Run focused ownership proof and Force Stop regressions | Proven absence wins; matching identity proceeds; empty legacy token keeps PID-only behavior | Fresh package tests passed; matching and empty-token controlled Runs both stopped through the public CLI | `focused-backend.txt`, `public-cli-force-stop.txt` | pass |
| 10 | Task 05 AC 1–5: Skill/guide diagnostics, flag precondition, no-subprocess statement, sync, and derived pins | Operator/documentation reader; docs/CLI | Follow Stop help and both guide sections; run Skill sync/check and sanctioned digest command | Both refusals/actions, marker, warning, no-fork claim, and single flag precondition agree; Skill pair and pins are current | Byte comparison and both Skill checks passed; repeat digest generation returned `changed:false` | `docs-and-skill.txt`, `baseline-digests.txt` | pass |
| 11 | Non-Goals: mismatch refusal and compare-and-set terminal completion are not weakened | Operator; CLI/backend | Try supervised flag on mismatch; rerun terminal completion regressions | No signal on mismatch; competing terminal outcomes remain rejected and replay stays idempotent | Mismatch helper remained live; 21 fresh store state/terminal tests passed | `public-cli-force-stop.txt`, `focused-backend.txt` | pass |
| 12 | Non-Goals: Windows/stub behavior, Stop Request semantics, signaling/session order, and legacy degradation stay unchanged | Operator; CLI/backend | Build Windows/stubs; run focused Stop Request, Force Stop order, and legacy regressions | No new Windows capture contract; graceful/force flow and Agent Session boundary remain ordered; legacy rows still work | Target builds and 24 fresh CLI regression cases passed | `focused-backend.txt` | pass |
| 13 | CLI UX and behavior probes: help, invalid/out-of-order inputs, repeat/deep-read, and color-independent output | Operator; CLI/docs | Read `stop --help`; try trailing flags, flag without `--force`, flag against wrong proof, repeated stop, and `ROUNDFIX_COLOR=never` list output | Help is actionable; invalid combinations exit 2 without mutation; repeated reads/stops are deterministic; marker is text, not color-only | Exit/output comparisons and persisted Run state matched every expectation | `cli-ux-probes.txt`, `public-cli-force-stop.txt` | pass |
| 14 | Pull Request review/check/Merge-Ready journey | Reviewer; Pull Request | Observe the Spec target branch Pull Request if one exists | Approval/check/thread/head evidence covers this build | Prompt confirms no open Pull Request; target/run ancestry is linear and the PRD has no Pull Request-specific criterion | `pull-request-environment.txt` | blocked (environment: no open Pull Request) |

## Findings

None. No product behavior contradicted the Spec.

## Blocked and skipped

- Row 6 — `blocked (environment: no safe public kernel-read failure
  injection)`. Equivalent current-build evidence covers warning cardinality,
  successful creation, marker set/unset, legacy migration, and repeated public
  marker reads. Unblock with a supervised host resource-failure exercise.
- Row 14 — `blocked (environment: no open Pull Request)`. The prompt proves the
  absence; target/run ancestry, exact commit scope, and the full local gate are
  the equivalent pre-PR evidence. Unblock after Daemon integration and opening
  a Pull Request from the Spec target branch.
- Skipped: none. Finding-blocked: none.

## Coverage

Covered: 5/5 user stories, 25/25 Task acceptance criteria, 4/4 Non-Goal
groups, Project Constraints, authoritative Verification, and CLI/docs sweeps.
Every runnable criterion passed. Story 3's creation-time kernel failure and
the Pull Request journey are environment-blocked with equivalent observed or
supervised evidence. All five high-risk probes passed: out-of-order flag,
invalid supervised flag, repeated stop/read, deep persisted read, and
color-independent marker.

Row totals: 12 pass, 0 fail, 2 blocked (environment), 0 blocked (finding),
0 skipped, 0 pending. Findings by impact: Blocks-Completion 0, Data-Loss 0,
Trust-Damage 0, Friction 0, Cosmetic 0.

## Final verdict

Pass — every runnable row passed, both environment-blocked rows carry their
cause, exact unblocking action, and equivalent current-build evidence, and no
row failed, was finding-blocked, or was skipped.
