---
spec: 0073-skill-versions-decoupled-from-the-binary
date: 2026-08-06
build: 5553c3aa820635e132d231fb89316d2320029f18
status: closed
verdict: partial
rows_blocked_environment: 0
rows_blocked_finding: 0
rows_blocked_declared: 1
surfaces: [backend, cli, docs]
---

# QA report — Skill versions decoupled from the binary

## Scope and environment

Full same-day rerun of the authored terminal gate on macOS in the isolated Run
Worktree. The actors are a Roundfix maintainer evolving an owned Skill and an
operator checking repository readiness. Public entry points are `make verify`,
the built `roundfix doctor` and `roundfix skills check` commands, the installed
Skill files, and the documented regeneration commands.

The PRD declares backend, CLI, and docs surfaces. It declares no frontend,
TUI, data migration, live service, credential, deployment, release, or Pull
Request journey. The prompt confirms no Pull Request is open. Success Metric 7
is declared unreachable unless a maintainer grants a bounded
`skills-lock.json` provenance change for the three named external Go Skills.

High-risk probes are an owned-Skill content edit without regeneration; equal,
newer, below-minimum, and missing-version states; an unreadable owned source
versus an absent Skill; a versionless and lower-version third-party Skill;
strict generated-guide digest drift; regeneration-only recovery; a pre-Spec
Baseline fixture; archived-artifact preservation; member-set expansion; and
corpus/literal absence sweeps.

Evidence directory:
`qa/evidence/2026-08-06-spec-0073-rerun-01/`.

## Static gate

`rtk make verify` ran unpiped from the Run Worktree root and exited 0. It
reported 3,522 passing Go tests across 26 packages, one passing isolated corpus
budget test, four passing Skill contract tests, a passing repository Skill Set
check, a successful build, and no Spec Consistency Check finding for Spec 0073.

Evidence: [`static-and-governance.md`](evidence/2026-08-06-spec-0073-rerun-01/static-and-governance.md).

## Results

| # | Story / criterion / sweep | Actor, entry point, and surface | Steps, expected observable, independent confirmation, persistence, and evidence | Status |
| - | --- | --- | --- | --- |
| Q-01 | Authored gate and dependency preconditions | QA Agent · `_tasks.md` and Task files · docs | `_tasks.md` names `task_06` as the sole terminal `qa` node. Its completed dependency `task_07` transitively closes every non-QA leaf; the Daemon retains settlement authority for `task_06`. | pass |
| Q-02 | Project Constraint — identifier strategy | Maintainer · PRD/TechSpec, implementation range, and `docs/agents/domain.md` · docs/backend | Both active artifacts identify the skill-declared version as a skill-owned comparison value and cite the operative domain guide. The implementation reads declarations and minima; it does not mint or infer a version. | pass |
| Q-03 | Project Constraint — authentication and HTTP | Maintainer · PRD/TechSpec, implementation range, and `docs/agents/agent-instructions.md` · docs/backend | Both artifacts cite the operative universal clause. The assembled path reads local Skill files, opens no endpoint, adds no transport/auth policy, and exposes no credential material. | pass |
| Q-04 | Project Constraint — active ADRs | Maintainer · accepted ADRs, PRD/TechSpec, and assembled behavior · docs/backend | ADR-0080/0081/0085/0091 are accepted and correctly applied to verdicts, deterministic fallout, regeneration-only relaxation, and the typed authored node. ADR-0093 is explicitly and correctly scoped to the separate Spec Consistency Check. | pass |
| Q-05 | Tooling authority, exact scope, and commit choreography | Maintainer · authorization record and Git ancestry · docs/infra | Authorization `c1ee13f9` predates Task 01. Fresh `git diff-tree` evidence covers every implementation/corrective commit and finds protected edits only in the authorized `Makefile` and owned-Skill paths, with sanctioned deterministic fallout and no misordered prerequisite/consequent repair. See [`static-and-governance.md`](evidence/2026-08-06-spec-0073-rerun-01/static-and-governance.md). | pass |
| Q-06 | Repository Verification | Maintainer · `rtk make verify` · backend/cli/docs | The exact unpiped gate exited 0 with 3,522 tests, the isolated budget test, four Skill contract tests, repository Skill Set check, build, and Spec check green. | pass |
| Q-07 | Every owned Skill declares one version and mirrors stay exact | Maintainer · Make targets and installed Skill files · docs/infra | Both positive checks exited 0. Injecting `coding-guidelines` into `OWNED_SKILLS` exited 2 and named its absent top-level version. The Roundfix canonical/mirror comparison was byte-exact. | pass |
| Q-08 | Equal, newer, below, and unversioned readiness stay distinct | Operator · rebuilt CLI fixtures · backend/cli | Current/equal and rebuilt `0.1.0` fixtures passed; rebuilt `0.0.1` blocked; removing the declaration produced `unversioned`. Doctor and Skills Check independently reopened the states. See [`cli-readiness.md`](evidence/2026-08-06-spec-0073-rerun-01/cli-readiness.md). | pass |
| Q-09 | Unreachable source is unresolvable, never missing | Operator · repository readiness fixture · backend/cli | Making the installed Skill file unreadable produced `skills: unversioned`; removing its full directory produced `skills: failed (missing: roundfix)`. The focused error-identity test also passed. | pass |
| Q-10 | Doctor and every Skill gate use one comparison | Operator · rebuilt `roundfix doctor` and `roundfix skills check` fixtures · cli | Both public surfaces agreed for satisfies, below, and unversioned when the binary and installed fixture matched. The below failure named `roundfix`, minimum `0.0.2`, found `0.0.1`, and the project-install upgrade action. | pass |
| Q-11 | Third-party Skills stay outside the owned-version contract | Operator · repository readiness fixture · backend/cli | Current Doctor passed 24 external Skills without an owned declaration. The fresh call-ledger probe passed for a versionless and lower-version third-party fixture, proving the owned comparison never receives it. | pass |
| Q-12 | Owned-Skill content edits do not gate compatibility | Maintainer · isolated tracked repository and exact `make verify` · backend/infra | QA edited canonical and mirrored owned-Skill content without regeneration; the real unpiped gate exited 0. Postflight status listed only those two Skill files, and the adjacent direct test independently proved every derived/corpus/archive byte stable. See [`owned-edit-and-focused.md`](evidence/2026-08-06-spec-0073-rerun-01/owned-edit-and-focused.md). | pass |
| Q-13 | Generated-guide digests still fail closed outside regeneration | Maintainer · strict-load and regeneration fixtures · backend/infra | The focused strict-load test rejected changed generated-guide bytes; regeneration mode accepted the bounded mismatch; adjacent strict and non-allowlisted cases remained rejecting. | pass |
| Q-14 | A pre-Spec Baseline remains valid and archived Specs remain byte-identical | Maintainer · compatibility corpus and exact Git/path digests · backend/docs | Both compatibility corpora and the readoption fixture passed. Exact range checks found no archived-Spec change, and the direct owned-edit probe preserved archived bytes. | pass |
| Q-15 | External Go Skills carry recorded provenance without setup-snapshot membership | Maintainer · `skills-lock.json`, setup snapshots, and `roundfix skills list` · backend/docs | The three Skills exist and dispatch, but lock, snapshot, and public-list searches found no provenance entry. This exactly matches PRD Unreachable Acceptance Success Metric 7: the operative grant excludes third-party Skills and does not authorize `skills-lock.json`. See [`provenance-and-absence.md`](evidence/2026-08-06-spec-0073-rerun-01/provenance-and-absence.md). | blocked (declared: Success Metric 7) |
| Q-16 | The Roundfix Skill documents the shipped contract and its mirror matches | Operator/maintainer · canonical Skill and mirror · docs | The Skill names the minimum comparison, all three states, the four-fact failure, unreachable-versus-missing and third-party boundaries, and the no-regeneration rule. The recursive canonical/mirror comparison exited 0. | pass |
| Q-17 | Acceptance asserts outcomes, not recorded digest/version literals | Maintainer · Spec, test, and corpus sweep · backend/docs | The eight implementation-range Go test files contain no copied `0.0.2` or 64-hex assertion. The dynamic corpus test rejected owned-Skill digests and the structural `contentDigest` sweep found none in the named snapshots/corpora. | pass |
| Q-18 | Non-Goals remain excluded | Maintainer · implementation range and real checks · backend/cli/docs | Setup/profile required-Skill membership did not change; third-party Skills remain outside the owned comparison; generated-guide digests remain strict; no new vendoring path exists; archived Specs and Spec 0067's ownership record are unchanged. Non-Roundfix owned Skills changed only by the declared-version field and mirror regeneration. | pass |

## Findings

None. The previous report's F-001 is corrected on build `5553c3aa`: the real
edited-Skill gate and the direct artifact-stability test both pass. The
previous F-002 now matches a pre-run Unreachable Acceptance declaration and is
classified as Q-15's declared block rather than rediscovered as a finding.

## Blocked and skipped

Q-15 is `blocked (declared: Success Metric 7)`. The observed state matches the
pre-run declaration: the three named external Go Skills exist and dispatch,
but none has a `skills-lock.json` provenance entry or setup-snapshot entry, and
the public list omits them. The cause is the absent protected-tooling grant;
the operative authorization explicitly excludes third-party Skills. A
maintainer must grant `skills-lock.json` for the three exact Skills, then a
bounded Task must record provenance without adding setup-snapshot membership.

Doctor's adapter/profile checks encountered sandbox-local readiness failures,
but its independent `skills:` check completed for every planned state, so no
acceptance row is environment-blocked. No Pull Request is open, and the Spec
defines no Pull Request journey. No row was skipped or finding-blocked.

## Coverage

Seventeen of 18 rows passed and one is declared-blocked. Counts are
`rows_blocked_environment: 0`, `rows_blocked_finding: 0`, and
`rows_blocked_declared: 1`; no row failed or was skipped. All three declared
surfaces ran. Frontend and TUI sweeps are not applicable because the PRD
declares neither.

The equal/newer/below/unversioned, unreadable-source, missing-Skill,
third-party call-ledger, owned-content edit, strict digest, regeneration,
legacy Baseline, archived-byte, member-expansion, literal/corpus absence,
documentation, and Non-Goal probes ran. Findings by impact: zero in every
tier.

## Final verdict

Partial: all 17 runnable rows passed with fresh evidence, but Q-15 remains
blocked for the exact pre-run Success Metric 7 declaration. Result: 17 pass,
0 fail, 0 environment-blocked, 0 finding-blocked, 1 declared-blocked, and
0 skipped. Grant and execute the bounded external-Skill provenance Task before
rerunning for `pass`.
