---
spec: 0044-upgrade-retention-and-formatter-compatibility
date: 2026-07-22
build: d9c6a36cf83f4296e235e7a0fff6558204d1199a
status: closed
verdict: pass
surfaces: [cli, docs, infra]
---

# QA report — Context-Driven Baseline upgrade retention and formatter compatibility

## Scope and environment

Full Spec QA on the daemon-owned Run Worktree at commit
`d9c6a36cf83f4296e235e7a0fff6558204d1199a`. The actors are a repository
maintainer using the setup CLI and an Agent reading the generated guidance.
The production-like entry point is
`.agents/skills/setup-context-driven/scripts/context_setup.py`; scratch
repositories must use bundled profiles and assets. The pinned real formatter
probe is `rtk bunx oxfmt@0.59.0 --check AGENTS.md docs/agents` in a disposable
TypeScript/Bun repository.

All nine Task files were `completed` before execution. No earlier QA Report
exists for this Spec. Evidence will be recorded in this report and under
`qa/evidence/2026-07-22-spec-0044/`.

## Static gate

`rtk make verify` exited `0`. It reported 1,694 passing Go tests across 20
packages, 170 passing canonical setup tests, 170 passing distributed setup
tests, successful asset loading, a passing Roundfix skill check, and a
successful Go build. Task Result evidence is therefore eligible for focused
credit where the named checks remain present in this build.

## Results

| # | Story / criterion / sweep | Actor and surface | Entry point and steps | Expected observable and independent confirmation | Persistence / probes | Evidence | Status |
| - | --- | --- | --- | --- | --- | --- | --- |
| QA-01 | Repository Verification and reproducible Task evidence | Maintainer; infra | Run `rtk make verify` from the repository root. | Exit `0`; formatting, Go tests, both setup skill suites, asset loading, skill synchronization, and build all pass. Named Task tests become creditable only after this gate. | Rerun any failing focused check only to diagnose; a code-caused failure stops flow QA. | [E-01](#e-01-full-repository-verification) | pass |
| QA-02 | Stories 1–2: fail-closed retention for unaccounted, unknown, unreachable, or weakened clauses | Maintainer; CLI | Through `context_setup.py audit` and `apply`, exercise the pre-0.9 fixture with one unaccounted clause, an unknown fingerprint, and weakened or missing targets. | Exit `1`, stable blocking finding names the clause or target, and repository bytes remain unchanged. A fresh audit confirms the same block. | Invalid/out-of-order input; apply attempt after blocked preview; compare repository snapshot before and after. | [E-02](#e-02-fail-closed-retention) | pass |
| QA-03 | Stories 1–2: complete normative Change Plan, confirmation, and idempotency | Maintainer; CLI | Resolve the known legacy transition; compare text and JSON accounting; confirm its digest; apply; audit again; apply again. Change only retention accounting and reuse the old digest. | Retained, moved, replaced, and rejected entries have identical stable order and reasons in text/JSON; accounting changes `planDigest`; stale confirmation exits `3`; successful apply records baseline identity; fresh audit and second apply have no file delta. Existing exits `0`, `1`, `2`, and `3` retain their meanings. | Stale confirmation, malformed confirmation, deep reopen through fresh audit, and second apply. | [E-03](#e-03-authorized-retention-transition) | pass |
| QA-04 | Story 3: complete generated operational contracts | Agent; docs and CLI | Apply a resolved bundled profile, then read generated Spec routing, findings, general-work, autonomous-work, and Secondbrain guides as the intended Agent. | The generated corpus distinguishes Spec routing from tracker hygiene; contains the full findings lifecycle and template; requires acceptance evidence and slice discipline; prohibits Supervisor feature implementation and names delegation; and gives the selected Secondbrain query, triggers, citations, secret safety, and read-only boundaries. Fresh audit remains clean. | Reopen generated files from disk; verify two supporting guides do not carry identical effective clause lists. | [E-04](#e-04-generated-operational-guidance) | pass |
| QA-05 | Story 4: one actionable dispatch entry per installed skill | Agent; docs and CLI | Apply each bundled profile and inspect `docs/agents/skill-dispatch.md` against its selected Repository Skill Set. Reapply one profile. | Every active installed skill appears once; distinct triggers remain under one ordered entry; framework-specific dispatch appears only when the profile proves the capability; repeated rendering is byte-identical. | Cross-profile comparison, repeated apply, and near-duplicate catalog negative check credited from current Task evidence. | [E-05](#e-05-skill-dispatch) | pass |
| QA-06 | Story 6: Repository-Owned Extension lifecycle | Maintainer; CLI | With `repository.extension.enabled=true`, preview and confirm first creation; edit the unmarked extension; audit, reapply, change profile/decision, then remove it. Also exercise `false`. | First creation is a visible, digest-confirmed operation outside `managedArtifacts`; later flows preserve modified bytes exactly; `false` neither creates nor removes; a missing previously selected extension is reported without recreation. | Modified bytes, false decision, profile transition, missing-file recovery, and second apply. | [E-06](#e-06-repository-owned-extension) | pass |
| QA-07 | Story 6: informational delegation floor | Maintainer; CLI | Audit scratch repositories with nested uncovered delegation, alias without delegation, covered delegation, managed/ignored/symlinked text, duplicates, and scan-limit pressure. | Eligible uncovered delegation emits deterministic `delegation.baseline-floor` information naming document/category; other cases emit none; duplicates collapse; limits fail safely; findings do not alter the Change Plan, bytes, or clean exit. | Symlink, ignored tree, duplicate paragraphs, and byte/file-count limits. | [E-07](#e-07-delegation-floor) | pass |
| QA-08 | Story 5: real Formatter-Stable Output composition | Maintainer; CLI and infra | In a disposable TypeScript/Bun repository: confirmed apply; run `rtk bunx oxfmt@0.59.0 --check AGENTS.md docs/agents`; run selected Verification; fresh audit; second apply; inspect diff. | Real pinned Oxfmt exits `0`; Verification exits `0`; audit is clean; second apply has empty `plannedChanges`; repository diff is empty. Audit and apply themselves execute no formatter. | Reopen after formatter check; second apply; run with Oxfmt absent for the ordinary hermetic proof. | [E-08](#e-08-real-formatter-composition) | pass |
| QA-09 | Declarative contract and legacy-ledger acceptance criteria from Tasks 01 and 03 | Maintainer; infra | Credit named mutation and side-effect tests only if QA-01 passes; inspect their current output and stable diagnostics. | Valid values normalize deterministically; invalid/unsafe contracts fail before repository inspection; every prior clause has one accepted mapping or recorded exclusion; weakened mappings fail; retired/conflicting clauses stay absent; loading performs no writes, process execution, or network access. | Repeat load; one-field mutations; side-effect guards. | [E-09](#e-09-declarative-contracts-and-ledger) | pass |
| QA-10 | Documentation sweep and public CLI compatibility from Task 09 | Maintainer and Agent; docs and CLI | Follow the canonical setup skill and user guide without implementation-only knowledge; compare documented command forms, JSON fields, exit categories, stdout/stderr ownership, offline boundary, retention recovery, extension ownership, delegation floor, dispatch, formatter proof, and maintainer synchronization steps with live help and flows. | Documented commands run as written; guidance matches observed behavior; canonical and distributed skill trees describe the same contract. | Invalid arguments; text versus JSON; fresh reader path through both documents. | [E-10](#e-10-documentation-and-cli-contract) | pass |
| QA-11 | Non-Goals and scope-creep sweep | Maintainer; CLI, docs, and infra | Inspect generated output and repository diff after all flows. | No legacy sample restoration, target-repository content restoration, retired sparse-checkout flow, obsolete model/runtime/product assumptions, conflicting REST policy, unconfirmed technology dispatch, nested instruction rewriting, formatter mutation, Doctor expansion, or Baseline ADR expansion. Nested instructions are read only for delegation findings. | Search generated corpus for retired clauses and inspect nested instruction bytes before/after. | [E-11](#e-11-non-goals) | pass |
| QA-12 | Canonical/distributed parity and public artifact consistency | Maintainer; infra and docs | Run the focused sync/asset checks and compare canonical/distributed setup trees after the public flows. | Skill trees are byte-identical; every bundled profile declares formatter compatibility; operator docs, generated guidance, assets, and command behavior agree. | Repeat comparison after all QA operations to catch accidental workspace mutation. | [E-12](#e-12-parity-and-artifact-consistency) | pass |

## Acceptance traceability

- PRD stories 1–6 map to QA-02 through QA-08.
- Task 01 criteria map to QA-01, QA-09, and QA-12.
- Task 02 criteria map to QA-04 and QA-12.
- Task 03 criteria map to QA-04, QA-09, and QA-11.
- Task 04 criteria map to QA-05 and QA-12.
- Task 05 criteria map to QA-02 and QA-03.
- Task 06 criteria map to QA-06.
- Task 07 criteria map to QA-07.
- Task 08 criteria map to QA-08 and QA-12.
- Task 09 criteria map to QA-10 and QA-12.
- Every PRD Non-Goal maps to QA-11.

## Evidence

### E-01: Full repository Verification

- Command: `rtk make verify`
- Exit: `0`
- Observed: `Go test: 1694 passed in 20 packages`; both setup suites reported
  `Ran 170 tests` and `OK`; asset loading printed
  `setup-context-driven assets: ok`; the Roundfix skill check passed; and the
  final `rtk go build` completed.
- Independent confirmation: the gate loaded both canonical and distributed
  catalogs and exercised both mirrored test suites before building the CLI.

### E-02: Fail-closed retention

- Public flow: `audit` and `apply` against a legacy manifest with an unknown
  managed-artifact fingerprint both exited `1` with
  `retention.baseline.unknown` naming `docs/agents/setup-context.json`.
- Persistence proof: the scratch repository digest was
  `6951607fe3797eccf3b254b726cc86702842304752fb035368ba44fb8312b48f`
  before and after both commands.
- Focused negative evidence:
  `rtk env PYTHONDONTWRITEBYTECODE=1 python3 -B -m unittest discover -s .agents/skills/setup-context-driven/tests -p 'test_upgrade_retention.py'`
  exited `0` with 7 tests, including unaccounted, unreachable/weakened,
  unknown, stale-confirmation, and unchanged-byte cases.

### E-03: Authorized retention transition

- Public preview exited `3` with only `plan.confirmation.required`, 27
  retention entries sorted by prior clause, all with reasons, and all four
  dispositions: `retained`, `moved`, `replaced`, and `rejected`.
- Text and JSON previews both exited `3`; every clause and reason appeared in
  the same order in both formats.
- Confirmed public apply with digest
  `9745f9b7af2d57ef47ad1f4692d64cbc41f9cb1576cda04ceebea78653454334`
  exited `0`. A fresh apply preview exited `0` with zero `plannedChanges`.
- Independent confirmation: the manifest records
  `generator.baseline=baseline.portable-v3`, retained the unrelated
  `generator.fixtureMetadata=preserved`, and the focused 7-test suite passed
  stale-accounting and exit-code cases.

### E-04: Generated operational guidance

- A confirmed public TypeScript/Bun apply exited `0` and generated the full
  guide corpus. Reopened files showed the four Spec routes, the copyable
  findings frontmatter and all lifecycle semantics, acceptance-evidence and
  slice rules, the Supervisor prohibition plus Roundfix Run delegation, and
  the exact Secondbrain query, consult triggers, citation, secret, and
  read-only boundaries.
- Independent confirmation: a fresh public audit through the deterministic
  installed-skill fixture exited `0` with zero findings and zero
  `plannedChanges`.
- Parity note: external Repository Skill Set bytes use the repository's
  checked test fixture because the QA environment does not contain the
  immutable external skill trees. The setup script, profile, decisions,
  templates, generated Markdown, and repository paths are production assets.

### E-05: Skill dispatch

- The generated TypeScript/Bun dispatch contained exactly 100 top-level skill
  entries for 100 selected setup skills; the sets matched exactly, every
  entry was unique and sorted, and `implement-spec` plus `implement-task`
  retained two distinct triggers under one owner each.
- `test_skill_dispatch.py` exited `0` with 9 tests, covering duplicate owners,
  reused trigger IDs, missing sets, framework isolation, and deterministic
  rendering.
- `test_macro_profiles.py` exited `0` with 8 tests, covering apply, audit,
  reapply, and exact setup/required/dispatch agreement across every bundled
  profile.

### E-06: Repository-Owned Extension

- Public preview with `repository.extension.enabled=true` exited `3` and
  planned exactly one `create repository extension` operation with a null
  preimage and a digest-bound postimage. Confirmed apply exited `0`.
- The created file had no setup marker and was absent from
  `managedArtifacts`; the manifest recorded only its extension identity and
  path.
- After adding repository-owned content, fresh audit and reapply both exited
  `0` with zero changes. The file hash stayed
  `936d211ecb9ef9c1c2474de317d8fcd0124ebf21c0be7b87f5e4b5994ab093d5`.
- A confirmed Rust-to-Go profile transition with the decision changed to
  `false` planned no extension operation and preserved the same hash. After
  removing the file and selecting the extension again, preview exited `1`
  with `reference.repository.missing`, planned no recreation, and left the
  path absent.

### E-07: Delegation floor

- A clean configured Rust fixture first audited at exit `0` with no findings.
  After adding duplicate nested frontend delegation, covered Rust delegation,
  alias-only prose, and ignored dependency-tree delegation, public audit still
  exited `0` and emitted exactly one informational finding:
  `packages/web/AGENTS.md`, `coverage.frontend`.
- The audit planned no changes and preserved the complete scratch repository
  hash at
  `0c5932402a63ff23b12a45a2c24f6d729bae98e237ab9f96d3f1563d5fb7c1fb`.
  Public apply also exited `0`, planned no changes, and retained the finding.
- `test_delegation.py` exited `0` with 6 tests, covering managed spans,
  ignored trees, symlinks, deterministic ordering, duplicate collapse,
  file/byte limits, and unchanged plan authority.

### E-08: Real formatter composition

- The first sandboxed formatter attempt could not write Bun's temp directory.
  The approved full-access rerun of the exact required command,
  `rtk bunx oxfmt@0.59.0 --check AGENTS.md docs/agents`, exited `0` and reported
  all 16 files correctly formatted in 127 ms.
- The selected fixture Verification,
  `rtk env PYTHONDONTWRITEBYTECODE=1 python3 -B .formatter-fixture-verify.py`,
  exited `0`.
- The generated corpus matched pinned golden digest
  `f001aebf81530abb9d5069145db4fe3f3c562306d7030503d65b87687dca5fbb`.
  Fresh audit and second apply, with Oxfmt absent from `PATH`, both exited `0`
  with zero changes and empty stderr.
- Independent persistence proof: the whole disposable repository hash stayed
  `fbb39d3e3ef5000f566e150c33474da3a81a840b00ef7377f3093474a6454ff1`
  across audit and reapply. `test_formatter_compatibility.py` also exited `0`
  with 2 hermetic process-rejection and provenance tests.

### E-09: Declarative contracts and ledger

- `test_upgrade_contracts.py` exited `0` with 6 tests covering deterministic
  immutable loading, stable invalid/unsafe diagnostics, supported schema
  compatibility, and side-effect guards.
- `test_legacy_rule_ledger.py` exited `0` with 3 tests covering exact mapping
  or exclusion accounting, weakened/missing rejection, retired-clause
  absence, and repository-authored byte preservation.
- These focused results run on the same build whose full gate passed in E-01.

### E-10: Documentation and CLI contract

- Live root, `audit`, and `apply` help exited `0` and matched the documented
  subcommands, arguments, stdout/stderr contract, and exits `0`–`3`.
- A malformed public confirmation produced JSON finding
  `plan.confirmation.invalid`, inner exit `2`, and empty stderr. Live retention
  previews proved text/JSON accounting parity; clean and blocked flows proved
  exits `0`, `1`, and `3`.
- The canonical skill and user guide both describe retention accounting,
  failure recovery, unmarked extension ownership, informational delegation,
  one-entry dispatch, and pinned formatter proof. Maintainer guidance names
  transition-ledger review, formatter-provenance refresh, the canonical
  source, `make skills-sync`, and `make skills-sync-check`.

### E-11: Non-Goals

- The generated maximal TypeScript/Bun corpus contained none of
  `.knowledge/`, `Fable`, `<Project name>`, `PostgreSQL 18`, `Better Auth`,
  `standard REST`, or `systems/<domain>`; `rg` exited `1` with no matches.
- The ledger test proved obsolete runtime defaults remain absent when runtime
  guidance is not selected. The dispatch tests proved framework entries stay
  profile-gated.
- Delegation audit preserved the entire repository hash, proving nested
  instructions were read but not rewritten. Formatter `--check`, fresh audit,
  and reapply preserved the formatter fixture hash. No Doctor or Baseline ADR
  behavior was exercised or generated.

### E-12: Parity and artifact consistency

- `rtk make skills-sync-check` exited `0`.
- `rtk diff -qr .agents/skills/setup-context-driven skills/setup-context-driven`
  exited `0` with no differences.
- Catalog inspection reported explicit formatter contracts for all profiles:
  TypeScript/Bun selects `formatter.oxfmt-markdown` 0.59.0; Go CLI/TUI and
  Rust CLI both declare `none`.
- E-01 exercised both 170-test mirrored suites and loaded both catalogs; E-04,
  E-05, E-08, and E-10 confirmed the generated and operator-facing contracts.

## Findings

None.

## Blocked and skipped

None. The initial managed-sandbox Oxfmt attempt lacked Bun temp-directory
access; the approved full-access rerun completed the same exact probe, so no
row remained blocked or skipped.

## Coverage

Passed: 6/6 user stories, all acceptance criteria from 9/9 completed Tasks,
all 9 PRD Non-Goals, and the declared CLI, docs, and infra surfaces. The 12/12
matrix rows passed. High-risk probes covered stale confirmation, invalid
input, reapply, profile transition, repository-authored byte preservation,
symlink/ignored-tree handling, scan limits, and the real pinned formatter.

## Final verdict

Pass. All 12 rows passed with zero failed, blocked, skipped, or pending rows
and zero findings in every impact tier; the Spec satisfies its PRD and Task
acceptance contracts on build `d9c6a36cf83f4296e235e7a0fff6558204d1199a`.
