---
spec: 0020-run-browser
date: 2026-07-07
verdict: pass
surfaces: [cli, docs]
---

# QA Report — Run Browser

## Summary

Exercised the built binary (`bin/roundfix` at the branch head) against the
real Run Database (51 Runs). Every text-surface contract held live: the
Active-only default with its hidden-count note, the eight enriched columns
(agent, UTC start, duration, branch), `--state`/`--limit` with both note
shapes, the removed `--active` flag, and every non-interactive and unknown-id
failure wording. The Run Browser's interactive flows cannot be driven from
this non-TTY session; they are credited from the byte-pinned model and loop
tests in the task Results, while their non-interactive halves were exercised
for real. Verdict: pass.

## Results

| # | Criterion / user story | Verdict | Evidence |
| - | ---------------------- | ------- | -------- |
| 1 | Story 1 — active-first navigable list | pass | Real: `runs list` default printed `No Runs found.` + `(51 terminal Run(s) hidden; use --state all)`, exit 0. Browser interactivity: `TestRunBrowserDefaultShowsActiveRunsNewestFirst`, `TestRunBrowserToggleShowsAllAndDistinguishesActive` (task_03 Result) |
| 2 | Story 2 — row context (agent, start, duration, branch) | pass | Real: `--state all --limit 5` printed 8 columns incl. `claude`, `2026-07-07T18:59:28Z`, `37m`, `ma/run-browser` |
| 3 | Story 3 — attach loop (browser → cockpit → refreshed browser) | pass | `TestAttachRunBrowserLoopOpensCockpitAndRefreshes`, `TestBrowserAttachCockpitIsTheExplicitAttachCockpit`, `TestAttachRunBrowserCancelExitsZeroWithoutAttaching` (task_04 Result) |
| 4 | Story 4 — bounded deterministic text | pass | Real: default bound + `(48 older Run(s) hidden; use --limit 0)` under `--limit 3`; `--limit 0` printed all 51 with empty stderr |
| 5 | Story 5 — number trap closed | pass | Real: `attach 41` → exit 2, `picker numbers are not stable Run ids — pass a run id or run 'roundfix attach' to pick interactively` |
| 6 | `--state` replaces `--active` | pass | Real: `--active` → exit 2 `flag provided but not defined`; `--state terminal` filtered correctly |
| 7 | Non-interactive contracts | pass | Real: bare `runs` non-TTY → exit 2 naming `runs list`; `attach --no-input` → exit 2 naming `runs list` |
| 8 | Non-Goal — no mutations from discovery | pass | Browser model holds no store handle (task_03 Result); cancel test pins zero side effects |
| 9 | Docs match behavior | pass | README/usage/SKILL rewritten (task_05 Result: zero stale `--active`/picker references); drift check green in `make verify` |

## Environment

Built binary from branch `ma/run-browser` head; real Run Database with 51
Runs across four repositories. Non-TTY session: browser navigation credited
from synchronous `Update`-driven model tests (the repo's TUI test contract).
Static gate: `make verify` — 978 tests, 19 packages, skills checks, build.
