---
spec: 0092-a-run-that-can-hand-back-its-work
date: 2026-08-11
build: b60deccf026136d6998b36c4ae9e65bcfba1ea25
status: closed
verdict: fail
rows_blocked_environment: 7
rows_blocked_finding: 0
rows_blocked_declared: 0
surfaces: [backend, cli]
---

# QA report — A Run that can hand back its work

## Scope and environment

Full authored terminal rerun for `task_07` against Run Worktree build
`b60deccf026136d6998b36c4ae9e65bcfba1ea25`. The actor is a Roundfix
maintainer using the built CLI and its daemon backend. Real flows use isolated
scratch repositories and Run stores where the no-commit QA boundary permits
them. The gate does not mutate the user checkout, commit, push, or open a Pull
Request.

The authoring precondition `rtk roundfix spec check
0092-a-run-that-can-hand-back-its-work --strict` exited 0 with `No findings.`
The manifest names `task_07` as the terminal `qa` node; every dependency is
`completed`. The PRD has no `Unreachable Acceptance` declarations. The prompt
states that no Pull Request is open, so Pull Request-dependent journeys remain
environment-blocked and require equivalent current-build evidence.

Evidence target:
`qa/evidence/2026-08-11-run-gate-01/command-evidence.md`.

## Static gate

`rtk env GOCACHE="$PWD/.gocache" go clean -testcache` exited 0. The first
`rtk make verify` attempt was environment-blocked when a repository integration
test reached sandbox-denied `api.github.com`; no code verdict was assigned. The
same repository cache was cleared again, and the authorized exact
`rtk make verify` rerun exited 0. Every Go package line was uncached, the
focused `skills` guard passed, the Roundfix skill check passed, and the binary
rebuilt from `b60deccf`.

## Results

| # | Story / criterion / sweep | Actor, entry point, and surface | Steps, expected observable, independent confirmation, and persistence | Evidence | Status |
| - | --- | --- | --- | --- | --- |
| QA-01 | Goal 1 and ADR-0104 outside evidence: unavailable preferred runtime activates its configured Fallback Selection | Maintainer; built `roundfix implement`; CLI/backend | Start a real Run against a preferred runtime that cannot serve, expect `agent_selection_failed` before Agent output and the notified fallback tuple; reopen events, attempts, and terminal outcome by Run ID | No live Run ID: the active Run Worktree and no-commit contract prevent a committed disposable Spec target. Equivalent current-build macro evidence passed. Outside origin: the pre-Spec 2026-08-08 quota measurement in `docs/handoffs/2026-08-08-two-quotas-out-and-a-gate-that-said-no.md`. See [command evidence](evidence/2026-08-11-run-gate-01/command-evidence.md). | blocked (environment: no authorized committed disposable Spec target for a live fallback Run) |
| QA-02 | Goal 1 guard: output before failure makes fallback ineligible and work-started emits once | Maintainer; built CLI plus public Run/event reads; CLI/backend | Exercise no-output, post-output, and multi-turn cases; expect exactly one `agent_work_started` after first output and no fallback after post-output failure; re-read persisted events/attempts after exit | Three Agent boundary cases, three Daemon fallback cases, and four public-CLI macro cases passed on this build. See [command evidence](evidence/2026-08-11-run-gate-01/command-evidence.md). | pass |
| QA-03 | Goal 2: failed Batch preserves a resolved Review Issue while unfinished work leaves the Run `Unresolved` | Maintainer; built `roundfix resolve`; CLI/backend | Run a multi-issue Batch that resolves one issue then fails; expect terminal outcome preserved, unfinished issue failed, exit 1 and `Unresolved`; reopen issue artifacts and Run record | No open Pull Request exists. Equivalent current-build Agent, Daemon, and public-CLI integration cases passed. See [command evidence](evidence/2026-08-11-run-gate-01/command-evidence.md). | blocked (environment: no open Pull Request with a runnable multi-issue Batch) |
| QA-04 | Goal 5: no path reports `Clean` while unresolved work remains | Maintainer; built CLI plus daemon outcome paths; CLI/backend | Sweep Batch failure, Verification failure, continuation, and journal paths; expect `Unresolved` whenever count is positive; confirm via persisted issues and terminal events on fresh reads | Four Daemon outcome cases and four CLI integration cases passed on this build. See [command evidence](evidence/2026-08-11-run-gate-01/command-evidence.md). | pass |
| QA-05 | Goal 2 positive seam: failed Batch with all Review Issues resolved may end `Clean` | Maintainer; built CLI/backend | Fail teardown or Verification after settlement; expect terminal issue preserved and zero unresolved work yields `Clean`; reopen issue and Run state after exit | Current-build Daemon and CLI settlement/outcome cases passed with persisted issue and Run assertions. See [command evidence](evidence/2026-08-11-run-gate-01/command-evidence.md). | pass |
| QA-06 | Goal 4: explicitly discard a superseded Run Branch after recording evidence | Maintainer; built `roundfix reconcile --discard-superseded`; CLI/backend | Apply to reachable scratch history; expect record with Run, branch, commits, files, and reason before removal; reopen record after branch/worktree absence and rerun report mode | Direct journey needs committed scratch history prohibited by this gate. Equivalent CLI/Daemon integration cases passed, including durable record after removal. See [command evidence](evidence/2026-08-11-run-gate-01/command-evidence.md). | blocked (environment: no authorized committed scratch Run Branch) |
| QA-07 | Goal 4 refusal probes: unreachable commit and record-write failure preserve both Git surfaces | Maintainer; built reconcile disposition; CLI/backend | Attempt both refusals; expect nonzero diagnostics naming the condition; compare exact branch/worktree snapshots before and after and rerun report mode | Same scratch-history boundary as QA-06. Both current-build refusal cases passed with exact surface-preservation assertions. See [command evidence](evidence/2026-08-11-run-gate-01/command-evidence.md). | blocked (environment: no authorized committed scratch Run Branch) |
| QA-08 | Goal 3: carry a stopped Run's settled Task forward on unchanged inputs | Maintainer; built `roundfix reconcile --carry-forward`; CLI/backend | Apply to a stopped scratch Run; expect checkout Task `completed` with source Run and settlement commit; reopen Task and implementation commit through fresh public reads | Direct journey needs a committed stopped scratch Run prohibited by this gate. Equivalent current-build CLI/spec cases passed with provenance. See [command evidence](evidence/2026-08-11-run-gate-01/command-evidence.md). | blocked (environment: no authorized committed scratch stopped Run) |
| QA-09 | Goal 3 moved-input refusal names Task/input and changes nothing | Maintainer; built carry-forward; CLI/backend | Change a declared input after settlement and apply; expect exit 2 naming Task/input; compare checkout HEAD, Task bytes, and implementation path before/after | Same scratch-history boundary as QA-08. The current-build refusal case passed and asserts exact checkout preservation. See [command evidence](evidence/2026-08-11-run-gate-01/command-evidence.md). | blocked (environment: no authorized committed scratch stopped Run) |
| QA-10 | Goal 3 atomicity: mixed eligible/ineligible Tasks refuse as a set | Maintainer; built carry-forward; CLI/backend | Present two settled Tasks and move one input; expect neither carried; reopen both Tasks and confirm neither commit reached checkout | Same scratch-history boundary as QA-08. The current-build mixed-set case passed and asserts neither Task was carried. See [command evidence](evidence/2026-08-11-run-gate-01/command-evidence.md). | blocked (environment: no authorized committed scratch stopped Run) |
| QA-11 | Explicit-action and Non-Goal sweep: report-only reconcile never disposes, carries, or retries | Maintainer; built `roundfix reconcile`; CLI/backend | Run without mutation flags; expect candidates only and byte-identical DB, checkout, branches, worktrees, and statuses; repeat report invocation | Built JSON dry-run exited 0 with `mode: dry-run`, zero applications, and `carryForwards: []`; exact no-flag integration cases passed. See [command evidence](evidence/2026-08-11-run-gate-01/command-evidence.md). | pass |
| QA-12 | Non-Goal sweep: chain contents/configuration and one-Run override behavior stay unchanged | Maintainer; built CLI/config reads; CLI/backend | Exercise mixed profile order and inspect help; expect only eligibility timing changed and no one-Run chain override; re-read attempts/config and help | Mixed-profile macro passed with persisted fallback order; built help adds no resolve/implement one-Run chain override. See [command evidence](evidence/2026-08-11-run-gate-01/command-evidence.md). | pass |
| QA-13 | CLI sweep: disposition acts are discoverable and preserve stream/exit contracts | Maintainer; built CLI help and parser; CLI | Read top-level/reconcile help and invoke validation paths; expect both flags documented, stdout reserved for requested output, stderr diagnostics, nonzero refusals; repeat help and JSON reads | Both help surfaces omit the flags, although parser refusals prove both flags exist and exit 2. Repeated F-02; see [command evidence](evidence/2026-08-11-run-gate-01/command-evidence.md). | fail |
| QA-14 | Glossary: coined `Selection Failure` and `Branch Disposition` are documented without silent term drift | Maintainer/future contributor; `CONTEXT.md` and emitted vocabulary; docs/CLI | Compare current source tokens and public terms with glossary; expect both coined terms accounted for; repeat focused search against the same build | `Selection Failure` is documented at `CONTEXT.md:199`; `Branch Disposition` is absent. Repeated F-03; see [command evidence](evidence/2026-08-11-run-gate-01/command-evidence.md). | fail |
| QA-15 | Commit-dependent Task scope and protected-tooling chronology audit | Maintainer; Git command surface; repository | Resolve every Task/repair commit with `git diff-tree`; expect only bounded paths and no unauthorized tooling mutation; independently inspect current files and ancestry | Fresh raw `diff-tree` audit found every Task/repair commit bounded, including Task 09, and no protected-tooling mutation. See [command evidence](evidence/2026-08-11-run-gate-01/command-evidence.md). | pass |
| QA-16 | Authoritative genuinely cold static gate | Maintainer; exact repository Verification; backend/CLI | Clear `<worktree>/.gocache` test cache then run exact `rtk make verify`; expect exit 0 and no `(cached)` package lines; inspect captured status/output | Cold authorized gate exited 0; no package line reported `(cached)`. See [command evidence](evidence/2026-08-11-run-gate-01/command-evidence.md). | pass |
| QA-17 | QA Report contract | Maintainer; report artifact; docs | Close every row, counts, evidence links, glossary terms, and verdict; expect terminal machine-readable frontmatter and zero pending rows; run authored report checks and re-read artifact | Closed report carries verdict `fail`, counts 7/0/0, both coined terms, zero pending result rows, and a resolving evidence link. See [command evidence](evidence/2026-08-11-run-gate-01/command-evidence.md). | pass |

High-risk probes are selected for the feature's state and destructive hazards:
no-output versus post-output failure, partial versus fully resolved Batch
failure, unreachable Git history, record-write failure, moved inputs, mixed-set
atomicity, repeated report-only invocation, and missing flag discoverability.

## Findings

### F-02 — reconcile disposition flags remain absent from help (repeated)

- Impact: `Friction`.
- Actor and journey: maintainer, QA-13 built CLI discovery.
- Expected: the named `--discard-superseded` and `--carry-forward` acts are
  visible in top-level and reconcile help.
- Actual: both help surfaces still list only `--apply` and `--format`; the real
  parser accepts both hidden flags and returns their validation failures with
  exit 2.
- Reproduction: run the four built CLI commands in command evidence.
- Affected rows: QA-13.
- History: repeated from F-02 in `qa-report-2026-08-11.md` on build
  `9ee875c3`; Task 09 did not address it.

### F-03 — `Branch Disposition` remains missing from the glossary (repeated)

- Impact: `Trust-Damage`.
- Actor and journey: maintainer/future contributor, QA-14 glossary check.
- Expected: both coined terms required by task 07 are documented in
  `CONTEXT.md`.
- Actual: `Selection Failure` is documented with `agent_selection_failed` at
  `CONTEXT.md:199-200`; the same focused search finds no `Branch Disposition`.
- Reproduction: run the focused glossary search in command evidence.
- Affected rows: QA-14.
- History: repeated from F-03 in `qa-report-2026-08-11.md` on build
  `9ee875c3`; Task 09 did not address it.

## Blocked and skipped

- QA-01 is environment-blocked because this active Run Worktree cannot safely
  host a nested Run and the QA contract forbids constructing the committed
  disposable Spec target a real fallback Run needs. Unblock with a
  maintainer-supplied committed scratch Spec repository/runtime profile or a
  supervised live Run. Equivalent current-build fallback evidence passed. Its
  outside source is the pre-Spec 2026-08-08 real quota measurement in
  `docs/handoffs/2026-08-08-two-quotas-out-and-a-gate-that-said-no.md`, not a
  Spec-authored rehearsal.
- QA-03 is environment-blocked because the prompt confirms there is no open
  Pull Request with the required multi-issue Batch. Unblock with a safe open
  test Pull Request and configured Review Source/runtime. Equivalent
  current-build Batch/outcome evidence passed.
- QA-06 and QA-07 need a committed scratch Run Branch and Worktree. QA-08
  through QA-10 need a committed stopped scratch Run. The no-commit contract
  forbids constructing those histories directly. Unblock with prepared
  disposable Run stores and Git surfaces or explicit scratch-commit authority.
  All corresponding current-build success, refusal, persistence, and atomicity
  integration cases passed.
- No row is finding-blocked, declared-blocked, or skipped. F-02 and F-03 are
  directly observed failures, not blockers masking other rows.

## Coverage

Final coverage: 8 pass, 2 fail, 7 environment-blocked, 0 finding-blocked,
0 declared-blocked, 0 skipped, and 0 pending. PRD goals are mapped 5/5;
Non-Goals are mapped 4/4. Backend and CLI surfaces were swept. Every
environment-blocked row records its cause, exact unblocking action, and fresh
equivalent current-build evidence. The prior static F-01 regression is fixed;
repeated F-02 and F-03 remain.

## Final verdict

Fail: document `--discard-superseded` and `--carry-forward` in both help
surfaces and add `Branch Disposition` to `CONTEXT.md`, then rerun QA-13,
QA-14, the cold gate, and an adjacent reconcile JSON canary. Row counts:
8 pass, 2 fail, 7 environment-blocked, 0 finding-blocked, 0 declared-blocked,
0 skipped. Finding impact counts: 0 Blocks-Completion, 0 Data-Loss,
1 Trust-Damage, 1 Friction, 0 Cosmetic.
