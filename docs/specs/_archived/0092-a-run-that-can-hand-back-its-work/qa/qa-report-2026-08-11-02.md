---
spec: 0092-a-run-that-can-hand-back-its-work
date: 2026-08-11
build: 32ccc445c9591a59687b2fe2d90e737402f49974
status: closed
verdict: fail
rows_blocked_environment: 7
rows_blocked_finding: 0
rows_blocked_declared: 0
surfaces: [backend, cli]
---

# QA report — A Run that can hand back its work

## Scope and environment

Full authored terminal rerun for `task_07` against Run Worktree build
`32ccc445c9591a59687b2fe2d90e737402f49974`. The actor is a Roundfix maintainer
using the built CLI and its daemon backend. The gate does not mutate the user
checkout, commit, push, or open a Pull Request.

The authoring precondition `rtk roundfix spec check
0092-a-run-that-can-hand-back-its-work --strict` exited 0 with `No findings.`
The manifest names `task_07` as the terminal `qa` node; every dependency is
`completed`. The PRD has no `Unreachable Acceptance` declarations. The prompt
states that no Pull Request is open.

Evidence target:
`qa/evidence/2026-08-11-run-gate-02/command-evidence.md`.

## Static gate

`rtk env GOCACHE="$PWD/.gocache" go clean -testcache` exited 0. The first exact
`rtk make verify` attempt was environment-blocked at sandbox-denied
`api.github.com`; the same repository cache was cleared again, and the gate was
rerun with that integration boundary authorized.

The authorized cold gate exited 2. Every listed Go package was uncached. The
only failed case was `TestRunCommandHelp/reconcile`: it still expects the old
reconcile synopsis without `--discard-superseded` or `--carry-forward`, while
Task 10 correctly added both names to the public help. Finding F-04 records this
code-caused exact-help-contract regression.

## Results

| # | Story / criterion / sweep | Actor, entry point, and surface | Steps and expected observable | Independent confirmation and persistence | Evidence | Status |
| - | --- | --- | --- | --- | --- | --- |
| QA-01 | Goal 1 and ADR-0104 outside evidence: unavailable preferred runtime activates its configured Fallback Selection | Maintainer; built `roundfix implement`; CLI/backend | Start a real Run against a preferred runtime that cannot serve; expect `agent_selection_failed` before Agent output and the notified fallback tuple | Reopen events, attempts, and terminal outcome by Run ID | No live Run ID: the active Run Worktree and no-commit contract prevent a committed disposable Spec target. Equivalent current-build Agent, Daemon, and public-CLI cases passed. Outside origin: `docs/handoffs/2026-08-08-two-quotas-out-and-a-gate-that-said-no.md:16-20,167-171`. See [command evidence](evidence/2026-08-11-run-gate-02/command-evidence.md). | blocked (environment: no authorized committed disposable Spec target for a live fallback Run) |
| QA-02 | Goal 1 guard: output before failure makes fallback ineligible and work-started emits once | Maintainer; built CLI and public Run/event reads; CLI/backend | Exercise no-output, post-output, and multi-turn cases; expect exactly one `agent_work_started` after first output and no fallback after post-output failure | Re-read persisted events and attempts after exit | Three Agent boundary cases, three Daemon fallback cases, and four public-CLI macro cases passed. See [command evidence](evidence/2026-08-11-run-gate-02/command-evidence.md). | pass |
| QA-03 | Goal 2: failed Batch preserves a resolved Review Issue while unfinished work leaves the Run `Unresolved` | Maintainer; built `roundfix resolve`; CLI/backend | Run a multi-issue Batch that resolves one issue then fails; expect preserved terminal outcome, unfinished issue failed, exit 1 and `Unresolved` | Reopen issue artifacts and Run record | The prompt confirms no open Pull Request. Equivalent current-build Agent, Daemon, and public-CLI integration cases passed. See [command evidence](evidence/2026-08-11-run-gate-02/command-evidence.md). | blocked (environment: no open Pull Request with a runnable multi-issue Batch) |
| QA-04 | Goal 5: no path reports `Clean` while unresolved work remains | Maintainer; CLI and daemon outcome paths; CLI/backend | Sweep Batch failure, Verification failure, continuation, and journal paths; expect `Unresolved` whenever the unresolved count is positive | Re-read persisted issues and terminal events | Current-build Daemon outcome and CLI integration cases passed with persisted-state assertions. See [command evidence](evidence/2026-08-11-run-gate-02/command-evidence.md). | pass |
| QA-05 | Goal 2 positive seam: failed Batch with all Review Issues resolved may end `Clean` | Maintainer; built CLI/backend | Fail teardown or Verification after settlement; expect preserved terminal issue and zero unresolved work yields `Clean` | Reopen issue and Run state after exit | Current-build settlement and Run outcome cases passed. See [command evidence](evidence/2026-08-11-run-gate-02/command-evidence.md). | pass |
| QA-06 | Goal 4: explicitly discard a superseded Run Branch after recording evidence | Maintainer; built `roundfix reconcile --discard-superseded`; CLI/backend | Apply to reachable scratch history; expect durable record before branch and worktree removal | Reopen record after both Git surfaces are absent and rerun report mode | A direct journey needs committed scratch history prohibited by this gate. Equivalent CLI/Daemon cases passed, including durable record after removal. See [command evidence](evidence/2026-08-11-run-gate-02/command-evidence.md). | blocked (environment: no authorized committed scratch Run Branch) |
| QA-07 | Goal 4 refusal probes: unreachable commit and record-write failure preserve both Git surfaces | Maintainer; built reconcile disposition; CLI/backend | Attempt both refusals; expect nonzero diagnostics naming the condition and no removals | Compare exact branch/worktree snapshots before and after | Same scratch-history boundary as QA-06. Both current-build refusal cases passed with exact surface-preservation assertions. See [command evidence](evidence/2026-08-11-run-gate-02/command-evidence.md). | blocked (environment: no authorized committed scratch Run Branch) |
| QA-08 | Goal 3: carry a stopped Run's settled Task forward on unchanged inputs | Maintainer; built `roundfix reconcile --carry-forward`; CLI/backend | Apply to a stopped scratch Run; expect checkout Task `completed` with source Run and settlement commit | Reopen Task and implementation commit through fresh public reads | A direct journey needs a committed stopped scratch Run prohibited by this gate. Equivalent current-build CLI/spec cases passed with provenance. See [command evidence](evidence/2026-08-11-run-gate-02/command-evidence.md). | blocked (environment: no authorized committed scratch stopped Run) |
| QA-09 | Goal 3 moved-input refusal names Task/input and changes nothing | Maintainer; built carry-forward; CLI/backend | Change a declared input after settlement and apply; expect exit 2 naming Task/input | Compare checkout HEAD, Task bytes, and implementation path before and after | Same scratch-history boundary as QA-08. The current-build moved-input case passed and asserts exact checkout preservation. See [command evidence](evidence/2026-08-11-run-gate-02/command-evidence.md). | blocked (environment: no authorized committed scratch stopped Run) |
| QA-10 | Goal 3 atomicity: mixed eligible/ineligible Tasks refuse as a set | Maintainer; built carry-forward; CLI/backend | Present two settled Tasks and move one input; expect neither carried | Reopen both Tasks and confirm neither implementation commit reached checkout | Same scratch-history boundary as QA-08. The current-build mixed-set case passed and asserts neither Task was carried. See [command evidence](evidence/2026-08-11-run-gate-02/command-evidence.md). | blocked (environment: no authorized committed scratch stopped Run) |
| QA-11 | Explicit-action and Non-Goal sweep: report-only reconcile never disposes, carries, or retries | Maintainer; built `roundfix reconcile`; CLI/backend | Run without mutation flags; expect candidates only and byte-identical repository/Run state | Repeat report invocation and compare state | Built JSON dry-run exited 0 with zero applications and `carryForwards: []`; exact no-flag integration cases passed. See [command evidence](evidence/2026-08-11-run-gate-02/command-evidence.md). | pass |
| QA-12 | Non-Goal sweep: chain contents/configuration and one-Run override behavior stay unchanged | Maintainer; built CLI/config reads; CLI/backend | Exercise mixed profile order and inspect help; expect only eligibility timing changed and no one-Run chain override | Re-read attempts, configuration, and help | Mixed-profile macro passed with persisted fallback order; built help exposes no resolve/implement one-Run chain override. See [command evidence](evidence/2026-08-11-run-gate-02/command-evidence.md). | pass |
| QA-13 | CLI sweep and prior F-02 retest: disposition acts are discoverable and preserve stream/exit contracts | Maintainer; built CLI help and parser; CLI | Read top-level/reconcile help and invoke validation paths; expect both flags documented, stdout reserved for output, stderr diagnostics, and nonzero refusals | Repeat help and JSON reads | Both built help surfaces now document both flags, resolving prior F-02, but the adjacent exact help-contract test fails on the new synopsis. Finding F-04; see [command evidence](evidence/2026-08-11-run-gate-02/command-evidence.md). | fail |
| QA-14 | Glossary and prior F-03 retest: coined `Selection Failure` and `Branch Disposition` are documented without silent term drift | Maintainer/future contributor; `CONTEXT.md` and emitted vocabulary; docs/CLI | Compare current source tokens and public terms with glossary; expect both coined terms accounted for | Repeat focused search against the same build | `Selection Failure` is documented at `CONTEXT.md:199-201`; `Branch Disposition` is documented at `CONTEXT.md:239-241`, resolving prior F-03. Emitted tokens match. See [command evidence](evidence/2026-08-11-run-gate-02/command-evidence.md). | pass |
| QA-15 | Commit-dependent Task scope and protected-tooling chronology audit | Maintainer; Git command surface; repository | Resolve every Task and corrective commit with `git diff-tree`; expect bounded paths and no unauthorized tooling mutation | Inspect current files and ancestry independently of Task Results | Fresh `core.fsmonitor=false diff-tree` commands resolved all 20 commits. Task commits stayed bounded; authoring/repair commits touched only Spec artifacts or the derived coverage record; no protected tooling path changed. See [command evidence](evidence/2026-08-11-run-gate-02/command-evidence.md). | pass |
| QA-16 | Authoritative genuinely cold static gate | Maintainer; exact repository Verification; backend/CLI | Clear the repository-owned Go cache and run exact `rtk make verify`; expect exit 0 and no cached package lines | Inspect full command exit and output | Cold authorized gate exited 2 on F-04; every listed package was uncached. See [command evidence](evidence/2026-08-11-run-gate-02/command-evidence.md). | fail |
| QA-17 | QA Report contract | Maintainer; report artifact; docs | Close every row, count, evidence link, glossary term, and verdict | Run the authored report checks and re-read the artifact | Closed report carries verdict `fail`, counts 7/0/0, both coined terms, zero pending result rows, and a resolving command-evidence link. | pass |

High-risk probes target the feature's state and destructive boundaries:
no-output versus post-output failure, partial versus fully resolved Batch
failure, unreachable Git history, record-write failure, moved inputs, mixed-set
atomicity, repeated report-only invocation, and flag discoverability.

## Findings

### F-04 — reconcile help contract pins the pre-disposition synopsis

- Impact: `Blocks-Completion`.
- Actor and journey: maintainer, QA-13 and QA-16 built CLI/static gate.
- Expected: the public reconcile help and its exact contract test both name
  `--discard-superseded` and `--carry-forward`, and `rtk make verify` exits 0.
- Actual: Task 10 updated the public help, but
  `TestRunCommandHelp/reconcile` at `internal/cli/cli_test.go:458` still expects
  `roundfix reconcile [run-id] [--apply] [--format <text|json>]` and fails on
  the two added flags.
- Reproduction: clear the repository-owned Go test cache and run `rtk make
  verify`; the exact output is in command evidence.
- Affected rows: QA-13 and QA-16.

## Blocked and skipped

- QA-01 is environment-blocked because this active Run Worktree cannot safely
  host a nested Run and the no-commit contract forbids constructing the
  committed disposable Spec target a real fallback Run needs. Unblock with a
  maintainer-supplied committed scratch Spec repository/runtime profile or a
  supervised live Run. Equivalent current-build fallback evidence passed. Its
  outside source is the pre-Spec 2026-08-08 quota measurement in
  `docs/handoffs/2026-08-08-two-quotas-out-and-a-gate-that-said-no.md`, not a
  Spec-authored rehearsal.
- QA-03 is environment-blocked because the prompt confirms there is no open
  Pull Request with the required multi-issue Batch. Unblock with a safe open
  test Pull Request and configured Review Source/runtime. Equivalent
  current-build Batch settlement and Run-outcome evidence passed.
- QA-06 and QA-07 need a prepared committed scratch Run Branch and Worktree.
  QA-08 through QA-10 need a prepared committed stopped scratch Run. The
  no-commit contract forbids constructing those histories directly. Unblock
  with maintainer-prepared disposable Run stores and Git surfaces or a separate
  supervised QA session authorized to prepare them. All corresponding
  current-build success, refusal, persistence, and atomicity integration cases
  passed.
- No row is finding-blocked, declared-blocked, or skipped. F-04 is a directly
  observed failure, not a blocker masking another row.

## Coverage

Final coverage: 8 pass, 2 fail, 7 environment-blocked, 0 finding-blocked,
0 declared-blocked, 0 skipped, and 0 pending. PRD goals are mapped 5/5;
Non-Goals are mapped 4/4. Backend and CLI surfaces were swept. Every
environment-blocked row records its cause, exact unblocking action, and fresh
equivalent current-build evidence. Prior F-02 and F-03 are fixed; F-04 is new.

## Final verdict

Fail: update the exact reconcile help contract to include
`--discard-superseded` and `--carry-forward`, then rerun QA-13, QA-16, and an
adjacent reconcile JSON/report-only canary. Row counts: 8 pass, 2 fail, 7
environment-blocked, 0 finding-blocked, 0 declared-blocked, 0 skipped. Finding
impact counts: 1 Blocks-Completion, 0 Data-Loss, 0 Trust-Damage, 0 Friction, 0
Cosmetic.
