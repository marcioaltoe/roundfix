---
spec: 0066-run-teardown-reclaims-what-it-created
date: 2026-08-05
build: f1a9b2f2
status: closed
verdict: pass
rows_blocked_environment: 4
rows_blocked_finding: 0
rows_blocked_declared: 0
surfaces: [backend, cli, docs]
---

# QA report — Run teardown reclaims what it created

## Scope and environment

Full authored terminal gate for `task_06` on the Run Worktree build
`f1a9b2f2`. The intended actors are a Roundfix operator stopping or reconciling
Runs and a reviewer starting `watch`. The public entry point is the locally
built `roundfix` CLI against isolated repositories and Run Databases. The gate
must not commit, push, open a Pull Request, or mutate the user checkout.

Pull Request fact supplied to the gate: no Pull Request is open and Pull
Request journeys are environment-blocked. The equivalent Branch Integrity
Preflight journey remains planned against a local real-Git fixture.

Evidence directory:
`docs/specs/0066-run-teardown-reclaims-what-it-created/qa/evidence/2026-08-05-qa-01/`.

## Static gate

`rtk make verify` ran unpiped and exited 0 on build `f1a9b2f2` (the build
metadata correctly rendered `f1a9b2f2-dirty` because this in-progress report
already existed). It reported 3,342 Go tests passed in 26 packages, the
isolated one-second Spec corpus budget passed, four Skill-policy tests passed,
the Roundfix Skill check passed for every required Skill, `roundfix spec check`
reported no findings for Spec 0066, and the production binary built at
`bin/roundfix`.

## Results

| # | Story / criterion / sweep | Actor and surface | Status | Evidence |
| - | --- | --- | --- | --- |
| R01 | Authored gate and Project Constraints: `task_06` is terminal after all non-QA leaves; identifier, auth/HTTP, ADR, and tooling rows cite operative guidance. Steps: inspect the manifest, every Task status, PRD/TechSpec constraints, accepted ADRs, authorization, commit ancestry, and exact changed paths. Expected: valid graph and bounded authorization predating the tooling Task. Independent confirmation: Git ancestry and `diff-tree`. Persistence: committed artifacts remain identical on a fresh read. | Maintainer; docs/Git | pass | `_tasks.md` names terminal `task_06`, whose sole dependency `task_05` is completed after tasks 01–04. Both active Spec artifacts account for all four Project Constraints and cite operative `docs/agents/` sources. Accepted ADRs 0044, 0052, 0053, 0080, 0081, 0091, and the expressly non-applicable relation candidate 0093 agree with those rows. Standing authorization commit `8dda1969` predates tooling Task commit `f1a9b2f2` (`git merge-base --is-ancestor ...` exit 0). `git -c core.fsmonitor=false diff-tree --no-commit-id --name-only -r f1a9b2f2` shows only the authorized Skill pair, task file, and deterministic files inside `DERIVED_DIGEST_PATHS`; no prerequisite or consequent fix commit exists. Fresh `diff -qr` confirms canonical/mirror parity. |
| R02 | Repository Verification. Steps: run `rtk make verify` unpiped. Expected: exit 0 with format, tests, Skill checks, and build passing. Independent confirmation: command exit status and final build output. Persistence: one full fresh invocation on this build. | Maintainer; backend/CLI/docs | pass | `rtk make verify` exited 0: 3,342 Go tests passed in 26 packages; the isolated corpus-budget and Skill-policy tests passed; `roundfix skills check` passed; Spec 0066 had no consistency findings; `bin/roundfix` built successfully. The reported skips are declared scope limits of the consistency checker, not skipped gate tests. |
| R03 | Roundfix Skill contract. Steps: follow the Skill as an operator and compare its `reconcile`, Preflight, and termination text with built help and observed behavior. Expected: both debris kinds and proofs, dry-run default, sole Preflight relaxation, and unprovable termination rule are accurate; canonical and mirror trees are byte-identical. Independent confirmation: `diff -qr` plus public help/flows. Persistence: fresh reads from both trees. | Operator; docs/CLI | pass | `reconcile --help`, `stop --help`, and `watch --help` exited 0 and matched the documented read-only default, `--apply`, Force Stop proof, and Preflight contract. Public JSON exposed the documented `runBranch` proof/action fields; public Force Stop emitted the documented unprovable failure. Focused Skill reads found the process/runBranch, unprovable, and superseded clauses. `diff -qr .agents/skills/roundfix skills/roundfix` exited 0, and `make verify` passed the Skill check. |
| R04 | Stop reaches an outliving descendant. Steps: start a Run-owned process tree whose middle process exits, stop it through the public CLI, and probe every recorded PID. Expected: no descendant remains. Independent confirmation: OS process reads after command completion. Persistence: repeat the probe after a delay. | Operator; CLI/backend | blocked (environment: sandbox denies owner-process identity inspection) | Built `bin/roundfix implement --detach` created real Run `run_20260805T043731Z_b614ba5fc96227f9`; the adapter's middle process exited and `ps` proved both owner PID 3302 and reparented grandchild PID 3370 had PPID 1 and process group 3302. Public `bin/roundfix stop --force ...` then failed at `prove owner process identity: operation not permitted`, so the success path was unreachable in this sandbox. Exact unblock: rerun this fixture in a full-access host session. Equivalent fresh evidence: `rtk go test ./internal/store -count=1 -run '<four named safety tests>' -v` passed the real Unix outliving-grandchild fixture; `rtk go test ./internal/store -count=20 -run 'TerminateTree'` passed 60 cases. The disposable process group was explicitly killed after capture and a fresh `ps` returned no rows. |
| R05 | Per-process termination proof and unprovable state. Steps: observe a proven tree and a fixture whose absence cannot be observed. Expected: one outcome per process; proven absence is distinct from unproven with a non-empty reason, and host silence never reads as success. Independent confirmation: public command/event output and focused fixture evidence. Persistence: reread recorded output. | Operator; CLI/backend | pass | The real public Force Stop returned exit 1 with named step `prove owner process identity`, kept the Run `ResolvingWithAgent`, and said `Run remains Active; Active Run lock retained`; subsequent `roundfix runs list --state active` and `ps` confirmed that state. Fresh named tests passed the complementary proven-outcome and injected host-table-error cases; the 20-run repetition passed. |
| R06 | Foreign-process and owner-identity safety. Steps: keep an unrelated process alive beside the owned tree and attempt a proven identity mismatch. Expected: unrelated process survives; mismatch refuses before signalling; legacy unreadable-identity escape does not widen. Independent confirmation: OS liveness plus exit code/diagnostic. Persistence: second liveness probe. | Operator; CLI/backend | pass | The public failure path left both owned fixture PIDs untouched when identity could not be proved. Fresh `TestOwnerProcessControllerTerminateTreeLeavesUnrelatedProcessRunning` and `TestOwnerProcessControllerRefusesMismatchedOwnerIdentity` passed in the four-test focused command; full `make verify` also passed all existing store behavior. No unreadable-identity bypass flag was supplied. |
| R07 | Four failed-cycle Run Branches classify as a set. Steps: create four failed QA-cycle branches on one target. Expected: newest is current, three older branches are releasable with the existing superseding-QA proof. Independent confirmation: text/JSON classification and Git refs. Persistence: fresh classification before mutation. | Operator; CLI/backend | pass | A real-Git fixture created reports dated August 1–4 on four terminal Run Branches. Public `bin/roundfix reconcile --format json` without a Run ID returned three `runBranchCandidates`, each superseded by `qa-report-2026-08-04.md`, and preserved the fourth as `current QA Report evidence and is not superseded`. Fresh branch-set tests passed 15 cases. |
| R08 | Valuable, ambiguous, Active, and single-branch behavior. Steps: add unintegrated work, malformed supersession evidence, an Active Run, and the legacy single-branch case. Expected: the first three are preserved with reasons; Active is never releasable; the legacy classification is unchanged. Independent confirmation: Git ancestry/content and classification output. Persistence: refs remain present. | Operator; CLI/backend | pass | The public dry-run preserved the current-evidence branch and a separate Active Run branch. After `valuable-untracked.txt` was added to an older worktree, public apply classified it `dirty`, preserved it, and applied only the two still-clean superseded candidates. Fresh file read returned `must survive reconcile`; ref/worktree snapshots retained dirty, current, and Active surfaces. The 15-case focused worktree suite covered unintegrated work, malformed proof, Active guard, legacy single-branch behavior, and stale apply evidence. |
| R09 | Branch Integrity Preflight relaxation. Steps: run `watch` with the four failed-cycle branches. Expected: it proceeds without deleting or moving refs, names each disregarded branch and proof, and every other refusal still blocks. Independent confirmation: before/after ref snapshots and diagnostics. Persistence: repeat the ref snapshot. Probes: ambiguous branch and another Active Run remain blocking. | Reviewer; CLI/backend | pass | Equivalent supervised local journey: fresh `rtk go test ./internal/cli -count=1 -run '<seven named Branch Integrity tests>' -v` passed 14 cases. Its real-Git `watch` fixture asserts exit 0, one diagnostic per disregarded branch with current/superseding report proof and `Git ref left unchanged`, identical full ref snapshots, and no integration call. Negative subtests kept valuable, unclassifiable, Active, lockless-bypass, and existing bypass/audit refusals. External Pull Request observation remains R14. |
| R10 | Reconcile dry-run and additive JSON contract. Steps: run default text and JSON reconcile without a Run ID against process and Run Branch debris. Expected: both kinds are discoverable with proof, ambiguity is preserved, no state changes, legacy JSON fields keep their names and meanings, and additive fields agree. Independent confirmation: Run Database/Git/process snapshots. Persistence: fresh second dry-run is identical. | Operator; CLI/backend | blocked (environment: sandbox denies process identity inspection) | Public no-ID JSON dry-runs exited 0, kept `schemaVersion: roundfix-reconcile/v1`, kept equal `results` and `runs`, reported three/four proven branch candidates depending on fixture state, and preserved current, Active, and dirty evidence. The process kind cannot be made a public candidate because the same host identity read failed with `operation not permitted` in R04. Exact unblock: full-access host session. Equivalent fresh evidence: five focused reconcile cases passed, including both debris kinds, proof fields, unchanged dry-run state, ambiguity, empty JSON, and legacy/additive field equality. |
| R11 | Reconcile apply and idempotence. Steps: apply the inspected candidates, then run apply and dry-run again. Expected: exactly the named candidates are reclaimed and both second passes are no-ops. Independent confirmation: missing owned PIDs/worktrees/refs plus unchanged preserved state. Persistence: second pass. Probe: stale or newly dirty evidence is refused. | Operator; CLI/backend | blocked (environment: sandbox denies process identity inspection) | Public apply released exactly four named clean branch candidates (`runBranchesApplied: 4`) and retained the Active branch. A second public apply returned zero candidates/applies, four `released` results, and the same Active preserved row; Git independently showed only the Active ref/worktree. A separate dirty probe applied two clean candidates and preserved the newly dirty one and its file. Process apply needs the full-access host condition from R04. Equivalent fresh reconcile and store suites passed both-kind exact-apply, per-PID proof, stale evidence, and idempotence cases. |
| R12 | Active Run guard on every reclamation path. Steps: include an Active Run with an owner process and Run Branch in stop/reconcile/Preflight fixtures. Expected: no reclamation path offers, signals, deletes, or disregards it. Independent confirmation: state, PID, worktree, and ref all remain. Persistence: fresh OS and Git reads after every path. | Operator; CLI/backend | pass | Public Force Stop's unprovable path left the real Run Active, retained its lock, and left both PIDs alive. Public reconcile emitted the Active Run branch only under `preservedCandidates`; after apply and the idempotent second pass, independent `runs list`, ref, and worktree reads still showed it. Fresh Preflight, reconcile, and store suites covered the Active process candidate, Active branch, and review guard paths. |
| R13 | Non-Goals and regression boundary. Steps: inspect changed paths and exercise relevant existing contracts. Expected: no Journal Retention/Event Journal/artifact-retention change; no GC/storage-compaction expansion; no deletion of implementation work; no signalling of foreign processes. Independent confirmation: Git diff inventory, focused regression flows, and full gate. Persistence: clean worktree apart from this report. | Maintainer/operator; backend/CLI | pass | `git diff --name-only 555373f3..f1a9b2f2` contains process control, worktree classification, Preflight/reconcile, tests, the authorized Skill pair, Task files, and sanctioned digest fallout; it contains no Journal Retention, Run Event Journal, GC, or artifact-retention implementation path. Public dirty-state evidence and fresh unrelated-process tests prove valuable work/foreign processes are preserved. Current checkout changes are confined to this `qa/` report/evidence tree. |
| R14 | Open Pull Request `watch` journey. Steps: observe the named Open Pull Request read-only. Expected: `watch` proceeds despite superseded failed-cycle branches and keeps other refusals. Independent confirmation: Pull Request state and Git refs. Persistence: second remote read. | Reviewer; Pull Request/CLI | blocked (environment: no open Pull Request) | The gate prompt proves no Pull Request is open and Pull Request journeys are environment-blocked. Exact unblock: open a Pull Request on `ma/spec-0066-implementation` and rerun the read-only journey. Equivalent observed evidence: R09's real-Git supervised `watch` journey passed with four failed-cycle branches, proof diagnostics, unchanged refs, and retained refusals. |

## Findings

No findings.

## Blocked and skipped

R04 is `blocked (environment: sandbox denies owner-process identity
inspection)`. Proof: the production Force Stop returned `operation not
permitted` at the identity step while the same live fixture was observable
through an approved read-only process-table check. Unblocking action: rerun the
same command in a full-access host session. Equivalent observed evidence is the
fresh real-Unix focused fixture and 20-run repetition recorded in R04.

R10 and R11 carry the same process-identity environment cause. Public branch
dry-run/apply/idempotence evidence and fresh both-kind reconcile/store fixtures
provide the equivalent evidence; a full-access host session is the unblocking
action for direct process candidate/apply observation.

R14 is `blocked (environment: no open Pull Request)`. Opening a Pull Request on
`ma/spec-0066-implementation` unblocks the remote read-only journey; R09 is the
equivalent supervised evidence.

## Coverage

All 14 planned rows reached a terminal status: 10 pass and 4
environment-blocked with recorded cause, exact unblocking action, and
equivalent observed or supervised evidence. No row failed, was finding-blocked,
declared-blocked, skipped, or left pending. Stories and criteria: all PRD goals,
6 Core Features, 4 Success Metrics, 4 Non-Goals, and every Task acceptance
criterion covered. Probes exercised: reparented grandchild, host denial,
identity mismatch, unrelated process, four-cycle branch set, Active Run, dirty
evidence, ambiguous evidence, repeated apply, no-ID discovery, JSON
compatibility, and retained Preflight refusals.

## Final verdict

Pass: every runnable criterion passed; all four environment-blocked rows carry
equivalent fresh evidence, and there are zero failed, finding-blocked,
declared-blocked, skipped, or pending rows.
