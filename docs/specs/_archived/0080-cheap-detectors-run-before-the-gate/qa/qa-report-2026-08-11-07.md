---
spec: 0080-cheap-detectors-run-before-the-gate
date: 2026-08-11
build: 9e1de04014497b86cdc11a5a6c0c0784e0523cd1
status: closed
verdict: pass
rows_blocked_environment: 3
rows_blocked_finding: 0
rows_blocked_declared: 0
surfaces: [backend, docs, infra, cli]
---

# QA report — Cheap detectors run before the gate

## Scope and environment

Full terminal rerun of the authored `task_08` QA node on Run Worktree branch
`roundfix/run-run_20260811T102444Z_e48130fc6d2e191c` at
`9e1de04014497b86cdc11a5a6c0c0784e0523cd1`. The intended actors enter through
the built `roundfix` CLI, the Daemon QA path, repository Git and report
artifacts, and the two public verification targets. No Pull Request is open;
Pull Request journeys are environment-blocked and must use equivalent
repository-observed evidence because the Run Worktree branch has no Pull
Request of its own.

The report-only boundary permits only this seeded QA Report and its evidence.
It forbids commits, pushes, Pull Request mutation, and disposable committed
Spec Runs. `rtk roundfix spec check
0080-cheap-detectors-run-before-the-gate --strict` exited 0 with `No findings.`
The Task Graph names `task_08` as the terminal `qa` node, and all six declared
dependencies are completed.

## Static gate

`rtk env GOCACHE="$PWD/.gocache" go clean -testcache` exited 0. The first
`rtk env GOCACHE="$PWD/.gocache" make verify` attempt reached the Go suite but
the managed sandbox denied its existing `api.github.com` integration path.
That is an environment boundary, not a code failure. The authorized rerun
exited 0, but it reused packages warmed by the interrupted attempt, so the
test cache was cleared again before accepting timing evidence.

The resulting cold `rtk env GOCACHE="$PWD/.gocache" make verify` exited 0 in
about 106.37 seconds; `internal/cli`, the earlier seeded-report failure
surface, ran uncached and passed in 105.715 seconds. The command also passed
the bundled-skill check and built `bin/roundfix` at this report's head.

`rtk env GOCACHE="$PWD/.gocache" make verify-docs` exited 0 in about 35.46
seconds. Its repository-contract tests proved sanctioned generated ownership
and byte identity, its docs contract passed, and its full Spec check reported
no findings for Spec 0080. A final rerun after the report matrix closed also
exited 0 in about 30 seconds.

A post-matrix read-only `rtk ./bin/roundfix spec audit
0080-cheap-detectors-run-before-the-gate --format json` diagnostic exited 1.
It reported the Spec target branch as undelivered relative to `origin/HEAD`,
which is expected before this gate permits a Pull Request, and preserved an
earlier orphan Run Worktree with one unintegrated path. `spec audit` is a
post-delivery audit rather than this repository's selected Verification or a
PRD acceptance path; no cleanup or integration mutation is authorized in this
report-only gate.

The first warm `rtk env GOCACHE="$PWD/.gocache" make verify-incremental`
exited 0 in 5.73 seconds. A persistence rerun exited 0 in 5.92 seconds. Both
remain below the adopted 10-second unchanged-tree budget.

## Mechanical findings

None.

## Results

| # | Story / criterion / sweep | Actor and surface | Status | Evidence |
| - | --- | --- | --- | --- |
| R01 | Success Metric 1; Stories 1 and 3; Features 1–3: a blocking mechanical fact is reported in seconds, cites its rule, and withholds the Agent Session. | Daemon operator; CLI/backend | blocked (environment: report-only no-commit Run cannot create a disposable committed Spec Run) | Equivalent Task-cycle evidence passed in 0.01 seconds: closed fail report, typed citation, mechanical event, no Agent request, no Verification use, and persisted settlement. See R01 below. |
| R02 | Success Metric 2; Stories 2, 4, and 6; Features 4–5: a corrective re-gate costs materially less because unchanged repository evidence carries with its original report/head, while moved or non-repository inputs re-observe. | QA gate agent and report reviewer; CLI/backend/docs | blocked (environment: report-only no-commit Run cannot create first and corrective committed Runs) | Equivalent Git-backed evidence passed 20 carry/refusal cases, including moved and non-repository defeat attempts and original-citation persistence. See R02 below. |
| R03 | Success Metric 3; Story 5; Feature 6: the local incremental tier meets the declared budget and the complete fresh gate remains distinct and authoritative. | Adopting-repository maintainer; CLI/infra | pass | Cold complete gate passed in about 106.37s; incremental runs passed in 5.73s and 5.92s; CI still selects `GOFLAGS: -count=1`, `VERIFY_TEST_TARGET: test-budget`, and `make verify`. See R03 below. |
| R04 | Success Metric 4; Feature 7: mechanical stage plus audit is never more permissive than audit alone across the fixture corpus, and typed verdict semantics remain unchanged. | Maintainer and report reader; backend/docs | pass | Cold full gate, corpus guards, five public CLI outcomes, and typed-count/verdict/recency matrices passed. See R04 below. |
| R05 | Success Metric 5; Features 1 and 3: every mechanical detector emits file, line, and fix citations; absent input records detector plus missing artifact. | QA gate agent; CLI/backend | pass | All four detector classes, multi-finding accumulation, materialization, presence-aware skips, and corpus non-regression passed. See R05 below. |
| R06 | ADR-0104 outside-evidence acceptance: independently re-check the historical timing and gate-economics evidence this Spec did not author. | Maintainer; docs/repository history | pass | Archived Spec 0071 independently records the 4.9s/88.9s split; archived 2026-07-29 findings independently record 15–20 minute and multi-round gate costs. See R06 below. |
| R07 | Vocabulary and prompt contract: emitted refusal codes have a durable owner, the QA prompt carries changed paths and previous-report identity, and all three typed blocked counts remain named. | CLI operator and future maintainer; CLI/docs | pass | Strict checking reported no finding or skip for Spec 0080; durable code mapping and all five focused prompt/context tests passed. See R07 below. |
| R08 | Tooling authorization and commit chronology: exact bounded paths, separate authorization, authorized edits, sanctioned fallout, and later consequent fix. | Maintainer/reviewer; Git/infra | pass | Fresh `diff-tree`, ancestry, SHA-256, exact Makefile diff, and repository-contract evidence all passed. See R08 below. |
| R09 | Pull Request approval, checks/status, unresolved threads, Merge-Ready, and review-artifact ancestry journeys. | Pull Request reviewer; GitHub/CLI | blocked (environment: no open Pull Request) | The prompt proves the absence. Equivalent prompt, unresolved/absent distinction, and terminal-loop evidence passed. See R09 below. |
| R10 | Non-Goals and scope preservation: no new command/flag, no CI workflow or verdict-semantic change, gate remains the terminal authored Task, agent audit remains, and unrelated suite/runtime ownership stays out of scope. | Maintainer and operator; CLI/backend/docs/infra | pass | Built help, strict graph, terminal-shape tests, Agent-stage tests, and exact path audits preserve every exclusion. See R10 below. |

## Detailed row plan and evidence

### R01

- Entry point: built `roundfix` CLI and the Daemon's real QA-stage runner.
- Expected observable: a blocking mechanical fact closes a fail report and
  emits the mechanical event before any Agent Session request.
- Independent confirmation: test the public Task-cycle boundary and inspect
  the persisted report/event facts.
- Persistence: reopen the generated report after the runner returns.
- Probes: multiple findings in one pass; absent detector input.
- Observed: a fresh JSON test-process run of
  `TestMechanicalStageWithholdsAgentSession` completed the real Task-cycle
  boundary in 0.01 seconds. It reopened the closed fail report, verified its
  finding-blocked row and citation, observed no Agent or Verifier call, and
  inspected the mechanical event and final Daemon settlement. The adjacent
  real-detector report-shape canary passed in 0.05 seconds. A production
  disposable Spec Run remains unreachable because it requires commits this
  assignment forbids; the focused runner supplies equivalent observed
  evidence without weakening the user-facing boundary.
- inputs:
  - kind: elapsed_time
    ref: wall-clock from QA stage start to mechanical verdict
  - kind: repository_path
    ref: internal/daemon/task_engine.go
  - kind: repository_path
    ref: internal/speccheck/**

### R02

- Entry point: the real Git-backed mechanical carry-forward resolver.
- Expected observable: only a passing row with unchanged repository evidence
  carries and cites its original report/head; moved, mixed, external, live,
  and elapsed-time inputs re-observe.
- Independent confirmation: compare materialized provenance with the exact Git
  range and snapshot digests.
- Persistence: carry an already-carried row again and retain the original
  establishing citation.
- Probes: changed bytes, changed glob membership, missing snapshot, and mixed
  `repository_path` plus `elapsed_time`.
- Observed: `TestCarriable` passed 14 named refusal subtests and two happy
  paths. Four real temporary-Git cases proved unchanged evidence carries with
  its establishing report/head, a non-ancestor refuses, changed declared
  evidence re-observes, and a later carry retains the original citation. The
  focused process exited 0 in 0.59 seconds. A first-versus-corrective
  production timing comparison remains unreachable because it requires two
  committed Runs; the Git-backed resolver and materialized provenance are the
  equivalent observed evidence for the claimed work reduction.
- inputs:
  - kind: elapsed_time
    ref: first gate and corrective re-gate durations
  - kind: repository_path
    ref: internal/speccheck/mechanical.go
  - kind: repository_path
    ref: internal/daemon/task_engine.go

### R03

- Entry point: public `make verify-incremental` and `make verify` targets.
- Expected observable: unchanged-tree incremental verification exits 0 within
  10 seconds; the cold complete target remains distinct, fresh, and green.
- Independent confirmation: inspect both target expansions and both adopted
  Baseline clauses.
- Persistence: rerun incremental verification after the complete gate.
- Probes: cache-warm repeat and explicitly cold complete run.
- Observed: the cold full gate passed after an explicit test-cache clear. The
  warm incremental runs passed in 5.73 and 5.92 seconds. `Makefile` keeps
  `verify` and `verify-incremental` as distinct targets; both adopted guide
  clauses state their distinct questions and fail closed when no incremental
  command exists. `.github/workflows/ci-verify.yml` still uses `GOFLAGS:
  -count=1`, `VERIFY_TEST_TARGET: test-budget`, and `make verify`; the Spec
  commit range changes no `.github/**` path.
- inputs:
  - kind: elapsed_time
    ref: incremental and complete verification durations
  - kind: repository_path
    ref: Makefile
  - kind: repository_path
    ref: docs/agents/agent-instructions.md
  - kind: repository_path
    ref: docs/agents/spec-routing.md

### R04

- Entry point: the fixture corpus and public QA verdict matrix.
- Expected observable: no fixture becomes more permissive, and missing,
  malformed, pass, partial, fail, and all blocked-cause outcomes remain intact.
- Independent confirmation: inspect current verdict code and repeat the corpus
  selection uncached.
- Persistence: run the same corpus in a fresh test process.
- Probes: malformed report, count mismatch, missing report, and three blocked
  causes.
- Observed: `TestRunImplementQAVerdictMatrix` passed `pass`, `partial`, `fail`,
  `missing_report`, and `unreadable_verdict`. The focused `internal/spec`
  matrix passed supported verdicts, typed blocked-count enforcement,
  missing/unreadable reports, and numeric same-day recency. The mechanical
  corpus non-regression and corpus-budget cases passed, and the cold full gate
  passed every package. No Spec commit changes `internal/spec/qa.go`.
- inputs:
  - kind: repository_path
    ref: internal/speccheck/testdata/**
  - kind: repository_path
    ref: internal/spec/qa.go
  - kind: repository_path
    ref: internal/spec/*_test.go

### R05

- Entry point: every mechanical detector carrier through the shipped checker
  and materializer surfaces.
- Expected observable: each finding has code, file, positive line, detail, and
  fix; each absence records detector plus missing artifact without a finding.
- Independent confirmation: compare the observed detector set with production
  registration and rerun it in a fresh process.
- Persistence: reopen the materialized report.
- Probes: multiple simultaneous findings and absence carriers.
- Observed: focused mechanical tests passed `QA-AUTH-PATHS`,
  `QA-CONSEQUENT-ORDER`, `QA-REPORT-SHAPE`, and `QA-EVIDENCE-PATH` green/red
  carriers. `TestMechanicalReportsAllFindings` accumulated all four in one
  result and required non-empty code, file, positive line, detail, and fix.
  Authorization, consequent, report, and evidence absence carriers recorded
  detector plus missing artifact without a finding. The seeded mechanical
  skip below remains visible and R08 manually covers the skipped chronology.
- inputs:
  - kind: repository_path
    ref: internal/speccheck/**

### R06

- Entry point: promoted references and their older repository-owned source
  artifacts.
- Expected observable: sources independent of this Spec support the recorded
  4.9-second/88.9-second verification split and repeated gate-round cost.
- Independent confirmation: Git history proves the measured Spec 0071 source
  predates Spec 0080, while the dated archived findings preserve their own
  independent observations and provenance.
- Persistence: record exact paths and commits in this report.
- Observed: the independent measured source at
  `docs/specs/_archived/0071-verification-cost/baseline/2026-08-03-after.md`
  records 4.9 seconds for an unchanged local gate and 88.9 seconds for the
  complete fresh run; its archive commit `8cc814c4` predates Spec 0080's
  authoring commit `142e3232`. Three archived 2026-07-29 findings separately
  record roughly 15–20 minutes per gate cycle, four cycles for Spec 0052, and
  five full rounds in another delivery. Those sources originated outside this
  Spec. Exact Spec 0079 92/29/30-minute figures were not independently
  reconstructed and are not needed for this row's pass.
- inputs:
  - kind: repository_path
    ref: docs/specs/_archived/0071-verification-cost/**
  - kind: repository_path
    ref: docs/findings/_archived/2026-07-29-qa-*.md

### R07

- Entry point: strict checker, built QA prompt contract, and durable vocabulary
  owner.
- Expected observable: stable refusal codes map to `CONTEXT.md`; the prompt
  names all typed counts and contains changed-path plus prior-report identity.
- Independent confirmation: compare emitted source literals, prompt output,
  TechSpec Vocabulary Contract, and `CONTEXT.md`.
- Persistence: repeat the strict checker and prompt tests.
- Observed: `CONTEXT.md` owns `Mechanical Refusal Code` and lists all four
  emitted tokens; the TechSpec Vocabulary Contract maps the production
  emitter to that owner. Strict Spec check returned `No findings.` with no
  Spec-0080 skip. Focused Agent tests passed changed and unchanged path bundles,
  existing/absent previous reports, all three typed count keys, report naming,
  environment-blocked Pull Request text, and unresolved-versus-proven-absence
  wording. The Daemon integration case passed with a real Git head.
- inputs:
  - kind: repository_path
    ref: CONTEXT.md
  - kind: repository_path
    ref: internal/agent/**
  - kind: repository_path
    ref: docs/specs/0080-cheap-detectors-run-before-the-gate/_techspec.md

### R08

- Entry point: actual Task commits selected from Git history and the current
  authorization record.
- Expected observable: protected tooling changes stay within exact bounded
  files plus deterministic declared regeneration output; authorization and
  prerequisite records precede causes, and consequent fixes follow separately.
- Independent confirmation: `git diff-tree` for every relevant commit and a
  fresh sanctioned convergence check.
- Persistence: record commit/path evidence in this report.
- Observed: `git diff-tree` shows Task 04 changed only the canonical QA skill,
  its mirror, and its Task file. Task 06 changed its four bounded sources,
  adopted guides, Task file, and the sixteen exact regeneration outputs now
  declared by the authorization. Task 07 changed only its Task file and
  `internal/baseline/preservation_test.go`. Ancestry checks proved the
  corrected authorization commit `58b6881d` precedes Tasks 04 and 06 and Task
  06 precedes the separate Task 07 consequence. Both QA skill copies have
  SHA-256 `7a3ee44d32fe9644ffd5b8acc1963abbe96e07fdbe702d69525c7c4a4892ca08`.
  The exact Task-06 Makefile diff only adds `verify-incremental` plus its PHONY
  token; the `verify:` declaration is untouched. `make verify-docs` passed its
  sanctioned-ownership and byte-identity repository contracts.
- inputs:
  - kind: repository_path
    ref: docs/workflow/authorizations/2026-08-06-proof-cost.md
  - kind: repository_path
    ref: .agents/skills/qa-gate/SKILL.md
  - kind: repository_path
    ref: internal/baseline/assets/**
  - kind: repository_path
    ref: Makefile

### R09

- Entry point: the prompt's authoritative Pull Request fact, never the Run
  Worktree branch.
- Expected observable: journeys are environment-blocked because no Pull
  Request exists, with equivalent prompt, review-boundary, and loop-order
  evidence.
- Independent confirmation: inspect public prompt and Daemon boundary tests.
- Persistence: retain the supplied Pull Request fact in this report.
- Observed: the supplied fact is `none open`. Focused prompt tests passed the
  required environment-blocked wording and the distinction between proven
  absence and resolution failure; the Daemon failure/timeout cases preserved
  the unresolved fact. Equivalent Task-cycle tests preserve the authored
  post-gate review ordering. Unblock only after this passing gate permits a
  Pull Request to exist; do not resolve the Run Worktree branch as one.
- inputs:
  - kind: live_service
    ref: Pull Request fact supplied by the Roundfix QA prompt
  - kind: repository_path
    ref: internal/agent/spec_prompt.go
  - kind: repository_path
    ref: internal/daemon/task_engine.go

### R10

- Entry point: built CLI help, Task Graph, Git history, and audit-stage flows.
- Expected observable: every Non-Goal remains excluded and no unrelated
  public surface changed.
- Independent confirmation: compare actual changed paths with the PRD and
  authorization.
- Persistence: retain help, graph, and path evidence in this report.
- Observed: `bin/roundfix help` exited 0 and exposes no mechanical-stage
  command or flag. Strict checking and `TestLoadRejectsInvalidQAGateShape`
  prove the gate remains unique, terminal, and leaf-covering;
  `TestTaskCycleQAStepSkippedUnlessEveryTaskCompleted` keeps it after all
  dependencies. Non-blocking stage evidence still opens one Agent Session.
  Exact `88453ed6^..HEAD` path audits found no change under `.github/**`,
  `cmd/roundfix/**`, or `internal/spec/qa.go`; no journal, lock, or suite-cost
  surface entered the Spec.
- inputs:
  - kind: repository_path
    ref: cmd/roundfix/**
  - kind: repository_path
    ref: internal/cli/**
  - kind: repository_path
    ref: internal/spec/qa.go
  - kind: repository_path
    ref: docs/specs/0080-cheap-detectors-run-before-the-gate/_tasks.md
  - kind: repository_path
    ref: .github/**

## Findings

None.

## Blocked and skipped

- R01 is environment-blocked because a real citation-checkable failure needs a
  disposable committed Spec Run, which the report-only assignment forbids.
  Equivalent observed Task-cycle and report/event evidence passed, including
  a measured 0.01-second blocking path. Unblock with a supervised disposable
  Run under commit authority.
- R02 is environment-blocked because first-versus-corrective re-gate timing
  needs two committed Runs. Equivalent Git-backed carry, refusal, materialized
  provenance, and persistence evidence passed. Unblock with two supervised
  disposable Runs on one committed graph.
- R09 is environment-blocked by the authoritative no-open-Pull-Request fact.
  Equivalent prompt and lifecycle evidence passed. Unblock after a passing
  gate allows the Spec target branch to open a Pull Request.
- No row is finding-blocked, declared-blocked, skipped, failed, or pending.
  The preserved `consequent-fix commit order` mechanical skip is not a skipped
  QA row: R08 performed the exact chronological Git audit manually and passed.

## Mechanical skips

| Detector | Missing artifact |
| --- | --- |
| consequent-fix commit order | consequent-fix declaration |

## Coverage

Closed 10/10 rows: 7 pass, 3 blocked (environment), 0 fail, 0 blocked
(finding), 0 blocked (declared), 0 skipped, and 0 pending. R01–R05 cover the
five Success Metrics in order. Together the matrix covers all six stories,
all eight core features, every Non-Goal class, outside evidence, the preserved
mechanical skip, tooling chronology, and Pull Request journeys. Carry-forward
defeat probes covered changed paths, changed content, glob membership,
non-ancestor heads, missing snapshots and citations, and mixed
non-repository/elapsed inputs.

Finding impact counts: 0 Blocks-Completion, 0 Data-Loss, 0 Trust-Damage, 0
Friction, and 0 Cosmetic.

## Final verdict

Pass: every runnable row passed; R01, R02, and R09 record proved environment
causes plus equivalent fresh observed evidence, and no row failed, remained
pending, was skipped, or was blocked by a finding or declaration.
