---
spec: 0018-external-spec-root
date: 2026-07-07
verdict: pass
surfaces: [cli, docs]
---

# QA Report — External Spec Root

## Summary

Reproduced the field failure layout end to end with a real Agent: a scratch
repository whose `docs/specs` is a symlink into an external knowledge tree,
`specs.root` configured to the external path, and one real Implement Run with
codex. The Run completed Clean, the Daemon commit carried only repository
code, the exact staging-guard warning fired, and the task status settled in
the external tree — the scenario that killed the conexus Run overnight now
completes. Validation of a bad root fails preflight naming the resolved path.
Verdict: pass.

## Results

| # | Criterion / user story | Verdict | Evidence |
| - | ---------------------- | ------- | -------- |
| 1 | Story 1 — symlinked layout completes and commits | pass | Real Run `run_20260707T125054Z_5c15348049bfb3a7` ended `Clean: all 1 Task(s) completed.`, exit 0, in a repo with `docs/specs -> ../../knowledge/.../docs/specs` |
| 2 | Staging-guard warning shape | pass | stderr: `roundfix: task file <external path> kept outside the repository; committed without it` (exact contract shape) |
| 3 | Story 2 — explicit external root, worktree-stable | pass | `.roundfixrc.yml` `specs.root` absolute external path; startup stderr `Spec Root: <external path>`; Task Graph read and executed from the Run Worktree against the external root |
| 4 | Task status settles in the external tree | pass | Post-run `task_01.md` in the knowledge tree carries `status: completed` |
| 5 | Story 3 — code commits carry only code | pass | `git show --stat` of the Task commit `feat: create done.txt`: exactly `done.txt | 1 +`, `Roundfix-Task: task_01` trailer, no task file |
| 6 | Validation rejects a bad root | pass | `specs.root: /nonexistent/specs-root` → Preflight exit 2: `specs.root resolved to "/nonexistent/specs-root", which does not exist; create the directory or update specs.root` |
| 7 | Non-Goal — knowledge repository never git-invoked | pass | The external tree in the scratch run is not a git repository at all and the Run completed; no git operations target it |
| 8 | Default layout unchanged | pass | This repository (default root) ran three Implement Runs today with task files riding in their commits; full suite green (878 tests) |
| 9 | Docs match behavior | pass | README Config documents `specs.root`; Command Boundaries documents the drop warnings, settle-without-commit, and shim-removal note; drift check green |

## Environment

Built binary from 43497cb. Scratch layout under the session scratchpad:
`knowledge/projects/smoke/docs/specs/0001-qa-smoke` (external, non-git) +
`repo/` (git, branch `ma/qa-smoke`, `docs/specs` symlink into the knowledge
tree). Real codex Agent Session via acpx 0.12.0. Trivial task (create
`done.txt`) with `test -f done.txt` verification to keep the Agent leg short
while exercising the full daemon commit boundary.
