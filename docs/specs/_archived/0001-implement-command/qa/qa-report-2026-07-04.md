---
spec: 0001-implement-command
date: 2026-07-04
verdict: partial
surfaces: [cli, data]
---

# QA Report — Implement Command

## Summary

All 10 tasks are `status: completed` and `make verify` passed fresh (406 tests
in 16 packages, `roundfix skills check`, build). The cli surface was exercised
live with a built binary (`roundfix 0.0.0-dev`) against nine throwaway fixture
repositories: the help contract, eleven Preflight Validation failure modes, the
all-completed no-op, and `attach` on an unknown run id all behave exactly as
the techspec API contract documents. The data surface was exercised live: a
hand-built schema-v3 Run Database migrated to v4 through the real binary,
preserving all rows, re-keying the lock, and staying stable on a second open.
Agent-driven flows (full Task execution, resume, QA step, cockpit) cannot run
here without a live ACP runtime; those stories are validated by the shipped
targeted tests, all re-run fresh and passing. **No defects were found.** The
verdict is `partial` solely because the PRD's dogfood success metric demands a
live end-to-end Run of a real Spec, which this environment cannot produce —
branch history carries no `Roundfix-Task` trailers, so no prior dogfood run
exists to cite either.

## Results

| #   | Criterion / user story                                                         | Verdict | Evidence |
| --- | ------------------------------------------------------------------------------ | ------- | -------- |
| S1  | Story 1 — run the Implement Command with a spec slug; graph executes in dependency order | pass | Tests (fake Agent, real CLI/engine/store/git): `TestRunImplementExecutesSpecEndToEnd`, `TestTaskCycleExecutesAgentVerifySettleCommitContract`. Live: S01 help contract, S13 graph-order stdout lines |
| S2  | Story 2 — each successful Task becomes one commit (code + task file, typed message, trailers) | pass | Tests over a real git repo: `TestTaskCycleRealRepoCommitsPerTaskExcludingPreexistingDirt`, `TestTaskCycleCommitStagesSnapshotDiffPlusTaskFile`; message + `Roundfix-Spec`/`Roundfix-Task` trailers asserted in `TestRunImplementExecutesSpecEndToEnd` |
| S3  | Story 3 — a new Run picks up every non-completed Task, no manual bookkeeping   | pass    | Tests: `TestRunImplementFailedTaskEndsUnresolvedAndResumeFinishesGraph`, `TestRunImplementResumesStaleInProgressTask`, `TestTaskCycleRerunsStaleTasksAndSkipsCompletedTasks`. Live: S13 all-completed no-op (exit 0, "no Run was created", 0 run rows) |
| S4  | Story 4 — Run refuses to start on malformed Spec, dirty tree, default branch   | pass    | Live, all exit 2 with the check and fix named, stdout empty, zero Run Database rows: S02 missing spec, S03 inactive (`draft`), S04 cycle (names task_01, task_02), S05 unknown need (names task_02 → task_99), S06 missing task file, S07 missing Verification (names the file and the fix), S08 dirty tree (commit/stash/discard guidance), S09 default-branch veto (names branch, default, `name-match` source) |
| S5  | Story 5 — opt-in QA step; QA Report committed as evidence                      | pass    | Tests: `TestRunImplementQAVerdictMatrix` (exits 0/1, Clean/Unresolved, QA commit whenever a report exists), `TestTaskCycleQAVerdictMatrixSettlesRunAndCommitsReport`, `TestTaskCycleQAStepSkippedUnlessEveryTaskCompleted`, `TestRunImplementQAOnlyRunSettlesOutcomeFromVerdict`. Live: S01 shows `--qa` with truthful copy |
| S6  | Story 6 — Attach to a running or finished spec Run                             | pass    | Tests: `TestAttachReplaysCompletedSpecRunReadOnly` (replay + follow, read-only), `TestCockpitSpecRunShowsTasksInGraphOrderAndRefreshesStatuses`, `TestCockpitSpecRunKeepsLastGoodStatusOnMidWriteTaskFile`, `TestRenderLiveRunViewSpecRunRendersTasksAsWorkItems`. Live: S14 unknown run id → exit 2, `roundfix attach failed: Run "run-does-not-exist" does not exist` |
| S7  | Story 7 — Interactive Input lists active Specs when the flag is omitted        | pass    | Tests: `TestCollectInputSpecPickerSelectsListedSpec`, `TestRunImplementInteractiveInputPicksSpecThroughCollector`, `TestRunImplementInteractiveInputRemembersAgentButNotSpec`. Live: S10 `--no-input` without `--spec` → exit 2 naming the flag; S11 no active Specs → exit 2 actionable message, never an empty picker |
| M1  | Metric — dogfood: a real Spec executes end-to-end via the Implement Command    | **blocked** | Requires a live ACP runtime; not available in this session. No prior dogfood evidence exists on the branch (no commits with `Roundfix-Task` trailers). Closest evidence: the fake-Agent e2e tests in S1/S2 rows |
| M2  | Metric — resumed Run after induced failure picks up only non-completed Tasks   | pass    | `TestRunImplementFailedTaskEndsUnresolvedAndResumeFinishesGraph`: first Run exits 1 Unresolved, second invocation executes only task_02/task_03 and finishes the graph |
| M3  | Metric — full existing review-path suite passes unchanged                      | pass    | `make verify` fresh this session: 406 tests, 16 packages, zero failures; task_05/task_09 Results record zero edits to review-path tests |
| M4  | Metric — QA opt-in produces a committed QA Report whose verdict settles the Run | pass   | Verdict matrix tests in row S5: only `pass` → Clean/exit 0; `partial`, `fail`, missing, unreadable → Unresolved/exit 1; report committed as `docs(qa): qa report for <slug> (<verdict>)` whenever it exists |
| D1  | Data — Run Database v3 → v4 migration on a scratch database                    | pass    | Live: hand-built v3 DB (3 runs, 1 lock) opened through the real binary → `user_version` 4, all 3 rows preserved with kinds/states intact, `spec_slug` column added, lock re-keyed to `(pr, owner/project#feature/review, run_v3_active)`; second open idempotent (same counts, same version). Test: `TestOpenMigratesV3RunDatabasePreservingRunsAndRekeyingLocks` |
| D2  | Data — work-target locks and by-Kind validation                                | pass    | Tests fresh: `TestCreateRunRejectsSecondActiveRunForSameSpecTarget`, `TestCompletedImplementRunReleasesSpecTargetLock`, `TestReviewKindActiveRunErrorTextUnchanged`, `TestActiveRunInGitRootFindsActiveRunsOfAnyKind`, `TestCreateRunValidatesRequiredFieldsByKind` |
| C1  | Contract — stdout discipline and stable exit codes                             | pass    | Live: every preflight failure wrote only to stderr (stdout empty) and exited 2; no-op wrote only the per-Task lines + summary to stdout and exited 0; help exited 0. Exit 1/130 paths: `TestRunImplementFailedTaskEndsUnresolvedAndResumeFinishesGraph`, `TestRunImplementStopRequestEndsStoppedWithInterruptMapping` |
| C2  | Contract — help lists exactly the implemented flags                            | pass    | Live S01: the eight documented flags (`--spec`, `--qa`, `--agent`, `--model`, `--agent-command`, `--agent-full-access`, `--interactive`, `--no-input`), no others; `TestRunImplementHelpListsExactlyImplementedFlags` |
| N1  | Non-Goals — no push, no PRs, no parallel execution from spec Runs              | pass    | Help text states "the Run never pushes"; `TestRunImplementExecutesSpecEndToEnd` asserts the Pusher and Review Source resolver are never invoked; `TaskCycle` executes sequentially by construction (`TestTaskCycleExecutesAgentVerifySettleCommitContract`); no push/PR flag exists on the command (S01) |

## Blocked

**M1 — dogfood metric.** "A real Spec in this repository executes end-to-end
via the Implement Command, producing one passing-verification commit per
completed Task" requires launching a live ACP Agent (codex, claude, or
opencode), which this QA session cannot do. The techspec planned this as Build
Order item 12, outside the 10 shipped tasks. The full pipeline around the
Agent seam is proven by the fake-Agent e2e tests over real git repositories
and the real store; the outstanding action is one live dogfood run (with the
induced-failure resume check) before the feature PR claims this metric.

## Flows exercised

Live, with the built binary against fixture repos (full transcript:
`qa/evidence/live-cli-transcript-2026-07-04.txt`):

- S01 `implement --help` — exit 0, eight flags, truthful copy
- S02–S09 Preflight Validation failures — missing spec, inactive spec,
  manifest cycle, unknown need, missing task file, missing Verification
  section, dirty worktree, default-branch veto: each exit 2, one actionable
  stderr message naming the check and fix, empty stdout
- S10 `--no-input` without `--spec` — exit 2 naming the missing flag
- S11 no active Specs — exit 2, actionable message, no empty picker
- S12 runtime probe failure (`--agent-command /nonexistent/...`) — exit 2,
  names the probe command, the exec error, and the install/`--agent-command`
  fix
- S13 all-Tasks-completed no-op — exit 0, per-Task lines in graph order,
  "All 2 Task(s) already completed; no Run was created."
- S14 `attach` on an unknown run id — exit 2, names the Run id
- S15 Run Database after all scenarios — 0 run rows, 0 lock rows,
  `user_version` 4 (preflight failures write nothing)
- Migration scenario — v3 fixture database migrated to v4 through the real
  binary; rows preserved, lock re-keyed, idempotent on re-open

Validated by fresh targeted test runs (all passing, this session):
`TestRunImplement*` + `TestAttachReplaysCompletedSpecRunReadOnly` (18 tests,
`internal/cli`), `TestTaskCycle*` (13 tests, `internal/daemon`), the store
migration/lock/validation set (8 tests, `internal/store`), and the cockpit /
picker / plain-text renderer set (5 tests, `internal/tui`).

## Environment

- macOS (darwin 25.5.0), branch `ma/implementation-daemon-acpx` at commit
  `412fad2`, clean worktree; no source, spec, or task file modified by QA.
- Static gate: `make verify` (fmt-check, `go test ./...` — 406 tests in 16
  packages, `roundfix skills check`, build).
- Binary: `go build ./cmd/roundfix` → `roundfix 0.0.0-dev`, run with an
  isolated `HOME` (scratch `~/.roundfix`) so no real user database or config
  was touched.
- Fixtures: nine throwaway git repos (one per preflight scenario) with
  generated `docs/specs/0002-demo/` artifacts on an `ma/qa-fixture` branch;
  a hand-built schema-v3 SQLite Run Database mirroring the
  `buildV3Fixture` DDL from `internal/store/store_test.go`; `sqlite3` CLI for
  database inspection.

## Issues found

No defects. Two follow-ups, neither blocking:

1. **Glossary gap (pre-existing, recorded by task_10):** `CONTEXT.md` has no
   entry for **Clean**, the terminal Run outcome printed by the CLI and named
   in help text; Unresolved Outcome is glossaried, its counterpart is not.
   Owner: glossary owner, per task_10's Result.
2. **Dogfood run outstanding (M1):** one live `roundfix implement` run over a
   real Spec with a live ACP runtime, plus the induced-failure resume check,
   is needed to close the PRD's first success metric.
