---
spec: 0017-run-discovery
date: 2026-07-07
verdict: pass
surfaces: [cli, docs]
---

# QA Report — Run discovery

## Summary

Exercised the built binary (`bin/roundfix` at 43497cb) as a user in three
repositories: this one, a scratch repository, and outside any repository.
Every listing contract held against real Run Database data, including a live
cross-repository discovery of an Active tax-poc Run through `--all`. The
interactive Attach picker cannot be driven from this non-TTY session; it is
credited from the daemon-verified CLI tests in task_03's Result, while its
non-interactive contract was exercised for real. Verdict: pass.

## Results

| # | Criterion / user story | Verdict | Evidence |
| - | ---------------------- | ------- | -------- |
| 1 | Story 1 — list Runs with id, state, kind, target | pass | `runs list` printed 8 real Runs newest first, columns `<id> <state> <kind> <target>` with `pr:18`/`spec:0017-run-discovery` targets |
| 2 | Story 2 — deterministic stable format for agents | pass | Same command twice produced identical bytes; Active marker `*` on state (`ResolvingWithAgent*`); ids untruncated |
| 3 | Story 3 — attach without run id | pass | Non-interactive: `attach --no-input` exits 2, stderr `missing Run ID; run 'roundfix runs list' to discover Runs or pass a Run ID`. Interactive picker credited from `TestAttachPickerSelectsRunByNumberActiveFirstNewestFirst`, `...ByIDWithExplicitAttachOutput` (byte-compare with explicit attach), `...CancelExitsZeroWithoutAttaching` (task_03 Result, verification-gated commit) |
| 4 | Repository scope default + `--all` | pass | Scratch repo listed only its own Run; `--all` listed roundfix, tax-poc (Active), and scratch Runs, each line naming the repository |
| 5 | `--active` filter | pass | `runs list --active` with no Active Runs printed `No Runs found.`, exit 0 |
| 6 | Empty result is not an error | pass | Same run as #5: single line, exit 0 |
| 7 | Unknown subcommand fails usefully | pass | `runs bogus` → exit 2, `unknown runs command "bogus"` + usage pointer |
| 8 | Outside a git repository without `--all` | pass | exit 2, error names `--all` as the alternative |
| 9 | Docs match behavior | pass | README Commands/Boundaries and usage guide document the columns, flags, empty contract, and picker; drift check green in `make verify` |

## Environment

Built binary from 43497cb (`make verify` green: 878 tests, 19 packages).
Real Run Database at `~/.roundfix/roundfix.db` with 8+ Runs across three
repositories. Non-TTY session: interactive picker selection not exercisable
here — covered by named CLI tests driving scripted picker input.
