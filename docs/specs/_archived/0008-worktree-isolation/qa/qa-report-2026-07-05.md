---
spec: 0008-worktree-isolation
date: 2026-07-05
verdict: pass
surfaces: [cli, infra]
---

# QA Report - Worktree Isolation

## Summary

Verdict: pass. I validated the completed Spec against the PRD user stories,
core features, non-goals, and every task acceptance criterion. The static gate
passed, focused Run Worktree integration flows passed, and the built binary
matched the shipped command and skill contracts.

No failures or blocked items remain. Browser QA was not applicable because the
PRD surfaces are `cli` and `infra`; the terminal cockpit and Attach behavior
were covered through synchronous TUI and CLI integration tests.

## Results

| # | Criterion / user story | Verdict | Evidence |
| - | ---------------------- | ------- | -------- |
| 1 | Preconditions: all Spec Tasks are completed before QA. | pass | Read `task_01.md` through `task_06.md`; each frontmatter has `status: completed` and a `## Result` section with verification evidence. |
| 2 | Static gate: repository verification must pass. | pass | `rtk make verify` passed after normal Go cache access was allowed: `go test ./...` reported 682 passed tests in 17 packages, `roundfix skills check` passed, and `go build -buildvcs=false -o bin/roundfix ./cmd/roundfix` completed. |
| US1 | Developer can edit and commit in the user checkout while a Run executes without contaminating either side. | pass | Focused flow check passed: `TestRunImplementWorktreeIsolationExcludesConcurrentUserCommit` and `TestRunResolveWorktreeIsolationExcludesConcurrentUserCommit` in `rtk go test ./internal/cli ./internal/tui ./internal/config ./internal/worktree -run ...` (27 passed). |
| US2 | Dirty implement checkout proceeds with a note instead of preflight rejection. | pass | `TestRunImplementDirtyWorkingTreePrintsNoteAndRuns` passed in the focused flow check; source scan found the note line in `internal/cli/implement.go`. |
| US3 | Clean Run commits fast-forward onto the user's branch and the Run Worktree is cleaned up. | pass | `TestRunImplementRealWorktreeFastForwardsAndCleansPreservingNonOverlappingUserDirt`, `TestRunResolvePushRunsAfterSuccessfulIntegrationAndCleansWorktree`, and `TestRunWatchReusesOneRealWorktreeAcrossRoundsAndCleansOnIntegratedClean` passed. |
| US4 | Overlapping changes or new commits end Integration Pending with the exact integration command. | pass | `TestRunImplementOverlapEndsIntegrationPendingAndPrintedCommandWorks` passed; `rtk rg` found `Integration command: git merge --ff-only ...` and `IntegrationPending: ... integrate with ...` in CLI fixtures and shipped skill text. |
| US5 | Failed Task work is preserved in the kept Run Worktree and `roundfix settle` uses that surface. | pass | `TestRunImplementUnresolvedKeepsRealRunWorktreeAndPrintsPath` and `TestRunSettleRetargetsKeptRunWorktreeAndCleansUpAfterIntegration` passed; `rtk ./bin/roundfix settle --help` states it re-runs Verification in the kept Run Worktree and commits Run Worktree changes plus the task file. |
| US6 | Live Run View and Attach read Task status from the execution workspace. | pass | `TestCockpitSpecRunReadsTaskStatusAndDetailFromWorkDir`, `TestAttachSpecRunReadsTasksFromKeptWorkDir`, and `TestAttachSpecRunFallsBackToGitRootWhenCleanWorkDirIsPruned` passed. |
| US7 | Verification can copy configured untracked files into the Run Worktree. | pass | `TestCreateUsesNamedRunBranchUnderRoundfixHomeAndCopiesFiles`, `TestLoadAppliesConfigPrecedence`, `TestLoadRejectsInvalidConfig`, and generated config checks passed; README and skill text document `worktree.copy`. |
| CF1 | Run Worktree lifecycle: create under Roundfix Home on a Run Branch, record path, clean up integrated Clean, keep others. | pass | `TestCreateUsesNamedRunBranchUnderRoundfixHomeAndCopiesFiles`, `TestCleanupCleanRemovesCleanRunWorktreeAndBranch`, `TestCleanupCleanKeepsDirtyRunWorktreeAndBranch`, and Run outcome tests passed. |
| CF2 | Execution retarget: Agent Session, prompts, Verification, snapshots, commits, task reads/writes, and QA use the Run Worktree. | pass | Implement, resolve, watch, and TUI WorkDir tests passed in the focused flow check; task 03 and 04 results cite Agent cwd/GitRoot assertions. |
| CF3 | Porcelain-only integration: fast-forward, ancestry-checked branch move, Integration Pending on refusal, Final Push after integration. | pass | Worktree and CLI flow tests passed; `rtk rg` found no `update-ref` production use under `internal/worktree`; Final Push ordering test passed. |
| CF4 | Preflight recalibration: dirty-tree rejection becomes a note while other preflight checks remain. | pass | Dirty-tree implement test passed; built `roundfix implement --help` still exposes the same public command and flags. |
| CF5 | Readers follow execution: Live Run View, Attach, settle, and reports read the recorded Run Worktree with fallback. | pass | Cockpit, Attach, settle, and kept-worktree report tests passed; source scan found `Run Worktree:` and `Run Worktree kept:` line shapes in CLI and skill text. |
| CF6 | Worktree provisioning and Artifact Directory default: explicit copy list, default empty, Artifact Directory default in Roundfix Home. | pass | Store/config focused check passed: `rtk go test ./internal/store ./internal/config -run ...` reported 13 passed tests; README and generated config text include `worktree.copy` and Roundfix Home `artifacts/<repo-id>`. |
| N1 | Non-goal: no parallel Task/Batch execution or ready-set scheduling shipped. | pass | Non-goal scan found future-planning references only in docs/handoffs, inbox, older specs, and ADR-0023; this Spec keeps one Run Worktree per Run and focused Run tests execute sequential flows. |
| N2 | Non-goal: no review artifact relocation into the spec tree. | pass | Fetch behavior remains unchanged per task 04 evidence; source/documentation scan keeps Artifact Directory language and does not add spec-tree review artifact storage for this Spec. |
| N3 | Non-goal: no change to commit messages, Verification semantics, or ownership. | pass | Existing commit message and Verification tests passed in `make verify`; settle and task engine tests still assert existing daemon message and Verification contracts. |
| N4 | Non-goal: no merge-conflict resolution path. | pass | Integration Pending tests passed and prove refusal leaves branch/user dirt intact and prints the manual command; skill text says divergence is handed to the developer. |
| T01-AC1 | Worktree creation works while the source branch is checked out; worktree starts clean and independent. | pass | `TestCreateUsesNamedRunBranchUnderRoundfixHomeAndCopiesFiles` passed in the focused flow check. |
| T01-AC2 | Integration matrix covers fast-forward, non-overlapping dirt, overlap pending, divergence pending, branch move, non-ancestry pending, and no phantom staged entries. | pass | `make verify` passed the full `internal/worktree` suite; task 01 result records the matrix test coverage. |
| T01-AC3 | Crash simulation is reaped only for terminal-Clean run ids; kept worktrees survive. | pass | `make verify` passed `TestPruneTerminalReapsCrashedTerminalCleanRunOnly`; task 01 result records this evidence. |
| T01-AC4 | No production wiring outside the new worktree package for task 01. | pass | Task 01 result records diff scope evidence; `make verify` passed after later wiring tasks completed. |
| T02-AC1 | Schema migration preserves rows/locks, reaches version 6, and legacy rows read empty `work_dir`. | pass | `TestOpenMigratesV5RunDatabasePreservingRunsLocksAndAddingWorkDir` passed in the store/config focused check. |
| T02-AC2 | Config tests cover valid copy list, absolute rejection, dot-dot rejection, new Artifact Directory default, and explicit-value compatibility. | pass | `TestLoadAppliesConfigPrecedence`, `TestLoadRejectsInvalidConfig`, `TestValidateArtifactDirectoryResolvesAndCreatesPaths`, and `TestValidateArtifactDirectoryRejectsInvalidPaths` passed. |
| T02-AC3 | Full suite passes with only deliberate review-path assertion updates. | pass | `rtk make verify` passed with 682 tests in 17 packages. |
| T03-AC1 | Multi-writer implement proof excludes user files from task commits and preserves user commits. | pass | `TestRunImplementWorktreeIsolationExcludesConcurrentUserCommit` passed. |
| T03-AC2 | Implement outcome matrix covers Clean fast-forward cleanup, overlap Integration Pending with working command, and Unresolved kept worktree. | pass | `TestRunImplementRealWorktreeFastForwardsAndCleansPreservingNonOverlappingUserDirt`, `TestRunImplementOverlapEndsIntegrationPendingAndPrintedCommandWorks`, and `TestRunImplementUnresolvedKeepsRealRunWorktreeAndPrintsPath` passed. |
| T03-AC3 | Dirty user tree no longer blocks implement. | pass | `TestRunImplementDirtyWorkingTreePrintsNoteAndRuns` passed. |
| T03-AC4 | Settle over a kept worktree verifies, commits, integrates, and cleans up. | pass | `TestRunSettleRetargetsKeptRunWorktreeAndCleansUpAfterIntegration` passed. |
| T03-AC5 | Full suite passes with only documented deliberate updates. | pass | `rtk make verify` passed. |
| T04-AC1 | Resolve Run with concurrent user commits creates batch commits free of user files; review suite passes over WorkDir. | pass | `TestRunResolveWorktreeIsolationExcludesConcurrentUserCommit` passed. |
| T04-AC2 | Final Push is invoked only after integration succeeds; integration refusal skips push and ends Integration Pending. | pass | `TestRunResolvePushRunsAfterSuccessfulIntegrationAndCleansWorktree` passed; task 04 result records the zero-push refusal fixture. |
| T04-AC3 | Watch uses one worktree across Rounds and cleans up only on integrated Clean. | pass | `TestRunWatchReusesOneRealWorktreeAcrossRoundsAndCleansOnIntegratedClean` passed. |
| T04-AC4 | Fetch behavior remains byte-identical; full suite passes. | pass | `rtk make verify` passed; task 04 result cites unchanged fetch assertions in `TestRunOperationalCommandAcceptsMVPFlags/fetch`. |
| T05-AC1 | Cockpit over a spec Run reads worktree-only statuses while user-root copies are stale. | pass | `TestCockpitSpecRunReadsTaskStatusAndDetailFromWorkDir` passed. |
| T05-AC2 | Attach reads kept worktree and falls back to user root for pruned Clean Run. | pass | `TestAttachSpecRunReadsTasksFromKeptWorkDir` and `TestAttachSpecRunFallsBackToGitRootWhenCleanWorkDirIsPruned` passed. |
| T05-AC3 | Review rendering snapshots remain unchanged. | pass | `make verify` passed `TestCockpitRenderSnapshots`; task 05 result cites unchanged review rendering snapshots. |
| T05-AC4 | Full suite passes. | pass | `rtk make verify` passed. |
| T06-AC1 | Canonical skill text matches shipped behavior and drift check passes. | pass | `rtk ./bin/roundfix skills check` passed: `Roundfix skill check passed: roundfix`. |
| T06-AC2 | Documented Integration Pending and Run Worktree line shapes appear in CLI fixtures. | pass | `rtk rg "IntegrationPending|Integration command|Run Worktree" internal/cli .agents/skills/roundfix/SKILL.md skills/roundfix/SKILL.md` found the documented line shapes in code, tests, and both skill copies. |
| T06-AC3 | README carries the cold-worktree note with `worktree.copy` remedy. | pass | `rtk rg "worktree.copy|Run Worktree|artifacts/<repo-id>" README.md` found the behavior notes and config remedy. |
| T06-AC4 | No new un-glossaried term. | pass | Read `CONTEXT.md`; Run Worktree, Run Branch, Integration Pending, Roundfix Home, and Artifact Directory are present. |

## Failures and blocked items

None.

## Environment

- Workspace: `/Users/marcio/.roundfix/worktrees/339f8dac2b687a04/run_20260705T205529Z_06c10dc305f94a2e`
- Build under test: `c7aedfc`
- Date: 2026-07-05
- Surfaces exercised: CLI commands, Run Worktree git integration behavior,
  store/config migration behavior, terminal cockpit/Attach behavior, docs and
  skill drift checks.
- Initial sandboxed `rtk make verify` failed before code execution because Go
  could not access `/Users/marcio/Library/Caches/go-build`; the same command
  was rerun with normal Go cache access and passed.
- Verification commands run:
  - `rtk make verify` - passed.
  - `rtk go test ./internal/cli ./internal/tui ./internal/config ./internal/worktree -run '...'` - passed, 27 tests in 4 packages.
  - `rtk go test ./internal/store ./internal/config -run '...'` - passed, 13 tests in 2 packages.
  - `rtk ./bin/roundfix --help` - passed.
  - `rtk ./bin/roundfix implement --help` - passed.
  - `rtk ./bin/roundfix settle --help` - passed.
  - `rtk ./bin/roundfix init --help` - passed.
  - `rtk ./bin/roundfix skills check` - passed.
  - `rtk rg ...` source/doc scans - passed for line-shape, config, glossary,
    and non-goal checks.
