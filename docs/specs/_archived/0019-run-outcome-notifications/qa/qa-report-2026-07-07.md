---
spec: 0019-run-outcome-notifications
date: 2026-07-07
verdict: pass
surfaces: [cli, docs]
---

# QA Report — Run Outcome Notifications

## Summary

Captured a real notification end to end: a scratch repository configured
`notify.command` to dump the notification environment to a file, and a real
Implement Run with codex fired it at the Clean outcome with all four
`ROUNDFIX_*` variables carrying correct values. The failure and no-tooling
paths are credited from the daemon-verified tests named in the task Results;
the default-native path ran live on every Run in this session without
disturbing any report or exit code. Verdict: pass.

## Results

| # | Criterion / user story | Verdict | Evidence |
| - | ---------------------- | ------- | -------- |
| 1 | Story 1 — notification on terminal outcome without watching | pass | Real Run `run_20260707T125054Z_5c15348049bfb3a7` (implement, Clean) wrote `notify-capture.txt` via the configured command at completion |
| 2 | Environment contract | pass | Captured file: `ROUNDFIX_RUN_ID=run_20260707T125054Z_5c15348049bfb3a7`, `ROUNDFIX_OUTCOME=Clean`, `ROUNDFIX_KIND=implement`, `ROUNDFIX_TARGET=spec:0001-qa-smoke` — all four, correct values |
| 3 | Story 2 — user-routed channel via one config value | pass | The capture *is* a custom channel: `notify.command: "env | grep ROUNDFIX_ > .../notify-capture.txt"` in Project Config, no code change |
| 4 | Story 3 — no tooling means no new failure mode | pass | `TestDesktopNotifierMissingToolIsNoop` (task_02 Result); this session's default-native Runs (0017/0018/0019 implements, watch pr:18) all kept their exact report shapes and exit codes |
| 5 | Failure warns and never alters the Run | pass | `TestRunOutcomeNotificationFailureWarnsAndJournalsWithoutChangingReportOrExit` (task_03 Result): exact stderr shape, Daemon Run Event, byte-identical stdout, unchanged exit code |
| 6 | Fetch does not notify | pass | `TestRunOutcomeNotificationsSkipFetch` (task_03 Result) |
| 7 | Disabled restores today's behavior | pass | `TestRunOutcomeNotificationsDisabledSkipsNotifier` (task_03 Result): notifier factory never constructed with `notify.enabled: false` |
| 8 | Timeout bound | pass | `TestCommandNotifierTimesOut` (task_02 Result): command killed at the bound, error wraps `context.DeadlineExceeded` |
| 9 | Docs match behavior | pass | README Config documents both keys, env contract, 30s bound, per-platform native behavior; usage guide names the notification in the detached flow; drift check green |

## Environment

Built binary from 43497cb (`make verify`: 878 tests, 19 packages). Real
notification captured in the same scratch Run used for the 0018 gate
(external Spec Root + codex), proving both features compose. Native desktop
path (osascript) fired live on this macOS session's Runs; its success is
silent by design, so the capturable evidence uses the command override.
