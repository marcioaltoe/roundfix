---
spec: 0076-force-stop-exit-proof
date: 2026-08-04
build: c035ebb19dcb6eb81844f5195a0b89abbf99e4e1
status: closed
verdict: fail
rows_blocked_environment: 0
rows_blocked_finding: 0
surfaces: [backend]
---

# QA report — Force Stop exit proof

## Scope and environment

This full gate validates the proof repaired by Spec 0076, not Force Stop's
unchanged production behavior. The actor is a Roundfix maintainer invoking the
Go test surface and the compiled helper test binary from the Run Worktree on
macOS. The build under test is
`c035ebb19dcb6eb81844f5195a0b89abbf99e4e1` on
`roundfix/run-run_20260804T153241Z_4346f8b92782fd3e`.

The QA prompt states that no Pull Request is open. The PRD has no
Pull-Request-observing story or acceptance criterion, so the matrix contains no
Pull Request journey. Network access, credentials, and external services are
not required.

The high-risk probes are selected to expose the defect class:

- interrupt: send `SIGTERM` to the `ignore` helper and prove it remains alive;
- forced termination: require the controller-observed exit to be `SIGKILL`;
- repetition: run the parent/helper pair 50 times to expose scheduling races;
- invalid ordering: make a disposable copy of the helper exit after readiness
  and prove the regression fails with the premature-exit diagnostic.

Evidence belongs under
`qa/evidence/2026-08-04-force-stop-exit-proof/`. No product, test, Task-status,
Task-graph, Git-history, Pull Request, or external-system mutation is in scope.

## Static gate

`rtk make verify` exited 0 on the report build. Its full filtered terminal
summary is recorded in
`qa/evidence/2026-08-04-force-stop-exit-proof/make-verify.md`: 3,137 Go tests
passed across 24 packages, the four focused skill tests passed, the Repository
Skill Set check passed, and the Roundfix binary built.

## Results

| # | Story / criterion / sweep | Actor, entry point, and surface | Steps, expected observable, independent confirmation, and persistence | Evidence | Status |
| - | --- | --- | --- | --- | --- |
| QA-01 | Authored gate and dependency precondition: `_tasks.md` names `task_03` as the terminal `qa` Task; every non-QA leaf is completed before execution. | QA Agent; Spec artifacts; backend workflow. | Read `_tasks.md` and every Task frontmatter. Observed one terminal QA node depending on `task_02`, with `task_01` and `task_02` completed. Confirmed against the manifest edges and Task files. Persistence is not applicable to immutable build artifacts. | `qa/evidence/2026-08-04-force-stop-exit-proof/constraint-audit.md` | pass |
| QA-02 | Project Constraint — identifier strategy is not applicable because the change creates no Internal Identifier. | QA Agent; PRD, TechSpec, Task commits; backend. | Read both constraint rows and inspected changed paths. No identifier surface changed; both Task commit diffs confirm the test-only scope. Persistence is not applicable. | `qa/evidence/2026-08-04-force-stop-exit-proof/constraint-audit.md` | pass |
| QA-03 | Project Constraint — authentication and HTTP are not applicable because the test-only change opens no transport and handles no credential. | QA Agent; PRD, TechSpec, Task commits; backend. | Read both constraint rows and inspected changed paths. Only assigned Task files and `internal/store/process_unix_test.go` changed; no application transport or credential path changed. Persistence is not applicable. | `qa/evidence/2026-08-04-force-stop-exit-proof/constraint-audit.md` | pass |
| QA-04 | Project Constraint — active ADR obligations: ADR-0089 applies; ADR-0080 and ADR-0091 govern this gate; ADR-0093 is accounted for as non-applicable. | QA Agent; accepted ADRs and Spec citations; backend workflow. | Confirmed all four ADRs are accepted and every cited operative source exists. The graph and report carry ADR-0080/0091's required shape; ADR-0093's detector boundary is outside this change. Persistence is not applicable. | `qa/evidence/2026-08-04-force-stop-exit-proof/constraint-audit.md` | pass |
| QA-05 | Project Constraint — Tooling Authority is not applicable, and actual Task scope contains no protected tooling path. | QA Agent; Git commit ancestry and changed-path audit; backend workflow. | `git diff-tree` for both Task commits showed only the assigned Task file and `internal/store/process_unix_test.go`. The planning commit predates both Task commits. No tooling authorization choreography applies. | `qa/evidence/2026-08-04-force-stop-exit-proof/constraint-audit.md` | pass |
| QA-06 | Repository static gate. | Roundfix maintainer; `rtk make verify`; backend build/test surface. | The exact repository Verification exited 0. Its observed summary reports 3,137 tests across 24 packages, four focused skill tests, the Repository Skill Set check, and the final binary build. Persistence is the complete fresh rerun on this build. | `qa/evidence/2026-08-04-force-stop-exit-proof/make-verify.md` | pass |
| QA-07 | PRD Goals and Core Feature 1; Task 01 AC 1–3; Task 03 direct-helper requirement: the `ignore` helper emits readiness, survives `SIGTERM`, stays alive, and emits no runtime fatal error. | Roundfix maintainer; compiled `internal/store` test binary in helper mode; backend process surface. | The standalone helper printed `ready`, survived `SIGTERM`, answered `alive` to `SIGUSR1`, remained alive, then exited with status 137 after explicit `SIGKILL`; its stderr contained neither forbidden runtime diagnostic. The independent liveness test exited 0. | `qa/evidence/2026-08-04-force-stop-exit-proof/helper-liveness.md` | pass |
| QA-08 | PRD Core Feature 4; Task 02 AC 2: the Force Stop proof requires escalation-caused `SIGKILL`, not merely process absence. | Roundfix maintainer; `go test` public test runner; backend process surface. | The focused proof exited 0 normally and under `-race`. Its assertion accepts only a signaled `WaitStatus` equal to `SIGKILL`; QA-07 independently proves the helper survived `SIGTERM` before controller escalation. | `qa/evidence/2026-08-04-force-stop-exit-proof/force-kill-proof.md` | pass |
| QA-09 | PRD Core Feature 2 and Success Metric 2; Task 02 AC 1 and 5: readiness/exit ordering remains clean across 50 parent/helper repetitions. | Roundfix maintainer; `go test` public test runner; backend process surface. | The raw force-kill proof ran 50 times and exited 0. Both graceful and force-kill proofs then ran together 50 times and exited 0. No pipeline hid either status; neither command emitted a fatal runtime or closed-pipe failure. | `qa/evidence/2026-08-04-force-stop-exit-proof/repeated-parent-helper.md` | pass |
| QA-10 | PRD Core Feature 3 and Success Metric 3; Task 02 AC 3–4; Task 03 AC 4: a premature helper exit fails the repaired regression and names the premature exit. | Roundfix maintainer; disposable source copy plus `go test`; backend test surface. | A one-line early return in a disposable clone made the focused proof exit 1 with `exited prematurely before controller force-kill escalation`. The unchanged Run Worktree then passed the identical command and remained source-clean. | `qa/evidence/2026-08-04-force-stop-exit-proof/premature-exit-mutation.md` | pass |
| QA-11 | Task 01 AC 4–5 and Task 02 AC 6: package and full parallel regression remain green while production files stay untouched. | Roundfix maintainer; `go test ./internal/store` and `go test -parallel 16 ./...`; backend suite. | `internal/store` exited 0, but the following full parallel sweep exited 1 when `TestRunImplementDetachSurvivesCallerProcessGroupKill` exceeded its fixed 5s line deadline at 6.21s. The test passed 20/20 focused runs and the isolated `internal/cli` package passed, narrowing the nondeterminism to full-suite load. | `qa/evidence/2026-08-04-force-stop-exit-proof/regression-sweep.md`; finding F-01 | fail |
| QA-12 | Non-Goals and timing guardrail: no Force Stop production behavior, unrelated store tests, controller symbol, sleep, timer, deadline, or empty-select workaround changed. | QA Agent; commit diffs and focused source inspection; backend scope. | The implementation diff changes only `internal/store/process_unix_test.go`; production `TerminateAndWait` is unchanged. The helper uses a registered signal channel and contains no empty select, sleep, timer, or deadline. | `qa/evidence/2026-08-04-force-stop-exit-proof/scope-audit.md` | pass |
| QA-13 | Task 03 AC 2–4 and QA Report contract: a dated report contains machine-readable verdict and typed blocked counts, plus direct liveness and independently observed mutation-failure evidence. | QA Agent; this QA Report and evidence tree; backend workflow. | The first same-day report uses the collision-safe canonical filename and carries a closed machine-readable verdict plus both typed blocked counts. All rows are terminal; the report links the direct-helper and independently observed premature-exit evidence. | This report and `qa/evidence/2026-08-04-force-stop-exit-proof/report-contract.md` | pass |

## Findings

### F-01 — Full parallel sweep intermittently times out before detached Run ID

- Impact: Trust-Damage.
- Actor and journey step: Roundfix maintainer, QA-11 full parallel regression
  command.
- Expected: `go test -parallel 16 ./...` exits 0.
- Actual: the command exited 1 because
  `TestRunImplementDetachSurvivesCallerProcessGroupKill` reached 6.21s while
  waiting for its first stdout line behind a fixed 5s deadline.
- Reproduction:
  `rtk proxy env GOCACHE=<worktree>/.gocache GOFLAGS=-buildvcs=false go test
  -parallel 16 ./...`.
- Isolation: the same test passed 20/20 focused runs in 0.74–0.97s; the complete
  `internal/cli` package passed alone in 19.790s; the earlier repository gate
  also passed. Spec 0076 changed no `internal/cli` path. The exact detach-start
  root cause is not established, but the evidence localizes the failure to a
  load-sensitive fixed deadline in the required full-suite environment.
- Evidence:
  `qa/evidence/2026-08-04-force-stop-exit-proof/regression-sweep.md`.
- Affected rows: QA-11.

## Blocked and skipped

None. No row is blocked or skipped.

## Coverage

Covered: all PRD goals, four Core Features, three Success Metrics, every Task
acceptance criterion, all three Non-Goals, all four Project Constraint axes,
the repository Verification, and four risk probes. Final counts: 12 pass, 1
fail, 0 environment-blocked, 0 finding-blocked, 0 skipped, 0 pending. Finding
counts by impact: 0 Blocks-Completion, 0 Data-Loss, 1 Trust-Damage, 0 Friction,
0 Cosmetic.

## Final verdict

Fail: stabilize the load-sensitive detached-Run readiness proof exposed by
F-01, then rerun the complete gate; 12 rows passed and 1 failed, with one
Trust-Damage finding and no blocked or skipped rows.
