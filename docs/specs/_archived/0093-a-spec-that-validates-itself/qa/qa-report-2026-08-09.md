---
spec: 0093-a-spec-that-validates-itself
date: 2026-08-09
build: b7680d00dd4ebed2024db5abf1e9061d815ae846
status: closed
verdict: fail
rows_blocked_environment: 0
rows_blocked_finding: 0
rows_blocked_declared: 0
surfaces: [backend, cli]
---

# QA report — A Spec that validates itself

## Scope and environment

This was the authored terminal QA gate for `task_07` on the Run Worktree build
`b7680d00dd4ebed2024db5abf1e9061d815ae846`. The manifest names `task_07` as
the sole `type: qa` node; it is terminal and its dependency, `task_06`, is
completed. Pull Request journeys were environment-blocked by the prompt fact
that no Pull Request is open, but the gate did not reach matrix planning and
therefore created no Pull Request rows.

The QA gate stopped at its mandatory authoring precondition. Per the gate
contract, no product matrix, static gate, cold-cache verification, CLI journey,
behavior probe, or outside-evidence row ran after this failure.

## Static gate

The mandatory authoring check ran before matrix construction:

```text
rtk go run -buildvcs=false ./cmd/roundfix spec check 0093-a-spec-that-validates-itself --strict
exit: 1

[error] SC-VOCABULARY-UNDOCUMENTED: internal/speccheck/citations.go emits
undocumented token "SC-CITATION-UNSUPPORTED" absent from CONTEXT.md

[error] SC-CITATION-UNSUPPORTED:
docs/specs/0093-a-spec-that-validates-itself/_prd.md claims "ADR-0081 makes the
generated skill copies rewritten by `make skills-sync` sanctioned fallout of
an authorized edit rather than separate targets.", but ADR-0081's subject is
"Sanctioned digest regeneration is fallout of the authorized edit"

Skipped:
SC-REF-UNRESOLVED: missing
docs/specs/0093-a-spec-that-validates-itself/references/_index.md
```

`make verify` did not run. A clean strict authoring check is a precondition of
the QA matrix and cannot be replaced by later static or product evidence.

## Results

No matrix was created. The authoring precondition failed before the gate's
planning boundary.

## Findings

### F-001 — Coined citation code is absent from the glossary

- Impact: `Blocks-Completion`
- Actor: Spec author running the mandatory authoring check.
- Journey step: gate precondition, before QA matrix construction.
- Expected: the full strict check exits `0`, with every emitted vocabulary
  token documented by the declared Vocabulary Contract.
- Actual: the command exited `1` with `SC-VOCABULARY-UNDOCUMENTED` because
  `SC-CITATION-UNSUPPORTED` is absent from `CONTEXT.md`.
- Reproduction: run the exact command recorded under **Static gate** from the
  repository root at the recorded build.
- Evidence: command output under **Static gate**.
- Affected rows: all prospective QA rows; the gate contract prohibits opening
  the matrix while the authoring precondition is red.

### F-002 — The PRD attributes skill-copy behavior to the wrong ADR

- Impact: `Trust-Damage`
- Actor: Spec author running the mandatory authoring check.
- Journey step: gate precondition, before QA matrix construction.
- Expected: every subject attribution is supported by the cited decision
  record.
- Actual: the command exited `1` with `SC-CITATION-UNSUPPORTED`; the PRD says
  ADR-0081 establishes generated skill-copy fallout, while ADR-0081's subject
  concerns sanctioned digest regeneration.
- Reproduction: run the exact command recorded under **Static gate** from the
  repository root at the recorded build.
- Evidence: command output under **Static gate**.
- Affected rows: all prospective QA rows; the gate contract prohibits opening
  the matrix while the authoring precondition is red.

## Blocked and skipped

No row is blocked or skipped because no matrix row was created. The gate itself
stopped on findings F-001 and F-002. To unblock a rerun:

1. Document `SC-CITATION-UNSUPPORTED` in `CONTEXT.md` through an authorized
   corrective Task consistent with the Spec's Vocabulary Contract.
2. Rewrite the PRD claim to match ADR-0081 or cite the decision record that
   carries the claimed skill-copy subject.
3. Rerun the strict authoring check until it exits `0`; then start a new dated
   QA report and execute the complete matrix on that build.

The skipped `SC-REF-UNRESOLVED` detector is not treated as equivalent QA
coverage. Once the precondition is clean, the rerun must retain the
corresponding reference-resolution row unless the detector actually runs.

## Coverage

- User stories passed: 0 of 0 planned; matrix construction did not begin.
- Acceptance criteria passed: 0 of 0 planned; matrix construction did not
  begin.
- Rows passed: 0.
- Rows failed: 0.
- `rows_blocked_environment`: 0.
- `rows_blocked_finding`: 0.
- `rows_blocked_declared`: 0.
- Findings: 1 `Blocks-Completion`, 1 `Trust-Damage`.
- Behavior probes, surface sweeps, Non-Goal checks, outside evidence, glossary
  journey, cold-cache gate, and authoring-skill refusal journeys: not planned or
  executed because the authoring precondition failed.

## Final verdict

`fail` — repair F-001 and F-002, obtain a clean
`roundfix spec check 0093-a-spec-that-validates-itself --strict`, and rerun the
full QA gate; this report contains zero matrix rows because the mandatory
precondition stopped the gate before planning.
