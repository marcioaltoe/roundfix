---
spec: 0093-a-spec-that-validates-itself
date: 2026-08-09
build: 90aa687f867054d9f087e4e1336e2a8117b374a0
status: closed
verdict: fail
rows_blocked_environment: 1
rows_blocked_finding: 1
rows_blocked_declared: 0
surfaces: [backend, cli]
---

# QA report — A Spec that validates itself

## Scope and environment

Full rerun of the authored terminal `task_07` gate on the Run Worktree build
`90aa687f867054d9f087e4e1336e2a8117b374a0`. The intended actor is a Spec
author or maintainer using the public `roundfix spec check` CLI. The package
implementation has no separately documented HTTP server, so the declared
backend surface is reached through that CLI and its public exit/output
contract. The prompt reports no open Pull Request; this Spec declares no Pull
Request acceptance journey.

The first strict-check attempt selected the sandbox-forbidden user Go cache and
failed with `operation not permitted`. The rerun uses the repository-local
`.gocache`, matching the Makefile's project contract. No customer credentials,
network access, or shared services are required.

## Static gate

The clean authoring precondition is:

```text
rtk proxy env GOCACHE="$PWD/.gocache" go run -buildvcs=false ./cmd/roundfix spec check 0093-a-spec-that-validates-itself --strict
exit: 0
Spec 0093-a-spec-that-validates-itself
No findings.
Skipped:
  SC-REF-UNRESOLVED: missing docs/specs/0093-a-spec-that-validates-itself/references/_index.md
```

The named skipped detector is not credited as equivalent coverage; row 12
retains its fallback check.

The authoritative cold-cache gate was attempted exactly after
`GOCACHE="$PWD/.gocache" go clean -testcache` exited `0`. `rtk make verify`
then reached its `test` prerequisite and was terminated by the managed sandbox:

```text
Network access to "api.github.com" was blocked: domain is not on the allowlist
for the current sandbox mode.
```

This is an environment block, not a code failure. The denied operation was not
retried. Every offline constituent that could be run independently passed:
`rtk make fmt-check`, `rtk make spec-budget`, `rtk make skills-sync-check`,
`rtk make skills-check`, `rtk make build`, and—after the build completed—
`rtk make spec-check`. The initial parallel `spec-check` attempt raced its
`build` prerequisite and found no binary; the dependency-ordered rerun exited
`0`. A fresh focused `internal/speccheck` suite also exited `0`.

## Results

| # | Story / criterion / sweep | Actor and surface | Status | Evidence |
| - | --- | --- | --- | --- |
| 1 | Unsupported Spec 0090 citation is rejected with the claim, cited subject, and non-zero exit | Spec author; CLI/backend | pass | Built CLI against a scratch Git repository copied from `internal/speccheck/testdata/citation/repo`: unscoped command exited `1`; output quoted the full `ADR-0083 makes \`make verify\`` claim, `Adopted sources move to their owning Spec`, both source locations, and a concrete fix. `--stage tasks --format json` independently confirmed the same finding and exit. |
| 2 | Supported citation passes and bare ADR listing is not misclassified; invalid input is rejected | Spec author; CLI/backend | pass | Current-build strict CLI check exited `0` on this Spec's supported claims. Fresh named backend probes `TestCitationAcceptsASupportedClaim`, `TestCitationBareListingIsNotAClaim`, and `TestCitationReportsHowManyClaimsItResolved` all passed; invalid `--stage review` exited `2` and named all accepted values. |
| 3 | `--stage prd|techspec|tasks` scopes cumulatively, reports skipped detectors, and leaves unscoped behavior unchanged | Spec author; CLI/backend | fail | On the same preserved false-citation repository, unscoped and `--stage tasks --format json` exited `1` with `SC-CITATION-UNSUPPORTED`, but `--stage prd` and `--stage techspec` both exited `0`, said `No findings`, and listed the semantic detector in neither findings nor skips. Invalid `--stage review` correctly exited `2` and named `prd`, `techspec`, and `tasks`. See F-001. |
| 4 | `write-prd`'s instructed closing command refuses to report while a finding stands | PRD author; CLI | fail | The skill's instructed `roundfix spec check <slug> --stage prd` command exited `0` and said `No findings` on the preserved false citation. See F-001. |
| 5 | `write-techspec`'s instructed closing command refuses to report while a finding stands | TechSpec author; CLI | fail | The skill's instructed `roundfix spec check <slug> --stage techspec` command exited `0` and said `No findings` on the preserved false citation. See F-001. |
| 6 | Default-sweep corpus findings remain unchanged, including across a fresh test process | Maintainer; backend | pass | Fresh `go test -count=1` run passed `TestStageScopeDefaultSweepIsUnchanged` and `TestCheckCorpusGolden`; `rtk make spec-check` independently completed the current active corpus with no findings. |
| 7 | A scoped run remains affordable against the PRD's 0.04 s baseline, with evidence independent of the Spec artifacts | Spec author; CLI | blocked (finding: F-001 — waits on semantic citation detector in scoped authoring) | Independent OS measurement from `/usr/bin/time -p` recorded `real 0.01` seconds for `bin/roundfix spec check 0093-a-spec-that-validates-itself --stage prd`, but F-001 proves that workload omits the feature's semantic detector, so it cannot establish the intended check's affordability. Unblock by registering the detector, then repeat the same measurement. |
| 8 | Coined token `SC-CITATION-UNSUPPORTED` satisfies the glossary contract | Maintainer; backend/CLI | pass | The strict precondition and full `rtk make spec-check` reported no `SC-VOCABULARY-UNDOCUMENTED`; fresh `TestCheckVocabularySatisfied` coverage passed in the focused `internal/speccheck` run. |
| 9 | The QA matrix drops only named file-decidable rules and every dropped rule is registered in the checker | QA maintainer; CLI/backend | pass | The skill's mapping names 20 `SC-*` rules; source lookup resolved every named constant. Full strict JSON on this build returned no findings and skipped only `SC-REF-UNRESOLVED`, which remains row 12. Fresh `TestProjectConstraintQAGate` and `TestProjectConstraintJourney` passed, proving the retained commit-dependent clauses. F-001 separately covers the semantic detector's omission from the narrower authoring stages. |
| 10 | Task 05 protected-tooling commit obeys exact bounded paths and authorization chronology | Maintainer; Git/infra | fail | Authorization commit `599a7c1b` is an ancestor of `bb101161`; fsmonitor-disabled `git diff-tree` showed only the two canonical authoring skills, their generated copies, and `task_05.md`; both copy pairs are byte-identical and no current tooling delta exists. The Task commit body, however, contains only its subject and Roundfix trailers and omits the standing grant's mandatory performance/reliability account. See F-002. |
| 11 | Task 06 protected-tooling commit obeys exact bounded paths and authorization chronology, including later fixes | Maintainer; Git/infra | fail | Authorization commit `599a7c1b` predates `b7680d00`; its `git diff-tree` paths are exactly canonical/generated `qa-gate` plus `task_06.md`, and the copies match. Consequent repairs `cacd73f9` and `90aa687f` are separate and follow the Task commit. The Task commit body omits the standing grant's mandatory performance/reliability account. See F-002. |
| 12 | Skipped `SC-REF-UNRESOLVED` has no unresolved adopted-source path in this Spec | Maintainer; docs/CLI | pass | Full strict JSON ran Task-context resolution with no finding and skipped only the absent `references/_index.md`. `test ! -e .../references` exited `0`, and `git ls-tree -r --name-only HEAD .../references` returned no path, independently proving this Spec adopted no source whose index entry could be unresolved. |
| 13 | Wrong-but-consistent goals remain outside checker judgment; no archived Spec is rewritten | Spec author; CLI/Git | pass | A scratch Git repository copied from the independent clean checker fixture was given the deliberately false but internally consistent goal “the Moon is made of cheese”; public strict JSON exited `0` with no findings, proving no goal judgment. `git diff --exit-code a4294d2e^ HEAD -- docs/specs/_archived` exited `0`, and complete status shows no archived untracked path. |
| 14 | Authoritative `make verify` passes on the Makefile-owned genuinely cold test cache with no cached package result | Maintainer; backend/CLI | blocked (environment: sandbox denied api.github.com during `make verify` test prerequisite) | Local-cache `go clean -testcache` exited `0`; the sandbox then terminated `rtk make verify` on denied `api.github.com`. Offline constituents `fmt-check`, `spec-budget`, `skills-sync-check`, `skills-check`, `build`, and `spec-check` passed, with uncached package results shown for the focused gates. Unblock by rerunning the exact cold-cache sequence in a full-access session. |
| 15 | QA report contract closes with a machine-readable verdict and exact blocked-row counts | Roundfix Daemon; docs | pass | Closed frontmatter records `verdict: fail`, environment `1`, finding `1`, declared `0`; table postflight proves 8 pass, 5 fail, 1 environment-block, 1 finding-block, and zero pending/skipped. |

### Focused command evidence

- Public false-citation journey: a current-build binary ran from a scratch Git
  repository copied from
  `internal/speccheck/testdata/citation/repo`. Unscoped text and `--stage tasks
  --format json` each exited `1` with the claim, ADR title, both source paths,
  and fix. Separate processes provide the reopen confirmation.
- Stage probes: `--stage prd` and `--stage techspec` each exited `0` with `No
  findings` on that same input; `--stage review` exited `2` and named the three
  accepted values.
- Citation controls: fresh named tests for supported text, bare listing, and
  resolved-claim count all passed. Current-build strict CLI also accepted the
  corrected live Spec.
- Corpus and glossary: fresh `TestStageScopeDefaultSweepIsUnchanged`,
  `TestCheckCorpusGolden`, and `TestCheckVocabularySatisfied` coverage passed;
  full active-corpus `make spec-check` exited `0`.
- External measurement: macOS `/usr/bin/time -p` reported `real 0.01`, `user
  0.00`, and `sys 0.00` for the PRD-scoped current-build command. The operating
  system timer and current built CLI—not text authored by this Spec—are the
  evidence origin. The result is retained but not credited because F-001 makes
  the measured workload incomplete.
- Checker/matrix mapping: the QA skill's mapping contains 20 named `SC-*`
  rules; source lookup resolved every code. Current full strict JSON was
  `{"findings":[],"skipped":[{"code":"SC-REF-UNRESOLVED",...}]}`. The only
  skipped rule has its retained fallback in row 12.
- Tooling chronology: fsmonitor-disabled `git diff-tree --no-commit-id
  --name-status -r` inspected `bb101161`, `b7680d00`, `cacd73f9`, and
  `90aa687f`; `git merge-base --is-ancestor` proved authorization `599a7c1b`
  predates both Task commits and `cacd73f9` follows Task 06. Byte comparisons
  passed for every canonical/generated skill pair.
- Scope controls: the absent references directory passed `test ! -e`, its
  `git ls-tree` was empty, and the full checker reported no Task-context
  reference finding. The archived-Spec diff from `a4294d2e^` through `HEAD` was
  empty. A separate wrong-but-consistent fixture exited `0` with no finding.

## Findings

### F-001 — Stage-scoped authoring omits the semantic citation detector

- Impact: `Blocks-Completion`
- Actor: PRD or TechSpec author following the shipped authoring skill.
- Journey step: run the required closing `roundfix spec check <slug> --stage
  prd|techspec` command while the artifact carries Spec 0090's original false
  attribution.
- Expected: each command resolves the PRD claim, emits
  `SC-CITATION-UNSUPPORTED` with both texts, and exits non-zero so the skill
  refuses to report completion.
- Actual: both scoped commands exited `0` and printed `No findings`; the same
  built CLI, repository, and artifact produced the expected finding under the
  unscoped and `tasks` scopes.
- Reproduction: build `./cmd/roundfix`; copy
  `internal/speccheck/testdata/citation/repo` into a scratch Git repository;
  run `roundfix spec check 0090-a-gate-that-could-have-failed --stage prd` and
  repeat with `--stage techspec`.
- Diagnosis: the stage registry and `checkAuthoringStage` path do not register
  or invoke `SC-CITATION-UNSUPPORTED`; the unscoped `Check` path does.
- Affected rows: 3, 4, and 5.

### F-002 — Tooling Task commits omit the standing-grant audit statement

- Impact: `Blocks-Completion`
- Actor: maintainer auditing protected-tooling authorization after the Daemon
  commits Tasks 05 and 06.
- Journey step: resolve the authorization commit, actual Task-commit paths,
  canonical/generated copies, later repairs, and the Task commit bodies.
- Expected: each protected-tooling commit states which performance half it
  serves and how reliability was preserved, as required by the standing grant
  and by Requirement 5 of Task 05 / Requirement 6 of Task 06.
- Actual: commits `bb101161` and `b7680d00` contain only a subject plus
  `Roundfix-Spec` / `Roundfix-Task` trailers. Neither contains the mandatory
  performance/reliability account supplied in its Task Result.
- Reproduction: run `git show -s --format=fuller bb101161` and repeat for
  `b7680d00`; compare their bodies with **Obligations that travel with it** in
  `docs/workflow/authorizations/2026-08-09-standing-tooling-authority-for-loop-performance.md`.
- Complete audit: authorization `599a7c1b` predates both Tasks; Task changed
  paths are exact; skill copies match; current tooling delta is empty;
  consequent repairs are separate and ordered after Task 06. No other
  authorization-shape defect was found.
- Affected rows: 10 and 11.

## Blocked and skipped

- Row 7 is `blocked (finding: F-001 — waits on semantic citation detector in
  scoped authoring)`. Register and execute `SC-CITATION-UNSUPPORTED` in both
  PRD and TechSpec authoring scopes, then repeat the same independent timing.
- Row 14 is `blocked (environment: sandbox denied api.github.com during make
  verify test prerequisite)`. A full-access session must run
  `GOCACHE="$PWD/.gocache" go clean -testcache` followed by `rtk make verify`
  and confirm no Go package reports `(cached)`.
- No row is declared-blocked or skipped. The prompt's “no open Pull Request”
  fact does not create a blocked row because this Spec declares no Pull Request
  journey.

## Coverage

- Rows: 8 pass, 5 fail, 1 `blocked (environment)`, 1 `blocked (finding)`, 0
  `blocked (declared)`, 0 skipped, 0 pending.
- PRD goals: 3 of 4 pass. Goal 2 fails because stage-scoped authoring omits the
  new citation detector; Goals 1, 3, and 4 have public-command, mapping, corpus,
  and scope evidence.
- Task 07 acceptance: 6 of 8 pass, 1 fails (both wired skills do not block),
  and 1 is finding-blocked (affordability of the complete scoped workload).
- Non-Goals: wrong-but-consistent content is not judged; the authored QA gate
  remains in place; post-commit checks remain command-backed; archived Specs
  have no diff.
- Behavior probes completed: false versus supported citation, bare listing,
  invalid stage, scoped versus unscoped output, fresh-process corpus execution,
  and wrong-but-consistent content. These target silent parser misses, false
  accusations, invalid/out-of-order CLI input, stage scope loss, cache-masked
  regressions, and scope creep.
- Surfaces: CLI exercised through a built operator binary in separate
  processes; backend package behavior exercised through fresh named tests.
  There is no documented HTTP endpoint or frontend/TUI surface for this Spec.
- `rows_blocked_environment`: 1; `rows_blocked_finding`: 1;
  `rows_blocked_declared`: 0.
- Findings by impact: 2 `Blocks-Completion`; no Data-Loss, Trust-Damage,
  Friction, or Cosmetic findings.

## Final verdict

`fail` — register `SC-CITATION-UNSUPPORTED` in the PRD and TechSpec stage
execution paths, repair the protected-tooling commit audit history through an
authorized approach, then rerun the full cold-cache gate in a session that may
reach `api.github.com` and start a new collision-safe QA report.
