---
spec: 0093-a-spec-that-validates-itself
date: 2026-08-09
build: b47142eb41c7275fd8759e5a4e968013f5ea5591
status: closed
verdict: fail
rows_blocked_environment: 1
rows_blocked_finding: 0
rows_blocked_declared: 0
surfaces: [backend, cli]
---

# QA report — A Spec that validates itself

## Scope and environment

Full rerun of the authored terminal `task_07` gate on the Run Worktree build
`b47142eb41c7275fd8759e5a4e968013f5ea5591`. The actor is a Spec author or
maintainer using the public `roundfix spec check` CLI. The declared backend
surface has no separately documented HTTP server and is reached through that
CLI and its exit/output contract. The prompt reports no open Pull Request, and
the Spec declares no Pull Request journey.

The gate uses the repository-local `.gocache`. Scratch repositories and
operator binaries may be created under the managed temporary directory; no
credentials, shared database, or customer service is required.

## Static gate

The authoring precondition passed before this matrix was opened:

```text
rtk proxy env GOCACHE="$PWD/.gocache" go run -buildvcs=false ./cmd/roundfix spec check 0093-a-spec-that-validates-itself --strict
exit: 0
Spec 0093-a-spec-that-validates-itself
No findings.
Skipped:
  SC-REF-UNRESOLVED: missing docs/specs/0093-a-spec-that-validates-itself/references/_index.md
```

The skipped detector is not credited as equivalent coverage; row 11 retains
its fallback check.

The authoritative gate used a genuinely cold repository-local test cache:

```text
rtk proxy env GOCACHE="$PWD/.gocache" go clean -testcache
exit: 0

rtk make verify
environment block: Network access to "api.github.com" was blocked because the
domain is not on the managed sandbox allowlist.
```

The denied operation was not retried. Current-build offline constituents
`rtk make fmt-check`, `rtk make spec-budget`, `rtk make skills-sync-check`,
`rtk make skills-check`, `rtk make build`, and the dependency-ordered
`rtk make spec-check` each exited `0`. Fresh `-count=1` suites for the changed
checker, its CLI contract, and the retained skill clauses also exited `0` and
reported uncached package results.

## Results

| # | Story / criterion / sweep | Actor and surface | Steps and expected observable | Independent confirmation / persistence | Evidence | Status |
| - | --- | --- | --- | --- | --- | --- |
| 1 | Unsupported Spec 0090 citation is rejected with the claim, cited subject, and non-zero exit | Spec author; CLI/backend | Run the current operator binary unscoped on the preserved false-citation fixture; expect `SC-CITATION-UNSUPPORTED`, both texts, and exit 1. | `--stage tasks --format json` in a separate process returned the same finding and exit. | Unscoped and JSON commands exited `1`; both quote the `ADR-0083 makes \`make verify\`` claim, `Adopted sources move to their owning Spec`, both paths, and the fix. | pass |
| 2 | `write-prd`'s instructed closing command refuses to report while a finding stands | PRD author; CLI | Run the skill-prescribed `roundfix spec check <slug> --stage prd` against the false-citation fixture; expect the semantic finding and exit 1. | A fresh focused stage test independently requires the same PRD finding. | Public command exited `1` with both texts; `TestStageScopeRunsTheCitationDetectorInAuthoringStages/PRD` passed with `-count=1`. | pass |
| 3 | `write-techspec`'s instructed closing command refuses to report while a finding stands | TechSpec author; CLI | Run the skill-prescribed `roundfix spec check <slug> --stage techspec` against the same fixture; expect the semantic finding and exit 1. | A fresh focused stage test independently requires the same TechSpec finding. | Public command exited `1` with both texts; `TestStageScopeRunsTheCitationDetectorInAuthoringStages/TechSpec` passed with `-count=1`. | pass |
| 4 | Stage scopes are cumulative, named, and safe on supported, bare-listing, and invalid inputs | Spec author; CLI/backend | Exercise `prd`, `techspec`, and `tasks`; supported claims and bare listings must not be accused; an invalid stage must exit 2 and name accepted values. | Named fresh-process package cases passed; `tasks` and unscoped public commands produced the same semantic finding. | `TestCitationAcceptsASupportedClaim`, `TestCitationBareListingIsNotAClaim`, `TestCitationReportsHowManyClaimsItResolved`, and all CLI stage cases passed; `--stage review` exited `2` and named `prd`, `techspec`, `tasks`. | pass |
| 5 | The default sweep's corpus findings are unchanged from before this Spec | Maintainer; backend | Run the default-sweep characterization and corpus golden with cache disabled; expect both to pass. | Run the public active-corpus `spec-check` target in a separate process. | `TestStageScopeDefaultSweepIsUnchanged` and `TestCheckCorpusGolden` passed with `-count=1`; `rtk make spec-check` exited `0` across the active corpus. | pass |
| 6 | A complete scoped run remains affordable against the PRD's 0.04-second baseline, with evidence outside the Spec artifacts | Spec author; CLI | Measure the current `--stage prd` operator command with the operating-system wall-clock timer; expect no slower than 0.04 seconds. | The measured false-citation workload emitted the semantic finding, proving the detector was included. | macOS `/usr/bin/time -p` reported `real 0.00`, `user 0.00`, `sys 0.00`; evidence origin is the OS timer and built CLI, not a Spec-authored artifact. | pass |
| 7 | Coined token `SC-CITATION-UNSUPPORTED` satisfies the glossary contract | Maintainer; backend/CLI | Run the strict authoring check and focused vocabulary check; expect no undocumented-token finding. | Resolve the glossary entry through `CONTEXT.md` and the public full checker. | Strict text/JSON and `rtk make spec-check` reported no vocabulary finding; `TestCheckVocabularySatisfied` passed; `CONTEXT.md:176` defines the token. | pass |
| 8 | The QA matrix drops only named file-decidable rules and every dropped rule runs in the checker | QA maintainer; CLI/backend | Resolve the QA skill's 20 named `SC-*` mappings in checker source; expect no missing rule. | Strict JSON skipped only `SC-REF-UNRESOLVED`, retained in row 11; fresh skill contract tests must preserve commit-dependent audit clauses. | Source lookup resolved all 20 codes; `TestProjectConstraintQAGate` and `TestProjectConstraintJourney` passed. | pass |
| 9 | Task 05 protected-tooling commit obeys exact bounded paths and authorization chronology | Maintainer; Git/infra | Resolve authorization and Task commit ancestry, actual `diff-tree` paths, commit body obligations, generated copies, and current delta; expect exact scope and valid chronology. | Git-object path reads and both canonical/generated byte comparisons passed. | Authorization `599a7c1b` predates `bb101161`; paths are exact and copies match, but the Task commit omits the grant-required performance/reliability statement. See repeated F-002. | fail |
| 10 | Task 06 protected-tooling commit obeys exact bounded paths and authorization chronology, including later fixes | Maintainer; Git/infra | Resolve authorization, Task commit, consequent-fix ancestry, actual paths, commit body obligations, generated copy, and current delta; expect exact scope and valid chronology. | Git-object reads prove `cacd73f9` and `90aa687f` are separate later commits; the canonical/generated QA skill matches. | Authorization `599a7c1b` predates `b7680d00`; Task paths are exact, later repairs are ordered after it, but the Task commit omits the required statement. The late audit explicitly says it does not make the commit compliant. See repeated F-002. | fail |
| 11 | Skipped `SC-REF-UNRESOLVED` has no unresolved adopted-source path in this Spec | Maintainer; docs/CLI | Use the public strict checker plus repository tree reads to prove the missing index corresponds to no adopted source. | Confirm both worktree and `HEAD` contain no Spec reference path. | Strict JSON has no reference finding; worktree existence check exited `0`, and `git ls-tree` returned no reference path. | pass |
| 12 | Non-Goals hold: wrong-but-consistent goals are not judged, the QA gate remains, commit checks remain, and archived Specs stay byte-identical | Spec author/maintainer; CLI/Git | Run the public checker on a scratch wrong-but-consistent artifact and inspect no archive delta. | Reopen through JSON and complete untracked-file status. | A scratch PRD/TechSpec both asserting “The Moon is made of cheese” exited `0` in text and JSON; archived-Spec diff from `a4294d2e^` through `HEAD` is empty; row 8 proves the gate and commit clauses remain. | pass |
| 13 | Authoritative `make verify` passes on the Makefile-owned genuinely cold test cache with no cached package result | Maintainer; backend/CLI | Clean the repository-local test cache, then run unpiped `rtk make verify`; expect exit 0 and no `(cached)` package result. | Current-build offline constituents and fresh `-count=1` feature suites passed. | Cold-cache clean exited `0`; managed sandbox denied `api.github.com` during the test prerequisite. Equivalent current-build evidence is listed under **Static gate**. | blocked (environment: managed sandbox denied api.github.com during make verify test prerequisite) |
| 14 | QA report contract closes with a machine-readable verdict and exact blocked-row counts | Roundfix Daemon; docs | Close every row, set the mechanical verdict, and record exact typed blocked counts. | Re-read frontmatter and count terminal row statuses after all other checks. | Closed frontmatter records verdict `fail`, environment `1`, finding `0`, declared `0`; table count is 11 pass, 2 fail, 1 environment block, zero pending/skipped. | pass |

## Findings

### F-002 — Tooling Task commits omit the standing-grant audit statement (repeated)

- Impact: `Blocks-Completion`.
- Actor: maintainer auditing protected-tooling authorization after the Daemon
  commits Tasks 05 and 06.
- Journey step: resolve authorization ancestry, actual Task-commit paths,
  canonical/generated copies, later repairs, and each Task commit body.
- Expected: each protected-tooling commit names the performance half it serves
  and how reliability was preserved, as required by the standing grant and by
  Task 05 Requirement 5 / Task 06 Requirement 6.
- Actual: `bb101161` and `b7680d00` still contain only their subject and
  Roundfix trailers. The addendum at `c6cf73d3` records the omission after the
  fact and explicitly states that it does not make either commit compliant.
- Reproduction: run `rtk git show -s --format=fuller bb101161` and repeat for
  `b7680d00`; compare the bodies with **Obligations that travel with it** in
  `docs/workflow/authorizations/2026-08-09-standing-tooling-authority-for-loop-performance.md`.
- Complete audit: authorization `599a7c1b` predates both Task commits; their
  committed paths exactly match their bounded lists; all canonical/generated
  skill pairs are byte-identical; the current tooling delta is empty; and Task
  06's consequent repairs are separate and ordered after their cause. No other
  authorization-shape problem was found.
- Affected rows: 9 and 10.

## Blocked and skipped

- Row 13 is `blocked (environment: managed sandbox denied api.github.com during
  make verify test prerequisite)`. The proof is the sandbox termination from
  the exact unpiped cold-cache gate. Equivalent observed evidence consists of
  the six current-build offline constituents and fresh uncached feature,
  command, and skill-contract suites listed under **Static gate**. A full-access
  session must rerun `GOCACHE="$PWD/.gocache" go clean -testcache` followed by
  `rtk make verify` and confirm no package reports `(cached)`.
- No row is finding-blocked, declared-blocked, or skipped. The prompt's “no
  open Pull Request” fact creates no row because the Spec declares no Pull
  Request journey.

## Coverage

Rows: 11 pass, 2 fail, 1 environment block, 0 finding blocks, 0 declared
blocks, 0 skipped, and 0 pending. Goals 1, 2, 3, and 4 have public-command, scoped
timing, corpus, mapping, and scope evidence; the protected-tooling settlement
contract still fails independently.

Task 07 acceptance has current evidence for the operator finding, unchanged
corpus, 0.00-second scoped measurement, glossary, both authoring-skill refusal
journeys, and all 20 dropped-rule mappings. No matrix row re-read this Spec's
own prose for governance; the one retained fallback used public checker and
Git tree state because `SC-REF-UNRESOLVED` was explicitly skipped.

Non-Goals passed: wrong-but-consistent content is not judged; the authored QA
gate remains; commit-dependent audits remain command-backed; archived Specs
have no diff. Probes covered false versus supported citation, bare listing,
invalid stage, scoped versus unscoped output, fresh-process corpus execution,
complete-workload timing, cache masking, chronology, and scope creep. CLI was
exercised through the built operator binary; backend behavior used fresh named
tests. There is no documented HTTP, frontend, or TUI surface for this Spec.

## Final verdict

`fail` — the feature's assembled operator journeys pass, including the repaired
authoring-stage detector, but Tasks 05 and 06 remain noncompliant with their
standing tooling grant. Repair that history through an authorized approach and
rerun the cold `make verify` gate in a full-access session before another QA
rerun.
