---
spec: 0059-run-storage-compaction-and-global-sanitation
date: 2026-08-06
build: bc44a3ac
status: closed
verdict: fail
rows_blocked_environment: 0
rows_blocked_finding: 2
rows_blocked_declared: 0
surfaces: [backend, cli, data, docs]
---

# QA report — Run storage compaction and global sanitation

## Scope and environment

Full authored terminal QA gate for commit `bc44a3ac` in the Run Worktree on
branch `roundfix/run-run_20260806T050835Z_e5574f34ee0020fa`. The intended actor
is a local Roundfix operator. The production-like entry point is the built
`roundfix` CLI against isolated `HOME` directories and scratch Git
repositories/databases. No Pull Request is open; this Spec declares no Pull
Request journey, and no Pull Request state is inferred from the per-Run branch.

The PRD contains no `Unreachable Acceptance` declaration. The gate therefore
cannot use `blocked (declared: ...)` for any row.

Evidence directory:
`docs/specs/0059-run-storage-compaction-and-global-sanitation/qa/evidence/2026-08-06-01/`.

High-risk behavior probes are selected where they exercise the stated data
safety hazards:

- Compaction: Active Run, concurrent writer, insufficient capacity, stale
  preview, and interrupted/reopened database.
- Sanitation: dry-run before apply, a second apply, cross-repository discovery,
  Review Artifact preservation, and ambiguous/outside-Home roots.
- Storage report: outside-Git execution, missing recorded root, unexpected
  input, repeat read, and exact before/after state comparison.

## Project Constraint audit

Passed. The active PRD and TechSpec each account for identifier strategy
(not applicable: identities stay unchanged), authentication and HTTP (not
applicable: local SQLite/filesystem only), active ADR obligations, and tooling
authority, with operative sources under `docs/agents/`.

The express authorization commit `9bdaedbe` predates and is an ancestor of the
Spec authoring commit `94d75767`. It names Spec 0059 and the exact canonical
Skill, mirror, and sanctioned ADR-0081 fallout paths. Exact `git diff-tree`
audits found no protected-tooling change in Tasks 01–04; Task 05 commit
`bc44a3ac` changed only `task_05.md`. Thus no authorization, prerequisite fix,
consequent fix, or out-of-scope protected mutation was folded into a Task
commit. Task 05's failure to make its required authorized change is recorded
separately as F-002.

## Static gate

- `rtk make verify` — passed on build `bc44a3ac` (exit 0): 3,515 Go tests
  passed across 26 packages; the corpus-budget test, four Skill tests,
  `roundfix skills check`, build, and Spec Consistency Check also passed.
  The consistency command reported only explicitly skipped checks for absent
  optional artifacts; it reported `No findings` for Spec 0059.

## Results

| # | Story / criterion / sweep | Actor and surface | Entry point and steps | Expected observable | Independent confirmation / persistence | Evidence | Status |
| - | --- | --- | --- | --- | --- | --- | --- |
| R01 | Authored gate preconditions and Project Constraint/tooling-authority audit | Maintainer; docs/repository | Read manifest, PRD, TechSpec, every Task Result, authorization, ADRs, and exact Task commit paths | QA node is terminal; dependencies completed; constraints are accounted for; protected tooling changes are authorized and correctly ordered | `task_06` is terminal and all dependencies are completed; four constraints accounted for; `9bdaedbe` predates authoring; exact Task paths contain no unauthorized tooling mutation | `evidence/2026-08-06-01/focused-and-audit.txt` | pass |
| R02 | Full repository verification | Maintainer; backend/cli/data/docs | Run `rtk make verify` unpiped on build `bc44a3ac` | Formatter, tests, Skill checks, and build all exit 0 | Exit 0; 3,515 tests in 26 packages, focused corpus/Skill tests, Skill check, build, and Spec check passed | Static gate above | pass |
| R03 | US1: explicit compaction preview and apply | Operator; CLI/data | Build CLI; run `roundfix gc compact`, inspect before/reclaimable/projected-after, then explicitly apply on a scratch Run Database with reclaimable pages | Preview is deterministic; apply reclaims the previewed bytes within declared tolerance; completion reports before/reclaimed/after | Public entry point rejected `compact` with exit 2; no preview/apply journey exists | `evidence/2026-08-06-01/cli-compaction-and-storage.txt`; F-001 | fail |
| R04 | US2: compaction safety refusals | Operator; CLI/data | Invoke compaction with an Active Run, another writer, insufficient capacity, and stale/out-of-order preview | Each refusal names its blocker or shortfall and occurs before mutation | All promised refusal journeys require the missing compaction route | `evidence/2026-08-06-01/cli-compaction-and-storage.txt`; F-001 | blocked (finding: F-001 — waits on shipped `gc compact` route) |
| R05 | Compaction interruption and retention separation | Operator; CLI/data | Exercise cancellation/reopen path and ordinary retention sweep | Database remains usable; retention never compacts automatically or changes its deletion policy | Interruption/reopen journey requires the missing route; retention separation remains covered by R13/R14 | `evidence/2026-08-06-01/cli-compaction-and-storage.txt`; F-001 | blocked (finding: F-001 — waits on shipped `gc compact` route) |
| R06 | US3: global sanitation discovers multiple repositories | Operator; CLI/data | Seed durable Runs for several repositories and invoke `roundfix gc sanitize` outside their repositories | Every recorded Artifact Root appears; discovery uses durable metadata rather than current repository scope | One machine-wide invocation reported all six roots recorded across six repository paths, independent of the current repository | `evidence/2026-08-06-01/sanitation-and-retention.txt` | pass |
| R07 | US5: six root classifications and proof | Cautious operator; CLI/data | Exercise active, orphaned, missing, overridden, outside-Home, and unsafe roots | Each classification and its evidence is reported; ambiguous roots read `preserved` | CLI emitted all six named classifications with durable/filesystem evidence; preserved files retained their hashes after apply | `evidence/2026-08-06-01/sanitation-and-retention.txt` | pass |
| R08 | Sanitation deletion boundary, Review Artifacts, and idempotence | Cautious operator; CLI/data | Run dry-run, apply, then a second apply with eligible, absent, ambiguous, and Review Artifact fixtures | Dry-run removes nothing; apply removes only proven eligible/absent Run directories; Review Artifacts and ambiguity survive; second apply reclaims zero | Dry-run preserved every path; first apply removed exactly 2 directories/144 bytes; second removed 0/0; Review and ambiguous hashes persisted | `evidence/2026-08-06-01/sanitation-and-retention.txt` | pass |
| R09 | US4: read-only storage report grouped everywhere | Operator; CLI/data | Run `roundfix storage report` with Runs spanning repositories/states/tables/roots, including a missing root and outside Git | One no-flag report groups measured bytes/rows by repository, state, table, and Artifact Root and labels missing roots | Two fresh processes reported six repository/root groups, two states, all tables, the missing root, database difference 0 within 4,096-byte tolerance, and artifact difference 0 | `evidence/2026-08-06-01/storage-report.txt` | pass |
| R10 | Storage report is read-only and input-safe | Operator; CLI/data | Hash Run Database and Artifact Roots; run report twice; try an unexpected flag; run with empty Home | Successful reads change no byte; unexpected input refuses without side effects; empty Home stays empty | Database SHA-256 stayed `259ca2...f35a4`; selected artifact hashes matched; empty Home stayed empty; bad input exited 2 | `evidence/2026-08-06-01/storage-report.txt`; `cli-compaction-and-storage.txt` | pass |
| R11 | Durable-table lifecycle coverage | Maintainer/operator; docs/data | Follow documented lifecycle policy and inspect a fresh migrated Run Database | Every Roundfix-owned durable table has an owner and retention rule; unknown fixture table fails coverage | Policy names five Roundfix tables and excludes SQLite metadata by owner; three focused live-schema/negative/preservation cases passed | `docs/user-guide/run-database-lifecycle.md`; `evidence/2026-08-06-01/focused-and-audit.txt` | pass |
| R12 | Run index, Active Run locks, and Agent Selection preservation | Operator; data/CLI | Seed Active and terminal Runs, locks, journals, and Agent Selection records; run retention | `runs`, Active Run locks, and Agent Selection rows survive; only eligible terminal journal rows prune | After fresh CLI retention and database reopen: Runs 7→7, locks 1→1, selections 2→2, events 2→1, `quick_check=ok` | `evidence/2026-08-06-01/sanitation-and-retention.txt` | pass |
| R13 | Existing per-repository GC remains unchanged | Operator; CLI/data | Run `roundfix gc`, `--dry-run`, apply, zero-retention, and help journeys | Existing per-repository behavior and Journal Retention semantics remain unchanged | Real dry-run/apply pruned only one eligible journal row; help contract passed; zero-retention and existing behavior also passed in the full 3,515-test gate | `evidence/2026-08-06-01/sanitation-and-retention.txt`; Static gate | pass |
| R14 | Non-Goal: no automatic, threshold, or scheduled compaction | Operator; CLI/data/docs | Run ordinary retention/startup paths and inspect public docs/help | No ordinary path compacts; compaction is explicit only | Real retention left database size 53,248 bytes; focused `PruneTerminalRunsNeverVacuumsAllocatedPages` passed; no scheduled/threshold route is documented or exposed | `evidence/2026-08-06-01/sanitation-and-retention.txt`; `evidence/2026-08-06-01/focused-and-audit.txt` | pass |
| R15 | Non-Goal: no extra Run-row or Active-lock deletion | Operator; data | Exercise retention and sanitation around Active and terminal Runs | No `runs` row or Active Run lock is deleted outside the stated policy | Counts remained Runs=7 and locks=1 through two sanitation applies and one retention apply; `quick_check=ok` | `evidence/2026-08-06-01/sanitation-and-retention.txt` | pass |
| R16 | Non-Goal: no remote/cloud cleanup or filesystem guessing | Cautious operator; CLI/data/docs | Place unrecorded and outside-Home paths near recorded roots; run sanitation apply | Only recorded metadata is trusted; remote/cloud and unrecorded paths remain outside scope | Outside-Home, overridden, unsafe, and Review paths retained exact hashes; only a directory beneath a recorded safe root and absent from the complete Run index was removed | `evidence/2026-08-06-01/sanitation-and-retention.txt` | pass |
| R17 | Non-Goal: terminal Run Worktree reconciliation unchanged | Operator; CLI/docs | Inspect affected paths and command/help contracts; run existing reconciliation checks credited by the full gate | No Reconcile Command or terminal Run Worktree behavior changes | Diff from pre-Spec base contains no `internal/worktree`, reconciliation implementation, or ADR-0053 change; full gate passed | `evidence/2026-08-06-01/focused-and-audit.txt`; Static gate | pass |
| R18 | CLI contract, failure recovery, and vocabulary sweep | Operator; CLI | Exercise `--help`, no-flag commands, invalid/out-of-order input, stderr/stdout, and exit codes | Commands use canonical terms, remain deterministic/non-interactive, and give actionable refusals without partial mutation | Report and sanitation contracts passed, but the primary `gc compact` command is absent and exits 2 before its promised behavior | `evidence/2026-08-06-01/cli-compaction-and-storage.txt`; F-001 | fail |
| R19 | Documentation and Roundfix Skill match shipped CLI | Operator/Agent; docs/CLI | Follow lifecycle guide and canonical Roundfix Skill without undocumented knowledge; compare every new command/flag/refusal | Docs teach the shipped report, sanitation, and explicit compaction behavior; Skill mirror is byte-identical | Lifecycle guide is accurate and mirror bytes match, but neither Skill copy contains any new report, sanitation, or compaction contract | `evidence/2026-08-06-01/focused-and-audit.txt`; F-002 | fail |
| R20 | Measurement invariant: no acceptance asserts a recorded size | Maintainer; docs/tests | Search PRD, TechSpec, Tasks, tests, and QA assertions for literal-size acceptance | All criteria assert moving relations/tolerances, not one recorded database or artifact size | Fixed numbers occur only as historical/problem/Result context; Acceptance Criteria and tests derive values and assert relations | `evidence/2026-08-06-01/focused-and-audit.txt` | pass |

## Findings

### F-001 — The shipped CLI has no compaction command

- Impact: `Blocks-Completion`.
- Actor and journey: operator, US1 at entry; operator, US2 before every safety
  refusal.
- Expected: `roundfix gc compact [--apply]` previews bytes before,
  reclaimable, and projected after; apply performs guarded compaction; Active
  Run, other-writer, and insufficient-capacity cases refuse by name.
- Actual: `roundfix gc --help` lists only `roundfix gc [--dry-run]` and
  `roundfix gc sanitize [--apply]`; `roundfix gc compact` exits 2 with
  `unexpected argument "compact"` and no side effects.
- Reproduction: build with `rtk make verify`, then run
  `HOME=<isolated> ./bin/roundfix gc --help` and
  `HOME=<isolated> ./bin/roundfix gc compact`.
- Evidence:
  `evidence/2026-08-06-01/cli-compaction-and-storage.txt` and Task 05's
  `## Result`, which independently records the same missing route.
- Affected rows: R03 and R18 fail; R04 and R05 are finding-blocked.

### F-002 — The canonical Roundfix Skill omits all three new operator surfaces

- Impact: `Trust-Damage`.
- Actor and journey: an Agent/operator following the canonical Roundfix Skill,
  R19 at the documentation entry point.
- Expected: the authorized Task 05 documents storage report, sanitation and
  compaction, then synchronizes the mirror and deterministic fallout.
- Actual: the canonical and mirrored Skill bytes match, but neither contains
  `storage report`, `gc sanitize`, or `gc compact`. Task 05's Result says it
  intentionally made no partial Skill change, yet the Task is marked
  `completed`.
- Reproduction: run `rtk make skills-sync-check`, compare the two Skill files,
  then search both for the three promised commands.
- Evidence: `evidence/2026-08-06-01/focused-and-audit.txt` and
  `task_05.md#result`.
- Affected row: R19 fails.

## Blocked and skipped

- R04 — `blocked (finding: F-001 — waits on shipped gc compact route)`.
  Unblock by adding the promised CLI preview/apply route, then observe each
  Active Run, other-writer, insufficient-capacity, and stale-preview refusal
  through a fresh built binary while confirming unchanged database bytes.
- R05 — `blocked (finding: F-001 — waits on shipped gc compact route)`.
  Unblock with the same route, then interrupt/reopen a scratch compaction and
  confirm the database remains readable and passes `quick_check`.
- Environment-blocked: 0. Declared-blocked: 0. Skipped: 0.

## Coverage

Twenty planned rows closed: 15 pass, 3 fail, 2 finding-blocked, 0
environment-blocked, 0 declared-blocked, 0 skipped, and 0 pending.

- User stories: US3, US4, and US5 pass; US1 fails; US2 is finding-blocked.
- Core features: global sanitation, lifecycle policy, and storage reporting
  pass; operator-reachable compaction fails.
- Every Task acceptance criterion maps to R01–R20: Task 01 to R09–R10; Task 02
  to R03–R05 and R13–R14; Task 03 to R06–R08 and R13; Task 04 to R11–R12 and
  R15; Task 05 to R02 and R19; Task 06 to R01, R03–R05, R08, R10, and R20.
- All five explicit Non-Goals map to R13–R17. Backend, CLI, data, and docs
  surfaces were exercised. No Pull Request journey was in Spec scope.
- Probes executed: Active Run/other-writer/capacity/stale-preview backend
  refusals; sanitation dry-run/apply/reapply and ambiguous-root preservation;
  storage report repeat/outside-Git/invalid-input/empty-Home checks.

## Final verdict

Fail. Ship the public `roundfix gc compact [--apply]` preview/apply route and
its refusal contract, then synchronize the canonical Roundfix Skill, mirror,
and sanctioned digest fallout before rerunning the full 20-row gate.
