---
spec: 0091-a-proof-that-can-refuse
date: 2026-08-10
build: 876e83c5f2590ca60fc253462ddcdbbe121a0731
status: closed
verdict: fail
rows_blocked_environment: 0
rows_blocked_finding: 0
rows_blocked_declared: 0
surfaces: [backend, cli]
---

# QA report — A proof that can refuse

## Scope and environment

Full same-day rerun of the authored terminal gate on Run Worktree branch
`roundfix/run-run_20260810T170505Z_a667230e70e6d25c`, build
`876e83c5f2590ca60fc253462ddcdbbe121a0731`, on macOS 26.5.2 arm64 with
Go 1.26.5 in `America/Sao_Paulo`. The actor is a Roundfix maintainer using the
built `roundfix profiles validate` CLI, the live ACP runtimes, and public Go
package runners. The exercised artifact identified itself as
`roundfix 0.4.0 (876e83c5-dirty, built 2026-08-10 14:23:19 -0300)`; `dirty`
reflects this report-only QA delta. The prompt confirms no Pull Request is
open; this Spec has no Pull Request acceptance journey, and the per-Run branch
must not be pushed or used to resolve one.

`rtk roundfix spec check 0091-a-proof-that-can-refuse --strict` exited 0 with
`No findings.` before this matrix was created. `_tasks.md` names `task_05` as
the terminal `type: qa` node. Its dependencies `task_07` and `task_04` are
completed, and every non-QA leaf reaches it.

Evidence root:
`docs/specs/0091-a-proof-that-can-refuse/qa/evidence/2026-08-10-gate-02/`.

## Static gate

`GOCACHE="$PWD/.gocache" rtk go clean -testcache` exited 0 immediately before
the authoritative run. Exact `rtk make verify` then exited 0 with authorized
network access. Every tested Go package reported an execution duration rather
than `(cached)`; the suite, focused skills contracts, repository skill check,
and binary build passed. An earlier sandboxed attempt was blocked from
`api.github.com` after warming part of the cache, so its later cached pass was
discarded and the cache was cleared again. See
`evidence/2026-08-10-gate-02/make-verify-cold.txt`.

## Results

| # | Story / criterion / sweep | Actor and surface | Steps and expected observable | Independent confirmation and persistence | Evidence path | Status |
| - | --- | --- | --- | --- | --- | --- |
| QA-01 | Goal 1, Goal 2, Task 05: live `codex` refusal | Maintainer; built CLI → live Codex ACP adapter | Validate a scratch profile naming a model absent from Codex's advertised catalogue; expect refusal, with the adapter message accepted unchanged when it fires first under ADR-0119. | Two fresh processes refused identically; repository and isolated fallback validation then passed a valid Codex tuple. | `evidence/2026-08-10-gate-02/live-codex.txt` | pass |
| QA-02 | Goals 1–2, Task 03, Task 05: exact formerly passing `claude / opus-9-does-not-exist / high` tuple now refuses | Maintainer; built CLI → live Claude ACP adapter | Validate the PRD's exact tuple; expect nonzero refusal. Where Roundfix owns the verdict, expect `model_not_advertised` and the advertised set; where the adapter refuses first, accept its unchanged message under ADR-0119. | Two fresh processes refused identically, replacing the PRD's `passed` before-state; isolated valid `opus/high` passed. | `evidence/2026-08-10-gate-02/live-claude.txt` | pass |
| QA-03 | Goal 1, Goal 2, Task 05, outside evidence: live `opencode` refusal | Maintainer; built CLI → live OpenCode ACP adapter | Validate a scratch profile naming a model absent from OpenCode's live advertised catalogue; expect refusal under ADR-0119. The outside source is the installed adapter, not a Spec fixture. | First and third processes returned the same adapter-first refusal; the second timed out. Two fresh repository processes then passed the valid OpenCode tuple. | `evidence/2026-08-10-gate-02/live-opencode.txt` | pass |
| QA-04 | Goal 5, Task 03, Task 05, Task 07: repository's five distinct tuples retain outcomes and encodings | Maintainer; built CLI against `.roundfixrc.yml` | Run `profiles validate`; expect all configured selections to pass with their established encodings and no `effective_selection_mismatch`. | The command passed four distinct tuples, and isolated repaired `claude/opus/high` passed; the temporary reroute still omits that fifth tuple from the repository profile. | `evidence/2026-08-10-gate-02/repository-profile.txt` | fail (F-006) |
| QA-05 | Goal 4, Task 04: a pre-session proof failure ends at the actionable diagnosis | Maintainer; built CLI with an invalid selection | Trigger a failure before a disposable session opens; expect no trailing cleanup error while the close failure remains recorded. | The built CLI appended `No named session`; code/test inspection showed the fixture assumes exit 4 while installed ACPX returns exit 1 for this absence. | `evidence/2026-08-10-gate-02/cleanup-diagnosis.txt` | fail (repeated F-003) |
| QA-06 | Task 01, Task 06, Task 07 regression corpus | Maintainer; backend and CLI public runners | Run the renamed characterization, all sixteen sibling cases, both detach journeys, and Task 07's matching/mismatch cases; expect every named contract to pass. | Agent selection reported 34 passes, CLI selection reported 6 passes, and the cold authoritative gate passed as the adjacent canary. | `evidence/2026-08-10-gate-02/regression-corpus.txt` | pass |
| QA-07 | Task 05 authoritative cold gate | Maintainer; repository verification | Clear the Makefile-exported worktree cache and run exact `rtk make verify`; expect exit 0 and no Go package line marked `(cached)`. | Exit 0; every package reported a duration, and the skills checks plus binary build passed. | `evidence/2026-08-10-gate-02/make-verify-cold.txt` | pass |
| QA-08 | Task 05 vocabulary acceptance: `Runtime Catalogue` is coined and public emitted terms are documented | Maintainer; docs and CLI contract | Search `CONTEXT.md` for the coined term and TechSpec vocabulary encodings; expect the domain contract to carry terms the feature introduced. | `CONTEXT.md` has no match; production defines `RuntimeCatalogue`, while the TechSpec declares encodings production and glossary do not carry. | `evidence/2026-08-10-gate-02/glossary.txt` | fail (repeated F-004) |
| QA-09 | Goal 3, Task 02, Task 05: catalogue proof sends no prompt | Maintainer; built CLI plus observable ACP invocation record | Validate live selections while capturing adapter/session evidence; expect catalogue ensure/show and no prompt or work turn. | Invalid and valid closed Claude Sessions each reported `0/0` history and `No history`; three exact no-prompt cases passed. | `evidence/2026-08-10-gate-02/token-free.txt` | pass |
| QA-10 | Task 02 seam: honest catalogue, canonical `opus` binding, contamination record, and unchanged intermediate verdicts | Maintainer; backend public runner | Exercise the named catalogue cases; expect pre-request models, `opus` → `opus[1m]`, contamination recording, and no Task-02 verdict change. | The 34-case Agent selection passed; live valid Claude and repository validations supplied assembled canaries. | `evidence/2026-08-10-gate-02/backend-catalogue.txt` | pass |
| QA-11 | Task 03 seam: membership net, adapter fast path, valid encodings, and contamination advisory | Maintainer; backend public runner and built CLI | Exercise membership, advertised-set, fast-path, encoding, contamination, and Claude characterization cases under ADR-0119. | Focused cases passed; live adapters exercised adapter-first refusal, while valid live selections retained their encodings. | `evidence/2026-08-10-gate-02/backend-membership.txt` | pass |
| QA-12 | Task 04 seam: absent-session close omitted; genuine close failure appended; leading diagnosis unchanged | Maintainer; backend public runner | Run both named close cases; expect exact diagnosis equality for absent-session and `AgentSessionCleanupError` for genuine cleanup failure. | Both fixtures passed, but the built CLI appended cleanup noise because installed ACPX reports missing session with exit 1 rather than the fixture's exit 4. | `evidence/2026-08-10-gate-02/backend-cleanup.txt` | fail (repeated F-003) |
| QA-13 | PRD Non-Goals: no vendor-existence validation, reasoning-policy change, picker/recommendation, or automatic repair | Maintainer; built CLI and Git/config read path | Refuse from runtime behavior, inspect CLI help, prove established reasoning encodings, and confirm scratch configs remain unchanged. | Established encodings remained, help stayed read-only, commit paths added no picker, and five executed scratch configs remained byte-identical. | `evidence/2026-08-10-gate-02/non-goals.txt` | pass |
| QA-14 | Project Constraint and bounded-scope audit: no protected tooling mutation; commits respect declared paths | Maintainer; Git commit read path | Resolve Task commits with `git diff-tree`; expect no tooling configuration/script/version-pin mutation and no implementation path outside each Task boundary. | Task commits stayed bounded. The `.roundfixrc.yml` prerequisite had a prior exact authorization and correct ancestry; its unrestored functional state is F-006. | `evidence/2026-08-10-gate-02/tooling-scope-audit.txt` | pass |

High-risk probes executed were fresh-process repeat after each invalid selection,
valid-after-invalid recovery, isolated reopen of every repository tuple,
scratch-config immutability, and genuine mismatch after the alias-normalization
repair. They target refusal ordering, cross-tuple state leakage, persistence,
and the risk that Task 07 weakened mismatch detection.

## Findings

### F-006 — The temporary Claude reroute remains after its repair landed

- Impact: Trust-Damage.
- Actor and step: a maintainer validates or reads the repository's own Agent
  Selection profile after Task 07 fixed `claude/opus/high`.
- Expected: Task 05's five distinct tuples are present and prove with their
  established encodings; the tooling authorization also names restoration as
  the expected end state after F-002 is fixed.
- Actual: both fresh repository validations pass only four distinct tuples.
  Isolated `claude/opus/high` now passes as `independent`, but commit `18ac1e9c`
  still removes it from five categories and says to restore it once F-002 is
  fixed.
- Reproduction: run `bin/roundfix profiles validate --json` from the repository
  root, then compare `.roundfixrc.yml` with authorization
  `docs/workflow/authorizations/2026-08-10-a-broken-tuple-that-blocks-its-own-repair.md`.
- Evidence: `evidence/2026-08-10-gate-02/repository-profile.txt` and
  `evidence/2026-08-10-gate-02/tooling-scope-audit.txt`.
- Affected row: QA-04.

### Repeated F-003 — A missing disposable session still adds cleanup noise

- Impact: Friction.
- Actor and step: a maintainer receives a proof failure before the Codex
  disposable Session opens.
- Expected: the actionable diagnosis ends without a close error for a Session
  that never existed; the cleanup observation remains recorded.
- Actual: installed ACPX returns exit 1 with `No named session`, and the built
  CLI appends that close error. Task 04 maps absence only from exit 4; its two
  fixtures pass but do not cover the installed exit shape.
- Reproduction: run the built invalid Codex scratch profile without access to
  initialize Codex state.
- Evidence: `evidence/2026-08-10-gate-02/cleanup-diagnosis.txt` and
  `evidence/2026-08-10-gate-02/backend-cleanup.txt`.
- Affected rows: QA-05, QA-12.

### Repeated F-004 — Runtime Catalogue vocabulary remains absent

- Impact: Trust-Damage.
- Actor and step: a maintainer looks up the coined term and the encodings named
  by the TechSpec Vocabulary Contract.
- Expected: `CONTEXT.md` documents `Runtime Catalogue` and the public emitted
  vocabulary the feature introduced.
- Actual: the glossary contains no matching term or encoding; production
  defines `RuntimeCatalogue`, while only the TechSpec contains
  `catalogue_contaminated`, and `catalogue_unavailable` is absent from both
  production and glossary.
- Reproduction: run the two searches recorded in the evidence file.
- Evidence: `evidence/2026-08-10-gate-02/glossary.txt`.
- Affected row: QA-08.

## Blocked and skipped

No row is blocked or skipped. The first static-gate attempt was sandbox-blocked
from `api.github.com`; an authorized rerun reached the dependency, and a second
cache clear produced valid cold evidence. One OpenCode refusal process timed
out, but the first and third fresh processes produced the same live refusal and
two repository processes proved the valid OpenCode tuple. The prompt confirms
no Pull Request is open, and this Spec defines no Pull Request journey.

## Coverage

- Rows: 14 total — 10 pass, 4 fail, 0 environment-blocked, 0 finding-blocked,
  0 declared-blocked, 0 skipped, 0 pending.
- PRD: Goals 1, 2, 3, and 5 pass under ADR-0119; Goal 4 fails on the assembled
  missing-session exit shape. All four Non-Goals pass.
- Tasks: Task 07 fixes F-002 and the Task 06 detach regressions; Task 05's
  five-tuple criterion fails because the prerequisite reroute remains. Task 04
  fails on installed ACPX behavior, and the domain vocabulary close check
  still fails.
- Surfaces: built CLI plus backend public runners; live Codex, Claude, and
  OpenCode adapters; cold repository gate; Git commit read path.
- Outside evidence: the installed live OpenCode adapter refused the invented
  model twice, with one disclosed intervening timeout; the source is external
  to this Spec's fixtures.
- Probes: fresh-process repeats, valid-after-invalid recovery, isolated repaired
  Claude tuple, genuine mismatch canary, closed-session history, and
  scratch-config immutability.
- Regression history: prior F-001 is accepted by ADR-0119; F-002 and F-005 are
  fixed; F-003 and F-004 repeat; F-006 is new.

## Final verdict

Fail: 4 of 14 rows fail across three findings (two Trust-Damage and one
Friction); restore the authorized Claude profile entries, support the installed
ACPX missing-session exit shape without appending cleanup noise, and settle the
Runtime Catalogue vocabulary contract before rerunning the full gate.
