# QA-05 — Finding-blocked refusal

Status: pass

The built Archive Command exited 2 with
`rows_blocked_finding is 1; expected 0`. A fresh read retained
`docs/specs/qa-case/` and found no archived destination.
