---
spec: 0014-run-store-retention
date: 2026-07-06
verdict: pass
surfaces: [cli, infra, docs]
---

# QA Report — Run Store Retention

## Summary

The Run Store Retention feature passed QA across CLI, data-store, filesystem,
preflight, and documentation surfaces. The static gate passed, the real
`roundfix gc` command pruned only eligible terminal Run journals and run
artifact directories in a scratch Run Database, `--dry-run` changed nothing,
`journal_retention: 0` skipped pruning, and the docs/skill surface matched the
shipped CLI.

## Results

| # | Criterion / user story | Verdict | Evidence |
| - | ---------------------- | ------- | -------- |
| 1 | PRD story 1: stale terminal Run Event Journal rows stop accumulating without bound. | pass | Scratch DB seeded with `run_terminal_old` completed on `2026-06-01T00:00:00Z` and 2 journal rows. Live `roundfix gc` reported `Runs pruned: 1` and `Journal rows removed: 2`; post-check showed `run_terminal_old` kept its `runs` row with `events=0`. |
| 2 | PRD story 2: `roundfix gc` reclaims storage on demand and reports what it freed. | pass | `rtk env HOME=/private/tmp/roundfix-retention-qa-fRf6pX/home ... bin/roundfix gc` exited 0 and printed `GC complete`, `Runs pruned: 1`, `Journal rows removed: 2`, `Artifact bytes reclaimed: 50`, and `Orphan artifact dirs removed: 1`. |
| 3 | PRD story 3: `roundfix gc --dry-run` previews the same set without deleting. | pass | `roundfix gc --dry-run` exited 0 and listed `run_terminal_old` plus orphan `run_orphan`, `Journal rows eligible: 2`, and `Artifact bytes reclaimable: 50`. Immediate DB/file checks still showed all 4 initial journal rows and all 4 run artifact directories present. |
| 4 | PRD story 4: retention never touches Active Runs, `runs` rows, or active-run locks. | pass | After live GC, `run_active_old` still had `events=1`, `run_recent` still had `events=1`, `run_rows` remained 3, and `active_locks` remained 1. `stat` confirmed `artifacts/runs/run_active_old` still existed. |
| 5 | PRD story 5: the preflight sweep prunes eligible terminal Runs automatically. | pass | Fresh focused checks passed: `rtk go test ./internal/cli/ -run TestRunImplementPreflightPrunesRetainedRunStorage -count=1` and `rtk go test ./internal/cli/ -run TestRunImplementPreflightRetention -count=1`. These cover positive sweep pruning, warning/non-fatal behavior, and `journal_retention: 0` skip behavior through the implement preflight path. |
| 6 | Core feature: `store.journal_retention` default is `336h`; `0` disables pruning. | pass | Focused config/store tests passed with `rtk go test ./internal/store/ ./internal/config/ -run 'TestPruneTerminalRuns|TestLoad.*Store|TestLoadRejectsUnknownStore' -count=1`. Real zero-retention run printed `GC skipped`, `Journal Retention: 0`, and `No pruning performed.` |
| 7 | Core feature: `PruneTerminalRuns` deletes only terminal Run Event Journal rows older than the cutoff and returns ids/counts. | pass | Task 01 result and focused tests passed. Real GC pruned only `run_terminal_old` journal rows; `run_terminal_old` remained in `runs`, while recent and Active journals remained. |
| 8 | Core feature: GC removes pruned Run artifact dirs and orphaned `runs/<id>` dirs under the run artifact root only. | pass | Live GC removed `artifacts/runs/run_terminal_old` and `artifacts/runs/run_orphan` (`stat` returned not found for both). `find artifacts/runs` after GC listed only `run_active_old/journal.log` and `run_recent/journal.log`. |
| 9 | Core feature: retention does not remove Review artifacts under the spec tree. | pass | `stat /private/tmp/roundfix-retention-qa-fRf6pX/work/docs/specs/0014-run-store-retention/reviews/round-001/issue_001.md` exited 0 after live GC. README and skill docs also state Review artifacts under the Spec tree are outside retention scope. |
| 10 | Core feature: preflight prune is best-effort and reuses the GC primitive. | pass | Focused preflight tests passed, including non-fatal warning and zero-retention cases. Static inspection found `sweepRunRetention` calling shared `pruneRunRetention`, and `pruneRunRetention` delegates deletion to `Store.PruneTerminalRuns`. |
| 11 | Config strict validation: unknown `store` keys still fail. | pass | Real CLI run with scratch User Config containing `store.unknown_key` exited 2 and printed `store.unknown_key is not a supported config key`. |
| 12 | `roundfix gc --help` is concise and truthful. | pass | `rtk env ROUNDFIX_COLOR=never bin/roundfix gc --help` exited 0 and printed `roundfix gc [--dry-run]`, terminal Run journal pruning, orphaned `runs/<id>` cleanup under the run artifact root, and safety text for Run rows, Active Run locks, and Review artifacts outside the run artifact root. |
| 13 | Docs accurately describe retention, GC, `--dry-run`, Active Run safety, preflight pruning, and Review artifact scope. | pass | `rtk grep -R "store.journal_retention\|roundfix gc\|Review artifacts\|Active Runs" README.md .agents/skills/roundfix/SKILL.md skills/roundfix/SKILL.md` found the expected README and skill coverage. |
| 14 | Roundfix Skill matches the CLI surface and the embedded copy has no drift. | pass | `rtk go run ./cmd/roundfix skills check` printed `Roundfix skill check passed: roundfix`; `rtk make skills-sync-check` exited 0 with no drift output. |
| 15 | Non-goals: retention does not delete `runs` rows, compact/vacuum SQLite, change journal replay/cursor format, or implement per-Run journal opt-in. | pass | Scratch DB post-check preserved all Run rows. Static inspection found no `DELETE FROM runs` or `DROP TABLE runs` in `internal/store` or `internal/cli`; `VACUUM` appeared only in spec text as a deferred non-goal. Retention deletion code is isolated to `DELETE FROM run_events` in `internal/store/journal.go`. |

## Failures and blocked items

None.

## Environment

- Workspace:
  `/Users/marcio/.roundfix/worktrees/roundfix-339f8dac/run_20260706T175119Z_34ec9ab4ae40d3d6`
- QA date: `2026-07-06`
- Static gate:
  - `rtk make verify` passed.
  - Output included `rtk go test ./...` with `Go test: 801 passed in 18 packages`,
    `Roundfix skill check passed: roundfix`, and
    `rtk go build -buildvcs=false -o bin/roundfix ./cmd/roundfix`.
- Scratch runtime:
  - Temporary home/work/artifact root:
    `/private/tmp/roundfix-retention-qa-fRf6pX`
  - Real CLI command used the built binary:
    `bin/roundfix`.
  - Scratch User Config set
    `defaults.artifact_dir: /private/tmp/roundfix-retention-qa-fRf6pX/artifacts`
    and `store.journal_retention: 336h`, then `0` for the disabled-retention
    check.
  - Scratch Run Database:
    `/private/tmp/roundfix-retention-qa-fRf6pX/home/.roundfix/roundfix.db`
    with old terminal, recent terminal, Active, and zero-retention terminal Run
    rows plus an Active Run lock.
- Additional verification commands:
  - `rtk go test ./internal/store/ ./internal/config/ -run 'TestPruneTerminalRuns|TestLoad.*Store|TestLoadRejectsUnknownStore' -count=1` passed with 8 tests in 2 packages.
  - `rtk go test ./internal/cli/ -run TestRunImplementPreflightRetention -count=1` passed with 2 tests in 1 package.
  - `rtk go test ./internal/cli/ -run TestRunImplementPreflightPrunesRetainedRunStorage -count=1` passed with 1 test in 1 package.
  - `rtk go run ./cmd/roundfix skills check` passed.
  - `rtk make skills-sync-check` passed with no drift output.
