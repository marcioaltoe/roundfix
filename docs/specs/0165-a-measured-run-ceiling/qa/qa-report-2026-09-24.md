---
spec: 0165-a-measured-run-ceiling
date: 2026-09-24
build: afd1cc014c7a4f641edc86199744fc811
auditing_binary: "0.16.0 (afd1cc01-dirty, built 2026-09-24 19:53:29 -0300)"
auditor_staleness: "current: build commit afd1cc01 is the audited HEAD; dirty suffix records this QA report"
status: closed
verdict: pass
rows_blocked_environment: 1
rows_blocked_finding: 0
rows_blocked_declared: 0
surfaces: [backend, cli, docs]
---

# QA Report

## Scope and environment

Full terminal QA for `0165-a-measured-run-ceiling` on macOS in the isolated
Run Worktree. The operator surface is the freshly built
`/tmp/roundfix-0165-qa-bin/roundfix`; scratch configuration, repositories, and
Run Database state live under `/tmp/roundfix-0165-qa.7PRNMv`.

The authored gate is `task_03`, it is terminal, and its dependencies
`task_01` and `task_02` are completed. The PRD declares backend and CLI
surfaces and no Unreachable Acceptance or frontend surface.

The precondition command
`/tmp/roundfix-0165-qa-bin/roundfix spec check 0165-a-measured-run-ceiling --strict`
exited `0` with `No findings`. It skipped only detectors whose named optional
artifacts are absent: a findings Rollup, backlog, a Vocabulary Contract, and a
references index.

An isolated `profiles validate --json` attempt selected the default npm-backed
adapter and was denied network access to `registry.npmjs.org` by the sandbox.
The built-binary journeys therefore use the already-installed
`/opt/homebrew/bin/codex-acp` adapter explicitly; no retry of the denied network
path is planned.

## Static gate

- Repository Verification: `make verify-changed` — pass (exit `0`), run by the
  Daemon outside the Agent sandbox. Diagnostics were removed on success. This
  gate records that supervised fact and did not rerun the command.
- Build: `make build BIN_DIR=/tmp/roundfix-0165-qa-bin BIN=/tmp/roundfix-0165-qa-bin/roundfix GOCACHE=/tmp/roundfix-0165-qa-gocache`
  — pass (exit `0`).
- Auditing binary: `/tmp/roundfix-0165-qa-bin/roundfix --version` printed
  `roundfix 0.16.0 (afd1cc01-dirty, built 2026-09-24 19:53:29 -0300)`.

## Performed repairs

None.

## Assigned repair failures

None.

## Authorization audit inputs

None.

## Mechanical findings

None.

## Results

| # | Story / criterion / sweep | Actor and surface | Status | Evidence |
| - | --- | --- | --- | --- |
| Q-01 | Machine-wide ceiling refuses a third Run, names both holders, and creates nothing | Operator; built CLI and Run Database | pass | Built `implement` exited `2`, named both holders, and a reopened database still contained exactly two Active Implement Runs. Provenance: Task 03 Requirement 2. |
| Q-02 | `runs.max_active: 0` disables the ceiling | Operator; built CLI and Run Database | pass | With the same two holders, built `implement` created `run_20260924T230024Z_6934dc031c9d7439`; the reopened database contained three Runs. Provenance: Task 03 Requirement 2. |
| Q-03 | Negative values are refused and the user guide documents default, refusal, and disable behavior | Operator; built CLI and docs | pass | The built CLI exited `2` with the exact validation error, preserved config bytes, and the guide states all four behaviors in its narrative, example, and reference table. Provenance: Task 03 Requirement 3. |
| Q-04 | The budget test finds its Run without the header line | Maintainer; CLI test surface | pass | The exact no-header regression passed; the Daemon-supervised changed-tree gate passed the assembled integration tests. Provenance: Task 03 Requirement 4. |
| Q-05 | This Spec's artifacts satisfy the promise rule | Spec author; strict Spec Check CLI | pass | The freshly built strict Spec Check exited `0` with `No findings`; the graph resolves `task_03` as terminal after completed `task_01` and `task_02`. Provenance: Task 03 Requirement 5. |
| Q-06 | The measured ceiling rests on evidence outside this Spec | Maintainer; Secondbrain measurement record | pass | The named external record documents five concurrent Runs, load near 15, three-of-three budget-test failures on two trees, a ten-minute gate timeout, and the conclusion that delivery above three Runs is not free. Provenance: outside-evidence row. |
| Q-07 | Pull Request controls have equivalent evidence before a Pull Request exists | Maintainer; delivery controls | blocked (environment: no open Pull Request) | Prompt-confirmed absence; all five controls are answered below and bound to audited HEAD `afd1cc01`. Provenance: Pull Request row. |
| Q-08 | Repository Verification passes for the assembled tree | Maintainer; repository gate | pass | Daemon-supervised `make verify-changed` exited `0`; diagnostics were removed on success and the Agent did not rerun it. Provenance: Task 03 Requirement 1; repository Verification. |

### Q-01

```yaml
inputs:
  - kind: repository_path
    ref: internal/cli/implement.go
  - kind: repository_path
    ref: internal/store/store.go
  - kind: repository_path
    ref: internal/config/config.go
```

- Entry point: built `roundfix implement` in a scratch repository.
- Expected: exit `2`, empty stdout, both cross-repository holders named, and
  unchanged Run count.
- Independent confirmation: built `roundfix runs list --all` or a read-only
  Run Database query after the refusal.
- Persistence: reopen the Run Database after the command.
- Observed: the command exited `2` and reported
  `Active Implement Run ceiling reached: 2 Run(s) hold 2 slot(s)`, followed by
  `run_20260924T190100Z_holdertwo` at `/tmp/qa-holder-two` for
  `0002-second-spec` and `run_20260924T190000Z_holderone` at
  `/tmp/qa-holder-one` for `0001-first-spec`.
- Confirmation: built `runs list --all --state all --limit 0` listed only the
  two holders; read-only SQL returned total Run count `2` and Active Implement
  Run count `2`.

### Q-02

```yaml
inputs:
  - kind: repository_path
    ref: internal/cli/implement.go
  - kind: repository_path
    ref: internal/config/config.go
```

- Entry point: built `roundfix implement` with the same holders and
  `runs.max_active: 0`.
- Expected: the command passes the ceiling check and creates a third Run.
- Independent confirmation: reopen/list the Run Database.
- Persistence: the third Run remains observable after command exit.
- Observed: with `max_active: 0`, built `implement` passed the ceiling check,
  created `run_20260924T230024Z_6934dc031c9d7439`, and printed its Run
  Worktree and operator report. The scratch Task later failed because its
  deliberately minimal `true` Verification was correctly classified as
  vacuous; that post-creation fixture result is outside this row's criterion.
- Confirmation: built `runs list --all --state all --limit 0` and read-only SQL
  both showed three persisted Runs, including the new fixture Run.

### Q-03

```yaml
inputs:
  - kind: repository_path
    ref: internal/config/config.go
  - kind: repository_path
    ref: docs/user-guide/configuration.md
```

- Entry point: built CLI with `runs.max_active: -1`, then the configuration
  guide as an operator.
- Expected: negative input exits `2` with its validation message; the guide
  names default `3`, `0` as disabled, negative refusal, and machine-wide scope.
- Independent confirmation: generated/default config and a second guide
  location agree.
- Persistence: config bytes remain unchanged after refusal.
- Observed: built `runs list --all` exited `2` with
  `runs.max_active must be greater than or equal to 0`. SHA-256 before and
  after was
  `1f2326fc6706e5c1cd102f83df6384f63c55e89520a25775ec0bed8e7e0a76f4`.
- Confirmation: `docs/user-guide/configuration.md` names machine-wide scope,
  default `3`, `0` as disabled, and negative-value refusal in the operator
  section, full example, and key table.

### Q-04

```yaml
inputs:
  - kind: repository_path
    ref: internal/cli/implement_test.go
```

- Entry point: focused Go tests for the public Implement Command diagnostics.
- Expected: the no-header case passes and the budget integration test remains
  stable across repeated runs.
- Independent confirmation: the integration case resolves and inspects the
  persisted Run through the Run Database.
- Persistence: repeated executions use new isolated fixtures.
- Observed: `GOCACHE=/tmp/roundfix-0165-qa-gocache go test -count=1 -v -run
  '^TestBudgetExceededRunIsFoundWithoutTheHeaderLine$' ./internal/cli` passed
  and read the Run id from a terminal `BudgetExceeded` diagnostic containing
  no `Implement Run: <id>` header.
- Confirmation: the combined focused command attempted first was denied by the
  sandbox when the integration path's best-effort version freshness check
  reached `api.github.com`. The exact no-header case was then run alone, while
  the Daemon's current `make verify-changed` pass supplies supervised assembled
  integration evidence. No denied network path was retried.

### Q-05

```yaml
inputs:
  - kind: repository_path
    ref: docs/specs/0165-a-measured-run-ceiling/_prd.md
  - kind: repository_path
    ref: docs/specs/0165-a-measured-run-ceiling/_techspec.md
  - kind: repository_path
    ref: docs/specs/0165-a-measured-run-ceiling/_tasks.md
  - kind: repository_path
    ref: docs/specs/0165-a-measured-run-ceiling/task_*.md
```

- Entry point: built `roundfix spec check ... --strict`.
- Expected: no promise-rule or other strict finding.
- Independent confirmation: the Task Graph resolves `task_03` as the terminal
  gate after both completed implementation Tasks.
- Persistence: rerunning from the same audited tree reads committed Spec
  artifacts plus the seeded QA report only.
- Observed: the strict command exited `0` with `No findings` and did not execute
  authored Verification. Its four skips each name an absent optional artifact,
  not a promise-rule detector.
- Confirmation: `_tasks.md` names `qa: task_03` and gives it both implementation
  Tasks as dependencies; both dependency files record `status: completed`.

### Q-06

```yaml
inputs:
  - kind: external_repository
    ref: /Users/marcio/dev/secondbrain/inbox/roundfix/2026-09-24-budget-test-flakes-under-load.md
```

- Entry point: the PRD's named Secondbrain source, not a Spec-authored
  rehearsal.
- Expected: the source records the five-Run/load-15 failure and the three-Run
  capacity conclusion that justify the default ceiling.
- Independent confirmation: compare its exact failure mode with the PRD and
  the changed product surface.
- Persistence: external evidence is re-observed and never carried forward.
- Observed: the 2026-09-24 Secondbrain inbox record states that five Runs plus
  local gates drove load near 15; the budget test failed three of three on
  `main` and the Spec 0157 branch; `internal/cli` crossed the gate's ten-minute
  timeout; and parallel delivery above three concurrent Runs is not free.
- Confirmation: its header-only failure mode matches the pre-change test helper
  and its machine-load consequence matches the new machine-wide ceiling. The
  source does not belong to this Spec and was not authored as its rehearsal.

### Q-07

```yaml
inputs:
  - kind: live_service
    ref: Pull Request state for feat/0165-measured-run-ceiling
```

- Entry point: Pull Request controls named by the QA contract.
- Expected: record the confirmed no-Open-Pull-Request environment cause and
  equivalent evidence for every control.
- Independent confirmation: bind all equivalent evidence to the audited HEAD.
- Persistence: live service state is never carried forward.
- Environment cause: the QA assignment confirms no Open Pull Request for
  `feat/0165-measured-run-ceiling`; the Run Worktree branch never has one.
- Approval: the maintainer-approved `_authorization.md`, committed at
  `59cbeaf7`, grants the bounded change and the `commit`, `push`,
  `pull_request`, and `merge` operations for this Spec.
- Checks and status: Daemon-supervised `make verify-changed` passed, and the
  freshly built strict Spec Check passed on HEAD `afd1cc01`.
- Unresolved review threads: none can stand because no Pull Request exists;
  the assignment reports no Open Pull Request to carry threads.
- Merge-Ready acceptance: none exists yet because the configured pre-Pull-
  Request review and Pull Request stages follow this authored gate.
- Review-artifact ancestry: no review artifact exists yet. Commit `59cbeaf7`
  is an ancestor of audited HEAD `afd1cc01`; the feature and test commits are
  `8ea41e4c` and `afd1cc01`, and the equivalent check evidence is bound to that
  audited head.

### Q-08

```yaml
inputs:
  - kind: repository_path
    ref: '**'
```

- Entry point: Daemon-supervised `make verify-changed`.
- Expected: exit `0` with no diagnostics retained.
- Independent confirmation: the Daemon supplied the exact command, result,
  and diagnostics disposition in the QA assignment.
- Persistence: this row is bound to the audited tree and current Run.
- Observed: the assignment records command `make verify-changed`, verdict
  `pass`, exit `0`, and diagnostics removed on success.
- Confirmation: the Agent did not execute any repository Verification command;
  the recorded result remains the Daemon's static-gate fact.

## Findings

No product findings.

## Blocked and skipped

- Q-07 — `blocked (environment: no open Pull Request)`. Unblock by completing
  the configured pre-Pull-Request review, opening the Pull Request on
  `feat/0165-measured-run-ceiling`, and observing the five controls on its
  current head. The equivalent evidence above answers every control available
  at this pre-Pull-Request gate, so this environment block does not cap the
  verdict.
- Environment note — the default isolated profile probe was denied npm network
  access, so Q-01 and Q-02 used the repository's local deterministic ACP
  preflight fixture with the built CLI. The focused budget integration attempt
  was similarly denied at the unrelated GitHub freshness lookup; Q-04 uses the
  exact no-header regression plus the Daemon-supervised assembled gate.

## Coverage

All eight declared sources are covered: 7 rows passed, 0 failed, 1 is blocked
by the confirmed no-Open-Pull-Request environment, and 0 were skipped.
`rows_blocked_environment: 1`, `rows_blocked_finding: 0`, and
`rows_blocked_declared: 0`. There is no frontend sweep and the PRD declares no
Unreachable Acceptance.

## Final verdict

Pass. Every runnable criterion passed, Q-07 records its environmental cause and
equivalent evidence for all Pull Request controls, and no row is failed,
finding-blocked, declared-blocked, or skipped.

## Mechanical skips

| Detector | Missing artifact |
| --- | --- |
| authorization bounded paths | tooling authorization |
| consequent-fix commit order | consequent-fix declaration |
| QA Report structure | QA Report |
| QA evidence paths | QA Report |
