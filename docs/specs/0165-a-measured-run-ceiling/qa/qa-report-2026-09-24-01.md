---
spec: 0165-a-measured-run-ceiling
date: 2026-09-24
build: 58723cfddf3669e73844b0107766bcb0ec614bcb
auditing_binary: "0.16.0 (58723cfd-dirty, built 2026-09-24 20:53:58 -0300)"
auditor_staleness: "current: build commit 58723cfd is the audited HEAD; dirty suffix records the seeded QA report"
status: closed
verdict: pass
rows_blocked_environment: 3
rows_blocked_finding: 0
rows_blocked_declared: 0
surfaces: [backend, cli, docs]
---

# QA Report

## Scope and environment

Full terminal QA for `0165-a-measured-run-ceiling` on macOS in the isolated
Run Worktree. The operator surface is the freshly built
`/private/tmp/roundfix-0165-rerun-qa-bin/roundfix`; scratch configuration,
repositories, and Run Database state will remain under `/private/tmp`.

The authored gate is `task_03`. It is terminal and depends on every non-QA
leaf: completed `task_04` and `task_05`. Their ancestors `task_01` and
`task_02` are also completed. The PRD declares backend and CLI surfaces and no
Unreachable Acceptance or frontend surface.

The precondition command
`/private/tmp/roundfix-0165-rerun-qa-bin/roundfix spec check 0165-a-measured-run-ceiling --strict`
exited `0` with `No findings`. It skipped only detectors whose named optional
artifacts are absent: a findings Rollup, backlog, Vocabulary Contract, and
references index.

The built-binary Implement journeys reached the required ACP profile proof
through `/opt/homebrew/bin/codex-acp` 1.13.1, but the sandbox denied that
adapter's access to `chatgpt.com`. A focused CLI regression command was also
stopped when its version-freshness path reached denied `api.github.com`
access. Neither denied network path was retried. Q-01 and Q-02 therefore use
the environment-blocked path with the current Daemon-supervised gate, named
Task result evidence, and runnable current-tree config/store regressions as
equivalent evidence.

## Static gate

- Repository Verification: `make verify-changed` — pass (exit `0`), run by the
  Daemon outside the Agent sandbox. Diagnostics were removed on success. This
  gate records that supervised fact and will not rerun the command.
- Build: `make build BIN_DIR=/private/tmp/roundfix-0165-rerun-qa-bin
  BIN=/private/tmp/roundfix-0165-rerun-qa-bin/roundfix
  GOCACHE=/private/tmp/roundfix-0165-rerun-qa-gocache` — pass (exit `0`).
- Auditing binary: `/private/tmp/roundfix-0165-rerun-qa-bin/roundfix --version`
  printed `roundfix 0.16.0 (58723cfd-dirty, built 2026-09-24 20:53:58 -0300)`.
- Focused no-header regression: `GOCACHE=/private/tmp/roundfix-0165-rerun-qa-gocache
  go test -count=1 -v -run '^TestBudgetExceededRunIsFoundWithoutTheHeaderLine$'
  ./internal/cli` — pass (exit `0`).
- Focused corrective canaries: `TestProjectConfigCannotRaiseTheRunCeiling` in
  `./internal/config` and `TestRunCeilingIsCountedInsideTheCreateTransaction`
  in `./internal/store` — pass (exit `0`).

## Performed repairs

None.

## Assigned repair failures

None.

## Authorization audit inputs

None.

## Mechanical findings

None.

## Results

| # | Story / criterion / sweep | Actor and surface | Status | Evidence |
| - | --- | --- | --- | --- |
| Q-01 | Machine-wide ceiling refuses a third Run, names both holders, and creates nothing | Operator; built CLI and Run Database | blocked (environment: ACP profile proof network denied) | The current binary reached ACP proof, where sandbox policy denied `chatgpt.com`; equivalent current-tree evidence is recorded below. Provenance: Task 03 Requirement 2. |
| Q-02 | `runs.max_active: 0` disables the ceiling | Operator; built CLI and Run Database | blocked (environment: ACP profile proof network denied) | The current binary reached the same denied ACP proof before Run creation; equivalent current-tree evidence is recorded below. Provenance: Task 03 Requirement 2. |
| Q-03 | Negative values are refused and the user guide documents the setting | Operator; built CLI and docs | pass | Built CLI exited `2` with the exact validation error, preserved config bytes, and the guide documents scope, default, disable, and refusal behavior. Provenance: Task 03 Requirement 3. |
| Q-04 | The budget test finds its Run without the header line | Maintainer; CLI test surface | pass | The exact no-header regression passed; the Daemon-supervised changed-tree gate passed the assembled tree. Provenance: Task 03 Requirement 4. |
| Q-05 | This Spec's artifacts satisfy the promise rule | Spec author; strict Spec Check CLI | pass | The freshly built strict checker exited `0` with `No findings`; the graph makes `task_03` terminal after every completed non-QA leaf. Provenance: Task 03 Requirement 5. |
| Q-06 | The measured ceiling rests on evidence outside this Spec | Maintainer; Secondbrain measurement record | pass | The named external record documents five concurrent Runs, load near 15, three-of-three failures on two trees, and the three-Run capacity conclusion. Provenance: outside-evidence row. |
| Q-07 | Pull Request controls have equivalent evidence before a Pull Request exists | Maintainer; delivery controls | blocked (environment: no open Pull Request) | Prompt-confirmed absence; all five controls are answered below and bound to audited HEAD `58723cfd`. Provenance: Pull Request row. |
| Q-08 | Repository Verification passes for the assembled tree | Maintainer; repository gate | pass | Daemon-supervised `make verify-changed` exited `0`; diagnostics were removed on success and the Agent did not rerun it. Provenance: Task 03 Requirement 1; repository Verification. |

### Q-01

```yaml
inputs:
  - kind: repository_path
    ref: internal/cli/implement.go
  - kind: repository_path
    ref: internal/store/store.go
  - kind: repository_path
    ref: internal/config/config.go
```

- Entry point: built `roundfix implement` in a scratch repository.
- Steps: seed two Active Implement Runs in different repositories, invoke a
  third `implement`, then reopen and list the machine-wide Run Database.
- Expected: exit `2`, empty stdout, both holders named, and unchanged Run count.
- Independent confirmation: built `roundfix runs list --all` or a read-only
  Run Database query after the refusal.
- Persistence: reopen the Run Database after the command.
- Evidence path: this row's exact command and focused output in this report.
- Environment cause: the built `implement` command reached the mandatory ACP
  profile proof through `/opt/homebrew/bin/codex-acp` 1.13.1, where the
  sandbox denied access to `chatgpt.com`; no Run was created. A later focused
  CLI command was stopped by the same sandbox class at `api.github.com` and
  was not retried.
- Equivalent evidence: the Daemon-supervised `make verify-changed` passed the
  current assembled tree. Task 01's Result names the reproducible
  `TestImplementRefusesAtTheActiveRunCeiling` observation: exit `2`, both
  cross-repository holders named, and the Run Database unchanged. The
  current-tree corrective canaries
  `TestProjectConfigCannotRaiseTheRunCeiling` and
  `TestRunCeilingIsCountedInsideTheCreateTransaction` both passed in this run.
- Unblock by allowing the local ACP adapter's profile-proof network access in
  a full-access QA session, then repeat the built-binary scratch journey.

### Q-02

```yaml
inputs:
  - kind: repository_path
    ref: internal/cli/implement.go
  - kind: repository_path
    ref: internal/store/store.go
  - kind: repository_path
    ref: internal/config/config.go
```

- Entry point: built `roundfix implement` with the same holders and
  `runs.max_active: 0`.
- Steps: disable the ceiling, invoke a third `implement`, then reopen and list
  the Run Database.
- Expected: the command passes the ceiling check and creates a third Run.
- Independent confirmation: reopen/list the Run Database.
- Persistence: the third Run remains observable after command exit.
- Evidence path: this row's exact command and focused output in this report.
- Environment cause: the built `implement` command reached the same mandatory
  ACP profile proof before Run creation and the sandbox denied
  `chatgpt.com`; the denied path was not retried.
- Equivalent evidence: the Daemon-supervised `make verify-changed` passed the
  current assembled tree. Task 01's Result names the reproducible
  `TestImplementRunCeilingZeroDisables` observation: the third Implement Run
  passed the ceiling check and the reopened Run Database contained three Runs.
  The current-tree Project Config scope and transactional-ceiling canaries also
  passed, confirming the corrective commits preserved the User Config
  contract and serialized creation.
- Unblock by allowing the local ACP adapter's profile-proof network access in
  a full-access QA session, then repeat the built-binary disabled-ceiling
  journey.

### Q-03

```yaml
inputs:
  - kind: repository_path
    ref: internal/config/config.go
  - kind: repository_path
    ref: docs/user-guide/configuration.md
```

- Entry point: built CLI with `runs.max_active: -1`, then the configuration
  guide as an operator.
- Steps: hash the config, invoke a read-only command, hash the config again,
  and inspect the operator guide's narrative, example, and key table.
- Expected: negative input exits `2` with its validation message; the guide
  names the setting, User Config scope, machine-wide scope, default `3`, `0` as
  disabled, and negative-value refusal.
- Independent confirmation: generated/default config and a second guide
  location agree.
- Persistence: config bytes remain unchanged after refusal.
- Evidence path: this row's exact command, hashes, and guide locations in this
  report.
- Observed: with isolated User Config `runs.max_active: -1`, the built
  `runs list --all` command exited `2` and printed
  `runs.max_active must be greater than or equal to 0`.
- Confirmation: the config SHA-256 remained
  `1f2326fc6706e5c1cd102f83df6384f63c55e89520a25775ec0bed8e7e0a76f4`
  before and after refusal. `docs/user-guide/configuration.md` names
  machine-wide scope, User Config ownership, default `3`, `0` as disabled,
  negative-value refusal, and ignored Project Config values in the operator
  section, full example, and key table. `internal/config/config.go` independently
  declares default `3`, validates negative values, and excludes Project Config
  from this overlay.

### Q-04

```yaml
inputs:
  - kind: repository_path
    ref: internal/cli/implement_test.go
```

- Entry point: focused Go tests for the public Implement Command diagnostics.
- Steps: run the no-header regression and the deterministic settled-work
  BudgetExceeded case from a fresh test process.
- Expected: the no-header case passes and the assembled budget behavior keeps
  its settled-work proof.
- Independent confirmation: the deterministic budget case inspects persisted
  status and commit effects after BudgetExceeded.
- Persistence: repeated executions use new isolated fixtures.
- Evidence path: focused test command and output in this report.
- Observed: `GOCACHE=/private/tmp/roundfix-0165-rerun-qa-gocache go test
  -count=1 -v -run '^TestBudgetExceededRunIsFoundWithoutTheHeaderLine$'
  ./internal/cli` exited `0` and reported the named test `PASS`.
- Confirmation: the current Daemon-supervised `make verify-changed` pass is
  the assembled static evidence for the restored budget integration behavior.
  Task 05's Result names the deterministic
  `TestBudgetExceededKeepsWorkSettledBeforeTheBudget` observation and the
  restored commit/status assertions. A combined focused attempt reached the
  sandbox's denied `api.github.com` freshness path; that denied path was not
  retried.

### Q-05

```yaml
inputs:
  - kind: repository_path
    ref: docs/specs/0165-a-measured-run-ceiling/_prd.md
  - kind: repository_path
    ref: docs/specs/0165-a-measured-run-ceiling/_techspec.md
  - kind: repository_path
    ref: docs/specs/0165-a-measured-run-ceiling/_tasks.md
  - kind: repository_path
    ref: docs/specs/0165-a-measured-run-ceiling/task_*.md
```

- Entry point: built `roundfix spec check ... --strict`.
- Steps: run the strict checker, then independently inspect graph topology and
  dependency status.
- Expected: no promise-rule or other strict finding.
- Independent confirmation: the Task Graph resolves `task_03` as the terminal
  gate after every completed non-QA leaf.
- Persistence: rerunning from the same audited tree reads the same committed
  Spec artifacts plus this seeded QA report.
- Evidence path: precondition output and graph/status inspection in this report.
- Observed: the freshly built strict command exited `0` with `No findings` and
  did not execute authored Verification. Its four skips each name an absent
  optional artifact, not a promise-rule detector.
- Confirmation: `_tasks.md` names `qa: task_03`; `task_03` depends on the two
  non-QA leaves `task_04` and `task_05`; those files and their ancestors
  `task_01` and `task_02` all record `status: completed`.

### Q-06

```yaml
inputs:
  - kind: external_repository
    ref: /Users/marcio/dev/secondbrain/inbox/roundfix/2026-09-24-budget-test-flakes-under-load.md
```

- Entry point: the PRD's named Secondbrain source, not a Spec-authored
  rehearsal.
- Steps: read the named external record and compare its measurements and
  failure mode with the PRD and changed surface.
- Expected: the source records the five-Run/load-15 failure and the three-Run
  capacity conclusion that justify the default ceiling.
- Independent confirmation: compare its exact failure mode with the PRD and
  changed product surface.
- Persistence: external evidence is re-observed and never carried forward.
- Evidence path: named external file and focused facts in this report.
- Observed: the 2026-09-24 Secondbrain inbox record states that five Runs plus
  local gates drove load near 15; the budget test failed three of three on
  `main` and the Spec 0157 branch; `internal/cli` crossed the gate's ten-minute
  timeout; and parallel delivery above three concurrent Runs is not free.
- Confirmation: its header-only failure mode matches the pre-change test
  helper and its machine-load consequence matches the new machine-wide
  ceiling. The source is outside this repository's Spec artifacts and was not
  authored as this Spec's rehearsal.

### Q-07

```yaml
inputs:
  - kind: live_service
    ref: Pull Request state for feat/0165-measured-run-ceiling
```

- Entry point: Pull Request controls named by the QA contract.
- Steps: use the prompt-confirmed PR fact, inspect approval and current-head
  evidence, and account for every unavailable live control.
- Expected: record the confirmed no-Open-Pull-Request environment cause and
  equivalent evidence for approval, checks and status, unresolved review
  threads, Merge-Ready acceptance, and review-artifact ancestry.
- Independent confirmation: bind all equivalent evidence to the audited HEAD.
- Persistence: live service state is never carried forward.
- Evidence path: QA assignment plus repository ancestry in this report.
- Environment cause: the QA assignment confirms no Open Pull Request for
  `feat/0165-measured-run-ceiling`; the Run Worktree branch never has one.
- Approval: the maintainer-approved `_authorization.md`, committed at
  `59cbeaf7`, grants the bounded change and the `commit`, `push`,
  `pull_request`, and `merge` operations for this Spec.
- Checks and status: Daemon-supervised `make verify-changed` passed, and the
  freshly built strict Spec Check passed on audited HEAD `58723cfd`.
- Unresolved review threads: none can stand because the assignment confirms
  there is no Open Pull Request to carry threads.
- Merge-Ready acceptance: none exists for the current head because the
  configured pre-Pull-Request review and Pull Request stages follow this
  authored gate.
- Review-artifact ancestry: no current-head review artifact exists yet.
  Authorization commit `59cbeaf7` and previous QA head `9044ab3e` are both
  ancestors of audited HEAD `58723cfd`; corrective commits `f3643b89` and
  `58723cfd` supply the current candidate.
- Unblock by completing the configured pre-Pull-Request review, opening the
  Pull Request on `feat/0165-measured-run-ceiling`, and observing all five
  controls on its current head.

### Q-08

```yaml
inputs:
  - kind: repository_path
    ref: '**'
```

- Entry point: Daemon-supervised `make verify-changed`.
- Steps: record the supplied command, exit status, verdict, and diagnostics
  disposition without rerunning it.
- Expected: exit `0` with no diagnostics retained.
- Independent confirmation: the Daemon supplied the exact command, result,
  and diagnostics disposition in the QA assignment.
- Persistence: this row is bound to the audited tree and current Run.
- Evidence path: QA assignment and static-gate section in this report.
- Observed: the assignment records command `make verify-changed`, verdict
  `pass`, exit `0`, and diagnostics removed on success.
- Confirmation: the Agent did not execute any repository Verification command;
  the recorded result remains the Daemon's static-gate fact.

## Findings

No product findings.

## Blocked and skipped

- Q-01 and Q-02 — `blocked (environment: ACP profile proof network denied)`.
  The built current-head binary reached its mandatory local `codex-acp`
  profile proof, where sandbox policy denied `chatgpt.com`. Unblock both rows
  by running their built-binary scratch journeys in a full-access QA session.
  The Daemon-supervised current-tree gate, named Task result evidence, and
  current corrective canaries provide equivalent evidence for this gate.
- Q-07 — `blocked (environment: no open Pull Request)`. Unblock by completing
  the configured pre-Pull-Request review, opening the Pull Request on
  `feat/0165-measured-run-ceiling`, and observing all five controls on its
  current head. The equivalent evidence above accounts for every control
  available at this pre-Pull-Request gate.
- No rows were skipped.

## Coverage

All eight declared sources are covered: 5 rows passed, 0 failed, 3 are blocked
by proved environment causes with equivalent observed or supervised evidence,
and 0 were skipped. `rows_blocked_environment: 3`,
`rows_blocked_finding: 0`, and `rows_blocked_declared: 0`. There is no frontend
sweep and the PRD declares no Unreachable Acceptance.

## Final verdict

Pass. Every runnable criterion passed, each environment-blocked row records its
cause, unblocking action, and equivalent evidence, and no row is failed,
finding-blocked, declared-blocked, or skipped.

## Mechanical skips

| Detector | Missing artifact |
| --- | --- |
| authorization bounded paths | tooling authorization |
| consequent-fix commit order | consequent-fix declaration |
| QA evidence paths | docs/specs/0165-a-measured-run-ceiling/qa/evidence |
