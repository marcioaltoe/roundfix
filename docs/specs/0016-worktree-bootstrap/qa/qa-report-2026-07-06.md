---
spec: 0016-worktree-bootstrap
date: 2026-07-06
verdict: pass
surfaces: [cli, infra, docs]
---

# QA Report - Worktree Bootstrap

## Summary

QA validated the completed Worktree Bootstrap spec against the real
`roundfix implement` command using scratch Git repositories, temp Roundfix
homes, and a fake `acpx` binary on `PATH`. The gate covered Run Worktree
bootstrap, concurrent Task Worktree bootstrap, non-zero failure, timeout
failure, empty-bootstrap compatibility, strict config validation, docs, and
skill drift. Every PRD story and task acceptance criterion passed.

## Results

| # | Criterion / user story | Verdict | Evidence |
| - | ---------------------- | ------- | -------- |
| 1 | Preconditions: every Task in the Spec Task Graph is completed before QA. | pass | `task_01.md`, `task_02.md`, and `task_03.md` all carry `status: completed`; no previous `qa/` report existed before this run. |
| 2 | Static gate: repository verifies before flow QA. | pass | `rtk make verify` exited 0: `Go test: 813 passed in 18 packages`, `Roundfix skill check passed: roundfix`, and `go build -buildvcs=false -o bin/roundfix ./cmd/roundfix` passed. |
| 3 | PRD story 1: a configured bootstrap prepares a TypeScript-style worktree before Verification. | pass | Scratch repo `/private/tmp/roundfix-qa-success.Ka6XIq` ran `bin/roundfix implement` with `worktree.bootstrap` and fake `acpx`; `ROUNDFIX_EXIT=0`, stdout ended `Clean: all 1 Task(s) completed.`, stderr included `bootstrap-output`, `Verification command passed: test -f bootstrap.ready && grep -q SECRET=qa bootstrap.ready`, and `Task task_01 completed.` |
| 4 | PRD story 2: bootstrap is repository configuration, not hardcoded Roundfix behavior. | pass | Scratch `.roundfixrc.yml` values drove all CLI flows. Focused config tests passed: `rtk go test ./internal/worktree/ ./internal/config/ ./internal/cli/` reported `365 passed in 3 packages`, including config precedence and strictness coverage. |
| 5 | PRD story 3: bootstrap failure ends the Run or Task with a clear `worktree bootstrap failed` message naming the command. | pass | Non-zero flow `/private/tmp/roundfix-qa-fail.8eNOYJ` exited `ROUNDFIX_EXIT=1` and stderr contained `worktree bootstrap failed: printf fail-output; exit 7: exit status 7`; timeout flow `/private/tmp/roundfix-qa-timeout.PRUpRu` contained `worktree bootstrap failed: sleep 1: timed out after 50ms`. |
| 6 | PRD story 4: shared-database projects can run sequentially and bootstrap once on the reused Run Worktree. | pass | Sequential flow `/private/tmp/roundfix-qa-success.Ka6XIq` used `worktree.concurrency: 1`; one `bootstrap-output` preceded Agent work and the task Verification passed in the Run Worktree. README documents the `worktree.concurrency: 1` recipe. |
| 7 | PRD story 5: `.env`-style untracked files are copied through `worktree.copy` as part of the recipe. | pass | Success and concurrent scratch repos used `.env` in `worktree.copy`; bootstrap commands required `test -f .env`, wrote `.env` contents to `bootstrap.ready`, and task Verification required `grep -q SECRET=qa bootstrap.ready`. |
| 8 | Core feature 1: `worktree.bootstrap` runs once in each newly created worktree after `worktree.copy`, in the worktree root, before Agent work and Verification; empty skips. | pass | Success flow proved copy-before-bootstrap and bootstrap-before-Verification. Concurrent flow `/private/tmp/roundfix-qa-concurrent.SUeSPS` reported `BOOTSTRAP_OUTPUT_COUNT=3` for one Run Worktree plus two Task Worktrees. Empty flow `/private/tmp/roundfix-qa-empty.6FmPlF` completed Clean with no bootstrap failure/output. |
| 9 | Core feature 2: bootstrap failure handling is distinct for Run Worktrees and Task Worktrees. | pass | Run Worktree failure stopped before Agent prompt (`PROMPT_STARTED=no`) with the bootstrap-failed reason. Task Worktree isolation flow `/private/tmp/roundfix-qa-isolation.yWDKy0` preserved `task_01` Task Worktree with `status: failed`, stderr contained `Task task_01 failed: worktree bootstrap failed: ./bootstrap.sh: exit status 7`, and `task_02` completed. |
| 10 | Core feature 3: bootstrap scope is once per worktree hosting Agent work, including Task Worktrees under concurrency. | pass | Concurrent success flow used `worktree.concurrency: 2`, completed both Tasks Clean, and emitted three bootstrap outputs: Run Worktree plus each Task Worktree. |
| 11 | Core feature 4: bootstrap execution is bounded and output is streamed/journaled like Run diagnostics. | pass | Timeout flow failed with `timed out after 50ms`. Non-zero and success flows showed bootstrap stdout (`fail-output`, `bootstrap-output`) on stderr. Task result evidence names Run Event Journal coverage via `TestRunImplementBootstrapFailureEndsFailedBeforeAgentWork` and `TestTaskCycleCreatesTaskWorktreesWithBootstrapBeforeAgentWork`; focused suites passed. |
| 12 | Core feature 5: docs tie `worktree.copy`, `worktree.bootstrap`, and `worktree.concurrency: 1` into an env-file recipe with gitignore safety. | pass | `rtk proxy grep -R -n "worktree.bootstrap\|bootstrap_timeout\|concurrency: 1\|gitignored\|database provisioning\|dependency installation" README.md .agents/skills/roundfix/SKILL.md skills/roundfix/SKILL.md` found the documented config, timeout, shared-DB recipe, gitignored env-file note, and ownership boundary. |
| 13 | Non-goals: Roundfix does not own database provisioning, dependency strategy, node_modules linking, caching, or a generic hook system. | pass | README and skill state Roundfix only invokes and times the command; dependency installation, database provisioning, migrations, seeding, and cache strategy live in the configured command. Config strictness rejects arbitrary new worktree keys. |
| 14 | Task 01 AC: Run Worktree bootstrap runs after copy and before Agent work. | pass | Success flow required copied `.env` before bootstrap and required `bootstrap.ready` before Verification. Focused tests passed, including `TestCreateRunsBootstrapAfterCopyInRunWorktreeRoot` and `TestRunImplementBootstrapRunsBeforeAgentWorkAndVerification`. |
| 15 | Task 01 AC: non-zero bootstrap exit ends the Run before any Batch, with the required stderr message. | pass | `/private/tmp/roundfix-qa-fail.8eNOYJ`: `ROUNDFIX_EXIT=1`, stderr `worktree bootstrap failed: printf fail-output; exit 7: exit status 7`, and `PROMPT_STARTED=no`. |
| 16 | Task 01 AC: bootstrap timeout fails with a timeout reason. | pass | `/private/tmp/roundfix-qa-timeout.PRUpRu`: `ROUNDFIX_EXIT=1`, stderr `worktree bootstrap failed: sleep 1: timed out after 50ms`, and `PROMPT_STARTED=no`. |
| 17 | Task 01 AC: empty `worktree.bootstrap` leaves behavior unchanged. | pass | `/private/tmp/roundfix-qa-empty.6FmPlF`: `ROUNDFIX_EXIT=0`, stdout `Clean: all 1 Task(s) completed.`, stderr showed normal Verification and Task completion with no bootstrap failure. Full suite passed. |
| 18 | Task 01 AC: config loads both keys with correct precedence and unknown `worktree` keys fail strict validation. | pass | Focused config suite passed. Black-box invalid config flow `/private/tmp/roundfix-qa-invalid-config.9EAdKi` exited `ROUNDFIX_EXIT=2` with `worktree.bootstrapp is not a supported config key`. |
| 19 | Task 02 AC: with bootstrap and concurrency > 1, each Task Worktree runs bootstrap before Agent work. | pass | `/private/tmp/roundfix-qa-concurrent.SUeSPS`: `ROUNDFIX_EXIT=0`, `Concurrency: 2`, both Task Verification commands passed against `bootstrap.ready`, both Tasks completed, and `BOOTSTRAP_OUTPUT_COUNT=3`. |
| 20 | Task 02 AC: a Task Worktree bootstrap failure settles only that Task failed and independent Tasks still run. | pass | `/private/tmp/roundfix-qa-isolation.yWDKy0`: stderr `Task task_01 failed: worktree bootstrap failed: ./bootstrap.sh: exit status 7`; preserved `task_01` Task Worktree status was `failed`; `task_02` Verification passed and completed; fake `acpx` prompt log contained only `task_02`. |
| 21 | Task 02 AC: empty bootstrap leaves concurrent Run behavior unchanged. | pass | Full suite and focused Task Worktree suite passed: `rtk go test ./internal/worktree/ ./internal/daemon/ ./internal/cli/` reported `372 passed in 3 packages`. Existing concurrent scheduler tests remain green; empty-bootstrap CLI control also completed Clean. |
| 22 | Task 02 AC: Task Worktree bootstrap reuses the task_01 primitive. | pass | Static search found one `runBootstrap` implementation in `internal/worktree/worktree.go` and both Run and Task creation paths call it through `BootstrapSpec`; focused worktree and daemon tests passed. |
| 23 | Task 03 AC: docs describe bootstrap, failure behavior, and the combined stateful monorepo recipe. | pass | README grep evidence found `worktree.bootstrap`, `worktree.bootstrap_timeout`, failure message shape, gitignored copy note, and `worktree.concurrency: 1` recipe. |
| 24 | Task 03 AC: Roundfix SKILL.md matches shipped CLI/config surface and embedded copy is regenerated. | pass | `.agents/skills/roundfix/SKILL.md` and `skills/roundfix/SKILL.md` both contain the Worktree Bootstrap config surface and recipe. `rtk make skills-sync-check` exited 0. |
| 25 | Task 03 AC: `roundfix skills check` passes and skill sync reports no drift. | pass | `rtk go run ./cmd/roundfix skills check` reported `Roundfix skill check passed: roundfix`; `rtk make skills-sync-check` exited 0. |

## Failures and blocked items

None.

## Observations

- In the Task Worktree bootstrap isolation flow, stderr and the preserved
  Task Worktree correctly carried `task_01` as failed with the bootstrap-failed
  reason, and independent `task_02` completed. The final deterministic stdout
  rendered `task_01 skipped` because the failed Task Worktree is preserved and
  not integrated into the Run Worktree. This did not block the stated
  acceptance criteria, but it is worth noting for future reporting polish.

## Environment

- Date: 2026-07-06.
- Workspace:
  `/Users/marcio/.roundfix/worktrees/roundfix-339f8dac/run_20260706T191851Z_69abdc11c5c9d5d2`.
- Application under test: `bin/roundfix`, built by `rtk make verify`.
- Scratch repos and Roundfix homes were created under `/private/tmp` with
  repository-local `commit.gpgsign false` to avoid host GPG signing config.
- Fake `acpx` binaries returned version `0.12.0`, handled session ensure/close,
  and emitted valid JSON-RPC prompt results while making only the task-file
  changes needed for each flow.
- No frontend surface exists for this spec, so no browser QA was run.
