---
spec: 0103-a-suite-that-leaks-nothing
date: 2026-08-15
build: 4482c01907014f7bbdbd10c5217c28a7873007fb
status: closed
verdict: partial
rows_blocked_environment: 6
rows_blocked_finding: 0
rows_blocked_declared: 0
surfaces: [backend, infra]
---

# QA Report

## Scope and environment

Full rerun of the authored terminal gate in the Run Worktree on branch
`roundfix/run-run_20260815T070144Z_32e0c01800ca3b92`. The product build under
gate is `4482c01907014f7bbdbd10c5217c28a7873007fb`; current head
`5daeb872bf14cdd0757785a80bf5280b1cfe0ba2` adds only the host equivalent
evidence, a backlog record, and the QA Task rule that consumes that evidence.
The Pull Request fact is `none open`; this Spec declares no Pull Request
journey. Actors are a maintainer running repository gates and an operator using
the Roundfix CLI. No credential is required. The PRD declares no Unreachable
Acceptance row.

`task_09` is the manifest's unique terminal `qa` node, and every dependency is
`completed`. `rtk roundfix spec check 0103-a-suite-that-leaks-nothing --strict`
exited zero with no findings. It skipped only `SC-REF-UNRESOLVED` because the
Spec has no `references/_index.md`; that detector did not run and therefore
does not replace a QA row.

The seeded report and the committed host record are the only QA-run inputs.
Scratch logs and binaries remain outside the Spec. No commit, push, or Pull
Request mutation is in scope.

## Static gate

`rtk make verify` ran the repository's authoritative gate and exited `2` at
`make test`. Every package passed except `internal/cli`, where
`TestRunForceStopLegacyRunWithoutOwnerIdentityStillStopsOwner` and
`TestRunForceStopOwnerProcessIntegrationProvesExitBeforeStoreCompletion`
failed before termination because macOS denied the `kern.proc.all`-backed
owned-tree enumeration with `operation not permitted`. This is the same proved
environment constraint recorded by the prior gate, and it implicates only the
host-integration confirmation in R01 and R08.

The earlier ACPX fixture family did not recur: `internal/agent` passed in 7.092s
inside this loaded gate. The committed host measurement at the product build
under gate, `qa/evidence/2026-08-15-the-authoritative-gate-on-the-host.md`,
records the complete `make verify` command exiting zero on the operator's
10-CPU, 16-GB machine at `4482c01907014f7bbdbd10c5217c28a7873007fb`.

## Mechanical findings

None.

## Results

| # | Story / criterion / sweep | Actor and surface | Status | Evidence |
| - | --- | --- | --- | --- |
| R01 | Stories 1–2: the authoritative suite completes without changing the repository or leaving a test process | Maintainer · infra | blocked (environment: macOS process-table access denied inside Agent Session) | Session gate reached only the two `kern.proc.all` denials; all other packages passed, and the committed same-product-build host run exited zero. See **Static gate** and **R01**. |
| R02 | Story 3 / Core Feature 4: compiled fixtures survive dense concurrent execution with no written executable, empty probe, or `ETXTBSY` | Maintainer · infra | blocked (environment: configured Go build cache denied) | Direct focused invocations were denied before setup; equivalent current-build evidence is the loaded gate's passing `internal/agent` package and the host's complete gate. See **R02**. |
| R03 | Story 3 / Core Feature 3: an already-dead fixture fails in under one second and names its fixture and exit status without changing the live-child budget | Maintainer · infra | pass | Carried from `qa-report-2026-08-14-03.md`; both declared repository inputs are byte-identical through current head. See **R03**. |
| R04 | Story 1 / Core Feature 1: the suite guard attributes created, modified, and removed repository paths while allowing isolated and ignored writes | Maintainer · infra | pass | Carried from `qa-report-2026-08-14-03.md`; the declared guard input is byte-identical. See **R04**. |
| R05 | Story 1: every process-spawning package installs the per-package guard and the repository contract names a missing installation | Maintainer · infra | pass | Carried from `qa-report-2026-08-14-03.md`; both declared contract inputs are byte-identical. See **R05**. |
| R06 | Story 2 / Core Feature 2: detach tests preserve the survival assertion but kill and reap every proven survivor, including a child that ignores the sentinel | Maintainer · infra | blocked (environment: host process-table enumeration denied) | Current CLI suite reached only the two unrelated Force Stop denials; independent `pgrep` was denied. Equivalent teardown evidence proves kill, reap, and PID absence. See **R06**. |
| R07 | Story 4 / Core Feature 5: Doctor reports found, empty, and partial Process Residue without changing exit status or writing a Run record | Operator · backend CLI | blocked (environment: configured Go build cache denied) | Fresh CLI build was denied; current command scenarios ran in the loaded CLI suite and unchanged prior built-CLI output supplies equivalent evidence. See **R07**. |
| R08 | Core Feature 6: Force Stop fails and names a surviving descendant, preserves clean-tree output, and leaves an unrelated process untouched | Operator · backend CLI | blocked (environment: macOS `kern.proc.all` denied) | Public tree directions ran without failure; only two real host integrations were denied before enumeration. Equivalent state and lineage evidence is recorded. See **R08**. |
| R09 | Story 5 / Core Feature 8: the mechanical gate refuses a binary and Gitlink, accepts readable evidence, and skips an absent evidence directory | Maintainer · backend CLI | pass | Current loaded gate passed `internal/speccheck`; the public strict command returned no findings and preserved its named skip. See **R09**. |
| R10 | Core Feature 7: both declared regeneration commands complete under the guard; undeclared and wrong-command writes remain violations | Maintainer · infra | pass | Carried from `qa-report-2026-08-14-03.md`; every declared regeneration and guard input is byte-identical. See **R10**. |
| R11 | Outside evidence: the repair rests on upstream golang/go#22315 and the independent 2026-08-06 measurement of four survivors consuming 2h40m CPU | Maintainer · backend/docs | blocked (environment: GitHub unreachable) | Both pre-Spec findings resolve, but browser, API, search, and `gh` paths returned no upstream issue content. See **R11**. |
| R12 | Non-Goals: no retry, sleep, deadline widening, concurrency change, or survival-property weakening was introduced | Maintainer · infra | pass | Fresh source and commit audit found no shortcut; the stress remains concurrent and the detach assertion still proves survival. See **R12**. |
| R13 | Non-Goals: no unrelated process is killed and garbage collection or retention behavior is unchanged | Operator · backend | pass | The current CLI suite ran the lineage canary without failure; the fresh path audit found no GC or retention change. See **R13**. |
| R14 | Commit-dependent tooling audit: no Task mutated protected repository tooling, and sanctioned derived output remains deterministic fallout only | Maintainer · infra | pass | All eleven implementation/corrective commits were audited with `git diff-tree`; none changed protected tooling. See **R14**. |
| R15 | Vocabulary closure: Process Residue is present in the glossary with the emitted Doctor semantics | Maintainer · docs/backend | pass | Carried from `qa-report-2026-08-14-03.md`; every declared vocabulary input is byte-identical, and strict checking passed fresh. See **R15**. |
| R16 | Report and evidence hygiene: the QA tree contains no committed binary, Gitlink, scratch repository, unresolved repository evidence path, or unowned change | Maintainer · infra | pass | QA entries are UTF-8 Markdown or ordinary mode-100644 files; no Gitlink, nested repository, binary, or unrelated worktree change exists. See **R16**. |

## Mechanical skips

| Detector | Missing artifact |
| --- | --- |
| authorization bounded paths | tooling authorization |
| consequent-fix commit order | consequent-fix declaration |

## Detailed evidence

### R01

```yaml
inputs:
  - kind: repository_path
    ref: Makefile
  - kind: repository_path
    ref: internal/**
  - kind: repository_path
    ref: docs/specs/0103-a-suite-that-leaks-nothing/qa/evidence/2026-08-15-the-authoritative-gate-on-the-host.md
  - kind: live_service
    ref: host process table
```

- Entry point: repository root; run `rtk make verify` as a maintainer.
- Expected: exit zero, unchanged tracked repository state, and no surviving test process.
- Independent confirmation/persistence: fresh Git status, host process read, and the same-build host measurement required by Task 09.
- Evidence path: this row and `qa/evidence/2026-08-15-the-authoritative-gate-on-the-host.md`.
- Observed: the Agent Session's `rtk make verify` exited 2 only because two
  Force Stop integrations could not read the macOS process table. Every other
  package passed, including `internal/agent`, so the previous ACPX saturation
  finding is closed on this build. A fresh Git status after the gate showed no
  tracked product change. The committed host measurement ran the same complete
  gate at product build `4482c01907014f7bbdbd10c5217c28a7873007fb` outside
  the Agent Session and exited zero.
- Equivalent evidence: `qa/evidence/2026-08-15-the-authoritative-gate-on-the-host.md`.
- Unblocking action: grant this Agent Session host process-table access, or run
  the complete gate on the operator host as the committed supervised check did.
- Status: blocked (environment: macOS process-table access denied inside Agent Session).

### R02

```yaml
inputs:
  - kind: repository_path
    ref: internal/agent/**
  - kind: repository_path
    ref: internal/cli/baseline_release_gate_test.go
  - kind: repository_path
    ref: internal/testfixture/**
```

- Entry point: Go test runner at gate parallelism plus existing adapter and formatter journeys.
- Expected: concurrent non-empty fixture probes with no exec error; deterministic refusal of a written executable; unchanged adapter and formatter behavior.
- Independent confirmation/persistence: fresh focused processes at two `GOMAXPROCS` settings.
- Evidence path: this row; scratch logs stay outside the Spec.
- Observed: each direct focused invocation was denied before test setup while
  reading `/Users/marcio/Library/Caches/go-build` (`operation not permitted`).
  The denial was not retried with a relocated cache. Equivalent evidence is
  fresh on this product build: the loaded authoritative gate reported
  `ok roundfix/internal/agent 7.092s`, and the committed host gate exited zero.
  The prior report's focused runs at the same source bytes passed the stress at
  gate parallelism and at `GOMAXPROCS=2` and `GOMAXPROCS=8`; the deterministic
  `os.SameFile` assertion still rejects a written executable.
- Unblocking action: permit the Agent Session to read its configured Go build
  cache, then rerun the focused stress and adapter/formatter journeys.
- Status: blocked (environment: configured Go build cache denied).

### R03

```yaml
inputs:
  - kind: repository_path
    ref: internal/agent/acpx_integration_test.go
  - kind: repository_path
    ref: internal/agent/acpx_runner_test.go
```

- Entry point: dead-fixture regression through the Go test binary.
- Expected: pass in under one second while asserting fixture name and `exit status 23`.
- Independent confirmation/persistence: inspect the 90-second live-child upper bound and absence of a retry.
- Evidence path: this row.
- Carried (established by: `qa-report-2026-08-14-03.md`; head:
  `5daeb872bf14cdd0757785a80bf5280b1cfe0ba2`). The declared inputs
  `internal/agent/acpx_integration_test.go` and
  `internal/agent/acpx_runner_test.go` are byte-identical from the prior report
  build through current head. That report observed the named regression pass in
  0.34s, with diagnostics requiring `immediate-exit acpx fixture` and `exit
  status 23`; the 90-second live-child budget and no-retry path are unchanged.
- Status: pass.

### R04

```yaml
inputs:
  - kind: repository_path
    ref: internal/suiteguard/**
```

- Entry point: suiteguard's real guarded `TestMain` subprocesses.
- Expected: created, modified, and removed paths are named; isolated and ignored writes pass; measured cost is emitted.
- Independent confirmation/persistence: every direction runs in a fresh subprocess with a new fingerprint.
- Evidence path: this row.
- Carried (established by: `qa-report-2026-08-14-03.md`; head:
  `5daeb872bf14cdd0757785a80bf5280b1cfe0ba2`). `git diff --name-status
  7dade8d8..HEAD -- internal/suiteguard` returned no path. The prior execution
  used fresh guarded subprocesses and observed all three change kinds, both
  accepted write directions, and measured fingerprint cost.
- Status: pass.

### R05

```yaml
inputs:
  - kind: repository_path
    ref: internal/**/*_test.go
  - kind: repository_path
    ref: internal/suiteguardcontract/**
```

- Entry point: repository-contract-tagged Go test sweep across `./internal/...`.
- Expected: every test-bearing package runs the named contract and the synthetic unguarded package is named.
- Independent confirmation/persistence: compare emitted package count with a fresh source inventory.
- Evidence path: this row.
- Carried (established by: `qa-report-2026-08-14-03.md`; head:
  `5daeb872bf14cdd0757785a80bf5280b1cfe0ba2`). The declared test sources and
  `internal/suiteguardcontract/**` are byte-identical. The prior execution
  observed the named contract in every test-bearing internal package and the
  synthetic unguarded-package diagnostic.
- Status: pass.

### R06

```yaml
inputs:
  - kind: repository_path
    ref: internal/cli/detach_test.go
  - kind: live_service
    ref: host process table
```

- Entry point: detach and non-cooperative teardown tests through the Go test binary.
- Expected: survival is observed before cleanup; every recorded child is killed, reaped, and absent afterward.
- Independent confirmation/persistence: fresh host process query after the test process exits.
- Evidence path: this row.
- Observed: the current `internal/cli` suite ran under the authoritative gate;
  its only failures were the two R08 host integrations. `rtk pgrep -af
  'roundfix.*test|cli.test|agent.test|store.test'` then exited 3 because the
  sandboxed `sysmond` service was unavailable. Equivalent evidence is the
  current unchanged teardown implementation plus the prior execution's
  subprocess assertions: each child survived its caller, then was killed,
  reaped, and proved absent, including the sentinel-ignoring case.
- Unblocking action: repeat the independent post-test process query in a
  full-access macOS session.
- Status: blocked (environment: host process-table enumeration denied).

### R07

```yaml
inputs:
  - kind: repository_path
    ref: internal/cli/doctor.go
  - kind: repository_path
    ref: internal/cli/doctor_test.go
  - kind: repository_path
    ref: internal/store/process*.go
  - kind: live_service
    ref: host process table and local Run Database
```

- Entry point: built `roundfix doctor` plus command-facing scenario tests.
- Expected: found, empty, and partial wording; unchanged residue exit semantics; no Run mutation.
- Independent confirmation/persistence: rerun the binary and compare Run listings around the read-only scenario.
- Evidence path: this row; any binary stays under `/private/tmp`.
- Observed: `rtk go build -buildvcs=false -o
  /private/tmp/roundfix-0103-qa04 ./cmd/roundfix` exited 1 before compilation
  because the configured Go cache returned `operation not permitted`. The
  denial was not retried with a relocated cache. The installed `roundfix
  doctor` is parity-deviating: it reached the host but lacks this build's
  residue line and exited 1 on unrelated profile and code-signature checks.
  Equivalent evidence is the current loaded CLI suite, whose only failures
  were the two Force Stop host integrations, plus the byte-identical prior
  built-CLI observation and five command-facing found/live/empty/partial/no-write
  scenarios.
- Unblocking action: build and run this product build in a session that may
  read its configured Go cache.
- Status: blocked (environment: configured Go build cache denied).

### R08

```yaml
inputs:
  - kind: repository_path
    ref: internal/cli/cli.go
  - kind: repository_path
    ref: internal/cli/cli_test.go
  - kind: repository_path
    ref: internal/store/process*.go
  - kind: live_service
    ref: host process table
```

- Entry point: Force Stop through the CLI dispatcher and real owner-process controller.
- Expected: surviving descendant keeps the Run Active and is named; clean tree remains byte-compatible; unrelated process remains alive.
- Independent confirmation/persistence: reload Run state and reread the unrelated PID after termination.
- Evidence path: this row.
- Observed: the current authoritative gate failed only
  `TestRunForceStopOwnerProcessIntegrationProvesExitBeforeStoreCompletion` and
  `TestRunForceStopLegacyRunWithoutOwnerIdentityStillStopsOwner`; both stopped
  before termination because owned-tree enumeration returned `operation not
  permitted`. The public surviving-descendant and clean-tree directions ran in
  the same CLI suite without failure. Equivalent observed evidence from the
  unchanged implementation includes fresh Run-state reloads and the real
  unrelated-process canary; the prior report records those exact results.
- Unblocking action: rerun the two named integrations with macOS process-table
  access.
- Status: blocked (environment: macOS `kern.proc.all` denied).

### R09

```yaml
inputs:
  - kind: repository_path
    ref: internal/speccheck/mechanical.go
  - kind: repository_path
    ref: internal/speccheck/mechanical_test.go
  - kind: repository_path
    ref: docs/specs/0103-a-suite-that-leaks-nothing/qa/**
```

- Entry point: four top-level evidence-refusal tests plus public `roundfix spec check --strict`.
- Expected: binary and Gitlink refused by path and kind, JSON accepted, absent evidence directory skipped.
- Independent confirmation/persistence: inspect tracked Git modes and rerun strict checking after closure.
- Evidence path: this row.
- Observed: this session's authoritative gate reported
  `ok roundfix/internal/speccheck 14.951s`, executing all four independent
  evidence-refusal directions on current code. The public
  `rtk roundfix spec check 0103-a-suite-that-leaks-nothing --strict` command
  exited zero with no findings and retained the explicit
  `SC-REF-UNRESOLVED` skip. The report's changing QA-tree input therefore was
  observed rather than carried.
- Status: pass.

### R10

```yaml
inputs:
  - kind: repository_path
    ref: Makefile
  - kind: repository_path
    ref: docs/workflow/authorizations/**
  - kind: repository_path
    ref: internal/baseline/**
  - kind: repository_path
    ref: internal/suiteguard/**
  - kind: repository_path
    ref: internal/suiteguardcontract/**
```

- Entry point: sanctioned regeneration commands and guard subprocess directions.
- Expected: both declared commands exit zero under the guard; wrong and absent declarations still name violations; bytes remain deterministic.
- Independent confirmation/persistence: rerun commands and compare repository bytes after each execution.
- Evidence path: this row; command logs stay outside the Spec.
- Carried (established by: `qa-report-2026-08-14-03.md`; head:
  `5daeb872bf14cdd0757785a80bf5280b1cfe0ba2`). A fresh path diff from the
  prior report build found no change to Makefile, authorization records,
  Baseline, suiteguard, or suiteguardcontract inputs. The prior row ran both
  sanctioned commands, all guard directions, and the deterministic-byte check.
- Status: pass.

### R11

```yaml
inputs:
  - kind: external_repository
    ref: https://github.com/golang/go/issues/22315
  - kind: repository_path
    ref: docs/findings/2026-08-10-a-fake-adapter-goes-silent-under-a-dense-start.md
  - kind: repository_path
    ref: docs/findings/2026-08-06-the-detach-tests-leak-the-process-they-prove-survives.md
```

- Entry point: upstream issue and pre-Spec repository Findings.
- Expected: independent sources support the `ETXTBSY` race and four survivors consuming 2h40m CPU.
- Independent confirmation/persistence: fresh reads resolve outside this Spec and retain the dated details.
- Evidence path: source URL and repository paths named above.
- Observed: fresh reads of the two pre-Spec Findings resolved. The 2026-08-06
  record names PIDs 33864, 55351, 54551, and 87902, ages from 1d22h to 3d14h,
  and 2h40m total CPU. The separate 2026-08-10 record captures the
  write-then-execute `ETXTBSY` signature before this Spec. Fresh browser, search,
  and GitHub API attempts returned no issue content; `rtk gh issue view 22315
  --repo golang/go` exited 1 with `error connecting to api.github.com`.
- Unblocking action: open golang/go#22315 in a network-enabled QA session and
  confirm it documents the concurrent write/fork/exec `ETXTBSY` hazard.
- Equivalent evidence: none for the external-repository half; previous QA
  reports cannot replace a fresh non-carriable external input.
- Status: blocked (environment: GitHub unreachable).

### R12

```yaml
inputs:
  - kind: repository_path
    ref: internal/agent/**
  - kind: repository_path
    ref: internal/cli/detach_test.go
  - kind: repository_path
    ref: .git
```

- Entry point: implementation-commit and source audit correlated with R02, R03, and R06.
- Expected: no retry, sleep, skip, deadline widening, concurrency change, or weakened survival assertion.
- Independent confirmation/persistence: commit-level diffs and current-source scans agree.
- Evidence path: this row.
- Observed: the fresh source audit found the stress derives workers from
  `runtime.GOMAXPROCS(0)`, keeps a two-worker floor and concurrent start barrier,
  and rejects non-hard-linked fixtures through `os.SameFile`. Its block has no
  retry, sleep, or skip. `agentWaitBudget` remains 90 seconds. The detach path
  still fails unless the child survives its caller before teardown, while the
  teardown kills, reaps, and rechecks the PID.
- Status: pass.

### R13

```yaml
inputs:
  - kind: repository_path
    ref: internal/cli/**
  - kind: repository_path
    ref: internal/store/**
  - kind: repository_path
    ref: .git
  - kind: live_service
    ref: unrelated-process canary
```

- Entry point: real lineage canary and implementation changed-path audit.
- Expected: unrelated PID survives; garbage-collection and retention behavior remain unchanged.
- Independent confirmation/persistence: second process read and commit-level path inspection.
- Evidence path: this row.
- Observed: the current loaded CLI suite's only failures were the two named
  owned-tree enumeration integrations; the real
  `TestOwnerProcessControllerTerminateTreeLeavesUnrelatedProcessRunning`
  canary ran without failure. A fresh base-to-head name-status audit found no
  garbage-collection, retention, journal, or prune implementation path.
- Status: pass.

### R14

```yaml
inputs:
  - kind: repository_path
    ref: .git
  - kind: repository_path
    ref: docs/specs/0103-a-suite-that-leaks-nothing/task_*.md
```

- Entry point: `git diff-tree` for every implementation and corrective commit plus current delta.
- Expected: no protected tooling path or version pin changed; no tooling authorization row is applicable.
- Independent confirmation/persistence: base-to-head name-status and per-commit path lists agree.
- Evidence path: this row.
- Observed: `git diff-tree --no-commit-id --name-only -r` ran for
  `3925cbce`, `96423c2b`, `298acc85`, `ddb7a6bd`, `646bbd98`, `a9a4ca00`,
  `bb9cb40d`, `8f074d05`, `de3a80ad`, `db5c57fa`, and `7dade8d8`. The paths are
  Go source/tests and their assigned Task files. None is a linter, formatter,
  test-runner/build configuration, script, ignore file, plugin declaration,
  dependency manifest, or version pin. Current tracked delta is empty; only
  this seeded report is untracked.
- Status: pass.

### R15

```yaml
inputs:
  - kind: repository_path
    ref: CONTEXT.md
  - kind: repository_path
    ref: internal/cli/doctor.go
  - kind: repository_path
    ref: docs/specs/0103-a-suite-that-leaks-nothing/_techspec.md
```

- Entry point: glossary read and built Doctor output.
- Expected: Process Residue is defined with the same reporting-only semantics the CLI emits.
- Independent confirmation/persistence: strict checker and fresh CLI output agree.
- Evidence path: this row.
- Carried (established by: `qa-report-2026-08-14-03.md`; head:
  `5daeb872bf14cdd0757785a80bf5280b1cfe0ba2`). `CONTEXT.md`, Doctor source,
  and the TechSpec are byte-identical; the fresh strict check returned no
  vocabulary finding. Process Residue remains the reporting-only term emitted
  by Doctor.
- Status: pass.

### R16

```yaml
inputs:
  - kind: repository_path
    ref: docs/specs/0103-a-suite-that-leaks-nothing/qa/**
  - kind: repository_path
    ref: .git
```

- Entry point: QA-tree Git-mode and file-type audit after every other row settles.
- Expected: no binary, Gitlink, scratch repository, missing repository evidence reference, or unowned change.
- Independent confirmation/persistence: public strict check rerun after the report closes.
- Evidence path: this report.
- Observed: the QA tree contains five reports and one committed host-evidence
  Markdown file. `git ls-files -s` reports mode `100644` for every committed QA
  entry and no mode `160000` entry. `file` identifies the new report and host
  evidence as UTF-8 text; a fresh `.git` search found no nested repository.
  `git status --short` names only this seeded report as untracked. Every
  repository evidence path used by a terminal row resolves; R11 separately
  records its unreachable external source. The public strict checker returned
  no findings.
- Status: pass.

## Findings

None. The previous report's ACPX saturation finding did not recur:
`internal/agent` passed inside the current loaded authoritative gate, and the
committed host gate passed completely at the product build under test.

## Blocked and skipped

- R01 is environment-blocked because the Agent Session cannot enumerate the
  macOS process table. The same-product-build host `make verify` pass is
  equivalent supervised evidence.
- R02 is environment-blocked because direct focused Go commands cannot read the
  configured build cache. The current loaded `internal/agent` pass and complete
  host gate are equivalent evidence.
- R06 is environment-blocked because `pgrep` cannot reach sandboxed `sysmond`.
  The kill, reap, and PID-absence assertions are equivalent observed evidence.
- R07 is environment-blocked because a fresh CLI build cannot read the
  configured Go cache. Current command scenarios and unchanged prior built-CLI
  output are equivalent evidence.
- R08 is environment-blocked because macOS denied `kern.proc.all` in two host
  integrations. The public state/output directions and unrelated-process canary
  are equivalent evidence.
- R11 is environment-blocked because GitHub returned no issue content and
  `gh` could not connect. Its local pre-Spec process measurement resolves, but
  the external-repository half has no equivalent current evidence.
- No QA row is finding-blocked, declared-blocked, or skipped. The two mechanical
  detector skips above remain preserved exactly from the seeded report.

## Coverage

All 16 planned rows are terminal: 10 pass and 6 are environment-blocked; none
fail, are finding-blocked, declared-blocked, skipped, or pending. Every PRD user
story and all eight core features have an executed row. All five Non-Goals pass
their scope checks. Story 5 and evidence hygiene pass on the current changing QA
tree rather than by carry-forward. The outside-evidence row freshly resolves
the independent 2026-08-06 process measurement but lacks current access to the
upstream issue.

High-risk probes covered repository-wide versus focused load, two machine
parallelism settings through equivalent evidence, immediate fixture death,
created/modified/removed guard directions, unguarded-package detection,
non-cooperative teardown, unreadable process tables, found/empty/partial
residue states, surviving and clean Force Stop trees, an unrelated-process
canary, wrong and absent regeneration declarations, and binary/Gitlink evidence
refusal. There is no frontend or TUI surface in this Spec.

The glossary check found Process Residue already defined with Doctor's
reporting-only semantics, so no domain term was introduced, changed, or retired
by this QA rerun. Counts: `rows_blocked_environment: 6`,
`rows_blocked_finding: 0`, and `rows_blocked_declared: 0`. Finding impacts are
zero at every tier.

## Final verdict

**Partial.** Re-observe golang/go#22315 in a network-enabled session before
claiming a passing gate; the five other environment-blocked rows already carry
equivalent observed or supervised evidence, and no product finding remains.
