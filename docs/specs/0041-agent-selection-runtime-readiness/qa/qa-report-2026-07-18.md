---
spec: 0041-agent-selection-runtime-readiness
date: 2026-07-18
build: f8bcc130b1b48a321a82f15ff577fe259b1a0172
status: closed
verdict: pass
surfaces: [cli, backend, docs]
---

# QA report — Agent Selection Runtime Readiness

## Scope and environment

Full Spec gate against commit `f8bcc130b1b48a321a82f15ff577fe259b1a0172` in the daemon-assigned Run Worktree on macOS, using the repository Go toolchain and isolated temporary configuration roots. Actors are a developer running Setup or Doctor and an operator starting a Run or managing Agent Selection Profiles. Public CLI flows use `go run -buildvcs=false ./cmd/roundfix` or the binary produced by the full verification gate. Backend-only capability, cleanup, and mutation-order criteria are credited only when the full static gate passes and the named task regressions remain reproducible.

The top-level `--help` command was used for surface discovery before this matrix was written; it is not counted as validation evidence. No network-dependent or credential-bearing Run will be started. The gate uses deterministic local fixtures for exact ACP capability behavior and temporary directories for mutation checks.

Command evidence: [2026-07-18 full gate](evidence/2026-07-18-full-gate/command-evidence.md).

## Static gate

`rtk make verify` passed on 2026-07-18 against the recorded source commit. It ran 1,680 Go tests in 20 packages, 79 setup-context-driven tests, both setup-context asset-catalog checks, all 14 shipped Roundfix Skill contracts, and the CLI build. The built artifact reports `f8bcc13-dirty` because the then-in-progress QA Report was an expected uncommitted daemon-owned artifact.

## Planned matrix

| # | Story / criterion / sweep | Actor and entry point | Steps and expected observable | Independent confirmation / persistence | Evidence | Status |
| - | --- | --- | --- | --- | --- | --- |
| 1 | US1, US2, US3, US4, US9; adapter identity and exact capability proof | Developer; `roundfix setup` and focused public-boundary regressions | Exercise official, legacy, unknown, unsupported, and missing adapter cases; expect bounded lineage/version evidence, exact Sol/high handling, and one recovery action | Re-read all target config bytes and proof/session logs; expect no write or prompt on rejection and closed disposable Sessions | 18 adapter tests, 18 capability tests, 23 exact-proof tests, 8 cleanup/no-prompt tests, full static gate | pass |
| 2 | US1, US8; Setup generated profiles and transaction boundary | Developer; `roundfix setup` with isolated config roots | Exercise generated Sol/high plus GPT-5.5/xhigh profiles, migration decline, `--no-input`, and proof failure | Run profile validation against persisted success; compare ACPX, User Config, and Project Config bytes for every rejected path | Fresh 3 config and 16 Setup transaction tests; scratch generated config and fresh `profiles show --json` | pass |
| 3 | US2, US3, US8; profile configuration proof before persistence | User; `roundfix profiles configure` | Exercise file, dry-run, yes/JSON, missing fallback, duplicate fallback, proof failure, decline, and success | Compare target bytes after every non-persisted path; validate the successful profile through a fresh public command | Built CLI duplicate-fallback probe with unchanged SHA-256; 14 CLI and 3 config tests | pass |
| 4 | US3, US4, US7; shared `profiles validate` readiness | Operator; `roundfix profiles validate` text and JSON | Exercise tuple deduplication and a classified capability failure | Repeat the command to prove a fresh disposable proof pass; compare text and JSON tuple/category/action evidence | 5 shared readiness tests and 2 operational/fallback tests; full gate | pass |
| 5 | US7, US9; profile-aware Doctor | Developer; `roundfix doctor` | Exercise successful aggregate readiness and legacy/profile failure; expect all required categories, bounded adapter evidence, and continued independent checks | Compare the same failing config through `profiles validate`; assert Doctor remains read-only and text-only | 7 Doctor profile-readiness tests, 1 continuation test, 6 Setup/Doctor adapter tests | pass |
| 6 | US5; profile-led invocation | Operator; `resolve`, `watch`, and `implement` with no Agent Selection flags | Inspect help and run deterministic command-boundary regressions; expect Task, QA, and review category profiles to remain authoritative | Confirm configured fallback order is unchanged and no legacy default is substituted | Built CLI help; 14 invocation-profile tests; 12 command-boundary tests; 12 help/fetch tests | pass |
| 7 | US6; complete one-Run override grammar | Operator; `resolve`, `watch`, and `implement` | Probe all one-flag and two-flag subsets plus a complete tuple and explicit empty reasoning; partial input must exit 2 with one grammar explanation | Confirm no Run, Session, worktree, artifact, config load mutation, or detached child for rejected subsets; fetch remains unchanged | Built CLI exit-2 probes across three commands and detach; scratch tree re-read; 38 focused tests | pass |
| 8 | CF2, CF3, CF10; exact advertised-state application | Operator; deterministic ACP public-boundary fixtures | Exercise independent controls, model variants, model-managed intent, rejection, mismatch, malformed evidence, cancellation, and timeout | Compare disposable and live operation order; verify every disposable Session closes and no prompt/token activity occurs | 18 capability, 23 exact-proof, 8 cleanup/cancel/timeout/no-prompt, and private-state guard tests | pass |
| 9 | CF4; one readiness coordinator and operational preflight ordering | Operator; `profiles validate` and Agent-starting preflight regressions | Exercise shared tuple references, first failure, repeated invocation, and operational rejection | Confirm identical classification/category evidence and absence of Run-side mutation before readiness | 5 shared coordinator tests, 2 preflight/fallback tests, and mutation guards in full gate | pass |
| 10 | CF5, CF6 and success metrics; generated defaults and official identifiers | User; `profiles show`, generated YAML, and catalog regressions | Expect Sol/high and GPT-5.5/xhigh defaults, no Terra/max generated default, and valid Sol/Terra/Luna identifiers | Compare public output with configuration and usage documentation | Scratch Project Config from `roundfix init`; fresh `profiles show --json`; focused config tests in static gate | pass |
| 11 | Docs surface and vocabulary | Intended reader; command help, README, user guides, Roundfix Skill, release runbook | Follow documented profile-led and complete-override forms; expect official adapter recovery, advisory-vs-proven distinction, and fail-before-mutation wording | Run documentation, link, shipped-skill, and canonical/embedded synchronization checks | Built command help; 4 docs-contract tests; 13 skill tests; sync/check targets; four durable links resolved | pass |
| 12 | Non-Goals and scope-creep sweep | Maintainer; source-backed boundary regressions and repository search | Check no private cache authority, automatic ranked selection, explicit-effort downgrade, fallback removal, post-prompt switching, benchmark reranking, or Spec 0036 implementation entered this Spec | Confirm production capability acquisition is public-boundary-only and externally managed skills have no authorial changes | Private-state guard; recommendation/config/fallback regressions; `584df1f..HEAD` changed-file and focused diffs | pass |
| 13 | Mandatory CLI/backend/docs surface sweep | Operator and reader; built CLI plus full verification | Check deterministic help/errors, stdout/stderr and exit contracts, read-only diagnostics, docs commands, race coverage, build, and whitespace | Re-run key reads after commands and resolve every cited evidence path | Full static gate, built CLI probes, seven race groups, docs/skill checks, final artifact inspection | pass |

High-risk behavior probes are invalid or out-of-order partial override input (rows 6-7), repeated validation to reject proof caching (row 4), interruption/cancellation and malformed capability evidence (row 8), and decline/output-failure rollback with byte comparison (rows 2-3). These target mutation ordering and exact-intent risks named by the PRD; browser, viewport, locale, and pointer-input probes do not apply to the declared CLI/backend/docs surfaces.

## Results

| # | Story / criterion / sweep | Actor and surface | Status | Evidence |
| - | --- | --- | --- | --- |
| 1 | Adapter identity and exact capability proof | Developer; CLI/backend | pass | Fresh focused groups passed: adapter agent 12, Setup/Doctor adapter 6, capability evidence 18, exact assignment/proof 23, and cleanup/cancellation/timeout/no-prompt 8. They reproduce official, legacy, unknown, unsupported, missing, independent-control, model-variant, rejection, mismatch, bounded evidence, cleanup, and zero-prompt behavior on the current build. |
| 2 | Setup generated profiles and transaction boundary | Developer; CLI/backend | pass | Fresh Setup groups passed 3 config and 16 CLI tests. They cover three distinct tuple proofs before persistence, official pinned adapter migration, decline/no-input/yes/proof/cleanup/write failure byte preservation, and persisted-profile revalidation. The scratch `init` plus fresh `profiles show` independently confirmed the generated proposal shape. |
| 3 | Profile configuration proof before persistence | User; CLI/backend | pass | Built CLI duplicate-fallback reproduction exited 2 before proof with JSON `changed:false`; `.roundfixrc.yml` SHA-256 remained `ec654c32b3fac6316b7b40ec431f1a4adefc3f79cec6f401afeffe9c72776696`. Fresh 14 CLI and 3 config tests cover file, interactive, dry-run, yes, JSON, decline, proof/cleanup/output failure, rollback, and recommendation non-insertion. |
| 4 | Shared profile validation readiness | Operator; CLI/backend | pass | Fresh shared readiness tests passed (5) with deduplication, stable references, aligned text/JSON classified failures, and fresh per-invocation proof; operational/fallback tests passed (2) with pre-mutation ordering and notification-first fallback preservation. |
| 5 | Profile-aware Doctor | Developer; CLI/backend | pass | Fresh Doctor profile-readiness tests passed (7), continuation passed (1), and shared adapter diagnostics passed (6). The tests compare Doctor and `profiles validate` on one config, assert five required categories and tuple/reference counts, continuation after failure, exit 1, text-only output, and no Run/Session/config/worktree/artifact mutation. |
| 6 | Profile-led invocation | Operator; CLI | pass | Built `resolve --help`, `watch --help`, and `implement --help` each present profile-led invocation and the complete tuple form; `fetch --help` exposes no selection flags. Fresh `TestInvocationProfileOverride` (14 tests), command selection-override (12), and help/fetch (12) groups passed and confirm Task, QA, and review category selection plus fallback preservation. |
| 7 | Complete one-Run override grammar | Operator; CLI | pass | Built CLI probes for bare agent/model/effort and two-field subsets across resolve, watch, implement, and detached implement all exited 2 with the identical grammar and no-side-effect message. The scratch repository still contained only its expected Project Config outside `.git`. Focused tests independently cover every subset, complete Sol/high, explicit empty reasoning, no config-load mutation, no detached child, and unchanged fetch behavior. |
| 8 | Exact advertised-state application | Operator; backend | pass | Fresh public ACP-boundary fixture tests passed: capability projection 18, private-state guard 1, assignment/proof 23, and cleanup/cancellation/timeout/no-prompt 8. Disposable and live paths share ordered operations; malformed, rejected, mismatched, cancelled, and timed-out paths close their disposable Session without a prompt. |
| 9 | Shared coordinator and preflight ordering | Operator; CLI/backend | pass | Fresh coordinator and operational-preflight groups passed. They prove exact tuple deduplication, stable category/fallback references, first-failure parity, no cross-command cache, preserved fallback order, and no Run-side mutation before readiness. |
| 10 | Generated defaults and official identifiers | User; CLI/docs | pass | In `/tmp/roundfix-qa-0041.ngHidk`, `roundfix init --scope project` created a Project Config whose required Codex profiles use Sol/high with GPT-5.5/xhigh fallback and whose frontend policy remains Claude Fable/medium to Sol/high. A fresh `profiles show --json` confirmed project source, inheritance, unchanged fallbacks, and advisory Terra/Luna entries without promoting them to operational defaults. The full static gate covers `TestBuiltinProfiles`, `TestDefaultConfigYAML`, and `TestModelCatalog`. |
| 11 | Documentation and vocabulary | Intended reader; docs/CLI | pass | Built help for Setup, Doctor, configure, validate, resolve, watch, implement, and fetch matched the profile-led, exact-proof, read-only, and all-or-none contracts. Documentation-contract tests passed (4), skill tests passed (13), `make skills-sync-check` and the shipped `skills check` passed, and the ADR-0055, validation record, dogfood finding, and Spec 0036 links resolve. |
| 12 | Non-Goals and scope-creep sweep | Maintainer; backend/docs | pass | The private-state guard passed with inaccessible ACPX/Codex private paths. Assignment and configuration tests reject explicit-effort downgrade, automatic recommendation insertion, missing fallbacks, and post-prompt fallback. The `584df1f..HEAD` changed-file review shows no Spec 0036 implementation files, no external project/runtime files, and no authorial edits outside the repo-owned Roundfix and setup-context skills. The profile diff changes only the generated fallback and validation remediation; advisory ranking data remains advisory and unmodified. |
| 13 | Mandatory surface sweep | Operator and reader; CLI/backend/docs | pass | `make verify` passed; built CLI help and exit-2 probes passed; seven focused race groups passed; Setup/Doctor/configuration mutation guards passed; docs, links, canonical/embedded skill sync, build, and command-evidence paths were independently re-read. |

## Findings

None. No Blocks-Completion, Data-Loss, Trust-Damage, Friction, or Cosmetic finding was observed.

## Blocked and skipped

None. All planned rows reached a terminal `pass` status; no external prerequisite or risk-based cut reduced coverage.

## Coverage

Passed 13 of 13 matrix rows, covering 9 of 9 user stories, 10 of 10 core features, all 10 success-metric groups, 9 of 9 Non-Goals, and the CLI/backend/docs surface sweep. The gate exercised invalid and out-of-order partial overrides, repeated validation, cancellation/timeout/malformed capability evidence, decline and output-failure rollback, and byte-identical no-mutation behavior. Browser, viewport, pointer-input, and locale sweeps were not applicable to the declared surfaces.

## Final verdict

Pass. Every PRD promise and explicit exclusion passed on build `f8bcc130b1b48a321a82f15ff577fe259b1a0172`: 13 passed, 0 failed, 0 blocked, 0 skipped, with zero findings in every impact tier.
