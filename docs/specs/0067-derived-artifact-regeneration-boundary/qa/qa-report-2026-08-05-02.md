---
spec: 0067-derived-artifact-regeneration-boundary
date: 2026-08-05
build: c6cf8033b7fe75d6f97404735e798cd42427ba89
status: closed
verdict: fail
rows_blocked_environment: 0
rows_blocked_finding: 0
rows_blocked_declared: 0
surfaces: [backend, infra, docs]
---

# QA report — Derived artifact regeneration boundary

## Scope and environment

Full rerun of the authored terminal gate after corrective `task_06`, on macOS
in the isolated Run Worktree. Actors are a Roundfix maintainer changing an
owned Skill, a contributor adding a derived artifact, and an operator
responding to a strict-load failure. Public entry points are
`make baseline-digests`, `make verify`, the documented `go test` regeneration
commands, and ownership records stored beside derived artifacts.

The PRD declares backend, infra, and docs surfaces and no frontend, data
migration, live service, credential, deployment, release, or Pull Request
journey. The prompt proves that no Pull Request is open and that Pull Request
journeys are environment-blocked; no PRD promise depends on one, so the fact is
recorded here rather than expanded into an unrelated product row. The PRD has
no `## Unreachable Acceptance` declarations.

High-risk probes are the previously failing one-invocation owned-Skill
recovery, a second idempotency run, unclassified and multiply classified
artifacts, invalid or ineffective dedicated commands, a frozen declaration
exposed to sanctioned regeneration, and independently measured record truth.

Evidence directory:
`qa/evidence/2026-08-05-spec-0067-rerun-02/`.

A supplemental direct Go invocation in the scratch clone resolved the host Go
cache outside the sandbox and was denied before product behavior. It was not
retried. The same remediation contract ran through the worktree-local focused
suite, providing equivalent observed evidence; no matrix row remained blocked.

## Static gate

`rtk make verify` ran unpiped from the Run Worktree root and exited 0. It
reported 3,349 passing Go tests across 26 packages, one passing isolated corpus
budget test, four passing Skill tests, a passing Repository Skill Set check, a
successful build, and no Spec Consistency Check finding for Spec 0067. The two
Spec 0067 skips concern an absent Vocabulary Contract and
`references/_index.md`; neither belongs to this Spec's promised behavior.

Governance and static evidence:
[`static-and-governance.md`](evidence/2026-08-05-spec-0067-rerun-02/static-and-governance.md).

## Results

| # | Story / criterion / sweep | Actor and surface | Steps, observable, confirmation, persistence, and evidence | Status |
| - | --- | --- | --- | --- |
| Q-01 | Authored gate and dependency preconditions | QA Agent · docs | `_tasks.md` names `task_05` as the sole terminal `qa` node. Its completed dependency `task_06` transitively closes every non-QA leaf; `task_05` remains Daemon-owned pending during the gate. | pass |
| Q-02 | Project Constraint — identifier strategy | Maintainer · docs | The PRD and TechSpec record non-applicability with `docs/agents/domain.md`. Range inspection found ownership metadata and no new or changed Internal Identifier, digest-field identity, or diagnostic-code identity. | pass |
| Q-03 | Project Constraint — authentication and HTTP | Maintainer · docs | The PRD and TechSpec record local-only non-applicability with `docs/agents/agent-instructions.md`. Range inspection and real flows found no auth, HTTP, secret, transport, or deployment surface. | pass |
| Q-04 | Project Constraint — active ADRs | Maintainer · docs/backend | ADR-0080/0085/0091 behavior held and ADR-0093 is correctly non-applicable. ADR-0081 explicitly says an owned-Skill edit regenerates parity-corpus pins, while the PRD and TechSpec require that corpus to be frozen and regenerated by nothing. The active-ADR row does not resolve that contract conflict. See F-002. | fail |
| Q-05 | Tooling authority, scope, and commit choreography | Maintainer · infra/docs | Authorization commit `2e560cea` predates Task 03 commit `c7ad3f62`, whose `git diff-tree` paths are exactly `Makefile` and `task_03.md`. Corrective `c6cf8033` is a separate descendant after the failed gate and changes the authorized `Makefile`, assigned Task 06 file, ownership test, and three records named by Task 06. No other tooling path, folded prerequisite repair, hand-edited pin, or misordered consequent repair was found. | pass |
| Q-06 | Repository Verification | Maintainer · backend/infra/docs | Unpiped `rtk make verify` exited 0 on `c6cf8033`; see Static gate and the governance evidence file. | pass |
| Q-07 | Every derived path resolves to exactly one stated owner | Contributor · backend/docs | The fresh ownership/measurement command selected the real scan and exited 0 as part of 26 passing focused tests; a fresh read after all probes still passed. | pass |
| Q-08 | Unclassified and multiply classified artifacts fail | Contributor · backend | The selected zero-record and multiple-record fixtures passed only after observing their named rejection, while the unmodified real-tree scan also passed. | pass |
| Q-09 | Record schema and dedicated-step claims are enforceable | Contributor · backend/docs | Invalid owner, missing reason/command, nonexistent command, wrong flag, and successful no-op probes all observed their required rejection; the isolated fixture restored its derived snapshot. | pass |
| Q-10 | Frozen parity corpus is marked frozen and regenerated by nothing | Maintainer · docs/infra | The real record now says `owner: sanctioned`; after a scratch owned-Skill edit, the public sanctioned command rewrote `fixtures/asset-sync.json` and `manifest.json`. That evidence is internally truthful but contradicts the PRD/TechSpec frozen acceptance. See F-002 and [`regeneration-and-boundaries.md`](evidence/2026-08-05-spec-0067-rerun-02/regeneration-and-boundaries.md). | fail |
| Q-11 | Every declared regeneration step really rewrites its artifacts | Maintainer · backend/infra | The declared-step suite ran in an isolated repository as part of the 26-test focused command. Synthetic dedicated repair, ineffective commands, sanctioned probes, frozen-rewrite rejection, unrelated-byte preservation, and snapshot restoration passed. | pass |
| Q-12 | One sanctioned command repairs an ordinary owned-Skill edit | Maintainer · infra | In a fresh clone, QA edited the owned QA Skill, ran `make skills-sync`, one `make baseline-digests` (`changed:true`), then unpiped `make verify`. Verification exited 0 with 3,349 Go tests and all remaining gates green. | pass |
| Q-13 | A second sanctioned run is a no-op and stays green | Maintainer · infra | The second scratch `make baseline-digests` reported `no changes` and `changed:false`; a second unpiped `make verify` exited 0 with the same gate counts. | pass |
| Q-14 | Ownership records match measured sanctioned behavior | Maintainer · backend/docs | `TestMeasuredSanctionedOwnershipMatchesRecords` ran in the focused command and passed; the scratch status independently confirmed the parity, plan-characterization, and catalog-diagnostic rewrites named by their `sanctioned` records. | pass |
| Q-15 | Failure remediation names only an effective human action | Operator · backend/docs | `rtk go test ./internal/baseline -count=1 -run '^TestDerivedOwnershipRemediation' -v` exited 0 with six tests. It covers exact sanctioned text plus dedicated/frozen record paths, effective actions/reasons, and rejection of guessed unowned remediation. A supplemental scratch invocation was sandbox-denied before behavior; equivalent worktree evidence is recorded. | pass |
| Q-16 | No derived artifact content or digest moved in this Spec | Maintainer · infra | The implementation-range diff over both derived roots, excluding ownership metadata, exited 0 with no diff. Only ten ownership records were added under those roots, and post-flow Run Worktree status contains only this QA report/evidence. | pass |
| Q-17 | Non-Goals remain excluded | Maintainer · backend/infra | Digest content and the ADR-0085 strict-load boundary stayed unchanged. The frozen Non-Goal did not: the delivered sanctioned command and record make the parity corpus regenerable and actually rewrite it. See F-002. | fail |
| Q-18 | Documentation is truthful and copyable without hidden knowledge | Maintainer · docs | The three corrected records now match measured behavior and the command list, but the active PRD and TechSpec still instruct readers that parity is frozen while Task 06 and the at-path record say the opposite and leave the decision to a maintainer. The assembled docs contract is contradictory. See F-002. | fail |

## Findings

### F-002 — The corrected record still contradicts the Spec's frozen boundary

- History: carried forward from prior F-002. The earlier false record is fixed;
  the same measured parity rewrite now exposes the unresolved PRD/TechSpec
  contradiction.
- Impact: `Trust-Damage`.
- Actor and step: Roundfix maintainer reading the at-path owner and then
  performing Q-12's ordinary owned-Skill regeneration journey.
- Expected: PRD Core Feature 3, Success Metric 3, Non-Goal, and Decision require
  parity to be marked `frozen`, regenerated by nothing, and kept frozen.
- Actual: the record says `owner: sanctioned`, and the sanctioned command
  rewrote `parity-corpus/v1/fixtures/asset-sync.json` and `manifest.json`.
  Corrective Task 06 explicitly leaves whether parity should be frozen as a
  later maintainer decision.
- Reproduction: read `_prd.md:73`, `_prd.md:88`, `_prd.md:98`, and
  `_prd.md:105`; read the parity `_ownership.yml`; in a fresh clone edit an
  owned Skill, run `make skills-sync`, then run `make baseline-digests` and
  inspect its changed-artifact list.
- Evidence:
  [`regeneration-and-boundaries.md`](evidence/2026-08-05-spec-0067-rerun-02/regeneration-and-boundaries.md).
- Affected rows: Q-04, Q-10, Q-17, Q-18.

## Blocked and skipped

None. Every planned row ran. The supplemental scratch Go-cache denial did not
block a row because the same remediation contract completed through the
worktree-local focused suite and the scratch perturbation was restored through
the public sanctioned command.

## Coverage

All 18 rows reached a terminal status: 14 passed and 4 failed. Counts are
`rows_blocked_environment: 0`, `rows_blocked_finding: 0`, and
`rows_blocked_declared: 0`; no row was skipped. All three declared surfaces ran.
Frontend and TUI sweeps are not applicable because the PRD declares neither.
The owned-Skill, one-run recovery, second-run idempotency, unclassified,
multiple-record, invalid/ineffective dedicated command, frozen-rewrite,
measured-owner, remediation, strict-load, and unchanged-derived-content probes
ran. The prior one-run and false-record findings are repaired; the frozen
product contract remains unresolved.

## Final verdict

Fail: one sanctioned regeneration now leaves Verification green and the
ownership records match measured behavior, but that behavior still violates
the active PRD/TechSpec requirement that the parity corpus remain frozen.
Result: 14 pass, 4 fail, 0 blocked, 0 skipped; findings: 1 Trust-Damage. Resolve
the product decision by aligning the PRD/TechSpec and implementation, then
rerun the full gate on a new build.
