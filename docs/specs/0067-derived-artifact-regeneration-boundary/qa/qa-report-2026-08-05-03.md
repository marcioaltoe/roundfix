---
spec: 0067-derived-artifact-regeneration-boundary
date: 2026-08-05
build: 9e668cfe4649c323694c55f8124ed73840260910
status: closed
verdict: fail
rows_blocked_environment: 0
rows_blocked_finding: 0
rows_blocked_declared: 0
surfaces: [backend, infra, docs]
---

# QA report — Derived artifact regeneration boundary

## Scope and environment

Full rerun of the authored terminal gate after corrective `task_07`, on macOS
in the isolated Run Worktree. Actors are a Roundfix maintainer changing an
owned Skill, a contributor adding a derived artifact, and an operator
responding to a strict-load failure. Public entry points are
`make baseline-digests`, `make verify`, documented `go test` regeneration
commands, and ownership records stored beside derived artifacts.

The PRD declares backend, infra, and docs surfaces. It declares no frontend,
data migration, live service, credential, deployment, release, or Pull Request
journey. The prompt confirms no Pull Request is open; no planned product row
depends on one. The PRD has no `## Unreachable Acceptance` declarations.

High-risk probes are the previously failing parity boundary, one-invocation
owned-Skill recovery, a second idempotency run, unclassified and multiply
classified artifacts, invalid and overlapping exceptions, an out-of-directory
exception, ineffective dedicated commands, a frozen resolved path exposed to
regeneration, and independently measured record truth.

Evidence directory:
`qa/evidence/2026-08-05-spec-0067-rerun-03/`.

## Static gate

`rtk make verify` ran unpiped from the Run Worktree root and exited 0. It
reported 3,382 passing Go tests across 26 packages, one passing isolated corpus
budget test, four passing Skill tests, a passing Repository Skill Set check, a
successful build, and no Spec Consistency Check finding for Spec 0067. The two
Spec 0067 skips concern an absent Vocabulary Contract section and
`references/_index.md`; neither is required by this Spec or on an acceptance
path.

Evidence: [`static-gate.md`](evidence/2026-08-05-spec-0067-rerun-03/static-gate.md).

## Results

| # | Story / criterion / sweep | Actor and surface | Steps, expected observable, independent confirmation, persistence, and evidence | Status |
| - | --- | --- | --- | --- |
| Q-01 | Authored gate and dependency preconditions | QA Agent · docs | `_tasks.md` names `task_05` as the sole terminal `qa` node. Its completed dependency `task_07` transitively closes every non-QA leaf; `task_05` remains Daemon-owned pending during this gate. | pass |
| Q-02 | Project Constraint — identifier strategy | Maintainer · docs | Both active Spec artifacts record non-applicability with `docs/agents/domain.md`; commit-range inspection found no new Internal Identifier or changed path, digest-field, or diagnostic-code identity contract. | pass |
| Q-03 | Project Constraint — authentication and HTTP | Maintainer · docs | Both artifacts record local-only non-applicability with `docs/agents/agent-instructions.md`; the implementation introduces no auth, HTTP, secret, transport, or deployment surface. | pass |
| Q-04 | Project Constraint — active ADRs | Maintainer · docs/backend | ADR-0080/0081/0085/0091 are accepted and applicable; ADR-0093 is correctly non-applicable to product behavior. Regeneration fallout, strict-load, authored-node, and verdict semantics align in the assembled artifacts. | pass |
| Q-05 | Tooling authority, exact scope, and commit choreography | Maintainer · infra/docs | Authorization `2e560cea` predates Task 03. `git diff-tree` proves Task 03 changes only `Makefile` plus its Task file; corrective Task 06 is a separate descendant with the authorized `Makefile` and named product/test paths; Task 07 touches no protected tooling. See [`governance-and-range.md`](evidence/2026-08-05-spec-0067-rerun-03/governance-and-range.md). | pass |
| Q-06 | Repository Verification | Maintainer · backend/infra/docs | Unpiped `rtk make verify` exited 0 on `9e668cfe`: 3,382 Go tests, the isolated corpus budget, four Skill tests, Repository Skill Set check, build, and Spec 0067 consistency check all passed. | pass |
| Q-07 | Every derived path resolves to exactly one stated owner | Contributor · backend/docs | The focused command exited 0. The real exhaustive scan passed before and after negative probes, with each actual artifact resolved by its most specific record or exception. | pass |
| Q-08 | Unclassified and multiply classified artifacts fail | Contributor · backend | Zero-record, multiple-record, and duplicate-exception fixtures passed only after observing their named rejection; the unchanged real scan remained green. | pass |
| Q-09 | Record and exception schema is enforceable | Contributor · backend/docs | Invalid owner, missing reason/command, outside-directory and duplicate exceptions, nonexistent/wrong commands, and successful no-op probes all observed the required rejection. Isolated snapshots restored. | pass |
| Q-10 | Parity corpus is frozen except for exactly two sanctioned files | Maintainer · docs/infra | The real directory has 17 artifacts. Its record is `frozen` with exactly the two required sanctioned exceptions, and the focused known-boundaries check resolves the other 15 artifacts frozen. See [`ownership-and-remediation.md`](evidence/2026-08-05-spec-0067-rerun-03/ownership-and-remediation.md). | pass |
| Q-11 | Frozen paths reject rewrites and sanctioned exceptions accept them | Maintainer · backend/infra | The isolated frozen-resolved-path probe observed `rewrote frozen artifact`; the sanctioned command restored both parity exception files while preserving every frozen path. | pass |
| Q-12 | Every declared regeneration class behaves as recorded | Maintainer · backend/infra | The focused command exited 0 for synthetic dedicated repair, sanctioned/frozen measurement, ineffective-command rejection, second-run persistence, unrelated-byte preservation, and snapshot restoration. | pass |
| Q-13 | One sanctioned command repairs an ordinary owned-Skill edit | Maintainer · infra | In a fresh clone, `make skills-sync` followed by one `make baseline-digests` reported `changed:true`; the immediately following unpiped `make verify` exited 0 with the full gate green. See [`owned-skill-regeneration.md`](evidence/2026-08-05-spec-0067-rerun-03/owned-skill-regeneration.md). | pass |
| Q-14 | A second sanctioned run is a no-op and stays green | Maintainer · infra | The second scratch regeneration reported `no changes` and `changed:false`; a second unpiped `make verify` exited 0 with the same 3,382-test gate result. | pass |
| Q-15 | Failure remediation names only an effective human action | Operator · backend/docs | The focused owner-class suite passed. A direct scratch `matrix.json` mismatch made the strict test exit 1 with `nothing regenerates`, the record path, and the 2026-07-30 reason; restoring the byte made the same test pass. | pass |
| Q-16 | No digest value or derived artifact content moved in this Spec | Maintainer · infra | Per-Task `git diff-tree` provenance shows no Spec implementation commit changed a digest or derived artifact byte. The broad range's unrelated merged-main delta is diagnosed in the governance evidence; Run Worktree derived roots stayed clean after QA. | pass |
| Q-17 | Non-Goals remain excluded | Maintainer · backend/infra | No digest algorithm or derived content changed in a Spec Task, strict loads still fail closed, the scratch sanctioned run touched only the two parity exceptions, and all 15 frozen parity artifacts stayed unchanged. | pass |
| Q-18 | Documentation is truthful and usable without hidden knowledge | Maintainer · docs | The records, PRD, TechSpec, measured behavior, and remediation agree, but `_tasks.md` and `task_07.md` say the parity directory has 15 files and 13 frozen files. The build has 17 artifacts: 2 sanctioned and 15 frozen. See F-003. | fail |

## Findings

### F-003 — Corrective Task understates the parity corpus by two files

- Impact: `Friction`
- Actor and step: maintainer reading the corrective Task rationale before
  changing or auditing the parity boundary in Q-18.
- Expected: the Task Graph rationale and Task overview state the measurable
  corpus shape that the ownership record and tests enforce.
- Actual: `_tasks.md` says the directory holds 15 files, and `task_07.md` says
  the sanctioned command rewrites two while the other 13 are frozen. The build
  contains 17 artifact files: the same two sanctioned exceptions and 15 frozen
  artifacts. The implementation and generalized acceptance criterion are
  correct; the two prose counts are stale.
- Reproduction: run
  `git ls-tree -r --name-only HEAD -- internal/baseline/testdata/parity-corpus`,
  exclude `_ownership.yml`, and count 17 artifacts; then read `_tasks.md:83`
  and `task_07.md:16-19`.
- Evidence:
  [`ownership-and-remediation.md`](evidence/2026-08-05-spec-0067-rerun-03/ownership-and-remediation.md).
- Affected row: Q-18.

## Blocked and skipped

None. Every planned row ran. The prompt confirms no Pull Request is open, but
the PRD defines no Pull Request acceptance journey, so no product row depends
on that unavailable surface.

## Coverage

All 18 rows reached a terminal status: 17 passed and 1 failed. Counts are
`rows_blocked_environment: 0`, `rows_blocked_finding: 0`, and
`rows_blocked_declared: 0`; no row was skipped. All three declared surfaces
ran. Frontend and TUI sweeps are not applicable because the PRD declares
neither. The owned-Skill, one-run recovery, second-run idempotency,
unclassified, multiple-record, duplicate/outside exception, invalid and
ineffective command, frozen-rewrite, exception precedence, measured-owner,
remediation, strict-load, unchanged-content, and Non-Goal probes ran.

## Final verdict

Fail: all functional and governance acceptance rows pass, but the corrective
Task Graph rationale and Task overview understate the real parity corpus by two
files. Result: 17 pass, 1 fail, 0 blocked, 0 skipped; findings: 1 Friction.
Correct the two stale counts, then rerun the docs row and one adjacent ownership
canary before repeating the full gate.
