---
spec: 0139-a-suite-that-passes-where-it-runs
date: 2026-09-16
build: 3ca473108e0083599d2533e993f97da0397b6e9b
auditing_binary: "0.12.0 (3ca47310-dirty, built 2026-09-16 11:10:07 -0300)"
auditor_staleness: "current: build commit 3ca47310 matches audited HEAD; dirty suffix records the seeded untracked QA report"
status: closed
verdict: pass
rows_blocked_environment: 1
rows_blocked_finding: 0
rows_blocked_declared: 0
surfaces: [backend, cli]
---

# QA Report — A suite that passes where it runs

## Performed repairs

None.

## Assigned repair failures

None.

## Authorization audit inputs

| Task | Outcome | Record | Revision | Detail |
| --- | --- | --- | --- | --- |
| task_03 | granted | docs/specs/0139-a-suite-that-passes-where-it-runs/_authorization.md | 88b53ed9b88df3e637fd463d5e32bfdcfa1d051c | operative grant |

## Mechanical findings

None.

## Scope and environment

- Scope: full terminal QA gate for Tasks 01-04 and every Task acceptance criterion.
- Actors: a Supervisor running repository Verification and a maintainer reading its QA evidence.
- Run Worktree: `roundfix/run-run_20260916T133117Z_382de75b68776482` at `3ca473108e0083599d2533e993f97da0397b6e9b`.
- Toolchain: `go version go1.26.5 darwin/arm64`.
- Rebuilt entry point: `/private/tmp/roundfix-qa-0139-bin/roundfix`, built from the audited tree with cache under `/private/tmp/roundfix-qa-0139-gocache`.
- Pull Request: none open; Pull Request journeys are environment-blocked by the supervised Run fact.
- Data and credentials: local Git fixtures and scratch worktrees only; no credentials required.

## Static gate

- `rtk make BIN_DIR=/private/tmp/roundfix-qa-0139-bin GOCACHE=/private/tmp/roundfix-qa-0139-gocache build` — pass; rebuilt the binary from the assembled tree.
- `rtk proxy /private/tmp/roundfix-qa-0139-bin/roundfix --version` — pass; `roundfix 0.12.0 (3ca47310-dirty, built 2026-09-16 11:10:07 -0300)`.
- `rtk proxy /private/tmp/roundfix-qa-0139-bin/roundfix spec check 0139-a-suite-that-passes-where-it-runs --strict` — pass; no findings and authored Verification commands were not executed.
- `rtk make verify` — pass in the required full-access machine context. A sandboxed attempt exited 2 only because the two unchanged force-stop tests could not read the macOS process table; the identical full-access command exited 0, including all tests, skill checks, and the build.

## Results

| # | Story / criterion / sweep | Actor and surface | Status | Evidence |
| - | --- | --- | --- | --- |
| Q-01 | User Story 1; outside evidence; Task 05 criteria 2 and 5 — compare Spec 0138 and `7a15e970` with the assembled repository Verification | Supervisor; CLI | pass | Spec 0138 records F-001; `7a15e970` exited 2; assembled `rtk make verify` exited 0 with full process-table access. |
| Q-02 | Task 01 criteria 1-3 — symlink fixtures, one-time hard-link boundary, stressed ACPX package | Maintainer; backend tests | pass | Source sweep found the per-test symlink and the sole hard link inside one-time package setup; five stressed package repetitions passed. |
| Q-03 | User Story 3; Task 02 criteria 1-3 — deadline-bound waits, diagnostic stacks, concurrent-load Task-cycle run | Maintainer; backend tests | pass | All eight waits use the deadline helper; its diagnostic test passed; three Task-cycle repetitions passed during an uncached baseline load. |
| Q-04 | User Story 4; Task 03 criteria 1-4; Task 05 criterion 3 — controlled authorization fixture and fresh-clone execution | Maintainer; backend tests | pass | Removed names stay absent, the guard uses ancestry, all audit tests pass, and a `--no-local` clone ran both controlled cases. |
| Q-05 | User Story 2; Task 04 criterion 1 — passing repository Verification runs once before the Agent and reaches the prompt as a fact | Supervisor; backend/CLI | pass | The focused Daemon gate run exercised the real verifier and passed the ordering, shared-capacity, report, Agent-session, prompt, and success-log-removal assertions. |
| Q-06 | Task 04 criteria 2 and 4 — command failure and exit 75 produce one refusal, retain diagnostics, and withhold the Agent without retry | Supervisor; backend/CLI | pass | Ordinary exit 42 and temporary exit 75 subtests passed with one attempt, retained diagnostics, no Agent session, and no retry. |
| Q-07 | Task 04 criterion 3 — an unobserved outcome refuses with cause and diagnostics state | Supervisor; backend/CLI | pass | Retained, reported-but-absent, and absent diagnostics subtests passed and preserved the cause plus explicit diagnostics state. |
| Q-08 | Task 04 criteria 5 and 6 — pre-existing mechanical withholding and an absent configured command skip the runner; regressions and log retention stay intact | Supervisor; backend/CLI | pass | Both skip paths and the existing verifier, mechanical-stage, QA prompt/report, and QA Task-cycle regressions passed. |
| Q-09 | Task 05 criterion 6 — scoped `go vet` compared with delivery target `7a15e970` | Maintainer; backend static analysis | pass | Both revisions emitted the same 31 `internal/agent` copylock diagnostics byte-for-byte; Daemon and speccheck emitted none. |
| Q-10 | Tooling authority — Task 03 changed paths and grant chronology match the exact governed allowlist | Maintainer; Git audit | pass | Git proved the separate operative grant is an ancestor; Task 03 changes only its Task file and the one bounded governed path. |
| Q-11 | Task 05 criterion 7 and Non-Goals — glossary impact and scope-creep sweep | Maintainer; docs/source audit | pass | No canonical term changed; the implementation diff stays inside the declared tests and QA-gate step, with every excluded surface untouched. |
| Q-12 | Mandatory Pull Request journey — approval, checks, unresolved threads, Merge-Ready evidence, and evidence ancestry | Supervisor; Pull Request | blocked (environment: no open Pull Request) | The Run fact proves the cause; all five controls have explicit local or supervised evidence below. |

## Matrix details

| Row | Entry and steps | Expected observable | Independent confirmation | Persistence / reopen | Evidence location |
| --- | --- | --- | --- | --- | --- |
| Q-01 | Read Spec 0138 report at `990bb65b`; run `make verify` at `7a15e970` and audited HEAD. | The historical report names F-001; base fails in the recorded package families; assembled tree exits 0. | Compare exact exit statuses and package output from detached worktrees. | Re-run from fresh detached worktrees. | Report command log and Git object identities. |
| Q-02 | Inspect adapter fixture calls; run the five-count, parallel package command. | Per-test fixture uses `os.Symlink`; only package setup uses `os.Link`; stress run exits 0. | Existing concurrent-exec test runs inside the whole package command. | Five uncached repetitions. | Report command log and source paths. |
| Q-03 | Sweep fixed waits; run helper test; run three Task-cycle repetitions while baseline load runs. | No fixed two-second waits; deadline and all-goroutine diagnostic assertions pass; stress run exits 0. | A separate concurrent package supplies load. | Three Task-cycle repetitions. | Report command log and source paths. |
| Q-04 | Run named audit tests, then clone the assembled branch with `--no-local` and run the controlled fixture verbosely. | Removed commit names stay absent; ancestry guard is present; all named tests pass and fresh clone shows both subtests running. | Fresh clone cannot inherit unreachable local objects. | Re-run in a newly allocated clone. | Report command log and scratch-clone identity. |
| Q-05 | Run the five named QA gate behavior tests and inspect ordering/prompt assertions. | One passing attempt precedes report and Agent work; prompt names command, exit 0, and diagnostics state. | Regression suite covers the public Daemon gate path with the real verifier. | Re-run uncached with `-count=1`. | Report command log and named test. |
| Q-06 | Run failed-Verification test cases for ordinary nonzero and 75. | Existing refusal report is `fail`, command/exit/diagnostics are named, Agent never starts, retry remains unavailable. | Read retained diagnostic assertions and attempt count in the same integration test. | Re-run uncached with `-count=1`. | Report command log and named test. |
| Q-07 | Run unobserved-Verification cases. | Each refusal names the cause and reports retained, missing, or not-retained diagnostics without an empty state. | Three diagnostic-state subtests confirm the distinctions. | Re-run uncached with `-count=1`. | Report command log and named test. |
| Q-08 | Run mechanical-withhold, no-command, verifier, prompt, report, and QA Task-cycle regressions. | Withheld and unconfigured paths run no repository Verification; existing behavior and retention assertions pass. | Full repository Verification later sweeps the assembled suite. | Re-run uncached, then run the repository gate. | Report command log and named tests. |
| Q-09 | Run scoped vet at base and assembled revisions. | No new diagnostic appears; identical base diagnostics are attributed to Spec 0123. | Compare normalized outputs and exit statuses. | Fresh detached base worktree and current tree. | Report command log. |
| Q-10 | Resolve Task 03 and authorization commits; inspect each commit with `git diff-tree`; prove ancestry and exact bounded paths. | Grant predates the Task commit; authorization is separate; governed changes stay within `internal/speccheck/mechanical_test.go` plus its Task file. | Git ancestry and commit trees, not Task prose. | Re-read immutable commit objects. | Report command log. |
| Q-11 | Compare changed paths and vocabulary against `CONTEXT.md` and the Spec's Non-Goals. | No project term was introduced, changed, or retired; no excluded surface changed. | Search tokens and review the complete delivery-target diff. | Re-open glossary and diff at audited HEAD. | Report command log. |
| Q-12 | Observe the supplied no-PR fact and enumerate all five PR controls using local or supervised evidence. | Cause is recorded; each unavailable PR control has an explicit observed absence or equivalent local evidence. | Rebuilt-head identity and repository Verification bind local evidence to the audited commit. | Re-read report and HEAD after checks finish. | This report and the supplied Run fact. |

## Row evidence

### Q-01

- Outside source 1: `990bb65b1e55ebc55140a932be70f8740e0a83e3:docs/specs/0138-a-matrix-the-spec-declares/qa/qa-report-2026-09-15.md`. Its F-001 records `rtk make verify` exiting 2 with failures in `internal/agent`, `internal/cli`, `internal/daemon`, and `internal/speccheck` at audited build `55062f44`.
- Outside source 2: delivery target `7a15e970feb4e98a5a1c896f35c210e3a5650c13`, exercised in detached worktree `/private/tmp/roundfix-qa0139-base.YyOr5A`. `rtk make GOCACHE=/private/tmp/roundfix-qa0139-base-gocache verify` exited 2. This observation reproduced fixed two-second Task-cycle failures in `internal/daemon` and the stale historical expectation in `internal/speccheck`; this repetition's intermittent `internal/agent` package passed. The sandbox also reproduced the unchanged force-stop process-table denial in `internal/cli`.
- Assembled sandbox observation: exact `rtk make verify` exited 2 only in the two unchanged force-stop integration tests, both with `read process table for non-session owner: operation not permitted`. `internal/agent`, `internal/daemon`, and `internal/speccheck` all passed on the assembled tree.
- Assembled machine observation: the identical `rtk make verify`, rerun with full process-table access after the proved sandbox denial, exited 0. It passed every package, the Roundfix-owned skill sync and contract checks, `roundfix skills check`, and the final stamped build.
- Resolved toolchain: `go version go1.26.5 darwin/arm64`.
- Result: the before and after exit statuses are 2 and 0. The external report supplies the intermittent ACPX failure this single base repetition did not reproduce, and the assembled full-access result proves the repository gate is clean where it runs.

### Q-02

- `rtk rg -n 'os\.(Link|Symlink)\(os\.Args\[0\]|provisionPackageAdapterLinks|func TestMain\(' internal/agent/acpx_runner_test.go` exited 0. It found `TestMain` calling `provisionPackageAdapterLinks`, the only `os.Link(os.Args[0], ...)` inside that helper, and `os.Symlink(os.Args[0], path)` in the per-test fixture.
- `rtk env GOCACHE=/private/tmp/roundfix-qa-0139-gocache go test -count=5 -parallel 16 ./internal/agent` exited 0: `ok roundfix/internal/agent 5.973s`. All five package repetitions include `TestFixtureBinarySurvivesConcurrentExec` and the sidecar-dependent adapter tests.
- Result: all three Task 01 acceptance criteria pass. No timeout, retry, skip, or removed parallelism was needed.

### Q-03

- The source sweep found no `time.After(2 * time.Second)` match and found eight `time.After(testWaitBound(t))` branches, `func testWaitBound`, and the `runtime.Stack(stack, true)` all-goroutine diagnostic path in `internal/daemon/task_engine_test.go`.
- `TestWaitBoundFollowsTheTestDeadline` passed in the focused Daemon run. It covers the deadline-derived bound, no-deadline fallback, awaited-event name, and a separate goroutine in the failure diagnostic.
- The gate started `rtk env GOCACHE=/private/tmp/roundfix-qa-0139-gocache go test -count=1 ./internal/baseline`, then ran `rtk env GOCACHE=/private/tmp/roundfix-qa-0139-gocache go test -count=3 -parallel 16 ./internal/daemon -run 'TestTaskCycle'` while that load remained active. The Task-cycle command exited 0 in 32.529s; the overlapping baseline command exited 0 in 67.850s.
- Result: all three Task 02 acceptance criteria pass, and a real hang retains the deadline-bound all-goroutine diagnostic asserted by the focused test.

### Q-04

- `rtk rg -n 'archiveCarrierCommit|archiveHelpCommit|verificationTaskCommit' internal/speccheck/mechanical_test.go` returned no matches (expected exit 1). `rtk rg -n 'merge-base.*--is-ancestor' internal/speccheck/mechanical_test.go` exited 0 at the reachability guard.
- `rtk env GOCACHE=/private/tmp/roundfix-qa-0139-gocache go test -count=1 -v ./internal/speccheck -run '^(TestAuditRefusesAGrantWidenedAfterItsConsumingCommit|TestAuditJudgesTheGrant|TestMechanicalAuditJudgesTheProjectRoot)$'` exited 0. Both controlled late-widening cases, all remaining grant-audit subtests, and the project-root characterization passed.
- Fresh-clone confirmation: `git clone --no-local --branch roundfix/run-run_20260916T133117Z_382de75b68776482 --single-branch ... /private/tmp/roundfix-qa0139-clone.iontWu` exited 0. The clone resolved HEAD `3ca473108e0083599d2533e993f97da0397b6e9b`.
- Inside that clone, `rtk env GOCACHE=/private/tmp/roundfix-qa0139-clone-gocache go test -count=1 -v ./internal/speccheck -run '^TestAuditRefusesAGrantWidenedAfterItsConsumingCommit$'` exited 0 and visibly ran both `later_widening_does_not_authorize_the_consuming_commit` and `authorizing_ancestor_already_bounds_the_governed_path`.
- Result: all four Task 03 acceptance criteria and Task 05's fresh-clone criterion pass.

### Q-05

- Command: `rtk env GOCACHE=/private/tmp/roundfix-qa-0139-gocache go test -count=1 -v ./internal/daemon -run '^(TestWaitBoundFollowsTheTestDeadline|TestQAGateRunsRepositoryVerificationBeforeAgentSession|TestQAGateRefusesOnFailedRepositoryVerification|TestQAGateRefusesOnUnobservedRepositoryVerification|TestQAGateSkipsRepositoryVerificationWhenMechanicalStageWithholds|TestQAGateWithoutConfiguredRepositoryVerificationLeavesItToTheGate|TestExecVerifier.*|TestMechanicalStage.*|TestTaskCycleQA.*|TestWriteMechanicalQAReport.*|TestQAMechanicalRequest.*)$'`.
- Result: exit 0. `TestQAGateRunsRepositoryVerificationBeforeAgentSession` passed against the real `ExecVerifier` path and asserted one attempt before report and Agent work, shared Verification Capacity, QA Work Item and attempt 1, the prescribed prompt statement, and removal of successful diagnostics.

### Q-06

- The same focused command exited 0. `TestQAGateRefusesOnFailedRepositoryVerification/ordinary_command_failure` and `/temporary_exit_has_no_retry` passed for exits 42 and 75. The assertions cover a single attempt, retained diagnostics, exact refusal text, `verdict: fail`, no Agent session, and `retry_available: false` for exit 75.

### Q-07

- The same focused command exited 0. All three `TestQAGateRefusesOnUnobservedRepositoryVerification` subtests passed: diagnostics retained, a reported path absent, and no diagnostics path. Each case names the unobserved cause and reports a non-empty diagnostics state.

### Q-08

- The same focused command exited 0. `TestQAGateSkipsRepositoryVerificationWhenMechanicalStageWithholds` and `TestQAGateWithoutConfiguredRepositoryVerificationLeavesItToTheGate` passed with no verifier attempt in either path.
- The focused command also passed the complete selected `TestExecVerifier*`, `TestMechanicalStage*`, `TestTaskCycleQA*`, `TestWriteMechanicalQAReport*`, and `TestQAMechanicalRequest*` regressions. `TestExecVerifierRemovesSuccessfulOutputArtifact`, `TestExecVerifierRetainsFailedOutputAsTypedCommandError`, and the temporary-exit diagnostic tests preserve the existing log-retention behavior.
- Harness deviation: the first direct Go invocation reached no test because the default user Go cache was sandbox-denied. The single corrected invocation above used the declared scratch cache and reached every named assertion.

### Q-09

- `rtk env GOCACHE=/private/tmp/roundfix-qa0139-vet-current go vet ./internal/agent ./internal/daemon ./internal/speccheck` at assembled HEAD exited 1 with 31 `passes lock by value` diagnostics for `internal/agent.ACPXRunner`.
- The identical scoped command at delivery target `7a15e970`, using `/private/tmp/roundfix-qa0139-vet-base`, also exited 1 with 31 diagnostics.
- Fresh captures at `/private/tmp/roundfix-qa0139-vet-current.txt` and `/private/tmp/roundfix-qa0139-vet-base.txt` each contain 31 lines; `cmp -s` exited 0. A search for `internal/daemon` or `internal/speccheck` in the diagnostics returned no match.
- Result: no diagnostic was introduced by this Spec. The 31 byte-identical runner mutex copying diagnostics remain pre-existing and are attributed to Spec 0123, as the PRD and Task 05 require.

### Q-10

- `rtk git diff-tree --no-commit-id --name-status -r d1d11a8b96bd2e6daca97e1579b62a597e2e47bf` exited 0 and named exactly `docs/specs/0139-a-suite-that-passes-where-it-runs/task_03.md` and `internal/speccheck/mechanical_test.go`.
- `rtk git merge-base --is-ancestor 4b86285f9d022d203062e72aa318256daf205849 d1d11a8b96bd2e6daca97e1579b62a597e2e47bf` exited 0. Commit `4b86285f` introduced the approved authorization on `main`; Task 03 settled later in its own commit.
- The authorization bounds `internal/speccheck/mechanical_test.go` and the task contract permits its own Task file. No prerequisite-fix or consequent-fix declaration exists; the Daemon-preserved Mechanical skip records that absent input instead of fabricating an order audit.

### Q-11

- `rtk git diff --name-status 88b53ed9..HEAD` names only the four completed Task files plus `internal/agent/acpx_runner_test.go`, `internal/cli/implement_test.go`, `internal/daemon/task_engine.go`, `internal/daemon/task_engine_test.go`, and `internal/speccheck/mechanical_test.go`.
- The complete diff leaves `.github`, `Makefile`, both qa-gate skill copies, `internal/runevent`, `go.mod`, and `go.sum` untouched. It therefore changes no CI workflow, Go toolchain pin, Verification configuration, qa-gate text, event publisher, or dependency.
- A zero-context added-line sweep found no `t.Skip`, fixed two-second wait, new `QA-*` token, `verification_*` classification, or `rows_blocked_*` key. The only retry matches set `TemporaryRetryAvailable: false` and assert that exit 75 does not retry. The change adds parallel tests and removes none.
- The production diff adds the gate step and prompt only. It does not copy logs into Spec evidence, change evidence-path detection, change the seeded report shape, or alter authorization-revision selection. The CLI test's one-line fixture change supplies a passing repository command for a minimal temporary repository; it does not change operational CLI behavior or the GitHub freshness check.
- `CONTEXT.md` already owns the canonical names used here: QA Report, Auditing Binary, Precondition Refusal, Unobserved Verification, Verification Capacity, and Daemon. The TechSpec's Vocabulary Contract says `No token is coined`, and the observed implementation adds no competing lifecycle state or emitted identifier.
- Result: no project term was introduced, changed, or retired, so `CONTEXT.md` needs no edit. Every PRD Non-Goal remains outside the implementation.

### Q-12

- Environment cause: the Supervisor prompt states that no Pull Request is open and Pull Request journeys are environment-blocked. The per-Run branch is never pushed and has no Pull Request of its own.
- Approval: no Pull Request approval or change acceptance has been obtained or claimed. The separate tooling authorization in ancestor `4b86285f` approves only the exact governed mutation and cannot stand in for Pull Request approval.
- Checks and status: the rebuilt binary's strict Spec Consistency Check exited 0 with no findings, and the exact full-access `rtk make verify` exited 0 at audited head `3ca473108e0083599d2533e993f97da0397b6e9b`. No GitHub status is claimed.
- Unresolved review threads: no Pull Request surface exists, so no review thread can be observed and none is claimed resolved.
- Merge-Ready acceptance: no Review Source evidence exists yet and no Merge-Ready state is claimed.
- Review-artifact ancestry: no `reviews/` artifact exists under this Spec. Target branch `feat/0139-a-suite-that-passes-where-it-runs` resolves to `88b53ed9b88df3e637fd463d5e32bfdcfa1d051c`; Git proves it is an ancestor of audited Run head `3ca473108e0083599d2533e993f97da0397b6e9b`. All local evidence in this report belongs to that audited head.
- Unblocking action: let the Daemon integrate this QA Report into the target branch, then open the Pull Request on that branch and observe its approval, current-head checks, review threads, Merge-Ready evidence, and any review artifact directly.

## Findings

None.

## Blocked and skipped

- Q-12 is `blocked (environment: no open Pull Request)`. Its row records the supervised cause, approval state, checks and status, review-thread state, Merge-Ready state, review-artifact ancestry, and exact unblocking action.
- No row is finding-blocked, declared-blocked, or skipped. The preserved Mechanical skips below are Daemon detector output, not matrix-row outcomes.
- The first focused Go test attempts did not reach assertions because the sandbox denied the default user Go cache. Their single corrected commands used `/private/tmp` and passed.
- The sandboxed assembled `rtk make verify` reached the suite and failed only where two unchanged force-stop tests require process-table access. The identical full-access gate exited 0; Q-01 records both observations and the unchanged-base reproduction.

## Coverage

- Results: 11 pass, 1 environment-blocked with complete equivalent evidence, 0 fail, 0 finding-blocked, 0 declared-blocked, 0 skipped, and 0 pending.
- User stories: Story 1 → Q-01 and Q-02; Story 2 → Q-05 through Q-08; Story 3 → Q-03; Story 4 → Q-04.
- PRD Goals: Goal 1 → Q-01 through Q-04; Goal 2 → Q-05 through Q-08; Goal 3 → Q-03; Goal 4 → Q-05 through Q-11.
- Core Features: Feature 1 → Q-02; Feature 2 → Q-03; Feature 3 → Q-04; Feature 4 → Q-05; Feature 5 → Q-06 and Q-07; Feature 6 → Q-05 and Q-08; Feature 7 → Q-07, Q-08, and Q-11.
- Task 01 criteria 1-3 → Q-02.
- Task 02 criteria 1-3 → Q-03.
- Task 03 criteria 1-4 → Q-04.
- Task 04 criterion 1 → Q-05; criteria 2 and 4 → Q-06; criterion 3 → Q-07; criteria 5 and 6 → Q-08.
- Task 05 acceptance: complete Task criterion coverage → Q-02 through Q-08; before/after Verification → Q-01; fresh clone → Q-04; stress runs → Q-02 and Q-03; clean repository Verification → Q-01; scoped vet → Q-09; glossary and terminal counters → Q-11 and this section.
- Declared intentional breaks: configured Verification leaves the Agent → Q-05; failed and unobserved Verification withhold the Agent → Q-06 and Q-07; the historical subtest is replaced by a controlled fixture → Q-04.
- Regression locks: existing ACPX, Task-cycle, verifier, audit, mechanical-stage, prompt, report, and QA Task-cycle assertions → Q-02 through Q-08; failure still writes `verdict: fail` → Q-06 and Q-07; success/failure log retention → Q-05, Q-06, and Q-08; report shape and evidence behavior stay unchanged → Q-08 and the authored report marker; prior mechanical withholding still withholds → Q-08.
- Cross-cutting obligations: outside evidence → Q-01; tooling authority → Q-10; glossary and every Non-Goal → Q-11; Pull Request environment equivalence → Q-12.
- High-risk probes: five repeated parallel ACPX package runs test concurrent exec churn; three Task-cycle repetitions under concurrent baseline load test machine slowness; ordinary failure, exit 75, unobserved diagnostics states, prior mechanical withholding, and an absent command test invalid or out-of-order gate inputs; the `--no-local` clone tests reopen behavior without stale objects.
- Surfaces: backend and CLI exercised. No frontend, TUI, data migration, infrastructure apply, authentication, or HTTP surface is declared by this Spec.
- Success Metrics: the delivered-tree `make verify` metric is observed in Q-01. A rerun of Spec 0138 and the next two independent Spec gates are post-delivery longitudinal measurements, not states this terminal child gate can create without starting or mutating other Spec Runs. This report records the leading evidence they depend on — the clean assembled gate and the Daemon pass/refusal paths — without claiming those future Runs occurred.
- Counters: `rows_blocked_environment: 1`, `rows_blocked_finding: 0`, and `rows_blocked_declared: 0` match the Results table.

## Final verdict

Pass: every runnable row passed, Q-12 records its no-Pull-Request cause and complete equivalent evidence, all evidence locations resolve, and no row failed, remained pending, or was finding-blocked, declared-blocked, or skipped.

## Mechanical skips

| Detector | Missing artifact |
| --- | --- |
| consequent-fix commit order | consequent-fix declaration |
| QA Report structure | QA Report |
| QA evidence paths | QA Report |
