---
spec: 0005-tui-cockpit
date: 2026-07-05
verdict: pass
surfaces: [frontend, cli]
---

# QA Report — TUI Cockpit Redesign

## Summary

The full repository gate passed on the current build, then the cockpit was
checked through the PRD's intended evidence path: synchronous Bubble Tea model
flows, render snapshots, focused viewport tests, CLI attach tests, and safe
production CLI smoke commands. Every PRD user story, core feature, task
acceptance criterion, and non-goal passed. No failures or blocked items remain.

## Results

| # | Criterion / user story | Verdict | Evidence |
| - | ---------------------- | ------- | -------- |
| 1 | Static gate: repository verification must pass before flow QA. | pass | `rtk make verify` exited 0: `go test ./...` reported 579 passed in 16 packages, `roundfix skills check` passed, and `go build` produced `bin/roundfix`. |
| 2 | CLI surface: help exposes the Implement Command, Attach, and skills commands without stderr noise. | pass | `rtk ./bin/roundfix --help`, `rtk ./bin/roundfix implement --help`, `rtk ./bin/roundfix attach --help`, and `rtk ./bin/roundfix skills check` exited 0. |
| 3 | CLI surface: a completed Spec without `--qa` prints Task statuses and creates no Run. | pass | `rtk env HOME=/private/tmp/roundfix-qa-home-2026-07-05 ./bin/roundfix implement --spec 0005-tui-cockpit --agent codex --no-input` exited 0 and printed all seven Tasks as completed plus `All 7 Task(s) already completed; no Run was created.` |
| 4 | User story 1: review Runs show a Batch-grouped Work Queue with per-item status markers and a dominant timeline. | pass | `rtk go test ./internal/tui/ -run 'TestCockpit(...)'` exited 0 with 55 focused cockpit tests; covered `TestCockpitReviewPhaseRowAndTwoPaneStructure`, row marker tests, Batch separator tests, footer totals, and dominant timeline width. |
| 5 | User story 2: spec Runs use the same cockpit with a spec phase row, not a reduced pane. | pass | Focused cockpit tests passed, including `TestCockpitSpecPhaseRowCoversQAOmittedAndLocked`; `TestCockpitRenderSnapshots` passed with spec snapshots at 88x24 and 120x40. |
| 6 | User story 3: Enter opens Work Item detail as a centered modal, scrollable and dismissable without losing context. | pass | Focused cockpit tests passed, including Enter/Esc, `D` toggle, scroll, paging, inert queue navigation, byte-identical close context, and spec task detail stale-reload coverage. |
| 7 | User story 4: Attach replays the same cockpit from the Run Event Journal for finished or live Runs. | pass | `rtk go test ./internal/tui/ -run 'TestCockpit(...)'` covered finished review and spec Attach cockpit tests; `rtk go test ./internal/cli/ -run 'Test(AttachReplaysCompletedRunReadOnly|AttachReplaysCompletedSpecRunReadOnly|...)'` exited 0 with 7 CLI tests. |
| 8 | User story 5: Follow Mode suspends on scrollback and resumes at the tail. | pass | `rtk go test ./internal/tui/ -run 'TestViewport(...)'` exited 0 with 4 viewport tests, including `TestViewportFollowModeTracksGroupedBatchBoundaries`. |
| 9 | User story 6: small terminals degrade to a usable single-surface fallback. | pass | Focused cockpit tests passed, including `TestCockpitResponsiveFallbackAndStableSizes` for 80x24 collapse, 120x40 and 200x50 two-pane layouts, and `TestCockpitDegenerateSizesRenderEmptyWithoutPanic`. |
| 10 | Core feature 1: two persistent surfaces, Work Queue left and widest Session Timeline right, no persistent third pane. | pass | `TestCockpitReviewPhaseRowAndTwoPaneStructure`, `TestCockpitTimelinePaneIsDominantAtTestedSizes`, and cockpit snapshots passed; non-goal sweep found no shipped cockpit output containing the retired third-pane labels. |
| 11 | Core feature 2: phase row renders review and spec sequences with `[done]`, `[run]`, `[wait]`, and `[locked]` markers. | pass | `TestCockpitReviewPhaseRowAndTwoPaneStructure` and `TestCockpitSpecPhaseRowCoversQAOmittedAndLocked` passed; snapshots show review and spec phase rows. |
| 12 | Core feature 3: Work Queue rows group by Batch, show status marker, optional severity, ordinal, title, location, and compact totals. | pass | `TestCockpitWorkQueueRowsRenderMarkersMetadataAndOptionalSeverity`, `TestCockpitWorkQueueBatchSeparatorShowsOrdinalAndElapsed`, and `TestCockpitWorkQueueFooterTotalsForRunKinds` passed. |
| 13 | Core feature 4: timeline events group per Batch and kind while preserving raw payloads and Follow Mode. | pass | `rtk go test ./internal/tui/ -run 'TestViewport(...)'` passed review grouping, spec task/QA grouping, grouped Follow Mode, and chunk coalescing tests. |
| 14 | Core feature 5: Detail Modal opens with Enter, toggles with `D`, closes with Esc, scrolls with keys, and shows review artifacts or task files read-only. | pass | Focused cockpit tests passed for review issue detail, spec task detail, modal scroll/page keys, full-surface fallback, and Attach detach key behavior. |
| 15 | Core feature 6: Attach parity and journal discipline. | pass | TUI Attach cockpit tests and CLI read-only attach tests passed; CLI attach tests assert no new Runs and unchanged terminal Run state. |
| 16 | Core feature 7: non-TTY renderer and stderr behavior are unchanged. | pass | `rtk go test ./internal/cli/ -run 'TestRunNoAgentConsoleRejectsInteractiveCockpit|TestRunOperationalCommandPreservesStdoutReportOrdering|TestAttachReplaysCompletedSpecRunReadOnly|TestAttachReplaysCompletedRunReadOnly'` exited 0 with 6 tests; `rtk go test ./internal/tui/ -run 'TestRenderLiveRunView|TestCockpitRenderSnapshots'` exited 0 with 13 tests. |
| 17 | Task 01 AC: snapshot tests exist for listed states/sizes and stayed identical across renderer decomposition. | pass | Task Result cites snapshot capture and neutrality evidence; current `TestCockpitRenderSnapshots` passed with 11 snapshots. |
| 18 | Task 01 AC: no exported API, behavior, or key-routing changes in the decomposition step. | pass | Task Result records package-internal render helper extraction only; full current gate passed. |
| 19 | Task 01 AC: full suite passes. | pass | `rtk make verify` exited 0. |
| 20 | Task 02 AC: review fixture renders queue, timeline, and phase row matching the mockup structure. | pass | `TestCockpitReviewPhaseRowAndTwoPaneStructure` passed and asserts Work Queue, Session Timeline, phase sequence, and retired labels absent. |
| 21 | Task 02 AC: spec fixture covers QA locked and omitted cases. | pass | `TestCockpitSpecPhaseRowCoversQAOmittedAndLocked` passed. |
| 22 | Task 02 AC: timeline pane is wider at every tested size. | pass | `TestCockpitTimelinePaneIsDominantAtTestedSizes` passed. |
| 23 | Task 02 AC: full suite passes with deliberate task_01 snapshot updates. | pass | `rtk make verify` and `TestCockpitRenderSnapshots` passed. |
| 24 | Task 03 AC: row tests cover all supported status labels plus severity-present and severity-absent cases. | pass | `TestCockpitWorkQueueRowsRenderMarkersMetadataAndOptionalSeverity` passed in the focused cockpit run. |
| 25 | Task 03 AC: separator tests pin Batch ordinal and elapsed rendering. | pass | `TestCockpitWorkQueueBatchSeparatorShowsOrdinalAndElapsed` passed. |
| 26 | Task 03 AC: totals footer asserts review and spec fixtures. | pass | `TestCockpitWorkQueueFooterTotalsForRunKinds` passed. |
| 27 | Task 03 AC: full suite passes. | pass | `rtk make verify` exited 0. |
| 28 | Task 04 AC: review journal fixture renders grouped Batches with plan, tool, and think sections. | pass | `TestViewportGroupsReviewTimelineByBatchAndKind` passed. |
| 29 | Task 04 AC: spec journal fixture groups task and QA milestones legibly. | pass | `TestViewportGroupsSpecTimelineTaskAndQAMilestones` passed. |
| 30 | Task 04 AC: Follow Mode tests pass unchanged plus grouped-tail tests. | pass | `TestViewportFollowModeTracksGroupedBatchBoundaries` passed; full suite passed. |
| 31 | Task 04 AC: full suite passes. | pass | `rtk make verify` exited 0. |
| 32 | Task 05 AC: Enter, `D`, Esc, scroll, paging, and inert queue navigation work while the modal is open. | pass | Focused cockpit tests passed for Enter/Esc detail, `D` toggle, modal scrolling, paging, and queue-key suspension. |
| 33 | Task 05 AC: render before opening equals render after closing for both Run kinds. | pass | Focused cockpit tests passed byte-comparison assertions for review and spec Runs. |
| 34 | Task 05 AC: task-file detail is read-only and survives corrupted mid-write reload with a stale marker. | pass | `TestCockpitSpecTaskDetailModalRestoresContextAndSurvivesStaleReload` passed. |
| 35 | Task 05 AC: full suite passes. | pass | `rtk make verify` exited 0. |
| 36 | Task 06 AC: footer tests pin each state's hint line for both Run kinds. | pass | `TestCockpitFooterHintsForStatesAndRunKinds` passed. |
| 37 | Task 06 AC: 80x24 is collapsed fallback and 120x40 is two-pane. | pass | `TestCockpitResponsiveFallbackAndStableSizes` passed. |
| 38 | Task 06 AC: zero and negative dimensions return empty strings without panicking. | pass | `TestCockpitDegenerateSizesRenderEmptyWithoutPanic` passed. |
| 39 | Task 06 AC: full suite passes. | pass | `rtk make verify` exited 0. |
| 40 | Task 07 AC: Attach tests replay both Run kinds through the new cockpit with modal working and Run state untouched. | pass | TUI Attach tests passed; CLI Attach tests passed and assert read-only behavior. |
| 41 | Task 07 AC: skill text matches shipped keys and surfaces; drift check passes. | pass | `rtk ./bin/roundfix skills check` and the skill check inside `rtk make verify` passed. |
| 42 | Task 07 AC: design docs match shipped behavior or carry dated correction. | pass | `docs/specs/0005-tui-cockpit/design/ui-redesign-plan.md` includes the 2026-07-05 shipped contract corrections; `rtk rg` sweep matched them. |
| 43 | Task 07 AC: no new un-glossaried term. | pass | `CONTEXT.md` defines Cockpit, Work Queue, Phase Row, Session Timeline, Detail Modal, Follow Mode, Attach, and related Run vocabulary; task Result records the glossary pass. |
| 44 | Non-goal: no web dashboard, provider logos, pictographic icons, or decorative elements. | pass | `rtk rg` non-goal sweep found these terms only in PRD/design non-goals; rendered snapshots use terminal text markers and borders. |
| 45 | Non-goal: no persistent third detail pane. | pass | Normal and detail-open snapshots passed: the base view has Work Queue plus Session Timeline; detail appears only as `REVIEW.ISSUE` modal overlay. |
| 46 | Non-goal: no new review workflow or source-thread mutation from the UI. | pass | Attach code path is read-only; CLI attach tests passed and assert no new Runs or state mutation. |
| 47 | Non-goal: no Run Event storage, journal schema, or agent contract changes. | pass | Timeline grouping tests passed against journal events; `rtk make verify` passed store, daemon, agent, and TUI packages. |
| 48 | Non-goal: no non-TTY or stderr behavior changes. | pass | CLI stdout/stderr ordering and non-TTY tests passed; production `implement` no-op smoke produced only the expected Task stdout contract. |
| 49 | Non-goal: no CLI framework migration. | pass | CLI help and command paths ran through the built stdlib-flag binary; `rtk rg` did not find Cobra/spf13 usage in shipped code. |

## Failures and blocked items

None.

## Environment

- Date: 2026-07-05.
- Repository: `/Users/marcio/dev/roundfix`.
- Branch: `ma/implementation-daemon-acpx`.
- Initial `rtk git status --short` hit a local fsmonitor IPC error; retrying
  with `rtk git -c core.fsmonitor=false status --short` showed a clean
  worktree before report creation.
- Built application: `bin/roundfix`, produced by `rtk make verify`.
- Static gate: `rtk make verify`.
- Focused frontend checks:
  `rtk go test ./internal/tui/ -run 'TestCockpit(...)'`,
  `rtk go test ./internal/tui/ -run 'TestViewport(...)'`,
  `rtk go test ./internal/tui/ -run 'TestRenderLiveRunView|TestCockpitRenderSnapshots'`.
- Focused CLI checks:
  `rtk ./bin/roundfix --help`,
  `rtk ./bin/roundfix implement --help`,
  `rtk ./bin/roundfix attach --help`,
  `rtk ./bin/roundfix skills check`,
  `rtk env HOME=/private/tmp/roundfix-qa-home-2026-07-05 ./bin/roundfix implement --spec 0005-tui-cockpit --agent codex --no-input`,
  and targeted `./internal/cli` tests for Attach and non-TTY contracts.
- Sandbox note: the first production `implement` smoke used the real
  `/Users/marcio/.roundfix` config and was blocked by an artifact-directory
  write-check permission. A single full-access retry proved the no-Run path,
  and the same smoke passed with an isolated temporary `HOME` to avoid relying
  on the user's Roundfix Home.
