---
spec: 0169-override-and-ordinal-follow-ups
date: 2026-09-25
build: 1c132a416f7e8fcef847eb4dda5190b888783b24
auditing_binary: "roundfix 0.16.0 (built from 1c132a416f7e8fcef847eb4dda5190b888783b24)"
auditor_staleness: "current: built from the audited HEAD and reports the declared tree version 0.16.0"
status: closed
verdict: pass
rows_blocked_environment: 1
rows_blocked_finding: 0
rows_blocked_declared: 0
surfaces: [backend, cli, docs]
---

# QA Report — Override and ordinal follow-ups

## Scope and environment

Full terminal gate for build `1c132a41` on macOS. The intended actors are a
maintainer invoking the local CLI and a Spec author running strict consistency
checks. The production-like entry point is a `roundfix 0.16.0` binary built
from the audited HEAD at `/tmp/roundfix-qa-0169.MTfYOx/roundfix`; scratch
repositories and Specs remain outside the source tree. The installed
`roundfix 0.15.0` is stale and is not used as product evidence.

The strict authoring precondition passed with the built binary: `roundfix spec
check 0169-override-and-ordinal-follow-ups --strict` reported no findings and
did not execute authored Verification commands. The Daemon owns Task status
and repository Verification. This gate does not commit, push, or open a Pull
Request.

## Static gate

- Repository Verification: the Daemon ran `make verify-changed`; exit `0`,
  verdict `pass`, diagnostics removed on success. The gate did not rerun it.
- Built binary: `/tmp/roundfix-qa-0169.MTfYOx/roundfix version` reported
  `roundfix 0.16.0`.

## Performed repairs

None.

## Assigned repair failures

None.

## Authorization audit inputs

| Task | Outcome | Record | Revision | Detail |
| --- | --- | --- | --- | --- |
| task_01 | granted | docs/specs/0169-override-and-ordinal-follow-ups/_authorization.md | 407fef9ec26f30f37b7710305c2f51daac64b209 | operative grant |

## Mechanical findings

None.

## Results

| # | Story / criterion / sweep | Actor and surface | Status | Evidence |
| - | --- | --- | --- | --- |
| R1 | Repository Verification | Daemon; repository gate | pass | Requirement 1; repository Verification — supervised `make verify-changed` exited 0; diagnostics removed on success. |
| R2 | Override records an incomplete QA Task status and omits it for a completed QA Task | Maintainer; CLI/backend | pass | Requirement 2 — two public CLI archive flows exited 0; fresh archived reads found `failed` only for the failed Task case. |
| R3 | Skills, mirrors, and command guide match the executable override contract | Maintainer/agent; CLI/docs | pass | Requirement 3 — skill validation and mirror comparisons passed; CLI help and both public archive flows matched the documented refusal/stamp contract; tooling history was authorized. |
| R4 | Same-Spec ordinal collisions fail and missing Task Graphs expose the skipped detector | Spec author; CLI/backend | pass | Requirement 4 — public Spec checks emitted the collision, cleared it after an independent ordinal, and listed the missing-graph skip. |
| R5 | This Spec's promises map to Tasks and executable coverage | Spec author; CLI/docs | pass | Requirement 5 — current-tree strict Spec check exited 0 with no findings after the behavior rows ran. |
| R6 | Spec 0159 independently recorded the five follow-up gaps | Maintainer/reviewer; repository evidence | pass | outside-evidence row — archived Spec 0159 names all five gaps at committed provenance; a pre-feature binary reproduced the absent same-Spec detector. |
| R7 | Pull Request controls | Maintainer/reviewer; Pull Request | blocked (environment: no open Pull Request) | Pull Request row — prompt-provided absence plus equivalent evidence for approval, checks/status, threads, Merge-Ready state, and ancestry. |

## Row evidence

### R1

```yaml
inputs:
  - kind: live_service
    ref: Roundfix Daemon repository Verification result
```

The Roundfix Daemon reported `make verify-changed` exit `0` and removed the
success diagnostics. This is supervised evidence supplied outside the Agent
sandbox, recorded as a fact and not re-derived.

### R2

```yaml
inputs:
  - kind: repository_path
    ref: cmd/roundfix/**
  - kind: repository_path
    ref: internal/spec/**
```

One disposable clone at HEAD supplied a real Git repository with two isolated
Spec fixtures. For each fixture, the built command entered through `roundfix
archive <slug> --qa-override --approval ... --reason ...`:

- failed QA Task plus `pass` report: exit `0`; the archived `_prd.md` records
  `qa_override_qa_outcome: pass` and `qa_override_qa_task_status: failed`;
- completed QA Task plus `fail` report: exit `0`; the archived `_prd.md`
  records `qa_override_qa_outcome: fail` and omits
  `qa_override_qa_task_status`.

Fresh filesystem reads confirmed both active Spec paths disappeared, both
archive destinations exist, and each archived Task and QA report remains
present. The exact assertion command exited `0`.

### R3

```yaml
inputs:
  - kind: repository_path
    ref: .agents/skills/archive-spec/**
  - kind: repository_path
    ref: .agents/skills/roundfix/**
  - kind: repository_path
    ref: skills/archive-spec/**
  - kind: repository_path
    ref: skills/roundfix/**
  - kind: repository_path
    ref: docs/user-guide/commands.md
  - kind: repository_path
    ref: docs/specs/0169-override-and-ordinal-follow-ups/_authorization.md
```

The built `roundfix skills check` command exited `0`. Exact `cmp` checks proved
the canonical and distributed `archive-spec` copies match and the canonical and
distributed `roundfix` copies match. The documented command was then exercised
by R2 rather than accepted from file reading alone. Built `archive --help`
states that the override requires approval and reason, preserves QA evidence,
and is refused when QA already qualifies; R2 proved the status-stamp behavior.

Focused text inspection found the current refusal rule and
`qa_override_qa_task_status` in both Roundfix skill copies, the command-only and
never-hand-stamp instructions in both archive-spec copies, and the conditional
field in `docs/user-guide/commands.md`.

The governed-path audit resolved Task 01 to commit `c2834d28`. `git diff-tree
--no-commit-id --name-only -r c2834d28` showed only the six paths bounded by
the approved authorization plus the ordinary command guide and assigned Task
file. Authorization commit `407fef9e` is an ancestor of the Task commit. The
sanctioned `make skills-sync` regeneration is recorded in Task 01, and the
fresh byte comparisons confirm its current output. No consequent-fix
declaration exists, as retained in Mechanical skips.

### R4

```yaml
inputs:
  - kind: repository_path
    ref: cmd/roundfix/**
  - kind: repository_path
    ref: internal/speccheck/**
```

The built CLI checked disposable Specs through the public `roundfix spec check`
surface:

- two Tasks claiming ordinal `9998` at different paths exited `1` with one
  `SC-ORDINAL-CLAIMED` error naming the Spec, ordinal, and both paths;
- changing the second claim to distinct ordinal `9997` removed
  `SC-ORDINAL-CLAIMED` on a fresh invocation (the deliberately minimal fixture
  still returned unrelated authoring findings);
- a Spec containing `_prd.md` but no `_tasks.md` exited `1` and listed
  `SC-ORDINAL-CLAIMED: missing docs/specs/qa-missing-task-graph/_tasks.md` under
  `Skipped`.

Each invocation reparsed the scratch files, providing an independent public
read after the input change.

### R5

```yaml
inputs:
  - kind: repository_path
    ref: docs/specs/0169-override-and-ordinal-follow-ups/**
```

A fresh `/tmp/roundfix-qa-0169.MTfYOx/roundfix spec check
0169-override-and-ordinal-follow-ups --strict` invocation exited `0`, reported
`No findings`, and explicitly did not execute authored Verification commands.
This public check confirms the PRD promises map into the TechSpec and Task
Graph; rows R2-R4 independently executed the mapped behavior rather than
relying on the mapping alone. The announced skips concern absent optional
rollup, backlog, Vocabulary Contract, and reference-index artifacts, not the
promise rule.

### R6

```yaml
inputs:
  - kind: repository_path
    ref: docs/history/specs/0159-archive-override-and-authoring-rules/_prd.md
```

The outside source is archived Spec 0159's `## Recorded limits`, committed at
`ae47621cef7eae56af2e0a85537f9a519ecd6ba0` (`feat: archive override and
authoring rules (#247)`). It names the five gaps verbatim before this Spec's
implementation: incomplete-QA status provenance, stale Roundfix wording,
archive-spec hand-stamping, same-Spec duplicate ordinals, and the missing-Task-
Graph skip.

This row does not rest on file reading alone. The installed pre-feature
`/Users/marcio/go/bin/roundfix` reports `0.15.0`; against the same disposable
duplicate-ordinal fixture used by R4, its public Spec check emitted no
`SC-ORDINAL-CLAIMED`. The built `0.16.0` binary emitted the claimed error for
the identical input. The older binary also rejected `--qa-override` as unknown,
so it was not used to infer the later override details; those remain grounded
in Spec 0159's committed review record and R2's current public behavior.

### R7

```yaml
inputs:
  - kind: live_service
    ref: Pull Request for fix/0169-override-and-ordinal-follow-ups
```

- Cause: the QA prompt states that no Pull Request is open. The Run Worktree
  branch is never pushed and has no Pull Request of its own.
- Approval: `_authorization.md` records the maintainer's approved 2026-09-25
  implementation and delivery scope; no post-change Pull Request approval was
  obtained.
- Checks and status: Daemon-supervised `make verify-changed` passed with exit
  `0`, and rows R2-R6 record the current candidate's public-flow, contract, and
  outside-evidence checks.
- Unresolved review threads: none exists because no Pull Request or thread set
  exists.
- Merge-Ready acceptance: none exists yet because no Review Source object
  exists for a Pull Request.
- Review-artifact ancestry: no Pull Request review artifact exists yet. The
  audited candidate head is `1c132a416f7e8fcef847eb4dda5190b888783b24`;
  `fix/0169-override-and-ordinal-follow-ups` remains at
  `407fef9ec26f30f37b7710305c2f51daac64b209`, which is an ancestor of the
  candidate, until Daemon integration.
- Unblocking action: integrate the Run into the target branch and open the
  Pull Request during the later delivery stage.

## Findings

None.

## Blocked and skipped

R7 is blocked by the declared environment fact that no Pull Request is open.
Its evidence block records all five equivalent-control states and the later
delivery action that unblocks the live journey. No row is finding-blocked,
declared-blocked, or skipped.

## Coverage

All 7 rows settled: 6 passed and 1 is environment-blocked with equivalent
observed or supervised evidence. The matrix covers Requirements 1-5 plus the
outside-evidence, Pull Request, and repository Verification sources.
`rows_blocked_environment: 1`, `rows_blocked_finding: 0`, and
`rows_blocked_declared: 0`. No Unreachable Acceptance declaration, frontend
sweep, behavior probe, Non-Goal observable, or risk-based skip applies to this
declared matrix.

## Final verdict

Pass: every runnable row passed, R7 records its environment cause and each
equivalent Pull Request control, and no row failed, remained pending, or was
finding-blocked, declared-blocked, or skipped.

## Mechanical skips

| Detector | Missing artifact |
| --- | --- |
| consequent-fix commit order | consequent-fix declaration |
| QA Report structure | QA Report |
| QA evidence paths | QA Report |
