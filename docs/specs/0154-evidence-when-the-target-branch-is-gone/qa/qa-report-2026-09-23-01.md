---
spec: 0154-evidence-when-the-target-branch-is-gone
date: 2026-09-23
build: f12fa7adfdf720603bdb6580a0fbccd11cf5a338
auditing_binary: "0.16.0 (40fb765f, built 2026-09-23 08:02:21 -0300)"
auditor_staleness: "stale: commit ancestry: build commit predates audited tree"
status: closed
verdict: pass
rows_blocked_environment: 1
rows_blocked_finding: 0
rows_blocked_declared: 0
surfaces: [backend, cli, docs]
---

# QA Report

## Scope and environment

- Scope: full authored terminal gate for Task 04 on build
  `f12fa7adfdf720603bdb6580a0fbccd11cf5a338`.
- Actor: a local Roundfix operator inspecting retained terminal Runs.
- Entry points: the built `roundfix reconcile` CLI, repository Git reads, the
  shipped Roundfix skill, and the daemon-owned repository Verification fact.
- Authoring precondition: `roundfix spec check
  0154-evidence-when-the-target-branch-is-gone --strict` exited 0 with no
  findings. Authored Verification commands were not executed by that check.
- Pull Request: none open; Pull Request journeys are environment-blocked.
- Repository Verification: daemon-owned; the supplied result is `make verify`
  exit 0 with diagnostics removed on success. This gate will not rerun it.
- Parity: CLI journeys use a binary built from the audited worktree and scratch
  local Git repositories. No network service or credential is involved.
- Sandbox parity: the first build attempt could not write the user's Go cache;
  the successful build used `GOCACHE=/private/tmp/roundfix-qa-0154-gocache`
  without changing source, flags, or the resulting command surface.

## Performed repairs

None.

## Assigned repair failures

None.

## Authorization audit inputs

| Task | Outcome | Record | Revision | Detail |
| --- | --- | --- | --- | --- |
| task_03 | granted | docs/specs/0154-evidence-when-the-target-branch-is-gone/_authorization.md | 77c711917374354b8812b8e57ee4adec70a448ba | operative grant |

## Mechanical findings

None.

## Static gate

- `make verify` — pass (exit 0), run once by the Daemon outside the Agent
  sandbox; diagnostics were removed on success. Per the QA contract, the Agent
  did not rerun it.
- `roundfix spec check 0154-evidence-when-the-target-branch-is-gone --strict`
  — pass (exit 0), no findings. The checker disclosed four inapplicable
  detectors: no findings rollup, no backlog, no TechSpec Vocabulary Contract,
  and no `references/_index.md`.

## Execution log

- `GOCACHE=/private/tmp/roundfix-qa-0154-gocache go build -o
  /private/tmp/roundfix-qa-0154-current ./cmd/roundfix` — pass (exit 0).
- `/private/tmp/roundfix-qa-0154-current --version` — pass (exit 0), reported
  `roundfix 0.16.0`.
- `/private/tmp/roundfix-qa-0154-current reconcile
  run_20260921T160122Z_07824e2f31e39776 --format json` — pass (exit 0), one
  unintegrated Run preserved with the missing superseding-report reason.
- `/private/tmp/roundfix-qa-0154-current reconcile
  run_20260921T110125Z_a0e8bf6b87cd6066 --format json` — pass (exit 0), one
  dirty Run preserved and no releasable Run Branch emitted.
- `GOCACHE=/private/tmp/roundfix-qa-0154-gocache go test -count=1 -v
  ./internal/worktree -run 'Test(Reconcile(FallsBackToTheDefaultBranch|FallbackRequiresArchivedEvidence|PresentTargetAcceptsEitherSpecPath|PreservesWithoutDefaultBranchEvidence|ReasonNamesTheMissingProof)|InspectTerminalRun(Safe|Unintegrated|ClassifiesSupersededQAReport|Dirty)|ClassifyRunBranchSet(FourFailedCycles|PreservesUnintegratedImplementationWork|PreservesBranchWithoutSupersededProof))$'`
  — pass (exit 0).
- `/private/tmp/roundfix-qa-0154-current skills check` — pass (exit 0).
- `cmp -s .agents/skills/roundfix/SKILL.md skills/roundfix/SKILL.md` — pass
  (exit 0).
- Read-only `git diff-tree`, `merge-base`, `show-ref`, `ls-tree`, status,
  branch, and Worktree reads produced the authority, outside-evidence, dirty,
  and persistence observations recorded in R4, R8, and R10.

## Results

| # | Status | Provenance |
| - | --- | --- |
| R1 | pass | Requirement 1; repository Verification |
| R2 | pass | Requirement 2 |
| R3 | pass | Requirement 3 |
| R4 | pass | Requirement 4 |
| R5 | pass | Requirement 5 |
| R6 | pass | Requirement 6 |
| R7 | pass | Requirement 7 |
| R8 | pass | Requirement 7 |
| R9 | pass | Requirement 8 |
| R10 | pass | outside-evidence row |
| R11 | blocked (environment: no open Pull Request) | Pull Request row |

## Row evidence

### R1

```yaml
inputs:
  - kind: repository_path
    ref: "**"
```

- Actor and surface: daemon; repository gate.
- Entry point and steps: run `make verify` once for the audited tree.
- Expected observable: exit 0 and no retained success diagnostics.
- Independent confirmation: daemon settlement reports the exit code and
  diagnostic disposition.
- Persistence check: not applicable to a static gate.
- Evidence path: daemon-owned gate fact in this report.
- Result: pass. The supplied daemon fact carries the exact command, exit code,
  and successful diagnostic disposition.

### R2

```yaml
inputs:
  - kind: repository_path
    ref: internal/worktree/worktree.go
  - kind: repository_path
    ref: internal/worktree/worktree_test.go
  - kind: repository_path
    ref: internal/cli/reconcile.go
```

- Actor and surface: local operator; CLI backed by the Worktree reconciler.
- Entry point and steps: use `roundfix reconcile` on a scratch Run whose work
  was squash-merged into the default branch, delete its target branch, and add
  newer archived-path QA Report content evidence.
- Expected observable: the Run is releasable and the CLI names the archived
  superseding QA Report evidence.
- Independent confirmation: inspect the scratch Git graph and repeat the
  read-only reconcile command.
- Persistence check: classification remains stable across a fresh CLI process.
- Evidence path: focused command output captured in this report.
- Result: pass. The audited HEAD built as `/private/tmp/roundfix-qa-0154-current`
  and reported `roundfix 0.16.0`. A fresh focused run passed
  `TestReconcileFallsBackToTheDefaultBranch`; its scratch repository deletes
  the target after a squash merge, proves the Run Branch is not an ancestor of
  the default branch, and returns the archived report
  `docs/history/specs/0066-run-teardown-reclaims-what-it-created/qa/qa-report-2026-07-29.md`
  as the releasable proof. The broader fresh package run also passed
  `TestReconcileFallbackRequiresArchivedEvidence/archived_Spec_evidence_releases`.

### R3

```yaml
inputs:
  - kind: repository_path
    ref: internal/worktree/worktree.go
  - kind: repository_path
    ref: internal/worktree/worktree_test.go
  - kind: repository_path
    ref: internal/cli/reconcile.go
```

- Actor and surface: local operator; CLI backed by the Worktree reconciler.
- Entry point and steps: repeat the absent-target journey with no archived
  evidence, with active-path-only evidence, with no resolvable default branch,
  and with evidence for another Spec.
- Expected observable: every case stays preserved.
- Independent confirmation: inspect the fixture refs and exact classifications.
- Persistence check: repeat in a fresh package process.
- Evidence path: focused command output captured in this report.
- Result: pass. The built CLI inspected real retained Run
  `run_20260921T160122Z_07824e2f31e39776` from the user checkout and exited 0
  while preserving it; the Run Branch candidate named
  `Spec "0152-one-declared-acceptance-policy": default branch has no
  superseding QA Report after target branch disappeared`. The fresh focused
  package run passed both missing-evidence and unresolved-default subcases,
  `TestReconcileFallbackRequiresArchivedEvidence/active_Spec_evidence_preserves`,
  and `TestClassifyRunBranchSetPreservesBranchWithoutSupersededProof`, which
  covers evidence that cannot prove the Run's own Spec.

### R4

```yaml
inputs:
  - kind: repository_path
    ref: internal/worktree/worktree.go
  - kind: repository_path
    ref: internal/worktree/worktree_test.go
```

- Actor and surface: local operator; CLI and local Worktree state.
- Entry point and steps: make an evidenced absent-target Run dirty and attempt
  the public apply flow.
- Expected observable: apply refuses release and the Worktree remains.
- Independent confirmation: a Git status read and Worktree/branch lookup after
  the command.
- Persistence check: the retained Worktree is still present after a fresh read.
- Evidence path: focused command output captured in this report.
- Result: pass. The built CLI classified real retained Run
  `run_20260921T110125Z_a0e8bf6b87cd6066` as `dirty`, reported one preserved
  result, and emitted no Run Branch candidate. Fresh Git reads found the
  modified `task_02.md`, the Run Branch at `1551b11f`, and the linked Worktree
  still present. The focused `TestReconcileFallsBackToTheDefaultBranch` also
  dirtied an otherwise evidenced scratch Worktree immediately before apply and
  passed its refusal plus retained-branch and retained-Worktree assertions.

### R5

```yaml
inputs:
  - kind: repository_path
    ref: internal/worktree/worktree.go
  - kind: repository_path
    ref: internal/worktree/worktree_test.go
```

- Actor and surface: local operator; CLI backed by the Worktree reconciler.
- Entry point and steps: exercise safe, superseded, unintegrated, and dirty
  Runs while the target branch exists, including active and archived Spec paths.
- Expected observable: every established classification remains unchanged and
  either Spec path remains accepted for a present target.
- Independent confirmation: focused compatibility assertions read the exact
  public classification and reason.
- Persistence check: repeat against a fresh process/fixture.
- Evidence path: focused command output captured in this report.
- Result: pass. The fresh focused package run passed
  `TestInspectTerminalRunSafe`, `TestInspectTerminalRunUnintegrated`,
  `TestInspectTerminalRunClassifiesSupersededQAReport`,
  `TestInspectTerminalRunDirty`,
  `TestClassifyRunBranchSetFourFailedCycles`,
  `TestClassifyRunBranchSetPreservesUnintegratedImplementationWork`, and
  `TestClassifyRunBranchSetPreservesBranchWithoutSupersededProof`. It also
  passed both active- and archived-path subcases of
  `TestReconcilePresentTargetAcceptsEitherSpecPath`.

### R6

```yaml
inputs:
  - kind: repository_path
    ref: internal/worktree/worktree.go
  - kind: repository_path
    ref: internal/worktree/worktree_test.go
```

- Actor and surface: local operator; CLI reason output.
- Entry point and steps: inspect preserved reasons for missing archived
  evidence, active-path-only evidence, and an unresolvable default branch.
- Expected observable: reasons are distinct, name the Spec and missing proof,
  and remain within the existing length bound.
- Independent confirmation: exact-string or required-fragment assertions plus
  byte-length assertions.
- Persistence check: rerun classification from a fresh fixture.
- Evidence path: focused command output captured in this report.
- Result: pass. The public CLI emitted the Spec slug and missing superseding QA
  Report in the real retained Run's preserved candidate. The fresh focused run
  passed both exact-string and byte-bound subcases of
  `TestReconcileReasonNamesTheMissingProof`; the active-only archive-state case
  also passed assertions requiring `archived Spec path` and `Spec is not
  archived` in its distinct reason.

### R7

```yaml
inputs:
  - kind: repository_path
    ref: .agents/skills/roundfix/SKILL.md
  - kind: repository_path
    ref: skills/roundfix/SKILL.md
```

- Actor and surface: Roundfix skill reader; docs.
- Entry point and steps: follow the shipped reconcile guidance and compare its
  canonical and distributed copies.
- Expected observable: both copies describe default-branch fallback, archived
  positive evidence, all preserved cases, and the missing-proof reason; copies
  match.
- Independent confirmation: the public skill checker and byte comparison.
- Persistence check: reload both files after the check.
- Evidence path: focused command output captured in this report.
- Result: pass. `/private/tmp/roundfix-qa-0154-current skills check` reported
  every shipped skill valid; `cmp -s` exited 0; and fresh searches found the
  complete absent-target fallback, positive-evidence rule, unarchived-Spec
  preservation, other-Spec preservation, and missing-proof reason contract at
  lines 818-824 of both byte-identical skill copies.

### R8

```yaml
inputs:
  - kind: repository_path
    ref: docs/specs/0154-evidence-when-the-target-branch-is-gone/_authorization.md
  - kind: repository_path
    ref: .agents/skills/roundfix/SKILL.md
  - kind: repository_path
    ref: skills/roundfix/SKILL.md
```

- Actor and surface: maintainer; Git history and tooling authority.
- Entry point and steps: read every Task commit's actual changed paths with
  `git diff-tree`, then compare governed paths and chronology with the approved
  record.
- Expected observable: governed mutations stay within the approved files plus
  each assigned Task file, and authorization predates implementation.
- Independent confirmation: inspect authorization revision and ancestry.
- Persistence check: repeat the reads from audited HEAD.
- Evidence path: focused Git output captured in this report.
- Result: pass. `git diff-tree --no-commit-id --name-only -r` showed Task 01
  changed its Task file and the two ordinary Worktree Go files; Task 02 changed
  the same ordinary Go files and its Task file; Task 03 changed only the two
  approved skill paths and its assigned Task file; Task 05 changed only the two
  ordinary Worktree Go files and its assigned Task file. The authorization
  commit `891cd45f` changed only `_authorization.md`; its operative revision
  `77c71191` precedes all four Task commits and grants both skill paths.
  `internal/speccheck/governed.go` classifies the skill paths as plugin
  declarations. No prerequisite- or consequent-fix commit exists.

### R9

```yaml
inputs:
  - kind: repository_path
    ref: docs/specs/0154-evidence-when-the-target-branch-is-gone/_prd.md
  - kind: repository_path
    ref: docs/specs/0154-evidence-when-the-target-branch-is-gone/_techspec.md
  - kind: repository_path
    ref: docs/specs/0154-evidence-when-the-target-branch-is-gone/_tasks.md
  - kind: repository_path
    ref: docs/specs/0154-evidence-when-the-target-branch-is-gone/task_04.md
```

- Actor and surface: Spec author; public `roundfix spec check` command.
- Entry point and steps: run the strict consistency check on this Spec.
- Expected observable: no promise-rule finding; Success Metrics and API
  Contracts name a consuming Task through explicit references.
- Independent confirmation: inspect the Task 04 References named by the check.
- Persistence check: rerun the read against audited HEAD.
- Evidence path: strict-check output captured in this report.
- Result: pass. The strict checker exited 0 with no findings, and Task 04's
  `## References` explicitly names PRD Success Metrics 1-3 and TechSpec API
  Contracts 1-3 as consuming coverage.

### R10

```yaml
inputs:
  - kind: external_repository
    ref: /Users/marcio/dev/roundfix
```

- Actor and surface: maintainer; external user checkout and its Git history.
- Entry point and steps: identify a real squash-merged Run Branch whose commit
  is not an ancestor of the default branch while its Spec content exists there.
- Expected observable: external history demonstrates why ancestry cannot prove
  the delivered squash case and why content evidence is required.
- Independent confirmation: compare Run commit, merge base, and archived Spec
  content from the user checkout.
- Persistence check: repeat the read-only Git queries.
- Evidence path: focused Git output captured in this report.
- Result: pass. Source repository `/Users/marcio/dev/roundfix`, which this Spec
  did not build: Run Branch
  `roundfix/run-run_20260921T160122Z_07824e2f31e39776` resolved to
  `eb60e6dbeeb42c2bb6021b08f5fc50ddfe82f047`; `main` resolved to
  `35fb7839338b6a9e3210332025a1cae0f9ebbced`; the target branch lookup failed
  because the ref is absent; and `git merge-base --is-ancestor <run-branch>
  main` exited 1. A fresh `git ls-tree` on `main` nevertheless found three QA
  Reports under `docs/history/specs/0152-one-declared-acceptance-policy/qa/`,
  last changed by archive commit `f3931f68`. This independent repository
  history demonstrates the real squash/deleted-target condition in which
  ancestry cannot establish delivery and archived content evidence is needed.

### R11

```yaml
inputs:
  - kind: live_service
    ref: Pull Request state for feat/0154-evidence-when-the-branch-is-gone
```

- Actor and surface: reviewer; Pull Request controls.
- Entry point and steps: observe approval, checks/status, unresolved threads,
  Merge-Ready acceptance, and review-artifact ancestry for the target branch.
- Expected observable: all five controls are evidenced.
- Independent confirmation: compare each artifact with the claimed head.
- Persistence check: fresh Pull Request read when available.
- Evidence path: supplied environment fact and equivalent supervised evidence
  recorded in this report.
- Result: blocked (environment: no open Pull Request), with equivalent evidence
  for every control:
  - approval: no acceptance was obtained because no Pull Request exists;
  - checks and status: daemon-owned `make verify` exited 0;
  - unresolved review threads: no Pull Request exists, so no review-thread set
    exists to leave unresolved;
  - Merge-Ready acceptance: no Review Source evidence exists yet;
  - review-artifact ancestry: no review artifact or Pull Request head exists,
    so no ancestry claim was made.
  The unblocking action is to open the Pull Request on
  `feat/0154-evidence-when-the-branch-is-gone` after this gate settles, then
  observe all five controls against the Pull Request's actual head.

## Findings

None.

## Blocked and skipped

- R11 is environment-blocked because the QA prompt proves that no Pull Request
  is open. All five Pull Request controls carry explicit equivalent evidence
  above. No matrix row is finding-blocked, declared-blocked, or skipped.

## Coverage

Complete: 10 rows passed and 1 row is environment-blocked with equivalent
evidence. All eight declared verification Requirements and the mandatory
outside-evidence, Pull Request, and repository Verification sources appear in
provenance; no row covers another source. Requirement 9 constrains every row's
evidence and is not a declared verification source. The PRD declares no
Unreachable Acceptance and no frontend surface. Counts:
`rows_blocked_environment: 1`, `rows_blocked_finding: 0`,
`rows_blocked_declared: 0`; failed: 0; skipped: 0.

## Mechanical skips

| Detector | Missing artifact |
| --- | --- |
| authorization bounded paths | Task commits |
| consequent-fix commit order | consequent-fix declaration |
| QA evidence paths | docs/specs/0154-evidence-when-the-target-branch-is-gone/qa/evidence |

## Final verdict

Pass. Every runnable row passed; the sole environment-blocked Pull Request row
records its cause, unblocking action, and equivalent evidence for all controls.
There are no findings, declared blocks, finding blocks, skipped rows, or
unresolved evidence paths.
