---
spec: 0148-a-profile-that-declares-both-tiers
date: 2026-09-19
build: 4bd34d51dd0e5cbabde8d94412e578d92ecc5b50
auditing_binary: "0.14.1 (eaae7abc, built 2026-09-19 11:31:18 -0300)"
auditor_staleness: "stale: commit ancestry: build commit predates audited tree"
status: closed
verdict: pass
rows_blocked_environment: 1
rows_blocked_finding: 0
rows_blocked_declared: 0
surfaces: [backend, docs]
---

# QA Report

## Scope and environment

- Full gate for the terminal `task_04` node at `4bd34d51dd0e5cbabde8d94412e578d92ecc5b50`.
- Actor: a maintainer or Agent reading a Baseline Profile through the built `roundfix` CLI.
- Public entry points: `roundfix baseline plan`, `roundfix baseline profile show`, and `roundfix baseline profile validate`.
- The strict authoring precondition passed. The repository Verification is the Daemon-supervised result supplied to this gate and is not rerun.
- The auditing binary predates the audited tree, as recorded in frontmatter. A fresh QA binary is built from the audited tree for public CLI journeys.

## Static gate

- `roundfix spec check 0148-a-profile-that-declares-both-tiers --strict`: pass; no findings and authored Verification commands were not executed.
- `make verify`: pass, exit 0. The Daemon ran this outside the Agent sandbox and removed diagnostics on success; this gate did not rerun it.
- `go test -count=1 -run '^TestProfileDeclaresBothVerificationTiers$' ./internal/baseline`: pass.
- `go test -count=1 -run '^TestCatalogCompatibility$|^TestBaselinePlanCharacterization$' ./internal/baseline`: pass.
- `make baseline-digests` in a disposable clone of the audited build: pass with `{"schemaVersion":1,"type":"baseline-digests","ok":true,"changed":false}`.

## Performed repairs

None.

## Assigned repair failures

None.

## Authorization audit inputs

None.

## Mechanical findings

None.

## Results

| # | Story / criterion / sweep | Actor and surface | Status | Evidence | Provenance |
| - | --- | --- | --- | --- | --- |
| R1 | Record the repository Verification result | Daemon / repository gate | pass | Daemon-supervised `make verify` exited 0 | Requirement 1; repository Verification |
| R2 | Both tiers are declared beside one another and remain independent | Maintainer / Baseline CLI | pass | Public plan emitted separate gate and incremental projections; independence regression passed | Requirement 2 |
| R3 | A Profile without an incremental tier reports true absence | Maintainer / Baseline CLI | pass | Public Go Profile plan emitted only the gate and `incremental_count: 0`; absence regression passed | Requirement 3 |
| R4 | Derived catalog and plan artifacts agree with the Profile and sanctioned regeneration | Maintainer / Baseline regeneration contract | pass | Compatibility tests passed; sanctioned regeneration reported `changed:false` in a disposable clone | Requirement 4 |
| R5 | Publication paths remain untouched | Maintainer / Git-visible delivery surface | pass | Exact delivery-range diff was empty and named blob IDs matched | Requirement 5 |
| R6 | The shipped clauses' requested value now exists, while guide publication remains out of scope | Agent / shipped guidance and Baseline CLI | pass | Two named shipped clauses agreed with the public projection; publication token remained absent | Requirement 6; outside-evidence row |
| R7 | Spec promises and Task commits remain inside the approved authority | Maintainer / Spec and Git history | pass | Strict Spec check passed; approval predated the governed mutation; every Task commit path was accounted for | Requirement 7 |
| R8 | Pull Request controls for the Spec target branch | Maintainer / Pull Request | blocked (environment: no open Pull Request) | Prompt-supplied environment fact plus equivalent supervised control evidence below | Pull Request row |

## Row evidence

### R1

```yaml
inputs:
  - kind: repository_path
    ref: "**"
```

Expected: the repository Verification exits 0. Independent confirmation: the Daemon records the exact exit status outside the Agent sandbox. Persistence: the fact applies to the audited build named above.

Observed: the supplied Daemon fact records `make verify` passing at exit 0, with diagnostics removed on success. This row does not infer the result from narrower checks.

### R2

```yaml
inputs:
  - kind: repository_path
    ref: "**"
```

Expected: a public Baseline plan projects `verification.incremental` and `verification.gate` as distinct records with independent commands and sources. Independent confirmation: the focused reader test changes each tier separately. Persistence: rerunning the plan from the same committed fixture produces the same projections.

Observed: a CLI binary built from the audited tree ran `baseline plan --profile standard-typescript-monorepo --format json` against a disposable clone fixture. The plan emitted `verification.gate` as `repository-gate`, command `make verify`, classification `repository-command`, and `verification.incremental` as `incremental`, command `make verify-incremental`, classification `profile-expectation`; both named `Makefile` independently. A fresh second plan produced the same projection. `TestProfileDeclaresBothVerificationTiers` passed and changed each tier separately while comparing the other's full value and source.

### R3

```yaml
inputs:
  - kind: repository_path
    ref: "**"
```

Expected: a Profile without `verification.incremental` emits no projection for it, while its complete gate remains. Independent confirmation: a fresh public profile validation and focused reader test. Persistence: the committed characterization retains the absent shape.

Observed: `baseline plan --profile go-cli-tui --format json` on the same disposable clone emitted only `verification.gate` and the focused JSON read returned `incremental_count: 0`; it emitted neither an empty incremental value nor the gate under the incremental identifier. `baseline profile validate go-cli-tui --format json` returned `state: valid`. The focused reader regression also removed the incremental declaration and compared every remaining projection byte-for-structure.

### R4

```yaml
inputs:
  - kind: repository_path
    ref: "**"
```

Expected: fresh compatibility and characterization checks accept the derived files, and Git chronology identifies `make baseline-digests` as the sanctioned producer. Independent confirmation: catalog digest and changed-path inspection. Persistence: the generated artifacts are committed at the audited build.

Observed: the two fresh tests passed. Running the sanctioned `make baseline-digests` command in a disposable clone of `4bd34d51` returned `ok:true, changed:false`, independently confirming that the committed catalog digest, normalized catalog, and four plan characterizations match their canonical sources. Commit `4bd34d51` contains only `task_02.md` and those six derived artifacts; its recorded Task result names the same sanctioned command and its original `changed:true` result.

### R5

```yaml
inputs:
  - kind: repository_path
    ref: "**"
```

Expected: the Spec commits have no changes in any publication path. Independent confirmation: compare each named path's blob at the pre-Task parent and audited head. Persistence: Git ancestry carries the unchanged blobs.

Observed: `git diff --quiet 50666b84^ 4bd34d51 -- docs/agents internal/baseline/assets/templates <formatter-golden>` exited 0. The guide template, template index, setup decision record, and formatter golden retained blob IDs `8d96424b`, `be66fe1d`, `e2a0073b`, and `887fd0ea`. The first audit script invocation stopped before blob comparison because `path` is a reserved zsh array; the corrected evidence command used `target_path` and exited 0 without changing repository bytes.

### R6

```yaml
inputs:
  - kind: repository_path
    ref: "**"
```

Expected: both shipped clauses name a declared incremental command, the public plan projects that value, and the generated guides still do not publish it. Independent confirmation: the two separately owned clauses and public CLI output agree. Persistence: the evidence is committed at the audited build.

Observed outside evidence: `docs/agents/agent-instructions.md:30` requires the incremental command declared by the active Baseline Profile and says omission leaves the clause unmet; `docs/agents/spec-routing.md:15` independently requires the same declared command for each Task. These shipped guides predate and sit outside this Spec's own artifacts. The public plan supplied the requested value as `make verify-incremental`. A negative search confirmed that `verification.incremental` and `make verify-incremental` remain absent from the generated guides, guide template, template index, setup decision record, and formatter golden, preserving the Spec's publication boundary.

### R7

```yaml
inputs:
  - kind: repository_path
    ref: "**"
```

Expected: strict Spec checking passes; authorization predates governed mutation; every Task commit changes only its Task file, authorized governed paths, or ordinary source/derived evidence in its declared slice. Independent confirmation: inspect every Task commit with `git diff-tree`. Persistence: commit ancestry fixes ordering and scope.

Observed: `roundfix spec check 0148-a-profile-that-declares-both-tiers --strict` exited 0 with no findings and did not execute authored Verification. The authorization visible at `50666b84^` was approved on 2026-09-19, consumed by this Spec, permitted `implement` and `commit`, and bounded the governed Profile asset before Task 01 changed it. `git diff-tree` showed Task 01 changed its Task file, the authorized Profile asset, and ordinary Go test source; the intervening repair changed only `task_02.md`; Task 02 changed its Task file and six ordinary derived outputs. The later narrowing retains the sole governed path this reduced Spec used. No authorization, prerequisite fix, or consequent fix was folded into either Task commit.

### R8

```yaml
inputs:
  - kind: external_repository
    ref: feat/0148-both-tiers Pull Request state
  - kind: live_service
    ref: GitHub Pull Request controls
```

Expected: approval, checks/status, unresolved threads, Merge-Ready acceptance, and review-artifact ancestry are observed. The prompt states that no Pull Request is open, so this row uses the equivalent-evidence path or remains environment-blocked where a control has no equivalent evidence.

Observed cause: the Roundfix QA prompt states, "Pull Request: none open; Pull Request journeys are environment-blocked." Unblocking action: open the Spec Pull Request on `feat/0148-both-tiers` after the authored delivery sequence reaches that step.

Equivalent supervised controls:

- Approval: no Pull Request acceptance was obtained; the 2026-09-19 authorization is scope authority, not review approval.
- Checks and status: the Daemon-supervised repository Verification, `make verify`, passed at exit 0 for the audited candidate.
- Unresolved review threads: none stands because no Pull Request or review-thread surface exists yet.
- Merge-Ready acceptance: none exists yet; this gate makes no Merge-Ready claim.
- Review-artifact ancestry: no review artifact exists yet, so no artifact claims a different head. The audited candidate is `4bd34d51dd0e5cbabde8d94412e578d92ecc5b50`, and the Spec target branch named by the prompt is `feat/0148-both-tiers`.

## Mechanical skips

| Detector | Missing artifact |
| --- | --- |
| authorization bounded paths | Task commits |
| consequent-fix commit order | consequent-fix declaration |
| QA Report structure | QA Report |
| QA evidence paths | QA Report |

## Findings

None.

## Blocked and skipped

- R8 is blocked by the declared environment fact that no Pull Request is open. All five Pull Request controls have equivalent observed or supervised evidence above. No row is finding-blocked, declared-blocked, or skipped.

## Coverage

- Coverage sources: 10 of 10 covered — Requirements 1-7, repository Verification, outside-evidence row, and Pull Request row. Requirement 1 and repository Verification share R1; Requirement 6 and outside evidence share R6.
- Results: 7 pass, 1 blocked (environment), 0 fail, 0 finding-blocked, 0 declared-blocked, 0 skipped, 0 pending.
- No PRD Unreachable Acceptance declarations exist. The PRD declares no frontend surface, so no frontend sweep applies.
- No default risk probes or Non-Goal sources apply because the authored QA Task declares the matrix through seven `MUST run` or `MUST verify` Requirements.
- Block counts: `rows_blocked_environment: 1`, `rows_blocked_finding: 0`, `rows_blocked_declared: 0`.

## Final verdict

Pass. Every runnable criterion passed; the sole environment-blocked Pull Request row records its cause and equivalent supervised evidence, and the delivery workflow must establish live Pull Request approval, review, and Merge-Ready controls after a Pull Request opens.
