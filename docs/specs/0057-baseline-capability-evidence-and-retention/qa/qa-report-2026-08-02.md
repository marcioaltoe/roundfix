---
spec: 0057-baseline-capability-evidence-and-retention
date: 2026-08-02
build: 431251809cc8b05f6006f9b5c36f35744aa391be
status: closed
verdict: fail
rows_blocked_environment: 1
rows_blocked_finding: 13
surfaces: [backend, cli, docs]
---

# QA report — Baseline capability evidence and retention

## Scope and environment

Full gate against the Run Worktree build on macOS in
`/Users/marcio/.roundfix/worktrees/roundfix-339f8dac/run_20260802T151023Z_fe336345b404c5b0`.
The actor is a repository maintainer using the built `roundfix` CLI against
scratch repositories. Pull Request journeys are environment-blocked because the
QA prompt confirms that no Pull Request is open. No commit, push, Pull Request,
release, or external repository mutation is authorized.

Evidence root:
`qa/evidence/2026-08-02-spec-0057/`.

Flow QA stopped after the complete Project Constraint and tooling audit, as
required by the gate contract. The active PRD does not account for ADR-0087,
which governs one of this Spec's core behaviors. The audit also found completed
Task artifacts that explicitly record missing implementation. No public CLI
journey was run past those findings.

## Planned matrix

Every row starts pending. Rows are ordered by risk and user impact.

| # | Story / criterion / sweep | Actor and entry point | Surface | Steps and probes | Expected observable | Independent confirmation and persistence | Evidence | Status |
| - | --- | --- | --- | --- | --- | --- | --- | --- |
| 1 | Project Constraints: identifier strategy, authentication and HTTP, active ADRs, tooling authority | QA agent; PRD, TechSpec, operative `docs/agents/` sources | docs | Reconcile both Spec artifacts with active clauses and ADRs | Each category states applicability with a reason and cites an operative source | Compare PRD and TechSpec against accepted ADR bodies and repository guidance | F-001; `_prd.md:37-44`, `_techspec.md:41-47`, ADR-0087 | fail |
| 2 | Protected-tooling authorization, bounded paths, and commit choreography | QA agent; Git ancestry from Spec authoring through Task commits | backend | Resolve every Task commit with `git diff-tree`; inspect authorization and digest-regeneration ancestry | Only expressly authorized tooling files and deterministic fallout changed, with authorization before Task mutation | Cross-check worktree delta and chronological ancestry; verify sanctioned regeneration evidence if pins changed | `git -c core.fsmonitor=false diff-tree --no-commit-id --name-only -r <commit>` for all 13 Task commits | pass |
| 3 | US1: same-identity Profile or catalog drift accounts every previous clause or stops action-required | Maintainer; built `roundfix baseline` against a scratch repository | cli | Adopt, change digests under the same identity, remove one known clause; probe replay and invalid order | Unaccounted clause is named and counted; no apply is offered; fully accounted drift remains ready with a non-empty ledger | Reopen the generated plan/result through the public CLI and re-run from unchanged scratch state | F-001 stopped flow QA | blocked (finding: F-001) |
| 4 | Clause-level delta renders before the unchanged file ledger | Maintainer; consolidated Baseline review | cli | Produce accounted and unaccounted drift; inspect ordering and counts | Every previous clause and disposition renders before the file ledger; counts sum; unaccounted identity is visible | Compare machine plan data with human output and repeat plan | F-001; F-002 (`3230efb` changed only `task_09.md`) | blocked (finding: F-001) |
| 5 | US2: declared-file, executable, and required-stack divergences render their evaluated probe evidence | Maintainer; `roundfix baseline` plan/re-check | cli | Trigger each probe kind; include absent and failed executable candidates | Inspected paths/states/expected content or PATH candidate appear; stack output names selected technology, both resolutions, and decision cascade | Compare human output with additive machine divergence probe/evidence fields | F-001; F-002 (`6192029`) | blocked (finding: F-001) |
| 6 | Requirement-strength grouping and advisory language | Maintainer; mixed divergence set through public CLI | cli | Trigger blocking, advisory, and informational divergences; inspect order | Both human and machine output group all three strengths; every advisory says it does not block readiness or apply before its next action | Re-run with advisory-only data and confirm readiness remains unchanged | F-001; F-002 (`6192029`) | blocked (finding: F-001) |
| 7 | US3: bounded symlink discovery never executes candidates | Maintainer; Baseline executable capability probe with scratch PATH | backend, cli | Direct file, one-hop, multi-hop, cycle, broken link, non-executable target, absence; invocation-marker probe | Valid links satisfy; cycle, broken-link, non-executable, and absence are distinct; candidate is reported; marker stays absent | Inspect marker and run again after recreating PATH chain | F-001 stopped flow QA; static gate only | blocked (finding: F-001) |
| 8 | US4: read-only capability re-check needs zero decisions and matches full-plan outcomes | Maintainer; documented re-check command | cli | Run with unresolved decisions; byte-snapshot repository and journal; invalid no-Profile repository | Same capability outcomes and rendering as full plan; no writes; named no-Profile error | Compare tree and journal before/after, then compare machine outcomes field by field | F-001; F-002 (`4205b14`) | blocked (finding: F-001) |
| 9 | US5: remediate-and-re-run is a fourth distinct prompt outcome | Maintainer; Profile-divergence prompt | cli | Select remediate outcome on mixed divergences; compare decline/adapt/proceed; double-select probe | Exits without repository writes, prints each remediation and re-check command, journals distinctly; adaptation says removal-only | Inspect repository and journal, then reopen recorded outcome | F-001; F-002 (`604f958`) | blocked (finding: F-001) |
| 10 | US6: current managed carriers do not warn; unmanaged and unclassifiable carriers still warn | Maintainer; apply then idempotent re-plan | cli | Verified apply/re-plan; add unmanaged nested carrier, stale managed guide, recognized extension, unknown marker | Clean re-plan has zero managed-guide warnings; only positive classifications suppress warnings; remaining warning names managed/preserved boundary | Repeat plan after fresh load and inspect Source Baseline retention input | F-001 stopped flow QA; static gate only | blocked (finding: F-001) |
| 11 | Portable Verification role mapping requires a declared command and never executes it | Maintainer; repository Profile alignment through public Baseline path | backend, cli | Mapped-present, unmapped, mapped-absent; marker-producing declared command | Present mapping satisfies and records command; absent/unmapped stays divergent; marker remains absent | Inspect machine alignment result and marker after repeated plan | F-001 stopped flow QA; static gate only | blocked (finding: F-001) |
| 12 | US7: result status matrix reports five evidence axes without optimistic completion | Maintainer; first apply and idempotent reapply | cli | Run first apply, retention-free reapply, retention-accounted apply, idempotent reapply | Five axes each read `verified` or `not run`; completion requires verified retention plus verified idempotence | Compare human and machine output; reopen result and repeat exact preimage | F-001 stopped flow QA; static gate only | blocked (finding: F-001) |
| 13 | US8 and Non-Goal: ready plans remain ready; different-Profile transition keeps default-branch outcome | Maintainer; public adopt-update-change-Profile journey | cli | Adopt, update, change Profile, repeat; invalid/out-of-order probe | Journey completes and Setup Manifest records the new Profile; same-identity retention gate remains intact | Re-plan from persisted manifest and run public macro characterization | F-001 stopped flow QA; static gate only | blocked (finding: F-001) |
| 14 | Machine contracts are additive and preimage/digest/apply fail-closed behavior is unchanged | Automation consumer; JSON plan/apply outputs | cli | Decode old fields plus new probe/evidence/group/delta/status fields; tamper digest/preimage | Prior fields keep names and meanings; tampering is rejected; plan and re-check execute no repository Verification | Fresh decode through public JSON path and unchanged-tree confirmation | F-001 stopped flow QA; static gate only | blocked (finding: F-001) |
| 15 | Documentation journey is runnable and matches shipped behavior | Maintainer; `docs/user-guide/commands.md` | docs, cli | Follow re-check, four outcomes, retention, matrix, symlink, and advisory instructions without source knowledge | Every documented command and behavior exists as written | Compare `--help`, real command output, and documentation after fresh read | F-001; F-002; docs commit `4312518` claims behavior absent from Task Results | blocked (finding: F-001) |
| 16 | Repository static Verification | Maintainer; repository root | backend, cli, docs | Run exact authoritative `rtk make verify` once, without a pipeline | Full gate exits 0 with no failures | Record exit code and inspect final worktree status | `rtk make verify`: exit 0; 3,041 Go tests, 4 Skill tests, Skill check, build | pass |
| 17 | Pull Request approval, checks, review threads, Merge-Ready, and review-artifact ancestry | Maintainer; Open Pull Request | cli, docs | Read-only PR inspection | Open PR evidence proves each required state | Prompt states no Pull Request is open; equivalent PR evidence is unavailable | QA prompt: `Pull Request: none open` | blocked (environment: no open Pull Request) |

Behavior probes are concentrated on the high-risk journeys: repeat/replay for
retention and idempotence, invalid or out-of-order Profile transitions, absent
versus failed executable candidates, no-Profile re-check, double selection at
the divergence prompt, and tampered digest/preimage data.

## Static gate

`rtk make verify` exited 0 on build `431251809cc8b05f6006f9b5c36f35744aa391be`:

- `rtk go test ./...`: 3,041 tests passed in 24 packages.
- The four selected Skill contract tests passed.
- `rtk go run -buildvcs=false ./cmd/roundfix skills check`: passed for all
  required repository Skills.
- `rtk go build -buildvcs=false ... -o bin/roundfix ./cmd/roundfix`: passed.

The build identified itself as `4312518-dirty` because the in-progress QA
Report was an untracked worktree artifact when the gate ran. No format, test,
Skill, or build failure was reported.

## Results

| # | Story / criterion / sweep | Actor and surface | Status | Evidence |
| - | --- | --- | --- | --- |
| 1 | Project Constraint audit | QA agent; docs | fail | F-001: the active PRD omits governing ADR-0087 |
| 2 | Protected-tooling audit | QA agent; Git | pass | All authoring and Task commits inspected with `diff-tree`; no protected tooling changed |
| 3–15 | PRD user stories, criteria, Non-Goals, and public surface sweeps | Maintainer; backend/CLI/docs | blocked (finding: F-001) | Mandatory audit stop; F-002 independently affects rows 4–6, 8–9, and 15 |
| 16 | Repository Verification | Maintainer; backend/CLI/docs | pass | `rtk make verify`, exit 0 |
| 17 | Pull Request journey | Maintainer; PR/docs | blocked (environment: no open Pull Request) | QA prompt confirms no open Pull Request; no equivalent PR evidence exists |

## Findings

### F-001 — Active PRD omits the executable-discovery ADR

- Impact: Trust-Damage.
- Actor and step: QA agent, Project Constraint audit before flow QA.
- Expected: the active PRD and TechSpec each account for every applicable
  active ADR and cite `docs/agents/domain.md`.
- Actual: the TechSpec's Active ADR obligations include ADR-0087 and its
  executable-discovery design repeatedly cites that ADR. Task 02 also makes
  ADR-0087 normative. The PRD's Active ADR obligations stop at ADR-0075 and do
  not account for ADR-0087.
- Reproduction: compare `_prd.md:37-44`, `_techspec.md:41-47`,
  `task_02.md:23-26`, and
  `docs/adr/0087-executable-discovery-resolves-links-without-executing.md`.
- Evidence: commit `8fa601a` added ADR-0087 and the TechSpec together after the
  current PRD constraint snapshot; `rtk grep -n "ADR-0087|Active ADR obligations" ...`
  shows the omission.
- Affected rows: 1 and, by the mandatory audit stop, 3–15.

### F-002 — Completed Task artifacts record absent implementation

- Impact: Blocks-Completion.
- Actor and step: maintainer expecting completed Tasks to supply the public
  Baseline journeys required by the Spec.
- Expected: every completed Task has implemented behavior, reproducible Result
  evidence, and an implementation-bearing Task commit.
- Actual: Task 04 says probe rendering and grouping were not implemented;
  Task 06 says the re-check was not implemented; Task 07 says the fourth prompt
  outcome was not implemented. Their commits (`6192029`, `4205b14`, `604f958`)
  changed only their own Task files. Task 09 is also completed, has no
  `## Result`, and commit `3230efb` changed only `task_09.md`. Task 12 then
  documents those behaviors as shipped.
- Reproduction: read each affected Task's `## Result`, then run
  `rtk git -c core.fsmonitor=false diff-tree --no-commit-id --name-only -r`
  for the four commits above.
- Evidence limitation: the public CLI reproduction was not run because F-001
  requires flow QA to stop first. No pass or fail credit is inferred from
  source inspection; affected flow rows remain finding-blocked.
- Affected rows: 4–6, 8–9, and 15.

## Blocked and skipped

- Rows 3–15 are `blocked (finding: F-001)`. Unblocking requires updating the
  active PRD Project Constraint snapshot to account for ADR-0087, correcting
  the false Task settlements through properly scoped corrective Tasks, and
  starting a new QA run against the corrected build.
- Row 17 is `blocked (environment: no open Pull Request)`. The QA prompt is the
  proof. Unblocking requires an Open Pull Request on
  `ma/baseline-capability-evidence-and-retention`, followed by read-only checks
  of approval, checks, unresolved review threads, Merge-Ready evidence, and
  review-artifact ancestry. No equivalent PR evidence was available.
- No row was skipped. No row remains pending.

## Coverage

17/17 rows are terminal: 2 pass, 1 fail, 13 finding-blocked, 1
environment-blocked, 0 skipped, and 0 pending. All 8 user stories, 12 Core
Features, stated Non-Goals, Success Metrics, Project Constraints, static gate,
and Pull Request journey were mapped. No public user story earned pass credit
because the mandatory pre-flow audit failed.

## Final verdict

Fail. The Project Constraint audit failed and 13 flow rows are finding-blocked;
repair F-001 and F-002, then rerun the full gate on the corrected build. The
green repository Verification is focused static evidence only and does not
establish the missing public Baseline journeys.
