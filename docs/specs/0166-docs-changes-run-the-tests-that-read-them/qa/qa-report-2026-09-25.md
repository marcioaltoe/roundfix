---
spec: 0166-docs-changes-run-the-tests-that-read-them
date: 2026-09-25
build: de52445ee3263860cd34f17c18a8f5f93ff4de85
auditing_binary: "0.16.0"
auditor_staleness: "current: declared tree version: auditing binary 0.16.0 does not predate 0.16.0"
status: closed
verdict: pass
rows_blocked_environment: 1
rows_blocked_finding: 0
rows_blocked_declared: 0
surfaces: [backend]
---

# QA Report

## Performed repairs

None.

## Assigned repair failures

None.

## Authorization audit inputs

None.

## Mechanical findings

None.

## Scope and environment

- Full terminal QA gate for committed candidate `de52445ee3263860cd34f17c18a8f5f93ff4de85` on Run Worktree branch `roundfix/run-run_20260925T124336Z_67c72420783312e5`.
- Actor surface: repository maintainer through the public `verify-select` and Spec Consistency Check CLIs; reviewer controls through equivalent evidence because the assignment states that no Pull Request is open.
- The current-source `/private/tmp/roundfix-0166-qa` binary reported `roundfix 0.16.0` and ran the strict precondition. The installed PATH binary reports 0.15.0 and was not used for the matrix.
- Scratch clones used committed Git changes and task-scoped Go caches. `GOPROXY=off GOSUMDB=off` kept the journeys hermetic against cached dependencies.
- The first disposable commit attempt inherited GPG signing and failed because the sandbox cannot access the host keyring. It wrote no commit; every scratch journey used `commit.gpgsign=false` for its isolated fixture commit.

## Static gate

- `/private/tmp/roundfix-0166-qa spec check 0166-docs-changes-run-the-tests-that-read-them --strict` exited 0 twice and reported no findings without executing authored Verification commands.
- The Daemon ran `make verify-changed` outside the Agent sandbox and reported exit 0. Success diagnostics were removed. The Agent did not rerun repository Verification.

## Results

| # | Status | Provenance |
| - | --- | --- |
| QA-01 | pass | Requirement 1; repository Verification |
| QA-02 | pass | Requirement 2 |
| QA-03 | pass | Requirement 2 |
| QA-04 | pass | Requirement 3 |
| QA-05 | pass | Requirement 4 |
| QA-06 | pass | outside evidence |
| QA-07 | blocked (environment: no open Pull Request) | Pull Request |

## Planned row evidence

### QA-01

```yaml
inputs:
  - kind: repository_path
    ref: "**"
```

- Actor and surface: Daemon; repository verification gate.
- Entry point: `make verify-changed`, run outside the Agent sandbox.
- Steps: record the Daemon-provided command, exit status, and retained diagnostics.
- Expected observable: exit 0 with no retained success diagnostics.
- Independent confirmation: the QA assignment's repository Verification fact.
- Persistence check: bind the result to audited head `de52445ee3263860cd34f17c18a8f5f93ff4de85`.
- Evidence: the Daemon ran `make verify-changed` outside the Agent sandbox and reported exit 0; success diagnostics were removed. The Agent did not rerun this command.

### QA-02

```yaml
inputs:
  - kind: repository_path
    ref: cmd/verify-select/**
  - kind: repository_path
    ref: internal/verifyselect/**
  - kind: repository_path
    ref: go.mod
  - kind: repository_path
    ref: go.sum
```

- Actor and surface: maintainer; `verify-select` CLI.
- Entry point: `go run ./cmd/verify-select -base HEAD~1` in a scratch clone.
- Steps: create a commit that changes only `docs/user-guide/run-database-lifecycle.md`, then run the selector from the committed candidate.
- Expected observable: exit 0 and output containing both `core` and `baseline`.
- Independent confirmation: inspect the scratch commit's changed paths and rerun the command.
- Persistence check: rerun against the same committed scratch head.
- Evidence: scratch head `a27ba4596bfbd9bd680e3ed0def3bae186f90958`; `git diff-tree --no-commit-id --name-only -r HEAD` printed only `docs/user-guide/run-database-lifecycle.md`. Two consecutive `GOCACHE=/private/tmp/roundfix-0166-docs-gocache GOPROXY=off GOSUMDB=off go run ./cmd/verify-select -base HEAD~1` runs each exited 0 and printed `core` then `baseline`.

### QA-03

```yaml
inputs:
  - kind: repository_path
    ref: cmd/verify-select/**
  - kind: repository_path
    ref: internal/verifyselect/**
  - kind: repository_path
    ref: go.mod
  - kind: repository_path
    ref: go.sum
```

- Actor and surface: maintainer; `verify-select` CLI.
- Entry point: `go run ./cmd/verify-select -base HEAD~1` in a scratch clone.
- Steps: create a commit that changes only `README.md`, then run the selector from the committed candidate.
- Expected observable: exit 0 and output containing both `core` and `baseline`.
- Independent confirmation: inspect the scratch commit's changed paths and rerun the command.
- Persistence check: rerun against the same committed scratch head.
- Evidence: scratch head `03aaf6d16663b0b971bb960a81a65cc94645a08a`; `git diff-tree --no-commit-id --name-only -r HEAD` printed only `README.md`. Two consecutive `GOCACHE=/private/tmp/roundfix-0166-readme-gocache GOPROXY=off GOSUMDB=off go run ./cmd/verify-select -base HEAD~1` runs each exited 0 and printed `core` then `baseline`.

### QA-04

```yaml
inputs:
  - kind: repository_path
    ref: cmd/verify-select/**
  - kind: repository_path
    ref: internal/verifyselect/**
  - kind: repository_path
    ref: go.mod
  - kind: repository_path
    ref: go.sum
```

- Actor and surface: maintainer; `verify-select` CLI.
- Entry point: `go run ./cmd/verify-select -base HEAD~1` in a scratch clone.
- Steps: create a commit that changes only `internal/app/README.md`, then run the selector from the committed candidate.
- Expected observable: exit 0 and output containing `core` but not `baseline`.
- Independent confirmation: inspect the scratch commit's changed paths and rerun the command.
- Persistence check: rerun against the same committed scratch head.
- Evidence: scratch head `8ae528ec0829a299bc9fd03cb3a87b8d3b6540ba`; `git diff-tree --no-commit-id --name-only -r HEAD` printed only `internal/app/README.md`. Two consecutive `GOCACHE=/private/tmp/roundfix-0166-package-gocache GOPROXY=off GOSUMDB=off go run ./cmd/verify-select -base HEAD~1` runs each exited 0 and printed only `core`.

### QA-05

```yaml
inputs:
  - kind: repository_path
    ref: docs/specs/0166-docs-changes-run-the-tests-that-read-them/_prd.md
  - kind: repository_path
    ref: docs/specs/0166-docs-changes-run-the-tests-that-read-them/_techspec.md
  - kind: repository_path
    ref: docs/specs/0166-docs-changes-run-the-tests-that-read-them/_tasks.md
  - kind: repository_path
    ref: docs/specs/0166-docs-changes-run-the-tests-that-read-them/task_01.md
  - kind: repository_path
    ref: docs/specs/0166-docs-changes-run-the-tests-that-read-them/task_02.md
```

- Actor and surface: Spec author; Spec Consistency Check CLI.
- Entry point: worktree-built Roundfix 0.16.0.
- Steps: run `spec check 0166-docs-changes-run-the-tests-that-read-them --strict` without executing authored Verification commands.
- Expected observable: exit 0 with no findings, including no promise-to-Task coverage finding.
- Independent confirmation: inspect the emitted skipped-detector list to distinguish detectors that did not run.
- Persistence check: rerun the same command against the unchanged head.
- Evidence: `/private/tmp/roundfix-0166-qa spec check 0166-docs-changes-run-the-tests-that-read-them --strict` exited 0 twice against the audited head and reported `No findings. Authored Verification commands were not executed.` No promise-to-Task coverage detector was skipped. The emitted skips were limited to missing rollup, backlog, Vocabulary Contract, and references index inputs.

### QA-06

```yaml
inputs:
  - kind: repository_path
    ref: docs/history/specs/0155-a-verify-that-runs-what-changed/_prd.md
  - kind: repository_path
    ref: docs/history/specs/0155-a-verify-that-runs-what-changed/qa/qa-report-2026-09-24-01.md
```

- Actor and surface: maintainer; archived predecessor evidence.
- Entry point: Spec 0155's archived PRD and terminal QA report, which this Spec did not author.
- Steps: inspect the recorded documentation-only escape and its independent selector reproduction.
- Expected observable: the prior shipped behavior let documentation-only changes select no set, establishing the defect this Spec corrects.
- Independent confirmation: trace the archived report to its recorded build and command output.
- Persistence check: compare both archived inputs at the audited head.
- Evidence: the archived Spec 0155 PRD records that a `docs/**`-only or root-Markdown-only change selected no set and names a `go run ./cmd/verify-select -base HEAD~1` reproduction. Its terminal report binds the predecessor gate to build `1d7410b92e400b5e7d26008aec0096462b9fa15c`; `git log` binds both archived artifacts to predecessor delivery commit `d0ab2637f0ca8240df7f9da06716ae59f368e4d8`, and `git diff --exit-code 51232b1a..HEAD -- docs/history/specs/0155-a-verify-that-runs-what-changed/_prd.md docs/history/specs/0155-a-verify-that-runs-what-changed/qa/qa-report-2026-09-24-01.md` exited 0. Fresh independent reproduction from that predecessor delivery created scratch head `f931cf5a6dc848ea1afd9efb105819b8b6661e26`; its diff contained only `README.md`, and two selector runs each exited 0 with a zero-byte stdout file. This evidence originates in the predecessor delivery and its archived review, not in Spec 0166's artifacts.

### QA-07

```yaml
inputs:
  - kind: live_service
    ref: Pull Request for fix/0166-docs-changes-run-their-tests
```

- Actor and surface: reviewer; Pull Request controls.
- Entry point: the QA assignment's Pull Request fact.
- Steps: record the environmental cause and equivalent evidence for approval, checks and status, unresolved review threads, Merge-Ready acceptance, and review-artifact ancestry.
- Expected observable: no Open Pull Request exists; every unavailable control has an explicit equivalent observation bound to the expected head.
- Independent confirmation: compare the Run Worktree head and Spec target branch head.
- Persistence check: live state is non-carriable and must be re-observed on a later run.
- Evidence: the QA assignment states that no Pull Request is open and Pull Request journeys are environment-blocked. Equivalent controls: approval — no acceptance was obtained; checks and status — the Daemon reported `make verify-changed` exit 0 for this candidate; unresolved review threads — none can stand because no Pull Request exists; Merge-Ready acceptance — no Review Source evidence or acceptance exists yet; review-artifact ancestry — no review artifact exists yet. The claimed candidate is `de52445ee3263860cd34f17c18a8f5f93ff4de85`; target branch `fix/0166-docs-changes-run-their-tests` is `2413acf4827d1231a111b494f5bfbfa9b513e328`, and `git merge-base --is-ancestor fix/0166-docs-changes-run-their-tests HEAD` exited 0. Unblocking action: open the Spec Pull Request from the target branch after the Daemon integrates the candidate, then observe its live controls read-only.

## Mechanical skips

| Detector | Missing artifact |
| --- | --- |
| authorization bounded paths | tooling authorization |
| consequent-fix commit order | consequent-fix declaration |
| QA Report structure | QA Report |
| QA evidence paths | QA Report |

These are the seeded mechanical detector skips for missing pre-Agent inputs,
not skipped QA matrix rows. They remain preserved as supplied.

## Findings

None.

## Blocked and skipped

- QA-07 is blocked by the declared environment fact that no Pull Request is open. Its row records equivalent evidence for approval, checks and status, unresolved review threads, Merge-Ready acceptance, and review-artifact ancestry, plus the action that makes the live journey reachable.
- No row is finding-blocked, declared-blocked, failed, or skipped.

## Coverage

- Coverage sources: Requirements 1-4, outside evidence, Pull Request, and repository Verification. Every source appears in the Results table; no row names another source.
- Results: 6 passed, 1 environment-blocked with equivalent evidence, 0 failed, 0 finding-blocked, 0 declared-blocked, and 0 skipped.
- Requirement 2's two named public journeys each have their own row. Both survived an immediate rerun from the same committed scratch head.
- No `## Unreachable Acceptance` declaration and no frontend surface apply to this Spec.
- The matrix accepted no row from file-read evidence alone: each behavioral row used CLI execution, Git path confirmation, a repeat observation, or the daemon's supervised Verification fact.

## Final verdict

Pass: every runnable row passed; QA-07 is environment-blocked with complete equivalent evidence; no finding, declared block, failure, or skipped matrix row remains.
