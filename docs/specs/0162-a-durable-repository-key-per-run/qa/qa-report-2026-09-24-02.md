---
spec: 0162-a-durable-repository-key-per-run
date: 2026-09-24
build: 934e7fe069a7cb8f73570f3bb19596c3fe73d2c2
auditing_binary: "0.16.0 (934e7fe0)"
auditor_staleness: "current: build commit equals the audited tree"
status: closed
verdict: pass
rows_blocked_environment: 1
rows_blocked_finding: 0
rows_blocked_declared: 0
surfaces: [backend, cli]
---

# QA Report

## Scope and environment

Full rerun of the authored terminal gate for corrective head
`934e7fe069a7cb8f73570f3bb19596c3fe73d2c2`. The actor is a Roundfix operator
using a fresh current-tree CLI against isolated local Git repositories, linked
worktrees, Run Databases and Artifact Directories under `/private/tmp`. The
Pull Request fact is supplied by the QA assignment; no network, credential,
commit, push or Pull Request mutation is in scope.

The strict authoring precondition passed before the matrix ran:
`roundfix spec check 0162-a-durable-repository-key-per-run --strict` exited 0
with `No findings`. The installed checker identified itself as `roundfix
0.15.0` and did not execute authored Verification commands. A fresh
current-tree CLI identified itself as `roundfix 0.16.0 (934e7fe0)`; its strict
check also exited 0 with `No findings`.

The CLI and existing QA fixture helper were built with
`GOCACHE=/private/tmp/roundfix-0162-final-gocache`. Git history reads emitted a
non-blocking fsmonitor IPC warning while still returning complete path output;
no row depended on fsmonitor state.

## Static gate

- Repository Verification: the QA assignment records the Daemon-owned
  `make verify-changed` run as passed with exit 0 and diagnostics removed on
  success. This report records that supervised fact and did not rerun the
  command.
- Current-tree build: `go build -buildvcs=false -ldflags '-X
  roundfix/internal/app.BuildCommit=934e7fe0' -o
  /private/tmp/roundfix-0162-final ./cmd/roundfix` with the task-scoped cache
  exited 0; the binary reports `roundfix 0.16.0 (934e7fe0)`.
- Focused regression: fifteen named repository-key cases across
  `internal/config`, `internal/store` and `internal/cli` passed in one fresh
  `go test -count=1 -v` run. They included removed-worktree reconciliation,
  bare-repository identity/listing, cross-repository refusal, migration,
  sanitation and Run Window behavior.
- Historical migration sweep: `go test -count=1 ./internal/store` exited 0
  (`ok roundfix/internal/store 8.880s`).

## Performed repairs

None.

## Assigned repair failures

None.

## Authorization audit inputs

- Authorization commit `a940b6df63edde572f8dcdef26c8d31598a46ec5`
  is an ancestor of Task commits `d37c6e52`, `d3adbd64`, `dd433947` and
  `934e7fe0` and grants the bounded governed path
  `internal/cli/cli_test.go` for this Spec.
- `git diff-tree --no-commit-id --name-only -r d37c6e52` returned only
  `task_01.md`, `internal/store/store.go` and `internal/store/store_test.go`.
- `git diff-tree --no-commit-id --name-only -r d3adbd64` returned only
  `task_02.md`, `internal/cli/gc_test.go`, `internal/cli/reconcile.go`,
  `internal/cli/reconcile_test.go`, `internal/cli/window_test.go`,
  `internal/store/journal.go` and `internal/store/store.go`.
- `git diff-tree --no-commit-id --name-only -r dd433947` returned only
  `task_04.md`, `internal/cli/reconcile.go`, `internal/cli/reconcile_test.go`,
  `internal/store/store.go` and `internal/store/store_test.go`.
- `git diff-tree --no-commit-id --name-only -r 934e7fe0` returned only
  `task_05.md`, `internal/cli/reconcile.go`, `internal/cli/reconcile_test.go`,
  `internal/config/config.go`, `internal/config/config_test.go` and
  `internal/store/store_test.go`.
- No Task commit changed the governed path, so no tooling authorization or
  consequent-fix chronology row was runnable.

## Mechanical findings

None.

## Results

| # | Status | Provenance |
| - | --- | --- |
| R1 | pass | Requirement 1; repository Verification |
| R2 | pass | Requirement 2 |
| R3 | pass | Requirement 3; outside-evidence row |
| R4 | pass | Requirement 4 |
| R5 | pass | Requirement 5 |
| R6 | pass | Requirement 5 |
| R7 | pass | Requirement 6 |
| R8 | blocked (environment: no open Pull Request) | Pull Request row |

## Row evidence

### R1

- Actor and surface: Daemon; repository gate.
- Steps: record the Daemon's command, exit status and diagnostics fact.
- Expected observable: `make verify-changed` exits 0 with no retained success
  diagnostics.
- Independent confirmation: bind the prompt-supplied fact to this report and
  audited build.
- Persistence: not applicable; this is one supervised static-gate result.
- Evidence: the QA assignment binds `command: make verify-changed`, `verdict:
  pass (exit 0)` and `diagnostics: removed on success` to this gate. This is
  supervised evidence from the Daemon that owns repository Verification,
  recorded as a fact rather than re-derived in the Agent sandbox.

### R2

```yaml
inputs:
  - kind: repository_path
    ref: internal/store/store.go
  - kind: repository_path
    ref: internal/config/config.go
  - kind: repository_path
    ref: docs/specs/0162-a-durable-repository-key-per-run/qa/evidence/2026-09-24-spec-0162/fixture/main.go
```

- Actor and surface: operator; Run Database through the built CLI and an
  independent SQLite read.
- Steps: create a fresh linked-worktree Run, trigger migration of two v16
  databases through `window show`, inspect resolvable, unresolvable and removed
  worktree rows, compare fresh and migrated schemas, and run the package's
  historical migration sweep.
- Expected observable: the new and resolvable migrated rows carry the main
  checkout key; unresolvable and removed-worktree rows keep an empty key;
  schema version is 17 and fresh and migrated schemas match.
- Independent confirmation: `/usr/bin/sqlite3` reads the reopened databases;
  the fixture helper separately compares the schema declarations.
- Persistence: close and reopen each database before the independent read.
- Evidence: fresh fixture `/private/tmp/roundfix-0162-final-live.r2viez`
  recorded linked Run `run_20260924T225448Z_8c7c2a90d6998260`; SQLite observed
  `git_root` at `/linked`, `repository_root` at `/main` and schema version 17.
  Migration fixture `/private/tmp/roundfix-0162-final-migration.QmARpe`
  upgraded from v16 to v17: SQLite observed the main key for the resolvable row
  and an empty key for the malformed checkout, while the independent helper
  returned `schemaMatchesFresh: true`. Removed-worktree fixture
  `/private/tmp/roundfix-0162-final-removed-migration.R9yZXP` removed its real
  linked worktree before the public command opened the writer store; SQLite
  then observed schema version 17 and empty keys for both rows. The fresh full
  `internal/store` package run passed its historical migration corpus.

### R3

```yaml
inputs:
  - kind: repository_path
    ref: internal/store/store.go
  - kind: repository_path
    ref: internal/cli/runs.go
  - kind: external_repository
    ref: scratch Git repository observed independently with git worktree list and git rev-parse --git-common-dir
```

- Actor and surface: operator; `roundfix runs list` plus native Git.
- Steps: record a linked-worktree Run, remove the worktree, then list from the
  main checkout twice.
- Expected observable: both listings retain the Run and exclude the unrelated
  repository's Run.
- Independent confirmation: native Git proves the main and linked checkouts
  shared one common directory before removal and shows only main afterwards.
- Persistence: use a second fresh CLI process after removal.
- Evidence: scratch repository
  `/private/tmp/roundfix-0162-final-live.r2viez` recorded main and linked
  worktrees at commit `9fc84ecc54264232f6e3eef889cb2f74779d970f`; the linked
  checkout's common directory was the main checkout's `.git`. After native
  `git worktree remove`, the fixture's second native listing showed only main.
  Two fresh `runs list --state all --limit 0` processes each listed
  removed-checkout Run `run_20260924T225448Z_8c7c2a90d6998260` with branch
  `feature/linked` and omitted unrelated Run
  `run_20260924T225448Z_1bf041531665f025`. Both emitted the existing retained-
  Run inspection warning for the absent checkout without dropping the Run.
  These native Git observations are outside evidence from a scratch repository
  the Spec did not author.

### R4

```yaml
inputs:
  - kind: repository_path
    ref: internal/cli/reconcile.go
  - kind: repository_path
    ref: internal/store/store.go
```

- Actor and surface: operator; `roundfix reconcile <run-id> --format=json`.
- Steps: after removing the linked worktree, reconcile its Run from main twice,
  run bare `reconcile`, and request the unrelated repository's Run by ID.
- Expected observable: the linked Run is accepted from main; the unrelated
  request is refused before JSON output and names both repositories.
- Independent confirmation: repeat the accepted read in a fresh CLI process
  and inspect the refused command's exit status and diagnostic.
- Persistence: all observations use fresh CLI processes after worktree removal.
- Evidence: fixture `/private/tmp/roundfix-0162-final-live.r2viez` recorded
  linked Run `run_20260924T225448Z_8c7c2a90d6998260` with main as its key,
  then removed the linked worktree. Two explicit reconcile processes and one
  bare reconcile process from main exited 0, returned only that Run and
  classified it `released`, with zero operational failures. Unrelated Run
  `run_20260924T225448Z_1bf041531665f025` exited 2 with no JSON and named its
  `/other` repository and the current `/main` repository. The focused
  `TestReconcileFromMainAfterALinkedWorktreeIsRemoved` run separately passed
  all-terminal, explicit-Run and apply subtests.

### R5

```yaml
inputs:
  - kind: repository_path
    ref: internal/cli/gc.go
  - kind: repository_path
    ref: internal/store/store.go
```

- Actor and surface: operator; `roundfix gc sanitize`.
- Steps: place a terminal linked-worktree Run under the shared Artifact Root,
  remove the worktree and preview sanitation twice from main.
- Expected observable: the shared root is classified `orphaned`, never
  `overridden`.
- Independent confirmation: the second preview repeats the classification and
  a file check proves the artifact remains because no apply flag was supplied.
- Persistence: use a second fresh CLI process.
- Evidence: two fresh previews in
  `/private/tmp/roundfix-0162-final-live.r2viez/main` exited 0, named shared
  root `.../artifacts/33106bc805435faa`, classified it `orphaned`, reported
  one terminal Run and never printed `Classification: overridden`. The Run's
  `marker.txt` remained present after both dry runs.

### R6

```yaml
inputs:
  - kind: repository_path
    ref: internal/cli/window.go
  - kind: repository_path
    ref: internal/store/store.go
```

- Actor and surface: operator; `roundfix window show` and `roundfix window
  clear`.
- Steps: seed a legacy window under a linked checkout's exact path, show it
  from that worktree, clear it, then show it again.
- Expected observable: the stored cutoff is shown, clear reports success and a
  fresh show reports no Run Window.
- Independent confirmation: direct SQLite and helper reads prove both key
  variants are absent after clear.
- Persistence: use a fresh process after the mutation.
- Evidence: fixture `/private/tmp/roundfix-0162-final-window.C2cwiD` seeded one
  row under its exact linked checkout. The public show command exited 0 and
  printed cutoff `2026-09-24 22:55 -03`; clear exited 0 and printed `Run Window
  cleared`; a fresh show printed `No Run Window is set`. Independent helper and
  SQLite reads both observed zero remaining rows.

### R7

```yaml
inputs:
  - kind: repository_path
    ref: docs/specs/0162-a-durable-repository-key-per-run/**
  - kind: repository_path
    ref: internal/speccheck/**
```

- Actor and surface: Spec author; `roundfix spec check --strict`.
- Steps: run the strict checker from the audited tree, then repeat it after the
  report body settles.
- Expected observable: no findings, including no unmapped declared promise.
- Independent confirmation: use command output and exit status, never a file
  read alone.
- Persistence: repeat the current-tree checker after the report body settles.
- Evidence: installed `roundfix 0.15.0` and current-tree `roundfix
  0.16.0 (934e7fe0)` each exited 0 with `No findings` and explicitly reported
  that authored Verification commands were not executed. Both named skips for
  absent rollup, backlog, Vocabulary Contract and references index; none is the
  promise-rule detector. The current-tree checker exited 0 with `No findings`
  again after the report body settled.

### R8

```yaml
inputs:
  - kind: live_service
    ref: prompt-supplied Pull Request fact and Daemon-supervised repository Verification
```

- Actor and surface: reviewer; Pull Request journey.
- Steps: account for approval, checks and status, unresolved review threads,
  Merge-Ready acceptance and review-artifact ancestry for the expected head.
- Expected observable: the no-open-Pull-Request cause is explicit and every
  control has equivalent observed or supervised evidence.
- Independent confirmation: bind every control to expected head
  `934e7fe069a7cb8f73570f3bb19596c3fe73d2c2`.
- Persistence: not applicable while no Pull Request exists.
- Evidence: the QA assignment states `Pull Request: none open`, identifies
  `fix/0162-durable-repository-key` as the target branch and distinguishes it
  from the never-pushed Run Worktree branch. Equivalent controls: approval —
  no change acceptance has been obtained; `_authorization.md` grants bounded
  execution and delivery authority but is not review approval; checks and
  status — the Daemon ran `make verify-changed` for this candidate and recorded
  exit 0; unresolved review threads — none can stand because no Pull Request or
  thread collection exists; Merge-Ready acceptance — none exists for this
  corrective head because configured pre-PR review follows the authored gate;
  review-artifact ancestry — the Spec has no `reviews/` path or artifact to
  misbind, while this report is bound to expected head
  `934e7fe069a7cb8f73570f3bb19596c3fe73d2c2`. The target branch resolves to
  invalidation head `fcda71daac406279fd3fef6053fe8362dbf44e83`;
  integrating the Run is a later Daemon-owned step.

## Findings

None.

## Blocked and skipped

- R8 is `blocked (environment: no open Pull Request)`. The QA assignment proves
  the absence and R8 records equivalent evidence for approval, checks and
  status, unresolved review threads, Merge-Ready acceptance and review-artifact
  ancestry. Unblocking action: after the Daemon integrates this Run, perform
  configured pre-PR review and open the Pull Request from
  `fix/0162-durable-repository-key`; those later workflow steps are outside
  this gate.
- The seeded mechanical-stage skips remain recorded below. The QA-stage Git
  audit resolved every Task commit and found no governed-path change; no
  consequent-fix declaration exists. Neither skip omitted a coverage source.

## Coverage

Eight of eight rows reached a terminal status: seven passed and one is blocked
only by the no-open-Pull-Request environment with equivalent evidence.
Requirements 1-6, the outside-evidence row, Pull Request row and repository
Verification each appear in provenance; no row names another source. The PRD
declares no Unreachable Acceptance and no frontend surface. Counts:
`rows_blocked_environment: 1`, `rows_blocked_finding: 0`,
`rows_blocked_declared: 0`. No row failed, was skipped or remained pending.

## Final verdict

Pass — every runnable criterion passed on corrective head `934e7fe0`, the one
environment-blocked row carries equivalent evidence for every Pull Request
control, and no finding-blocked, declared-blocked or skipped row remains.
Results by status: seven pass, one environment-blocked; findings by impact
tier: none.

## Mechanical skips

| Detector | Missing artifact |
| --- | --- |
| authorization bounded paths | Task commits |
| consequent-fix commit order | consequent-fix declaration |
