---
spec: 0035-agent-selection-profiles
date: 2026-07-17
build: 0323564-dirty
status: closed
verdict: pass
surfaces: [cli, backend, data, docs, frontend]
---

# QA report — Agent Selection Profiles

## Scope and environment

Full QA gate for `0035-agent-selection-profiles` on commit `0323564`. The verified binary was built by `make verify` as `bin/roundfix` with build stamp `0323564-dirty` because this QA report and evidence files were uncommitted during the gate.

The previous `2026-07-17` report was closed with `verdict: partial` because `task_08.md` was still `in_progress`. This run replaces that stale readiness result after all ten task files reported `status: completed`.

Public entry points exercised:

- `make verify` from the repository root.
- Built `roundfix` CLI binary from `./cmd/roundfix`.
- Scratch Roundfix Home and scratch Git repository under `/private/tmp/roundfix-qa-0035.2e1xd3`.
- Fake pinned `acpx` and adapter executables for disposable proof checks; the Roundfix CLI, Project Config, SQLite proof flow, and command logs remained the real product boundary.

Parity note: TUI behavior was validated through the repository's synchronous Bubble Tea model tests and public command projections, matching the repository's no-terminal-emulation testing contract.

## Static gate

Passed.

- Command: `rtk proxy env GOCACHE=/private/tmp/roundfix-gocache make verify`
- Result: exit `0`
- Evidence: Go tests reported `1549 passed in 20 packages`; setup-context-driven unittest suite reported `79` tests passing; `roundfix skills check` passed; `go build` produced `bin/roundfix`.

## Results

| # | Story / criterion / sweep | Actor and surface | Status | Evidence |
| - | --- | --- | --- | --- |
| 1 | Task readiness precondition: every `task_NN.md` is completed and has acceptance evidence. | Roundfix QA agent · docs | pass | `qa/evidence/2026-07-17-agent-selection-profiles/task-status.txt`; all ten tasks are `status: completed` with valid Task Types, and each task `## Result` names verification evidence. |
| 2 | Full repository verification gate passes for the assembled build. | Roundfix QA agent · cli/backend/data/docs/frontend | pass | `rtk proxy env GOCACHE=/private/tmp/roundfix-gocache make verify` exited `0`; output included 1549 Go tests, skill sync/checks, and build. |
| 3 | Built-in required profiles resolve to official Preferred Selections, ordered Fallback Chains, and exactly five advisory recommendations in stable JSON. | Developer · cli/backend | pass | `profiles-show-backend.json`, `profiles-show-frontend.json`, `profiles-show-qa.json`, and `profiles-show-review.json`; Python assertion verified schema `roundfix/profiles/v1`, built-in tuples, five ranks, dated snapshot, and `category_specific: false`. |
| 4 | Optional Task Type profiles inherit effective `general` when absent without duplicating stored configuration. | Developer · cli/backend | pass | `profiles-show-data.json`; assertion verified `category: data`, `inherited_from: general`, `recommendation_source: general`, and five recommendations. |
| 5 | Unknown profile category fails as a usage error with diagnostics on stderr and no stdout payload. | Automation author · cli | pass | `profiles-show-unknown.stdout` is 0 bytes; `profiles-show-unknown.stderr` names `unknown profile category "unknown"` and the supported values; command exited `2`. |
| 6 | `profiles configure --dry-run --json` validates a complete file fragment, reports `changed: false`, and leaves Project Config unchanged. | Developer · cli/docs | pass | `profiles-configure-dry-run.json`; before/after SHA-256 for scratch `.roundfixrc.yml` stayed `f12bd08699cb2d354b67f9edf5f741149751fcd6d3749d45cdc89653be266fba`. |
| 7 | `profiles configure --scope project --file ... --json` writes only complete `profiles` schema and preserves unrelated Project Config keys. | Developer · cli/data | pass | `profiles-configure-write.json` reported `changed: true`; scratch `.roundfixrc.yml` retained `defaults.verification` and `notify.enabled` while adding only `profiles`; `profiles-show-backend-after-configure.json` resolved source `project`. |
| 8 | `profiles validate --json` proves distinct selections through disposable sessions, reports every referencing category, sends no prompt, and closes sessions. | Automation author · cli/backend | pass | `profiles-validate.json` reported `ok: true` and 8 passed proofs; `acpx-validate.log` has 8 `sessions ensure`, 8 `sessions close`, and no `prompt` invocation. |
| 9 | Dedicated `qa` and `review` profiles are visible and validated independently from implementation Task categories. | Developer · cli/backend | pass | `profiles-show-qa.json`, `profiles-show-review.json`, and `profiles-validate-review.json`; assertion verified review-only validation references and both preferred/fallback roles. |
| 10 | Mixed backend/frontend Tasks plus QA use exact configured selections and independent owned sessions. | Developer · cli/backend/data | pass | `macro-test.txt`; `rtk proxy env GOCACHE=/private/tmp/roundfix-gocache go test ./internal/cli -run 'TestAgentSelectionProfilesMacro' -count=1` passed. Task 10 Result records exact backend, frontend, fallback, and QA selections asserted by the macro. |
| 11 | Pre-prompt Preferred failure emits durable and caller-visible fallback notification before cross-runtime fallback activation and first prompt. | Supervisor · cli/frontend/data | pass | `macro-test.txt` and `projection-tests.txt`; macro passed with durable SQLite notification-before-fallback guard, caller-visible message, and ordered Run Event assertions. |
| 12 | No fallback occurs after `agent_work_started`; prompt, tool, verification, cancellation, rate-limit, or session-loss failures do not start a replacement session. | Supervisor · backend/data | pass | `macro-test.txt`; macro post-start failure companion passed. Static gate also passed Task 06/10 post-start no-fallback tests. |
| 13 | Invalid or missing Task Type fails before disposable proof, Run row, branch, worktree, or Agent invocation. | Maintainer · cli/backend/data | pass | `preflight-tests.txt` and `macro-test.txt`; focused preflight tests and macro invalid Task Type companion passed with zero proof/Run side effects. |
| 14 | Run surfaces project category, scope identity, source, role, attempt order, exact selection, status, and reason consistently across stderr, Console Log, Attach/TUI, and JSON events. | Supervisor · frontend/cli/data | pass | `projection-tests.txt`; `internal/runevent`, `internal/tui`, and `internal/cli` projection tests passed. `events-help.txt` confirms public `agent-selection` stream filter. |
| 15 | Public `events --filter` help and user guide document `agent-selection` as an accepted filter. | Automation author · cli/docs | pass | `events-help.txt` lists `task-status,batch,verification,outcome,agent-selection`; `docs-profile-guidance.txt` includes matching command-reference lines. |
| 16 | One-Run Preferred Selection overrides preserve configured Fallback Chains and warn when applied across multiple Task or QA categories. | Developer · cli/backend | pass | `preflight-tests.txt`; focused `InvocationProfileOverride` and operational preflight tests passed under the current build. Task 05 Result names the warning and fallback-preservation assertions. |
| 17 | Recommendation ranking remains advisory: no benchmark auto-routing, live fetch, configuration mutation, or fallback-order mutation. | Developer · cli/backend/docs | pass | `profiles-show-backend.txt`, show JSON files, dry-run/write evidence, and macro snapshot evidence; recommendations stayed five-entry dated output and did not alter config or fallback order. |
| 18 | Runtime-owned configuration, authentication, credentials, prompts, raw ACP payloads, tokens, cookies, and secrets are not written to profile projections or persistence. | Developer · data/docs | pass | `runtime-owned-before.sha256` equals `runtime-owned-after.sha256`; `profiles-validate-review.json` remained review-scoped; Task 07/08 privacy tests passed in static gate. |
| 19 | Documentation can be followed by an intended reader for show, configure, validate, fallback boundaries, optional inheritance, and migration guidance. | Developer · docs/cli | pass | Executed documented `profiles show`, `profiles configure --dry-run --json`, `profiles configure --json`, and `profiles validate --json` flows in scratch environment; `docs-profile-guidance.txt` confirms usage/configuration/skill guidance. |
| 20 | Non-goals hold: no skill-owned model configuration, no Run-time Task Type inference, no unsupported benchmark runtimes, and optional categories are not mandatory. | Roundfix QA agent · cli/docs/backend | pass | Task 02/03/09/10 evidence, `profiles-show-data.json`, `docs-profile-guidance.txt`, `preflight-tests.txt`, and `macro-test.txt`; no tested path inferred Task Type, mutated skills into model configuration, or made optional categories mandatory. |

## Findings

None.

## Blocked and skipped

None.

## Coverage

- QA matrix rows: 20 passed, 0 failed, 0 blocked, 0 skipped.
- PRD user stories: 7 passed / 7 total.
- Core features: 11 passed / 11 total.
- Success metrics: 9 passed / 9 total.
- Non-goals: 10 checked / 10 total.
- Task files: 10 completed / 10 total.
- Public behavior probes: unknown category usage error, dry-run no mutation, confirmed Project Config write, disposable validation cleanup, runtime-owned sentinel no mutation, mixed-profile macro, pre-prompt fallback, post-start no-fallback, invalid Task Type zero side effects, and `agent-selection` event filter help.

## Final verdict

Pass: every planned row passed on build `0323564-dirty`; no findings, blocked rows, or skipped scope remain.
