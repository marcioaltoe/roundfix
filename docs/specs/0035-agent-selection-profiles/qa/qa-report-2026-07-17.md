---
spec: 0035-agent-selection-profiles
date: 2026-07-17
build: c504e96
status: closed
verdict: partial
surfaces: [cli, backend, data, docs, frontend]
---

# QA report — Agent Selection Profiles

## Scope and environment

This QA gate attempted to validate every PRD user story, core feature, success metric, non-goal, and Task acceptance criterion for `0035-agent-selection-profiles`.

The requested worktree path `run_20260717T090719Z_7cfb5d54c92c65ac` was absent when shell commands ran. The active sibling worktree used for inspection was `run_20260717T090901Z_fa74c156c3cac40c`, which contains the same spec slug and today's QA report path.

Final QA could not proceed because the required task-readiness precondition failed: every task must be `completed` before the assembled feature gate starts. `task_08.md` is still `status: in_progress`, with one unchecked subtask and one unchecked acceptance criterion for the public `events --filter` help contract.

## Static gate

Not run. The QA gate stopped before static verification because the spec has an incomplete task. Running flow validation or `make verify` after a task-readiness failure would not produce a valid final QA verdict for the assembled spec.

## Results

| # | Story / criterion / sweep | Actor and surface | Status | Evidence |
| - | --- | --- | --- | --- |
| 1 | Task readiness precondition: every `task_NN.md` must be completed before final QA. | Roundfix QA agent · docs | blocked | `rtk rg -n "^status:" .../docs/specs/0035-agent-selection-profiles/task_*.md` showed `task_08.md:4:status: in_progress`; every other task reported `completed`. |
| 2 | Task 08 acceptance criterion: `roundfix events --help` lists `agent-selection` as an accepted filter, and the user guide documents the same value. | Operator · cli/docs | blocked | `task_08.md` still has unchecked subtask `Document agent-selection in the public events --filter help and user guidance` and unchecked acceptance criterion for the same contract. |
| 3 | PRD user stories 1-7 and core features 1-11 against the real application. | Developer, Supervisor, automation author · cli/backend/data/docs/frontend | blocked | Scope is blocked by incomplete Task 08; final assembled behavior was not exercised. |
| 4 | Repository static verification gate. | Roundfix QA agent · cli/backend/data/docs/frontend | blocked | `make verify` was intentionally not run after task-readiness failed. |
| 5 | Non-goal sweep: no benchmark auto-routing, live benchmark fetch, skill-owned model configuration, runtime Task Type inference, post-prompt fallback, non-selection fallback, runtime-owned config mutation, unsupported benchmark runtimes, category-specific benchmark claims, or mandatory optional categories. | Roundfix QA agent · cli/backend/data/docs/frontend | blocked | Scope is blocked by incomplete Task 08; non-goal checks were not exercised against the current build. |

## Findings

No product defect was filed from this gate. The run is blocked by spec readiness, not by an observed application failure.

## Blocked and skipped

- `task_08.md` is not completed. Unblocking action: finish or resolve the `events --filter` help/user-guide contract, settle `task_08.md` to `status: completed`, then rerun the QA gate from the beginning.
- PRD story validation, CLI/user-flow validation, non-goal checks, and `make verify` are blocked until every task file is completed.

## Coverage

- Task readiness: 9 passed, 1 blocked.
- PRD user stories: 0 validated, 7 blocked by task readiness.
- Core features: 0 validated, 11 blocked by task readiness.
- Non-goals: 0 validated, 10 blocked by task readiness.
- Static gate: 0 run, 1 blocked.

## Final verdict

Partial: final QA cannot pass because `task_08.md` remains `in_progress`; rerun the QA gate after Task 08 is completed.
