---
spec: 0151-a-supersession-the-archive-can-see
date: 2026-09-20
build: 05757c2e1058d85609ade1d68b25943e7aa22588
auditing_binary: "0.15.0 (30a41b83, built 2026-09-20 00:19:50 -0300)"
auditor_staleness: "stale: commit ancestry: build commit predates audited tree"
status: closed
verdict: pass
rows_blocked_environment: 1
rows_blocked_finding: 0
rows_blocked_declared: 0
surfaces: [backend, cli, docs]
---

# QA Report

## Scope and environment

Full terminal rerun for build
`05757c2e1058d85609ade1d68b25943e7aa22588` on the Run Worktree branch. The
actor is a local Roundfix operator. CLI journeys use a binary built from the
audited tree in a disposable directory under `/private/tmp`. The Pull Request
fact is supervised: no Pull Request is open for the Spec target branch, so that
row uses the equivalent-evidence path. The Daemon already ran repository
Verification; this gate records that result without rerunning it.

The authored QA node is `task_04`. It is terminal and depends on `task_01`,
`task_02`, `task_03`, `task_05`, and `task_06`; all five dependencies are
`completed`. `roundfix spec check 0151-a-supersession-the-archive-can-see
--strict` passed with no findings. The current report declares fresh inputs and
does not carry any row forward from the prior head.

## Static gate

- `make verify` — pass, exit `0`, as supplied by the Daemon; diagnostics were
  removed on success. The QA Agent did not rerun it.
- `roundfix spec check 0151-a-supersession-the-archive-can-see --strict` — pass,
  exit `0`; no findings and authored Verification commands were not executed.
  The command reported four skips: `SC-ROLLUP-MEMBER`, `SC-BACKLOG-UNMOVED`,
  the vocabulary documentation detector, and `SC-REF-UNRESOLVED`.

## Performed repairs

None.

## Assigned repair failures

None.

## Authorization audit inputs

The mechanical stage supplied none. The Agent audit resolved authorization
commits `2b73ac3f79489a2c772741b978a5dc0a6df68d47` and
`03cfb8ee29c7c0ec9fb822a9defb251d49316709`, and Task commits
`7d9e94d8a7cd5802dcea4c2d0b1dfd4cac20b5e4`,
`1335873901dc9f96083e47a37764229960f66177`,
`60224664f49268383026f9b4a8bee15b89072b10`,
`0d1d6becd2bf6675ed0d6716bbc22da4920bd501`, and
`05757c2e1058d85609ade1d68b25943e7aa22588`.

## Mechanical findings

None.

## Results

| # | Story / criterion / sweep | Actor and surface | Status | Provenance | Evidence |
| - | --- | --- | --- | --- | --- |
| R1 | Repository Verification | Daemon / repository | pass | Requirement 1; repository Verification | Daemon fact records `make verify` exit `0`; evidence file, “Preconditions and static gate” |
| R2 | A real no-Task-Graph Spec refuses without a supersession, records one, archives byte-identically, and remains usable as a deliverer | Local operator / CLI and filesystem | pass | Requirement 2; Requirement 5; outside-evidence row | Fresh rerun after input correction; evidence file, “Real repository journey and outside evidence” |
| R3 | Every declared supersede refusal preserves the Spec root; active and archived deliverers succeed; an amendment adds one file | Local operator / CLI and filesystem | pass | Requirement 3; Requirement 5 | Fresh rerun after input correction; evidence file, “Refusals and amendment preservation” |
| R4 | A Task Graph keeps archive's existing evidence behavior with and without a supersession | Local operator / CLI and filesystem | pass | Requirement 4 | Fresh rerun after input correction; evidence file, “Task Graph regression” |
| R5 | Canonical skill, mirror, user guide, and public help describe the command and changed archive precondition | Local operator / docs and CLI | pass | Requirement 6 | Fresh rerun after input correction; evidence file, “Documentation and help” |
| R6 | Every Task commit remains inside the operative authorization and required chronology | QA auditor / Git | pass | Requirement 6 | Fresh rerun after input correction; evidence file, “Authorization and commit scope” |
| R7 | This Spec's promises name consuming Tasks | QA auditor / Spec consistency CLI | pass | Requirement 7 | Fresh rerun after input correction; evidence file, “Promise coverage” |
| R8 | Pull Request controls use supervised equivalent evidence because no Pull Request is open | Reviewer / Pull Request | blocked (environment: no open Pull Request; equivalent evidence recorded) | Pull Request row | Prompt facts and all five equivalent controls; evidence file, “Pull Request equivalent evidence” |

## Row evidence

### R1

```yaml
inputs:
  - kind: repository_path
    ref: "**"
```

- Steps: record the Daemon-supplied command, exit code, and diagnostics
  disposition.
- Expected observable: repository Verification exits `0`.
- Independent confirmation: the supervised Daemon fact names the command and
  result separately from this report.
- Persistence: not applicable to a static repository gate.
- Observed: pass, exit `0`; diagnostics were removed on success.
- Evidence:
  `docs/specs/0151-a-supersession-the-archive-can-see/qa/evidence/2026-09-20-spec-0151-rerun-01/qa-run.md`.

### R2

```yaml
inputs:
  - kind: repository_path
    ref: "**"
```

- Steps: copy real Specs 0128 and 0147 into a disposable Git repository;
  archive before supersession; supersede; archive again; compare the moved
  tree; then use the supersession-archived Spec as the deliverer for a second
  supersession.
- Expected observable: initial exit `2` names the missing Task Graph; both
  supersede commands and archive exit `0`; the moved tree and preserved PRD are
  byte-identical.
- Independent confirmation: fresh destination reads, SHA-256 manifests, and
  Git status after the CLI processes exit.
- Persistence: archived content remains readable and usable by a later public
  CLI invocation.
- Observed: initial archive exited `2`; both supersede commands and the second
  archive exited `0`; both directory comparisons passed; the preserved PRD hash
  remained `45dd42316513ac8a91a4142c7de32d0989087abe6506511cb03233695905d22a`.
- Evidence:
  `docs/specs/0151-a-supersession-the-archive-can-see/qa/evidence/2026-09-20-spec-0151-rerun-01/qa-run.md`.

### R3

```yaml
inputs:
  - kind: repository_path
    ref: "**"
```

- Steps: exercise the command's unknown target, absent deliverer,
  self-supersession, non-active, malformed, duplicate, unmarked archived, and
  unknown-flag refusals; exercise active and conventionally archived accepted
  deliverers.
- Expected observable: refusals exit `2`, name their condition, and write
  nothing; accepted cases exit `0` and add only `_supersession.md`.
- Independent confirmation: fresh filesystem manifests after each process and
  focused regression tests over the same public outcomes.
- Persistence: duplicate refusal preserves the existing amendment hash.
- Observed: all nine refusal probes exited `2` and left Git clean; active and
  archived accepted paths exited `0`; the duplicate probe preserved amendment
  hash `d8c4cdc67ffc15bffd565776b881e749beecce79ec17d3efc2b15484f2d07473`;
  the focused Go regression exited `0`.
- Evidence:
  `docs/specs/0151-a-supersession-the-archive-can-see/qa/evidence/2026-09-20-spec-0151-rerun-01/qa-run.md`.

### R4

```yaml
inputs:
  - kind: repository_path
    ref: "**"
```

- Steps: invoke archive on a pending Task Graph before and after adding a valid
  supersession through the public command.
- Expected observable: both archive calls exit `2` with the same incomplete
  Task refusal and leave all pre-existing files byte-identical.
- Independent confirmation: fresh manifests after both commands.
- Persistence: the second archive call reads the amended on-disk state.
- Observed: both archive calls exited `2` with the identical pending `task_04`
  reason; fresh Git status named only `_supersession.md`.
- Evidence:
  `docs/specs/0151-a-supersession-the-archive-can-see/qa/evidence/2026-09-20-spec-0151-rerun-01/qa-run.md`.

### R5

```yaml
inputs:
  - kind: repository_path
    ref: "**"
```

- Steps: compare canonical skill and mirror; run the public skill checker;
  execute both help surfaces; inspect required refusal and archive wording.
- Expected observable: mirrors match, all documentation carries the promised
  behavior, and public help exposes the command.
- Independent confirmation: skill check and CLI help execute successfully.
- Persistence: repeat the mirror comparison after the commands exit.
- Observed: both comparisons, skill check, help commands, and focused contract
  searches passed. Two preliminary harness assertions were corrected after
  direct inspection showed they exceeded or mismatched the authored wording;
  no product artifact changed.
- Evidence:
  `docs/specs/0151-a-supersession-the-archive-can-see/qa/evidence/2026-09-20-spec-0151-rerun-01/qa-run.md`.

### R6

```yaml
inputs:
  - kind: repository_path
    ref: "**"
```

- Steps: resolve every Task commit, enumerate it with `git diff-tree`, classify
  governed paths, and prove authorization and fix chronology by ancestry.
- Expected observable: every governed mutation is within the exact grant and
  authorized before consumption; no prerequisite or consequent fix is folded
  into the wrong commit.
- Independent confirmation: Git ancestry checks and a fresh mirror comparison.
- Persistence: evidence is tied to immutable commit IDs.
- Observed: every Task commit's paths were enumerated; only the three bounded
  paths were governed; their authorization commits precede consumption; the
  two corrective Tasks touched no governed path; mirror comparison passed.
- Evidence:
  `docs/specs/0151-a-supersession-the-archive-can-see/qa/evidence/2026-09-20-spec-0151-rerun-01/qa-run.md`.

### R7

```yaml
inputs:
  - kind: repository_path
    ref: "**"
```

- Steps: execute the strict Spec consistency command and cross-check Task 04's
  References against the declared Goals, Core Features, Success Metrics,
  Testing Approach, and API Contracts.
- Expected observable: no promise-to-Task coverage finding.
- Independent confirmation: the CLI result and direct citation cross-check
  agree.
- Persistence: both read the committed Spec artifacts at the audited head.
- Observed: the current-tree strict check exited `0` with no findings and the
  direct Task 04 References cross-check passed.
- Evidence:
  `docs/specs/0151-a-supersession-the-archive-can-see/qa/evidence/2026-09-20-spec-0151-rerun-01/qa-run.md`.

### R8

```yaml
inputs:
  - kind: live_service
    ref: "Pull Request state for feat/0151-supersession-the-archive-can-see"
```

- Steps: apply the supervised no-Pull-Request fact to approval, checks/status,
  unresolved threads, Merge-Ready acceptance, and review-artifact ancestry.
- Expected observable: typed environment block with every control answered and
  no mutation attempted.
- Independent confirmation: the prompt separately states that the target has
  no open Pull Request and the Run Worktree branch has none of its own.
- Persistence: live Pull Request state is intentionally not carriable.
- Observed: no approval exists; repository Verification passed; no Pull Request
  review thread stands; no Merge-Ready acceptance or review artifact exists;
  no Pull Request mutation was attempted.
- Evidence:
  `docs/specs/0151-a-supersession-the-archive-can-see/qa/evidence/2026-09-20-spec-0151-rerun-01/qa-run.md`.

## Findings

None.

## Blocked and skipped

- R8 — `blocked (environment: no open Pull Request)`. Equivalent evidence:
  no approval was obtained; repository Verification passed; no Pull Request
  review thread stands; no Merge-Ready acceptance exists; and no review
  artifact claims ancestry. Unblocking action: complete the post-QA review
  policy and open the Pull Request on
  `feat/0151-supersession-the-archive-can-see`.
- Skipped rows: none.

## Coverage

All 10 coverage sources appear in 8 terminal rows: 7 passed and 1 is
environment-blocked with equivalent supervised evidence. Counts:
`rows_blocked_environment: 1`, `rows_blocked_finding: 0`, and
`rows_blocked_declared: 0`. No row failed, remained pending, or was skipped.
No frontend surface or PRD `Unreachable Acceptance` declaration applies. The
outside-evidence source was the repository's real Specs 0128, 0129, and 0147,
exercised through the current public binary in a disposable repository.

## Final verdict

Pass: every runnable criterion passed, the one environment-blocked Pull Request
row records its cause and equivalent supervised evidence, and no row is
finding-blocked, declared-blocked, failed, pending, or skipped.

## Mechanical skips

| Detector | Missing artifact |
| --- | --- |
| authorization bounded paths | Task commits |
| consequent-fix commit order | consequent-fix declaration |
