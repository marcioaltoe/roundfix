---
spec: 0151-a-supersession-the-archive-can-see
date: 2026-09-19
build: 60224664f49268383026f9b4a8bee15b89072b10
auditing_binary: "0.15.0 (60224664, built 2026-09-19 23:20:46 -0300)"
auditor_staleness: "current: commit ancestry: build commit equals audited tree"
status: closed
verdict: pass
rows_blocked_environment: 1
rows_blocked_finding: 0
rows_blocked_declared: 0
surfaces: [backend, cli, docs]
---

# QA Report

## Scope and environment

Full terminal gate for build `60224664f49268383026f9b4a8bee15b89072b10` on
the Run Worktree branch. The actor is a local Roundfix operator. CLI journeys
use `./bin/roundfix` from disposable Git repositories under `/private/tmp` with
the built-in `docs/specs` root. The Pull Request fact is supervised: no Pull
Request is open for the Spec target branch, so Pull Request journeys use the
equivalent-evidence path. The Daemon already ran `make verify`; this gate must
record that result without rerunning it.

The authored QA node is `task_04`. It is terminal and depends on `task_01`,
`task_02`, and `task_03`; all three dependencies are `completed`. The current
binary ran `roundfix spec check 0151-a-supersession-the-archive-can-see
--strict` and reported no findings. Its skipped detectors were
`SC-ROLLUP-MEMBER`, `SC-BACKLOG-UNMOVED`, the vocabulary documentation detector,
and `SC-REF-UNRESOLVED`; none replaces a row below.

Evidence root:
`docs/specs/0151-a-supersession-the-archive-can-see/qa/evidence/2026-09-19-spec-0151/`.

## Static gate

- `make verify` — pass, exit `0`, as supplied by the Daemon; diagnostics were
  removed on success. The QA Agent did not rerun it.
- `./bin/roundfix spec check 0151-a-supersession-the-archive-can-see --strict`
  — pass; no findings, authored Verification commands not executed.

## Performed repairs

None.

## Assigned repair failures

None.

## Authorization audit inputs

- Authorization commits: `2b73ac3f79489a2c772741b978a5dc0a6df68d47`,
  `03cfb8ee29c7c0ec9fb822a9defb251d49316709`.
- Task commits: `7d9e94d8a7cd5802dcea4c2d0b1dfd4cac20b5e4`,
  `1335873901dc9f96083e47a37764229960f66177`,
  `60224664f49268383026f9b4a8bee15b89072b10`.

## Mechanical findings

None.

## Results

| # | Story / criterion / sweep | Actor and surface | Status | Provenance |
| - | --- | --- | --- | --- |
| R1 | Repository Verification | Daemon / repository | pass | Requirement 1; repository Verification |
| R2 | A no-Task-Graph Spec refuses without a supersession, then records one and archives byte-identically | Local operator / CLI and filesystem | pass | Requirement 2; Requirement 5; outside-evidence row |
| R3 | Every declared supersede refusal leaves the Spec root byte-identical; the accepted amendment adds only one file | Local operator / CLI and filesystem | pass | Requirement 3; Requirement 5 |
| R4 | A Task Graph keeps archive's existing evidence behavior with and without a supersession | Local operator / CLI and filesystem | pass | Requirement 4 |
| R5 | Canonical skill, mirror, and user guide describe the command and archive precondition | Local operator / docs and CLI help | pass | Requirement 6 |
| R6 | Task commits remain inside the operative authorization and satisfy commit chronology | QA auditor / Git | pass | Requirement 6 |
| R7 | The Spec's declared promises name consuming Tasks | QA auditor / Spec consistency CLI | pass | Requirement 7 |
| R8 | Pull Request controls use supervised equivalent evidence because no Pull Request is open | Reviewer / Pull Request | blocked (environment: no open Pull Request; equivalent evidence recorded) | Pull Request row |

## Row evidence

### R1

```yaml
inputs:
  - kind: repository_path
    ref: "**"
```

- Steps: record the Daemon-supplied `make verify` command, exit code, and
  diagnostics disposition.
- Expected: exit 0 with no retained diagnostics.
- Independent confirmation: the supervised Daemon fact names the command and
  exit code separately from this report.
- Persistence: not applicable to a static repository gate.
- Evidence: `qa/evidence/2026-09-19-spec-0151/static-and-preconditions.md`.

### R2

```yaml
inputs:
  - kind: repository_path
    ref: "docs/specs/0128-release-planning-with-bare-stable-tags/**"
  - kind: repository_path
    ref: "docs/history/specs/0147-a-planner-that-reads-both-tag-spellings/**"
  - kind: repository_path
    ref: "bin/roundfix"
```

- Steps: copy the real repository's active Spec 0128 and archived Spec 0147
  into a disposable Git repository; attempt archive before supersession; run
  `supersede`; archive again; compare file hashes before and after the move.
- Expected: the first archive exits 2 with the missing-Task-Graph reason;
  supersede and the second archive exit 0; the archived tree equals the amended
  active tree byte-for-byte.
- Independent confirmation: fresh filesystem reads and SHA-256 manifests from
  the archived destination.
- Persistence: re-read the archived destination after both CLI processes exit.
- Evidence: `qa/evidence/2026-09-19-spec-0151/real-spec-journey.md`.

### R3

```yaml
inputs:
  - kind: repository_path
    ref: "bin/roundfix"
```

- Steps: in fresh disposable Git repositories, exercise unknown superseded
  Spec, unknown superseding Spec, self-supersession, second supersession, and
  unknown-flag refusals; compare SHA-256 manifests around every refusal. Run one
  accepted supersession and compare all pre-existing files.
- Expected: every refusal exits 2, names its condition, and changes no file;
  acceptance exits 0 and adds only `_supersession.md`.
- Independent confirmation: a separate manifest read after each process exits.
- Persistence: re-open the accepted amendment and verify its frontmatter/body.
- Evidence: `qa/evidence/2026-09-19-spec-0151/refusal-and-amendment-journey.md`.

### R4

```yaml
inputs:
  - kind: repository_path
    ref: "bin/roundfix"
```

- Steps: create a disposable Spec with a Task Graph containing a pending Task;
  invoke archive before and after adding a valid supersession amendment.
- Expected: both invocations exit 2 with the same incomplete-Task refusal and
  leave the Spec byte-identical.
- Independent confirmation: fresh SHA-256 manifests after both commands.
- Persistence: re-run from the same on-disk state with the amendment present.
- Evidence: `qa/evidence/2026-09-19-spec-0151/task-graph-regression.md`.

### R5

```yaml
inputs:
  - kind: repository_path
    ref: ".agents/skills/roundfix/SKILL.md"
  - kind: repository_path
    ref: "skills/roundfix/SKILL.md"
  - kind: repository_path
    ref: "docs/user-guide/commands.md"
  - kind: repository_path
    ref: "bin/roundfix"
```

- Steps: compare canonical skill and mirror byte-for-byte; follow the user-guide
  invocation through built CLI help; check required refusal and archive wording.
- Expected: skill copies match, all three documents carry the invocation and
  promised behavior, and public help exposes it.
- Independent confirmation: `./bin/roundfix skills check` and CLI help output.
- Persistence: repeat the byte comparison after the commands exit.
- Evidence: `qa/evidence/2026-09-19-spec-0151/docs-and-help.md`.

### R6

```yaml
inputs:
  - kind: repository_path
    ref: "docs/specs/0151-a-supersession-the-archive-can-see/_authorization.md"
  - kind: repository_path
    ref: "docs/specs/0151-a-supersession-the-archive-can-see/task_0[1-3].md"
```

- Steps: resolve Task commits from Git history, enumerate each with
  `git diff-tree --no-commit-id --name-only -r`, and compare governed paths with
  the authorization's exact bounded paths and chronological ancestry.
- Expected: every governed mutation is authorized before its Task commit; no
  governed path falls outside the bounded list; ordinary source, test, and guide
  paths remain outside the governed set under ADR-0130; the mirror is current.
- Independent confirmation: Git parent/ancestor queries and a clean mirror
  comparison.
- Persistence: evidence is tied to immutable commit IDs.
- Evidence: `qa/evidence/2026-09-19-spec-0151/authorization-audit.md`.

### R7

```yaml
inputs:
  - kind: repository_path
    ref: "docs/specs/0151-a-supersession-the-archive-can-see/**"
  - kind: repository_path
    ref: "bin/roundfix"
```

- Steps: run the strict Spec consistency command with the current auditing
  binary and inspect its complete findings/skips result.
- Expected: no finding says a Goal, Core Feature, Success Metric, or API
  Contract lacks a consuming Task.
- Independent confirmation: cross-check Task 04 References against the PRD and
  TechSpec coverage declarations.
- Persistence: the command reads the committed Spec artifacts at the audited
  head.
- Evidence: `qa/evidence/2026-09-19-spec-0151/static-and-preconditions.md`.

### R8

```yaml
inputs:
  - kind: live_service
    ref: "Pull Request state for feat/0151-supersession-the-archive-can-see"
```

- Steps: use the supervised prompt facts and equivalent evidence for approval,
  checks/status, unresolved review threads, Merge-Ready acceptance, and
  review-artifact ancestry.
- Expected: the row records `blocked (environment: no open Pull Request)` and
  answers every control without attempting mutation or resolving from the Run
  Worktree branch.
- Independent confirmation: the prompt separately states that Pull Request
  journeys are environment-blocked and that the Run Worktree branch has no Pull
  Request of its own.
- Persistence: live Pull Request state is intentionally not carriable.
- Evidence: `qa/evidence/2026-09-19-spec-0151/pull-request-equivalent.md`.

## Findings

None.

## Blocked and skipped

- R8 — `blocked (environment: no open Pull Request)`. The supervised prompt
  proves the target branch has no open Pull Request and the Run Worktree branch
  has none of its own. Equivalent evidence records: no approval obtained;
  `make verify` passed; no Pull Request review thread stands; no Merge-Ready
  acceptance exists yet; and no review artifact claims ancestry. Unblocking
  action: finish the post-QA pre-PR review policy and open the Pull Request on
  `feat/0151-supersession-the-archive-can-see`.
- Skipped rows: none.

## Coverage

All 10 coverage sources appear in 8 terminal rows: 7 passed and 1 is
environment-blocked with equivalent supervised evidence. Counts:
`rows_blocked_environment: 1`, `rows_blocked_finding: 0`, and
`rows_blocked_declared: 0`. No row failed or was skipped. No frontend surface or
PRD `Unreachable Acceptance` declaration applies. The outside-evidence source
was the repository's real Spec 0128 and archived Spec 0147, exercised through
the built public CLI in a disposable Git repository.

## Final verdict

Pass: every runnable criterion passed, the one environment-blocked Pull Request
row records its cause and equivalent supervised evidence, and no row is
finding-blocked, declared-blocked, failed, pending, or skipped.

## Mechanical skips

| Detector | Missing artifact |
| --- | --- |
| authorization bounded paths | Task commits |
| consequent-fix commit order | consequent-fix declaration |
| QA Report structure | QA Report |
| QA evidence paths | QA Report |
