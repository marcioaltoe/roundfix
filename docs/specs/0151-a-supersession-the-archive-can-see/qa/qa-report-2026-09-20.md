---
spec: 0151-a-supersession-the-archive-can-see
date: 2026-09-20
build: 0d1d6becd2bf6675ed0d6716bbc22da4920bd501
auditing_binary: "0.15.0 (0d1d6bec, built 2026-09-20 00:05:48 -0300)"
auditor_staleness: "current: commit ancestry: binary build commit equals audited tree"
status: closed
verdict: pass
rows_blocked_environment: 1
rows_blocked_finding: 0
rows_blocked_declared: 0
surfaces: [backend, cli, docs]
---

# QA Report

## Scope and environment

Full terminal rerun for build
`0d1d6becd2bf6675ed0d6716bbc22da4920bd501` on the Run Worktree branch. The
actor is a local Roundfix operator. CLI journeys used the current
`./bin/roundfix` in disposable Git repositories under
`/private/tmp/roundfix-qa0151-20260920.d5PosW/`. The Pull Request fact is
supervised: no Pull Request is open for the Spec target branch, so that row
uses the equivalent-evidence path. The Daemon already ran repository
Verification; this gate records that result without rerunning it.

The authored QA node is `task_04`. It is terminal and depends on `task_01`,
`task_02`, `task_03`, and `task_05`; all four dependencies are `completed`.
The current binary's strict Spec check reported no findings. Its skips were
`SC-ROLLUP-MEMBER`, `SC-BACKLOG-UNMOVED`, the vocabulary documentation
detector, and `SC-REF-UNRESOLVED`; none replaces a row below. The seeded report
declared no typed row inputs, so no row is eligible for carry-forward and every
row was re-observed.

Evidence:
`qa/evidence/2026-09-20-spec-0151/qa-run.md`.

## Static gate

- `make verify` — pass, exit `0`, as supplied by the Daemon; diagnostics were
  removed on success. The QA Agent did not rerun it.
- `./bin/roundfix spec check 0151-a-supersession-the-archive-can-see --strict`
  — pass, exit `0`; no findings and authored Verification commands were not
  executed.

## Performed repairs

None.

## Assigned repair failures

None.

## Authorization audit inputs

None.

## Mechanical findings

None.

## Results

| # | Story / criterion / sweep | Actor and surface | Status | Provenance | Evidence |
| - | --- | --- | --- | --- | --- |
| R1 | Repository Verification | Daemon / repository | pass | Requirement 1; repository Verification | Daemon fact records `make verify` exit `0`; evidence file, “Preconditions and static gate” |
| R2 | A real no-Task-Graph Spec refuses without a supersession, then records one and archives byte-identically | Local operator / CLI and filesystem | pass | Requirement 2; Requirement 5; outside-evidence row | Real Specs 0128 and 0147; evidence file, “Real repository journey and outside evidence” |
| R3 | Every declared supersede refusal preserves the Spec root; active and archived deliverers succeed; an amendment adds one file | Local operator / CLI and filesystem | pass | Requirement 3; Requirement 5 | Public CLI exits, fresh Git reads, stable amendment hash, and focused regression; evidence file, “Refusals and amendment preservation” |
| R4 | A Task Graph keeps archive's existing evidence behavior with and without a supersession | Local operator / CLI and filesystem | pass | Requirement 4 | Identical pending-Task refusal before and after amendment; evidence file, “Task Graph regression” |
| R5 | Canonical skill, mirror, user guide, and public help describe the command and changed archive precondition | Local operator / docs and CLI | pass | Requirement 6 | Mirror comparison, skill check, help execution, and focused text search; evidence file, “Documentation and help” |
| R6 | Every Task commit remains inside the operative authorization and required chronology | QA auditor / Git | pass | Requirement 6 | `git diff-tree` for authorization and Task commits plus ancestry checks; evidence file, “Authorization and commit scope” |
| R7 | This Spec's promises name consuming Tasks | QA auditor / Spec consistency CLI | pass | Requirement 7 | Strict public Spec check and manual reference cross-check; evidence file, “Preconditions and static gate” |
| R8 | Pull Request controls use supervised equivalent evidence because no Pull Request is open | Reviewer / Pull Request | blocked (environment: no open Pull Request; equivalent evidence recorded) | Pull Request row | Prompt facts and all five equivalent controls; evidence file, “Pull Request equivalent evidence” |

## Row evidence

### R1

- Steps: record the Daemon-supplied command, exit code, and diagnostics
  disposition.
- Expected observable: repository Verification exits `0`.
- Independent confirmation: the supervised Daemon fact names the command and
  result separately from this report.
- Persistence: not applicable to a static gate.

### R2

- Steps: copy the repository's real active Spec 0128 and archived Spec 0147
  into a disposable Git repository; archive before supersession; supersede;
  archive again; compare the saved amended tree with the archived destination.
- Expected observable: initial exit `2` names the missing Task Graph;
  supersede and the second archive exit `0`; the moved tree is byte-identical.
- Independent confirmation: fresh destination read and `diff -qr` after both
  CLI processes exit.
- Persistence: archived content remains readable at the history destination.

### R3

- Steps: exercise unknown target, absent deliverer, self-supersession, draft,
  archived-in-place, malformed, statusless, duplicate, and unknown-flag
  refusals; exercise active and archived accepted deliverers.
- Expected observable: refusals exit `2`, name their condition, and write
  nothing; accepted cases exit `0` and add only `_supersession.md`.
- Independent confirmation: clean/focused Git status reads, amendment content
  and hash re-read after process exit, plus the focused Go regression command.
- Persistence: the duplicate refusal leaves the previously written amendment's
  SHA-256 unchanged.

### R4

- Steps: invoke archive on a pending Task Graph before and after adding a valid
  supersession through the public command.
- Expected observable: both archive calls exit `2` with the identical pending
  `task_04` reason.
- Independent confirmation: fresh Git status and diff show only the amendment.
- Persistence: the second archive call reads the amended on-disk state.

### R5

- Steps: compare skill copies, run the public skill checker, execute both help
  surfaces, and search the three human-readable documents.
- Expected observable: mirrors match and every surface describes the command,
  refusals, and no-Task-Graph archive proof.
- Independent confirmation: `skills check` exits `0` after the byte comparison.
- Persistence: a repeated mirror comparison after the commands remains clean.

### R6

- Steps: enumerate every authorization and Task commit with `git diff-tree`,
  classify governed paths, and prove authorization ancestry.
- Expected observable: only the three exact bounded paths are governed changes;
  each is authorized before consumption; no folded prerequisite/consequent fix
  or derived pin exists.
- Independent confirmation: Git ancestry checks exit `0`, and the regenerated
  mirror compares byte-identically with its canonical source.
- Persistence: evidence is tied to immutable commit IDs.

### R7

- Steps: execute the strict Spec consistency command and cross-check Task 04's
  References against the declared Goals, Core Features, Success Metrics,
  Testing Approach, and API Contracts.
- Expected observable: no promise-to-Task coverage finding.
- Independent confirmation: the CLI result and direct citation cross-check
  agree.
- Persistence: both read the committed Spec artifacts at the audited head.

### R8

- Steps: apply the supervised no-Pull-Request fact to approval, checks/status,
  unresolved threads, Merge-Ready acceptance, and review-artifact ancestry.
- Expected observable: typed environment block with every control answered and
  no mutation attempted.
- Independent confirmation: the prompt separately states that the target has
  no open Pull Request and the Run Worktree branch has none of its own.
- Persistence: live Pull Request state is intentionally not carriable.

## Findings

None.

## Blocked and skipped

- R8 — `blocked (environment: no open Pull Request)`. Equivalent evidence:
  no approval was obtained; repository Verification passed; no Pull Request
  review thread stands; no Merge-Ready acceptance exists yet; and no review
  artifact claims ancestry. Unblocking action: complete the post-QA review
  policy and open the Pull Request on
  `feat/0151-supersession-the-archive-can-see`.
- Skipped rows: none.

## Coverage

All 10 coverage sources appear in 8 terminal rows: 7 passed and 1 is
environment-blocked with equivalent supervised evidence. Counts:
`rows_blocked_environment: 1`, `rows_blocked_finding: 0`, and
`rows_blocked_declared: 0`. No row failed, remained pending, or was skipped.
No frontend surface or PRD `Unreachable Acceptance` declaration applies. The
outside-evidence source was the repository's real Spec 0128 and archived Spec
0147, exercised through the current public binary in a disposable repository.

## Final verdict

Pass: every runnable criterion passed, the one environment-blocked Pull Request
row records its cause and equivalent supervised evidence, and no row is
finding-blocked, declared-blocked, failed, pending, or skipped.

## Mechanical skips

| Detector | Missing artifact |
| --- | --- |
| authorization bounded paths | Task commits |
| consequent-fix commit order | consequent-fix declaration |
