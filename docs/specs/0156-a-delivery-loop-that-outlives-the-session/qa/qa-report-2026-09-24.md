---
spec: 0156-a-delivery-loop-that-outlives-the-session
date: 2026-09-24
build: 46f4f7ee6084211aa2c2e6c44c6313d7f32e1347
auditing_binary: "roundfix 0.16.0 (/private/tmp/roundfix-0156-qa, built from 46f4f7ee6084211aa2c2e6c44c6313d7f32e1347 with -buildvcs=false)"
auditor_staleness: "current: binary built from the audited worktree before QA execution"
status: closed
verdict: pass
rows_blocked_environment: 1
rows_blocked_finding: 0
rows_blocked_declared: 0
surfaces: [backend, cli, docs]
---

# QA report — A delivery loop that outlives the session

## Scope and environment

- Scope: full authored terminal gate for build `46f4f7ee6084211aa2c2e6c44c6313d7f32e1347` on Run Worktree branch `roundfix/run-run_20260924T171847Z_7717ccb9fc2635ca`.
- Target branch: `feat/0156-durable-delivery-loop` at `ba43492a1b21d1e64586e45c03f682c76e5b2fdb`; it is an ancestor of the audited build.
- Actor: a Roundfix operator using the built CLI and the fake GitHub boundary supplied for hermetic delivery journeys.
- QA binary: `/private/tmp/roundfix-0156-qa`, reported version `roundfix 0.16.0`, built from the audited tree with `-buildvcs=false` and a task-scoped Go cache.
- Seeded mechanical auditor: `0.16.0 (e38c8561, built 2026-09-24 08:07:17 -0300)`. Its materialized skips below are preserved; the current-tree binary independently repeated the strict Spec check.
- Pull Request: none open; Pull Request journeys are environment-blocked by the Run contract.
- Repository Verification: Daemon-supervised `make verify` passed with exit 0; QA will record that result and will not rerun it.
- Precondition: `/private/tmp/roundfix-0156-qa spec check 0156-a-delivery-loop-that-outlives-the-session --strict` passed with no findings. The checker skipped only artifacts absent from this Spec: `docs/findings` rollup, `docs/backlog`, a Vocabulary Contract, and `references/_index.md`.

## Static gate

- `make verify`: passed with exit 0 in the Daemon-supervised repository Verification; not rerun by QA.
- Current-tree build and strict Spec consistency check: passed.

## Performed repairs

None.

## Assigned repair failures

None.

## Authorization audit inputs

None.

The seeded mechanical stage materialized no inputs. R8 records the final gate's independent commit-path and authorization-ancestry audit.

## Mechanical findings

None.

## Results

| # | Story / criterion / sweep | Actor and surface | Status | Provenance |
| - | --- | --- | --- | --- |
| R1 | Repository Verification is green for the assembled tree. | Daemon / repository gate | pass | Requirement 1; repository Verification |
| R2 | A two-Spec fixture queue reaches `merged` for every item without operator action between items. | Operator / delivery engine | pass | Requirement 2 |
| R3 | Resume after lost push, pull-request, and merge receipts creates no duplicate pull request and performs no duplicate merge. | Operator / delivery engine | pass | Requirement 3 |
| R4 | A non-archive commit after review parks the item as `review-stale` before publication. | Operator / delivery engine | pass | Requirement 4 |
| R5 | Missing `merge` authorization parks the item before any external action. | Maintainer / authorization boundary | pass | Requirement 5 |
| R6 | A parked item does not stop later items. | Operator / delivery engine | pass | Requirement 6 |
| R7 | Delivery tests do not reach GitHub, and both shipped skill copies describe the command family and remain byte-identical. | Maintainer / test and docs surfaces | pass | Requirement 7 |
| R8 | This Spec satisfies its promise rule and every Task commit stays inside the approved authority and chronology. | Maintainer / Spec and Git history | pass | Requirement 8 |
| R9 | Independent historical evidence supports the need for durable orchestration outside a terminal session. | Maintainer / prior Run evidence | pass | outside-evidence row |
| R10 | Pull Request approval, checks, review threads, Merge-Ready acceptance, and review-artifact ancestry are accounted for. | Reviewer / Pull Request | blocked (environment: no open Pull Request) | Pull Request row |

## Mechanical skips

These seed-stage skips are preserved as materialized. R8 supplies the later Task-commit audit, and the closed report supplies the later structure and evidence review.

| Detector | Missing artifact |
| --- | --- |
| authorization bounded paths | Task commits |
| consequent-fix commit order | consequent-fix declaration |
| QA Report structure | QA Report |
| QA evidence paths | QA Report |

## Row evidence

### R1

```yaml
inputs:
  - kind: repository_path
    ref: "**/*"
```

The Daemon ran `make verify` outside the Agent sandbox against the assembled tree and reported exit 0 with diagnostics removed on success. This is supervised static-gate evidence, not a QA rerun.

### R2

```yaml
inputs:
  - kind: repository_path
    ref: internal/delivery/**
```

`GOCACHE=/private/tmp/roundfix-0156-qa-gocache go test -count=1 -v -run '^TestDeliveryEngineMergesAQueueWithoutAnOperator$' ./internal/delivery` passed. The executable fixture processed two ordered Specs, exercised enabled review for one and a recorded `none` omission for the other, observed exactly two pull-request and two merge effects, reread both persisted items as `merged` with merge commits, found no unmatched intents, and asserted the full stage-event order for each item.

### R3

```yaml
inputs:
  - kind: repository_path
    ref: internal/delivery/**
```

`GOCACHE=/private/tmp/roundfix-0156-qa-gocache go test -count=1 -v -run '^TestDeliveryEngineResumesWithoutDoubleEffects$' ./internal/delivery` passed all three subcases: unmatched push intent, unmatched pull-request intent, and unmatched merge intent. Each subcase reread the durable queue as `merged`, asserted the cumulative external-effect counts did not increase for the already-observed action, and confirmed no unmatched intent remained.

### R4

```yaml
inputs:
  - kind: repository_path
    ref: internal/delivery/**
```

`GOCACHE=/private/tmp/roundfix-0156-qa-gocache go test -count=1 -v -run '^TestDeliveryEngineParksAStaleReview$' ./internal/delivery` passed. The fixture supplied an archive result whose parent was a foreign commit, then independently reread the first item as `parked` with blocker `review-stale`, observed zero external calls for it, and confirmed the following item still reached `merged`.

### R5

```yaml
inputs:
  - kind: repository_path
    ref: internal/delivery/**
  - kind: repository_path
    ref: docs/specs/0156-a-delivery-loop-that-outlives-the-session/_authorization.md
```

`GOCACHE=/private/tmp/roundfix-0156-qa-gocache go test -count=1 -v -run '^TestDeliveryEngineParksUnauthorizedBeforeExternalAction$' ./internal/delivery` passed. The fixture omitted `merge` authority, reread the persisted item as `parked` with blocker `unauthorized`, and asserted zero calls crossed the pull-request boundary.

### R6

```yaml
inputs:
  - kind: repository_path
    ref: internal/delivery/**
```

`GOCACHE=/private/tmp/roundfix-0156-qa-gocache go test -count=1 -v -run '^TestDeliveryEngineParksDeclaredBlockersAndContinues$' ./internal/delivery` passed. The fixture persisted five distinct blockers (`run-unresolved`, `review-findings`, `review-blocked`, `gate-failed`, and `checks-failed`), then reread the sixth and later item as `merged`.

### R7

```yaml
inputs:
  - kind: repository_path
    ref: internal/delivery/**
  - kind: repository_path
    ref: .agents/skills/roundfix/SKILL.md
  - kind: repository_path
    ref: skills/roundfix/SKILL.md
  - kind: repository_path
    ref: docs/user-guide/commands.md
```

`GOCACHE=/private/tmp/roundfix-0156-qa-gocache GOPROXY=off GOSUMDB=off go test -count=1 -v ./internal/delivery` passed every Delivery Engine and GitHub boundary case. Inspection of the executed tests found every `GitHubCLI` constructed with `scriptedCommandRunner`, every engine journey using `fakePullRequestBoundary`, and no direct `exec.Command` or HTTP client in test code; unexpected boundary commands fail the scripts. `/private/tmp/roundfix-0156-qa skills check` passed. `cmp -s .agents/skills/roundfix/SKILL.md skills/roundfix/SKILL.md` exited 0, and both copies plus `docs/user-guide/commands.md` expose `deliver start`, `status`, `resume`, and `stop`.

### R8

```yaml
inputs:
  - kind: repository_path
    ref: docs/specs/0156-a-delivery-loop-that-outlives-the-session/**
  - kind: repository_path
    ref: .agents/skills/roundfix/SKILL.md
  - kind: repository_path
    ref: skills/roundfix/SKILL.md
  - kind: repository_path
    ref: internal/cli/cli_test.go
```

The current-tree strict Spec check passed before the matrix with no findings, including the Spec promise mapping. Fresh `git diff-tree --no-commit-id --name-status -r <commit>` audits covered the authorization, Spec, and every Task commit from `b76b9732` through `46f4f7ee`. The authorization landed alone at `b76b9732` before every implementation commit. Task 04's only governed change was the expressly bounded `internal/cli/cli_test.go`; Task 05's governed changes were exactly the bounded `.agents/skills/roundfix/SKILL.md` and `skills/roundfix/SKILL.md`; other changed paths were ordinary source, docs, and assigned Task files. `git merge-base --is-ancestor b76b9732 <task-commit>` exited 0 for Tasks 01-05 and 07. No prerequisite fix was folded into a Task commit, and no declared consequent-fix commit exists.

### R9

```yaml
inputs:
  - kind: external_repository
    ref: /Users/marcio/dev/roundfix
  - kind: elapsed_time
    ref: Spec 0154 Run chronology measured on 2026-09-24
```

Outside source: the machine-wide Run Database as read from the separate user checkout `/private/tmp/claude-501/-Users-marcio-dev-roundfix/0f9d9d2d-9b0a-4336-8d50-a94fb7322388/scratchpad/wt-0156`, not this Spec's artifacts. The installed `roundfix 0.15.0`, which matches that database's schema 13 read surface, ran `roundfix runs list --all --state all --limit 0` and showed three Spec 0154 Runs starting at `09:38:16Z`, `11:26:56Z`, and `19:03:36Z`, with durations `1h6m`, `32m`, and `37m`. Independent `roundfix events <run-id> --filter outcome` reads confirmed each reached `Clean` at `10:44:37Z`, `11:59:47Z`, and `19:40:44Z`. The observed gaps between a Clean outcome and the next Run start were about 42 minutes and 7 hours 4 minutes, while the processes themselves completed; this independently supports the Spec's claim that orchestration gaps, rather than Run execution, dominate elapsed delivery time.

### R10

```yaml
inputs:
  - kind: live_service
    ref: Pull Request for feat/0156-durable-delivery-loop
  - kind: repository_path
    ref: docs/specs/0156-a-delivery-loop-that-outlives-the-session/_authorization.md
```

The prompt proves there is no open Pull Request for `feat/0156-durable-delivery-loop`; the Run Worktree branch is never pushed and has no Pull Request of its own. Equivalent evidence for every control: approval — the Spec authorization is `approved`, while no Pull Request approval exists yet; checks and status — Daemon-supervised `make verify` passed with exit 0 and the current-tree strict Spec check passed; unresolved review threads — none can exist without a Pull Request; Merge-Ready acceptance — no review has run yet because the authored QA gate precedes pre-PR review; review-artifact ancestry — no file exists under the Spec's `reviews/` tree, the target is `ba43492a1b21d1e64586e45c03f682c76e5b2fdb`, the audited Run head is `46f4f7ee6084211aa2c2e6c44c6313d7f32e1347`, and `git merge-base --is-ancestor feat/0156-durable-delivery-loop HEAD` exited 0. Unblocking action: integrate the Run, perform the configured pre-PR review and archive stages, then open the Pull Request from the target branch.

## Findings

None.

## Blocked and skipped

- R10 is blocked by the declared environment fact that no Pull Request is open. Every Pull Request control has equivalent observed or supervised evidence, so this environment block does not prevent a passing verdict.

## Coverage

All 10 planned rows reached terminal outcomes: 9 passed and 1 was environment-blocked with equivalent evidence. Coverage is 8 of 8 declared Requirements, the repository Verification, the outside-evidence row, and the Pull Request row. `rows_blocked_environment: 1`, `rows_blocked_finding: 0`, and `rows_blocked_declared: 0`. The PRD declares no Unreachable Acceptance rows and no frontend surface. No matrix row was skipped.

## Final verdict

Pass. Every runnable row passed; R10 is environment-blocked because no Pull Request exists and records equivalent evidence for approval, checks and status, unresolved review threads, Merge-Ready acceptance, and review-artifact ancestry. There are no findings, declared blocks, finding blocks, skipped rows, or unresolved evidence paths.
