# Configurable artifact directory

Roundfix stores markdown Round and Review Issue artifacts in `<git-root>/.roundfix/` by default, but lets User Config or Project Config override the Artifact Directory. This keeps markdown artifacts local to the repository by default while allowing teams to place review artifacts in a preferred controlled directory such as a user-level Roundfix folder or a repository docs folder.
