# Tooling authorization — Spec 0079, one door for fleet knowledge (2026-08-06)

On 2026-08-06, at the close of the v0.4.0 session, the maintainer granted the
full tooling boundary for Spec 0079 in the write-prd authorization gate,
choosing "Grant completo" over the reduced and pause options presented with
exact paths.

## Authorized paths

bounded files:

- `.agents/skills/write-idea/**` and its `skills/write-idea/**` mirror,
  regenerated by `make skills-sync` — the inbox-first reading rule and the
  research-capture step (F7, F11).
- `.agents/skills/write-prd/**` and its `skills/write-prd/**` mirror,
  regenerated by `make skills-sync` — the inbox-first reading rule (F7).
- `internal/baseline/assets/modules/context-workflow.json` — the backlog,
  findings, rollup, archival, and inbox-flow clauses (F3, F4, F5).
- `internal/baseline/assets/modules/secondbrain.json` — the carve-out
  amending the write prohibition: sessions may create files under `inbox/**`
  in the Secondbrain and nothing else (F1, F11).
- `docs/agents/docs-layout.md` and `docs/agents/secondbrain.md` — adoption of
  the regenerated managed-block postimages, in the Spec 0075 choreography.

Deterministic digest fallout of these edits is sanctioned by ADR-0081 and
needs no separate grant; any additional byte rewritten only by the sanctioned
regeneration chain is that fallout.

## Outside this grant, recorded as external dependencies

These surfaces are not this repository's tooling and are not granted here;
each is executed under its own contract with the maintainer's approval there:

- `~/.claude/skills/knowledge-workspace/**` — the maintainer's global skill
  carrying the door mechanics and templates.
- The maintainer's user-level hook configuration for the session-end capture
  (F8).
- The `secondbrain` repository — its `AGENTS.md` inbox carve-out, the
  `inbox/` namespace creation, and the qmd collection, governed by that
  repository's own contract.

## Boundary

The grant is bounded by purpose: teaching the authorial skills to route
through the capture door and carrying the fleet-knowledge clauses to the
guides and their Baseline modules. It does not reach other members of
`OWNED_SKILLS`, and a Spec needing one asks separately.
