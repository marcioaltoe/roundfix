# Tooling authorization — autonomous authoring is implicit (2026-08-01)

The maintainer directed that choosing autonomous work already authorizes
creating the TechSpec and Task Graph for every Spec that needs them, and
instructed that the guidance be changed where it does not say so, then
committed and pushed.

## Authorized paths

- `.agents/skills/write-tasks/SKILL.md` — replace the precondition's
  confirmation gate for a missing TechSpec with "author it, then decompose".
- `skills/write-tasks/SKILL.md` — the embedded mirror, regenerated by
  `make skills-sync`; it is an `OWNED_SKILLS` member and cannot drift.

Deterministic fallout of the authorized edit, sanctioned by ADR-0081:
`internal/baseline/assets/setups/go-cli.json`,
`internal/baseline/assets/setups/rust-cli.json`,
`internal/baseline/assets/setups/typescript-bun.json`,
`internal/baseline/testdata/catalog.digest`,
`internal/baseline/testdata/catalog.normalized.json`,
`internal/baseline/testdata/parity-corpus/v1/fixtures/asset-sync.json`,
`internal/baseline/testdata/parity-corpus/v1/manifest.json`, and
`internal/baseline/testdata/catalog.diagnostics.golden.json`.

## Not tooling

`docs/agents/autonomous-work.md` carries the rule itself. The changed lines sit
outside the `setup-context-driven` markers, so they are repository-authored
content and need no tooling authorization. The managed block was already
permissive — it says only "The Supervisor authors Specs" — so no generated
guidance had to change and the Baseline module was not touched.
