---
spec: 0072-qa-is-a-task-not-a-flag
date: 2026-08-03
build: f20aeb9c0d7632d783c042544efc7bc960d1f4a3
status: closed
verdict: fail
rows_blocked_environment: 1
rows_blocked_finding: 15
surfaces: [backend, cli, docs]
---

# QA report — QA is a Task, not a flag

## Scope and environment

Full Spec QA is planned against Run Worktree build
`f20aeb9c0d7632d783c042544efc7bc960d1f4a3` on macOS in the per-Run branch
`roundfix/run-run_20260803T145834Z_f352a6cf014bbea1`. The actor-facing entry
point is the built `roundfix` CLI; documentation journeys begin from the
repository-owned skills and agent guides. The Pull Request fact supplied to
the gate is `none open`, so the read-only Pull Request journey is planned as
environment-blocked. This checkout may write only this report and its QA
evidence; it will not commit, push, open a Pull Request, or change Task status.

This Spec has an explicit bootstrap exception in `_techspec.md`: the Run-start
binary predates Task Type `qa`, so this Spec keeps its v1 manifest and invokes
its own closing gate through the legacy Run boundary. The supplied QA prompt
is the observed invocation authority for this one gate. The new authored-node
contract is still exercised as the product under test.

## Static gate

`rtk make verify` ran alone against the report build and exited `2`. Its test
stage reported 3,092 passed, 7 failed, and 2 skipped across 24 packages:

- `internal/baseline`: four `TestBaselinePlanCharacterization` cases still
  expect catalog digest
  `sha256:2c10850ac7a7357963ca3c7f7168102560a194ed6d6b9c7234df5ef3494abcbb`
  while the current catalog produces
  `sha256:cda1727aacd1976a213d5a75d36b21d0a32f12832b886615666278b2378a0ffd`;
  `TestCatalogDiagnosticCharacterization` also contains the prior mutated
  Rust setup-digest diagnostic.
- `internal/spec`: `TestCoverageEquivalence` reports fourteen newly executed
  QA-contract tests and three removed prior test identities that have not been
  reconciled in the coverage-equivalence contract.

Focused reproductions with writable task-local Go caches exited `1` with the
same assertions:

```text
rtk env GOCACHE=/private/tmp/roundfix-qa0072-gocache-baseline go test ./internal/baseline -count=1 -run '^(TestBaselinePlanCharacterization|TestCatalogDiagnosticCharacterization)$'
rtk env GOCACHE=/private/tmp/roundfix-qa0072-gocache-spec go test ./internal/spec -count=1 -run '^TestCoverageEquivalence$'
```

The failures are deterministic contract mismatches, not environment failures.
Per the gate contract, code-caused repository Verification failure stopped
public flow QA.

## Results

| # | Story / criterion / sweep | Actor and surface | Status | Evidence |
| - | --- | --- | --- | --- |
| 1 | QA entry precondition: every implementation Task is completed; the Spec's documented v1 bootstrap exception accounts for the absent authored QA node | QA Agent; Task Graph | pass | All six Task files are `completed`. `_tasks.md` and `_techspec.md` both record the bootstrap exception, and the supplied Run prompt invokes this one legacy closing gate. |
| 2 | Project Constraint: identifier strategy accounts for Task Type `qa` and creates no second owner for status or dependencies | Maintainer; backend/docs | pass | Both active artifacts account for the identifier strategy and cite `docs/agents/domain.md`; the TechSpec keeps status in Task files and dependencies in the Task Graph, matching `CONTEXT.md`. |
| 3 | Project Constraint: authentication and HTTP are inapplicable with an operative source and no invented credential or transport policy | Maintainer; backend/docs | pass | Both active artifacts give a reason and cite `docs/agents/agent-instructions.md`; the six exact Task diffs add no authentication, HTTP, credential, or transport surface. |
| 4 | Project Constraint: active ADR obligations preserve ADR-0080 verdict semantics and implement ADR-0088/ADR-0091 | Maintainer; backend/docs | pass | ADRs 0080, 0088, and 0091 are accepted and cited. The planned live compatibility check was stopped by F-002, but applicability and source accounting are complete. |
| 5 | Project Constraint and tooling audit: exact bounded skill paths are expressly authorized in both active Spec artifacts; authorization precedes the Task; commit scope and sanctioned digest fallout match it | Maintainer; docs/tooling | fail | F-001. The cited authorization grant names only Specs 0063, 0064, 0065, and 0067, plus later addenda for 0071; `rtk rg -n '0072'` finds no grant. The PRD says only “the owned skill pair,” not exact bounded paths. See tooling audit below. |
| 6 | Repository Verification passes on the current build | Maintainer; backend/cli/docs | fail | F-002. `rtk make verify` exited 2: 3,092 passed, 7 failed, 2 skipped. Focused reruns reproduce Baseline characterization and coverage-equivalence failures. |
| 7 | An author includes QA once: the manifest names one terminal `type: qa` Task whose dependencies cover every non-QA leaf | Spec author; docs/Task Graph | blocked (finding: F-001) | Governance preflight and the red static gate prohibit flow QA on this build. Task-level tests are supporting evidence only. |
| 8 | An author legitimately declines QA once with `qa: declined` and a non-empty `qa_reason`; a missing reason is rejected | Spec author; docs/Task Graph | blocked (finding: F-001) | Governance preflight and F-002 stopped the public scratch-Spec journey. Probe not run. |
| 9 | `roundfix implement --spec <slug>` runs an unsettled authored gate after all implementation Tasks complete, without a QA parameter | Operator; CLI/backend | blocked (finding: F-001) | Public end-to-end Run not started on a governance-invalid, verification-red build. Replay probe not run. |
| 10 | The removed `--qa` parameter fails preflight as unknown, names the Task Graph remediation, writes no requested output, and creates no Run | Operator; CLI | blocked (finding: F-001) | Public CLI journey and `runs list` confirmation not run after preflight failure. Task Result evidence is not a user-seat substitute. |
| 11 | Appending a Task after a completed gate invalidates the result and names every inserted or unsettled dependency | Spec author; CLI/backend | blocked (finding: F-001) | Public out-of-order append probe not run. Static coverage confirms the named test exists but cannot prove the journey. |
| 12 | A QA node that is non-terminal or fails to cover a leaf is rejected with the uncovered or dependent Task named | Spec author; CLI/backend | blocked (finding: F-001) | Public malformed-graph probes not run after governance/static stop. |
| 13 | A declined or legacy graph never starts a gate or emits gate events, and an all-completed graph is a no-op | Operator; CLI/backend | blocked (finding: F-001) | Public repeated-invocation and event-confirmation probes not run. |
| 14 | A failed implementation dependency withholds the gate, leaves it pending and resumable, and emits no QA report or event | Operator; CLI/backend | blocked (finding: F-001) | Public failed-Task Run and Run Event Stream confirmation not run. |
| 15 | A hand-edited `type: qa` node cannot enter an ordinary Agent Session | Operator; CLI/backend | blocked (finding: F-001) | Public malformed-graph Agent-routing probe not run. |
| 16 | The gate's report path, verdict vocabulary, typed blocked counts, and `daemon.qa` event semantics remain compatible with ADR-0080 | QA Agent/operator; CLI/backend | blocked (finding: F-001) | This report has the required fields, but public pass/partial/fail scratch Runs were stopped by F-001/F-002. |
| 17 | Gate executions remain countable as terminal Task history; the three Spec 0057 closings are legible as three | Maintainer; docs/backend | blocked (finding: F-001) | Historical artifact inspection cannot replace the planned public history journey on the assembled build. |
| 18 | Legacy Specs load unchanged and every archived artifact remains byte-identical | Maintainer; CLI/backend/docs | blocked (finding: F-001) | No public archived-manifest load was run. Task-level characterization is supporting evidence only. |
| 19 | Repository guides and shipped skills consistently teach the authored gate, contain no stale `--qa` guidance, and are followable without undocumented knowledge | Supervisor/Spec author; docs | blocked (finding: F-001) | A static `rtk rg` found no stale parameter in the guides/skill pair, but the documented public flow was not runnable after preflight. |
| 20 | CLI surface sweep: Implement help omits the parameter; output/diagnostic streams and exit codes are deterministic and non-interactive | Operator/automation; CLI | blocked (finding: F-001) | Help, stream, exit-code, and non-interactive public sweep not run after the gate stopped. |
| 21 | Non-Goals: gate matrix derivation, verdict meanings, and the Daemon's withholding remain unchanged; no runtime skip path appears | Maintainer/operator; backend/cli | blocked (finding: F-001) | Changed-path inspection supports scope, but live dependency and verdict behavior was not exercised. |
| 22 | Non-Goal: Spec 0071's test-execution-cost boundary is not widened by this Spec | Maintainer; backend/docs | pass | `git diff --name-status ad7d205..HEAD` changes no `Makefile`, `.github/workflows/`, or Spec 0071 path. `gate-cost-2026-08-03.md` keeps the static gate and user-flow work intact and records the micro-trim honestly. |
| 23 | Pull Request delivery/review journey | Maintainer; Pull Request | blocked (environment: no open Pull Request) | The supplied fact confirms no open Pull Request. `HEAD`, `ma/qa-task-not-flag`, and `origin/ma/qa-task-not-flag` all resolve to `f20aeb9`; this equivalently proves build/branch identity but cannot prove Pull Request checks, approval, or unresolved-thread state. Unblock by opening the Spec Pull Request on `ma/qa-task-not-flag`. |

## Findings

### F-001 — Spec 0072 lacks traceable, exact tooling authorization

Impact: `Blocks-Completion`.

The maintainer/QA Agent reaches the Project Constraint audit before flow QA.
The PRD and TechSpec both claim that
`docs/workflow/authorizations/2026-08-02-queued-spec-tooling.md` authorizes
this Spec's owned-skill changes. The cited source instead says it unblocks
four named Specs—0063, 0064, 0065, and 0067—and its later addenda cover other
bounded work, including 0071. It contains no `0072` entry. The PRD also uses
the category “the owned skill pair” instead of exact repository-relative
paths, so the two active artifacts do not both carry the required exact
boundary.

Reproduction:

1. Read the Tooling authority row in `_prd.md` and `_techspec.md`.
2. Run
   `rtk rg -n '0072|Which Spec uses what|Authorized paths' docs/workflow/authorizations/2026-08-02-queued-spec-tooling.md`.
3. Observe headings for the path grant but no match for `0072`, then read the
   “Which Spec uses what” list naming only 0063, 0064, 0065, and 0067.
4. Inspect Task 04 commit `160d417` with
   `git diff-tree --no-commit-id --name-only -r`: it changes the two canonical
   skills, mirrors, and derived digest paths.

Expected: an authorization record explicitly names Spec 0072 and exact bounded
paths, and both active Spec artifacts cite that same boundary. Actual: the
claimed source does not authorize 0072, and the PRD boundary is not exact.

The complete chronology/scope audit found no additional shape defect:
authorization-source commit `2e560ce` and Spec commit `ad7d205` are ancestors
of Task 04 commit `160d417`; authorization was not folded into the Task;
`git diff-tree` for all six Task commits found protected tooling only in Task
04; its changed paths stay within the skill/mirror and ADR-0081 derived-pins
shape; and `rtk make baseline-digests` in an isolated archive of `HEAD` exited
0 with `changed:false`, proving current derived pins match canonical sources.
No prerequisite or consequent tooling fix was folded into or misordered around
the Task commit.

Affected rows: 5 and 7–21.

### F-002 — Repository Verification is red on the assembled build

Impact: `Blocks-Completion`.

The maintainer runs the repository's sole completion gate through its public
operational path. `rtk make verify` exits `2` during `go test` with deterministic
Baseline characterization drift and coverage-equivalence drift. The focused
commands in the Static gate section reproduce both groups with writable caches.

Expected: `rtk make verify` exits 0 with no formatting, test, skill, or build
failure. Actual: it exits 2 with seven failing tests. The failures require an
intentional contract reconciliation in an authorized corrective Task; QA did
not edit tests, fixtures, generated expectations, or implementation code.

Affected rows: 6 and 7–21.

## Blocked and skipped

- Rows 7–21 are `blocked (finding: F-001)`. F-001 independently blocks flow
  QA at the Project Constraint preflight; F-002 independently requires the
  same stop at the static gate. Unblock by recording exact Spec 0072 tooling
  authority in both active artifacts and the source grant, then settling the
  Baseline and coverage contract failures through an authorized corrective
  Task and rerunning `rtk make verify`.
- Row 23 is `blocked (environment: no open Pull Request)`. Local and remote
  target-branch identity is proven, but no equivalent evidence can establish
  Pull Request checks, approval, or thread state. Open the Pull Request on
  `ma/qa-task-not-flag`, then observe it read-only in the rerun.
- No row is skipped and no row remains pending.

## Coverage

Results: 5 passed, 2 failed, 15 finding-blocked, 1 environment-blocked, 0
skipped, and 0 pending. `rows_blocked_environment: 1` and
`rows_blocked_finding: 15` match the table exactly.

The matrix maps all seven Core Features, all six Success Metrics, all four
Non-Goals, all three declared surfaces, the four Project Constraint classes,
repository Verification, and the supplied Pull Request fact. Planned high-risk
probes covered invalid input, out-of-order graph growth, replay after
settlement, and repeated no-op invocation; none ran because governance and
static gates stop flow QA first. No frontend surface is declared.

## Final verdict

Fail: the assembled build cannot enter public flow QA until Spec 0072 carries
traceable exact tooling authorization and `rtk make verify` passes; current
counts are 5 pass, 2 fail, 15 finding-blocked, 1 environment-blocked, and no
skipped or pending rows.
