# Regeneration and boundary evidence

## Owned-Skill journey

A fresh local clone at `/private/tmp/spec0067-qa-rerun-vIIe3j/repo` was made
from build `c6cf8033`. QA added a scratch-only comment to the owned
`.agents/skills/qa-gate/SKILL.md`, then ran these public maintainer commands:

1. `rtk make skills-sync` exited 0 and changed the canonical Skill plus its
   `skills/qa-gate/` mirror.
2. First `rtk make baseline-digests` exited 0 with `"changed":true`. It
   regenerated the three setup assets, catalog diagnostic, digest and
   normalized catalog, both parity-corpus generated files, and all four
   plan-characterization goldens.
3. First `rtk make verify` exited 0: 3,349 Go tests passed across 26 packages,
   the isolated corpus budget and four Skill tests passed, the Repository Skill
   Set check passed, the binary built, and Spec 0067 had no consistency
   findings.
4. Second `rtk make baseline-digests` exited 0 with
   `baseline-digests: no changes` and `"changed":false`.
5. Second `rtk make verify` also exited 0 with the same gate counts.

This directly repairs prior finding F-001: one sanctioned invocation now
leaves Verification green, and a second invocation is idempotent.

## Ownership, negative, measurement, and remediation probes

From the Run Worktree:

```text
rtk go test ./internal/baseline -count=1 \
  -run '^(TestDerivedOwnership.*|TestMeasuredSanctionedOwnershipMatchesRecords|TestDeclaredStepRegenerationAndFrozenBoundaries)$' -v
exit 0 — 26 passed

rtk go test ./internal/baseline -count=1 \
  -run '^TestDerivedOwnershipRemediation' -v
exit 0 — 6 passed
```

The selected tests exercise the real scan plus zero-record, multiple-record,
schema, nonexistent-command, wrong-flag, successful-no-op, frozen-rewrite,
measured sanctioned ownership, second-run persistence, and all remediation
owner classes. Their isolated repositories restore their derived snapshots.

A supplemental direct Go invocation in the scratch clone hit the sandboxed
host Go cache and exited before product behavior. It was not retried. The
worktree-local remediation suite above provides equivalent observed evidence,
and `make baseline-digests` restored the scratch perturbation successfully.

## Remaining contract contradiction

Measured public behavior and the records agree:

- `internal/baseline/testdata/parity-corpus/_ownership.yml` says
  `owner: sanctioned` and names the two files rewritten after an owned-Skill
  edit.
- The first scratch sanctioned run did rewrite
  `parity-corpus/v1/fixtures/asset-sync.json` and `parity-corpus/v1/manifest.json`.
- The plan-characterization and catalog-diagnostic records also say
  `owner: sanctioned`, matching `BASELINE_DIGEST_STEPS` and the measured test.

The active Spec says the opposite: `_prd.md` lines 14, 73, 88, 98, and 105
require the parity corpus to be frozen and regenerated by nothing;
`_techspec.md` lines 13, 66, 91-94, 121, 182-190 repeat that contract.
Corrective `task_06.md` lines 27, 48-50, and 168-170 explicitly leave the
decision unresolved. The corrective records are truthful, but the assembled
feature still violates the PRD's frozen boundary and Non-Goal.
