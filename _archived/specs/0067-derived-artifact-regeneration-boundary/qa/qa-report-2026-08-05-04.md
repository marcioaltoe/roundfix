---
spec: 0067-derived-artifact-regeneration-boundary
date: 2026-08-05
build: e91bf4088b7547ab1f1c4a15c78d1427e769f032
status: closed
verdict: pass
rows_blocked_environment: 0
rows_blocked_finding: 0
rows_blocked_declared: 0
surfaces: [backend, infra, docs]
---

# QA report — Derived artifact regeneration boundary

## Scope and environment

Full rerun of the authored terminal gate after the documentation correction at
`e91bf408`, on macOS in the isolated Run Worktree. Actors are a Roundfix
maintainer changing an owned Skill, a contributor adding a derived artifact,
and an operator responding to a strict-load failure. Public entry points are
`make baseline-digests`, `make verify`, documented `go test` regeneration
commands, and ownership records stored beside derived artifacts.

The PRD declares backend, infra, and docs surfaces. It declares no frontend,
data migration, live service, credential, deployment, release, or Pull Request
journey. The prompt confirms no Pull Request is open; no planned product row
depends on one. The PRD has no `## Unreachable Acceptance` declarations.

High-risk probes are the previously failing documentation count, its adjacent
ownership-resolution canary, one-invocation owned-Skill recovery, a second
idempotency run, unclassified and multiply classified artifacts, invalid and
overlapping exceptions, an out-of-directory exception, ineffective dedicated
commands, a frozen resolved path exposed to regeneration, independently
measured record truth, and unchanged derived bytes.

Evidence directory:
`qa/evidence/2026-08-05-spec-0067-rerun-04/`.

## Static gate

`rtk make verify` ran unpiped from the Run Worktree root and exited 0. It
reported 3,382 passing Go tests across 26 packages, one passing isolated corpus
budget test, four passing Skill tests, a passing Repository Skill Set check, a
successful build, and no Spec Consistency Check finding for Spec 0067. The two
Spec 0067 skips concern an absent Vocabulary Contract section and
`references/_index.md`; neither is required by this Spec or on an acceptance
path.

Evidence: [`static-gate.md`](evidence/2026-08-05-spec-0067-rerun-04/static-gate.md).

## Results

| # | Story / criterion / sweep | Actor, entry point, and surface | Steps, expected observable, independent confirmation, persistence, and evidence | Status |
| - | --- | --- | --- | --- |
| Q-01 | Authored gate and dependency preconditions | QA Agent · `_tasks.md` and Task files · docs | `_tasks.md` names `task_05` as the sole terminal `qa` node. Its completed dependency `task_07` transitively closes every non-QA leaf; `task_05` remains Daemon-owned pending during this gate. | pass |
| Q-02 | Project Constraint — identifier strategy | Maintainer · PRD/TechSpec and implementation range · docs | Both active Spec artifacts record non-applicability with `docs/agents/domain.md`; commit-range inspection found no new Internal Identifier or changed path, digest-field, or diagnostic-code identity contract. | pass |
| Q-03 | Project Constraint — authentication and HTTP | Maintainer · PRD/TechSpec and implementation range · docs | Both artifacts record local-only non-applicability with `docs/agents/agent-instructions.md`; the implementation and public journeys introduce no auth, HTTP, secret, transport, or deployment surface. | pass |
| Q-04 | Project Constraint — active ADRs | Maintainer · accepted ADRs, PRD/TechSpec, public regeneration flow · docs/backend | ADR-0080/0081/0085/0091 are accepted and applicable; ADR-0093 is correctly non-applicable to product behavior. Regeneration fallout, strict-load, authored-node, and verdict semantics align in the assembled artifacts. | pass |
| Q-05 | Tooling authority, exact scope, and commit choreography | Maintainer · authorization record and Git ancestry · infra/docs | Authorization `2e560cea` predates Task 03. Fresh `git diff-tree` inspection proves Task 03 changes only `Makefile` plus its Task file; Task 06 is a separate descendant with the authorized `Makefile` and named product/test paths; Task 07 and the documentation correction touch no protected tooling. See [`governance-and-range.md`](evidence/2026-08-05-spec-0067-rerun-04/governance-and-range.md). | pass |
| Q-06 | Repository Verification | Maintainer · `rtk make verify` · backend/infra/docs | Unpiped `rtk make verify` exited 0 on `e91bf408`: 3,382 Go tests, the isolated corpus budget, four Skill tests, Repository Skill Set check, build, and Spec 0067 consistency check all passed. | pass |
| Q-07 | Every derived path resolves to exactly one stated owner | Contributor · focused ownership suite · backend/docs | The fresh focused command exited 0. The real exhaustive scan passed before and after negative probes, with each actual artifact resolved by its most specific record or exception. | pass |
| Q-08 | Unclassified and multiply classified artifacts fail | Contributor · negative ownership fixtures · backend | Zero-record, multiple-record, and duplicate-exception fixtures passed only after observing their named rejection; the unchanged real scan remained green. | pass |
| Q-09 | Record and exception schema is enforceable | Contributor · focused schema/declared-step suite · backend/docs | Invalid owner, missing reason/command, outside-directory and duplicate exceptions, nonexistent/wrong commands, and successful no-op probes all observed the required rejection. Isolated snapshots restored. | pass |
| Q-10 | Parity corpus is frozen except for exactly two sanctioned files | Maintainer · ownership record and tracked-tree count · docs/infra | The reproducible tracked-tree count is 17 artifacts. Its record is `frozen` with exactly the two required sanctioned exceptions, and the focused known-boundaries check resolves the other 15 artifacts frozen. See [`ownership-and-remediation.md`](evidence/2026-08-05-spec-0067-rerun-04/ownership-and-remediation.md). | pass |
| Q-11 | Frozen paths reject rewrites and sanctioned exceptions accept them | Maintainer · isolated regeneration fixture · backend/infra | The isolated frozen-resolved-path probe observed `rewrote frozen artifact`; the sanctioned command restored both parity exception files while preserving every frozen path. | pass |
| Q-12 | Every declared regeneration class behaves as recorded | Maintainer · isolated declared-step and measured-owner suites · backend/infra | The 66.047-second focused command exited 0 for synthetic dedicated repair, sanctioned/frozen measurement, ineffective-command rejection, second-run persistence, unrelated-byte preservation, and snapshot restoration. | pass |
| Q-13 | One sanctioned command repairs an ordinary owned-Skill edit | Maintainer · scratch clone, `make skills-sync`, `make baseline-digests`, `make verify` · infra | In a fresh clone, `make skills-sync` followed by one `make baseline-digests` reported `changed:true`; the immediately following unpiped `make verify` exited 0 with the full gate green. See [`owned-skill-regeneration.md`](evidence/2026-08-05-spec-0067-rerun-04/owned-skill-regeneration.md). | pass |
| Q-14 | A second sanctioned run is a no-op and stays green | Maintainer · same scratch clone · infra | The second scratch regeneration reported `no changes` and `changed:false`; a second unpiped `make verify` exited 0 with the same 3,382-test gate result. | pass |
| Q-15 | Failure remediation names only an effective human action | Operator · strict-load failure and remediation suite · backend/docs | The focused owner-class suite passed. A direct scratch `matrix.json` mismatch made the strict test exit 1 with `nothing regenerates`, the record path, and the 2026-07-30 reason; restoring the byte made the same test pass. | pass |
| Q-16 | No digest value or derived artifact content moved in this Spec | Maintainer · per-Task Git provenance and post-flow status · infra | Per-Task `git diff-tree` provenance shows no Spec implementation commit changed a digest or derived artifact byte. Run Worktree derived roots stayed clean after QA. | pass |
| Q-17 | Non-Goals remain excluded | Maintainer · Git range, strict load, and scratch regeneration · backend/infra | No digest algorithm or derived content changed in a Spec Task, strict loads still fail closed, the scratch sanctioned run touched only the two parity exceptions, and all 15 frozen parity artifacts stayed unchanged. | pass |
| Q-18 | Documentation is truthful and usable without hidden knowledge | Maintainer · PRD, TechSpec, Task Graph, Task 07, ownership record, and public commands · docs | `_tasks.md` and `task_07.md` now state 17 artifacts, two sanctioned exceptions, and 15 frozen artifacts, matching a fresh `git ls-tree` count, the ownership record, and measured behavior. The focused Q-10 ownership canary passed on the same build. | pass |

## Findings

None.

## Blocked and skipped

None. Every planned row ran. The prompt confirms no Pull Request is open, but
the PRD defines no Pull Request acceptance journey, so no product row depends
on that unavailable surface.

## Coverage

All 18 rows passed. Counts are `rows_blocked_environment: 0`,
`rows_blocked_finding: 0`, and `rows_blocked_declared: 0`; no row was skipped.
All three declared surfaces ran. Frontend and TUI sweeps are not applicable
because the PRD declares neither. The owned-Skill, one-run recovery, second-run
idempotency, unclassified, multiple-record, duplicate/outside exception,
invalid and ineffective command, frozen-rewrite, exception precedence,
measured-owner, remediation, strict-load, unchanged-content, corrected-docs,
and Non-Goal probes ran.

## Final verdict

Pass: every runnable story and criterion passed on build `e91bf408`; there are
no findings, blocked rows, declared-blocked rows, or skipped rows. Result: 18
pass, 0 fail, 0 blocked, 0 skipped.
