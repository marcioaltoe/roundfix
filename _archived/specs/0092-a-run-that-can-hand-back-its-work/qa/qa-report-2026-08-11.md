---
spec: 0092-a-run-that-can-hand-back-its-work
date: 2026-08-11
build: 9ee875c32afbdcd538baec596b41df410fd735d1
status: closed
verdict: fail
rows_blocked_environment: 7
rows_blocked_finding: 0
rows_blocked_declared: 0
surfaces: [backend, cli]
---

# QA report — A Run that can hand back its work

## Scope and environment

Full authored terminal gate for `task_07`, against Run Worktree build
`9ee875c32afbdcd538baec596b41df410fd735d1`. The actor is a Roundfix maintainer
using the built `roundfix` CLI. Real flows use isolated scratch repositories and
Run stores; the gate never mutates the user checkout, commits this report,
pushes, or opens a Pull Request.

Authoring precondition: `rtk roundfix spec check
0092-a-run-that-can-hand-back-its-work --strict` exited 0 with `No findings.`
The manifest names `task_07` as `qa`; it is terminal and all non-QA leaf
dependencies are completed. The PRD declares no `Unreachable Acceptance` rows.
The prompt confirms no Pull Request is open, so no Spec acceptance relies on a
Pull Request journey.

Evidence target: `qa/evidence/2026-08-11-run-gate/command-evidence.md`.

## Static gate

The repository cache named by the Makefile was cleared with
`GOCACHE=<worktree>/.gocache go clean -testcache`. The first full attempt hit a
sandbox denial at `api.github.com`, so the cache was cleared again and the gate
was rerun with the repository integration boundary authorized.

The authoritative cold `rtk make verify` exited 2. One test failed:
`TestRunReconcileJSONMatchesTextFields` observed the new top-level JSON field
`carryForwards`, while its exact expected field set omits it. Every other listed
package passed without `(cached)`. A focused uncached rerun of that single test
also exited 1. Finding F-01 records the code-caused contract drift.

## Results

| # | Story / criterion / sweep | Actor and surface | Steps | Expected observable | Independent confirmation and persistence | Evidence | Status |
| - | --- | --- | --- | --- | --- | --- | --- |
| QA-01 | Goal 1 and ADR-0104 outside evidence: a real Run whose preferred runtime cannot serve activates its configured Fallback Selection | Maintainer; built CLI and daemon backend | Run an isolated external scratch repository with a preferred selection that cannot start and a proven fallback; record Run ID, unavailability, selection failure, notification, and fallback tuple | Preferred attempt ends as `agent_selection_failed` before Agent output; the configured fallback is attempted after notification | Re-read Run events and persisted selection attempts by Run ID; reopen the Run outcome | No live Run ID: the no-commit boundary prevents constructing the committed disposable Spec target or nesting a Run in this active Run Worktree. Equivalent current-build evidence: the three selection/fallback command groups in [command evidence](evidence/2026-08-11-run-gate/command-evidence.md). | blocked (environment: no authorized committed disposable Spec target for a live fallback Run) |
| QA-02 | Goal 1 guard: Agent output before failure makes fallback ineligible; work-started is emitted once | Maintainer; built CLI and daemon backend | Run the high-risk post-output failure case and a multi-turn Session case | `agent_work_started` precedes the first Agent output exactly once; no fallback attempt follows a post-output failure | Re-read persisted events and attempts after process exit | Fresh agent, daemon, and public-CLI integration cases all passed; see [command evidence](evidence/2026-08-11-run-gate/command-evidence.md). | pass |
| QA-03 | Goal 2: a failed Batch preserves at least one resolved Review Issue while unfinished work leaves the Run `Unresolved` | Maintainer; built CLI and daemon backend | Drive a Batch that resolves one issue and then fails with another unresolved | Resolved issue keeps its terminal outcome; unfinished issue is failed; Run exits 1 as `Unresolved` | Re-read both issue artifacts and the terminal Run record after command exit | No open Pull Request exists, so the real Review Source journey is unreachable. Equivalent current-build agent/daemon/CLI cases all passed; see [command evidence](evidence/2026-08-11-run-gate/command-evidence.md). | blocked (environment: no open Pull Request with a runnable multi-issue Batch) |
| QA-04 | Goal 5: no path reports `Clean` while unresolved work remains | Maintainer; CLI/backend regression sweep | Exercise Batch failure, Verification failure, later-Batch continuation, and event-journal paths that can derive a terminal outcome | Every case with a positive unresolved count ends `Unresolved`; `Clean` appears only with zero unresolved work | Inspect public exit/outcome plus persisted issue set for each named path | Four daemon and four CLI integration cases passed on the current build; see [command evidence](evidence/2026-08-11-run-gate/command-evidence.md). | pass |
| QA-05 | Goal 2 positive seam: a failed Batch with every Review Issue resolved may end `Clean` | Maintainer; built CLI and daemon backend | Drive the teardown/Verification failure case after the issue is already resolved | Terminal issue remains resolved; unresolved count is zero; Run is `Clean` | Re-read issue and Run state after command completion | Current-build outcome and issue-status cases passed; see [command evidence](evidence/2026-08-11-run-gate/command-evidence.md). | pass |
| QA-06 | Goal 4: discard a superseded Run Branch through Roundfix, with its evidence written first | Maintainer; built `reconcile --discard-superseded` CLI | Use an isolated stopped Run whose commits are reachable; apply the explicit flag | Record contains Run, branch, commits, files, and classification before branch/worktree removal | Reopen the record after both Git surfaces are absent; rerun reconcile | The QA contract forbids creating the scratch commits/Run store needed for a destructive built-binary journey. Equivalent current-build CLI and daemon cases passed; see [command evidence](evidence/2026-08-11-run-gate/command-evidence.md). | blocked (environment: no authorized committed scratch Run Branch) |
| QA-07 | Goal 4 refusal probe: unreachable commit and record-write failure preserve both Git surfaces | Maintainer; built CLI/backend | Attempt discard with an unreachable commit, then with an unwritable record destination | Command refuses with the failed condition; neither branch nor worktree is removed | Compare exact Git-surface snapshot before/after and rerun report mode | Same scratch-history boundary as QA-06. Both focused refusal cases passed and preserved exact surfaces; see [command evidence](evidence/2026-08-11-run-gate/command-evidence.md). | blocked (environment: no authorized committed scratch Run Branch) |
| QA-08 | Goal 3: carry a stopped Run's settled Task forward when Verification passed and declared inputs are unchanged | Maintainer; built `reconcile --carry-forward` CLI | Apply the explicit flag to an isolated stopped Run with a settled Task | Checkout reads `completed` and records exact source Run and settlement commit | Reopen the Task and implementation commit from a fresh public read after process exit | The no-commit boundary prevents constructing or mutating a disposable stopped Run. Equivalent CLI/spec cases passed with provenance; see [command evidence](evidence/2026-08-11-run-gate/command-evidence.md). | blocked (environment: no authorized committed scratch stopped Run) |
| QA-09 | Goal 3 refusal probe: a moved declared input names the Task and input and changes nothing | Maintainer; built CLI/backend | Move one declared input after settlement and attempt carry-forward | Exit 2 refusal names Task and input | Compare checkout HEAD, Task bytes, and implementation path before/after | Same scratch-history boundary as QA-08. The focused moved-input refusal passed with exact before/after checks; see [command evidence](evidence/2026-08-11-run-gate/command-evidence.md). | blocked (environment: no authorized committed scratch stopped Run) |
| QA-10 | Goal 3 atomicity probe: a mixed eligible/ineligible set refuses the whole set | Maintainer; built CLI/backend | Present two settled Tasks and move only one Task's input | Neither Task is carried; refusal names the ineligible Task/input | Reopen both Tasks and confirm neither implementation commit reached checkout | Same scratch-history boundary as QA-08. The mixed-set case passed and carried neither Task; see [command evidence](evidence/2026-08-11-run-gate/command-evidence.md). | blocked (environment: no authorized committed scratch stopped Run) |
| QA-11 | Explicit-action and Non-Goal sweep: report-only reconcile neither disposes nor carries forward, and no failed Task is retried automatically | Maintainer; built CLI | Run `reconcile` without either disposition flag against both candidate types | Candidates are reported; Run database, checkout, branches, worktrees, and Task statuses remain byte-identical | Exact before/after snapshots and a second report-only invocation | Both no-flag integration cases passed with exact state comparisons; no retry surface was added. See [command evidence](evidence/2026-08-11-run-gate/command-evidence.md). | pass |
| QA-12 | Non-Goal sweep: Fallback Chain contents/configuration and one-Run override behavior do not change | Maintainer; CLI/config surface | Inspect public help/config behavior and exercise the existing mixed-profile chain | Only eligibility timing changes; configured tuples/order stay intact; no one-Run chain override appears | Re-read effective profile/attempt order and CLI help after the run | Mixed-profile integration passed; built help exposes no new resolve/implement chain override. See [command evidence](evidence/2026-08-11-run-gate/command-evidence.md). | pass |
| QA-13 | CLI sweep: the two named disposition acts are discoverable and preserve stdout/stderr/exit-code contracts | Maintainer; built CLI | Read top-level and reconcile help; invoke report, success, and refusal paths | `--discard-superseded` and `--carry-forward` are discoverable; requested output stays on stdout, diagnostics on stderr, refusals are nonzero | Repeat help from the installed/built command and compare machine-readable output | Both help surfaces omit both flags. The real parser accepts them and sends validation diagnostics to stderr with exit 2. Finding F-02; see [command evidence](evidence/2026-08-11-run-gate/command-evidence.md). | fail |
| QA-14 | Glossary check: `Selection Failure` and `Branch Disposition` are coined or accounted for without dropping/changing canonical terms silently | Maintainer; docs plus CLI/event vocabulary | Compare emitted tokens and public disposition vocabulary with `CONTEXT.md` | `Selection Failure` is documented as `agent_selection_failed`; `Branch Disposition` is present or a finding names the missing term | Re-run focused glossary searches against current source and `CONTEXT.md` | `Selection Failure` is documented at `CONTEXT.md:199`; `Branch Disposition` is absent. Finding F-03; see [command evidence](evidence/2026-08-11-run-gate/command-evidence.md). | fail |
| QA-15 | Commit-dependent scope/tooling audit for Tasks 01–06 and 08 | Maintainer; Git command surface | Resolve each Daemon Task commit with `git diff-tree`; compare actual paths with each bounded list and inspect for protected tooling paths | Every path is bounded; no protected-tooling mutation exists without authorization; commit chronology is traceable | Re-read current files and ancestry rather than trusting Task Results | Raw `diff-tree` audit found every Task/repair commit bounded and no protected-tooling mutation; separate authoring/corpus commits are named in [command evidence](evidence/2026-08-11-run-gate/command-evidence.md). | pass |
| QA-16 | Authoritative cold static gate | Maintainer; repository Verification | Clean the repository-owned Go cache and test cache, then run exact `rtk make verify` | Exit 0; no package reports `(cached)` | Inspect command exit and package output from the cold run | Cold authorized gate exited 2 on F-01; the focused single-test reproduction also failed. See [command evidence](evidence/2026-08-11-run-gate/command-evidence.md). | fail |
| QA-17 | QA Report contract | Maintainer; report artifact | Close every row, resolve every evidence path, and validate frontmatter and coined-term coverage | Machine-readable terminal verdict; exact environment/finding/declared blocked counts; zero pending rows | Run the authored Task verification shape against this report and inspect status counts | The report is closed with verdict `fail`, counts 7/0/0, no pending row, both coined terms present in the report, and a resolving [command evidence](evidence/2026-08-11-run-gate/command-evidence.md) path. | pass |

High-risk probes are selected because they test the feature's destructive and
state-boundary hazards: no-output versus post-output failure, partial versus
fully resolved Batch failure, unreachable Git history, record-write failure,
moved inputs, mixed-set atomicity, and omission of the explicit flag.

## Findings

### F-01 — reconcile JSON field contract omits `carryForwards`

- Impact: `Blocks-Completion`.
- Actor and journey: maintainer, QA-16 authoritative cold gate.
- Expected: the exact JSON field contract accepts every public field emitted by
  `reconcile --format=json`, and `rtk make verify` exits 0.
- Actual: Task 06 added `reconcileReport.CarryForwards` at
  `internal/cli/reconcile.go:83`, but the exact expected list at
  `internal/cli/cli_test.go:560` was not updated. The full gate and isolated
  reproduction both fail on the extra `carryForwards` field.
- Reproduction: clear `<worktree>/.gocache` test results, run `rtk make verify`,
  or run the isolated test recorded in command evidence.
- Affected rows: QA-16.

### F-02 — reconcile disposition flags are absent from help

- Impact: `Friction`.
- Actor and journey: maintainer, QA-13 built CLI discovery.
- Expected: the named `--discard-superseded` and `--carry-forward` acts are
  visible in top-level and reconcile command help.
- Actual: both help surfaces list only `--apply` and `--format`; the real parser
  still accepts both hidden flags and validates them with exit 2.
- Reproduction: run the built binary's `--help` and `reconcile --help` commands
  from command evidence.
- Affected rows: QA-13.

### F-03 — `Branch Disposition` is missing from the glossary

- Impact: `Trust-Damage`.
- Actor and journey: maintainer/future contributor, QA-14 glossary check.
- Expected: Task 07's two coined terms are documented in `CONTEXT.md` using the
  public vocabulary.
- Actual: `Selection Failure` is documented and linked to
  `agent_selection_failed`; `Branch Disposition` has no glossary entry.
- Reproduction: run the focused glossary search from command evidence.
- Affected rows: QA-14.

## Blocked and skipped

- QA-01 is environment-blocked because this active Run Worktree cannot safely
  host a nested Run and the QA contract does not authorize creating commits for
  a disposable Spec target. Unblock with a maintainer-supplied committed scratch
  Spec repository and runtime profile, or a supervised live Run. Equivalent
  current-build selection/fallback evidence passed.
- QA-03 is environment-blocked because the prompt confirms there is no open Pull
  Request carrying the required multi-issue Batch. Unblock with a safe open test
  Pull Request and configured Review Source/runtime. Equivalent current-build
  Batch/outcome integration evidence passed.
- QA-06 through QA-10 are environment-blocked because their destructive Git
  histories require committed scratch Run Branches/Worktrees, while this gate
  has no commit authority. Unblock with prepared disposable Run stores and Git
  surfaces or explicit scratch-commit authority. All corresponding success,
  refusal, persistence, and atomicity integration cases passed.
- No row is finding-blocked, declared-blocked, or skipped. Findings directly
  failed the rows that observed them; they did not prevent unrelated execution.

## Coverage

Final coverage: 7 pass, 3 fail, 7 environment-blocked, 0 finding-blocked,
0 declared-blocked, 0 skipped, and 0 pending. PRD goals are mapped 5/5;
Non-Goals are mapped 4/4. Backend and CLI surfaces were swept. Every
environment-blocked row records its cause, unblocking action, and equivalent
current-build evidence.

## Final verdict

Fail: correct F-01 through F-03, then rerun the cold gate and the affected CLI
and glossary rows before this Spec can proceed. Row counts: 7 pass, 3 fail, 7
environment-blocked, 0 finding-blocked, 0 declared-blocked, 0 skipped. Finding
impact counts: 1 Blocks-Completion, 0 Data-Loss, 1 Trust-Damage, 1 Friction, 0
Cosmetic.
