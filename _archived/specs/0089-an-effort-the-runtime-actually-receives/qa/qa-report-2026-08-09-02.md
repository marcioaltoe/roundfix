---
spec: 0089-an-effort-the-runtime-actually-receives
date: 2026-08-09
build: 77e08aa8
status: closed
verdict: partial
rows_blocked_environment: 4
rows_blocked_finding: 0
rows_blocked_declared: 0
surfaces: [backend, cli, docs]
---

# QA report — An effort the runtime actually receives (rerun 02)

## Scope and environment

Full scope. Rerun of `qa-report-2026-08-09.md`, which returned `fail` on three
findings (F-001 configuration never changed behind a vacuous gate, F-002 warm-up
prompt performed Agent work, F-003 `runtime_deferred` absent from the glossary).
This run starts with those three rows, then walks the remaining matrix.

Build `77e08aa8` on `ma/specs-0082-0083`. Binary built with
`go build -buildvcs=false -o <scratch>/roundfix ./cmd/roundfix`. Live runtime:
OpenCode 1.18.15 through acpx 0.13.0; adapters `codex-acp` 1.1.14 and
`claude-agent-acp` 0.66.0 as reported by `profiles validate --json`.

One acceptance target changed between the two runs. The Spec authored
`openrouter/deepseek/deepseek-v4-pro` at `xhigh`; the maintainer superseded it on
2026-08-09 with `openrouter/deepseek/deepseek-v4-flash-0731` at `max`. Rows that
name the authored tuple are checked against the superseding one. The
authorization record was amended in `77e08aa8` to carry that instruction, and
that amendment is itself reported in row 16.

Parity gap: this session's permission classifier refused every `roundfix
resolve` invocation, so no Roundfix Run could be started. That gap is the whole
of `rows_blocked_environment` and is recorded per row below.

## Static gate

`GOCACHE="$PWD/.gocache" go clean -testcache && make verify` — exit `0`, zero
`(cached)` package lines, 27 package results. Evidence:
`qa/evidence/2026-08-09-gate-02/make-verify.txt` and `build.txt`.

That gate ran at `8b84033d`. The report's build is `77e08aa8`, which adds one
documentation-only commit on top (the authorization amendment in row 16) and no
Go source, so the recorded result still describes this tree.

The bare `go clean -testcache` form was not used: it clears the user-level cache
rather than the one the Makefile exports, and produced a false green during Spec
0088.

Two `make verify` runs earlier in this session disagreed on the same tree —
exit `2` then exit `0` — because
`TestACPXRunCancellationCommandFailuresWarnAndContinue` waited 5s for the fake
Agent to start where every sibling wait in its harness uses the shared 90s
`agentWaitBudget`. Under the full parallel suite that spawn does not fit in 5s.
Fixed in `f3f80265`; recorded here because a non-deterministic authoritative gate
makes every decision taken with it unreliable, including this Spec's earlier
Task settlements.

## Results

| #   | Story / criterion / sweep                                            | Actor and surface    | Status                                             | Evidence |
| --- | -------------------------------------------------------------------- | -------------------- | -------------------------------------------------- | -------- |
| 01  | Static gate on a genuinely cold Makefile cache                       | maintainer / cli     | pass                                               | `make-verify.txt`, `build.txt` — exit 0, 0 cached lines, 27 packages |
| 02  | F-001 regression — every OpenCode selection carries the effort       | maintainer / cli     | pass                                               | `rows-02-03-05.txt` — 6/6 at `deepseek-v4-flash-0731` / `max`, 0 mismatches |
| 03  | F-001 regression — the Task gate can fail when no work was done      | maintainer / cli     | pass                                               | `rows-02-03-05.txt` — corrected gate exits 1 against the pre-fix config; the superseded `grep -q 'reasoning_effort: xhigh'` exits 0 against it |
| 04  | F-002 regression — the warm-up turn is structurally inert            | maintainer / backend | pass                                               | Live acpx session: inert turn produced 0 tool calls; `TestACPXPromptArgsInertWarmupWithholdsTools` in `warmup-receipt-tests.txt` |
| 05  | F-003 regression — `runtime_deferred` is defined in the glossary     | maintainer / docs    | pass                                               | `rows-02-03-05.txt` — 1 occurrence in `CONTEXT.md`, in the `Selection Encoding` entry |
| 06  | Goal 1 — a non-empty OpenCode effort is configurable and delivered   | maintainer / cli     | pass                                               | `profiles-validate.json`, `encodings.txt` — the tuple proves; live session applied `max` and read back `currentValue: max` |
| 07  | Goal 2 — preflight stays token-free and claims only what it observed | maintainer / cli     | pass                                               | `encodings.txt` — reports `runtime_deferred`, never `independent`, for the deferred tuple; proof sends no prompt |
| 08  | Goal 3 — every work turn runs at the requested effort, the first too | maintainer / backend | blocked (environment: no Roundfix Run permitted)   | Equivalent: `TestACPXSessionEffortCharacterizationDeclaredBreakWarmSessionOrdersEffortBeforeWork` passes; live sequence applied `max` before the first work prompt |
| 09  | Goal 4 — an unadvertised effort fails closed before any Run          | maintainer / cli     | pass                                               | `fail-closed.txt` — exit 2, `reasoning_control_not_advertised`, no Run created |
| 10  | Goal 5 — codex and claude selection and proof are unchanged          | maintainer / cli     | pass                                               | `encodings.txt` — 4 tuples still `independent`; `rows-17-19-nongoals.txt` shows claude's mapping unchanged, opencode only joined its case |
| 11  | Feature — warm-up raises the queue owner and publishes the receipt   | maintainer / backend | blocked (environment: no Roundfix Run permitted)   | Equivalent: `TestACPXRunWarmSessionPublishesEffectiveEffortReceipt` and `TestProjectStreamEventProjectsSelectionReceiptInExistingCategory` pass; live `set effort max` after the inert turn returned `currentValue: max` |
| 12  | Feature — `runtime_deferred` is kept apart from `independent`        | maintainer / cli     | pass                                               | `encodings.txt` — 1 deferred, 4 independent, distinct tokens |
| 13  | Acceptance Run at the configured tuple with the observed effort      | maintainer / backend | blocked (environment: no Roundfix Run permitted)   | `resolve-refusals.txt`; equivalent evidence recorded under Blocked below |
| 14  | Probe re-run against the recorded defaults                           | maintainer / cli     | pass                                               | `row-14-probe.txt` — observed `low, high, max` default `low`, identical to the adopted reference |
| 15  | Outside-evidence row (ADR-0104)                                      | maintainer / docs    | pass                                               | `outside-evidence.txt` — Secondbrain `wiki/concepts/modelos-custos-e-selecao.md`, compiled from a daily 06:30 OpenRouter fetch |
| 16  | Tooling authorization audit and bounded-path check                   | maintainer / cli     | pass                                               | `authorization-audit.txt` — authorization is an ancestor of both authorized commits; bounded paths respected; stale selection amended in `77e08aa8` |
| 17  | Non-Goal — no other runtime's behavior changed                       | maintainer / cli     | pass                                               | `rows-17-19-nongoals.txt` |
| 18  | Non-Goal — no Model Catalog or effort picker in interactive surfaces | maintainer / cli     | pass                                               | `rows-17-19-nongoals.txt` — no interactive path touched |
| 19  | Non-Goal — no claim that a higher effort improves outcomes           | maintainer / docs    | pass                                               | `rows-17-19-nongoals.txt` — every match is a Non-Goal declaring the absence |
| 20  | Probe — warm-up is idempotent per Agent Session                      | maintainer / backend | blocked (environment: no Roundfix Run permitted)   | Equivalent: `TestACPXRunWarmSessionIsIdempotent` passes |
| 21  | Probe — an unadvertised effort is rejected for the configured model  | maintainer / cli     | pass                                               | `fail-closed.txt` — `medium` rejected for `deepseek-v4-flash-0731`, advertised set named in the error |

## Findings

None. The three findings from `qa-report-2026-08-09.md` are closed and each has a
regression row above (02/03 for F-001, 04 for F-002, 05 for F-003).

Two facts are recorded here without being findings against this Spec, because
both are already owned elsewhere:

- The flaky authoritative gate described under Static gate. Fixed in this branch.
- `reconcile` classifies a stopped Run's branch `unintegrated` and preserves it,
  while Branch Integrity Preflight then refuses every new Run until a human
  merges or deletes that branch. Three such branches from this Spec's own failed
  Runs blocked row 13's first two attempts; each held only re-executions of
  `task_01` and `task_02` whose work was already integrated. Discarded with the
  record in `discarded-run-worktrees.txt`. This is the backlog item about a
  stopped Run discarding proven Task work, seen from its other side.

## Blocked and skipped

Four rows are environment-blocked on one cause: this QA session's permission
classifier refused `roundfix resolve --pr 143`, and no other Roundfix Run was
available to exercise the `runtime_deferred` path end to end. Proof in
`resolve-refusals.txt`.

Unblocking action: a maintainer runs `roundfix resolve --pr 143` — or any Run
whose Agent Work Category routes to the `opencode` runtime — and the gate reads
the Run identifier, the proven tuple, and the observed effective effort from the
Run Event receipt in `roundfix events <run-id>`.

Equivalent observed evidence recorded for rows 08, 11, and 20: the live acpx
sequence that Roundfix performs — `sessions ensure --model
openrouter/deepseek/deepseek-v4-flash-0731`, an inert warm-up turn that produced
zero tool calls, `set effort max` returning `currentValue: max`, then an ordinary
prompt that used tools normally — together with the unit gates named in each row.
Row 13 asks specifically for Roundfix's own Run Event receipt, which no
equivalent can substitute for, so it stays blocked rather than credited.

## Coverage

- Goals: 4 of 5 passed live; Goal 3 environment-blocked with equivalent evidence.
- Core features: 2 of 3 passed live; the receipt half of the warm-up feature is
  environment-blocked with equivalent evidence.
- Non-Goals checked: 3 of 3, none breached.
- Regression rows for the prior gate's findings: 4 of 4 passed.
- `rows_blocked_environment`: 4. `rows_blocked_finding`: 0.
  `rows_blocked_declared`: 0.
- Probes: unadvertised-effort rejection, warm-up idempotence, cold-cache gate
  determinism, authorization ancestry.
- Outside-evidence row: satisfied, not blocked, so it does not hold up pull
  request preparation under ADR-0104.

## Final verdict

`partial` — every runnable row passed and no finding survived, but four rows
need a Roundfix Run this session was not permitted to start; a maintainer must
run one on the `opencode` runtime and read the effort receipt from
`roundfix events` to close rows 08, 11, 13, and 20.

Counts: 17 pass, 0 fail, 4 blocked (environment), 0 blocked (finding), 0 blocked
(declared), 0 skipped. Findings by impact: none.
