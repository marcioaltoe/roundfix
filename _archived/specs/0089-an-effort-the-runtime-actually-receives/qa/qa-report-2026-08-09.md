---
spec: 0089-an-effort-the-runtime-actually-receives
date: 2026-08-09
build: 23834e8a895a377c974b36693f6d1d63437b2acc
status: closed
verdict: fail
rows_blocked_environment: 1
rows_blocked_finding: 1
rows_blocked_declared: 0
surfaces: [backend, cli, docs]
---

# QA report — An effort the runtime actually receives

## Scope and environment

Full terminal gate for Spec 0089. The actor is a Roundfix maintainer operating
the built CLI from the Run Worktree on macOS in `America/Sao_Paulo`. The report
starts before flow execution with every row pending. Evidence belongs under
`qa/evidence/2026-08-09-gate-01/`.

The manifest names `task_08` as the terminal `type: qa` node. Its dependencies
`task_05`, `task_06`, and `task_07` are completed, and every earlier Task is
completed through their dependency chain. The PRD declares no `## Unreachable
Acceptance` section, so no row can use a declared-blocked status. Pull Request
#143 is open on the Spec target branch; this gate does not mutate it.

High-risk behavior probes are: an unadvertised effort before Run creation,
first-turn ordering, a second work turn on the same Agent Session, a fresh
session/reopen, and a replay of the pre-Spec runtime measurement. These probes
target the failure modes named by ADR-0108 and the TechSpec rather than generic
CLI edge cases.

## Static gate

`GOCACHE="$PWD/.gocache" go clean -testcache` exited 0, followed by
`rtk make verify` exiting 0 in a full-access session. No package line reported
`(cached)`. The initial sandboxed cold attempt was interrupted by blocked
`api.github.com` access and was not credited; the cache was cleared again before
the successful run. See `qa/evidence/2026-08-09-gate-01/make-verify-cold.txt`.

## Results

| # | Story / criterion / sweep | Actor and surface | Status | Evidence |
| - | --- | --- | --- | --- |
| QA-01 | Authored gate, Project Constraints, protected-tooling authorization, commit choreography, changed-path scope, and generated-copy provenance | Maintainer; docs and Git entry points. Inspect the graph, both active Spec constraint sections, authorization, Task commits, and their exact diffs. Expected: all required applicability rows cite operative `docs/agents/` paths; authorization predates bounded tooling commits; no out-of-scope tooling path or folded prerequisite/consequent fix. Confirm independently with `git diff-tree` per commit and ancestry. Persistence: repository history. | fail | Authorization and Task 06 choreography pass, but Task 05 commit `c6361581` contains only `task_05.md`; `.roundfixrc.yml` did not change and its six OpenCode efforts remain empty. See `qa/evidence/2026-08-09-gate-01/tooling-audit.txt` and F-001. |
| QA-02 | Cold authoritative repository gate | Maintainer; repository CLI/build entry point. Clear the Makefile-selected test cache, run `make verify` unpiped, and inspect package lines. Expected: exit 0 and no `(cached)` package result. Confirm with the command exit and fresh Git status. Persistence: build is reproducible from this commit. | pass | Repository-local cache clear exited 0; the full-access `rtk make verify` exited 0 with fresh durations on every package and no `(cached)` line. Evidence: `qa/evidence/2026-08-09-gate-01/make-verify-cold.txt`. |
| QA-03 | Goals 1 and 3: a configured `openrouter/deepseek/deepseek-v4-pro/xhigh` effort reaches a real Agent before its first work turn | Maintainer; built `roundfix` CLI and OpenCode runtime. Execute one safe scratch Spec Task through a real Run using the Project Config tuple. Expected: Run id, proven tuple, `runtime_deferred`, one warm-up, an applied-effort receipt with requested and observed `xhigh`, then the work turn. Confirm through the public Run Event Stream and scratch task observable after reopening. Persistence: Run Event Journal plus scratch repository state. | blocked (environment: scratch Task Graph requires a commit but QA has no commit authority) | Both internal and external scratch Spec attempts exited 2 before Run creation and explicitly reported no side effects. Equivalent evidence exact-proved the tuple and observed live xhigh application, but produced no Run id or receipt and exposed F-002. Evidence: `qa/evidence/2026-08-09-gate-01/real-run-xhigh/attempt.txt`. |
| QA-04 | Goal 2: Preflight stays token-free and reports advertised, not applied, proof | Maintainer; built `roundfix profiles validate --json` and `roundfix doctor`. Expected: OpenCode selection passes as `runtime_deferred`, with the requested effort advertised and no claim that Preflight applied it. Confirm by comparing readiness output with the later Run receipt. Persistence: captured JSON/text output. | pass | Disposable Project Config proof exited 0: OpenCode `deepseek-v4-pro/xhigh` passed as `runtime_deferred`; Codex and Claude fallbacks passed independently. The live session later supplied applied evidence. See `qa/evidence/2026-08-09-gate-01/preflight-proof.txt`. |
| QA-05 | Goal 3 behavior probes: second work turn is not warmed twice; a fresh session is warmed and receives the effort again | Maintainer; real runtime/Run entry point. Execute or inspect two turns on one Agent Session and reopen through a fresh session. Expected: one warm-up per session, all work turns after the matching applied receipt. Confirm through command/event order and public task observables. Persistence: event evidence for both session lifecycles. | fail | The exact `Session setup.` prompt performed reasoning, repository/Spec reads, and began `rtk roundfix doctor` before xhigh was applied. The unsafe first turn invalidates the ordering promise; the probe was stopped before a second turn. Evidence: `qa/evidence/2026-08-09-gate-01/session-lifecycle.txt`; F-002. |
| QA-06 | Goal 4 and invalid-input probe: an unadvertised effort fails closed before a Run starts | Maintainer; built CLI in an isolated scratch repository. Request a value absent from the selected model's advertised efforts. Expected: nonzero preflight result naming the advertised values, no Run id, no Run/worktree/task mutation. Confirm with `roundfix runs list` and exact before/after scratch status. Persistence: captured command output. | pass | `low` exited 2 with `reasoning_control_not_advertised`, advertised `[high, xhigh]`, and a concrete repair. The scratch repository had no Runs before or after. Evidence: `qa/evidence/2026-08-09-gate-01/unadvertised-effort.txt`. |
| QA-07 | Goal 5: Codex and Claude proof/application remain unchanged | Maintainer; built CLI readiness plus public Run evidence where available. Expected: configured Codex and Claude tuples still validate through `independent`; no OpenCode warm-up behavior appears in their selection path. Confirm with readiness output and focused current-build application evidence credited only after the cold gate passes. Persistence: report command output and named tests. | pass | Live profile validation passed Codex and Claude with `independent`; the genuinely cold full suite passed their current-build application tests, and no CLI/catalog path changed. Evidence: `qa/evidence/2026-08-09-gate-01/unchanged-runtimes.txt`. |
| QA-08 | Outside-evidence acceptance and Goal 1 comparison: replay the adopted pre-Spec OpenCode measurement and test requested effort delivery | Maintainer; live `acpx`/OpenCode public session commands. Origin: the pre-change direct measurement adopted at `references/2026-08-09-the-opencode-runtime-hands-back-the-floor-of-every-range.md`, itself measured outside the Spec on 2026-08-08. Expected: record current advertised/default efforts for all four candidates, then warm and request `xhigh` on DeepSeek V4 Pro and observe it. Confirm by a fresh session read. Persistence: captured strict JSON and concise extraction. | pass | Fresh sessions reproduced all four defaults. `set effort xhigh` exited 0 after the queue owner existed, and a separate session read observed `deepseek-v4-pro/xhigh`. This narrow pass also exposed F-002. Evidence: `qa/evidence/2026-08-09-gate-01/outside-measurement/summary.txt`. |
| QA-09 | CLI/config/docs assembled surface: accepted non-empty OpenCode effort, six Project Config selections at `xhigh`, canonical/embedded skill equality, and durable model-selection reference | Maintainer; built CLI plus documented repository paths. Follow the public commands and docs without implementation knowledge. Expected: config loads, every explicit OpenCode tuple is `deepseek-v4-pro/xhigh`, the shipped skill states the split proof, embedded bytes match, and the durable reference names runtime/adapter/date/defaults without a Spec path. Confirm through a second read and byte comparison. Persistence: committed config/docs. | fail | Skill synchronization and the durable reference pass, but built `profiles show --json` reports all six shipped OpenCode selections with empty effort and the config retains stale refusal comments. Evidence: `qa/evidence/2026-08-09-gate-01/assembled-surfaces.txt`; F-001. |
| QA-10 | Glossary check required by Task 08: `runtime_deferred` is coined and extends the existing Selection Encoding term | Maintainer; `CONTEXT.md` documentation surface. Expected: record whether the coined token and changed definition are represented in the glossary without inventing a synonym. Confirm with an exact glossary search and the accepted ADR. Persistence: `CONTEXT.md` at this build. | fail | `Selection Encoding` exists but omits coined public token `runtime_deferred`; Spec check skipped vocabulary enforcement because the TechSpec has no Vocabulary Contract. Evidence: `qa/evidence/2026-08-09-gate-01/glossary-check.txt`; F-003. |
| QA-11 | Non-Goals and scope-creep sweep | Maintainer; CLI/help, diff, and docs surfaces. Expected: no OpenCode picker/catalog was added; no claim or measurement says higher effort improves Roundfix outcomes; Codex/Claude behavior is not broadened; archived Spec 0088 remains byte-identical. Confirm with changed paths, relevant help/docs, and exact Git object comparison for the archived Spec. Persistence: Git history. | pass | No archived Spec, CLI, or catalog path changed; no picker or outcome-improvement claim shipped; live proofs kept Codex/Claude independent. Evidence: `qa/evidence/2026-08-09-gate-01/non-goals.txt`. |
| QA-12 | Backend failure and recovery sweep | Maintainer; built CLI/runtime. Exercise contradictory observed effort and runtime/session interruption through safe public or supervised evidence. Expected: mismatch stops before work, inputs remain inspectable, and retry/fresh session provides a concrete recovery path. Confirm from current-build behavior and Run/session evidence. Persistence: captured diagnostics and fresh-session result. | blocked (finding: F-002 — waits on inert pre-effort session warm-up) | Invalid-input recovery is clear, and mismatch has cold-suite evidence, but a live retry/fresh-session journey repeats unintended Agent work before effort application. Evidence: `qa/evidence/2026-08-09-gate-01/recovery.txt`. |

## Findings

### F-001 — Task 05 settled without the proving Project Config change

- Impact: Blocks-Completion.
- Actor and journey: a maintainer configuring the repository to run OpenCode at
  `openrouter/deepseek/deepseek-v4-pro/xhigh`; QA-01, QA-03, QA-04, and QA-09.
- Expected: every explicit OpenCode selection in `.roundfixrc.yml` carries
  `reasoning_effort: xhigh`, and the comments describe session warm-up and
  applied-effort proof.
- Actual: all six selections still carry `reasoning_effort: ""`, the comments
  still state that non-empty effort is refused, and Task 05 commit `c6361581`
  changed only `task_05.md` while its Result claims the configuration changed.
- Reproduction: run `git diff-tree --no-commit-id --name-status -r c6361581`,
  `git diff 202c2f8d..HEAD -- .roundfixrc.yml`, and search each `runtime:
  opencode` block in `.roundfixrc.yml`.
- Evidence: `qa/evidence/2026-08-09-gate-01/tooling-audit.txt`.

### F-002 — The setup prompt performs Agent work before effort application

- Impact: Blocks-Completion.
- Actor and journey: a maintainer starting any `runtime_deferred` OpenCode Agent
  Session; QA-03, QA-05, and QA-12.
- Expected: `Session setup.` raises the queue owner as a non-work setup turn;
  Roundfix then applies and observes the requested effort before the first work
  turn.
- Actual: on live OpenCode 1.18.15/acpx 0.13.0 with DeepSeek V4 Pro, the exact
  prompt caused reasoning, directory and Spec reads, and an in-progress
  `rtk roundfix doctor` tool call while the session still had its default
  `high` effort. The probe was interrupted before applying `xhigh`.
- Reproduction: ensure the DeepSeek V4 Pro session, confirm default `high`, send
  exact prompt bytes `Session setup.`, and observe the ACP event stream before
  issuing `set effort xhigh`.
- Evidence: `qa/evidence/2026-08-09-gate-01/session-lifecycle.txt` and
  `qa/evidence/2026-08-09-gate-01/outside-measurement/summary.txt`.

### F-003 — The public runtime_deferred encoding is absent from the glossary

- Impact: Trust-Damage.
- Actor and journey: a maintainer reading `profiles validate` or readiness
  evidence and consulting the canonical vocabulary; QA-10.
- Expected: the existing `Selection Encoding` entry defines the coined
  `runtime_deferred` token reported by the built CLI.
- Actual: `CONTEXT.md` defines four encodings but has no `runtime_deferred`
  occurrence. The TechSpec lacks a Vocabulary Contract, so Spec check skipped
  automated detection.
- Reproduction: run `rg -n runtime_deferred CONTEXT.md`, then compare the
  `Selection Encoding` entry with live `profiles validate --json` output.
- Evidence: `qa/evidence/2026-08-09-gate-01/glossary-check.txt`.

## Blocked and skipped

- QA-03 is `blocked (environment: scratch Task Graph requires a commit but QA
  has no commit authority)`. Two allowed setup shapes were attempted; both
  exited 2 before Run creation and explicitly reported no side effects. A
  full-access rerun would not remove the commit-authority conflict. Equivalent
  observed evidence proves the tuple and live effort application, but no Run id
  or receipt exists. Unblock by committing the disposable Task Graph in an
  authorized setup session, then run the scratch Spec and capture its public
  event stream.
- QA-12 is `blocked (finding: F-002 — waits on inert pre-effort session
  warm-up)`. A live retry cannot prove recovery while the setup prompt itself
  performs work before effort application. Unblock by repairing the warm-up
  boundary, then rerun a first turn, second turn, and fresh session.
- No row was skipped or declared-blocked.

## Coverage

Result: 6 pass, 4 fail, 1 environment-blocked, 1 finding-blocked, 0
declared-blocked, 0 skipped, and 0 pending across 12 rows.

- Goal 1: the built capability accepts and applies `xhigh` in live equivalent
  evidence, but the required Project Config still requests an empty effort
  (F-001); the real Run row is environment-blocked.
- Goal 2: passed through a live `runtime_deferred` profile proof that did not
  claim application.
- Goal 3: failed because the setup turn performs Agent work at the default
  effort before `xhigh` is applied (F-002).
- Goal 4: passed; an unadvertised `low` fails closed before Run creation and
  names `[high, xhigh]`.
- Goal 5: passed; live Codex and Claude proofs remain `independent`, backed by
  the genuinely cold full suite.
- Surfaces: backend runtime lifecycle, CLI profiles/validation/help, and shipped
  docs/config/skill were all exercised.
- Probes: invalid effort passed; measurement replay passed; first-turn ordering
  failed; second turn and fresh-session recovery are finding-blocked; the real
  Run is environment-blocked.
- Outside evidence: freshly replayed from the adopted pre-change live
  measurement, with source provenance and current runtime versions recorded.
- Non-Goals: all four groups passed with no scope creep.
- Glossary: failed; `runtime_deferred` is coined but absent from the canonical
  `Selection Encoding` entry (F-003).
- Pull Request #143 was observed only as the prompt's open-PR fact. The PRD has
  no PR-specific journey, and this gate performed no PR mutation.

## Final verdict

Fail. Before rerunning, land the authorized `.roundfixrc.yml` profile change,
replace the active setup prompt with a boundary that cannot perform Agent work
before effort application, and add `runtime_deferred` to the canonical
Selection Encoding glossary contract; then authorize a committed disposable
Task Graph if the rerun must capture the real Run receipt required by QA-03.
Counts: 6 pass, 4 fail, 1 environment-blocked, 1 finding-blocked; findings are
2 Blocks-Completion and 1 Trust-Damage.
