---
spec: 0043-context-driven-baseline-coverage-and-skill-restoration
date: 2026-07-22
build: 8a3cfb76ca66db821926c41630a19d69b21b0624
status: closed
verdict: pass
surfaces: [cli, docs, infra]
---

# QA report — Context-Driven Baseline coverage and Repository Skill Set restoration

## Scope and environment

Full final gate for all six PRD user stories, all acceptance criteria in
`task_01.md` through `task_07.md`, the declared CLI/docs/infra surfaces, and
the PRD Non-Goals. The intended actors are a repository maintainer configuring
or auditing a repository and an Agent following the generated guidance.

The build under test is commit `8a3cfb76ca66db821926c41630a19d69b21b0624`
in the daemon-assigned Roundfix Run Worktree. Real flows use disposable Git
repositories under the system temporary directory so setup apply and skill
restoration cannot mutate this repository. The canonical public entry point is
`.agents/skills/setup-context-driven/scripts/context_setup.py`; documentation
is followed from `.agents/skills/setup-context-driven/SKILL.md` and
`docs/user-guide/context-driven-development.md`.

## Static gate

`rtk make verify` exited `0` on build
`8a3cfb76ca66db821926c41630a19d69b21b0624`. It passed 1,687 Go tests in
20 packages and 124 setup-context Python tests, loaded both setup asset
catalogs, passed the shipped Roundfix skill check (including the canonical
`setup-context-driven` and `qa-gate` skills), and built `bin/roundfix`.

## Results

| # | Story / criterion / sweep | Actor and surface | Status | Evidence |
| - | --- | --- | --- | --- |
| QA-01 | Repository static gate: formatting, Go tests, setup tests, asset validation, skill synchronization, shipped-skill checks, and build | Maintainer · infra · entry: `rtk make verify`; expected: exit 0; confirmation: final gate summaries; persistence: rerun-safe | pass | `rtk make verify` exit 0; 1,687 Go tests, 124 setup tests, both catalogs, skill check, and build passed (Static gate) |
| QA-02 | Story 1 + Task 01: versioned rule, coverage, dispatch, typed-reference, immutable-source, and digest contracts reject every named malformed shape without writes or network access | Maintainer · CLI/infra · entry: focused asset suite after static gate; expected: stable diagnostics for mutations and deterministic valid loads; confirmation: canonical and embedded catalogs load; persistence: repeated load is identical | pass | `test_assets.py`: 12/12 pass; QA-01 loaded both bundled catalogs; Task 01 named deterministic/mutation/no-side-effect evidence credited |
| QA-03 | Stories 1–2 + Task 02: every profile renders universal/applicable rules, selected Verification, exact active skill dispatch, compact root pointers, frontend design pointer, monorepo guide, preserved unmarked bytes, and deterministic output | Agent and maintainer · CLI/docs · entry: real resolved audit/apply on disposable repositories plus macro suite; expected: generated guidance matches active modules; confirmation: fresh audit and second apply; persistence: byte-identical rerender | pass | Real Go CLI/TUI profile flow: decision exit 3, resolved audit digest `062427...235b`, no-confirm exit 3/no writes, confirmed apply exit 0, fresh audit empty; generated Verification/dispatch/root pointers inspected; README preserved; Task 02 named macro evidence credited after QA-01 |
| QA-04 | Story 3 + Task 03: typed managed references resolve only within the selected Decision Plan; stale files cannot satisfy excluded targets; frontend `DESIGN.md` absence blocks; unsafe/outside paths fail; all finite artifact branches settle deterministically | Maintainer · CLI · entry: real audit on frontend and monorepo fixtures plus decision/reference suites; expected: stable path-specific findings or clean selected graph; confirmation: repository snapshot unchanged; persistence: repeated audit matches | pass | Frontend audit without `DESIGN.md` exit 1 with sole `reference.repository.missing`, no generated target; repository-authored target unlocked exact transition; monorepo guide selected; Task 03 decision/reference evidence credited after QA-01 |
| QA-05 | Story 4 + Task 04: one complete Change Plan previews create/refresh/remove/rename/reference edits with reasons and digests; missing/stale/malformed confirmation is safe; matching digest applies exact delta; ambiguous ownership requires a decision; rollback and idempotency hold | Maintainer · CLI/infra · entry: real audit/apply transition and apply suite; expected: exits 3/2/0 by category with stdout JSON only; confirmation: before/after tree diff and fresh audit; persistence: second apply empty | pass | Go→TypeScript and TypeScript→Rust real transitions listed removals and digests; stale digest exit 3 preserved AGENTS SHA-256; current digest applied exit 0; unmarked extension survived; fresh Rust audit empty; Task 04 rollback/ownership/Go→Rust evidence credited after QA-01 |
| QA-06 | Story 5 + Task 05: snapshot v2 uses immutable portable provenance and complete-tree digests; nested drift and unsafe entries block; remediation is exact; audit stays local/read-only; repo-owned precedence and byte-idempotent sync hold | Maintainer · CLI/infra · entry: real nested-drift audit plus sync/skill suites; expected: exact remediation and no mutation/network call; confirmation: repository snapshot unchanged; persistence: repeated audit/sync identical | pass | Manifest audit reported `skills.required.drift` for nested addition with exact provider/source/full ref/path/digest/preview argv; restoration removed only drift; Task 05 unsafe/source-precedence/offline/sync evidence credited after QA-01 |
| QA-07 | Story 5 + Task 06: `restore-skills` previews exact directory/lock changes, acquires one exact immutable source, requires the plan digest, preserves unrelated state, rejects source/security/adapter failures, rolls back injected failures, audits clean, and becomes idempotent | Maintainer · CLI/infra · entry: real disposable-Git restore flow plus restoration suite; expected: schema `setup-context-driven/restore-v1`, categorized exits, portable lock, exact digest; confirmation: final audit and tree read; persistence: second restore empty | pass | Real offline exact-commit `coding-guidelines` restoration: preview/stale exits 3, exact apply exit 0, obsolete and nested drift removed, portable lock written, audit no longer finds skill, second restore exit 0/empty; Task 06 grouped-acquisition/security/rollback evidence credited after QA-01 |
| QA-08 | Story 6 + Task 07: canonical skill and user/maintainer docs provide one accurate non-interactive audit → plan → confirm → apply → audit → explicit restore sequence; ownership and Spec 0036 boundary are explicit; distributed copy matches | Agent and maintainer · docs/infra · entry: follow docs and run documented commands/checks; expected: schemas, exits, fields, boundaries, and prohibited guidance checks pass; confirmation: canonical/embedded diff; persistence: links and commands remain runnable | pass | `test_workflow.py`: 7/7 pass; both docs contain preview/confirm recipes; `make skills-sync-check` and recursive canonical/embedded diff exit 0; Spec 0036/Doctor boundary and explicit-only restoration inspected |
| QA-09 | CLI sweep: JSON remains parseable on success and failure, stdout contains requested results only, exits 0/1/2/3 are categorized, commands never prompt, and mutating operations are confirmation-bound | Agent · CLI · entry: audit/apply/restore help and behavior probes; expected: stable schemas and no mixed diagnostics; confirmation: JSON parser and captured stderr/exit codes; persistence: repeat probes match | pass | Separate stream capture: audit exit 0 and malformed apply/invalid restore exits 2 all had parseable versioned stdout JSON and empty stderr; real reference exit 1 and confirmation exits 3 were parseable; help documented commands/exits and no prompt occurred |
| QA-10 | Behavior probes: stale plan after preimage change, repeated audit/apply, invalid/out-of-order confirmation, explicit drift restoration only, and offline exact-source restoration | Maintainer · CLI/infra · entry: disposable repositories; expected: no unauthorized mutation and actionable recovery; confirmation: fresh filesystem digest/read; persistence: reopen/re-execute | pass | Missing confirmation, stale setup plan, stale restoration plan, repeated audit/apply, nested drift, offline exact-source acquisition, fresh public audit, and empty second restore all produced safe categorized outcomes |
| QA-11 | Non-Goals: no project-specific architecture generation, nested package instructions, generic skill package management, extra-skill removal, automatic audit/apply restoration, upstream-managed content changes, Doctor expansion, or Baseline ADR expansion | Repository author · docs/infra · entry: generated tree, help/docs, and scoped diff inspection; expected: exclusions remain absent; confirmation: path/content searches and repository diff; persistence: repeated apply cannot introduce them | pass | Generated fixture has only root `AGENTS.md`; `DESIGN.md` remained repository-authored; docs forbid project-specific generation, automatic/generic restoration, and extra-skill removal; Spec commit range changes only repo-owned setup sources/distribution/tests/docs/tasks/Makefile, with no Go Doctor, ADR, or upstream skill path |
| QA-12 | Full profile and transition coverage: all bundled profiles and every artifact-changing decision combination reach either a clean selected graph or the declared stable blocker; applied delta equals authorized plan and reapply is empty | Maintainer · CLI/infra · entry: macro/decision/preview/apply suites and representative real flows; expected: all profile/transition fixtures pass; confirmation: fresh audit and tree comparison; persistence: deterministic rerun | pass | `test_macro_profiles.py`: 8/8 pass; `test_decision_plan_contracts.py`: 10/10 pass; representative Go, TypeScript/Bun, and Rust real flows exercised; Task 04 apply parity/rollback suite passed inside QA-01 |

## Findings

None.

## Real CLI evidence

### Go CLI/TUI profile adoption

- Fixture: disposable Git repository
  `/private/tmp/roundfix-qa-0043.UNmKuv` with repository-authored `README.md`.
- Unresolved `audit --profile go-cli-tui --format json` exited `3`, returned
  schema `setup-context-driven/audit-v1`, and named seven required decisions,
  including the universal `verification.gate` decision.
- Resolved audit with `autonomous.enabled=false`,
  `domain.layout=single-context`, `language.generated=English`,
  `secondbrain.enabled=false`, `spec.scaffold=true`,
  `triage.external=false`, and `verification.gate=make verify` exited `0` and
  returned plan digest
  `06242741149c20608a6f77e5098180bdbcd1641d4181d15d05a3cb9fe8af235b`.
- Apply without `--confirm-plan` exited `3` with
  `plan.confirmation.required`; `rg --files` still found only `README.md`.
- Apply with the exact digest exited `0`. A fresh resolved audit exited `0`
  with `plannedChanges: []`; the selected Verification, exact active-module
  skill dispatch, and compact root pointers were present in generated guides.
  `README.md` retained its original bytes.

### Reference and transition safety

- Auditing the frontend-enabled TypeScript/Bun profile without `DESIGN.md`
  exited `1` with one `reference.repository.missing` finding for
  `guide.frontend`; setup did not create the repository-owned file.
- After the fixture supplied its own `DESIGN.md`, audit exited `0` and the
  complete Go-to-TypeScript plan named every obsolete Go, CLI, and TUI block
  and guide removal before apply. Confirmed apply exited `0`, preserved
  `DESIGN.md`, created `guide.monorepo`, and removed the old managed guides.
- A TypeScript-to-Rust preview returned digest
  `7ad6c3a64d08787db7bc851ff69c9fc0c84fe844237df1d0c9c9bb39bac40107`.
  Adding an unmarked repository-authored AGENTS extension changed the current
  plan. Applying the old digest exited `3` with `plan.confirmation.stale`; the
  AGENTS SHA-256 remained
  `7a701209d969baff37b81bc140a3397a5cd31de60f1eb218feff94bbaa38c309`.
- Applying the recomputed digest exited `0`. The unmarked extension survived,
  generated guidance named `cargo test`, and a fresh resolved Rust audit
  exited `0` with an empty plan.

### Immutable skill drift and restoration

- Manifest-based audit reported required-skill findings with portable GitHub
  identity, full commit `236847f6956134bf468abb641bac0493a899bca5`, source
  path, expected complete-tree digest, and exact `previewArgv`; audit did not
  restore anything automatically.
- Offline restore preview for external `coding-guidelines` used
  `/Users/marcio/dev/skills` at the declared commit, returned schema
  `setup-context-driven/restore-v1`, exited `3`, and named the skill file and
  exact lock edit. Adding `obsolete.txt` made the old digest stale; apply
  exited `3`, kept the file, and recomputed a plan that explicitly removed it.
- Confirming digest
  `0cbba725e3af1dd80bb72da97a7f30d502518a8f58aca2c158c988d200e51cbb`
  exited `0`, installed the exact `SKILL.md`, removed the obsolete file, and
  wrote a portable lock entry with no absolute source path. A second restore
  exited `0` with `skills: []` and `plannedChanges: []`; public audit contained
  no finding for `coding-guidelines`.
- Adding nested `nested/drift.txt` produced `skills.required.drift` with exact
  remediation. Preview listed only that removal, and confirmed restoration
  removed it while leaving the lock unchanged.

### CLI, documentation, and scope evidence

- Focused suites passed: `test_assets.py` 12/12,
  `test_macro_profiles.py` 8/8, `test_decision_plan_contracts.py` 10/10,
  and `test_workflow.py` 7/7.
- `rtk make skills-sync-check` exited `0`, and
  `rtk diff -r .agents/skills/setup-context-driven skills/setup-context-driven`
  produced no differences. Both operator documents contain the explicit
  `restore-skills` preview and confirmed recipes.
- A subprocess capture parsed stdout independently for a successful audit,
  malformed apply digest, and invalid restoration skill. Results were exits
  `0`, `2`, and `2`; schemas were `audit-v1`, `audit-v1`, and `restore-v1`;
  stderr was empty in all three cases. Real flows also exercised exits `1`
  and `3` without prompts.
- The Spec commit range `e051a51..8a3cfb7` changes only the repo-owned
  `setup-context-driven` skill and synchronized embedded copy, its tests and
  fixtures, `Makefile`, Spec Task evidence, and the Context-Driven user guide.
  It contains no Roundfix Go/Doctor, Baseline ADR, nested-instructions, or
  upstream-managed skill change.

## Blocked and skipped

None. Every planned check ran in the assigned sandbox; the pinned external
source was available through the declared offline Git checkout, so no network
or full-access exception was needed.

## Coverage

Passed: 6/6 PRD user stories, 54/54 task acceptance criteria, 12/12 matrix
rows, 3/3 declared surfaces, 8/8 Non-Goals groups, and all five risk-based
behavior probes. No row is failed, blocked, skipped, or pending.

## Final verdict

Pass. Every criterion passed on build
`8a3cfb76ca66db821926c41630a19d69b21b0624`: 12 passed, 0 failed, 0 blocked,
0 skipped, and 0 findings across all impact tiers.
