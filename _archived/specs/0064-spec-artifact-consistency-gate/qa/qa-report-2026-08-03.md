---
spec: 0064-spec-artifact-consistency-gate
date: 2026-08-03
build: 02c35e274900788d15b0be12de36117c1da8e1a3
status: closed
verdict: fail
rows_blocked_environment: 0
rows_blocked_finding: 12
surfaces: [backend, cli, docs]
---

# QA report — Spec artifact consistency gate

## Scope and environment

Full terminal QA for `task_09`, run from the retained Run Worktree on macOS
through the built `roundfix` CLI. The actor is a Spec author or repository
maintainer. The public entry points are `bin/roundfix spec check`, top-level
and command help, and `make verify`. Test repositories under
`internal/speccheck/testdata/` supply scratch artifact inputs; the production
repository supplies the active-corpus and gate journeys.

The prompt reports no Open Pull Request. The PRD declares no Pull Request
surface or Pull Request-dependent acceptance, so no Pull Request journey is
mapped. No mutation, delivery, or review-source action is in scope.

Evidence directory:
`qa/evidence/2026-08-03-spec-check-gate/`. Exact commands and concise observed
outputs are recorded in this report. Build parity deviations and blocked
conditions are recorded per row.

### Project Constraint and graph preflight

- `task_09` is the manifest's declared `qa` node. It is terminal and depends
  on `task_08`, the only non-QA leaf; that dependency is `completed` and
  transitively covers tasks 01–07.
- The PRD and TechSpec each account for identifier strategy, authentication
  and HTTP, active ADR obligations, and tooling authority, citing
  `docs/agents/domain.md` or `docs/agents/agent-instructions.md` as operative
  sources.
- `task_08` is the only tooling Task. Its exact bounded tooling path is
  `Makefile`, authorized for Spec 0064 by
  `docs/workflow/authorizations/2026-08-02-queued-spec-tooling.md` before the
  Task commit.
- `git diff-tree --no-commit-id --name-only -r` was run for every implementation
  Task commit from `8cb3f85` through `02c35e2`. Only task 08 changed protected
  tooling. Commit `02c35e2` contains exactly `Makefile` and `task_08.md`.
  Authorization commit `2e560ce` predates it; no prerequisite or consequent
  tooling fix was folded into, or ordered around, the Task commit.
- The final status before closing contains only this untracked `qa/` artifact.
  `git diff --check` exits 0.

## Static gate

`rtk make verify` failed with exit 2 at its executed `test` prerequisite:

```text
rtk go test -parallel 16 ./...
Go test: 3197 passed, 1 failed, 2 skipped in 25 packages
speccheck (52 passed, 1 failed)
[FAIL] TestCheckCorpusGoldenAndBudget
full Spec corpus sweep took 3.036010375s, want under 1s
make: *** [test] Error 1
```

The exact test stage reproduced the failure with exit 1 and a 4.024416542s
sweep. The isolated diagnostic command
`rtk env GOCACHE=/private/tmp/roundfix-0064-qa-gocache GOFLAGS=-buildvcs=false go test ./internal/speccheck -count=1 -run '^TestCheckCorpusGoldenAndBudget$' -v`
passed in 0.64s. This contrast localizes the failure to a load-sensitive
wall-clock assertion under the repository's normal concurrent suite; it does
not turn the mandatory gate green. Per the QA gate, code-caused static failure
stops flow QA.

## Results

| # | Story / criterion / sweep | Actor, entry point, and surface | Steps and behavior probes | Expected observable, independent confirmation, and persistence | Evidence | Status |
| --- | --- | --- | --- | --- | --- | --- |
| Q-01 | Authored-gate precondition; Project Constraints; tooling authorization and commit choreography | Maintainer; Spec artifacts and Git ancestry; docs | Validate graph shape/status. Read both Constraint sections and operative sources. Inspect every Task commit with `git diff-tree`; prove the authorization predates task 08; inspect current delta. | QA node is terminal and runnable. All four Constraint categories are accounted for. Only authorized `Makefile` plus task 08 changed in its Task commit; no folded prerequisite or consequent fix and no out-of-scope tooling path exists. Re-read Git status after flow QA. | `_tasks.md`; both Constraint sections; authorization `2e560ce`; Task commit `02c35e2`; all Task changed-path lists; final status only `qa/` | pass |
| Q-02 | Core Feature 1; clean named-Spec text journey; task 09 clean exit requirement | Spec author; `bin/roundfix spec check 0064-spec-artifact-consistency-gate`; cli/backend | Build the binary, invoke the named Spec, capture stdout/stderr and exit. Repeat after other probes. | Exit 0; a clean report names the Spec; stderr has no diagnostic. Second invocation stays clean and repository state persists unchanged. | Not run: F-001 stopped flow QA after the static gate. | blocked (finding: F-001) |
| Q-03 | Core Feature 1; all-active sweep; Goals 1 and 2 | Maintainer; `bin/roundfix spec check`; cli/backend | Invoke with no slug. Probe a second invocation and measure elapsed time. | Every active Spec is checked, no error exits non-zero, elapsed time is in seconds. A fresh repeat returns the same results. | Not run: F-001 stopped flow QA after the static gate. | blocked (finding: F-001) |
| Q-04 | Core Feature 1 and task 04 JSON acceptance | Automation user; `--format json`; cli/backend | Check one Spec and multiple explicit Specs in JSON. Parse every stdout line independently. | One parseable `roundfix-speccheck/v1` object per Spec; requested slugs are present; stderr stays diagnostic-only. A repeated parse returns the same objects. | Not run: F-001 stopped flow QA after the static gate. | blocked (finding: F-001) |
| Q-05 | Core Features 2 and 7; task 09 error exit requirement | Spec author; built CLI against an error fixture; cli/backend | Invoke a fixture containing a located contradiction. Probe repeated invocation. | Exit 1; output carries the stable code, severity `error`, summary, at least two `path:line` locations, and concrete fix. Fixture bytes remain unchanged and the repeat reproduces the result. | Not run: F-001 stopped flow QA after the static gate. | blocked (finding: F-001) |
| Q-06 | Core Feature 4; gap semantics; task 09 gap exit requirement | Spec author; built CLI against gap-only fixture; cli/backend | Run default, then `--strict`, then default again. | Gap-only exits 0 with severity `gap`; strict promotes it and exits 1; the final default run again exits 0, proving no persisted promotion. | Not run: F-001 stopped flow QA after the static gate. | blocked (finding: F-001) |
| Q-07 | Core Feature 3; Project Constraint and tooling detector family | Spec author; built CLI against constraint/tooling fixtures; cli/backend | Exercise missing, unreasoned, missing-source, unauthorized, unbounded, clean, and no-TechSpec fixtures. Include invalid or out-of-order input where applicable. | Each dirty fixture reports its declared `SC-*` error and both sides; clean exits 0; absent TechSpec records detector skips rather than errors. Repeats are stable. | Not run: F-001 stopped flow QA after the static gate. | blocked (finding: F-001) |
| Q-08 | Core Feature 5; ADR, coverage, and reference resolution | Spec author; built CLI against citation/coverage/reference fixtures; cli/backend | Exercise direct unlisted ADR, related ADR, unmapped range, untasked feature, unresolved Task Context, and clean collective ranges. | Direct contradictions exit 1 and locate both sides; relation candidate stays a gap; valid collective ranges stay clean; unresolved paths name declaration and missing target. | Not run: F-001 stopped flow QA after the static gate. | blocked (finding: F-001) |
| Q-09 | Core Feature 6; emitted-vocabulary documentation | Spec author; built CLI against vocabulary fixtures; cli/backend | Exercise five-emitted/four-documented, satisfied, repeated-token, invalid-pattern, missing-path, and absent-contract cases. | The fifth token reports once; satisfied contract is clean; invalid and unreadable inputs return findings without panic; absent contract records a skip; fix text teaches `emits`, `pattern`, and `documented-in`. | Not run: F-001 stopped flow QA after the static gate. | blocked (finding: F-001) |
| Q-10 | Success Metric 1; four motivating finding replays | Maintainer; built CLI against replay fixtures; cli/backend | Run replay-0058-qa-001, replay-0058-qa-004, replay-0056-f-001, and replay-0056-f-002. | Observed codes match the source reports: coverage-unmapped, vocabulary-undocumented, ADR-unlisted plus ADR-related, and coverage-unmapped. Provenance READMEs name the report-authored limitation. | Not run: F-001 stopped flow QA after the static gate. | blocked (finding: F-001) |
| Q-11 | Success Metrics 2 and 3; full-corpus budget and observable archived false-positive counts | Maintainer; focused corpus characterization command; backend | Run the real corpus characterization/budget check and inspect its current golden. | Sweep completes below one second. Active counts are zero. Archived error/gap counts match the checked-in observation and remain unchanged on rerun. | Focused check passed at 0.64s. Golden: active all zero; archived counts include 35 ADR gaps, 35 ADR errors, 320 missing Constraints, 424 unmapped coverage units, 183 unresolved references, and the remaining per-code counts in `corpus-golden.json`. Both full-suite attempts matched the golden before failing only their duration assertion. | pass |
| Q-12 | CLI and docs sweep; command discoverability and vocabulary | New Spec author; `roundfix --help`, `roundfix spec check --help`, `make help`, `CONTEXT.md`; cli/docs | Follow only documented help. Check command name, flags, formats, strict meaning, exit-code guidance, make target, glossary vocabulary, and copy consistency. | The support command is discoverable and runnable without undocumented knowledge; terms match the Spec; help routes diagnostics correctly; no misleading mutation or QA-verdict claim appears. | Not run: F-001 stopped flow QA after the static gate. | blocked (finding: F-001) |
| Q-13 | CLI failure and recovery sweep | Spec author; public CLI; cli | Exercise unknown slug, invalid format, unexpected flag, and flags before/after slugs. Repeat a valid command after every usage failure. | Usage failures exit 2, name the bad input on stderr, and leave stdout empty. Valid recovery works immediately; interspersed supported flags are accepted. | Not run: F-001 stopped flow QA after the static gate. | blocked (finding: F-001) |
| Q-14 | Non-Goals and scope-creep check | Maintainer; public CLI plus fresh Git/hash reads; cli/docs | Hash/inspect Spec inputs before and after checks. Search command output for QA verdicts. Exercise `implement --help` and inspect the shipped command list. | No Spec artifact is edited; no QA verdict is emitted; no auto-correction exists; no consistency precondition is added to `implement`; the command judges written agreement, not prose style or decision correctness. | Not run: F-001 stopped flow QA after the static gate. | blocked (finding: F-001) |
| Q-15 | Mandatory repository static gate and Makefile integration | Contributor; `make verify`; cli/backend/infra | Run the full gate without piping output; confirm the built binary executes `spec-check` after build. | Exit 0 with format, tests, skill checks, build, and Spec consistency all green. A failing executed check would stop flow QA as code-caused. | `rtk make verify`: exit 2, sweep 3.036s. Exact `rtk go test -parallel 16 ./...`: exit 1, sweep 4.024s. Isolated corpus test: pass at 0.64s. | fail |

## Findings

### F-001 — Corpus wall-clock assertion fails in the normal repository gate

- Impact: Trust-Damage.
- Actor and journey: contributor running Q-15, `make verify`, at the test
  prerequisite.
- Expected: the required local gate exits 0 and the corpus budget test proves
  the authored sub-second sweep without load-dependent failures.
- Actual: `make verify` exits 2 because the corpus sweep measures 3.036s. The
  exact `go test -parallel 16 ./...` stage reproduces at 4.024s and exits 1,
  while the same test alone measures 0.64s and passes.
- Reproduction:
  1. From build `02c35e274900788d15b0be12de36117c1da8e1a3`, run
     `rtk make verify`.
  2. Observe `TestCheckCorpusGoldenAndBudget` fail at
     `constraints_characterization_test.go:177` before later gate
     prerequisites can run.
  3. Run `rtk go test -parallel 16 ./...`; observe the same test fail again.
  4. Run the focused command recorded in Static gate; observe 0.64s and pass.
- Evidence: Static gate output and Q-15 above.
- Affected rows: Q-02–Q-10 and Q-12–Q-15.
- Required repair: make the repository's required gate provide a stable,
  controlled proof of the sub-second product budget, or make the sweep meet
  that budget under the gate's declared execution conditions. Do not weaken
  the acceptance limit or hide the failing exit. Then rerun `make verify` and
  start a new full QA report.

## Blocked and skipped

Q-02–Q-10 and Q-12–Q-14 are `blocked (finding: F-001)` because the code-caused
static failure stops flow QA. Exact unblocking action: repair F-001, obtain a
fresh successful `make verify`, then rerun every public CLI and docs journey.

The prompt confirms no Open Pull Request, but the Spec has no Pull Request
journey or acceptance criterion, so this fact blocks no matrix row and
`rows_blocked_environment` remains zero. No row was skipped.

## Coverage

Planned: 15 rows covering all seven Core Features, all three Success Metrics,
the four Non-Goals, the authored task 09 acceptance criteria, the mandatory
CLI/backend/docs sweeps, and high-risk probes for repeatability, invalid input,
interspersed flags, strict-mode state leakage, missing artifacts, and
read-only persistence.

Terminal counts: 2 pass, 1 fail, 12 blocked by finding, 0 blocked by
environment, 0 skipped, 0 pending. One Trust-Damage finding is open. Static
failure prevented live coverage of the seven Core Features and four Non-Goals;
only governance and the isolated corpus success metrics received credit.

## Final verdict

Fail: repair the load-sensitive corpus budget proof, rerun `make verify`, and
then execute all 12 finding-blocked user journeys against one unchanged build.
