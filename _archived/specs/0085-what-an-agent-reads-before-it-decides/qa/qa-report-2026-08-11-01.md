---
spec: 0085-what-an-agent-reads-before-it-decides
date: 2026-08-11
build: a3402108ac9ef56d066874981abcd43a1406379a
status: closed
verdict: fail
rows_blocked_environment: 0
rows_blocked_finding: 0
rows_blocked_declared: 0
surfaces: [backend, infra, docs]
---

# QA Report

## Scope and environment

Full rerun of the authored terminal gate for Task 08 at `HEAD`
`a3402108ac9ef56d066874981abcd43a1406379a` on the per-Run branch
`roundfix/run-run_20260811T233056Z_117f0122e662f8b6`. Actors are a Roundfix
operator using a current-checkout binary built with `-buildvcs=false`, an Agent
reading repository guidance, and a maintainer auditing commit provenance. The
prompt confirms that no Pull Request is open; this Spec declares no Pull
Request surface or Pull Request-specific journey.

The Task Graph names `task_08` as the unique terminal `qa` node and makes it
depend on completed Tasks 05, 06, 07, and 09. Every transitive non-QA
dependency is also `completed`. The built strict Spec check reported no
findings. It explicitly skipped `SC-VOCABULARY-UNDOCUMENTED` because the
TechSpec has no `## Vocabulary Contract`, so QA-09 retains that manual check.

Disposable clones under `/private/tmp/roundfix-spec0085-*` isolated the real
Archive and Baseline Command flows and the regeneration audit from the source
checkout. The first sandboxed cold gate and focused CLI probe reached the
expected `api.github.com` boundary; each authoritative attempt was rerun with
network access after resetting the Makefile-owned cache. The local Secondbrain
index and precise external source were readable. `qmd query` failed with
`SQLITE_CANTOPEN`, but direct observation of the named source kept QA-08
runnable.

## Static gate

- Authoring precondition: `rtk /private/tmp/roundfix-spec0085-rerun/roundfix
  spec check 0085-what-an-agent-reads-before-it-decides --strict` exited 0 with
  no findings; `SC-VOCABULARY-UNDOCUMENTED` was explicitly skipped.
- Authoritative cold gate: after `rtk env GOCACHE="$PWD/.gocache" go clean
  -testcache`, the full-access `rtk make verify` exited 2. Every package except
  `internal/cli` passed. The single failure was
  `TestRunImplementVerificationCapacityAndDaemonStatusIntegratedFlow`, which
  observed one of two Agent starts before its independent five-second timeout;
  see F-01 and QA-13.
- Focused diagnosis: the failed test passed 10/10 with `-count=10` in 7.835s.
  Fresh blame and source inspection show that its Agent-start helper uses a
  hard five-second timer while the same suite documents and uses a 90-second
  `implementWaitBudget` for waits that run alongside parallel siblings.

## Mechanical findings

None.

## Results

| # | Story / criterion / sweep | Actor and surface | Status | Evidence |
| - | --- | --- | --- | --- |
| QA-01 | PRD Goal 1 / Task 08: every retired family resolves under the single archive root through the built CLI | Operator / CLI + backend | pass | A real Archive Command moved an active passing Spec to `_archived/specs/`, a fresh read found it there, and a second invocation safely refused with exit 2. The built strict checker passed; the resolver/package gate passed for all four kinds; `_archived/specs`, `_archived/findings`, and `_archived/adr` hold the existing retired artifacts, while no retired backlog record exists to materialize the empty `_archived/backlog` directory. |
| QA-02 | PRD Goal 2: retired material leaves active read paths and remains reachable through forward pointers | Agent / docs | pass | The only retired ADR is under `_archived/adr`, no retired ADR remains under `docs/adr`, its `superseded_by: ADR-0108` target resolves in the active tree, and the active consumer sweep found no old per-tree archive path. |
| QA-03 | PRD Goal 3: review excludes one archive root with no per-tree filters and preserves non-archive filters | Maintainer / infra | pass | YAML parsing found exactly `!**/_archived/**`, zero per-tree filters, and 106 total filters. The complete Task 07-to-HEAD diff is the single root marker comment beside the unchanged exclusion. |
| QA-04 | PRD Goal 4 / Task 08: real Baseline Command reaches both rendered guides with unconditional consultation | Maintainer / CLI + infra + docs | pass | A current built binary applied the real Baseline update to a disposable clone, refreshed the setup manifest and authoring trigger, and left the already-current Secondbrain and docs-layout managed regions intact. Fresh reads found the unconditional clause and new archive paths; a second update returned `state:"verified"`, no file changes, and `idempotence:"verified"`. |
| QA-05 | PRD Goal 5 / Task 08: every ADR has one lifecycle format; retired records have resolvable forward pointers | Agent / docs | pass | Whole-corpus audit reported `adrs=116 active=115 archived=1 retired=1 legacy_body_statuses=0 active_retired=0 unresolved_pointers=0`; every file carries the five lifecycle fields. |
| QA-06 | Task 06 protected-tooling audit: prior authorization, exact bounded paths, and sanctioned regeneration provenance | Maintainer / Git + infra | pass | Authorization commit `1a31c965` is an ancestor of `27c4eb45`; `diff-tree` contains the assigned Task, authorized consultation sources, and Makefile-declared deterministic fallout. Current scratch regeneration returned `ok:true, changed:false`; Task 09 changes only archive-layout clauses in the shared module and leaves Task 06's Secondbrain clause and binding unchanged. |
| QA-07 | Task 07 protected-tooling audit: prior authorization, exact bounded paths, and consequent-fix chronology | Maintainer / Git + infra | pass | Authorization `1a31c965` is an ancestor of `7512a28e`; `diff-tree` contains only `.coderabbit.yaml` and `task_07.md`, and no descendant commit changes `.coderabbit.yaml`. |
| QA-08 | ADR-0104 outside evidence supports removing retired guidance from the default read path while retaining discoverability | Agent / external docs | pass | Re-read the original Secondbrain page at `/Users/marcio/dev/secondbrain/wiki/concepts/arquitetura-de-instrucoes-e-progressive-disclosure.md`; it independently states that stale guidance can be treated as current instruction and that progressive disclosure must retain discoverability. |
| QA-09 | Manual vocabulary/glossary check retained because `SC-VOCABULARY-UNDOCUMENTED` was skipped | Agent / docs | pass | Fresh `CONTEXT.md` comparison found the existing Archive Command, lifecycle, and Secondbrain concepts. `ArchiveKind` and `ArchiveDir` remain implementation identifiers; this Spec coins, changes, and drops no ubiquitous-language term. |
| QA-10 | Non-Goals: no archived artifact was deleted and ADR decision text was not re-litigated | Maintainer / Git + docs | pass | Pre/post Git tree IDs are identical for Specs (`1781d09e...`) and findings (`d506a2f8...`). ADR-0106's source and destination blobs both equal `05f3daa5...`, and Git detects a 100% rename. |
| QA-11 | Non-Goal: the Secondbrain capture and inbox contract did not change | Agent / docs + infra | pass | Fresh reads retain the capture/inbox clauses, and the current-head diff from the previous report head is empty for `docs/agents/secondbrain.md` and `modules/secondbrain.json`. Task 06's exact change remains limited to consultation and its authoring binding. |
| QA-12 | Non-Goal: no Roundfix Run-time command behavior changed | Operator / CLI + backend | pass | The complete code-path diff contains no `cmd`, agent, daemon, rounds, runevent, preflight, or config change. Built root, Doctor, and Implement help remain usable, and the focused Implement integration journey passed 10/10. |
| QA-13 | Task 08 Requirement 10: authoritative gate passes from the Makefile-owned cold cache | Maintainer / repository gate | fail | Full-access `rtk make verify` exited 2 on the five-second Agent-start wait in one CLI integration test; F-01. |
| QA-14 | Task 09 protected-tooling audit: widened authorization predates the corrective commit, paths are bounded, and regeneration is reproducible | Maintainer / Git + infra | fail | The widened authorization and Task boundary commits are ancestors, and regeneration is clean, but the complete audit found one canonical module changed outside its authorized subject and two paths outside Task 09's exact bounded scope; F-02. |

## Detailed evidence

### QA-01

```yaml
inputs:
  - kind: repository_path
    ref: internal/spec/archive.go
  - kind: repository_path
    ref: internal/cli/archive.go
  - kind: repository_path
    ref: _archived/*
```

In a disposable clone, the current binary printed:

```text
archived 0004-watch-merge-readiness -> _archived/specs/0004-watch-merge-readiness
```

A fresh filesystem read found the full Spec at that destination. Repeating the
same command returned exit 2, reported the active source absent, and promised
no Run, Agent, commit, or push side effect. The built help names
`_archived/specs/<slug>/`. The current archive tree contains the existing Spec,
finding, and retired ADR families; the resolver's passing four-kind test owns
the zero-member backlog destination without requiring Git to track an empty
directory.

### QA-02

```yaml
inputs:
  - kind: repository_path
    ref: docs/adr/*.md
  - kind: repository_path
    ref: _archived/adr/*.md
  - kind: repository_path
    ref: docs/agents/**
  - kind: repository_path
    ref: .agents/skills/**
  - kind: repository_path
    ref: skills/**
```

The corpus audit found 115 active ADRs, one archived retired ADR, no retired
status under `docs/adr`, and a resolvable `ADR-0108` successor. The focused
active-source scan for `docs/specs/_archived` or `docs/findings/_archived`
across `internal`, `docs/agents`, `.agents`, and `skills` returned no matches.

### QA-03

```yaml
inputs:
  - kind: repository_path
    ref: .coderabbit.yaml
  - kind: repository_path
    ref: internal/spec/archive.go
```

`YAML.safe_load` reported:

```text
archive_filters=["!**/_archived/**"] per_tree=0 total_filters=106
```

The exact `7512a28e^..HEAD` configuration diff remains one added marker comment
and no filter mutation.

### QA-04

```yaml
inputs:
  - kind: repository_path
    ref: internal/baseline/assets/modules/*.json
  - kind: repository_path
    ref: internal/baseline/assets/source-baselines/**
  - kind: repository_path
    ref: docs/agents/secondbrain.md
  - kind: repository_path
    ref: docs/agents/docs-layout.md
```

The first public update returned `ok:true`, `state:"verified"`, verified
approved postimages and profile alignment, refreshed `setup-context.json` and
`skill-dispatch.md`, and identified the already-current managed regions without
removing any line. Fresh reads found
`knowledge-workspace-secondbrain-authoring`, the unconditional consultation
clause, `_archived/findings/`, and `_archived/specs/`. The second public update
returned no changes and `idempotence:"verified"`.

### QA-05

```yaml
inputs:
  - kind: repository_path
    ref: docs/adr/*.md
  - kind: repository_path
    ref: _archived/adr/*.md
```

The fresh 116-file audit required `status`, `created_at`, `updated_at`,
`deprecated_at`, and `superseded_by`; rejected body-only status lines; rejected
retired records in the active tree; and resolved each retired pointer:

```text
adrs=116 active=115 archived=1 retired=1 legacy_body_statuses=0 active_retired=0 unresolved_pointers=0
```

### QA-06

`git merge-base --is-ancestor 1a31c965 27c4eb45` exited 0. Fresh
`git diff-tree --no-commit-id --name-status -r 27c4eb45` lists the assigned Task,
the authorized consultation module/binding and rendered guide, and deterministic
artifacts under the Makefile's Baseline regeneration paths. A clean disposable
clone ran `rtk make baseline-digests` with:

```json
{"schemaVersion":1,"type":"baseline-digests","ok":true,"changed":false}
```

Task 09 later edits the shared `context-workflow` module, but its exact patch is
limited to three archive-layout clauses and does not repair, reorder, or alter
Task 06's Secondbrain authoring binding.

### QA-07

`git merge-base --is-ancestor 1a31c965 7512a28e` exited 0. Fresh
`diff-tree` lists only `.coderabbit.yaml` and `task_07.md`. The descendant log
through this build contains no later `.coderabbit.yaml` commit.

### QA-08

```yaml
inputs:
  - kind: external_repository
    ref: /Users/marcio/dev/secondbrain/wiki/concepts/arquitetura-de-instrucoes-e-progressive-disclosure.md
  - kind: repository_path
    ref: docs/specs/0085-what-an-agent-reads-before-it-decides/references/_index.md
  - kind: repository_path
    ref: _archived/**/*
```

The original external page, created before this Spec and backed by its own
source list, says obsolete guidance is more dangerous for agents because it can
be interpreted as current instruction. It separately says progressive
disclosure should make the next document discoverable instead of hiding
knowledge. That supports both removal from the default read path and retention
of a forward trail. The required `qmd query` route failed with
`SQLITE_CANTOPEN`; direct observation of the precise source supplied equivalent
evidence, so the row was not blocked.

### QA-09

```yaml
inputs:
  - kind: repository_path
    ref: docs/specs/0085-what-an-agent-reads-before-it-decides/_techspec.md
  - kind: repository_path
    ref: CONTEXT.md
```

`CONTEXT.md` already defines the Archive Command and carries the lifecycle and
Secondbrain vocabulary used by the Spec. The new archive root is a layout, not
a new domain entity, and the resolver names are Go identifiers. No glossary
change is required.

### QA-10

```yaml
inputs:
  - kind: repository_path
    ref: _archived/**/*
  - kind: repository_path
    ref: docs/adr/*.md
```

Fresh Git object comparisons reported:

```text
Specs:    1781d09ecf5f525fb3d4231fe1d83435ea14c34b == 1781d09ecf5f525fb3d4231fe1d83435ea14c34b
Findings: d506a2f8d845fb3fbabcc0317100ef44b00be3ce == d506a2f8d845fb3fbabcc0317100ef44b00be3ce
ADR-0106: 05f3daa511c7677c18fc05228cdf29e38b2788be == 05f3daa511c7677c18fc05228cdf29e38b2788be
```

Git's exact comparison identifies ADR-0106 as a 100% rename from `docs/adr` to
`_archived/adr`.

### QA-11

```yaml
inputs:
  - kind: repository_path
    ref: internal/baseline/assets/modules/secondbrain.json
  - kind: repository_path
    ref: docs/agents/secondbrain.md
```

The current files retain the complete capture, inbox, safety, and ingestion
clauses. `git diff 5c1e5034..HEAD` is empty for both inputs, so the corrective
Task did not expand the consultation change into the capture contract.

### QA-12

```yaml
inputs:
  - kind: repository_path
    ref: cmd/**/*.go
  - kind: repository_path
    ref: internal/{agent,daemon,rounds,runevent,preflight,config}/**/*.go
```

The complete changed-path query from `main` returned no file in the declared
Run-time packages. Built root, Doctor, and Implement help exited 0. The one
full-suite failure is a test-only five-second wait introduced before this Spec;
the exact public Implement integration test passed 10 focused repetitions.

### QA-13

```yaml
inputs:
  - kind: repository_path
    ref: Makefile
  - kind: repository_path
    ref: internal/cli/implement_test.go
```

The Makefile exports `GOCACHE ?= $(CURDIR)/.gocache`. After clearing that exact
cache, full-access `rtk make verify` reached every package and exited 2 only in
`internal/cli`:

```text
--- FAIL: TestRunImplementVerificationCapacityAndDaemonStatusIntegratedFlow
    implement_test.go:2361: timed out waiting for 2 Agent starts; got 1 ([task_01])
```

Fresh blame places the five-second literal in `waitImplementAgentStarts` at
commit `1a31c965`; the same file's `implementWaitBudget = 90 * time.Second`
explicitly says tight waits under parallel siblings measure machine load rather
than product behavior. A focused full-access `-count=10` run passed all ten
iterations, which narrows the failure to the parallel cold-gate wait without
turning the red authoritative command green.

### QA-14

```yaml
inputs:
  - kind: repository_path
    ref: docs/workflow/authorizations/2026-08-09-what-an-agent-reads-before-it-decides.md
  - kind: repository_path
    ref: docs/specs/0085-what-an-agent-reads-before-it-decides/task_09.md
  - kind: repository_path
    ref: internal/baseline/assets/modules/context-workflow.json
  - kind: repository_path
    ref: skills/baseline_skill_contract_test.go
  - kind: repository_path
    ref: skills/owned_skill_edit_repocontract_test.go
```

Both chronology checks passed:

```text
task09_widened_auth_ancestor=0
task09_boundary_ancestor=0
```

The clean-clone regeneration returned `changed:false`, `make
skills-sync-check` passed, and the clone remained clean. The complete
`diff-tree` audit, however, reported 54 old/new path entries, 52 inside Task
09's declared patterns, and these two outside them:

```text
skills/baseline_skill_contract_test.go
skills/owned_skill_edit_repocontract_test.go
```

Neither file is one of the six authorized canonical Skills or its generated
`skills/<name>/SKILL.md` mirror, and `make skills-sync` does not generate either
root test. Separately, Task 09 changes three archive-layout clauses in
`internal/baseline/assets/modules/context-workflow.json`. The authorization
names that module only for binding the Secondbrain consultation to Idea, PRD,
and TechSpec authoring; its 2026-08-11 widening adds `spec-workflow`, not an
archive-layout grant for `context-workflow`. No missing or late authorization,
folded authorization commit, derived-pin drift, or post-Task consequent fix was
found; the two scope violations and one subject violation are the complete
authorization-shape result.

## Findings

### F-01 — The authoritative gate retains a five-second parallel Agent-start wait

- Impact: **Blocks-Completion** — Task 08 explicitly requires the authoritative
  cold gate to pass, and it exits 2.
- Expected: after clearing the Makefile-owned cache, `make verify` exits 0.
- Actual: under the full parallel suite, one of two Agent starts did not reach
  the test probe within the helper's five-second literal. All other packages
  passed, and the same test passed 10/10 focused repetitions.
- Root cause: `waitImplementAgentStarts` does not use the suite's documented
  90-second condition budget; its independent five-second timer becomes a
  machine-load assertion under `go test -parallel 16`.
- Reproduction: `rtk env GOCACHE="$PWD/.gocache" go clean -testcache`, then
  full-access `rtk make verify` at `a3402108`; inspect
  `internal/cli/implement_test.go:1291` and run the named test with `-count=10`.
- Affected row: QA-13. No archive, Baseline, documentation, or tooling audit
  row depends on this unrelated wait and those rows remained runnable.

### F-02 — Task 09 exceeds both its protected subject and its exact file boundary

- Impact: **Trust-Damage** — the repository's protected-tooling gate can no
  longer prove that the maintainer authorized every canonical clause change or
  that the corrective Task stayed inside its declared slice.
- Expected: every protected canonical edit matches the authorization's exact
  path and subject, and every commit path is inside Task 09's bounded list plus
  deterministic declared fallout.
- Actual: `context-workflow.json` changes archive-layout clauses although its
  grant is limited to the Secondbrain binding. Two root Skill contract tests
  are neither declared Task paths nor generated Skill mirrors.
- Reproduction: compare `git diff-tree --no-commit-id --name-status -r
  a3402108` with Task 09's Bounded scope and the authorization's original and
  widened path-purpose grants; inspect the three named paths in QA-14.
- Affected row: QA-14. The authorization and boundary records both predate the
  Task commit, and regeneration is reproducible, so those parts are not
  findings.

## Blocked and skipped

No QA row was blocked or skipped. The initial sandboxed verification attempt
was superseded by the full-access cold run, and the failed `qmd` index query was
superseded by directly reading the precise outside source. The two failed rows
are findings, not environment blocks.

## Coverage

Fourteen rows closed: 12 passed and 2 failed, with zero pending, blocked, or
skipped rows. All five PRD Goals, all four Non-Goals, the current corrective
Task, the real Archive and Baseline Commands, the ADR corpus, all three
protected-tooling commits, outside evidence, glossary obligation, and cold
authoritative gate were exercised. The three failures from the previous report
were repaired: the archive-path package regressions now pass, the retired ADR
left the active read path, and active guidance no longer teaches the old
layout. No frontend surface is declared.

The two pre-materialized mechanical skips were discharged manually across
QA-06, QA-07, and QA-14: bounded paths were compared with complete Task commit
`diff-tree` output, and authorization, prerequisite, Task, regeneration, and
consequent-fix chronology were checked through the current head.

## Mechanical skips

| Detector | Missing artifact |
| --- | --- |
| authorization bounded paths | Task commits |
| consequent-fix commit order | consequent-fix declaration |

## Final verdict

**Fail.** Replace the five-second Agent-start wait with the suite's real
condition budget at the owning test seam, then replay Task 09 with an exact
archive-layout authorization and bounded Task record landing before the
`context-workflow.json` and two root Skill contract-test edits; a late grant
cannot repair their current commit chronology. Rerun this gate afterwards.
Counts: 12 pass, 2 fail, 0 environment-blocked, 0 finding-blocked, 0
declared-blocked, 0 skipped; findings: 1 Blocks-Completion and 1 Trust-Damage.
