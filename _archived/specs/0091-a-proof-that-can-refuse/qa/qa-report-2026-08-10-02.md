---
spec: 0091-a-proof-that-can-refuse
date: 2026-08-10
build: ce7dc4ba9c992ca30969f73d3333227d7e242cf8
status: closed
verdict: fail
rows_blocked_environment: 0
rows_blocked_finding: 0
rows_blocked_declared: 0
surfaces: [backend, cli]
---

# QA report — A proof that can refuse

## Scope and environment

Full same-day rerun of the authored terminal gate on Run Worktree branch
`roundfix/run-run_20260810T182132Z_c4b6ca44c9c7f634`, build
`ce7dc4ba9c992ca30969f73d3333227d7e242cf8`, on macOS 26.5.2 arm64 with
Go 1.26.5 in `America/Sao_Paulo`. The actor is a Roundfix maintainer using the
built `roundfix profiles validate` CLI, live ACP runtimes, public Go package
runners, and Git's read-only commit path. The prompt confirms no Pull Request
is open; this Spec declares no Pull Request acceptance journey, and the per-Run
branch must not be pushed or used to resolve one.

The exercised binary identified itself as
`roundfix 0.4.0 (ce7dc4ba-dirty, built 2026-08-10 15:34:52 -0300)`; `dirty`
reflects this report-only QA delta after `HEAD`.

`rtk roundfix spec check 0091-a-proof-that-can-refuse --strict` exited 0 with
`No findings.` before this matrix was created. `_tasks.md` names `task_05` as
the terminal `type: qa` node. Its dependencies `task_07` and `task_08` are
completed, and their ancestry reaches every non-QA leaf.

This rerun starts with prior F-006, repeated F-003, and repeated F-004. Evidence
root: `docs/specs/0091-a-proof-that-can-refuse/qa/evidence/2026-08-10-gate-03/`.

## Static gate

The first sandboxed `rtk make verify` attempt was denied access to
`api.github.com` after partially warming the cache, so it was discarded. The
worktree cache was cleared again with
`rtk proxy env GOCACHE=<worktree>/.gocache go clean -testcache` (exit 0).
The authoritative full-access `rtk make verify` then exited 0. Every Go package
reported an execution duration rather than `(cached)`; the focused skills
contract, repository skill check, and binary build also passed. See
`evidence/2026-08-10-gate-03/make-verify-cold.txt`.

## Results

| # | Story / criterion / sweep | Actor and surface | Steps and expected observable | Independent confirmation and persistence | Evidence path | Status |
| - | --- | --- | --- | --- | --- | --- |
| QA-01 | Goal 1, Goal 2, Task 05: live `codex` refusal | Maintainer; built CLI to live Codex ACP adapter | Validate a scratch profile naming an unoffered Codex model; expect a nonzero refusal and whichever adapter- or Roundfix-owned diagnosis fires first under ADR-0119. | Two fresh processes returned the same adapter-first `selection_rejected`; a valid configured Codex tuple then passed. | `evidence/2026-08-10-gate-03/live-codex.txt` | pass |
| QA-02 | Goals 1-2, Task 03, Task 05: exact formerly passing `claude / opus-9-does-not-exist / high` tuple now refuses | Maintainer; built CLI to live Claude ACP adapter | Validate the PRD's exact tuple; expect refusal, and an advertised set when Roundfix owns the verdict. | Two fresh processes replaced the PRD's `passed` state with the same adapter-first refusal; restored `opus/high` then passed. | `evidence/2026-08-10-gate-03/live-claude.txt` | pass |
| QA-03 | Goals 1-2, Task 05, outside evidence: live `opencode` refusal | Maintainer; built CLI to live OpenCode ACP adapter | Validate a scratch profile naming an unoffered OpenCode model; expect refusal under ADR-0119. The outside source is the installed adapter's live behavior, not a Spec fixture. | Two fresh OpenCode 1.18.15 processes returned the same adapter-first `model not found`; the configured tuple then passed as `runtime_deferred`. | `evidence/2026-08-10-gate-03/live-opencode.txt` | pass |
| QA-04 | Goal 5, Task 03, Task 05, Task 07: repository's five distinct tuples retain outcomes and encodings | Maintainer; built CLI against `.roundfixrc.yml` | Run `profiles validate`; expect all configured selections to pass, including restored `claude/opus/high`, with established encodings. | Two fresh processes exited 0 with the same five proofs; restored `claude/opus/high` passed as `independent`, and no mismatch remained. | `evidence/2026-08-10-gate-03/repository-profile.txt` | pass |
| QA-05 | Goal 4, Tasks 04 and 08: pre-session failure ends at the actionable diagnosis | Maintainer; built CLI with a pre-session failure | Trigger installed ACPX's exit-1 `No named session` shape; expect no appended cleanup error while the observation remains recorded. | Installed ACPX's exit-1 absence produced a separate cleanup warning; the returned diagnosis ended at the original Codex failure with no appended close error. | `evidence/2026-08-10-gate-03/cleanup-diagnosis.txt` | pass |
| QA-06 | Tasks 01, 03, 06, 07, and 08 regression corpus | Maintainer; backend and CLI public runners | Run the renamed characterization, all sibling sequence cases, matching/mismatch cases, and both Task 08 fixtures; expect every named contract to pass. | All selected Agent and CLI cases passed, including both Task 08 shapes and both detach journeys; the cold gate passed as canary. | `evidence/2026-08-10-gate-03/regression-corpus.txt` | pass |
| QA-07 | Task 05 authoritative cold gate | Maintainer; repository verification | Clear the worktree cache and run exact `rtk make verify`; expect exit 0 and no Go package line marked `(cached)`. | Exit 0 after a second cache clear; every package carried a duration, and skills checks plus binary build passed. | `evidence/2026-08-10-gate-03/make-verify-cold.txt` | pass |
| QA-08 | Task 05 vocabulary acceptance: `Runtime Catalogue` is coined and emitted vocabulary is honest | Maintainer; docs and CLI contract | Search `CONTEXT.md`, the TechSpec vocabulary contract, and production; expect the coined concept to be documented and no unsupported public encoding claim. | `CONTEXT.md` now defines `Runtime Catalogue`; production records contamination in proof evidence without emitting either speculative token, and strict Spec check reports no vocabulary finding. | `evidence/2026-08-10-gate-03/glossary.txt` | pass |
| QA-09 | Goal 3, Task 02, Task 05: catalogue proof sends no prompt | Maintainer; built CLI plus observable ACP/session evidence | Validate live selections; expect catalogue ensure/show with no prompt or work turn. | Two invalid and one passing closed Claude preflight Session each reported `0/0` and `No history`; exact no-prompt cases passed. | `evidence/2026-08-10-gate-03/token-free.txt` | pass |
| QA-10 | Task 02 seam: honest catalogue, canonical `opus` binding, contamination record, and unchanged intermediate verdicts | Maintainer; backend public runner | Exercise named catalogue cases; expect pre-request models, `opus` to `opus[1m]`, contamination recording, and no Task-02 verdict change. | Named catalogue cases passed; restored live Claude and two full repository validations supplied assembled canaries. | `evidence/2026-08-10-gate-03/backend-catalogue.txt` | pass |
| QA-11 | Task 03 seam: membership net, adapter fast path, valid encodings, and contamination advisory | Maintainer; backend public runner and built CLI | Exercise membership, advertised-set, fast-path, encoding, contamination, and Claude characterization cases under ADR-0119. | Focused cases passed; all three live adapters refused twice, while five valid tuples retained their encodings. | `evidence/2026-08-10-gate-03/backend-membership.txt` | pass |
| QA-12 | Tasks 04 and 08 seam: both missing-session shapes are omitted, genuine close failure stays appended, and unrelated exit 1 is preserved | Maintainer; backend public runner | Run the exact close/absence tests; expect exit 4 and installed exit 1 omission, recorded warnings, genuine cleanup chaining, and unchanged unrelated exit-1 classification. | Five top-level cases passed, including both absence shapes; exact diagnosis/error-type assertions preserve genuine and unrelated failures. | `evidence/2026-08-10-gate-03/backend-cleanup.txt` | pass |
| QA-13 | PRD Non-Goals: no vendor-existence validation, reasoning-policy change, picker/recommendation, or automatic repair | Maintainer; built CLI and Git/config read path | Refuse from runtime advertisement, inspect CLI help/commits, prove established reasoning encodings, and confirm scratch profiles remain unchanged. | Encodings persisted, help stayed read-only, relevant commits add no picker, and all three executed scratch profiles remained byte-identical. | `evidence/2026-08-10-gate-03/non-goals.txt` | pass |
| QA-14 | Project Constraint and bounded-scope audit: protected tooling authorization and Task commits respect exact paths/chronology | Maintainer; Git commit read path | Resolve every relevant commit with `git diff-tree`; expect exact bounded Task paths, authorized `.roundfixrc.yml` chronology, and no unauthorized tooling mutation. | Task commits and ancestry are bounded, but restoration commit `061a442e` combines authorized `.roundfixrc.yml` with out-of-bound `CONTEXT.md`. | `evidence/2026-08-10-gate-03/tooling-scope-audit.txt` | fail (F-007) |

Planned high-risk probes are fresh-process repeat after invalid selections,
valid-after-invalid recovery, scratch-profile immutability, an unrelated exit-1
canary, and a genuine effective-selection mismatch after alias normalization.

## Findings

### F-007 — The authorized profile restoration is bundled with an unbounded glossary change

- Impact: Trust-Damage.
- Actor and step: a maintainer audits the protected `.roundfixrc.yml`
  restoration after Task 07 fixed the Claude tuple.
- Expected: the authorized tooling change occupies its own exact bounded
  commit; the grant permits only `.roundfixrc.yml` entries naming
  `claude/opus/high`.
- Actual: commit `061a442e` changes both `.roundfixrc.yml` and `CONTEXT.md`.
  The glossary path is outside the authorization and is not an assigned Task
  file, so the protected change cannot pass the exact commit-scope gate.
- Reproduction: run
  `rtk git -c core.fsmonitor=false diff-tree --no-commit-id --name-only -r 061a442e`
  and compare the two paths with the authorization's sole `paths` entry.
- Evidence: `evidence/2026-08-10-gate-03/tooling-scope-audit.txt`.
- Affected row: QA-14.

## Blocked and skipped

No row is blocked or skipped. The first cold-gate attempt was sandbox-blocked
from `api.github.com`; an authorized rerun followed a second cache clear and
produced valid cold evidence. The first focused cleanup command selected the
macOS user Go cache and was denied by the sandbox; its worktree-local-cache
rerun passed. Neither transient boundary leaves acceptance scope uncovered.
The prompt confirms no Pull Request is open, and this Spec declares no Pull
Request acceptance journey.

## Coverage

- Rows: 14 total — 13 pass, 1 fail, 0 environment-blocked, 0
  finding-blocked, 0 declared-blocked, 0 skipped, 0 pending.
- PRD: all five Goals and all four Non-Goals pass through the built CLI and
  backend public runners.
- Tasks: every acceptance cluster is mapped. Tasks 07 and 08 close prior F-006
  and repeated F-003; the `Runtime Catalogue` glossary correction closes prior
  F-004. The commit-dependent tooling audit fails independently as F-007.
- Surfaces: built CLI, backend package runners, live Codex/Claude/OpenCode ACP
  adapters, closed-session history, cold repository gate, and Git commit read
  path.
- Outside evidence: installed OpenCode 1.18.15 through ACPX 0.13.0 refused the
  invented model in two fresh processes; this source is outside the Spec's
  fixtures.
- Probes: fresh-process refusal repeats, valid-after-invalid recovery,
  scratch-profile immutability, unrelated exit-1 preservation, and genuine
  effective-selection mismatch.
- Regression history: prior F-001 is accepted under ADR-0119; F-002, F-003,
  F-004, F-005, and F-006 are fixed; F-007 is new.

## Final verdict

Fail: 1 of 14 rows fails with one Trust-Damage finding. Split `061a442e` so the
authorized `.roundfixrc.yml` restoration occupies an exact bounded commit and
the `CONTEXT.md` glossary change lands separately, then rerun the gate; all
functional, live-runtime, no-prompt, cleanup, vocabulary, and cold-verification
rows pass on build `ce7dc4ba9c992ca30969f73d3333227d7e242cf8`.
