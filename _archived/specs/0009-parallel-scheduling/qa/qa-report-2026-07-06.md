---
spec: 0009-parallel-scheduling
date: 2026-07-06
verdict: pass
surfaces: [cli, infra]
---

# QA Report - Parallel Scheduling

## Summary

All six task files are `status: completed`, and the full repository gate passed
fresh for this checkout. The CLI and infra surfaces were exercised with the
built binary, scratch config repositories, current Run attach replay, current
Run Branch history, focused hermetic integration tests, and the scheduler race
gate. No failures or blocked items were found.

## Evidence Index

- E1: `rtk make verify` passed with approved full filesystem access after the
  sandbox-only Go cache denial: `711` Go tests across `17` packages,
  `roundfix skills check`, and `go build`.
- E2: Built binary help/config flows passed: `./bin/roundfix --help`,
  `implement --help`, `settle --help`; scratch `roundfix init --scope project`
  generated `worktree.location` and `worktree.concurrency: 2`; invalid
  `resolve.concurrent` config exited `2` before side effects and pointed to
  `worktree.concurrency`.
- E3: Focused functional tests passed: `rtk go test ./internal/config/
  ./internal/worktree/ ./internal/daemon/ ./internal/cli/ ./internal/tui/
  -run '<parallel scheduling matrix>'` reported `21 passed in 5 packages`.
- E4: Focused config validation tests passed: `rtk go test ./internal/config/
  -run 'TestLoadRejectsInvalidConfig|TestInitCreatesUserConfig|...'` reported
  `15 passed in 1 packages`.
- E5: Scheduler race gate passed: `rtk go test -race ./internal/daemon/`
  reported `57 passed in 1 packages`.
- E6: Docs/skill/binary checks passed: `rtk go run -buildvcs=false
  ./cmd/roundfix skills check`; `.agents/skills/roundfix/SKILL.md` and
  `skills/roundfix/SKILL.md` are byte-identical; fixture and binary string
  checks found `Concurrency`, reap, settle-conflict, and integration-conflict
  line shapes.
- E7: Current Run attach replay for
  `run_20260706T092324Z_98c1c46801bfe778` showed Spec
  `0009-parallel-scheduling`, `Concurrency: 2`, the expected Run Worktree, and
  all six task rows completed before this QA child work.
- E8: Current Run Branch history contains exactly one `Roundfix-Task` commit
  for each task in graph order: `task_01`, `task_02`, `task_03`, `task_04`,
  `task_05`, `task_06`.

## Results

| # | Criterion / user story | Verdict | Evidence |
| - | ---------------------- | ------- | -------- |
| US1 | Wide Wave Tasks execute concurrently so wall-clock follows graph depth rather than size. | pass | E3 `TestTaskCycleSchedulesIndependentWaveWithConcurrencyCap` observes overlap at cap 2; E5 race clean. |
| US2 | Run report and Run Branch stay readable: task lines in graph order, one commit per Task regardless of completion order. | pass | E3 `TestRenderImplementTaskLinesKeepsGraphOrderWhenCompletionReversed`; E8 one task commit per `Roundfix-Task` trailer. |
| US3 | Integration conflicts fail the conflicting Task, name the conflict, and keep the Task Worktree. | pass | E3 worktree/daemon/settle conflict tests; E6 fixture and binary strings include the conflict line shapes. |
| US4 | Cockpit shows every executing Task simultaneously with its own state. | pass | E3 `TestCockpitSpecRunDerivesConcurrentTaskStateFromJournal` and attach replay determinism tests; E7 live attach renders the spec run with concurrency. |
| US5 | Concurrency config has conservative default and sequential opt-out. | pass | E2 generated config has default `2`; E4 validates hierarchy, invalid values, and generated output; E3 covers concurrency 1 parity. |
| US6 | Worktree parent directory is configurable while slug/run-id segments stay fixed. | pass | E4 config/path tests; E2 generated config documents parent location; E3 worktree lifecycle tests use derived sibling paths. |
| US7 | Empty kept worktrees and Run Branches from terminal Runs are automatically reaped. | pass | E3 implement preflight and `stop --force` reap tests; E6 fixture/binary reap strings. |
| CF1 | Ready-set scheduler runs current Wave up to `worktree.concurrency`, preserves failure policy, and honors stop boundaries. | pass | E3 scheduler cap, dependency skip, conflict, and stop-boundary tests; E5 race clean. |
| CF2 | Each concurrent Task runs in its own Task Worktree, with failed Task worktrees kept and successful ones removed after integration. | pass | E3 task worktree creation, cleanup, conflict-keep, and settle tests. |
| CF3 | Settled Tasks integrate through a serialized queue; conflicts settle failed; QA runs last alone in the Run Worktree. | pass | E3 fast-forward/cherry-pick/conflict matrix and scheduler conflict tests; task_03 Result records QA unchanged and static gate E1 passed. |
| CF4 | `worktree.location` resolves repo > global > builtin and appends `<repo-slug>/<run-id>` unconditionally. | pass | E4 hierarchy/path tests; E2 generated config and scratch validation. |
| CF5 | Preflight and `stop --force` reap provably empty terminal Run/Task branches and worktrees. | pass | E3 reap tests; E6 public line shape checks. |
| CF6 | Journal, Work Queue, stdout task lines, and Attach are concurrency-correct and deterministic. | pass | E3 TUI/CLI replay and graph-order tests; E7 live attach replay. |
| NG1 | Review-path `resolve`/`watch` parallelism was not introduced. | pass | E1 full suite, E2 help unchanged for review commands, and no new review parallelism surface found in QA checks. |
| NG2 | Retry budgets, escalation, and scheduling priorities were not introduced. | pass | E2 public help/config check shows only the intended new config keys; no such behavior surfaced. |
| NG3 | Cross-Run parallelism remains out of scope. | pass | E1 existing Active Run lock tests remain green through full gate; no new cross-run CLI surface found. |
| NG4 | Merge-conflict resolution was not introduced; conflicts fail by design. | pass | E3/E6 conflict tests and strings show failed settlement with named path, not resolution. |
| NG5 | Distributed or remote execution was not introduced. | pass | E2 help/config checks and E1 full gate show local CLI/worktree behavior only. |
| T01-AC1 | Config hierarchy covers builtin, User override, and Project override for both new keys. | pass | E4 `TestLoadAppliesWorktreeConfigHierarchy`. |
| T01-AC2 | Path tests cover readable unique slugs, in-repo rejection, and `~` expansion. | pass | E3 `TestDeriveRootPathUsesReadableUniqueRepoSlug`; E4 invalid-value and expansion config tests. |
| T01-AC3 | `resolve.concurrent` fails with pointing message; generated config omits it and documents both new keys. | pass | E2 scratch binary flow; E4 generated/invalid config tests. |
| T01-AC4 | Existing worktree tests pass with deliberate path-shape updates. | pass | E1 full gate; E3 worktree matrix tests. |
| T02-AC1 | Two Task Worktrees from one tip integrate first by fast-forward and second by cherry-pick, preserving messages/trailers in completion order. | pass | E3 `TestTaskWorktreesIntegrateFirstByFastForwardThenCherryPick`. |
| T02-AC2 | Induced conflict names the path, leaves Run Branch tip unchanged, and keeps the conflicting Task Worktree. | pass | E3 `TestIntegrateTaskReturnsConflictAndLeavesRunBranchUnmoved`. |
| T02-AC3 | Success cleanup removes worktree and branch; failure keeps both. | pass | E3 cleanup and conflict-keep tests. |
| T02-AC4 | Reap matrix removes only empty terminal Run/Task branches and keeps valuable/non-terminal work. | pass | E3 `TestPruneTerminalReapsOnlyEmptyTerminalRunAndTaskBranches`. |
| T02-AC5 | Full suite passes; no scheduler wiring outside the package for task_02. | pass | E1 full gate; task_02 Result evidence reviewed. |
| T03-AC1 | A 4-Task independent Wave at concurrency 2 overlaps, makes 4 commits, has correct counts, and deterministic journal replay. | pass | E3 `TestTaskCycleSchedulesIndependentWaveWithConcurrencyCap`; E5 race clean. |
| T03-AC2 | Dependency gating holds under concurrency and failed-dependency chains are skipped. | pass | E3 `TestTaskCycleGatesDependenciesAndSkipsFailedDependencyChainsUnderConcurrency`. |
| T03-AC3 | Integration conflict settles the Task failed, keeps worktree, journals reason, and independents complete. | pass | E3 `TestTaskCycleIntegrationConflictSettlesTaskFailedAndKeepsTaskWorktree`. |
| T03-AC4 | Stop Request mid-Wave drains running Tasks, starts nothing new, and ends Stopped. | pass | E3 `TestTaskCycleStopRequestMidWaveDrainsRunningTasksAndStartsNothingNew`. |
| T03-AC5 | Concurrency 1 preserves existing TaskCycle behavior and daemon race gate is clean. | pass | E3 sequential-parity coverage; E5 race clean. |
| T04-AC1 | Interleaved journal fixture renders two `Executing` rows in graph order and updates totals after out-of-order settlement. | pass | E3 `TestCockpitSpecRunDerivesConcurrentTaskStateFromJournal`. |
| T04-AC2 | Reversed-completion fixture keeps stdout task lines in graph order, byte-exact. | pass | E3 `TestRenderImplementTaskLinesKeepsGraphOrderWhenCompletionReversed`. |
| T04-AC3 | Attach over the fixture renders identically to live pass. | pass | E3 `TestCockpitSpecRunInterleavedTaskReplayMatchesLivePolling`; E7 live attach smoke. |
| T04-AC4 | Review snapshots unchanged and full suite passes. | pass | E1 full gate; E3 TUI/CLI focused tests. |
| T05-AC1 | Failed concurrent Task's kept Task Worktree settles, verifies, commits, integrates, and cleans up. | pass | E3 `TestRunSettleRetargetsKeptTaskWorktreeAndCleansUpAfterIntegration`. |
| T05-AC2 | Sequential Run Worktree fallback and legacy current-repository fallback still pass. | pass | E1 full gate; task_05 Result evidence reviewed for existing settle tests. |
| T05-AC3 | Settle-time conflict exits 1, names paths, and keeps surfaces. | pass | E3 `TestRunSettleTaskWorktreeIntegrationConflictKeepsSurfaces`; E6 binary/fixture strings. |
| T05-AC4 | Force-stopping a Run with zero settled work reaps worktrees/branches with report lines; Runs with commits are kept. | pass | E3 `TestRunStopForceReapsEmptyRunAndTaskWorktrees` and `TestRunStopForceKeepsRunWorktreeWithCommits`. |
| T05-AC5 | Full suite passes. | pass | E1 full gate. |
| T06-AC1 | Canonical skill text matches shipped behavior and drift check passes. | pass | E6 skill check and byte-identical skill copies. |
| T06-AC2 | Documented line shapes appear verbatim in CLI test fixtures. | pass | E6 fixture grep for concurrency, reap, settle conflict, and integration conflict strings. |
| T06-AC3 | README documents both keys with hierarchy and fixed segments rule. | pass | E6 README/glossary grep; E2 generated config. |
| T06-AC4 | No new un-glossaried term. | pass | E6 `CONTEXT.md` defines `Wave` and `Task Worktree`. |

## Failures and blocked items

None.

## Environment

- Repo/worktree:
  `/Users/marcio/.roundfix/worktrees/roundfix-339f8dac/run_20260706T092324Z_98c1c46801bfe778`.
- Date: `2026-07-06`.
- Static gate command: `rtk make verify`. The first sandboxed run failed
  because Go attempted to use `/Users/marcio/Library/Caches/go-build`; the
  same command passed after approved full filesystem access.
- Binary: `bin/roundfix`, built by `make verify`.
- Scratch config repo:
  `/private/tmp/roundfix-qa-0009-config.LMEuX0`.
- Current Run Database read-only checks: `~/.roundfix/roundfix.db` confirmed
  run `run_20260706T092324Z_98c1c46801bfe778` is an `implement` run for this
  spec. The attach command was read-only; the follow process was terminated
  after the replay snapshot.
- No commits, pushes, PRs, or source-code edits were performed by this QA
  child agent.
