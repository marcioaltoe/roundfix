---
spec: 0075-typed-docs-backlog
date: 2026-08-05
build: c24df276
status: closed
verdict: pass
rows_blocked_environment: 1
rows_blocked_finding: 0
rows_blocked_declared: 0
surfaces: [backend, docs]
---

# QA report — A typed backlog for triage intent

## Scope and environment

Full terminal gate for the authored `task_05` node on the Run Worktree build
`c24df276`. The intended actors are a maintainer applying the Context-Driven
Baseline and a documentation contributor using only the generated layout
guide. The public surfaces are the built `roundfix baseline` CLI and the
generated documentation contract. Pull Request journeys are not part of this
Spec; the prompt confirms that no Pull Request is open.

The gate runs on macOS in the Run Worktree. Fresh-application and template
authoring journeys use disposable directories under `/private/tmp`; no
credential or external service is required. Selected probes are: a fresh
generation rather than the checked-in postimage, a second generation for
idempotency, template use without reading the Spec, all four type variants,
the invalid abbreviation `refact`, and lifecycle branches for promotion and
decline. These probes target the documented drift and ambiguity risks.

The fresh application used the built `bin/roundfix` against a detached,
sparse disposable clone rooted at commit `914da344`. The clone exposed a
declared `rtk make verify` command and the two Profile-required documentation
skills, but no pre-existing Baseline-managed guide. `baseline plan` produced
portable plan digest
`sha256:da9d3e533b53e118a30a7de97139bb653a6f4f0c913aacb4fbe7105711488b4f`;
`baseline apply` exited 0 with `state: verified`, and an immediate re-plan
returned an empty `fileChanges` array.

Durable focused evidence:

- [Fresh Baseline journey](evidence/2026-08-05-typed-docs-backlog/fresh-baseline-summary.md)
- [Template-only authoring fixtures](evidence/2026-08-05-typed-docs-backlog/template-authoring-fixtures.md)

## Static gate

`rtk make verify` exited 0 on build `c24df276-dirty`. The unpiped gate
reported 3,410 passing Go tests across 26 packages, the focused Spec budget
test, four skill-policy tests, a passing Roundfix skill check, a successful
build, and `bin/roundfix spec check` reporting no findings for Spec 0075.
The Spec check transparently skipped its vocabulary-contract detector because
this TechSpec has no `Vocabulary Contract` heading and its reference-index
detector because the Spec has no adopted `references/`; those presence-aware
skips are reported by the checker and are not failures.

## Governance audit

Both active artifacts contain complete Project Constraints rows and cite
operative sources that resolve under `docs/agents/`. Identifier strategy is
applicable and uses the canonical Backlog Entry/type vocabulary;
authentication and HTTP are inapplicable because no transport or credential
surface exists; the active ADR row covers the digest, backlog, knowledge-flow,
and authored-gate obligations; tooling authority is inapplicable.

`git diff-tree --no-commit-id --name-only -r` inventories were read for all
eight Run commits. Task 01 changed only its Task file and
`internal/baseline/assets/modules/context-workflow.json`. The independent
consequent fix `8436808c` follows Task 01 and changes only Task 02 plus
`internal/baseline/preservation_test.go`, replacing the stale literal count
with named corpus expectations. Task 02 then changes its Task file and the
declared derived Baseline assets/corpus. Tasks 03 and 04 change only their Task
files plus the local guide/backlog entry and glossary respectively. No
`Makefile`, workflow, skill, dependency manifest, ignore file, plugin
declaration, or production Go path changed. The 2026-08-05 maintainer record
classifies Baseline module assets as product content, not protected tooling;
the two fresh `make baseline-digests` passes returning `changed:false`
independently confirm the derived pins match their canonical sources.

## Planned matrix

| # | Story / criterion / sweep | Actor and entry point | Steps and expected observable | Independent confirmation and persistence | Evidence | Status |
| - | --- | --- | --- | --- | --- | --- |
| QA-01 | Authored terminal QA node and completed dependencies | QA operator; `_tasks.md` and Task frontmatter | Resolve `qa: task_05`; prove it is `type: qa`, terminal, and depends on every non-QA leaf; both direct leaves must be completed | Re-read every Task status and graph edge | `_tasks.md`; `task_01.md`–`task_05.md` | pass |
| QA-02 | Project Constraints and protected-change audit | Maintainer; active PRD, TechSpec, operative guides, Git ancestry | Account for identifier, auth/HTTP, active ADRs, and tooling authority; resolve actual changed paths and any authorization/prerequisite/consequent commits | Compare declared bounded scope with `git diff-tree` inventories and authorization record | PRD; TechSpec; `docs/agents/`; Git history | pass |
| QA-03 | Repository Verification | Maintainer; repository root | Run exactly `rtk make verify`; expect exit 0 with every configured target green | Exit code and complete target summary from the unpiped command | Current command output | pass |
| QA-04 | Fresh Baseline apply produces the contract | Adopting maintainer; built `roundfix baseline` CLI in a disposable repository | Plan and apply the active Baseline without using this repository's checked-in guide; expect generated `docs/agents/docs-layout.md` to contain the backlog contract | Re-plan or re-apply and compare the generated guide byte-for-byte | Disposable repository plus generated guide | pass |
| QA-05 | One-job layout and operational frontmatter | Documentation contributor; freshly generated layout guide | Find `docs/backlog/`, dated filename rule, `type`, `status`, `created`, `spec`, and `reason`, plus lifecycle values | Inspect the generated guide after the idempotency run | Fresh generated guide | pass |
| QA-06 | Write a `feat` entry from the template alone | Documentation contributor; freshly generated guide | Copy only the `feat` template into a disposable backlog entry and fill Opportunity, Value, and Shape; expect a structurally complete entry | Reopen the written entry and validate headings/frontmatter without consulting the Spec | Disposable `feat` entry | pass |
| QA-07 | Write a `fix` entry from the template alone | Documentation contributor; freshly generated guide | Copy only the `fix` template and fill Symptom, Where, Expected, and Evidence including honest `none yet` | Reopen and validate the entry independently | Disposable `fix` entry | pass |
| QA-08 | Write a `perf` entry from the template alone | Documentation contributor; freshly generated guide | Copy only the `perf` template and fill Slow, Measured, and Target with concrete values | Reopen and validate the entry independently | Disposable `perf` entry | pass |
| QA-09 | Write a `refactor` entry from the template alone | Documentation contributor; freshly generated guide | Copy only the `refactor` template and fill Tangled, Cost, and Shape; verify `refact` is never offered | Reopen and validate the entry; search the generated contract for the invalid abbreviation | Disposable `refactor` entry; generated guide | pass |
| QA-10 | Boundary, promotion, decline, and extension semantics | Maintainer and contributor; freshly generated guide | Follow the finding/backlog boundary in both directions; model `promoted` with Spec link and `declined` with reason; verify `feat` is not `write-idea` and extension requires a contract change plus corpus re-record | Re-read generated contract and compare with ADR-0092 | Fresh generated guide; ADR-0092 | pass |
| QA-11 | Repository adoption and canonical vocabulary | Repository maintainer; `docs/agents/docs-layout.md`, `docs/backlog/`, and `CONTEXT.md` | Confirm the local guide matches generated contract, the seeded entry uses exact frontmatter and real content, and Backlog Entry uses canonical vocabulary | Compare the managed guide block to the regenerated golden and reopen the seeded entry/glossary | Checked-in guide, entry, glossary, formatter golden | pass |
| QA-12 | Findings stay observations and are not migrated | Repository maintainer; Git history and `docs/findings/` | Prove no finding changed, moved, or became backlog content; prove the pre-existing findings contract clause is byte-identical | Compare relevant commits against the pre-Spec base and diff the clause bytes | Git path inventory and clause diff | pass |
| QA-13 | Compatibility corpus and digest chain are deterministic | Baseline maintainer; disposable source checkout | Run the sanctioned regeneration sequence twice; expect the second settled state to be unchanged and corpus checks to pass | Compare tracked state before and after the second run; inspect every moved path against the declared break list | Disposable checkout command output and diff | blocked (environment: inherited Go cache outside managed sandbox) |
| QA-14 | No Roundfix binary reads or validates the backlog | CLI operator; built `roundfix` help and source-change inventory | Exercise public CLI help and search command surface; prove the Spec introduced no Go change or backlog command/validation path | Compare every Spec commit path with the pre-Spec base and run the built CLI | CLI output; Git path inventory | pass |
| QA-15 | No guide references backlog-entry content | Documentation maintainer; generated and checked-in guides | Search guides for links or dependencies on a concrete `docs/backlog/*.md` entry; expect none | Repeat over the freshly generated guide and all checked-in agent guides | Search output | pass |
| QA-16 | Non-Goals: no skill changes, finding deprecation, note migration, or binary behavior | Maintainer; complete Spec diff | Audit the full changed-path and content diff for all four exclusions | Compare changed paths with PRD Non-Goals and public-surface evidence | Git diff and preceding rows | pass |

## Results

| # | Story / criterion / sweep | Actor and surface | Status | Evidence |
| - | --- | --- | --- | --- |
| QA-01 | Authored terminal QA node and completed dependencies | QA operator; docs | pass | `qa: task_05`; Task 05 is `type: qa`, terminal, depends on completed Tasks 03 and 04, and those leaves close all non-QA paths. |
| QA-02 | Project Constraints and protected-change audit | Maintainer; docs/Git | pass | Four constraint categories are explicit in PRD and TechSpec with resolving sources. Eight commit inventories show no protected tooling mutation; consequent test fix follows its cause; regenerated assets converge with `changed:false`. |
| QA-03 | Repository Verification | Maintainer; backend | pass | `rtk make verify` exited 0: 3,410 Go tests passed; skill check, build, and Spec check passed with no Spec 0075 finding. |
| QA-04 | Fresh Baseline apply produces the contract | Adopting maintainer; CLI/docs | pass | [Fresh Baseline journey](evidence/2026-08-05-typed-docs-backlog/fresh-baseline-summary.md): public plan/apply exited 0, result state `verified`, re-plan had zero file changes, generated guide hash `000ae9…b91`. |
| QA-05 | One-job layout and operational frontmatter | Contributor; docs | pass | [Fresh Baseline journey](evidence/2026-08-05-typed-docs-backlog/fresh-baseline-summary.md): generated guide names `docs/backlog/`, dated filenames, five fields, all lifecycle values, and all four types. |
| QA-06 | `feat` template usability | Contributor; docs | pass | [Template-only fixtures](evidence/2026-08-05-typed-docs-backlog/template-authoring-fixtures.md): exact five-field frontmatter plus Opportunity, Value, and Shape; independent probes exited 0. |
| QA-07 | `fix` template usability | Contributor; docs | pass | [Template-only fixtures](evidence/2026-08-05-typed-docs-backlog/template-authoring-fixtures.md): exact five-field frontmatter plus Symptom, Where, Expected, and Evidence; `none yet` worked as documented. |
| QA-08 | `perf` template usability | Contributor; docs | pass | [Template-only fixtures](evidence/2026-08-05-typed-docs-backlog/template-authoring-fixtures.md): exact five-field frontmatter plus Slow, Measured, and Target; independent probes exited 0. |
| QA-09 | `refactor` template usability | Contributor; docs | pass | [Template-only fixtures](evidence/2026-08-05-typed-docs-backlog/template-authoring-fixtures.md): exact five-field frontmatter plus Tangled, Cost, and Shape; standalone `refact` search returned no match. |
| QA-10 | Boundary, lifecycle, and extension semantics | Maintainer/contributor; docs | pass | [Template-only fixtures](evidence/2026-08-05-typed-docs-backlog/template-authoring-fixtures.md): promoted probe moved with Spec slug; declined probe retained reason; fresh guide carries both boundary directions and extension rule. |
| QA-11 | Repository adoption and glossary | Maintainer; docs | pass | Local `guide.docs-layout` block and regenerated golden share SHA-256 `e7ec5b5aa092a12661d95b7639340ff4656994390e97933599f9f0a58e38599e`; seeded entry has five fields, real measurements/targets, and a resolving evidence link; glossary names the lifecycle and four types. |
| QA-12 | Findings preservation and no migration | Maintainer; docs/Git | pass | Full pre-Spec-to-build path diff has no `docs/findings/` change. Filtered findings-clause bytes before and after share SHA-256 `37517e5f3dc66819f61f5a7bb8ace1921282415f10551d2defa5c3eb0985b570`. |
| QA-13 | Corpus and digest determinism | Baseline maintainer; backend/data | blocked (environment: inherited Go cache outside managed sandbox) | [Fresh Baseline journey](evidence/2026-08-05-typed-docs-backlog/fresh-baseline-summary.md): both digest runs exited 0 with `changed:false`; checkout stayed clean. Direct re-record commands hit the recorded sandbox cache denial. Equivalent evidence includes completed Task 02 and the passing full gate. |
| QA-14 | No binary backlog behavior | CLI operator; backend/CLI | pass | Built root and Baseline help expose no backlog command. Production-Go diff is empty; current Go source has no `docs/backlog` or Backlog Entry reference. |
| QA-15 | No guide depends on backlog-entry content | Documentation maintainer; docs | pass | Concrete dated-entry/link search across `docs/agents/` returned no match; the same fresh guide used for authoring contains only the generic contract. |
| QA-16 | Non-Goals and scope-creep sweep | Maintainer; docs/backend | pass | Full 22-path Spec diff changes no skill, finding, production Go, workflow, Makefile, or dependency file; the only backlog path is a new entry, not a migration. Findings remain active and no command behavior was added. |

## Findings

None.

## Blocked and skipped

QA-13 is blocked by the managed sandbox's inherited Go cache:
`go env GOCACHE` resolves to
`/Users/marcio/Library/Caches/go-build`, and both direct characterization
re-record commands failed when opening an object below that path. A full-access
session with a readable Go cache unblocks those two commands. Equivalent
evidence exists on this build: Task 02 is Daemon-settled `completed`;
`rtk make verify` passed the baseline package; two fresh sanctioned
`rtk make baseline-digests` runs each exited 0 with
`{"ok":true,"changed":false}`; and the detached scratch checkout remained
clean. No row is finding-blocked or skipped.

## Coverage

All 16 rows settled: 15 pass, 1 environment-blocked with equivalent observed
and Daemon-supervised evidence, 0 fail, 0 finding-blocked, 0 declared-blocked,
and 0 skipped. All PRD goals, seven Core Features, four Non-Goals, four Task 05
requirements, and every Task acceptance criterion are covered. The fresh-apply
idempotency, four template variants, invalid abbreviation, promotion, and
decline probes all ran.

## Final verdict

Pass: every runnable row passed, QA-13 has a proved environment cause plus
equivalent fresh and Daemon-supervised evidence, and no row failed, was
finding-blocked, declared-blocked, or skipped.
