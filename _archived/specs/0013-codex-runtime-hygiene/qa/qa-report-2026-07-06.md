---
spec: 0013-codex-runtime-hygiene
date: 2026-07-06
verdict: pass
surfaces: [cli, infra, docs]
---

# QA Report - Codex Runtime Hygiene

## Summary

QA exercised the completed Spec against the built `roundfix` binary on Darwin,
the full repository verification gate, focused Go tests for the Doctor Command
and codex runtime hygiene, and the Roundfix skill/docs sync checks. The previous
same-date failure is fixed: Doctor now inspects the configured `CODEX_PATH`
before falling back to `PATH`. All PRD user stories, task acceptance criteria,
and non-goals pass.

## Results

| # | Criterion / user story | Verdict | Evidence |
| - | ---------------------- | ------- | -------- |
| 1 | User story 1: macOS developer learns that PATH codex is quarantined or not accepted before a Run dies. | pass | `rtk ./bin/roundfix doctor` exited 1 on Darwin and printed `codex: failed (/Users/marcio/.local/bin/codex has an invalid or missing code signature; next: reinstall codex with the official curl installer into ~/.local/bin, then set CODEX_PATH to that binary)`. |
| 2 | User story 2: diagnosis prints the curl reinstall fix. | pass | The host Doctor run and the quarantined-fixture run both printed `next: reinstall codex with the official curl installer into ~/.local/bin, then set CODEX_PATH to that binary`. |
| 3 | User story 3: Doctor reports every Run prerequisite in one place without starting a Run. | pass | `rtk ./bin/roundfix doctor` printed one line each for `node`, `acpx`, `agent`, and `codex`; `rtk ./bin/roundfix doctor --help` describes the command as diagnosis-only. |
| 4 | User story 4: agent loops spawn a verified-clean codex. | pass | `rtk go test ./internal/agent/ -run Codex` passed 8 tests, including configured clean path selection, PATH fallback, quarantined-only failure, once-per-session inspection, and non-Darwin unchanged behavior. |
| 5 | User story 5: Linux/Windows report codex hygiene not-applicable rather than fail. | pass | `rtk go test ./internal/... -run Codex` passed 15 tests, including non-Darwin inspector behavior; `rtk go test ./internal/cli/ -run Doctor` passed the not-applicable Doctor case. |
| 6 | Core feature: `roundfix doctor` runs Node, pinned acpx, Agent probe, and codex hygiene, one line each, failing nonzero on Run-breaking failures. | pass | `rtk ./bin/roundfix doctor` printed all four check lines and exited 1 for the host codex failure; `rtk /usr/bin/env CODEX_PATH=/private/tmp/roundfix-qa-codex-clean ./bin/roundfix doctor` exited 0 when the configured fixture was signed and not quarantined. |
| 7 | Core feature: Doctor mutates nothing and remains distinct from Setup. | pass | `rtk ./bin/roundfix doctor --help` says `Doctor mutates nothing`; `rtk go test ./internal/cli/ -run Doctor` passed 5 tests, including the no-disk-mutation assertion. |
| 8 | Core feature: macOS codex hygiene checks quarantine and signing acceptance and reports the reinstall fix. | pass | A signed configured fixture passed, then `rtk proxy xattr -w com.apple.quarantine ...` marked it quarantined and the same Doctor command failed with `is quarantined with com.apple.quarantine; next: reinstall codex...`. `internal/codex/hygiene.go` uses read-only `xattr <path>` and `codesign --verify <path>`. |
| 9 | Core feature: codex hygiene covers a configured codex path before `PATH`. | pass | `rtk /usr/bin/env CODEX_PATH=/tmp/roundfix-qa-nonexistent-codex ./bin/roundfix doctor` inspected `/tmp/roundfix-qa-nonexistent-codex` and reported the `xattr` failure for that path, rather than inspecting `/Users/marcio/.local/bin/codex` from `PATH`. `internal/cli/health.go` passes `os.Getenv("CODEX_PATH")` into `codex.Inspector`. |
| 10 | Core feature: non-Darwin codex hygiene reports not-applicable. | pass | `rtk go test ./internal/... -run Codex` passed non-Darwin inspector and spawn cases; `rtk go test ./internal/cli/ -run Doctor` passed the Doctor skipped-case coverage. |
| 11 | Core feature: codex-acp launch resolves and passes a verified-clean codex. | pass | `rtk go test ./internal/agent/ -run Codex` passed `TestACPXRunCodexUsesConfiguredCleanPathOnDarwin`, `TestACPXRunCodexFallsBackToCleanPathWhenConfiguredPathIsQuarantined`, and `TestACPXRunCodexSurfacesQuarantinedPathWithoutSpawning`. |
| 12 | Non-goal: no automatic codex download, reinstall, or de-quarantine. | pass | Static search found the reinstall text only as a next action. Production code uses `xattr` read and `codesign --verify`; no `xattr -d`, `spctl --add`, or curl install execution appears in `internal`, README, or the Roundfix skill. |
| 13 | Non-goal: no Roundfix binary notarization/signing work. | pass | Static search found notarization/signing references only in codex hygiene docs and code comments; this Spec does not add Roundfix binary signing behavior. |
| 14 | Non-goal: no Claude/OpenCode hygiene checks. | pass | Hygiene behavior is scoped to `internal/codex`; Doctor still runs only the generic configured Agent probe for non-codex runtimes. README and the Roundfix skill document codex runtime hygiene only. |
| 15 | Non-goal: no background watcher or periodic health poll. | pass | `rtk ./bin/roundfix --help` exposes on-demand `roundfix doctor`; no hygiene watcher or polling command is present. |
| 16 | Non-goal: no Windows Defender or Linux equivalent checks. | pass | Non-Darwin behavior is `skipped`/not-applicable in the focused inspector and Doctor tests; docs state the codex check is skipped on non-Darwin platforms. |
| 17 | Task 01 AC: Node, acpx, and Agent probe checks live in a shared unit returning structured results. | pass | `rtk go test ./internal/cli/ -run Setup` passed 9 tests; `internal/cli/health.go` defines structured `CheckResult` values for `node`, `acpx`, `agent`, and `codex`. |
| 18 | Task 01 AC: `roundfix setup` output and mutating behavior are unchanged. | pass | `rtk go test ./internal/cli/ -run Setup` passed 9 tests. |
| 19 | Task 01 AC: no new command or flag introduced by task 01. | pass | Task 01 result records no command or flag added in that slice; current `doctor` command arrives through task 03. |
| 20 | Task 02 AC: quarantined codex yields failure with curl reinstall fix. | pass | The quarantined configured fixture made `roundfix doctor` fail with `is quarantined with com.apple.quarantine` and the reinstall next action; `rtk go test ./internal/... -run Codex` also passed the quarantined table case. |
| 21 | Task 02 AC: un-notarized/not-accepted codex yields failure. | pass | `rtk go test ./internal/... -run Codex` passed the not-accepted path codex case; the real host codex also failed Doctor with `invalid or missing code signature`. |
| 22 | Task 02 AC: clean codex yields passing result. | pass | The ad-hoc signed configured fixture at `/private/tmp/roundfix-qa-codex-clean` made `rtk /usr/bin/env CODEX_PATH=/private/tmp/roundfix-qa-codex-clean ./bin/roundfix doctor` exit 0 with `codex: ok (... valid code signature and no com.apple.quarantine attribute)`. |
| 23 | Task 02 AC: non-Darwin inspector reports not-applicable and never fails. | pass | `rtk go test ./internal/... -run Codex` passed 15 tests, including the non-Darwin inspector case. |
| 24 | Task 02 AC: quarantine and acceptance probes are injected in tests. | pass | `rtk go test ./internal/... -run Codex` passed the fake-probe inspector tests; no real `xattr` or `codesign` is needed for those table cases. |
| 25 | Task 03 AC: `roundfix doctor` prints one line per check with status and next action. | pass | Built CLI Doctor runs printed `node`, `acpx`, `agent`, and `codex`; focused Doctor tests passed exact-output cases. |
| 26 | Task 03 AC: quarantined codex on macOS fails and exits nonzero. | pass | `rtk /usr/bin/env CODEX_PATH=/private/tmp/roundfix-qa-codex-clean ./bin/roundfix doctor` exited 1 after the fixture received `com.apple.quarantine`. |
| 27 | Task 03 AC: Linux/Windows codex line reports not-applicable and does not fail on that account. | pass | `rtk go test ./internal/cli/ -run Doctor` passed the non-Darwin not-applicable Doctor case. |
| 28 | Task 03 AC: Doctor mutates nothing on disk. | pass | Focused Doctor tests passed the no-disk-mutation assertion; real Doctor help states the command mutates nothing and the command has no install/config-write flags. |
| 29 | Task 04 AC: codex-acp resolves configured clean codex over quarantined PATH entry. | pass | `rtk go test ./internal/agent/ -run Codex` passed `TestACPXRunCodexUsesConfiguredCleanPathOnDarwin`. |
| 30 | Task 04 AC: only quarantined codex surfaces risk rather than silently spawning. | pass | `rtk go test ./internal/agent/ -run Codex` passed `TestACPXRunCodexSurfacesQuarantinedPathWithoutSpawning`. |
| 31 | Task 04 AC: codex hygiene is inspected once per Run resolution, not every exec. | pass | `rtk go test ./internal/agent/ -run Codex` passed `TestACPXRunCodexInspectsOncePerSessionResolution`. |
| 32 | Task 04 AC: non-macOS spawn behavior is unchanged. | pass | `rtk go test ./internal/agent/ -run Codex` passed `TestACPXRunCodexLeavesNonDarwinSpawnUnchanged`. |
| 33 | Task 05 AC: docs accurately describe `roundfix doctor`, its checks, and macOS-only remediation. | pass | README and `.agents/skills/roundfix/SKILL.md` document Doctor, `CODEX_PATH` before `PATH`, `com.apple.quarantine`, `codesign --verify`, curl reinstall remediation, and non-Darwin skipped behavior. |
| 34 | Task 05 AC: SKILL.md matches the shipped CLI surface and embedded copy is regenerated. | pass | `rtk ./bin/roundfix skills check` passed; `.agents/skills/roundfix/SKILL.md` and `skills/roundfix/SKILL.md` both include the Doctor Command surface. |
| 35 | Task 05 AC: `roundfix skills check` passes and `skills-sync-check` reports no drift. | pass | `rtk ./bin/roundfix skills check` exited 0 with `Roundfix skill check passed: roundfix`; `rtk make skills-sync-check` exited 0 with no diff output. |

## Failures and blocked items

None.

## Environment

- Worktree:
  `/Users/marcio/.roundfix/worktrees/roundfix-339f8dac/run_20260706T173904Z_4b479c1491ff3e93`
- Host platform: `darwin` from `rtk go env GOOS`.
- Static gate:
  - `rtk make verify` first failed inside the sandbox because Go tried to open
    `~/Library/Caches/go-build/...`.
  - The same `rtk make verify` command passed outside the sandbox with 785
    tests in 18 packages, `Roundfix skill check passed: roundfix`, and
    `bin/roundfix` built.
- Focused gates:
  - `rtk go test ./internal/cli/ -run Doctor` passed 5 tests.
  - `rtk go test ./internal/cli/ -run Setup` passed 9 tests.
  - `rtk go test ./internal/... -run Codex` passed 15 tests in 16 packages.
  - `rtk go test ./internal/agent/ -run Codex` passed 8 tests.
- CLI flows:
  - `rtk ./bin/roundfix --help` listed `roundfix doctor`.
  - `rtk ./bin/roundfix doctor --help` described the read-only Doctor command.
  - `rtk ./bin/roundfix doctor` printed four check lines and exited 1 because
    the host codex has an invalid or missing code signature.
  - `rtk /usr/bin/env CODEX_PATH=/tmp/roundfix-qa-nonexistent-codex ./bin/roundfix doctor`
    inspected the configured path and failed on that path's `xattr` error.
  - `rtk proxy cp /bin/echo /private/tmp/roundfix-qa-codex-clean`, then
    `rtk proxy codesign --force --sign - /private/tmp/roundfix-qa-codex-clean`
    created the controlled configured-path fixture.
  - `rtk /usr/bin/env CODEX_PATH=/private/tmp/roundfix-qa-codex-clean ./bin/roundfix doctor`
    exited 0 before the fixture was quarantined.
  - `rtk proxy xattr -w com.apple.quarantine roundfix-qa /private/tmp/roundfix-qa-codex-clean`
    marked only the QA fixture as quarantined.
  - The same configured-path Doctor command then exited 1 with the quarantine
    failure and reinstall next action.
- Docs/skill checks:
  - `rtk ./bin/roundfix skills check` passed.
  - `rtk make skills-sync-check` passed with no diff output.
- Local search note: `rg` is not installed in this sandbox, so repository
  searches used `rtk proxy grep` / `rtk proxy find` after `rtk proxy rg`
  reported `rg: No such file or directory`.
