---
spec: 0069-review-run-targets-its-pull-request
date: 2026-08-05
build: 609356ca
status: closed
verdict: pass
rows_blocked_environment: 6
rows_blocked_finding: 0
rows_blocked_declared: 0
surfaces: [backend, cli, docs]
---

# QA report — A Review Run targets its Pull Request

## Scope and environment

Full rerun of the authored terminal QA Task after corrective task_05. The Run
Worktree branch is `roundfix/run-run_20260805T233355Z_685638f9a8bae25b`, the
Spec target branch is `ma/0069-review-run-targets-its-pull-request`, and the
source build is `609356ca60b49b1944ab388ca17f086b43e07784`. The built
binary reports `609356ca-dirty` because this QA Report is the only worktree
delta. The QA prompt states that no Pull Request is open;
Pull Request journeys are environment-blocked and require equivalent observed
or supervised evidence. The gate will not commit, push, open or mutate a Pull
Request, or move a working tree.

The public CLI probes used a shared scratch clone at
`/private/tmp/roundfix-qa0069.5Vknrn/repo`, with `ma/other-review` checked out
and `ma/feature-review` supplied as explicit PR Head Branch metadata. Both
branches pointed to the source build. This makes mismatch and fork Preflight
paths runnable without representing the synthetic metadata as a live Pull
Request. The scratch clone stayed clean and on `ma/other-review` throughout.

Actors: a Roundfix operator invoking `fetch`, `resolve`, or `watch`; the
maintainer auditing the Spec, authorization, and Git ancestry. Surfaces: the
built CLI, backend Run/Store boundaries, documentation, filesystem state, and
Git refs. The PRD has no `## Unreachable Acceptance` declaration.

High-risk probes: an absent explicit Artifact Directory on refusal, each
Preflight refusal family, explicit target input, fork head input, movement
before each write boundary, detached refusal cleanup, and a matching-checkout
canary. These target the two historical failure shapes, the prior F-001/F-002
findings, and bypass or regression paths introduced by the corrective Task.

## Static gate

- `rtk make verify` — exit 0. Fresh results: 3,499 Go tests passed in 26
  packages; the Spec-check corpus passed; four Skill repository tests passed;
  `roundfix skills check` passed for all 14 required owned skills; and the
  `-buildvcs=false` binary build passed. The binary identifies the source as
  `609356ca-dirty` because this in-progress report is the only worktree delta.
- The repository gate's `bin/roundfix spec check` reported `No findings.` for
  Spec 0069. It skipped only the structurally absent TechSpec Vocabulary
  Contract and `references/_index.md` checks; the direct Project Constraint
  audit remains a separate row.
- `rtk go run -buildvcs=false ./cmd/roundfix spec check 0069-review-run-targets-its-pull-request --format text`
  — exit 0, `No findings.` with the same two structurally absent subchecks.
- `rtk go test ./internal/cli -count=1 -run '^(TestReviewCommandsRefuseTargetMismatchWithoutSideEffects|TestReviewCommandsRefuseWithoutCreatingArtifactDirectory|TestReviewCommandsCreateArtifactDirectoryAfterPreflightPasses|TestRunDetachedReviewRefusalLeavesArtifactDirectoryAbsent|TestRunOperationalCommandRejectsInvalidConfigAndArtifactDirectory|TestRunOperationalCommandAppliesConfigAndCLIArtifactDirPrecedence|TestRunReviewCommandsRefuseDirtyTrackedCheckout|TestRunResolveSelectionPreflightRejectionReportsTupleAndCreatesNoRun|TestRunWatchSelectionPreflightFailureCreatesNoRun|TestRunFetchRejectsDuplicateActiveRun|TestRunDetachedCommand.*)$' -v`
  — exit 0, 36 tests passed.
- `rtk go test ./internal/preflight -count=1 -run '^(TestRunValidatesPullRequestTargetResolutionShapes|TestRunRefusesForkHeadBeforeBranchMismatch|TestRunRefusesTargetMismatchForEveryReviewCommand)$' -v`
  — exit 0, 10 tests passed.
- `rtk go test ./internal/cli -count=1 -run '^(TestRunResolveCheckoutMovedBeforeBatchInterruptsWithIssueUnsettled|TestRunWatchArtifactEvidenceInheritedWithoutCurrentHeadPolling|TestRunWatchCheckoutMovedBeforeFinalPushInterruptsAndPushesNothing|TestRunResolveCommitsOnUserBranchWithoutRunBranch|TestRunResolvePushRunsFromUserCheckoutWithoutRunWorktree|TestBranchIntegrityPreflightWatchDisregardsOnlySupersededFailedQACycles)$' -v`
  — exit 0, 6 tests passed.
- `rtk go test ./internal/watch ./internal/daemon ./internal/store -count=1 -run '^(TestRunCheckoutMovedBeforeReviewArtifactCommitInterrupts|TestRunCheckoutMovedAtBatchBoundaryUsesInterruptionOutcome|TestTargetGuardComparesRecordedBranchAndRevision|TestFinalPushCheckoutMovedBoundaryPushesNothing|TestResolveCycleCheckoutMovedBeforeBatchLeavesReviewIssuesUnsettled|TestResolveCycleCheckoutMovedAtBatchCommitCommitsNothing|TestCompleteRunAcceptsCheckoutMovedAsTerminal|TestCreateReviewRunRecordsTargetBranchAndRevision)$' -v`
  — exit 0, 11 tests passed across three packages.
- `rtk make skills-sync-check` — exit 0, four Skill repository tests passed.
- `rtk diff -qr .agents/skills/roundfix skills/roundfix` — exit 0 with no
  output; the canonical Skill and mirror are byte-identical.
- `rtk go test ./internal/baseline -count=1 -run '^(TestBaselinePlanCharacterization|TestCatalogDiagnosticCharacterization)$' -v`
  — exit 0, eight characterization tests passed.

No static failure blocked a row.

## Results

| # | Story / criterion / sweep | Actor and surface | Status | Evidence |
| - | --- | --- | --- | --- |
| 1 | Project Constraint audit: identifier strategy, authentication/HTTP, active ADR obligations, tooling authority, authorization chronology, exact changed paths, and cross-artifact truth | Maintainer — docs/Git | pass | PRD and TechSpec each account for all four constraint classes and cite operative sources under `docs/agents/`. Identifier strategy is inapplicable because existing Pull Request, branch, revision, and Run identities are retained. Authentication/HTTP reuses the existing forge resolver without new policy. The active ADR row accounts for the terminal outcome, artifact commit, Review Source Evidence, QA, graph, generated-artifact, relation-only, and consistency obligations. The standing CLI-synchronisation grant names Spec 0069 and bounds the Skill/mirror; `c1ee13f9…` is an ancestor of authoring `17b12b5e…` and tooling Task `b22ecaa9…`. Per-commit `git diff-tree` paths show Tasks 01, 02, and 05 changed implementation/tests and their Task files; Task 03 changed only its Task file, the authorized Skill/mirror, and sanctioned `DERIVED_DIGEST_PATHS` characterization output. No authorization, prerequisite fix, consequent fix, Go source, or out-of-scope tooling path was folded into Task 03. The corrected TechSpec now truthfully says the PRD carried `not applicable` until the authoring pass corrected it, closing prior F-002. |
| 2 | Repository full Verification and Spec consistency | Operator — backend/CLI/docs | pass | `rtk make verify` exited 0 with 3,499 tests, Skill checks, Spec checks, and build green. Spec 0069 reported `No findings.`; its two structurally unavailable subchecks are recorded above. |
| 3 | Mismatched checkout refuses `fetch`, `resolve`, and `watch` at Preflight with exit 2, names the Pull Request, both branches and revisions, and recovery; no Run or filesystem side effect is created | Operator — CLI/filesystem | pass | The built `fetch`, `resolve`, and `watch` commands each exited 2 and named Open Pull Request `#123`, expected `ma/feature-review`, found `ma/other-review`, both revisions, the no-Run statement, and `git switch -- 'ma/feature-review'`. Before/after `ls -ld` proved all three explicit Artifact Directories stayed absent. Detached `resolve` and `watch` also exited 2; their Artifact Directories remained absent and the scratch root retained only `repo/`, proving temporary console cleanup. The 36-test refusal suite independently confirmed zero Run, Review Source, Agent, commit, push, and filesystem effects. Prior F-001 did not reproduce. |
| 4 | Every other Preflight refusal leaves a previously absent Artifact Directory absent; a passing Preflight still creates it | Operator — CLI/filesystem | pass | The 36-test public CLI runner suite passed across shared Preflight failures, Branch Integrity refusal, missing Compatible Artifacts, Agent Selection refusal, duplicate Active Run, detached refusal, configuration precedence, and positive `fetch`/`resolve`/`watch` creation. These task-level criteria are safely credited because the named filesystem assertions passed after full Verification; the built target-mismatch and detached probes independently confirmed the repaired path. |
| 5 | Explicit `--head-branch` / `--head-repo` cannot bypass mismatch validation | Operator — CLI/Git | pass | All five built mismatch probes supplied explicit Head Repository and PR Head Branch metadata and refused. The 10-test Preflight suite passed the resolved and explicit mismatch/match table. Independent confirmation showed `ma/other-review`, HEAD `609356ca…`, and clean porcelain after every probe. |
| 6 | Matching checkout preserves the existing review journey | Operator — CLI | blocked (environment: no open Pull Request) | Cause: the QA prompt proves no Open Pull Request. Equivalent supervised evidence: the matching resolved/explicit Preflight rows, existing operational resolve/watch flows, user-checkout commit/push canaries, and artifact-evidence flow passed in the focused Preflight and CLI runs. Independent confirmation: full `make verify` passed. Unblock by providing a disposable Open Pull Request on the Spec target branch and read-only Review Source access. |
| 7 | Movement before a Batch commit reaches `CheckoutMoved`, commits nothing, and leaves Review Issues unsettled | Operator — backend/CLI | blocked (environment: no open Pull Request) | Cause: no Open Pull Request can start the live Run. Equivalent supervised evidence: `TestRunResolveCheckoutMovedBeforeBatchInterruptsWithIssueUnsettled`, `TestResolveCycleCheckoutMovedBeforeBatchLeavesReviewIssuesUnsettled`, and `TestResolveCycleCheckoutMovedAtBatchCommitCommitsNothing` passed, inspecting persisted issue status and zero commit calls. Unblock with the disposable Pull Request and a safely controlled checkout. |
| 8 | Movement before the review-artifact commit reaches `CheckoutMoved` and publishes no artifact commit | Operator — backend/CLI | blocked (environment: no open Pull Request) | Cause: no Open Pull Request. Equivalent supervised evidence: `TestRunCheckoutMovedBeforeReviewArtifactCommitInterrupts` passed, observing `CheckoutMoved`, a terminal reason and next action, and zero artifact-publisher calls. Unblock with the disposable Pull Request and a safely controlled checkout. |
| 9 | Movement before Final Push reaches `CheckoutMoved` and pushes nothing | Operator — backend/CLI | blocked (environment: no open Pull Request) | Cause: no Open Pull Request. Equivalent supervised evidence: `TestRunWatchCheckoutMovedBeforeFinalPushInterruptsAndPushesNothing` and `TestFinalPushCheckoutMovedBoundaryPushesNothing` passed, observing the interruption report and zero pushes. Unblock with the disposable Pull Request, a safely controlled checkout, and supervised push-boundary observation. |
| 10 | `CheckoutMoved` is distinct from `Failed`, settles through compare-and-set, releases the Active Run lock, and remains retryable | Operator — backend/store | blocked (environment: no open Pull Request) | Cause: no Open Pull Request. Equivalent supervised evidence: `TestCompleteRunAcceptsCheckoutMovedAsTerminal`, the CLI interruption test, and Batch-boundary tests passed, confirming terminal state, completion timestamp, lock release, and no failed/resolved/invalid Review Issue mutation. Unblock with the disposable Pull Request and read-only Run Database observation. |
| 11 | Every review artifact commit in an exercised successful Run lands on the Pull Request's PR Head Branch, confirmed from Git | Operator — CLI/Git | blocked (environment: no open Pull Request) | Cause: no Open Pull Request. Equivalent supervised evidence: `TestRunWatchArtifactEvidenceInheritedWithoutCurrentHeadPolling` passed with real Git, confirming `feature/review` resolved to the Daemon review-artifact commit whose parent was the verified head and whose subject matched ADR-0036. Unblock with the disposable Pull Request and read-only Git ref inspection. |
| 12 | Roundfix never checks out, restores, or moves the working tree | Operator — CLI/Git | pass | Fresh reads after target-mismatch, fork, and detached probes showed the scratch clone still on `ma/other-review` at `609356ca…` with clean porcelain. The Run Worktree also stayed on its original Run Branch and source revision; only this report is untracked. |
| 13 | Fork Pull Requests remain unsupported and refuse clearly before branch comparison | Operator — CLI | pass | Built `roundfix fetch` with Base Repository `owner/base` and Head Repository `contributor/fork` exited 2, named both repositories and the out-of-scope fork cause, left its Artifact Directory absent, and did not move Git state. The focused fork test independently confirmed this is not `TargetMismatch`. |
| 14 | Review Issue fetching, batching, classification, and Branch Integrity Preflight rules remain unchanged | Operator — backend/CLI | pass | Full `make verify` passed. The focused CLI run included the real-Git Branch Integrity canary and existing operational review flows; all passed. Commit path audits show the corrective Task changed only Artifact Directory timing, detached temporary-log placement, their tests, and its Task file—no schema, classification, or Branch Integrity rule. |
| 15 | The canonical Roundfix Skill documents the public contract, its mirror is byte-identical, and authorized derived artifacts match canonical sources | Operator/maintainer — docs/tooling | pass | Lines 1042–1061 of both Skill copies document Preflight validation, exit 2, recovery, no side effects, `CheckoutMoved`, unsettled issues, and no working-tree movement. `skills-sync-check`, recursive parity, eight characterization tests, and full Verification passed. Task 03 changed no Go source. |

## Findings

None. Prior F-001 and F-002 are closed on the corrected build.

## Blocked and skipped

Rows 6–11 are `blocked (environment: no open Pull Request)`. The cause is the
explicit Pull Request fact in the QA prompt, not a failed discovery attempt.
Each row records fresh equivalent supervised evidence, its independent
confirmation where applicable, and the exact environment needed for a live
repeat. Under ADR-0080, these six environment blocks do not cap the verdict.

No row is finding-blocked, declared-blocked, skipped, or pending.

## Coverage

- Results: 9 pass, 0 fail, 6 environment-blocked with equivalent evidence, 0
  finding-blocked, 0 declared-blocked, 0 skipped, 0 pending.
- Findings by impact: 0 Blocks-Completion, 0 Data-Loss, 0 Trust-Damage, 0
  Friction, 0 Cosmetic.
- Covered: all five Core Features, all Goals and Success Metrics, task_04 and
  corrective task_05 requirements and acceptance criteria, all four
  Non-Goals, the Project Constraint/tooling audit, and backend/CLI/docs
  surfaces.
- Probes completed: explicit target input, fork head input, absent Artifact
  Directory, detached refusal cleanup, movement before all three write
  boundaries, matching-checkout canary, and working-tree persistence.

## Final verdict

Pass: every runnable row passed, every Pull Request journey blocked by the
environment has fresh equivalent supervised evidence and an explicit live
unblocking path, and no row is failed, finding-blocked, declared-blocked,
skipped, or pending.
