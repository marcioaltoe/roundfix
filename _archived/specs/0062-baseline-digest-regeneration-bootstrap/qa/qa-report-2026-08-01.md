---
spec: 0062-baseline-digest-regeneration-bootstrap
date: 2026-08-01
build: 76709c563468623d6dbc33f8d09e361e1645861f
status: closed
verdict: fail
rows_blocked_environment: 1
rows_blocked_finding: 1
surfaces: [backend, infra, docs]
---

# QA report — Baseline digest regeneration bootstrap

## Scope and environment

Full Spec gate against Run Worktree build
`76709c563468623d6dbc33f8d09e361e1645861f` on
`roundfix/run-run_20260801T153216Z_b0146286401f3654`. The intended actors are a
Roundfix maintainer regenerating Baseline artifacts and an operator relying on
strict catalog validation. The public entry points are `make baseline-digests`,
the built Roundfix CLI/catalog-loading paths, and
`docs/workflow/baseline-digest-regeneration.md`.

Pull Request fact: no Pull Request is open for the Spec target branch. Pull
Request observation is environment-blocked; this Run Worktree branch
is never pushed and is not a Pull Request lookup target.

The high-risk regeneration journey includes four behavior probes: a stale pin
on the first invocation, an immediate second invocation for idempotence, an
inconsistency that remains after rewriting, and a new manifest requirement
presented out of order. Scratch copies isolated deliberate catalog edits
from this QA checkout.

## Static gate

`rtk make verify` exited 0 on the assembled build: 2,953 Go tests passed in
24 packages; the four focused Skill tests passed; `roundfix skills check`
accepted all 14 required repository skills; and the production binary built
with `-buildvcs=false`. The build metadata recorded `76709c5-dirty` because
this in-progress QA Report already existed, not because implementation files
changed.

## Results

| # | Story / criterion / sweep | Actor and surface | Status | Evidence |
| - | --- | --- | --- | --- |
| Q-01 | Project Constraints account for identifier strategy, authentication and HTTP, active ADR obligations, and tooling authority in both active Spec artifacts, with operative `docs/agents/` sources. | QA auditor / docs | pass | `_prd.md` and `_techspec.md` independently account for all four constraints, cite `docs/agents/domain.md` or `docs/agents/agent-instructions.md`, bind ADR-0081/ADR-0085, and name the exact `Makefile` authorization record. ADR-0063 is inapplicable because no transport exists; ADR-0077's snapshot contract is satisfied. |
| Q-02 | The authorized `Makefile` tooling change has exact bounded scope, authorization and prerequisite chronology, separate Task commit shape, consequent-fix ordering, and sanctioned derived-pin provenance. | QA auditor / infra | pass | Authorization/Spec commit `e03fa07` is an ancestor of Task 04 commit `0775ad4`. `git diff-tree` proves `0775ad4` changes only `Makefile` and `task_04.md`; the other five Task commits contain no protected-tooling path. Task 04 is separate from its prerequisites, no consequent fix is folded before/into it, and no digest pin was hand-edited. |
| Q-03 | The repository Verification gate passes on the assembled build. | Maintainer / backend, infra, docs | pass | `rtk make verify` exited 0: 2,953 Go tests in 24 packages, 4 Skill tests, 14 required Skill contracts, and production build. |
| Q-04 | Outside regeneration, the characterization corpus remains deterministic and strict: all covered catalogs retain their expected diagnostic sets; the formatter mismatch code still fires; other codes are unchanged except the two guided message details. | Operator / backend | pass | Focused contract command passed 12 tests/subtests; a second characterization run passed and retained SHA-256 `d1165372…81a4`. A stale-pin scratch load failed with the unchanged mismatch code. Commit `a069e95` changes only three details under the two intended codes. [Command evidence](evidence/2026-08-01-baseline-digest-bootstrap/command-results.md#focused-contract-checks). |
| Q-05 | Regeneration mode is unreachable from public/ordinary loads and defers exactly `catalog.profile.formatter.goldenDigest.mismatch`; a non-allowlisted diagnostic remains fatal. | Operator / backend | pass | `TestCatalogRegenerationMode` and `TestRegenerationBreaksGoldenDigestCycle` passed. Caller inspection found the lowercase regeneration entry points only in `internal/baseline/`; the one-entry allowlist is asserted exactly. The ordinary scratch load remained strict. [Command evidence](evidence/2026-08-01-baseline-digest-bootstrap/command-results.md#focused-contract-checks). |
| Q-06 | After an ordinary Baseline module edit changes a generated guide and invalidates the formatter pin, one `make baseline-digests` invocation refreshes derived pins and succeeds. | Maintainer / infra | fail | F-01: two fresh archives reproduced the failure. The first target invocation rewrote the pin/catalog files, then exited 2 in `TestFormatterComposition`; only a second invocation exited 0. [Reproduction](evidence/2026-08-01-baseline-digest-bootstrap/command-results.md#one-invocation-regeneration-reproduction). |
| Q-07 | A second consecutive regeneration is idempotent and reports `"changed":false`; the target retains its step list, derived-path scan, structured success output, and final strict-validation stage. | Maintainer / infra | pass | Two consecutive target runs on an unmodified archive exited 0 with `{"ok":true,"changed":false}`. `rtk make -n baseline-digests` preserved the five update steps, scans, comparison, strict-validation stage, and success shape. [Command evidence](evidence/2026-08-01-baseline-digest-bootstrap/command-results.md#clean-tree-idempotence-and-target-shape). |
| Q-08 | If regeneration leaves the catalog inconsistent, the final strict stage fails in the existing structured shape instead of reporting success. | Maintainer / infra | pass | The focused ordinary strict load rejected the inconsistent scratch catalog. The current target recipe runs that same non-update `TestCatalogCompatibility` after regeneration/comparison and before success, preserves nonzero status, and binds the trap to `strict_validation_failed`, `strict-validation`, `retryable:false`; the full gate passed this implementation. [Command evidence](evidence/2026-08-01-baseline-digest-bootstrap/command-results.md#clean-tree-idempotence-and-target-shape). |
| Q-09 | Missing Source Baseline clause and rule rows keep their codes and subjects, name the missing identity, say the regenerator maintains but never creates rows, and require the human to add the row first. | Maintainer / backend, infra | pass | A public target run with `clause.core.qa-gate-new-clause` and no row exited 2, named that clause, and said the regenerator never creates rows. The clause/rule focused cases passed; source and golden diffs retain both codes/subjects. [Command evidence](evidence/2026-08-01-baseline-digest-bootstrap/command-results.md#missing-manifest-row). |
| Q-10 | The operating document can be followed without undocumented knowledge: it states the strict boundary, exact one-code allowlist, strict close, manual manifest-row step, recovery command, and ADR-0085 link. | Maintainer / docs | blocked (finding: F-01) | The document contains every required contract element and its ADR link resolves. Its missing-row branch matched the public target, but the documented stale-pin branch says one invocation closes successfully and is blocked by F-01. |
| Q-11 | Non-Goals remain excluded: regeneration does not create Source Baseline rows; no frozen-parity marker or ownership change ships; no global diagnostic relaxation, Verification weakening, retention change, or unrelated Baseline profile/module content change appears. | QA auditor / backend, infra, docs | pass | The missing-row scratch flow created no row. The Spec diff changes no module/profile/Source Baseline assets and adds no parity marker; strict default tests and the full gate pass, and no retention or Verification configuration changed. [Scope evidence](evidence/2026-08-01-baseline-digest-bootstrap/command-results.md#governance-and-scope). |
| Q-12 | Pull Request checks, approval, unresolved review threads, Merge-Ready evidence, and review-artifact ancestry are observable read-only when an Open Pull Request exists. | Maintainer / Pull Request | blocked (environment: no open Pull Request) | The QA prompt states no Pull Request is open on `ma/baseline-digest-regeneration-bootstrap`. Local ancestry and Verification are observed equivalents for build integrity, but no local evidence can prove GitHub approval, checks, unresolved threads, or Merge-Ready state. Unblock by opening the target-branch Pull Request and rerunning this row read-only. |

## Findings

### F-01 — First regeneration still fails after rewriting the stale pin

- Impact: Blocks-Completion.
- Actor and journey: Roundfix maintainer or autonomous Spec loop, Q-06, first
  `make baseline-digests` invocation after an existing module rule changes its
  generated guide and leaves the formatter digest stale.
- Expected: the one sanctioned invocation defers the stale pin, rewrites its
  derived artifacts, re-validates strictly, and exits 0.
- Actual: the formatter step rewrites the profile pin and normalized catalog,
  then `ApplyPlan` reloads the strict embedded catalog, sees the pre-run pin,
  and exits 1. The target exits 2 with `errorCode: regeneration_failed` and
  `stage: ./internal/baseline:TestFormatterComposition`. A second full target
  invocation exits 0 with `changed:false`, proving the first failed after
  performing the required rewrite.
- Reproduction: extract build `76709c5`; change the existing backend boundary
  rule and its generated formatter fixture without changing the formatter pin;
  confirm ordinary `TestCatalogCompatibility` reports
  `catalog.profile.formatter.goldenDigest.mismatch`; run
  `make baseline-digests` once. Repeat from a fresh archive for independent
  confirmation.
- Evidence: [command results](evidence/2026-08-01-baseline-digest-bootstrap/command-results.md#one-invocation-regeneration-reproduction).
- Affected rows: Q-06 fails; Q-10 is finding-blocked because its documented
  recovery promise cannot be followed successfully.

## Blocked and skipped

- Q-10 — blocked by finding F-01. Unblock by repairing the single-invocation
  formatter/update path, then rerun the stale-pin documentation journey and an
  adjacent clean-tree idempotence canary.
- Q-12 — blocked by environment: no open Pull Request. The prompt proves the
  absence. Open a Pull Request from `ma/baseline-digest-regeneration-bootstrap`
  and rerun approval, checks, unresolved-thread, Merge-Ready, and review-artifact
  ancestry observation. Local build evidence does not substitute for those
  GitHub-specific signals.

## Coverage

All three PRD goals, four Core Features, four Success Metrics, four Non-Goal
groups, 32 Task acceptance criteria, three declared surfaces, and the Pull
Request journey were mapped. The real application refutes the one-invocation
goal, Core Feature 2, Success Metric 1, and Task 03's assembled regeneration
criterion despite its focused fixture passing. The other 31 Task criteria have
current Task evidence and passed repository Verification or fresh flow evidence.

Final row counts: 9 pass, 1 fail, 1 blocked by environment, 1 blocked by a
finding, 0 skipped, 0 pending. Impact totals: 1 Blocks-Completion; 0 Data-Loss,
Trust-Damage, Friction, or Cosmetic findings. Probes executed: stale-pin first
invocation, immediate rerun, clean-state double invocation, strict invalid
load, and out-of-order missing manifest row. No frontend or TUI exists in this
Spec; viewport, browser, console, network, and accessibility sweeps are not
applicable.

## Final verdict

Fail — `make baseline-digests` still requires two invocations for the ordinary
module-edit/stale-pin journey; repair F-01 and rerun Q-06, Q-10, the full
repository gate, and the clean-tree idempotence canary before this Spec can
pass QA.
