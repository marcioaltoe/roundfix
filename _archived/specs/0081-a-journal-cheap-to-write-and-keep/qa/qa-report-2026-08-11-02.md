---
spec: 0081-a-journal-cheap-to-write-and-keep
date: 2026-08-11
build: faa69a5a1da188b0955e7ff41b1dbfc7f195ae5e
status: closed
verdict: pass
rows_blocked_environment: 0
rows_blocked_finding: 0
rows_blocked_declared: 0
surfaces: [backend, data, docs, cli]
---

# QA Report — A journal cheap to write and cheap to keep

## Scope and environment

Full rerun of the authored terminal `task_09` gate against Run Worktree build
`faa69a5a1da188b0955e7ff41b1dbfc7f195ae5e`. The Run Worktree is report-only:
no commit, push, Pull Request creation, customer credential, or operator Run
Database mutation is authorized. No Pull Request is open, and this Spec has no
Pull Request acceptance journey. Real data journeys use fresh temporary
Roundfix Homes and production Store, CLI, and TUI paths compiled from this
build.

The manifest names `task_09` as the terminal `type: qa` node. Its dependencies
`task_06`, `task_08`, `task_10`, and `task_11` are completed. The strict Spec
check passed with no findings; `SC-VOCABULARY-UNDOCUMENTED` was skipped because
the TechSpec has no Vocabulary Contract, so R06 retains that obligation.

## Static gate

- `rtk roundfix spec check 0081-a-journal-cheap-to-write-and-keep --strict`:
  passed; no findings; `SC-VOCABULARY-UNDOCUMENTED` skipped as recorded above.
- `rtk env GOCACHE=/private/tmp/roundfix-0081-qa02-gocache
  GOTMPDIR=/private/tmp/roundfix-0081-qa02-gotmp make verify`: environment-
  blocked after `go test -parallel 16 ./...` began because the sandbox denied
  `api.github.com`; the denial was not retried. The initial launch did not
  reach repository code because the disposable `GOTMPDIR` did not yet exist;
  after creating it, the command reached the network boundary above.
- Equivalent current-build evidence passed: `make fmt-check
  skills-sync-check skills-check build`; full `internal/store`, `internal/tui`,
  and `internal/runevent` package tests; every named consumer replay; and the
  focused CLI, data, concurrency, batching, retention, and TUI journeys below.
- `rtk env GOCACHE=/private/tmp/roundfix-0081-qa02-gocache
  GOTMPDIR=/private/tmp/roundfix-0081-qa02-gotmp make verify-docs`: passed,
  including the build, documentation contracts, measured sanctioned-ownership
  checks, corpus budget, and full Spec consistency sweep. The expected missing
  Vocabulary Contract skip remains.

## Mechanical findings

None.

## Results

| # | Story / criterion / sweep | Actor and surface | Status | Evidence |
| - | --- | --- | --- | --- |
| R01 | Run-start wall-clock is independent of journal size across empty and full-retention databases. | Session starting a Run; data/backend | pass | Two fresh observations held p50 at 41–51 us for both empty and 10,000-event databases; the former size curve was absent. Evidence: `docs/specs/0081-a-journal-cheap-to-write-and-keep/qa/evidence/2026-08-11-gate-02/evidence.md#r01--run-start-cost`. |
| R02 | Six parallel Runs at the pre-raise 5,000 ms `busy_timeout` complete with zero `SQLITE_BUSY`; cursors remain contiguous and publisher order survives cancellation. | Operator running six Runs; data/backend | pass | Six Runs persisted 1,536/1,536 events with zero `SQLITE_BUSY`; read-back and cancellation rehearsals passed. Evidence: `docs/specs/0081-a-journal-cheap-to-write-and-keep/qa/evidence/2026-08-11-gate-02/evidence.md#r02--parallel-runs`. |
| R03 | Event-write cost per 1,000 agent events improves against a pre-change same-boundary baseline because 128-event batching amortizes transactions, not because fewer events are produced. | Daemon appending agent output; data/backend | pass | Fresh three-sample medians fell from 164,756 to 36,848 us/1,000, a 77.6% reduction with all 1,536 events preserved. Evidence: `docs/specs/0081-a-journal-cheap-to-write-and-keep/qa/evidence/2026-08-11-gate-02/evidence.md#r03--same-boundary-write-cost`. |
| R04 | Bytes per Run are reported before and after, and retained Runs keep raw payload bytes under the unchanged terminal-only, age-based retention shape. | Maintainer deciding retention; data/docs | pass | Fresh bytes remained 61,440 / 2,162,688 / 21,135,360 at 0 / 1,000 / 10,000 events; raw bytes, terminal/age GC behavior, and exact ADR/lifecycle preservation passed. Evidence: `docs/specs/0081-a-journal-cheap-to-write-and-keep/qa/evidence/2026-08-11-gate-02/evidence.md#r04--bytes-and-retention-shape`. |
| R05 | Cockpit refresh cost tracks new events rather than total events while rendering, summary fallback, Follow Mode, and terminal-size states remain stable. | Operator watching a long Run; TUI through CLI | pass | The same two new events cost 2 headers + 1 full row after both 20- and 10,000-event backlogs; twelve adjacent TUI checks passed. Evidence: `docs/specs/0081-a-journal-cheap-to-write-and-keep/qa/evidence/2026-08-11-gate-02/evidence.md#r05--cockpit-refresh-and-tui-sweep`. |
| R06 | One recorded pre-change journal behaves identically through `events`, Attach, Cockpit rendering, reconcile replay detection, and `gc`; command/schema vocabulary stays unchanged. | Supervisor/operator; CLI/TUI/data/docs | pass | All five relocated digests matched; the full consumer replay passed in the current tree and again after moving the Spec to `_archived` in a disposable source copy. Evidence: `docs/specs/0081-a-journal-cheap-to-write-and-keep/qa/evidence/2026-08-11-gate-02/evidence.md#r06--archive-safe-pre-change-consumer-replay`. |

## Detailed row evidence

### R01

```yaml
inputs:
  - kind: repository_path
    ref: internal/store/journal_baseline_test.go
  - kind: repository_path
    ref: internal/store/journal.go
  - kind: repository_path
    ref: docs/specs/0081-a-journal-cheap-to-write-and-keep/baseline/*.md
  - kind: elapsed_time
    ref: current-build same-machine two-sample Run-start measurement
```

Entry point: production retention preflight over newly migrated temporary Run
Databases. Independent confirmation: a second fresh harness run. Persistence:
production Store reopen/read path. Probes: empty, 1,000 events, 10,000 events,
and repeat run. Status: **pass**. The two p50 sets were 51/113/41 us and
51/40/41 us across 0/1,000/10,000 events; the 10,000-event database was
21,135,360 bytes versus 61,440 bytes empty without a larger p50.

### R02

```yaml
inputs:
  - kind: repository_path
    ref: internal/store/journal_parallel_runs_test.go
  - kind: repository_path
    ref: internal/store/journal_writer.go
  - kind: repository_path
    ref: internal/store/writelock_*.go
  - kind: elapsed_time
    ref: current-build six-Run concurrency scenario
```

Entry point: six independent writer Stores against one newly migrated Run
Database. Independent confirmation: per-Run production-reader read-back.
Persistence: cursor/order checks after commit. Probes: synchronized publish,
exact 5,000 ms timeout, and cancellation while holding the writer lock. Status:
**pass**. Six Runs completed all 1,536 events with zero typed `SQLITE_BUSY`;
wall time was 59,111 us, and read-back plus cancellation passed.

### R03

```yaml
inputs:
  - kind: repository_path
    ref: docs/specs/0081-a-journal-cheap-to-write-and-keep/baseline/task10-measurement-*.txt
  - kind: repository_path
    ref: docs/specs/0081-a-journal-cheap-to-write-and-keep/baseline/2026-08-11-before.md
  - kind: repository_path
    ref: docs/specs/0081-a-journal-cheap-to-write-and-keep/baseline/2026-08-11-repaired.md
  - kind: repository_path
    ref: internal/store/journal_batch_test.go
  - kind: elapsed_time
    ref: same-machine three-sample before and after measurement
```

Entry point: production Journal Sink boundary on pre-change commit `a2a4c86b`
and the current build. Independent confirmation: three samples per build and
persisted event read-back. Persistence: publish-through-flush commit boundary.
Probes: equal harness core, equal 1,536-event count, count/linger/immediate/
explicit flush, ambiguous commits, and raw payload bytes. Status: **pass**.
Medians fell from 164,756 to 36,848 us per 1,000, a 77.6% reduction with no
event-count change; all batch canaries passed.

### R04

```yaml
inputs:
  - kind: repository_path
    ref: docs/findings/2026-08-06-three-gigabytes-of-event-journal-inside-the-retention-window.md
  - kind: repository_path
    ref: docs/specs/0081-a-journal-cheap-to-write-and-keep/baseline/*.md
  - kind: repository_path
    ref: docs/adr/0008-run-event-payload-stores-raw-producer-json.md
  - kind: repository_path
    ref: docs/adr/0033-the-run-event-journal-is-pruned-by-retention.md
```

Entry point: outside maintainer measurement plus current-build temporary
databases. Independent confirmation: exact ADR/lifecycle preservation diff.
Persistence: reopen after batch commit. Probes: 0/1,000/10,000 events, raw JSON,
zero retention, dry-run, actual prune, and no vacuum. Status: **pass**. Current
bytes match the recorded before; ten focused checks passed and the exact
`a2a4c86b..HEAD` diff of `.roundfixrc.yml`, module files, ADR-0008, ADR-0033,
and the lifecycle guide is empty. The outside source is the pre-Spec maintainer
Finding that recorded 2,915 MB of journal rows and 1,645,457 events.

### R05

```yaml
inputs:
  - kind: repository_path
    ref: internal/tui/cockpit.go
  - kind: repository_path
    ref: internal/tui/cockpit_forward_cursor_test.go
  - kind: repository_path
    ref: internal/tui/testdata/cockpit_snapshots/*
```

Entry point: CLI Live Run View Cockpit model over the production reader.
Independent confirmation: replay and snapshots. Persistence: idle repoll and
completed-Run Attach replay. Probes: 10,000-event backlog, missing payload,
responsive/degenerate sizes, keyboard, Follow Mode, and no color. Status:
**pass**. Both the 20- and 10,000-event cases cost 2 headers, 1 full row, and
67 payload bytes for the same two-event refresh; twelve adjacent TUI checks
passed.

### R06

```yaml
inputs:
  - kind: repository_path
    ref: internal/store/testdata/2026-08-11-prechange-roundfix.db
  - kind: repository_path
    ref: internal/store/testdata/2026-08-11-prechange-consumer-expectations.json
  - kind: repository_path
    ref: internal/store/testdata/2026-08-11-prechange-cockpit.golden
  - kind: repository_path
    ref: internal/store/journal_consumer_corpus_test.go
  - kind: repository_path
    ref: CONTEXT.md
```

Entry point: built current CLI and production consumer packages reading the
pre-change database from `internal/store/testdata`. Independent confirmation:
recorded digests, public help, and a second run after the Spec was moved to
`_archived` in a disposable `HEAD` archive. Persistence: database reopen per
consumer. Probes: `events`, Attach, full Cockpit, reconcile, fixed-cutoff GC,
archive relocation, and public vocabulary. Status: **pass**. All five digests
match; both current and post-archive corpus runs passed; no store source names
the active Spec slug; all four public help commands exited 0 with canonical
vocabulary.

## Findings

None. The previous report had no finding, and the Task 11 archive-safety seam
passes both in the current tree and with the Spec actually relocated.

## Blocked and skipped

No matrix row is blocked or skipped. The full `make verify` process is
environment-blocked at sandbox-denied `api.github.com` access inside the Go
test target; it was not retried. Equivalent current-build evidence passed for
every feature-owning package, named CLI/TUI consumer, formatting, skill sync
and check, build, and each R01–R06 journey. Pull Request journeys are not part
of this Spec's acceptance surface; the prompt confirms no Pull Request is open.

## Coverage

Closed with zero pending, failed, blocked, or skipped rows: 6 pass. R01–R06 map
the six Success Metrics in order and collectively cover all six User Stories
and Core Features 1–7. R04 carries outside evidence from the pre-Spec
maintainer Finding. R05 covers the required 10,000-event cost boundary plus
responsive, degenerate, keyboard, Follow Mode, Attach, snapshot,
summary-fallback, and no-color TUI states. R06 retains the skipped Vocabulary
Contract obligation and proves Task 11's fixture relocation after archive.

The exact `a2a4c86b..HEAD` non-goal checks show no `.roundfixrc.yml`, module,
ADR-0008, ADR-0033, or lifecycle-guide change. Schema, SQLite, stream shape,
scheduling, event count, GC compaction, and the one terminal-only age window
remain unchanged; no second store or payload shedding landed.

Rows blocked by environment: 0. Rows blocked by finding: 0. Rows blocked by
declaration: 0. Findings by impact: 0 Blocks-Completion, 0 Data-Loss, 0
Trust-Damage, 0 Friction, and 0 Cosmetic.

## Final verdict

**Pass.** All six Success Metric rows pass on build `faa69a5a`; there are no
failed, finding-blocked, declared-blocked, or skipped rows. The sandbox blocked
the network-reaching whole-suite process, but equivalent observed evidence
covers every feature path and every other `make verify` prerequisite, so the
environment boundary does not cap the verdict under ADR-0080.

## Mechanical skips

| Detector | Missing artifact |
| --- | --- |
| authorization bounded paths | tooling authorization |
| consequent-fix commit order | consequent-fix declaration |
