---
spec: 0026-model-fallback-guardrail
date: 2026-07-11
verdict: pass
surfaces:
  - cli
  - backend
  - docs
---

# QA Report: Model fallback guardrail

## Verdict

Pass. Every PRD user story and every Task acceptance criterion passed. The
full repository gate was green, the compiled CLI was exercised in an isolated
real Git repository with a controlled ACPX boundary, the resulting Run record
was inspected in the real SQLite Run Database, and the canonical and embedded
Roundfix Skills remained synchronized.

There is no frontend surface in this Spec, so browser validation and
screenshots do not apply.

## Environment

- Repository commit: `58074082c852e7cd7171da813862ed28f3608284`
- Branch: `roundfix/run-run_20260711T122548Z_15d5b443aab93f18`
- Platform: `Darwin 25.5.0 arm64`
- Go: `go1.26.5 darwin/arm64`
- Application: freshly built `bin/roundfix` from `rtk make verify`
- Runtime fixture: executable ACPX boundary that deterministically rejected
  `claude/broken-model/maximum`, accepted or rejected catalog candidates per
  scenario, and logged every disposable Session operation
- Application state: isolated real Git repository, isolated `HOME`, and the
  normal Roundfix SQLite Run Database and Run Worktree paths

The confirmed-flow fixture intentionally performed no Task edit. Its Run
therefore settled `Unresolved` after proving Run creation and effective
selection persistence; that terminal outcome is expected and is outside this
guardrail's contract.

## Verification gates

| Command | Result | Evidence |
| --- | --- | --- |
| `rtk make verify` | Pass | 1,070 tests passed in 19 packages; all 14 shipped Skills validated; binary built. |
| `rtk go test ./internal/agent` | Pass | 113 tests passed. |
| `rtk go test ./internal/cli` | Pass | 385 tests passed. |
| `rtk make skills-sync-check` | Pass | Exit 0 with zero canonical/embedded drift. |
| `rtk go run -buildvcs=false ./cmd/roundfix skills check` | Pass | All 14 shipped Skills passed validation. |

## Real application flows

| Flow | Result | Observed evidence |
| --- | --- | --- |
| Non-interactive proven fallback | Pass | `roundfix implement --spec 0001-qa-fallback --agent claude --no-input` exited 2. The stderr report named `broken-model/maximum`, proved `default/maximum`, and printed `Re-run: roundfix implement --spec 0001-qa-fallback --agent claude --no-input --model default --reasoning-effort maximum`. `roundfix runs list --state all` printed `No Runs found.` |
| Interactive decline | Pass | With interactive output forced through the real CLI, the command printed the failed selection, `default/maximum`, the token caveat, and exactly one `Use this Fallback Selection for this Run? [y/N]:` question. Answering `n` exited 2 and `runs list` still printed `No Runs found.` |
| Interactive confirmation | Pass | Answering `y` created `run_20260711T130043Z_ed72a006677f44d3`. A direct Run Database query returned `agent=claude`, `model=default`, `reasoning_effort=maximum`, and `spec_slug=0001-qa-fallback`. The Project Config hash remained `54c7c53f14e072e5709e3ada6bb8c3ce5a51a005`, and `git diff --exit-code -- .roundfixrc.yml` exited 0. |
| Model-managed fallback | Pass | When all explicit efforts were rejected, the report rendered `Default Reasoning Effort: model-managed` and printed the explicit recipe `--model default --reasoning-effort ""`; the command exited 2 before Run creation. |
| No functional candidate | Pass | With every candidate rejected, the command exited 2, preserved the original actionable selection error, printed `Fallback probe found no functional selection.`, listed models `default, opus, fable, sonnet, haiku` newest-first and efforts `maximum, high, default` highest-first, and created no Run. |
| Disposable probe Sessions | Pass | The ACPX invocation log showed the failed selection excluded from the fallback catalog pass, candidates attempted in catalog order, efforts attempted `maximum`, `high`, `default`, and `sessions close` issued for every disposable Session, including failed attempts. |

The buffer-captured CLI suite additionally proved stdout/stderr separation,
the exact resolve/watch/implement re-run recipes, execution of a printed
selection through successful preflight, detached-child prompt suppression,
empty-answer decline, effective selection in headers and Agent requests, and
unchanged exit codes.

## PRD user stories

| ID | Result | Evidence |
| --- | --- | --- |
| US1 — show and confirm a proven fallback interactively | Pass | Real interactive decline and confirmation flows showed one question only, the failed and fallback selections, and the token-cost caveat. Confirmation created a Run; decline created none. `TestRunReviewAgentCommandsConfirmFallbackAsEffectiveSelection`, `TestRunResolveDeclinesInteractiveFallbackWithoutCreatingRun`, and `TestRunImplementConfirmsModelManagedFallbackAsEffectiveSelection` passed. |
| US2 — deterministic non-interactive recovery | Pass | Real `--no-input` flow exited 2 with the deterministic stderr report and same-command explicit-flags recipe, with no Run. Resolve, watch, implement, non-interactive-stderr, and detached-child variants passed in `internal/cli`. |
| US3 — orchestrator relays the decision to the human | Pass | Both Skill copies state that an orchestrating agent must relay the failed selection, Fallback Selection, and caveat, wait for the human decision, and must not answer, choose, or re-run autonomously. Sync and validation gates passed. |
| US4 — effective fallback is visible and auditable | Pass | The real Run Database stored `claude/default/maximum`. Buffer-captured confirm tests passed for resolve, watch, and implement and assert the Run header, Run record, and Agent request all carry the fallback. |

## Core feature coverage

| ID | Result | Evidence |
| --- | --- | --- |
| CF1 — ordered fallback probe | Pass | Real ACPX logs and `TestACPXProbeFallbackUsesCandidateAndEffortOrder` prove same-runtime catalog order, highest-first effort order, failed-model exclusion, disposable Sessions, and cleanup. |
| CF2 — interactive confirmation | Pass | Real `n` and `y` flows prove the report, caveat, single question, decline failure, and confirmed one-Run selection. |
| CF3 — non-interactive report without standing authorization | Pass | Real no-input and model-managed flows prove exit 2 and explicit recipe output. Static search found no production `allow-fallback`, `allow_fallback`, fallback config key, or pre-authorization mechanism. |
| CF4 — effective selection persistence | Pass | Real SQLite row stored `default/maximum`; config hash and diff stayed unchanged. Header and Agent-request assertions passed in the CLI suite. |
| CF5 — guidance | Pass | README documents both flows; canonical and embedded Skills document the relay prohibition and recipe; CONTEXT.md defines Fallback Selection. Skill sync and validation passed. |
| CF6 — no-candidate reporting | Pass | Real no-candidate flow retained the original error and listed all probed model and effort candidates; `TestRunResolveSelectionFailureReportsProbedCandidatesWhenNoFallbackWorks` passed. |

## Task acceptance criteria

| Criterion | Result | Evidence |
| --- | --- | --- |
| task_01 AC1 — catalog models newest-first, efforts highest-first, failed model skipped, new disposable Session per model | Pass | Real ACPX logs plus `TestACPXProbeFallbackUsesCandidateAndEffortOrder`. |
| task_01 AC2 — return the first fully functional exact selection | Pass | Real probe selected `default/maximum` only after ensure and effort assignment succeeded; Run Database stored the exact pair after confirmation. |
| task_01 AC3 — functional model with no explicit effort returns model-managed | Pass | Real effort-rejection flow selected `default` with empty effort and rendered model-managed; `TestACPXProbeFallbackClassifiesModelManagedReasoning` passed. |
| task_01 AC4 — no functional selection reports none and closes every Session | Pass | Real no-candidate and model-managed logs contain matching `sessions close` operations; cleanup and cancellation tests passed. |
| task_02 AC1 — no-input or detached failure keeps exit code, reports fallback, creates no Run | Pass | Real no-input flow exited 2 and `runs list` found none; detached-child suppression passed in `TestRunResolveDetachedChildReportsFallbackWithDetachedRerun`. |
| task_02 AC2 — same-command explicit-flags recipe passes preflight in the fake | Pass | Real report printed the exact implement recipe; `TestRunReviewAgentCommandsReportProvenFallbackWithoutCreatingRun` executes the printed resolve selection and asserts successful preflight. |
| task_02 AC3 — model-managed display and explicit empty effort recipe | Pass | Real model-managed flow printed `model-managed` and `--reasoning-effort ""`; watch and implement model-managed tests passed. |
| task_02 AC4 — no-candidate result preserves actionable error and lists candidates | Pass | Real no-candidate report and the resolve no-fallback test both preserve recovery guidance and print ordered candidate lists. |
| task_03 AC1 — one prompt; yes creates a Run whose header and record carry the fallback | Pass | Real prompt occurred once and confirmation created a Run with the fallback pair. Resolve/watch/implement confirm tests assert header, Run record, and Agent request. |
| task_03 AC2 — no or empty answer creates no Run and exits with Preflight failure | Pass | Real `n` flow exited 2 with no Run; `TestRunResolveDeclinesInteractiveFallbackWithoutCreatingRun` covers both `no` and empty input. |
| task_03 AC3 — no-input or detach never prompts and keeps task_02 report | Pass | Real `--no-input` output contained no question; no-input and detached-child suppression tests passed. |
| task_03 AC4 — confirmed fallback does not change configuration | Pass | Real Project Config hash was identical before and after and the file had no diff; confirm tests assert byte-for-byte config stability. |
| task_04 AC1 — Skill relay rule and recipe, with zero embedded drift | Pass | Both Skill copies contain the prohibition and explicit-flags/model-managed recipe; sync check exited 0. |
| task_04 AC2 — README documents interactive and non-interactive flows | Pass | README Agent selection guidance names the prompt, caveat, confirmation behavior, exit 2 report, and explicit recipe. |
| task_04 AC3 — CONTEXT.md defines Fallback Selection | Pass | CONTEXT.md defines the ordered, proven, one-Run-only Fallback Selection and model-managed effort. |

## Non-goals and regression boundaries

| Boundary | Result | Evidence |
| --- | --- | --- |
| No autonomous or pre-authorized fallback | Pass | No production flag/config key exists; Skill guidance expressly prohibits autonomous confirmation. |
| No User Config, Project Config, or runtime-owned config persistence | Pass | Real Project Config remained unchanged; implementation and CLI tests assert config stability. Probe configuration is confined to disposable Sessions. |
| No cross-runtime probing | Pass | Real failure and all candidates remained on `claude`; fallback candidates derive from `ModelCatalog(runtime.ID)`. |
| No fallback for non-selection failures | Pass | `ProbeFallback` is reachable only after `errors.As` identifies `SelectionPreflightError`; other probe errors return directly. |
| No mid-Run model replacement | Pass | Static call-site inspection found fallback probing only in command selection preflight before Run creation. |
| No retry of the failed model | Pass | Real invocation logs show the failed `broken-model` probe once, followed by catalog candidates that exclude it. |
| No built-in default change | Pass | The fallback path consumes the existing Model Catalog and runtime reasoning vocabulary without mutating defaults or configuration. |

## Failures and blocked checks

None.
