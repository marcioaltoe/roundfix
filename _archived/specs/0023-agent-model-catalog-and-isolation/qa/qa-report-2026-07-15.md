---
spec: 0023-agent-model-catalog-and-isolation
date: 2026-07-15
verdict: pass
surfaces: [frontend, cli, data, infra, docs]
---

# QA Report — Agent Model Catalog and Isolation

## Summary

Retroactive QA gate (the spec shipped without a report; archive requires one). Exercised the real binary against live preflight flows in a scratch repository, plus real-Run header evidence from the 2026-07-14/15 dogfood Runs, on top of a green static gate (1235 tests, skills check, build). Every user story and core feature passes; one historical anomaly from 2026-07-14 is recorded as an observation with a follow-up pointer. Verdict: **pass**.

## Results

| #  | Criterion / user story                                                       | Verdict | Evidence |
| -- | ---------------------------------------------------------------------------- | ------- | -------- |
| S1 | Roundfix selects model/reasoning explicitly; runtime config cannot alter it  | pass    | Run headers always concrete: `Agent Model: gpt-5.5` + `Default Reasoning Effort: xhigh` (run_20260714T191017Z) and `gpt-5.6-sol` + `model-managed` (run_20260714T180906Z); never `auto`. ADR-0037 selection tests in suite. |
| S2 | Project Config per-runtime pins with overlay precedence                      | pass    | Scratch `.roundfixrc.yml` pinning `runtimes.codex.model: gpt-5.6-sol` probed `agent: ok` via doctor; repo pin surfaced in run_20260714T180906Z header. Overlay tests in suite. |
| S3 | Interactive Input presents the ordered Model Catalogs                        | pass    | Catalog definitions match the PRD exactly — 7 ordered Codex entries, 5 ordered Claude entries with the PRD descriptions; 4 catalog tests + 10 TUI Interactive Input tests pass (daemon-verified task evidence; no manual TTY session in this sandbox). |
| S4 | One-Run `--model`/`--reasoning-effort` overrides outrank config              | pass    | Runs 191017Z and 205328Z launched with `--model gpt-5.5 --reasoning-effort xhigh` over the repo's `gpt-5.6-sol` pin; headers show the override values; both Runs ended Clean. |
| S5 | Custom (non-catalog) model values pass through to the availability preflight | pass    | `totally-bogus-model-xyz` was not blocked by catalog validation — it reached the availability probe and failed there, proving pass-through; `gpt-5.6-sol` as a config value is accepted. |
| S6 | Unavailable model fails Preflight Validation before a Run exists, actionably | pass    | Live: `implement --model totally-bogus-model-xyz` → exit 2, "did not create a Run", names runtime/model/reasoning, advertised-model list, two recovery paths, and a confirmation-gated Fallback Selection with a re-run command (not auto-applied). See observation below. |
| S7 | Skill and usage docs ship the contract                                       | pass    | `roundfix skills check` green; README carries per-runtime config examples, both catalogs, Codex+Claude one-Run override examples, OpenCode explicit-config requirement, `defaults.model` migration, and recovery guidance (task_06 Result). |
| F2 | OpenCode without both values configured fails preflight                      | pass    | Live: `implement --agent opencode --no-input` → exit 2, "missing model; configure runtimes.opencode or pass invocation overrides". Claude's built-in reasoning default was later changed to empty by spec 0025 (ADR-0040) — superseded, not a defect. |
| F10 | Deprecated `defaults.model` warns exactly once on stderr and is ignored     | pass    | Live: scratch config with `defaults.model` → exactly one stderr line `config: defaults.model is deprecated and ignored; use runtimes.<runtime>.model`; command proceeded, exit 0. |
| F9 | Effective selection visible, never `auto`                                    | pass    | All observed run headers and doctor output show concrete values (`model-managed` for empty effort, per spec 0025 vocabulary). |
| NG | No silent fallback; no runtime auto-install                                  | pass    | Preflight failure suggests but never applies the Fallback Selection (exit 2 + explicit re-run command); recovery text instructs the user to update the runtime rather than doing it. |

## Failures and blocked items

None.

**Observation (not a current failure):** on 2026-07-14, `gpt-5.6-sol` passed this same preflight while codex-acp did not advertise it, creating run_20260714T180906Z whose batches all failed at prompt time with `agent/protocol error`. The advertised-model list is dynamic (by 2026-07-15 it includes `gpt-5.6-sol`, and an unadvertised model now fails preflight correctly), so the discrepancy no longer reproduces — but it shows probe-time and prompt-time model application can diverge. Recorded in `docs/findings/2026-07-14-implement-0027-launch-findings.md` §1 as a follow-up: the probe should apply the Agent Model the same way Batch prompts do.

## Environment

- Binary: `bin/roundfix` built from branch `ma/dogfood-findings-remediation` at the 0028 integration (commit 62bada6), macOS arm64.
- Static gate: `make verify` — 1235 tests in 19 packages, `roundfix skills check`, build; all green.
- Live preflight flows ran in a scratch git repository under the session scratchpad with a probe spec (`9001-qa-probe`), non-default branch, acpx 0.12.0, codex-acp 0.16.0, codex CLI 0.144.4, Node v26.5.0.
- Real-Run evidence from the 2026-07-14/15 dogfood console logs (runs 180906Z, 191017Z, 205328Z).
- Interactive Input (TUI) was not driven in a real TTY (headless sandbox); its criteria are credited from the catalog definitions and the passing TUI Interactive Input test suite, per the daemon-verified-evidence rule.
