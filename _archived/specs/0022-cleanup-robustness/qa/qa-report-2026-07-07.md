---
spec: 0022-cleanup-robustness
date: 2026-07-07
verdict: pass
surfaces: [cli, docs]
---

# QA Report — Cleanup Robustness

## Summary

The fix ran in production the same afternoon it shipped: five Implement Runs
(0021 × 3, 0020 × 2) ended Clean after it landed, every Run Worktree was
removed — including two kept-then-reaped worktrees from `-32603`-failed runs
— and the worktrees directory is empty. The warn-and-continue contract and
debris-removal are pinned by the task's injected-failure and dirty-worktree
tests. Docs rules landed in both owned skills. Verdict: pass.

## Results

| # | Criterion / user story | Verdict | Evidence |
| - | ---------------------- | ------- | -------- |
| 1 | Forced removal on every Roundfix-owned site | pass | Real: `ls ~/.roundfix/worktrees/roundfix-339f8dac/` → empty after five Clean cleanups and preflight-sweep reaps today; test: removal with untracked debris (task_01 Result) |
| 2 | Cleanup failure never demotes a Clean Run | pass | Injected-failure CLI test: byte-identical report and exit code, warning `roundfix: Run Worktree cleanup failed; kept <path>: <reason>`, journaled event (task_01 Result) |
| 3 | Pre-integration and kept-worktree behavior unchanged | pass | Existing kept-worktree suites pass unchanged; the `-32603`-failed 0020/0021 runs kept their worktrees for settle exactly as before |
| 4 | Settle recovery documented, skip-verify rejected | pass | roundfix SKILL.md Settle section (edit `## Verification`, re-run settle); the recovery was also exercised for real three times today (0021 task_01/02, 0020 task_02 settles) |
| 5 | write-tasks authoring rules | pass | write-tasks SKILL.md carries hermetic-verification and no-commit-criteria rules (task_02 Result); drift check green |
| 6 | Non-Goal — no verification/settle semantics change | pass | Settle still re-runs the task's Verification verbatim (observed in today's three real settles, including one legitimate verification failure on a flaky test that blocked settling until re-run) |

## Environment

Built binary at the branch head; real evidence from today's production runs
on this repository (the fix's own dogfood). Static gate `make verify` green
(978 tests, 19 packages).
