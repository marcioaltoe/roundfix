---
spec: 0058-npm-trusted-publishing-and-release-preflight
date: 2026-08-01
build: 4493addcc79c4e6d40a2b95471536082e9c5d1b3
status: closed
verdict: fail
rows_blocked_environment: 3
rows_blocked_finding: 0
surfaces: [infra, docs]
---

# QA report — npm Trusted Publishing and release preflight

## Scope and environment

Full Spec rerun on the Run Worktree branch
`roundfix/run-run_20260801T115720Z_8a465f97adf7c8d0`, build
`4493addcc79c4e6d40a2b95471536082e9c5d1b3`, on macOS in the managed
Roundfix QA sandbox. The actor is the Roundfix release maintainer. The public
surfaces are `.github/workflows/release.yml` and
`docs/user-guide/release-runbook.md`.

The prompt states that no Pull Request is open, so Pull Request journeys are
environment-blocked. The Run Worktree branch is never pushed and will not be
queried as though it had a Pull Request. Evidence for this run belongs under
`qa/evidence/2026-08-01-qa-01/`.

## Static gate

`rtk make verify` exited 0: 2,941 Go tests passed in 24 packages, the four
focused Skill/Baseline tests passed, `roundfix skills check` passed, and the
CLI build completed. See `qa/evidence/2026-08-01-qa-01/command-log.md`.

## Results

“Confirm” is the independent public or fresh-read confirmation; “persist” is
the applicable replay or durable-state check.

| # | Story / criterion / sweep | Actor · entry · surface | Observed and independently confirmed | Status |
| - | --- | --- | --- | --- |
| 01 | Seven completed Tasks with reproducible Results | QA operator · Spec artifacts · docs | Fresh task scan found seven `completed` statuses, acceptance evidence, and a complete chain; re-read by `remediation_scope_probe.rb` | pass |
| 02 | Both active Spec artifacts satisfy all Project Constraints | QA operator · PRD/TechSpec · docs | Both cover identifiers, authentication/HTTP, ADRs, and tooling with operative sources; compared with active ADRs and agent guides | pass |
| 03 | Protected-tooling authorization, scope, and chronology | QA operator · Git history · infra | Authorization commit `397227ff` precedes every tooling Task; exact `diff-tree` paths and ancestry pass through `4493add`; no fix is folded into a tooling Task | pass |
| 04 | Repository Verification | QA operator · `rtk make verify` · infra | Exit 0; 2,941 Go tests, four focused tests, Skill check, and build passed on this checkout | pass |
| 05 | Node/npm runtime, permissions, and registry contract | Maintainer · workflow · infra | YAML parse and runtime boundary reject 11.5.0, accept 11.5.1/12.0.0, and retain Node 24, both grants, and npm registry URL; replay passed | pass |
| 06 | OIDC-first publication and named identity failure | Maintainer · extracted publish step · infra | Six token-free first attempts observed; injected `ENEEDAUTH` produced named `identity:` handling; fake npm call log confirmed | pass |
| 07 | Open fallback retries once and records coordinate | Maintainer · extracted publish step · infra | Auth failure with window `1` made one token retry, completed, and recorded only the coordinate; replay passed | pass |
| 08 | Closed fallback stops without retry | Maintainer · extracted publish step · infra | Auth failure with window `0` emitted named `identity:`, made one call, produced no record, and stopped; replay passed | pass |
| 09 | Task 06 and prior QA-002: non-auth failure attribution | Diagnosing maintainer · extracted publish step · infra | Network timeout emitted `publish:` plus raw npm cause, exited 1, and never entered identity/token fallback; original reproduction now passes | pass |
| 10 | Token confidentiality, external switch, and summaries | Maintainer · publish step/summary · infra | Sentinel absent from output and summary; only one fallback-scoped token reference; configured switch and empty/non-empty summaries passed | pass |
| 11 | One derived six-coordinate Release Set reused for publication | Maintainer · workflow · infra | Manifest-derived five platforms plus launcher matched every preflight scenario and exact publish order; replay passed | pass |
| 12 | Used-version classification via `.versions` and `.time` | Maintainer · Publication Preflight · infra | Exact classifier returned `used` for both committed fixtures on fresh execution | pass |
| 13 | Cooldown and eligible classification | Maintainer · Publication Preflight · infra | Exact classifier returned `cooldown` with expiry and `eligible`; stubbed workflow independently confirmed | pass |
| 14 | Malformed, absent, HTTP, and transport failure handling | Maintainer · Publication Preflight · infra | Every case stopped and became `undetermined:` or the documented `registry:` absent state; none became eligible; replay passed | pass |
| 15 | Runtime output separates all five causes and names coordinates | Diagnosing maintainer · workflow output · infra | Fresh probes observed `registry:`, `undetermined:`, `runtime:`, `identity:`, and `publish:` on their applicable paths | pass |
| 16 | Preflight placement and mutation barrier | Maintainer/installer · workflow · infra | Structural audit proves Verify → preflight → build; used/cooldown/absent/undetermined cases stop before mutating stages | pass |
| 17 | Tag and dispatch trigger/validation contract | Maintainer · workflow trigger · infra | Tag, valid dispatch, invalid input, and mismatch paths used the correct target and shared checks; replay passed | pass |
| 18 | Publish-free dispatch rehearsal | Maintainer · `workflow_dispatch` · infra | All three mutating steps are push-only; dispatch executes validation and preflight scripts only; fresh YAML re-read confirmed persistence | pass |
| 19 | Tag authority, Verification, changelog, and GitHub Release-last contract | Maintainer · tag path · infra | Tag path passed; unchanged Verify, cross-compile body, changelog/GitHub Release body and order were compared with the pre-Spec base | pass |
| 20 | Package, asset, order, contents, and Upgrade compatibility | Maintainer/installer · manifests and gate · infra | Six names, five unique assets, platform-first calls, unchanged distribution paths, and release-asset compatibility gate passed | pass |
| 21 | Trusted-publisher setup for all six coordinates | Maintainer · runbook · docs | Manifest-derived six names all bind owner `marcioaltoe`, repository `roundfix`, workflow `release.yml`; publish-time validation is stated | pass |
| 22 | Fallback close evidence and repository removals | Maintainer · runbook · docs | Empty-record condition, external switch, variable/secret removal, and later fallback-branch removal all remain in one procedure | pass |
| 23 | Task 07 and prior QA-003: per-package registry shutdown | Maintainer · runbook · docs | All six coordinates, empty-record ordering, token-publication disablement, reopened-settings confirmation, and repository removals passed fresh assertions | pass |
| 24 | Runbook documents every emitted failure prefix with recovery | Diagnosing maintainer · failure vocabulary table · docs | Workflow emits five prefixes, but the table has four and omits `publish:`; `failure_vocabulary_probe.rb` exits 1 | fail |
| 25 | Canonical glossary terms | Maintainer · `CONTEXT.md` · docs | Publication Preflight and Release Set definitions match the workflow and PRD on fresh comparison | pass |
| 26 | Non-Goals remain excluded | QA operator · Git scope/workflow · infra/docs | No package/tag rename, Release Plan path, registry/channel change, unpublished reuse, or cooldown retry was introduced | pass |
| 27 | Real tagged OIDC release with empty fallback | Maintainer · live GitHub Actions/npm · infra | Local boundary replay covers set/order/fallback, but cannot prove GitHub OIDC exchange, six live bindings, or remote persistence | blocked (environment: workflow exists only in an unpushed Run Worktree and QA lacks authorization for an irreversible tag/release) |
| 28 | Live publish-free dispatch over all six coordinates | Maintainer · live GitHub Actions/npm · infra | Sandbox DNS blocked npm today; equivalent observed evidence exists from the byte-identical preflight in the 2026-07-31 approved full-access run, which reached npm, reported all six used coordinates, and stopped before mutation | blocked (environment: workflow is not on a remote branch and managed-sandbox DNS cannot resolve registry.npmjs.org) |
| 29 | Pull Request checks, review, ancestry, and Merge-Ready journey | Reviewer · Open Pull Request · infra | Prompt confirms no Open Pull Request; no equivalent review evidence exists and the Run Worktree branch was not queried | blocked (environment: no open Pull Request) |

High-risk probes selected for this rerun: npm floor boundary, malformed body,
HTTP and transport failures, already-used version, single-version unpublish,
full-package cooldown, invalid and mismatched dispatch input, fallback enabled,
fallback closed, non-authentication publish failure, token-sentinel leakage, and
multi-coordinate failure accumulation. These probe the irreversible release
boundary and failure attribution. Browser viewport, session, locale, and
accessibility probes do not apply because the Spec declares no frontend or TUI.

## Findings

### QA-004 — Runbook omits the workflow's `publish:` failure prefix

- Impact: Trust-Damage.
- Actor and step: diagnosing maintainer, `Publish to npm` fails for a
  non-authentication reason and emits `publish:`.
- Expected: Task 05's acceptance contract says the runbook documents every
  workflow failure prefix with its emitting stage, meaning, and first recovery
  step. Story 3 requires failure attribution to lead recovery to the right
  place.
- Actual: the workflow emits `identity:`, `publish:`, `registry:`, `runtime:`,
  and `undetermined:`. The runbook table documents every prefix except
  `publish:`.
- Reproduction: run `rtk ruby
  docs/specs/0058-npm-trusted-publishing-and-release-preflight/qa/evidence/2026-08-01-qa-01/failure_vocabulary_probe.rb`.
- Evidence: `failure_vocabulary_probe.rb`, `command-log.md`,
  `.github/workflows/release.yml`, and
  `docs/user-guide/release-runbook.md`.
- Affected rows: 24.

Prior findings QA-001, QA-002, and QA-003 are closed on this build: the amended
PRD matches npm's identity-preflight limit, non-auth publication failures are
evidence-attributed without fallback, and the registry-side token shutdown is
documented for all six coordinates.

## Blocked and skipped

- Row 27 needs an integrated workflow, all six npm trusted-publisher bindings,
  an authorized fresh tag, and a successful run whose fallback record is
  empty. Local substitution cannot prove GitHub's OIDC exchange.
- Row 28 needs the workflow on a remote branch for `workflow_dispatch` and a
  network-capable runner. Equivalent read-only evidence exists because the
  current preflight is byte-identical to the prior approved full-access live
  run and today's local execution still proved the six-row failure barrier.
- Row 29 needs an Open Pull Request on
  `ma/npm-trusted-publishing-and-release-preflight`; none exists, so approval,
  checks, unresolved threads, review-artifact ancestry, and Merge-Ready
  evidence are unavailable.
- Skipped rows: 0. Pending rows: 0.

## Coverage

All 29 planned rows are terminal: 25 pass, 1 fail, 3 blocked by environment,
0 blocked by findings, 0 skipped, and 0 pending. The matrix covers all four
user stories, all 43 Task acceptance criteria, Project Constraints,
protected-tooling history, repository Verification, Non-Goals, both live
success metrics, and the mandatory Pull Request fact row.

Findings by impact: 0 Blocks-Completion, 0 Data-Loss, 1 Trust-Damage,
0 Friction, 0 Cosmetic. `rows_blocked_environment: 3` and
`rows_blocked_finding: 0` match the matrix.

## Final verdict

Fail: document the `publish:` prefix with its emitting stage, meaning, and
first recovery step, then rerun the gate; a later passing gate still needs
equivalent evidence for the real OIDC release and Pull Request journeys.
