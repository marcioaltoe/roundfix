# Agent instructions

This setup-owned guide defines the portable baseline. Repository authors own
project-specific extensions outside setup markers and may add stricter rules.
The selected repository Verification is {{verification.gate}}. Agent-created
branches **MUST** use the selected {{branch.prefix}} prefix.

{{artifact.rules}}
