{
  "schemaVersion": "setup-context-driven/module-v3",
  "id": "context-workflow",
  "version": 15,
  "kind": "workflow",
  "title": "CONTEXT-driven workflow",
  "dependsOn": [
    "core"
  ],
  "conflictsWith": [],
  "requiredSkills": [
    "archive-spec",
    "brainstorming",
    "business-analyst",
    "council",
    "domain-modeling",
    "grill-with-docs",
    "grilling",
    "implement-spec",
    "implement-task",
    "knowledge-workspace",
    "qa-gate",
    "setup-context-driven",
    "the-fool",
    "write-idea",
    "write-prd",
    "write-tasks",
    "write-techspec"
  ],
  "skillDispatch": [
    {
      "skill": "archive-spec",
      "triggers": [
        {
          "id": "trigger.context-workflow.archive-spec",
          "when": "Archiving a completed and QA-passed Spec."
        }
      ]
    },
    {
      "skill": "brainstorming",
      "triggers": [
        {
          "id": "trigger.context-workflow.brainstorming",
          "when": "Starting creative feature or behavior design before implementation."
        }
      ]
    },
    {
      "skill": "business-analyst",
      "triggers": [
        {
          "id": "trigger.context-workflow.business-analyst",
          "when": "Evaluating product viability, options, KPIs, or business trade-offs."
        }
      ]
    },
    {
      "skill": "council",
      "triggers": [
        {
          "id": "trigger.context-workflow.council",
          "when": "Debating a high-impact product or architecture decision through multiple advisors."
        }
      ]
    },
    {
      "skill": "domain-modeling",
      "triggers": [
        {
          "id": "trigger.context-workflow.domain-modeling",
          "when": "Defining or changing domain vocabulary, ownership, or bounded-context relationships."
        }
      ]
    },
    {
      "skill": "grill-with-docs",
      "triggers": [
        {
          "id": "trigger.context-workflow.grill-with-docs",
          "when": "Stress-testing a plan or decision against repository documents."
        }
      ]
    },
    {
      "skill": "grilling",
      "triggers": [
        {
          "id": "trigger.context-workflow.grilling",
          "when": "Stress-testing an idea or plan through direct questions."
        }
      ]
    },
    {
      "skill": "implement-spec",
      "triggers": [
        {
          "id": "trigger.context-workflow.implement-spec",
          "when": "Executing every pending Task in a Spec Task Graph."
        },
        {
          "id": "trigger.autonomous-work.implement-spec",
          "when": "Delegating a complete autonomous Spec Task Graph."
        }
      ]
    },
    {
      "skill": "implement-task",
      "triggers": [
        {
          "id": "trigger.context-workflow.implement-task",
          "when": "Executing exactly one assigned Spec Task."
        },
        {
          "id": "trigger.autonomous-work.implement-task",
          "when": "Delegating one autonomous Task to an ACP Runtime."
        }
      ]
    },
    {
      "skill": "knowledge-workspace",
      "triggers": [
        {
          "id": "trigger.context-workflow.knowledge-workspace",
          "when": "Consulting the configured knowledge workspace for relevant prior context."
        },
        {
          "id": "trigger.context-workflow.knowledge-workspace-secondbrain-authoring",
          "when": "Consulting the Secondbrain before authoring an Idea, PRD, or TechSpec."
        }
      ]
    },
    {
      "skill": "qa-gate",
      "triggers": [
        {
          "id": "trigger.context-workflow.qa-gate",
          "when": "Running final Spec QA after implementation Tasks complete."
        }
      ]
    },
    {
      "skill": "setup-context-driven",
      "triggers": [
        {
          "id": "trigger.context-workflow.setup-context-driven",
          "when": "Auditing or applying the Context-Driven Baseline for first adoption, or refreshing an adopted repository with `roundfix baseline update`."
        }
      ]
    },
    {
      "skill": "the-fool",
      "triggers": [
        {
          "id": "trigger.context-workflow.the-fool",
          "when": "Challenging a proposal through a pre-mortem, red-team, or evidence audit."
        }
      ]
    },
    {
      "skill": "write-idea",
      "triggers": [
        {
          "id": "trigger.context-workflow.write-idea",
          "when": "Expanding a product-level idea into a researched opportunity artifact."
        }
      ]
    },
    {
      "skill": "write-prd",
      "triggers": [
        {
          "id": "trigger.context-workflow.write-prd",
          "when": "Defining product requirements and user outcomes."
        }
      ]
    },
    {
      "skill": "write-tasks",
      "triggers": [
        {
          "id": "trigger.context-workflow.write-tasks",
          "when": "Decomposing an approved Spec into a dependency-ordered Task Graph."
        }
      ]
    },
    {
      "skill": "write-techspec",
      "triggers": [
        {
          "id": "trigger.context-workflow.write-techspec",
          "when": "Settling implementation architecture and technical contracts."
        }
      ]
    }
  ],
  "requiredDecisions": [
    "domain.layout"
  ],
  "rootBlocks": [
    {
      "id": "root.context-workflow",
      "version": 2,
      "template": "template.root.context-workflow",
      "rules": [],
      "references": [
        {
          "id": "reference.root.context.domain",
          "token": "reference.domain",
          "ownership": "setup",
          "managedId": "guide.domain"
        },
        {
          "id": "reference.root.context.docs-layout",
          "token": "reference.docs-layout",
          "ownership": "setup",
          "managedId": "guide.docs-layout"
        }
      ]
    }
  ],
  "supportingGuides": [
    {
      "id": "guide.docs-layout",
      "version": 10,
      "path": "docs/agents/docs-layout.md",
      "template": "template.guide.docs-layout",
      "rules": [
        "rule.context.docs-layout"
      ],
      "references": []
    },
    {
      "id": "guide.domain",
      "version": 6,
      "path": "docs/agents/domain.md",
      "template": "template.guide.domain",
      "rules": [
        "rule.context.domain-docs"
      ],
      "references": []
    }
  ],
  "rules": [
    {
      "id": "rule.context.domain-docs",
      "version": 5,
      "coverage": [
        "coverage.context-workflow",
        "coverage.research"
      ],
      "clauses": [
        {
          "id": "clause.context.read-domain-contract",
          "enforcement": "mandatory",
          "guidance": "Read the repository's selected domain context and relevant accepted ADRs before naming domain concepts or changing behavior. Flag conflicts instead of silently overriding repository decisions."
        },
        {
          "id": "clause.domain.canonical-language",
          "enforcement": "mandatory",
          "guidance": "Use the repository's canonical domain terms in code names, tests, user-facing copy, Specs, and delivery notes. Call out a missing term instead of inventing a competing synonym."
        },
        {
          "id": "clause.domain.layout-decision",
          "enforcement": "mandatory",
          "guidance": "Follow the repository's declared single-context or multi-context layout. Setup can require that decision but cannot infer bounded contexts from directory names."
        },
        {
          "id": "clause.domain.glossary-currency",
          "enforcement": "mandatory",
          "guidance": "At the close of a Spec, feature, refactor, or fix, check whether the work introduced, changed, or retired a term the glossary should carry, and update the domain context through `domain-modeling` when it did. The check is what is obliged; the update follows only from a check that found something, and neither waits for human interaction. Reach for `grilling` only when a term is ambiguous enough to need sharpening before it is written down."
        }
      ]
    },
    {
      "id": "rule.context.docs-layout",
      "version": 11,
      "coverage": [
        "coverage.context-workflow"
      ],
      "clauses": [
        {
          "id": "clause.context.adr-01-template",
          "enforcement": "mandatory",
          "guidance": "When creating a new ADR, prepend this repository-owned lifecycle overlay to the body contract in `.agents/skills/domain-modeling/ADR-FORMAT.md`:\n\n```markdown\n---\nstatus: proposed # proposed | accepted | rejected | deprecated | superseded\ncreated_at: YYYY-MM-DDTHH:MM:SSZ\nupdated_at: YYYY-MM-DDTHH:MM:SSZ\ndeprecated_at: null # null or YYYY-MM-DDTHH:MM:SSZ\nsuperseded_by: null # null or ADR-NNNN\n---\n\n# <Short title of the decision>\n\n<One to three sentences describing the context, decision, and reason.>\n```"
        },
        {
          "id": "clause.context.adr-02-active-status",
          "enforcement": "mandatory",
          "guidance": "Only `accepted` is active. Treat `proposed`, `rejected`, `deprecated`, and `superseded` ADRs as inactive."
        },
        {
          "id": "clause.context.adr-03-legacy-compatibility",
          "enforcement": "mandatory",
          "guidance": "Treat a legacy ADR without lifecycle frontmatter as active unless its body explicitly marks it inactive. Do not rewrite existing ADRs solely to adopt lifecycle metadata."
        },
        {
          "id": "clause.context.docs-one-job-per-directory",
          "enforcement": "mandatory",
          "guidance": "Give each documentation directory one job: `docs/_inbox/` for raw notes, `docs/adr/` for decisions, `docs/agents/` for agent guidance, `docs/design/` for design artifacts, `docs/backlog/` for dated, typed intent not yet committed to a Spec, `docs/findings/` for dated investigations, `docs/handoffs/` for session continuity, `docs/references/` for external pointers and durable project reference documents, and `docs/user-guide/` for human documentation. Preserve repository-authored extensions outside setup markers."
        },
        {
          "id": "clause.context.docs-upstream-flow",
          "enforcement": "mandatory",
          "guidance": "Durable knowledge flows upstream only: the project glossary (`CONTEXT.md`) and the agent guides reference accepted ADRs and never reference `docs/specs/` or `docs/findings/` content. Findings are dated reports that become Specs, not reference material; a document meant as durable project reference belongs in `docs/references/`."
        },
        {
          "id": "clause.context.backlog-01-operational-contract",
          "enforcement": "mandatory",
          "guidance": "Name backlog entries `YYYY-MM-DD-<kebab-slug>.md`. Use this complete copyable Backlog Operational Contract:\n\n```markdown\n---\ntype: feat # feat | fix | perf | refactor\nstatus: open # open | promoted | declined\ncreated: YYYY-MM-DD\nspec: null # Spec slug when status: promoted\nreason: null # required when status: declined\n---\n```\n\nFor `type: feat`, use:\n\n```markdown\n# <Title — the intent in one line>\n\n## Opportunity\n\n<What could exist and for whom.>\n\n## Value\n\n<Why it would matter; the hypothesis.>\n\n## Shape\n\n<The rough form of a solution, explicitly non-binding.>\n```\n\nFor `type: fix`, use:\n\n```markdown\n# <Title — the defect in one line>\n\n## Symptom\n\n<What misbehaves, as a user or operator sees it.>\n\n## Where\n\n<The surface, command, or package, as known.>\n\n## Expected\n\n<The behavior that should replace it.>\n\n## Evidence\n\n<A finding link when one exists; `none yet` is honest.>\n```\n\nFor `type: perf`, use:\n\n```markdown\n# <Title — the cost in one line>\n\n## Slow\n\n<What is slow, for whom, and in which operation.>\n\n## Measured\n\n<The number that says so and how it was measured.>\n\n## Target\n\n<The number that would settle it.>\n```\n\nFor `type: refactor`, use:\n\n```markdown\n# <Title — the tangle in one line>\n\n## Tangled\n\n<What resists change, and where it is duplicated or coupled.>\n\n## Cost\n\n<What it makes slow, risky, or wrong to touch.>\n\n## Shape\n\n<The structure that would replace it, explicitly non-binding.>\n```\n\nKeep `open` entries in `docs/backlog/`. When a Spec adopts an entry, set `status: promoted` and `spec` to that Spec's slug, then move the entry to `docs/specs/<slug>/references/`; git history remains the discovery trail. Set `status: declined` only with a non-null `reason`. The current type set is open: a new type must be a Conventional Commits type that expresses intent. Adding a type is a contract change that requires a corpus re-record, never an informal addition. Use `refactor` as the canonical token, never an abbreviation."
        },
        {
          "id": "clause.context.backlog-02-finding-boundary",
          "enforcement": "mandatory",
          "guidance": "Keep evidence and intent distinct in both directions: a finding records what happened and is never a commitment; it is evidence-backed, immutable history. A backlog entry records what to do next and is never evidence. A finding may spawn a backlog entry; a backlog entry needs no finding. A `feat` entry is upstream raw material that the spec pipeline may consume, never the `write-idea` artifact itself."
        },
        {
          "id": "clause.context.findings-01-frontmatter",
          "enforcement": "mandatory",
          "guidance": "Use this complete copyable Findings Operational Contract:\n\n```markdown\n---\nstatus: pending # pending | partial | deferred | done\ncreated_at: YYYY-MM-DD\nupdated_at: YYYY-MM-DD\n---\n\n# <Area> — <short title> (YYYY-MM-DD)\n\n<Two to four sentences describing the session or investigation, the attempted outcome, and links to adjacent evidence.>\n\n## 1. <Finding title — symptom, not hypothesis>\n\n- Symptom / evidence: <observed behavior, command output, identifiers, and paths needed to reproduce>\n- Root cause: <proven cause, or `unknown` with what was ruled out>\n- Action / suggestion: <fix, mitigation, or route to a Spec, direct change, or upstream report>\n\n## 2. <Next finding>\n\n## What worked — keep\n\n<Optional evidence about behavior worth preserving.>\n\n## Addendum — YYYY-MM-DD — <short title>\n\n<Append new evidence, root-cause proof, status context, or routing links without rewriting the original observation.>\n```"
        },
        {
          "id": "clause.context.findings-08-rollup",
          "enforcement": "mandatory",
          "guidance": "A Rollup is a Finding of `kind: rollup` that consolidates related Findings. It lives beside active Findings under `docs/findings/`, shares their lifecycle contract, and declares a non-empty `members:` list of Finding basenames. Every member must resolve under `docs/findings/` or `docs/findings/_archived/`. Use this extension:\n\n```yaml\nkind: rollup\nmembers:\n  - YYYY-MM-DD-<finding-slug>.md\n```"
        },
        {
          "id": "clause.context.findings-09-archive",
          "enforcement": "mandatory",
          "guidance": "Use `docs/findings/_archived/` as the archival home for Findings. Every archived Finding requires an `absorbed_by:` license that resolves to an active Rollup basename or a Spec slug. Use this extension:\n\n```yaml\nabsorbed_by: <rollup-basename-or-spec-slug>\n```"
        },
        {
          "id": "clause.context.findings-10-live-work-health",
          "enforcement": "mandatory",
          "guidance": "Read a findings directory holding only live work as `health`, not loss: Rollups and `docs/findings/_archived/` hold what was learned. Do not restore absorbed Findings merely to repopulate the active directory."
        },
        {
          "id": "clause.context.findings-11-rollup-closure",
          "enforcement": "mandatory",
          "guidance": "When a Rollup has `no open members`, review it as a candidate for its own closure and close it through the existing Finding lifecycle contract when its work is settled."
        },
        {
          "id": "clause.context.findings-02-pending",
          "enforcement": "mandatory",
          "guidance": "Use `pending` when the finding is new and has no implementation Spec."
        },
        {
          "id": "clause.context.findings-03-partial",
          "enforcement": "mandatory",
          "guidance": "Use `partial` when a linked Spec covers only the selected implementation scope. Record the reason the remaining observations are unnecessary and link the covering Spec."
        },
        {
          "id": "clause.context.findings-04-deferred",
          "enforcement": "mandatory",
          "guidance": "Use `deferred` only when the finding will not be implemented. Record the reason for deferral."
        },
        {
          "id": "clause.context.findings-05-done",
          "enforcement": "mandatory",
          "guidance": "Set `status: done` as soon as the Spec is created and linked; do not wait for its Tasks, QA, archive, or release."
        },
        {
          "id": "clause.context.findings-06-append-evidence",
          "enforcement": "mandatory",
          "guidance": "Treat findings as immutable history: append evidence and routing links as dated addenda instead of rewriting the original observations."
        },
        {
          "id": "clause.context.findings-07-update-timestamp",
          "enforcement": "mandatory",
          "guidance": "Update `updated_at` whenever status changes or an evidence addendum is appended; keep `created_at` as the document creation date."
        },
        {
          "id": "clause.context.inbox-01-triage",
          "enforcement": "mandatory",
          "guidance": "Triage resolves one pending Inbox Entry into exactly one Finding, one Backlog Entry, or one recorded discard. Preserve the ADR-0092 boundary: evidence never becomes intent without a human choice. A minted Finding or Backlog Entry must cite the Inbox Entry's provenance."
        },
        {
          "id": "clause.context.inbox-02-fleet-flow",
          "enforcement": "mandatory",
          "guidance": "When a Finding's lifecycle closes, mint each typed Backlog Entry that its recorded actions call for, while preserving the boundary between evidence and intent. Where a fleet observation is captured before it reaches this repository is the Secondbrain guidance's concern, not this one's."
        }
      ]
    }
  ],
  "repositoryExtensions": []
}
