{
  "schemaVersion": "setup-context-driven/module-v3",
  "id": "autonomous-work",
  "version": 7,
  "kind": "workflow",
  "title": "Autonomous Supervisor and ACP Runtime model",
  "dependsOn": [
    "context-workflow"
  ],
  "conflictsWith": [],
  "requiredSkills": [],
  "skillDispatch": [],
  "requiredDecisions": [
    "autonomous.enabled",
    "runtime.backend",
    "runtime.design"
  ],
  "rootBlocks": [
    {
      "id": "root.autonomous-work",
      "version": 2,
      "template": "template.root.autonomous-work",
      "rules": [],
      "references": [
        {
          "id": "reference.root.autonomous-work",
          "token": "reference.autonomous-work",
          "ownership": "setup",
          "managedId": "guide.autonomous-work"
        }
      ]
    }
  ],
  "supportingGuides": [
    {
      "id": "guide.autonomous-work",
      "version": 7,
      "path": "docs/agents/autonomous-work.md",
      "template": "template.guide.autonomous-work",
      "rules": [
        "rule.autonomous.supervisor-delegates",
        "rule.autonomous.verification-gate",
        "rule.autonomous.loop"
      ],
      "references": []
    }
  ],
  "rules": [
    {
      "id": "rule.autonomous.supervisor-delegates",
      "version": 3,
      "coverage": [
        "coverage.autonomous-work"
      ],
      "clauses": [
        {
          "id": "clause.autonomous.delegate-through-roundfix",
          "enforcement": "mandatory",
          "guidance": "The Supervisor authors Specs, starts and monitors Runs, and orchestrates outcomes. Delegate implementation to the selected ACP Runtime through a Roundfix Run."
        },
        {
          "id": "clause.autonomous.supervisor-prohibition",
          "enforcement": "prohibited",
          "guidance": "The Supervisor must not write feature code or tests."
        }
      ]
    },
    {
      "id": "rule.autonomous.verification-gate",
      "version": 3,
      "coverage": [
        "coverage.autonomous-work",
        "coverage.verification"
      ],
      "clauses": [
        {
          "id": "clause.autonomous.daemon-verifies-task",
          "enforcement": "mandatory",
          "guidance": "The Daemon runs each Task's declared Verification verbatim. A Task can settle `completed` only after that Verification passes; failed diagnostics return to the same Agent Session for the bounded retry policy."
        }
      ]
    },
    {
      "id": "rule.autonomous.loop",
      "version": 1,
      "coverage": [
        "coverage.autonomous-work"
      ],
      "clauses": [
        {
          "id": "clause.autonomous.loop-01-qa-once",
          "enforcement": "mandatory",
          "guidance": "Request the QA gate once per Spec, after the Task Graph closes and every surface its acceptance observes exists and is clean. When a Spec's acceptance observes Pull Request review surfaces, open the Pull Request and drive its review to Clean before requesting QA: a gate that must observe a Pull Request cannot pass while none exists, and cannot accept one whose review is unprocessed. For those Specs, the order is implement, open the Pull Request, watch until Clean, and request QA once. Specs whose acceptance does not observe Pull Request review surfaces can request QA after the Task Graph closes without waiting for a Pull Request or review. In either case, wait for the terminal QA verdict and merge only when it is `pass`; stop the workflow on any non-`pass` verdict. Run the Implement Command without the QA flag while any Task is pending or a corrective Task is planned. The Daemon already gates QA on every Task reaching completed, so the cost is not the flag but re-requesting the gate after each corrective Task, which turns discovery into a serial chain of full cycles. Close several findings together; more than two corrective Tasks generated by QA findings means the decomposition needs re-examining. Rebuild any binary the gate exercises before requesting it, or the gate reports defects the running artifact does not contain."
        },
        {
          "id": "clause.autonomous.loop-02-autonomous-decomposition",
          "enforcement": "mandatory",
          "guidance": "Decide the Task Graph autonomously from a PRD and TechSpec that passed their own gates, and report the breakdown instead of blocking on approval. Stop and ask only when the next decision would change authority, architecture, or blast radius: protected tooling the Spec does not authorize with exact bounded files, a Task whose acceptance needs a person or a credential the repository cannot hold, a decomposition that only works by contradicting the TechSpec, an irreversible action inside one slice, or a verdict needing supervised judgement."
        },
        {
          "id": "clause.autonomous.loop-03-specify-the-hazard",
          "enforcement": "mandatory",
          "guidance": "State the hazard in a requirement, not only the objective: name where data comes from and who reads its destination. A time cap must say whether it cancels or abandons; a destination must say whether a secret may reach it. Requirements whose hazard is implicit are the largest source of rework."
        },
        {
          "id": "clause.autonomous.loop-04-verify-the-class",
          "enforcement": "mandatory",
          "guidance": "Write Verification that proves the class, not the case. When a finding is that nothing may do X, sweep every place that could, and put the detector where the risk is: a Task changing a contract with an external surface probes that surface rather than only compiling and unit-testing."
        },
        {
          "id": "clause.autonomous.loop-05-clean-is-not-evidence",
          "enforcement": "mandatory",
          "guidance": "Treat a terminal Clean and a resolved status as claims, not evidence. Read the diff after a Run reports Clean, and confirm a review actually happened before treating its silence as proof."
        },
        {
          "id": "clause.autonomous.loop-06-branch-hygiene",
          "enforcement": "mandatory",
          "guidance": "Never commit to a Run's branch or move the checkout away from it while that Run is Active. Roundfix integrates a Run Branch with a fast-forward merge and review Runs operate in the user checkout, so either action strands the Run or sends its commits to the wrong branch. Parallel work belongs in its own clone or waits."
        }
      ]
    }
  ],
  "repositoryExtensions": []
}
