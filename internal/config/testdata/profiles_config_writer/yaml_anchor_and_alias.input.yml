notify:
  command: &bootstrap_command |
    make bootstrap
worktree:
  bootstrap: *bootstrap_command
profiles:
  backend:
    preferred:
      runtime: codex
      model: anchor-primary
      reasoning_effort: high
    fallbacks:
      - runtime: claude
        model: anchor-fallback
        reasoning_effort: xhigh
