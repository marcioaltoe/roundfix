notify:
    command: |
        printf 'Run finished\n'
        printf 'Inspect the report before continuing\n'
profiles:
    backend:
        preferred:
            runtime: codex
            model: characterization-backend
            reasoning_effort: high
        fallbacks:
            - runtime: claude
              model: characterization-fallback
              reasoning_effort: xhigh
