# Merge this file over the organization settings instead of replacing them.
# Without it CodeRabbit uses only the highest-priority source, so every key the
# organization sets and this file omits falls back to the schema default rather
# than to the organization value.
inheritance: true

reviews:
  auto_review:
    enabled: false
    description_keyword: "coderabbit:review"
  # high_level_summary and sequence_diagrams are owned by the organization
  # settings; declaring them here would override that choice.
  profile: "assertive"
  path_filters:
    # =====================================================================
    # Files excluded from review, grouped by type
    # =====================================================================

    # ----- Agent / skill / tooling configs (NOT code) -----
    - "!.agents/**"
    - "!.claude/**"
    - "!.compozy/**"
    - "!.cursor/**"

    # ----- IDE / editor / OS noise -----
    - "!.vscode/**"
    - "!.idea/**"
    - "!**/.DS_Store"
    - "!**/Thumbs.db"
    - "!**/*.swp"
    - "!**/*.sw?"

    # ----- TypeScript / JavaScript build output -----
    - "!**/build/**"
    - "!**/dist/**"
    - "!**/dist-ssr/**"
    - "!**/node_modules/**"
    - "!**/out/**"
    - "!**/.next/**"
    - "!**/.turbo/**"
    - "!**/.cache/**"
    - "!**/.parcel-cache/**"
    - "!**/.vercel/**"
    - "!**/.mastra/**"
    - "!**/storybook-static/**"
    - "!**/*.tsbuildinfo"

    # ----- Auto-generated code (Drizzle, TanStack Router, OpenAPI, TS) -----
    - "!**/routeTree.gen.ts"
    - "!**/generated/**"
    - "!**/*generated*.ts"
    - "!**/database/migrations/**"
    - "!**/drizzle/**"
    - "!**/openapi.json"
    - "!**/openapi.yaml"
    - "!**/next-env.d.ts"

    # ----- Go -----
    - "!**/vendor/**"

    # ----- Rust -----
    - "!**/target/debug/**"
    - "!**/target/release/**"

    # ----- Python -----
    - "!**/.venv/**"
    - "!**/venv/**"
    - "!**/*.egg-info/**"
    - "!**/__pycache__/**"
    - "!**/.pytest_cache/**"
    - "!**/.mypy_cache/**"
    - "!**/.ruff_cache/**"
    - "!**/.tox/**"

    # ----- Lock / package manager noise -----
    - "!**/bun.lockb"
    - "!**/.pnp.*"
    - "!**/.yarn/**"

    # ----- Test coverage output -----
    - "!**/coverage/**"
    - "!**/.nyc_output/**"

    # ----- Test fixtures -----
    - "!**/fixtures/**"
    - "!**/__fixtures__/**"

    # ----- Logs / temp / runtime -----
    - "!**/logs/**"
    - "!**/*.log"
    - "!**/*storybook.log"
    - "!**/.tmp/**"
    - "!**/.eslintcache"

    # ----- Local databases / runtime data -----
    - "!**/*.db"
    - "!**/*.db-shm"
    - "!**/*.db-wal"
    - "!**/.storage/**"

    # ----- Secrets / env -----
    - "!**/.env"
    - "!**/.env.local"
    - "!**/.env.*.local"
    - "!**/*.local"
    - "!**/*.pem"

    # ----- One-off scripts -----
    - "!scripts/**"

    # =====================================================================
    # Files reviewed (explicit allowlist), grouped by type
    # =====================================================================

    # ----- TypeScript / JavaScript -----
    - "**/*.ts"
    - "**/*.tsx"
    - "**/*.js"
    - "**/*.jsx"
    - "**/*.mjs"
    - "**/*.cjs"

    # ----- Styles / markup -----
    - "**/*.css"
    - "**/*.html"

    # ----- SQL -----
    - "**/*.sql"

    # ----- Go -----
    - "**/*.go"
    - "**/go.mod"
    - "go.mod"

    # ----- Rust -----
    - "**/*.rs"
    - "**/Cargo.toml"
    - "Cargo.toml"

    # ----- Python -----
    - "**/*.py"
    - "**/*.pyi"
    - "**/pyproject.toml"
    - "pyproject.toml"
    - "**/requirements*.txt"
    - "requirements*.txt"

    # ----- Node / workspace manifests -----
    - "**/package.json"
    - "package.json"
    - "**/tsconfig*.json"
    - "tsconfig*.json"
    - "turbo.json"

    # ----- Build / container -----
    - "Makefile"
    - "**/Makefile"
    - "Dockerfile"
    - "**/Dockerfile"
    - "docker-compose.yml"
    - "docker-compose.*.yml"

    # ----- CI -----
    - ".github/**/*"

    # ----- Documentation (Specs, ADRs, glossary, READMEs) -----
    - "docs/**/*.md"
    - "CONTEXT.md"
    - "**/README.md"
    - "README.md"

    # ----- Repository configuration -----
    - ".coderabbit.yaml"

knowledge_base:
  code_guidelines:
    enabled: true
    filePatterns:
      # ===================================================================
      # Documents CodeRabbit reads as project rules, grouped by authority
      # ===================================================================

      # ----- Agent instructions (root pointers + the guides they route to) -----
      - "AGENTS.md"
      - "**/AGENTS.md"
      - "CLAUDE.md"
      - "**/CLAUDE.md"
      - "docs/agents/**/*.md"
      - "**/docs/agents/**/*.md"

      # ----- Architecture decisions -----
      - "docs/adr/**/*.md"
      - "**/docs/adr/**/*.md"

      # ----- Domain glossary -----
      - "CONTEXT.md"
      - "**/CONTEXT.md"

      # ----- Design contract -----
      - "DESIGN.md"
      - "**/DESIGN.md"

      # ----- Database contract -----
      - "DATABASE.md"
      - "**/DATABASE.md"

      # ----- Skill definitions (read as rules, never reviewed — excluded in path_filters) -----
      - ".agents/skills/**/*.md"
      - ".claude/skills/**/*.md"
